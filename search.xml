<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>C基础：01.基本数据类型和指针入门</title>
    <url>/01.C%E5%9F%BA%E7%A1%80-%E5%9F%BA%E6%9C%AC%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E5%92%8C%E6%8C%87%E9%92%88%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="1-基本数据类型"><a href="#1-基本数据类型" class="headerlink" title="1. 基本数据类型"></a>1. 基本数据类型</h1><p><code>&amp;i</code>地址的占位符为<code>p</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 基本数据类型</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> i = <span class="number">100</span>;    <span class="comment">// 占位符d</span></span><br><span class="line">    <span class="type">double</span> d = <span class="number">200</span>; <span class="comment">// 占位符lf</span></span><br><span class="line">    <span class="type">float</span> f = <span class="number">200</span>;  <span class="comment">// 占位符f</span></span><br><span class="line">    <span class="type">long</span> l = <span class="number">100</span>;  <span class="comment">// 占位符ld</span></span><br><span class="line">    <span class="type">short</span> s = <span class="number">100</span>; <span class="comment">// 占位符d</span></span><br><span class="line">    <span class="type">char</span> c = <span class="string">&#x27;D&#x27;</span>;  <span class="comment">// 占位符c</span></span><br><span class="line">    <span class="comment">// 字符串 char*</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 输出打印 ， 要用占位符</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i 的值是：%d\n&quot;</span>,i);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;d 的值是：%lf\n&quot;</span>,d);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;f 的值是：%f\n&quot;</span>,f);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;l 的值是：%ld\n&quot;</span>,l);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;s 的值是：%d\n&quot;</span>,s);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;c 的值是：%c\n&quot;</span>,c);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;c 的地址是：%p\n&quot;</span>,&amp;c);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">i 的值是：100</span><br><span class="line">d 的值是：200.000000</span><br><span class="line">f 的值是：200.000000</span><br><span class="line">l 的值是：100</span><br><span class="line">s 的值是：100</span><br><span class="line">c 的值是：D</span><br></pre></td></tr></table></figure>
<h1 id="2-变量的地址：任何的数据（变量）都会有地址（住址门牌号）"><a href="#2-变量的地址：任何的数据（变量）都会有地址（住址门牌号）" class="headerlink" title="2. 变量的地址：任何的数据（变量）都会有地址（住址门牌号）"></a>2. 变量的地址：任何的数据（变量）都会有地址（住址门牌号）</h1><ul>
<li>int占4个字节</li>
<li>double占8个字节</li>
<li>char占一个字节</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> i = <span class="number">100</span>;</span><br><span class="line">    <span class="comment">// &amp;i &amp; 代表变量的地址</span></span><br><span class="line">    <span class="comment">// 通过 debug 的调试，我们可以根据地址去查下该地址的数据 int 4 字节 sizeof() 获取数据类型所占的字节数</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i 的地址是：%p&quot;</span>,&amp;i); <span class="comment">// i 的地址是：000000000060FE1C</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;int 数据类型所占的字节数：%d\n&quot;</span>,<span class="built_in">sizeof</span>(<span class="type">int</span>));<span class="comment">// int 数据类型所占的字节数：4</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;double 数据类型所占的字节数：%d\n&quot;</span>,<span class="built_in">sizeof</span>(<span class="type">double</span>));<span class="comment">// double 数据类型所占的字节数：8</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;char 数据类型所占的字节数：%d\n&quot;</span>,<span class="built_in">sizeof</span>(<span class="type">char</span>));<span class="comment">// char 数据类型所占的字节数：1</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-指针变量和获取地址的值"><a href="#3-指针变量和获取地址的值" class="headerlink" title="3. 指针变量和获取地址的值"></a>3. 指针变量和获取地址的值</h1><p>通过 <code>*</code> 例如<code>*(&amp;i)</code>获取地址的值</p>
<p><code>指针（地址）变量</code>：指针存放（指向）的就是变量的地址。注意<strong>指针变量是一个变量</strong></p>
<font color=red>**一个指针的大小始终是32位系统占4个字节，64位系统占8字节**</font>

<p>指针为什么要有类型：</p>
<ol>
<li>为了地址增量的方便：例如double型的地址增量为8，而int型指针的地址增量为4</li>
<li>为了类型安全：帮助编译器发现你是否用了错误的类型</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> i = <span class="number">100</span>;</span><br><span class="line">    <span class="type">double</span> d = <span class="number">200</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i的值是：%d\n&quot;</span>,i); <span class="comment">// i的值是：100</span></span><br><span class="line">    <span class="comment">// 通过 * 获取地址的值</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i的值是：%d\n&quot;</span>,*(&amp;i)); <span class="comment">// i的值是：100</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//指针（地址）变量 指针存放（指向）的就是变量的地址</span></span><br><span class="line">    <span class="comment">// int* 代表的是 int 类型的指针 （地址）</span></span><br><span class="line">    <span class="type">int</span>* p = &amp;i;</span><br><span class="line">    <span class="comment">// double* p1 = &amp;d;</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i的值是：%d\n&quot;</span>,*p); <span class="comment">// i的值是：100</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-通过指针修改值"><a href="#4-通过指针修改值" class="headerlink" title="4. 通过指针修改值"></a>4. 通过指针修改值</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="type">int</span> i = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line">	<span class="type">int</span>* p = &amp;i;</span><br><span class="line"></span><br><span class="line">	i = <span class="number">200</span>;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;i的值是：%d\n&quot;</span>,i); <span class="comment">// i的值是：200</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 还有一种方式：获取指针的值进行间接的修改</span></span><br><span class="line">	*p = <span class="number">300</span>;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;i的值是：%d&quot;</span>, i); <span class="comment">// i的值是：300</span></span><br><span class="line"></span><br><span class="line">	getchar();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-题目：写个方法对两个值进行交换"><a href="#5-题目：写个方法对两个值进行交换" class="headerlink" title="5. 题目：写个方法对两个值进行交换"></a>5. 题目：写个方法对两个值进行交换</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">change</span><span class="params">(<span class="type">int</span> i)</span>&#123;<span class="comment">// 这个 i 是另外一个</span></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;change 中i的地址是：%p\n&quot;</span>,&amp;i);</span><br><span class="line">	i = <span class="number">200</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 通过地址来修改</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">change</span><span class="params">(<span class="type">int</span>* p)</span>&#123;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;change中i的地址是：%p\n&quot;</span>,p);</span><br><span class="line">	*p = <span class="number">200</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">change</span><span class="params">(<span class="type">int</span>* a,<span class="type">int</span>* b)</span>&#123; <span class="comment">// 2 面试题：对 a,b 两个值进行交换，但是不开辟额外的内存（不能用中间变量）</span></span><br><span class="line">    <span class="comment">// 大家在这里面写</span></span><br><span class="line">    <span class="type">int</span> temp = *a;</span><br><span class="line"></span><br><span class="line">    *a = *b;<span class="comment">// a的值 = b 的值</span></span><br><span class="line"></span><br><span class="line">    *b = temp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="6-题目：不开辟新的内存对两个值进行交换"><a href="#6-题目：不开辟新的内存对两个值进行交换" class="headerlink" title="6. 题目：不开辟新的内存对两个值进行交换"></a>6. 题目：不开辟新的内存对两个值进行交换</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">100</span>;</span><br><span class="line">    <span class="type">int</span> b = <span class="number">200</span>;</span><br><span class="line">    <span class="comment">// 交换</span></span><br><span class="line">    <span class="comment">// change(&amp;a,&amp;b);</span></span><br><span class="line"></span><br><span class="line">    a = a + b;<span class="comment">// a = 300</span></span><br><span class="line">    <span class="comment">//a = a - b;</span></span><br><span class="line">    b = a - b;<span class="comment">// b = 200</span></span><br><span class="line">    a = a - b;<span class="comment">// a = 100</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;交换后ab的值分别是：%d,%d&quot;</span>,a,b); <span class="comment">// 交换后ab的值分别是：200,100</span></span><br><span class="line"></span><br><span class="line">    getchar();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="7-指针变量的加深理解"><a href="#7-指针变量的加深理解" class="headerlink" title="7.指针变量的加深理解"></a>7.指针变量的加深理解</h1><p>指针变量只是一个<code>变量</code>它指向了地址值，<strong>修改指针变量并不会修改指向对象的地址值</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	<span class="type">int</span> a = <span class="number">100</span>;</span><br><span class="line">	<span class="type">int</span> b = <span class="number">200</span>;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// a = b 把b变量的值复制给a</span></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;a,b的地址分别是：%p,%p\n&quot;</span>,&amp;a,&amp;b); <span class="comment">// a,b的地址分别是：000000000060FE0C,000000000060FE08</span></span><br><span class="line"></span><br><span class="line">	<span class="type">int</span>* p1 = &amp;a;<span class="comment">// p1 是一个变量而已</span></span><br><span class="line">	<span class="type">int</span>* p2 = &amp;b;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// p1 = p2 是什么意思，有点类似于 a = b ；p1 和 p2 只是一个变量而已；</span></span><br><span class="line">	p1 = p2;<span class="comment">// 只是p1 的变量指向改成了指向b的地址，并没有改动 a 的地址</span></span><br><span class="line"></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;a,b的地址分别是：%p,%p\n&quot;</span>, &amp;a, &amp;b); <span class="comment">// a,b的地址分别是：000000000060FE0C,000000000060FE08</span></span><br><span class="line">	<span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>C知识</category>
      </categories>
      <tags>
        <tag>C学习</tag>
        <tag>C基础</tag>
      </tags>
  </entry>
  <entry>
    <title>C基础：03.随机数和内存开辟</title>
    <url>/03.C%E5%9F%BA%E7%A1%80-%E9%9A%8F%E6%9C%BA%E6%95%B0%E5%92%8C%E5%86%85%E5%AD%98%E5%BC%80%E8%BE%9F/</url>
    <content><![CDATA[<h1 id="1-生成随机数"><a href="#1-生成随机数" class="headerlink" title="1.生成随机数"></a>1.生成随机数</h1><ol>
<li>初始化随机发生器：<strong><code>srand(NULL)</code></strong>　<strong><code>srand((unsigned)time(NULL))</code></strong></li>
<li>调用函数：<strong><code>rand()</code></strong></li>
</ol>
<p>问题：<code>srand(NULL)</code>初始化发生器时每次随机数都是一样的。  </p>
<p>解决：<br>　　<br>　　初始化随机发生器时调用<strong><code>srand((unsigned)time(NULL))</code></strong>，每次发生器都是当前时间，所以随机数不会再一样了。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main()&#123;</span><br><span class="line">    // 可以随机但是每次运行，随机数都一致，解决的方法就是初始化发生器</span><br><span class="line">    // 初始化随机发生器，每次发生器都是当前时间，啰嗦，实用场景，在 c 层生成秘钥（安全）</span><br><span class="line">    srand((unsigned)time(NULL));</span><br><span class="line">    int i = 0;</span><br><span class="line">    for(; i&lt; 10; i++)</span><br><span class="line">        printf(&quot;%d\n&quot;,rand() % 100);</span><br><span class="line">    getchar();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-重点，内存开辟"><a href="#2-重点，内存开辟" class="headerlink" title="2.重点，内存开辟"></a>2.重点，内存开辟</h1><ul>
<li>c 有分区，四驱模型，栈，堆</li>
<li>栈(<strong>是一块连续的内存的区域</strong>)：占用内存空间最大值 2M ，开辟内存的方式是静态内存开辟 int arr[10<em>1024</em>1024]，方法结束会自动回收</li>
<li>堆(是用链表来存储的空闲内存地址的，<strong>是不连续的</strong>)：占用内存空间最大值 80% ，开辟内存的方式是动态内存开辟 ，不会自动回收必须手动回收（内存不再使用的时候一定要手动回收）</li>
</ul>
<p>静态内存开辟</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">staticlloc</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> arr[<span class="number">5</span>]; <span class="comment">// 静态开辟 0.8M</span></span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(; i &lt; <span class="number">5</span>; i++)&#123;</span><br><span class="line">        arr[i] = i;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d,%p\n&quot;</span>,*(arr+i),arr + i);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 静态内存开辟</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// Stack overflow 栈溢出， 如何判断一块内存中有没有写入数据， 判 NULL? ，判越界</span></span><br><span class="line">    <span class="comment">// int arr[10*1024*1024]; // 占用的内存是多少？ 大小？ 10M数组大小， int 占用4字节，40M空间</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// c 有分区，四驱模型，栈，堆</span></span><br><span class="line">    <span class="comment">// 栈： 占用内存空间最大值 2M, 开辟内存的方式是静态内存开辟 int arr[10*1024*1024], 方法结束会自动回收</span></span><br><span class="line">    <span class="comment">// 堆： 占用内存空间最大值 80%, 开内存的方式是动态内存开辟 不会自动回收必须手动回收（内存不再使用的时候一定要手动回收）</span></span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">true</span>)&#123;</span><br><span class="line">        <span class="built_in">Sleep</span>(<span class="number">100</span>);</span><br><span class="line">        <span class="built_in">staticlloc</span>();<span class="comment">// 不会内存溢出</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-动态内存开辟"><a href="#3-动态内存开辟" class="headerlink" title="3.动态内存开辟"></a>3.动态内存开辟</h1><ol>
<li>开辟内存：<code>malloc</code>　<strong><code>int* arr = (int*)malloc(sizeof(int)*num);</code></strong></li>
<li>释放内存： <code>free</code>　　<strong><code>free(arr)</code></strong> 不要反复去释放，那块地址一旦被释放，就有可能会被新的应用占用，不要去再次释放。</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">dynamiclloc</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span>* arr = (<span class="type">int</span>*)<span class="built_in">malloc</span>(<span class="number">1</span> * <span class="number">1024</span> * <span class="number">1024</span> * <span class="built_in">sizeof</span>(<span class="type">int</span>)); <span class="comment">// 40M</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 第二个重要的方法,手动释放 free , 释放arr 这一块内存</span></span><br><span class="line">    <span class="built_in">free</span>(arr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//动态内存开辟</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// int arr[1 * 1024 * 1024];</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 操作</span></span><br><span class="line">    <span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">        <span class="built_in">Sleep</span>(<span class="number">100</span>);</span><br><span class="line">        <span class="built_in">dynamiclloc</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/images/03动态内存开辟.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 假设我们现在需要接收用户所输入的数，然后进行排序</span></span><br><span class="line">    <span class="type">int</span> num;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;请输入数的个数：&quot;</span>);</span><br><span class="line">    <span class="comment">// 获取用户输入的值</span></span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>,&amp;num);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义一个数组来存放用户的数据</span></span><br><span class="line">    <span class="comment">// int arr[num]; 静态开辟，内存大小不能改变。</span></span><br><span class="line">    <span class="type">int</span>* arr = (<span class="type">int</span>*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="type">int</span>)*num);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// for 循环接收</span></span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> print_num;</span><br><span class="line">    <span class="keyword">for</span>(; i &lt; num; i++)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;请输入第%d个的值：&quot;</span>,i);</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>,&amp;print_num);</span><br><span class="line">        arr[i] = print_num; <span class="comment">// arr[i] = *(arr+i)</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d,%p\n&quot;</span>,*(arr+i),arr + i);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 排序 堆排序（4个小时），快排序</span></span><br><span class="line">    <span class="built_in">free</span>(arr);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">请输入数的个数：3</span><br><span class="line">请输入第0个的值：2</span><br><span class="line">2,0000000000AE1440</span><br><span class="line">请输入第1个的值：5</span><br><span class="line">5,0000000000AE1444</span><br><span class="line">请输入第2个的值：10</span><br><span class="line">10,0000000000AE1448</span><br></pre></td></tr></table></figure>
<h1 id="4-改变内存空间大小"><a href="#4-改变内存空间大小" class="headerlink" title="4.改变内存空间大小"></a>4.改变内存空间大小</h1><ul>
<li><code>realloc</code>：<strong><code>int* new_arr = (int*)realloc(arr, sizeof(int)*(num+new_num));</code></strong>    </li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 改变内存空间大小</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> num;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;请输入数的个数：&quot;</span>);</span><br><span class="line">    <span class="comment">// 获取用户输入的值</span></span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>,&amp;num);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 5 个值</span></span><br><span class="line">    <span class="type">int</span>* arr = (<span class="type">int</span>*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="type">int</span>)*num);</span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(; i &lt; num; i++)&#123;</span><br><span class="line">        arr[i] = i; <span class="comment">// arr[i] == *(arr + i);</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;开辟内存的指针：%p\n&quot;</span>,arr);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 再加点内存</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;请输入新增的个数：&quot;</span>);</span><br><span class="line">    <span class="comment">// 获取用户新输入的值</span></span><br><span class="line">    <span class="type">int</span> new_num;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>,&amp;new_num); <span class="comment">// 5</span></span><br><span class="line">    <span class="comment">// 原来那块内存的指针，总的大小（原来的大小+新的大小）</span></span><br><span class="line">    <span class="type">int</span>* new_arr = (<span class="type">int</span>*)<span class="built_in">realloc</span>(arr,<span class="built_in">sizeof</span>(<span class="type">int</span>)*(num+new_num));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(new_arr)&#123; <span class="comment">// = if(new_arr != NULL)</span></span><br><span class="line">        i = num;</span><br><span class="line">        <span class="keyword">for</span>(; i &lt; (num+new_num); i++)&#123;</span><br><span class="line">            arr[i] = i; <span class="comment">// arr[i] == *(arr + i);</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;新开辟内存的指针：%p\n&quot;</span>,new_arr);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 打印数组</span></span><br><span class="line">        <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(; i &lt; (num + new_num); i++)&#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;%d,%p\n&quot;</span>,*(arr + i),arr + i);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(new_arr)&#123;</span><br><span class="line">        <span class="comment">// 如果成功了，只需要释放新的地址就行</span></span><br><span class="line">        <span class="built_in">free</span>(new_arr);</span><br><span class="line">        new_arr = <span class="literal">NULL</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="comment">// 如果没成功，释放原来的内存</span></span><br><span class="line">        <span class="built_in">free</span>(arr);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 不要反复去释放，那块地址一旦被释放，就有可能会被新的应用占用，不要去再次释放。</span></span><br><span class="line">    <span class="comment">// free(new_arr);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 工作 C/C++ 精通</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">请输入数的个数：2</span><br><span class="line">开辟内存的指针：0000000000B81440</span><br><span class="line">请输入新增的个数：1</span><br><span class="line">新开辟内存的指针：0000000000B81440</span><br><span class="line">0,0000000000B81440</span><br><span class="line">1,0000000000B81444</span><br><span class="line">2,0000000000B81448</span><br></pre></td></tr></table></figure>
<p><img src="/images/03realloc.png" alt=""></p>
]]></content>
      <categories>
        <category>C知识</category>
      </categories>
      <tags>
        <tag>C学习</tag>
        <tag>C基础</tag>
      </tags>
  </entry>
  <entry>
    <title>C基础：04.字符串常见操作和指针练习</title>
    <url>/04.C%E5%9F%BA%E7%A1%80-%E5%AD%97%E7%AC%A6%E4%B8%B2%E5%B8%B8%E8%A7%81%E6%93%8D%E4%BD%9C%E5%92%8C%E6%8C%87%E9%92%88%E7%BB%83%E4%B9%A0/</url>
    <content><![CDATA[<h1 id="1-字符串定义"><a href="#1-字符串定义" class="headerlink" title="1.字符串定义"></a>1.字符串定义</h1><p><strong>字符串以 <code>\0</code> 结尾，动态开辟内存的时候也需要多考虑一位</strong></p>
<p>定义方式有两种：</p>
<ol>
<li><code>char str[] = &#123;&#39;E&#39;,&#39;a&#39;,&#39;s&#39;,&#39;t&#39;,&#39;r&#39;,&#39;i&#39;,&#39;s&#39;,&#39;e&#39;,&#39;\0&#39;&#125;;</code> : 可以修改</li>
<li><code>const char *str1 = &quot;Eastrise&quot;;</code>：不能修改，字符串常量存放再全局的const内存区</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="comment">// 第一种 字符串数组</span></span><br><span class="line">    <span class="type">char</span> str[] = &#123;<span class="string">&#x27;E&#x27;</span>,<span class="string">&#x27;a&#x27;</span>,<span class="string">&#x27;s&#x27;</span>,<span class="string">&#x27;t&#x27;</span>,<span class="string">&#x27;r&#x27;</span>,<span class="string">&#x27;i&#x27;</span>,<span class="string">&#x27;s&#x27;</span>,<span class="string">&#x27;e&#x27;</span>,<span class="string">&#x27;\0&#x27;</span>&#125;;</span><br><span class="line"></span><br><span class="line">    str[<span class="number">2</span>] = <span class="string">&#x27;y&#x27;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>,str); <span class="comment">// 字符串结尾必须是 &#x27;\0&#x27;，开辟内存也需要多开辟一个</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 第二种 不加 const 会警告：deprecated conversion from string constant to &#x27;char*&#x27;</span></span><br><span class="line">    <span class="comment">// 原因：</span></span><br><span class="line">    <span class="comment">//      &quot;Eastrise&quot;是字符串常量, 内存分配在全局的const内存区</span></span><br><span class="line">    <span class="comment">//       char * 声明了一个指针，而这个指针指向的是全局的const内存区，const内存区当然不会让你想改就改的。所以，如果你一定要写这块内存的话，那就是一个非常严重的内存错误。</span></span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> *str1 = <span class="string">&quot;Eastrise&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//    str1[2] = &#x27;y&#x27;;</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>,str1);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 两种定义的区别：一个能修改，一个不能修改</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-字符串长度获取"><a href="#2-字符串长度获取" class="headerlink" title="2.字符串长度获取"></a>2.字符串长度获取</h1><ol>
<li><p><code>strlen(name)</code></p>
</li>
<li><p>通过sizeof 进行计算（不好使）,不对</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const char str[] = &#123;&#x27;E&#x27;,&#x27;a&#x27;,&#x27;s&#x27;,&#x27;t&#x27;,&#x27;r&#x27;,&#x27;i&#x27;,&#x27;s&#x27;,&#x27;e&#x27;,&#x27;\0&#x27;,&#x27;i&#x27;,&#x27;s&#x27;&#125;;</span><br><span class="line">    </span><br><span class="line">// 怎么获取长度？有一种方式计算（不好使）</span><br><span class="line">printf(&quot;length is %d&quot;,sizeof(str)/sizeof(char)); // 打印出来 11，实际结果应该是 8</span><br></pre></td></tr></table></figure>
</li>
<li><p>写一个方法读取字符串的长度</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int strlen_(char *str)&#123;</span><br><span class="line">    // 怎么获取字符串的长度？ 不断读取字符，判断末尾 &#x27;\0&#x27;</span><br><span class="line"></span><br><span class="line">    int len = 0;</span><br><span class="line">    while (*str != &#x27;\0&#x27;)</span><br><span class="line">    &#123;</span><br><span class="line">        len++;</span><br><span class="line">        str++;</span><br><span class="line">    &#125;</span><br><span class="line">    return len;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h1 id="3-字符串与基本数据类型的转换"><a href="#3-字符串与基本数据类型的转换" class="headerlink" title="3.字符串与基本数据类型的转换"></a>3.字符串与基本数据类型的转换</h1><ul>
<li><code>atoi(num_str);</code> // <code>str 转 int</code>如果不能转换就是 0 ，后面如果有其他不是数字的就会被剔除 12xxx -&gt; 12</li>
<li><code>atof(num_str);</code> // <code>str 转 float</code> // 如果不能转换返回的是默认值 0.000000 </li>
<li><code>strtod(num_str, NULL);</code> // <code>str 转 double</code> // 如果不能转换返回的是默认值 0.000000 </li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">     <span class="type">const</span> <span class="type">char</span>* num1 = <span class="string">&quot;1&quot;</span>; <span class="comment">// -&gt; int float double</span></span><br><span class="line">     <span class="type">const</span> <span class="type">char</span>* num_str1 = <span class="string">&quot;12.0xxx&quot;</span>;</span><br><span class="line">     <span class="type">int</span> number1 = <span class="built_in">atoi</span>(num_str1); <span class="comment">// 如果不能转换就是 0 ，后面如果有其他不适数字的就会被剔除 12xxx -&gt; 12</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;number1 is %d\n&quot;</span>,number1);</span><br><span class="line"></span><br><span class="line">     <span class="type">const</span> <span class="type">char</span>* num_str2 = <span class="string">&quot;12.5f&quot;</span>;</span><br><span class="line">     <span class="type">float</span> number2 = <span class="built_in">atof</span>(num_str2); <span class="comment">// 如果不能转换返回的是默认值 0.0000</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;number2 is %f\n&quot;</span>,number2);</span><br><span class="line"></span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> *num_str3 = <span class="string">&quot;12.5xx&quot;</span>;</span><br><span class="line">    <span class="type">double</span> number3 = <span class="built_in">strtod</span>(num_str3,<span class="literal">NULL</span>);<span class="comment">// 如果不能转换就是 0.000 ，后面如果有其他不适数字的就会被剔除 12.5xxx -&gt; 12.5000</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;number3 is %lf\n&quot;</span>,number3);</span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">number1 is 12</span><br><span class="line">number2 is 12.500000</span><br><span class="line">number3 is 12.500000</span><br></pre></td></tr></table></figure>
<h1 id="4-字符串的比较"><a href="#4-字符串的比较" class="headerlink" title="4.字符串的比较"></a>4.字符串的比较</h1><ul>
<li><code>int rc = strcmp(str1, str2);</code>  // <code>区分大小写</code>比较，rc == 0代表相等，rc &gt; 0 str1 比 str2 大</li>
<li><code>int rc = strcmpi(str1, str2);</code>  // <code>不区分大小写</code>比较，rc == 0代表相等，反之不相等</li>
<li><code>int rc = strncmp(str1, str2,7);</code> // <code>区分大小写</code>比较， count 代表的是比较字符串前几个是否相等</li>
<li><code>int rc = strnicmp(str1, str2,7);</code> // <code>不区分大小写</code>比较， count 代表的是比较字符串前几个是否相等</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* str1 = <span class="string">&quot;Eastrise&quot;</span>;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* str2 = <span class="string">&quot;eastrise&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 大于小于等于</span></span><br><span class="line">    <span class="comment">// int rc = strcmp(str1,str2); // 区分大小写比较</span></span><br><span class="line">    <span class="comment">// int rc = _strcmpi(str1,str2); // 不区分大小写比较 c strcmpi c++ _strcmpi , android ndk strcasecmp;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 比较前几个</span></span><br><span class="line"><span class="comment">//     int rc = strncmp(str1,str2,7); // count 达标的是比较字符串前几个是否相等</span></span><br><span class="line">    <span class="type">int</span> rc = _strnicmp(str1,str2,<span class="number">6</span>); <span class="comment">// 不区分大小写的比较</span></span><br><span class="line">    <span class="keyword">if</span>(rc == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;相等&quot;</span>);</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;不相等&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-字符串查找，包含"><a href="#5-字符串查找，包含" class="headerlink" title="5.字符串查找，包含"></a>5.字符串查找，包含</h1><ul>
<li><p><code>char* pos = strstr(str, substr);</code> // 返回的是<code>字符串</code>第一次出现的位置（位置指针）, 如果没有找到返回的是空  </p>
<p>  获取位置: <code>int position = pos - str;</code></p>
</li>
<li><p><code>char* pos =  strchr(name,&#39;n&#39;);</code> // 返回的是<code>字符</code>第一次出现的位置（位置指针）, 如果没有找到返回的是空</p>
</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> *str = <span class="string">&quot;name not Eastrise&quot;</span>;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> *substr = <span class="string">&quot;E&quot;</span>;</span><br><span class="line">    <span class="type">char</span> *pos = <span class="built_in">strstr</span>(str, substr); <span class="comment">//  返回的是字符串第一次出现的位置（位置指针），如果没有找到返回的是空</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, pos); <span class="comment">// Eastrise;</span></span><br><span class="line">    <span class="comment">// 求一下位置 int 怎么办？ strstr</span></span><br><span class="line">    <span class="type">int</span> position = pos - str;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;第一次出现的位置是：%d\n&quot;</span>, position);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 包含？ pos 是不是空就可以了</span></span><br><span class="line">    <span class="keyword">if</span> (pos)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, <span class="string">&quot;包含&quot;</span>);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, <span class="string">&quot;不包含&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Eastrise</span><br><span class="line">第一次出现的位置是：9</span><br><span class="line">包含</span><br></pre></td></tr></table></figure>
<h1 id="6-字符串复制和拼接"><a href="#6-字符串复制和拼接" class="headerlink" title="6.字符串复制和拼接"></a>6.字符串复制和拼接</h1><ul>
<li><strong><code>strcpy(dst, src);</code></strong> // 复制</li>
<li><strong><code>strcat(dst,src);</code></strong> // 拼接</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//strcopy; copy进来</span></span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* str = <span class="string">&quot;eastrise&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* str1 = <span class="string">&quot; is&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// char* str1 = &quot;is&quot;;</span></span><br><span class="line">    <span class="type">char</span> cpy[<span class="built_in">strlen</span>(str)+<span class="built_in">strlen</span>(str1)];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// str 会 copy 到 cpy 里面</span></span><br><span class="line">    <span class="built_in">strcpy</span>(cpy,str);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>,cpy);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//拼接</span></span><br><span class="line">    <span class="built_in">strcat</span>(cpy,str1);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>,cpy);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">eastrise</span><br><span class="line">eastrise is</span><br></pre></td></tr></table></figure>
<h1 id="7-手写字符串截取"><a href="#7-手写字符串截取" class="headerlink" title="7.手写字符串截取"></a>7.手写字符串截取</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 截取</span></span><br><span class="line"><span class="function"><span class="type">char</span>* <span class="title">substr</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* str,<span class="type">int</span> start ,<span class="type">int</span> end)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 开辟一个字符串去存储我们的数据，开辟多大计算</span></span><br><span class="line">    <span class="comment">// char sub[end -start];</span></span><br><span class="line">    <span class="type">int</span> len = end - start;</span><br><span class="line">    <span class="keyword">if</span> (len == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">char</span>* sub = (<span class="type">char</span>*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="type">char</span>)*(len<span class="number">+1</span>));<span class="comment">// 记得+1，因为还有一个\0，在 NDK 一般会采用静态的数组存储 char sub[len]</span></span><br><span class="line">    <span class="comment">// malloc 一定要 free</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 遍历赋值</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i&lt; len; i++)&#123;</span><br><span class="line">        sub[i] = str[start+i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 标记字符串结尾，否则 print 无法判断结尾</span></span><br><span class="line">    sub[len] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%p\n&quot;</span>,sub);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// free(sub);</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> sub;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 字符串的截取</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* str = <span class="string">&quot;Eastrise is&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 截取第三个位置到第五个位置 3,5</span></span><br><span class="line">    <span class="type">char</span>* sub = <span class="built_in">substr</span>(str,<span class="number">0</span>,<span class="number">5</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%p\n&quot;</span>,sub);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>,sub);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 一定要free ，因为你的 substr 有动态开辟内存，但是真正开发中并不会这么做，自己的方法尽量要自己处理好内存</span></span><br><span class="line">    <span class="built_in">free</span>(sub);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0000000000B61440</span><br><span class="line">0000000000B61440</span><br><span class="line">Eastr</span><br></pre></td></tr></table></figure>
<h1 id="8-手写字符串小写转换"><a href="#8-手写字符串小写转换" class="headerlink" title="8.手写字符串小写转换"></a>8.手写字符串小写转换</h1><p>字符大小写转换</p>
<ul>
<li><code>tolower(ch);</code> // A-&gt;a</li>
<li><code>toupper(ch);</code> // a-&gt;A</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;ctype.h&gt;</span></span></span><br><span class="line"><span class="comment">// dest 用来存放结果， 大小自己指定</span></span><br><span class="line"><span class="comment">// source 需要转换的字符串</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">lower</span><span class="params">(<span class="type">char</span>* dest,<span class="type">const</span> <span class="type">char</span>* source)</span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (*source != <span class="string">&#x27;\0&#x27;</span>)&#123;</span><br><span class="line">        <span class="comment">// 拿当前字符</span></span><br><span class="line">        <span class="type">char</span> ch = *source;</span><br><span class="line">        <span class="comment">// 转换完成后赋值给 dest</span></span><br><span class="line">        *dest = <span class="built_in">tolower</span>(ch); <span class="comment">// a-&gt;a A-&gt;a</span></span><br><span class="line">        source++;</span><br><span class="line">        dest++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 标记字符串结尾</span></span><br><span class="line">    *dest = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">upper</span><span class="params">(<span class="type">char</span>* dest,<span class="type">const</span> <span class="type">char</span>* source)</span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (*source != <span class="string">&#x27;\0&#x27;</span>)&#123;</span><br><span class="line">        <span class="type">char</span> ch = *source;</span><br><span class="line">        *dest = <span class="built_in">toupper</span>(ch);</span><br><span class="line">        source ++;</span><br><span class="line">        dest ++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    *dest = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="9-手写字符串的替换"><a href="#9-手写字符串的替换" class="headerlink" title="9. 手写字符串的替换"></a>9. 手写字符串的替换</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> *<span class="title function_">substr</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *str, <span class="type">int</span> start, <span class="type">int</span> end)</span> &#123;</span><br><span class="line">    <span class="comment">// 开辟一个字符串去存储我们的数据，开辟多大计算</span></span><br><span class="line">    <span class="comment">// char sub[end -start];</span></span><br><span class="line">    <span class="type">int</span> len = end - start;</span><br><span class="line">    <span class="keyword">if</span> (len == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> nullptr;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">char</span> *sub = (<span class="type">char</span> *) <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="type">char</span>) * (len + <span class="number">1</span>));<span class="comment">// 记得+1，因为还有一个\0，在 NDK 一般会采用静态的数组存储 char sub[len]</span></span><br><span class="line">    <span class="comment">// malloc 一定要 free</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 遍历赋值</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; len; i++) &#123;</span><br><span class="line">        sub[i] = str[start + i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 标记字符串结尾，否则 print 无法判断结尾</span></span><br><span class="line">    sub[len] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%p\n&quot;</span>, sub);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// free(sub);</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> sub;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">char</span> *<span class="title function_">str_replace</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *str, <span class="type">const</span> <span class="type">char</span> *src, <span class="type">const</span> <span class="type">char</span> *dst)</span> &#123;</span><br><span class="line">    <span class="type">char</span> *pos = <span class="built_in">strstr</span>(str, src);</span><br><span class="line">    <span class="keyword">if</span> (!pos)</span><br><span class="line">        <span class="keyword">return</span> const_cast&lt;<span class="type">char</span> *&gt;(str);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 计算新的数组大小</span></span><br><span class="line">    <span class="type">char</span> result[<span class="built_in">strlen</span>(str) - <span class="built_in">strlen</span>(src) + <span class="built_in">strlen</span>(dst)];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 截取替换</span></span><br><span class="line">    <span class="type">int</span> start_position = pos - str;</span><br><span class="line">    <span class="type">char</span> *start = substr(str, <span class="number">0</span>, start_position);</span><br><span class="line">    <span class="type">char</span> *end = substr(str, start_position + <span class="built_in">strlen</span>(src), <span class="built_in">strlen</span>(str));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 拼接</span></span><br><span class="line">    <span class="keyword">if</span> (start)&#123;</span><br><span class="line">        <span class="built_in">strcpy</span>(result, start);</span><br><span class="line">        <span class="built_in">strcat</span>(result, dst);</span><br><span class="line">        <span class="built_in">free</span>(start);</span><br><span class="line">        <span class="keyword">if</span> (end)&#123;</span><br><span class="line">            <span class="built_in">strcat</span>(result, end);</span><br><span class="line">            <span class="built_in">free</span>(end);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="built_in">strcpy</span>(result, dst);</span><br><span class="line">        <span class="keyword">if</span> (end)&#123;</span><br><span class="line">            <span class="built_in">strcat</span>(result, end);</span><br><span class="line">            <span class="built_in">free</span>(end);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> str_replace(result, src, dst);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 字符串替换</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">char</span> *str = str_replace(<span class="string">&quot;aabbaabbfffaa&quot;</span>, <span class="string">&quot;aa&quot;</span>, <span class="string">&quot;ccc&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, str);</span><br><span class="line"></span><br><span class="line">    getchar();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">00000000006D1840</span><br><span class="line">00000000006D1860</span><br><span class="line">00000000006D1880</span><br><span class="line">00000000006D18A0</span><br><span class="line">cccbbcccbbfffccc</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C知识</category>
      </categories>
      <tags>
        <tag>C学习</tag>
        <tag>C基础</tag>
      </tags>
  </entry>
  <entry>
    <title>C基础：02.指针运算和函数指针</title>
    <url>/02.C%E5%9F%BA%E7%A1%80-%E6%8C%87%E9%92%88%E8%BF%90%E7%AE%97%E5%92%8C%E5%87%BD%E6%95%B0%E6%8C%87%E9%92%88/</url>
    <content><![CDATA[<h1 id="1-二级指针和多级指针"><a href="#1-二级指针和多级指针" class="headerlink" title="1.二级指针和多级指针"></a>1.二级指针和多级指针</h1><p><img src="/images/02二级指针.png" alt=""></p>
<p>二级指针：<strong>对一级指针取地址</strong></p>
<p>n级指针：<strong>对（n-1）指针取对峙</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> num = <span class="number">12</span>;</span><br><span class="line">    <span class="comment">// int* 指针  &amp; 取地址</span></span><br><span class="line">    <span class="type">int</span>* p = &amp;num;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span>** s_p = &amp;p; <span class="comment">// 对一级指针取地址</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//    int*** s_s_p = &amp;s_p; // 多级指针</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;p 的值是：%p, s_p 的值是：%p\n&quot;</span>,p,s_p);</span><br><span class="line">    <span class="comment">// p的地址是000000000060FE14 里面存的值为 12</span></span><br><span class="line">    <span class="comment">// s_p的地址是000000000060FE08 ，里面存的值是 000000000060FE14</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 通过二级指针获取值</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;获取最终的值是：%d&quot;</span>,**s_p);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();<span class="comment">// 暂停</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-数组与数组指针"><a href="#2-数组与数组指针" class="headerlink" title="2.数组与数组指针"></a>2.数组与数组指针</h1><ul>
<li>数组 arr 的值 = arr 取地址的值  = arr[0]的地址的值（首地址）</li>
<li>数组 arr_p[i] == *(arr_p+i)</li>
</ul>
<p>数组指针指向的是数组的首地址</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 定义数组</span></span><br><span class="line">    <span class="type">int</span> arr[] = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//遍历数组，linux 就有问题</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i&lt;<span class="number">4</span> ; i++)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>,arr[i]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// for 循环在 c 和 c++ 中的正确写法</span></span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(; i &lt; <span class="number">4</span>; i++)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>,arr[i]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 看一种现象：arr 的值 = arr 取地址的值 , arr 地址的值 = arr[0]的地址的值（首地址）</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;arr = %p\n&quot;</span>,arr); <span class="comment">// arr = 000000000060FE00</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;&amp;arr = %p\n&quot;</span>,&amp;arr); <span class="comment">// &amp;arr = 000000000060FE00</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;&amp;arr[0] = %p\n&quot;</span>,&amp;arr[<span class="number">0</span>]); <span class="comment">// &amp;arr[0] = 000000000060FE00</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 如何获取数组的指针</span></span><br><span class="line">    <span class="type">int</span>* arr_p = arr;<span class="comment">// 数组指针指向的是数组的首地址</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 做一系列的操作</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>,*arr_p); <span class="comment">// 1</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 对指针进行 ++</span></span><br><span class="line">    arr_p++;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>,*arr_p); <span class="comment">// 2</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 指针再往后挪动两位</span></span><br><span class="line">    arr_p += <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>,*arr_p); <span class="comment">// 4</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-1-采用指针遍历数据"><a href="#2-1-采用指针遍历数据" class="headerlink" title="2.1. 采用指针遍历数据"></a>2.1. 采用指针遍历数据</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> arr[] = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span>* arr_p = arr;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (; i &lt; <span class="number">4</span>; i++) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;位置%d的值是：%d\n&quot;</span>,i,*(arr_p+i));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">位置0的值是：1</span><br><span class="line">位置1的值是：2</span><br><span class="line">位置2的值是：3</span><br><span class="line">位置3的值是：4</span><br></pre></td></tr></table></figure>
<h2 id="2-2-采用指针循环为数组赋值"><a href="#2-2-采用指针循环为数组赋值" class="headerlink" title="2.2. 采用指针循环为数组赋值"></a>2.2. 采用指针循环为数组赋值</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> arr[<span class="number">4</span>]; <span class="comment">// 指定数组[4]</span></span><br><span class="line"></span><br><span class="line">    <span class="type">int</span>* arr_p = arr;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(; i&lt; <span class="number">4</span>; i++)&#123;</span><br><span class="line">        *(arr_p + i) = i;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(; i&lt; <span class="number">4</span>; i++)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;位置%d的值是：%d\n&quot;</span>,i,arr[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">位置0的值是：0</span><br><span class="line">位置1的值是：1</span><br><span class="line">位置2的值是：2</span><br><span class="line">位置3的值是：3</span><br></pre></td></tr></table></figure>
<h2 id="2-3-数组指针的几种操作方式"><a href="#2-3-数组指针的几种操作方式" class="headerlink" title="2.3.数组指针的几种操作方式"></a>2.3.数组指针的几种操作方式</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> arr[] = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span>* arr_p = arr;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(; i &lt; <span class="number">4</span>; i++)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;使用arr_p[i]算出位置%d的值是：%d\n&quot;</span>,i,arr_p[i]); <span class="comment">// == *(arr_p+i)</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;使用*(arr + i)算出位置%d的值是：%d\n&quot;</span>, i, *(arr + i));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">使用arr_p[i]算出位置0的值是：1</span><br><span class="line">使用*(arr + i)算出位置0的值是：1</span><br><span class="line">使用arr_p[i]算出位置1的值是：2</span><br><span class="line">使用*(arr + i)算出位置1的值是：2</span><br><span class="line">使用arr_p[i]算出位置2的值是：3</span><br><span class="line">使用*(arr + i)算出位置2的值是：3</span><br><span class="line">使用arr_p[i]算出位置3的值是：4</span><br><span class="line">使用*(arr + i)算出位置3的值是：4</span><br></pre></td></tr></table></figure>
<h1 id="3-函数指针"><a href="#3-函数指针" class="headerlink" title="3.函数指针"></a>3.函数指针</h1><p>方法指针怎么定义？ <code>方法的返回（*方法的名称）（方法的参数)</code>,中间的 * 号可以省略</p>
<p><code>函数指针跟数组类似  数组是 arr = &amp;arr,  函数指针是 void(*method)(int,int) = &amp;method,&amp;可以不要</code></p>
<p>例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 方法指针， 传两个数</span><br><span class="line">void operator1(void(method)(int,int),int num1,int num2)&#123;</span><br><span class="line">    method(num1,num2);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 加上*号也是可以的</span><br><span class="line">void operator1(void(*method)(int,int),int num1,int num2)&#123;</span><br><span class="line">    method(num1,num2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">call_back</span><span class="params">(<span class="type">int</span> current,<span class="type">int</span> total)</span></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;压缩的进度是：%d/%d\n&quot;</span>,current,total);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这个方法 3 ，他是一个额外的文件了</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">compress</span><span class="params">(<span class="type">char</span>* file_name,<span class="type">void</span>(*call_back)(<span class="type">int</span>,<span class="type">int</span>))</span></span>&#123;</span><br><span class="line">    <span class="built_in">call_back</span>(<span class="number">12</span>,<span class="number">24</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">void</span>(*call)(<span class="type">int</span>,<span class="type">int</span>); <span class="comment">// 声明了一个函数</span></span><br><span class="line">    call = call_back; <span class="comment">// 给函数指针赋值</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// call(1,2) 通过函数指针间接的调用call_back</span></span><br><span class="line">    <span class="built_in">compress</span>(<span class="string">&quot;1.png&quot;</span>,call);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>C知识</category>
      </categories>
      <tags>
        <tag>C学习</tag>
        <tag>C基础</tag>
      </tags>
  </entry>
  <entry>
    <title>JNI:02.数据类型，签名，Native层构建java对象，指针常量和常量指针</title>
    <url>/08.JNI%E5%9F%BA%E7%A1%80-Native%E5%B1%82%E6%9E%84%E5%BB%BAjava%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<p><strong>代码请看：<a href="https://github.com/EastUp/JavaJNITest/tree/master/src/com/east/jni08">08.JNI基础-Native层构建java对象</a></strong></p>
<h1 id="1-调用-java-层-static-的获取-uuid-的方法"><a href="#1-调用-java-层-static-的获取-uuid-的方法" class="headerlink" title="1. 调用 java 层 static 的获取 uuid 的方法"></a>1. 调用 java 层 static 的获取 uuid 的方法</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_jni08_Sample1_callStaticMethod</span></span><br><span class="line"><span class="params">(JNIEnv* env,jclass jclz)</span>&#123;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">// 2. 获取jmethodID</span></span><br><span class="line">	jmethodID j_mid = (*env)-&gt;GetStaticMethodID(env, jclz, <span class="string">&quot;getUUID&quot;</span>, <span class="string">&quot;()Ljava/lang/String;&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 1. 调用 getUUID 的方法 statics</span></span><br><span class="line">	jstring j_uuid = (*env)-&gt;CallStaticObjectMethod(env,jclz,j_mid);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// jstring -&gt; c_str</span></span><br><span class="line">	<span class="type">char</span>* c_uuid = (*env)-&gt;GetStringUTFChars(env,j_uuid,<span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 回收，字符串回收</span></span><br><span class="line">	(*env)-&gt;ReleaseStringChars(env,j_uuid,c_uuid);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;c_uuid = %s&quot;</span>,c_uuid);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Jni 回收内存：<br><code>(*env)-&gt;ReleaseStringUTFChars(env,j_uuid,c_uuid);</code></p>
<h1 id="2-jni-数据类型"><a href="#2-jni-数据类型" class="headerlink" title="2. jni 数据类型"></a>2. jni 数据类型</h1><ul>
<li>jstring -&gt; java String</li>
<li>jint -&gt; java int</li>
<li>jfloat -&gt; java float</li>
<li>jdouble -&gt; java double</li>
<li>jobject -&gt; java object</li>
<li>jintArray -&gt; java int[]</li>
<li>jdoubleArray -&gt; java double[]</li>
<li>jbyteArray -&gt; java byte[]<br>….</li>
</ul>
<h1 id="3-java数据类型对应的签名"><a href="#3-java数据类型对应的签名" class="headerlink" title="3.java数据类型对应的签名"></a>3.java数据类型对应的签名</h1><h2 id="3-1-基本数据类型签名"><a href="#3-1-基本数据类型签名" class="headerlink" title="3.1.基本数据类型签名"></a>3.1.基本数据类型签名</h2><ul>
<li>boolean -&gt; Z</li>
<li>byte    -&gt; B</li>
<li>char    -&gt; C</li>
<li>short   -&gt; S</li>
<li>int     -&gt; I</li>
<li>long    -&gt; J</li>
<li>float   -&gt; F</li>
<li>double  -&gt; D</li>
<li>void    -&gt; V</li>
</ul>
<h2 id="3-2-数组引用类型签名"><a href="#3-2-数组引用类型签名" class="headerlink" title="3.2.数组引用类型签名"></a>3.2.数组引用类型签名</h2><ul>
<li>int[]    -&gt; [I</li>
<li>float[]  -&gt; [f</li>
<li>byte[]   -&gt; [B</li>
<li>char[]   -&gt; [C</li>
<li>short[]  -&gt; [S</li>
<li>double[] -&gt; [D</li>
<li>long[]   -&gt; [F</li>
<li>boolean[]-&gt; [Z</li>
</ul>
<h2 id="3-3-引用数据类型签名"><a href="#3-3-引用数据类型签名" class="headerlink" title="3.3.引用数据类型签名"></a>3.3.引用数据类型签名</h2><ul>
<li>String -&gt; Ljava/lang/String;</li>
<li>Object[] -&gt; [L全类名;</li>
</ul>
<h1 id="4-常量指针和指针常量"><a href="#4-常量指针和指针常量" class="headerlink" title="4. 常量指针和指针常量"></a>4. 常量指针和指针常量</h1><ul>
<li>常量变量：被常量修饰的变量，不能再次被赋值 （Java）</li>
<li>常量指针：const 在 * 之前，指针的地址是可以被再次赋值的（可以修改的），指针地址上面的值（变量）是不能被修改的，常量指针的常量是不能被改变的。</li>
<li>指针常量：const 在 * 之后，指针的地址是不可以被再次赋值的（不可以修改的），指针地址上面的值（变量）能被修改的，指针常量的指针地址是不能被改变的。</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">	<span class="comment">// 常量，不能去修改</span></span><br><span class="line">	<span class="type">const</span> <span class="type">int</span> number = <span class="number">100</span>;</span><br><span class="line">	<span class="comment">// number = 200;</span></span><br><span class="line"></span><br><span class="line">	<span class="type">int</span> number1 = <span class="number">100</span>;</span><br><span class="line">	<span class="type">int</span> number2 = <span class="number">200</span>;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 常量指针（const 在 * 之前,地址可以被重新赋值，但是值不能改变）</span></span><br><span class="line">	<span class="comment">// int const * n_p = &amp;number2;</span></span><br><span class="line">	<span class="comment">// n_p = &amp;number1;</span></span><br><span class="line">	<span class="comment">// printf(&quot;n_p = %p&quot;,n_p); // 地址是可以重新被赋值的</span></span><br><span class="line">	<span class="comment">// *n_p = 300; // 值不能改变</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 指针常量（const 在 * 之后,地址不可以被重新赋值，但是值可以改变）</span></span><br><span class="line">	<span class="type">int</span>* <span class="type">const</span> n_p = &amp;number2;</span><br><span class="line">	<span class="comment">// n_p = &amp;number1; // 地址是不能被重新赋值的</span></span><br><span class="line">	*n_p = <span class="number">300</span>;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;number2 = %d&quot;</span>,number2);<span class="comment">// 300</span></span><br><span class="line"></span><br><span class="line">	getchar();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-Native-层构建-Java-对象"><a href="#5-Native-层构建-Java-对象" class="headerlink" title="5. Native 层构建 Java 对象"></a>5. Native 层构建 Java 对象</h1><ul>
<li>如果是一个类的构造方法获取jmethodID时，名字传<code>&lt;init&gt;</code></li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// c 层 创建 java 对象</span></span><br><span class="line">JNIEXPORT jobject JNICALL <span class="title function_">Java_com_east_jni08_Sample1_createPoint</span></span><br><span class="line"><span class="params">(JNIEnv* env, jclass jclz)</span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2. 需要获取 Point 的class name = &quot;全类名&quot;</span></span><br><span class="line">	<span class="comment">// 方法参数上的 jclz  指的是 Sample1.class</span></span><br><span class="line">	jclass point_clz = (*env)-&gt;FindClass(env,<span class="string">&quot;com/east/jni08/Point&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 3. 获取jmethodID ,构造方法写死 &quot;&lt;init&gt;&quot;</span></span><br><span class="line">	jmethodID j_mid = (*env)-&gt;GetMethodID(env, point_clz, <span class="string">&quot;&lt;init&gt;&quot;</span>,<span class="string">&quot;(II)V&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 1. 创建对象</span></span><br><span class="line">	jobject point = (*env)-&gt;NewObject(env,point_clz, j_mid, <span class="number">11</span>, <span class="number">12</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 练习下 y 重新赋值,调用 set 方法</span></span><br><span class="line">	j_mid = (*env)-&gt;GetMethodID(env,point_clz,<span class="string">&quot;setY&quot;</span>,<span class="string">&quot;(I)V&quot;</span>);</span><br><span class="line">	<span class="comment">/* va_list 集合</span></span><br><span class="line"><span class="comment">	void (JNICALL *CallVoidMethodV)</span></span><br><span class="line"><span class="comment">	(JNIEnv *env, jobject obj, jmethodID methodID, va_list args);</span></span><br><span class="line"><span class="comment">	// jvalue</span></span><br><span class="line"><span class="comment">	void (JNICALL *CallVoidMethodA)</span></span><br><span class="line"><span class="comment">	(JNIEnv *env, jobject obj, jmethodID methodID, const jvalue * args);</span></span><br><span class="line"><span class="comment">	*/</span></span><br><span class="line"></span><br><span class="line">	(*env)-&gt;CallObjectMethod(env,point,j_mid,<span class="number">33</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 直接重新赋值，相当于反射了但是比反射快</span></span><br><span class="line">	jfieldID j_fid = (*env)-&gt;GetFieldID(env,point_clz,<span class="string">&quot;y&quot;</span>,<span class="string">&quot;I&quot;</span>);</span><br><span class="line">	(*env)-&gt;SetIntField(env,point,j_fid,<span class="number">55</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> point;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>JNI</category>
      </categories>
      <tags>
        <tag>JNI学习</tag>
      </tags>
  </entry>
  <entry>
    <title>C基础：06.文件操作</title>
    <url>/06.C%E5%9F%BA%E7%A1%80-%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<h1 id="1-文件打开，读取，写入"><a href="#1-文件打开，读取，写入" class="headerlink" title="1.文件打开，读取，写入"></a>1.文件打开，读取，写入</h1><ul>
<li><code>siz_t fread(void *buffer,size_t size,size_t count, FILE* stream);</code> :以字节位计算长度,按照指定的长度和次数读取数据,遇到结尾或完成指定长度读取后停止.可用于文件复制</li>
<li><code>char *fgets(char *string, int n, FILE *stream);</code> :从流中读取一字符串,整行读取,遇到回车换行或结尾停止.在文本方式时使用.</li>
<li><code>int fputs(char *string, FILE *stream);</code>：将一个字符串写到一个文件流中 </li>
<li><code>FILE *fopen(char *filename, char *type);</code>：打开文件</li>
<li><code>int fclose(FILE *stream);</code>：关闭文件<br>打开：</li>
</ul>
<p><code>FILE* fopen(const char* filename,const char* mode);</code> // 打开文件 （文件名，模式）mode r(读) w(写) rb(作为二进制读) wb(作为二进制写) a（追加）<br>注意: <code>&quot;wb&quot; 模式 会新建一个文件 0KB</code></p>
<p>模式：</p>
<ul>
<li>“w” 打开只写文件，若文件存在则文件长度清为0，即该文件内容会消失。若文件不存在则建立该文件。</li>
<li>“wb” 只写打开或新建一个二进制文件；只允许写数据。若文件不存在则建立该文件。,不会清空文件</li>
<li>“wb+” 读写打开或建立一个二进制文件，允许读和写</li>
<li>“a” 以附加的方式打开只写文件。若文件不存在，则会建立该文件，如果文件存在，写入的数据会被加到文件尾，即文件原先的内容会被保留。（EOF符保留）</li>
<li>”a+“ 以附加方式打开可读写的文件。若文件不存在，则会建立该文件，如果文件存在，写入的数据会被加到文件尾后，即文件原先的内容会被保留。 （原来的EOF符不保留）</li>
</ul>
<h2 id="1-1-文件读取"><a href="#1-1-文件读取" class="headerlink" title="1.1.文件读取"></a>1.1.文件读取</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">char</span>* file_name = <span class="string">&quot;F:\\Sample.txt&quot;</span>; <span class="comment">// 源文件</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//文件读</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="comment">// 打开文件（文件名，模式） mode r(读) w(写) rb(作为二进制读) wb(作为二进制写) a（追加）</span></span><br><span class="line">    <span class="comment">// FILE* fopen(const char* filename,const char* mode);</span></span><br><span class="line">    <span class="comment">// char *fgets(char *string, int n, FILE *stream);</span></span><br><span class="line">    FILE* file = fopen(file_name,<span class="string">&quot;r&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(!file)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;文件打开失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);<span class="comment">// 退出程序</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">char</span> buffer[<span class="number">10</span>];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 缓冲区buffer, 长度10，文件指针</span></span><br><span class="line">    <span class="keyword">while</span> (fgets(buffer,<span class="number">10</span>,file))&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>,buffer);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//关闭文件</span></span><br><span class="line">    fclose(file);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="1-2-文件写入"><a href="#1-2-文件写入" class="headerlink" title="1.2.文件写入"></a>1.2.文件写入</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    FILE* file = <span class="built_in">fopen</span>(file_name,<span class="string">&quot;w&quot;</span>);  <span class="comment">//会清空原文件内容</span></span><br><span class="line">    <span class="keyword">if</span>(!file)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;文件打开失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);<span class="comment">// 退出程序</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">fputs</span>(<span class="string">&quot;I am eastrise&quot;</span>,file);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 关闭文件</span></span><br><span class="line">    <span class="built_in">fclose</span>(file);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-文件复制，当做二进制文件来操作"><a href="#2-文件复制，当做二进制文件来操作" class="headerlink" title="2.文件复制，当做二进制文件来操作"></a>2.文件复制，当做二进制文件来操作</h1><ul>
<li><code>int fread(void *ptr, int size, int nitems, FILE *stream);</code>：二进制流读取</li>
<li><code>int fwrite(void *ptr, int size, int nitems, FILE *stream);</code>: 二进制流写入</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    FILE* file = fopen(file_name,<span class="string">&quot;rb&quot;</span>); <span class="comment">// 流指针</span></span><br><span class="line">    FILE* file_copy = fopen(file_copy_name,<span class="string">&quot;wb&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(!file || !file_copy)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;文件打开失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// siz_t fread(void *buffer,size_t size,size_t count, FILE* stream);</span></span><br><span class="line">    <span class="type">int</span> buffer[<span class="number">512</span>]; <span class="comment">// 2048 = 4*512</span></span><br><span class="line">    <span class="type">int</span> len;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,<span class="keyword">sizeof</span>(buffer));</span><br><span class="line">    <span class="keyword">while</span> ((len = fread(buffer,<span class="keyword">sizeof</span>(<span class="type">int</span>),<span class="number">512</span>,file))!=<span class="number">0</span>)&#123;</span><br><span class="line">        fwrite(buffer,<span class="keyword">sizeof</span>(<span class="type">int</span>),len,file_copy);</span><br><span class="line">    &#125;</span><br><span class="line">    fclose(file);</span><br><span class="line">    fclose(file_copy);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-获取文件大小"><a href="#3-获取文件大小" class="headerlink" title="3.获取文件大小"></a>3.获取文件大小</h1><ul>
<li><code>int fseek(FILE *stream, long offset, int fromwhere);</code>：重定位流上的文件指针 </li>
<li><code>long ftell(FILE *stream);</code>: 返回当前文件指针，大小是字节</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    FILE* file = fopen(file_name,<span class="string">&quot;rb&quot;</span>);<span class="comment">// 流指针</span></span><br><span class="line">    <span class="keyword">if</span> (!file)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;文件打开失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);<span class="comment">// 退出程序</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 没有一个函数是可以直接拿文件大小</span></span><br><span class="line">    <span class="comment">// 思路：将文件的指针移动到最后，然后再去计算偏移量</span></span><br><span class="line">    <span class="comment">// 三个参数 SEEK_SET（开头），SEEK_CUR（当前），SEEK_END（移动到最后）</span></span><br><span class="line">    fseek(file,<span class="number">0</span>,SEEK_END);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 计算偏移的位置，ftell 从 0 开始统计到当前（SEEK_END）</span></span><br><span class="line">    <span class="type">long</span> file_size = ftell(file);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;文件大小：%ld个字节&quot;</span>,file_size);</span><br><span class="line"></span><br><span class="line">    fclose(file);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-文件加密、解密（该方法文件适合图片，如果文件中有中文就不适合）"><a href="#4-文件加密、解密（该方法文件适合图片，如果文件中有中文就不适合）" class="headerlink" title="4.文件加密、解密（该方法文件适合图片，如果文件中有中文就不适合）"></a>4.文件加密、解密（该方法文件适合图片，如果文件中有中文就不适合）</h1><h2 id="4-1-加密"><a href="#4-1-加密" class="headerlink" title="4.1.加密"></a>4.1.加密</h2><ul>
<li><code>int fgetc(FILE *stream);</code>:从文件流中读取字符 </li>
<li><code>int fputc(int ch, FILE *stream);</code>：将字符写到一个文件流中 </li>
</ul>
<p>思路：文件每个字节拿出来<code>^5</code>,^（相同为0，不同为1）</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 为了防止图片泄密，图片进行加密</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    FILE* file = fopen(file_image_name,<span class="string">&quot;rb&quot;</span>); <span class="comment">// 流指针</span></span><br><span class="line">    FILE* file_encrpy = fopen(file_encrpy_image_name,<span class="string">&quot;wb&quot;</span>); <span class="comment">// 新建一个文件 0KB</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!file || !file_encrpy)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;文件打开失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);<span class="comment">// 退出程序</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 破坏文件，还原</span></span><br><span class="line">    <span class="comment">// 思路：把每一个字节都拿出来，对每一个字节都处理，把某部分字节拿出来，进行处理</span></span><br><span class="line">    <span class="comment">// 10^5 异或 加密过程</span></span><br><span class="line">    <span class="comment">// 1010</span></span><br><span class="line">    <span class="comment">// ^0101</span></span><br><span class="line">    <span class="comment">// 1111 加密</span></span><br><span class="line">    <span class="type">int</span> c;<span class="comment">// EOF end of file</span></span><br><span class="line">    <span class="keyword">while</span> ((c = fgetc(file))!=EOF)&#123;</span><br><span class="line">        fputc(c ^ <span class="number">5</span>,file_encrpy);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="4-2-解密"><a href="#4-2-解密" class="headerlink" title="4.2.解密"></a>4.2.解密</h2><p>思路：文件每个字节拿出来<code>^5</code>,^（相同为0，不同为1）</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    FILE* file = fopen(file_encrpy_image_name,<span class="string">&quot;rb&quot;</span>); <span class="comment">// 流指针</span></span><br><span class="line">    FILE* file_decrpy = fopen(file_decrpy_image_name,<span class="string">&quot;wb&quot;</span>);<span class="comment">// 新建一个文件 0kb</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(!file || !file_decrpy)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;文件打开失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 解密 同样的去异或 5</span></span><br><span class="line">    <span class="comment">//  1111</span></span><br><span class="line">    <span class="comment">// ^0101</span></span><br><span class="line">    <span class="comment">//  1010  // 解密后 10</span></span><br><span class="line">    <span class="type">int</span> c; <span class="comment">// EOF end of file;</span></span><br><span class="line">    <span class="keyword">while</span>((c = fgetc(file))!=EOF)&#123;</span><br><span class="line">        fputc(c ^ <span class="number">5</span>,file_decrpy);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    fclose(file);</span><br><span class="line">    fclose(file_decrpy);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-字符串秘钥对文件进行加密、解密"><a href="#5-字符串秘钥对文件进行加密、解密" class="headerlink" title="5.字符串秘钥对文件进行加密、解密"></a>5.字符串秘钥对文件进行加密、解密</h1><h2 id="5-1-字符串秘钥对文件加密"><a href="#5-1-字符串秘钥对文件加密" class="headerlink" title="5.1.字符串秘钥对文件加密"></a>5.1.字符串秘钥对文件加密</h2><p>思路：文件每个字节拿出来按照顺序轮流跟字符串秘钥进行<code>^</code>操作,^（相同为0，不同为1）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* pass_word = <span class="string">&quot;12345&quot;</span>; <span class="comment">// 轮流进行^操作</span></span><br><span class="line"></span><br><span class="line">    FILE* file = <span class="built_in">fopen</span>(file_image_name,<span class="string">&quot;rb&quot;</span>); <span class="comment">// 流操作</span></span><br><span class="line">    FILE* file_encrpy = <span class="built_in">fopen</span>(file_encrpy_image_name,<span class="string">&quot;wb&quot;</span>); <span class="comment">// 新建一个文件 0KB</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!file || !file_encrpy)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;文件打开失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);<span class="comment">// 退出程序</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> c; <span class="comment">// EOF end of file</span></span><br><span class="line">    <span class="type">int</span> index = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> pass_len = <span class="built_in">strlen</span>(pass_word);</span><br><span class="line">    <span class="keyword">while</span>((c = <span class="built_in">fgetc</span>(file))!=EOF)&#123;</span><br><span class="line">        <span class="built_in">fputc</span>(c ^ pass_word[index % pass_len],file_encrpy);</span><br><span class="line">        index++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">fclose</span>(file);</span><br><span class="line">    <span class="built_in">fclose</span>(file_encrpy);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="5-2-字符串秘钥对文件解密"><a href="#5-2-字符串秘钥对文件解密" class="headerlink" title="5.2.字符串秘钥对文件解密"></a>5.2.字符串秘钥对文件解密</h2><p>思路：文件每个字节拿出来按照顺序轮流跟字符串秘钥进行<code>^</code>操作,^（相同为0，不同为1）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* pass_word = <span class="string">&quot;12345&quot;</span>;<span class="comment">// 轮流进行^操作</span></span><br><span class="line"></span><br><span class="line">    FILE* file = <span class="built_in">fopen</span>(file_encrpy_image_name,<span class="string">&quot;rb&quot;</span>); <span class="comment">// 流操作</span></span><br><span class="line">    FILE* file_decrpy = <span class="built_in">fopen</span>(file_decrpy_image_name,<span class="string">&quot;wb&quot;</span>); <span class="comment">// 新建一个文件 0KB</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!file || !file_decrpy)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;文件打开失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);<span class="comment">// 退出程序</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> c; <span class="comment">// EOF end of file</span></span><br><span class="line">    <span class="type">int</span> index = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> pass_len = <span class="built_in">strlen</span>(pass_word);</span><br><span class="line">    <span class="keyword">while</span> ((c = <span class="built_in">fgetc</span>(file)) != EOF)&#123;</span><br><span class="line">        <span class="built_in">fputc</span>(c ^ pass_word[index%pass_len],file_decrpy);</span><br><span class="line">        index++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">fclose</span>(file);</span><br><span class="line">    <span class="built_in">fclose</span>(file_decrpy);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="6-文件切割、合并"><a href="#6-文件切割、合并" class="headerlink" title="6. 文件切割、合并"></a>6. 文件切割、合并</h1><ul>
<li><code>int sprintf(char *string, char *farmat [,argument,...]);</code>：将格式化输出到字符串中 </li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">getFileSize</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* fileName)</span></span>&#123;</span><br><span class="line">    FILE* file = <span class="built_in">fopen</span>(fileName,<span class="string">&quot;rb&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(!file)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">fseek</span>(file,<span class="number">0</span>,SEEK_END);</span><br><span class="line">    <span class="type">int</span> file_size = <span class="built_in">ftell</span>(file);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">fclose</span>(file);</span><br><span class="line">    <span class="keyword">return</span> file_size;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="6-1-文件切割"><a href="#6-1-文件切割" class="headerlink" title="6.1.文件切割"></a>6.1.文件切割</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="comment">// 百度云 不大于4G的文件？ 8G 文件， 扔进去</span></span><br><span class="line">    <span class="comment">// 大文件（断点续传）</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 文件切割，把文件合作为一个思考</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 计算每个文件需要写入多少？</span></span><br><span class="line"></span><br><span class="line">    FILE* file = fopen(file_image_name,<span class="string">&quot;rb&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> file_size = getFileSize(file_image_name);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义 切多少，计算大小，指定文件名</span></span><br><span class="line">    <span class="type">int</span> file_number = <span class="number">3</span>;</span><br><span class="line">    <span class="comment">// 二位数组，char[] char*</span></span><br><span class="line">    <span class="comment">// image_0.png</span></span><br><span class="line">    <span class="comment">// image_1.png</span></span><br><span class="line">    <span class="comment">// image_2.png</span></span><br><span class="line">    <span class="comment">// char* file_name;</span></span><br><span class="line">    <span class="comment">// char*</span></span><br><span class="line">    <span class="type">char</span>** file_names = (<span class="type">char</span>**)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="type">char</span>*)*file_number);</span><br><span class="line">    <span class="type">int</span> preFileSize = file_size / file_number;</span><br><span class="line">    <span class="comment">// 计算复制</span></span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(; i&lt; <span class="number">3</span>; i++)&#123;</span><br><span class="line">        file_names[i] = (<span class="type">char</span>*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="type">char</span>)*<span class="number">100</span>);<span class="comment">// &#x27;\0&#x27;</span></span><br><span class="line">        <span class="built_in">sprintf</span>(file_names[i],file_split,i);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>,file_names[i]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(; i &lt; file_number; i++)&#123;</span><br><span class="line">        <span class="comment">// 从源文件中往切割文件写入数据</span></span><br><span class="line">        FILE* cur_file = fopen(file_names[i],<span class="string">&quot;wb&quot;</span>);  <span class="comment">// w 会新建文件</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 写多少？</span></span><br><span class="line">        <span class="type">int</span> start = i*preFileSize;</span><br><span class="line">        <span class="type">int</span> end = (i + <span class="number">1</span>)*preFileSize;</span><br><span class="line">        <span class="keyword">if</span>(i == file_number - <span class="number">1</span>)&#123;</span><br><span class="line">            end = file_size;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 第一个循环 file 读，第二个循环还是从 file 里面</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j = start; j&lt; end; j++)&#123;</span><br><span class="line">            <span class="comment">// 从 file 里面读，写入到 cur_file</span></span><br><span class="line">            fputc(fgetc(file),cur_file);</span><br><span class="line">        &#125;</span><br><span class="line">        fclose(cur_file);</span><br><span class="line">        <span class="built_in">free</span>(file_names[i]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    fclose(file);</span><br><span class="line">    <span class="built_in">free</span>(file_names);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="6-2-文件合并"><a href="#6-2-文件合并" class="headerlink" title="6.2.文件合并"></a>6.2.文件合并</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    FILE* file0 = fopen(<span class="string">&quot;F:\\image_0.jpg&quot;</span>,<span class="string">&quot;rb&quot;</span>);</span><br><span class="line">    FILE* file1 = fopen(<span class="string">&quot;F:\\image_1.jpg&quot;</span>,<span class="string">&quot;rb&quot;</span>);</span><br><span class="line">    FILE* file2 = fopen(<span class="string">&quot;F:\\image_2.jpg&quot;</span>,<span class="string">&quot;rb&quot;</span>);</span><br><span class="line"></span><br><span class="line">    FILE* file_merge = fopen(<span class="string">&quot;F:\\image_merge.jpg&quot;</span>,<span class="string">&quot;wb&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> c=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>((c = fgetc(file0))!=EOF)&#123;</span><br><span class="line">        fputc(c,file_merge);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>((c = fgetc(file1))!=EOF)&#123;</span><br><span class="line">        fputc(c,file_merge);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>((c = fgetc(file2))!=EOF)&#123;</span><br><span class="line">        fputc(c,file_merge);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    fclose(file0);</span><br><span class="line">    fclose(file1);</span><br><span class="line">    fclose(file2);</span><br><span class="line">    fclose(file_merge);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C知识</category>
      </categories>
      <tags>
        <tag>C学习</tag>
        <tag>C基础</tag>
      </tags>
  </entry>
  <entry>
    <title>JNI:01.JNIEnv 的实现原理</title>
    <url>/07.JNI%E5%9F%BA%E7%A1%80-JNIEnv%20%E7%9A%84%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<p><strong>代码请看：<a href="https://github.com/EastUp/JavaJNITest/tree/master/src/com/east/jni07">07.JNI基础-JNI Env 实现原理</a></strong></p>
<p><code>-&gt; 调用的情况下必须是一级指针 *取值</code></p>
<ul>
<li><code>env-&gt;GetXXFieldID</code>：获取字段的ID</li>
<li><code>env-&gt;GetXXField</code>：获取字段上的值,返回XX类型</li>
<li><code>env-&gt;GetMethodID</code>:获取方法的ID</li>
<li><code>env-&gt;CallXXMethod</code>：调用方法。返回XX类型</li>
</ul>
<h1 id="1-JNI-的一般开发流程："><a href="#1-JNI-的一般开发流程：" class="headerlink" title="1. JNI 的一般开发流程："></a>1. JNI 的一般开发流程：</h1><ol>
<li>定义好本地的 native 方法,生成<code>.class</code>文件。</li>
<li>在存放<code>原文件 .java</code>的包名文件夹的最前面（<code>src</code>文件夹下）使用javah 命令生成 .h 头文件  <code>javah -d jni -jni com.east.NdkSample</code><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">用法:</span><br><span class="line">  javah [options] &lt;classes&gt;</span><br><span class="line">其中, [options] 包括:</span><br><span class="line">  -o &lt;file&gt;                输出文件 (只能使用 -d 或 -o 之一)</span><br><span class="line">  -d &lt;dir&gt;                 输出目录</span><br><span class="line">  -v  -verbose             启用详细输出</span><br><span class="line">  -h  --help  -?           输出此消息</span><br><span class="line">  -version                 输出版本信息</span><br><span class="line">  -jni                     生成 JNI 样式的标头文件 (默认值)</span><br><span class="line">  -force                   始终写入输出文件</span><br><span class="line">  -classpath &lt;path&gt;        从中加载类的路径</span><br><span class="line">  -cp &lt;path&gt;               从中加载类的路径</span><br><span class="line">  -bootclasspath &lt;path&gt;    从中加载引导类的路径</span><br><span class="line">&lt;classes&gt; 是使用其全限定名称指定的</span><br><span class="line">(例如, java.lang.Object)。</span><br></pre></td></tr></table></figure></li>
<li>拷贝 <code>xxx.h</code>、<code>jni_md.h</code>、<code>jni.h</code> 到 VS 的工程目录并添加依赖进来。(jni_md.h和jni.h在java安装目录下搜索即可查找到)</li>
<li>实现我们头文件中的 native 方法</li>
<li>VS生成解决方案即可生成 <code>.dll 动态库</code>，java 引入 dll 动态库运行即可</li>
</ol>
<p>java 中加载动态库：</p>
<ul>
<li><code>System.loadLibrary</code> ：android 加载apk中的libs目录下 .so 库</li>
<li><code>System.load</code> : 加载一个具体路径上的 .so 库，去服务器上下载再进行加载(data/data)</li>
</ul>
<h1 id="2-详解-h-文件和实现文件"><a href="#2-详解-h-文件和实现文件" class="headerlink" title="2. 详解 .h 文件和实现文件"></a>2. 详解 .h 文件和实现文件</h1><p>com_east_NdkSample.h：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;jni.h&quot;</span></span></span><br><span class="line"><span class="comment">/* Header for class com_east_NdkSample */</span></span><br><span class="line"><span class="comment">// 用来打一个标记， c 在编译的时候会把头文件 copy 到你引入的地方，不管是重复引用还是相互引用都只会 copy 一次</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifndef</span> _Included_com_east_jni07_NdkSample</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> _Included_com_east_jni07_NdkSample</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> __cplusplus <span class="comment">// 相当于 if 语句 c++</span></span></span><br><span class="line"><span class="comment">// 不管是 c 还是 c++ 统一都是采用 c 的编译方法，因为在 c 里面是不允许函数重载的，但是在 c++ 里面可以</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> &#123;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Class:     com_east_jni07_NdkSample</span></span><br><span class="line"><span class="comment"> * Method:    getSingnaturePassword</span></span><br><span class="line"><span class="comment"> * Signature: ()Ljava/lang/String;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">JNIEXPORT jstring JNICALL <span class="title function_">Java_com_east_jni07_NdkSample_getSingnaturePassword</span></span><br><span class="line">  <span class="params">(JNIEnv *, jclass)</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> __cplusplus</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<p>Simple.c：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 实现我们的 native 方法</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;com_east_NdkSample.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// JNIEXPORT： JNI 一个关键字，不能少（编译能通过），标记为该方法可以被外部调用</span></span><br><span class="line"><span class="comment">// jstring：代表 java 中的 String</span></span><br><span class="line"><span class="comment">// JNICALL：也是一个关键字，可以少的 jni call</span></span><br><span class="line"><span class="comment">// jobject： 非静态方法时 java 传递下来的类对象，就是本项目中 NdkSample java 对象</span></span><br><span class="line"><span class="comment">// jclass：静态方法时，java 传递下来的 class 对象，就是本项目中的 NdkSample.class</span></span><br><span class="line">JNIEXPORT jstring JNICALL <span class="title function_">Java_com_east_NdkSample_getSingnaturePassword</span></span><br><span class="line">        <span class="params">(JNIEnv * env, jclass clz)</span>&#123;</span><br><span class="line">    <span class="comment">// C 中 typedef const struct JNINativeInterface* JNIEnv; 已经是一个结构体指针了</span></span><br><span class="line">    <span class="comment">// C++ 中 typedef _JNIEnv JNIEnv; // 就是一个结构体</span></span><br><span class="line">    <span class="comment">// JNIEnv * 其实已经是一个二级指针了，所以 -&gt; 调用的情况下必须是一级指针 *取值</span></span><br><span class="line">    <span class="keyword">return</span> (*env)-&gt;NewStringUTF(env, <span class="string">&quot;eastrise&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-JNIEnv-的实现原理"><a href="#3-JNIEnv-的实现原理" class="headerlink" title="3. JNIEnv 的实现原理"></a>3. JNIEnv 的实现原理</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">if</span> defined(__cplusplus) <span class="comment">// 如果是C++文件 </span></span></span><br><span class="line"><span class="keyword">typedef</span> _JNIEnv JNIEnv;    JNIEnv 就是结构体 _JNIEnv</span><br><span class="line"><span class="keyword">typedef</span> _JavaVM JavaVM;  </span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line"><span class="keyword">typedef</span> <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">JNINativeInterface</span>* <span class="title">JNIEnv</span>;</span> <span class="comment">// 如果是C文件 JNIEnv 是JNINativeInterface的一级指针</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">JNIInvokeInterface</span>* <span class="title">JavaVM</span>;</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<p>看下 <code>JNINativeInterface</code> 的部分代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">JNINativeInterface</span> &#123;</span></span><br><span class="line">    <span class="type">void</span>*       reserved0;</span><br><span class="line">    <span class="type">void</span>*       reserved1;</span><br><span class="line">    <span class="type">void</span>*       reserved2;</span><br><span class="line">    <span class="type">void</span>*       reserved3;</span><br><span class="line"></span><br><span class="line">    jint        (*GetVersion)(JNIEnv *); <span class="comment">// 函数指针</span></span><br><span class="line">    ....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>看下 <code>_JNIEnv</code> 的部分代码（调用的还是 <code>JNINativeInterface</code> 的方法）：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> _<span class="title">JNIEnv</span> &#123;</span></span><br><span class="line">    <span class="comment">/* do not rename this; it does not seem to be entirely opaque */</span></span><br><span class="line">    <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">JNINativeInterface</span>* <span class="title">functions</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> defined(__cplusplus) <span class="comment">// 如果是 C++ ，调用的还是 JNINativeInterface 的方法</span></span></span><br><span class="line"></span><br><span class="line">    jint <span class="title function_">GetVersion</span><span class="params">()</span></span><br><span class="line">    &#123; <span class="keyword">return</span> functions-&gt;GetVersion(this); &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-C-中访问和修改-java-的属性和方法"><a href="#4-C-中访问和修改-java-的属性和方法" class="headerlink" title="4. C 中访问和修改 java 的属性和方法"></a>4. C 中访问和修改 java 的属性和方法</h1><ul>
<li><code>env-&gt;GetXXFieldID</code>：获取字段的ID</li>
<li><code>env-&gt;GetXXField</code>：获取字段上的值,返回XX类型</li>
</ul>
<p>进入类编译后的<code>.class</code>文件，<strong>全包名路径</strong>的目录下。</p>
<p>获取类中：属性和方法签名：<code>javap -p -s com.east.jni07.NdkSample</code></p>
<ul>
<li><code>-s</code>:输出内部类型签名</li>
<li><code>-p</code>:显示所有类和成员</li>
</ul>
<h2 id="4-1-C中修改-java-的普通属性"><a href="#4-1-C中修改-java-的普通属性" class="headerlink" title="4.1 C中修改 java 的普通属性"></a>4.1 C中修改 java 的普通属性</h2><p>java 中的属性：<code>public String name = &quot;Eastrise&quot;;</code></p>
<p>java 中的本地方法：<code>public native void changeName();</code></p>
<p>c中的方法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_NdkSample1_changeName</span></span><br><span class="line"><span class="params">(JNIEnv * env, jobject jobj)</span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 1. 获取 jclass</span></span><br><span class="line">	jclass j_clz = (*env)-&gt;GetObjectClass(env,jobj);</span><br><span class="line">	<span class="comment">// 2. 获取 jfieldID</span></span><br><span class="line">	<span class="comment">// 获取 jfieldId (JNIEnv *env, jclass clazz, const char *name, const char *sig)</span></span><br><span class="line">	<span class="comment">// name 获取哪个属性的属性名 ,sig 属性的签名</span></span><br><span class="line">	jfieldID j_fid = (*env)-&gt;GetFieldID(env, j_clz, <span class="string">&quot;name&quot;</span>, <span class="string">&quot;Ljava/lang/String;&quot;</span>);</span><br><span class="line">	<span class="comment">// 1. 获取 name 的属性值</span></span><br><span class="line">	jstring j_str = (*env)-&gt;GetObjectField(env,jobj,j_fid);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 打印字符串 jstring -&gt; c_str</span></span><br><span class="line">	<span class="type">char</span>* c_str = (*env)-&gt;GetStringUTFChars(env, j_str, <span class="literal">NULL</span>);</span><br><span class="line">	</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;name is %s&quot;</span>, c_str);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 修改成 Jack</span></span><br><span class="line">	jstring jack_name = (*env)-&gt;NewStringUTF(env,<span class="string">&quot;Jack&quot;</span>);</span><br><span class="line">	(*env)-&gt;SetObjectField(env,jobj,j_fid,jack_name);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="4-2-C中修改-java-的静态属性"><a href="#4-2-C中修改-java-的静态属性" class="headerlink" title="4.2 C中修改 java 的静态属性"></a>4.2 C中修改 java 的静态属性</h2><p>java 中的属性：<code>public static int age = 24;</code></p>
<p>java 中的本地静态方法：<code>public static native void changeAge();</code></p>
<p>c中的方法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 静态方法参数会变为 jclass 代表 NdkSample1.class</span></span><br><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_NdkSample1_changeAge</span></span><br><span class="line"><span class="params">(JNIEnv * env, jclass jcls)</span>&#123;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">// 2. 获取 jfieldID</span></span><br><span class="line">	jfieldID j_fid = (*env)-&gt;GetStaticFieldID(env,jcls,<span class="string">&quot;age&quot;</span>,<span class="string">&quot;I&quot;</span>);</span><br><span class="line">	<span class="comment">// 1. 获取 age 的值 Static 获取静态的</span></span><br><span class="line">	jint age = (*env)-&gt;GetStaticIntField(env,jcls,j_fid);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// jnit == int</span></span><br><span class="line">	age += <span class="number">12</span>;</span><br><span class="line">	</span><br><span class="line">	(*env)-&gt;SetStaticIntField(env,jcls,j_fid,age);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="4-3-c-调用-java-方法"><a href="#4-3-c-调用-java-方法" class="headerlink" title="4.3 c 调用 java 方法"></a>4.3 c 调用 java 方法</h2><ul>
<li><code>env-&gt;GetMethodID</code>:获取方法的ID</li>
<li><code>env-&gt;CallXXMethod</code>：调用方法。返回XX类型</li>
</ul>
<p>java 中的方法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">public <span class="type">int</span> <span class="title function_">add</span><span class="params">(<span class="type">int</span> number1,<span class="type">int</span> number2)</span>&#123;</span><br><span class="line">	<span class="keyword">return</span> number1+number2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>c中的方法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// c 调用 java 层普通方法</span></span><br><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_NdkSample1_callAddMethod</span></span><br><span class="line"><span class="params">(JNIEnv * env, jobject jobj)</span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 3. 获取 jclass</span></span><br><span class="line">	jclass jcls = (*env)-&gt;GetObjectClass(env,jobj);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2. 获取 jmethodID</span></span><br><span class="line">	jmethodID j_mid = (*env)-&gt;GetMethodID(env,jcls,<span class="string">&quot;add&quot;</span>,<span class="string">&quot;(II)I&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 1. 调用 java 方法</span></span><br><span class="line">	jint sum = (*env)-&gt;CallObjectMethod(env,jobj,j_mid,<span class="number">2</span>,<span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;sum = %d&quot;</span>,sum);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>JNI</category>
      </categories>
      <tags>
        <tag>JNI学习</tag>
      </tags>
  </entry>
  <entry>
    <title>C基础：05.结构体联合体和枚举以及大小计算</title>
    <url>/05.C%E5%9F%BA%E7%A1%80-%E7%BB%93%E6%9E%84%E4%BD%93%E8%81%94%E5%90%88%E4%BD%93%E5%92%8C%E6%9E%9A%E4%B8%BE%E4%BB%A5%E5%8F%8A%E5%A4%A7%E5%B0%8F%E8%AE%A1%E7%AE%97/</url>
    <content><![CDATA[<h1 id="1-结构体的定义和使用"><a href="#1-结构体的定义和使用" class="headerlink" title="1.结构体的定义和使用"></a>1.结构体的定义和使用</h1><h2 id="1-1-结构体的定义"><a href="#1-1-结构体的定义" class="headerlink" title="1.1.结构体的定义"></a>1.1.结构体的定义</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一种方式</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Worker</span>&#123;</span> <span class="comment">// 定义一个结构体， 相当于 java 的 class struct Worker worker; // 初始化，没有初始化的情况下，那么里面所有的属性都是没有初始值的，salary</span></span><br><span class="line">    <span class="type">char</span> name[<span class="number">10</span>];</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">double</span> salary;</span><br><span class="line">&#125;;*/</span><br><span class="line"></span><br><span class="line"><span class="comment">// 第二种方式，可以直接取名字</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Worker</span>&#123;</span> <span class="comment">// 定义一个结构体， 相当于 java 的 class</span></span><br><span class="line">    <span class="type">char</span> name[<span class="number">20</span>];</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">double</span> salary;</span><br><span class="line">&#125;eastrise = &#123;<span class="string">&quot;Eastrise&quot;</span>,<span class="number">25</span>,<span class="number">1000</span>&#125;,jack; <span class="comment">// 这种方式会有一个默认的初始值,jack会有默认初始值</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Work</span>&#123;</span></span><br><span class="line">    <span class="type">char</span> name[<span class="number">10</span>];</span><br><span class="line">    <span class="type">char</span> grade[<span class="number">10</span>];</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 第三种方式，结构体嵌套</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Worker</span>&#123;</span><span class="comment">// 定义一个结构体，相当于 java 的 class</span></span><br><span class="line">	<span class="type">char</span> name[<span class="number">10</span>];</span><br><span class="line">	<span class="type">int</span> age;</span><br><span class="line">	<span class="type">double</span> salary;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 工作， name grade</span></span><br><span class="line">	Work work;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 第四种方式，结构体嵌套</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Worker</span>&#123;</span><span class="comment">// 定义一个结构体，相当于 java 的 class</span></span><br><span class="line">	<span class="type">char</span> name[<span class="number">10</span>];</span><br><span class="line">	<span class="type">int</span> age;</span><br><span class="line">	<span class="type">double</span> salary;</span><br><span class="line"></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">Work</span>&#123;</span></span><br><span class="line">		<span class="type">char</span> name[<span class="number">10</span>];</span><br><span class="line">		<span class="type">char</span> grade[<span class="number">10</span>];</span><br><span class="line">	&#125;work;</span><br><span class="line">	<span class="comment">// 工作， name grade</span></span><br><span class="line">	<span class="comment">// Work work;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h2 id="1-2-结构体的使用"><a href="#1-2-结构体的使用" class="headerlink" title="1.2.结构体的使用"></a>1.2.结构体的使用</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Worker</span>&#123; <span class="comment">// 定义一个结构体， 相当于 java 的 class</span></span><br><span class="line">    <span class="type">char</span> name[<span class="number">20</span>];</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">double</span> salary;</span><br><span class="line">&#125;eastrise = &#123;<span class="string">&quot;Eastrise&quot;</span>,<span class="number">25</span>,<span class="number">1000</span>&#125;,jack; <span class="comment">// 这种方式会有一个默认的初始值,jack会有默认初始值</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 结构体的定义和使用</span></span><br><span class="line"><span class="comment">// char* 不算基本数据类型</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">Worker</span> worker; <span class="comment">// 初始化，没有初始化的情况下，那么里面所有的属性都是没有初始值的，salary</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 赋值</span></span><br><span class="line">    worker.age = <span class="number">24</span>;</span><br><span class="line">    <span class="comment">// worker.name = &quot;Eastrise&quot;;</span></span><br><span class="line">    <span class="built_in">strcpy</span>(worker.name,<span class="string">&quot;JACK&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// jack.name = &quot;Jack&quot;;</span></span><br><span class="line">    <span class="built_in">strcpy</span>(jack.name,<span class="string">&quot;JACK&quot;</span>);</span><br><span class="line">    jack.age = <span class="number">24</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;name = %s, age = %d, salary = %lf&quot;</span>,eastrise.name,eastrise.age,eastrise.salary);</span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">name = Eastrise, age = 25, salary = 1000.000000</span><br></pre></td></tr></table></figure>
<h1 id="2-结构体指针和动态内存开辟"><a href="#2-结构体指针和动态内存开辟" class="headerlink" title="2.结构体指针和动态内存开辟"></a>2.结构体指针和动态内存开辟</h1><h2 id="2-1-通过指针取操作数据"><a href="#2-1-通过指针取操作数据" class="headerlink" title="2.1.通过指针取操作数据"></a>2.1.<strong>通过指针取操作数据</strong></h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Worker</span> <span class="title">worker</span> =</span> &#123;<span class="string">&quot;eastrise&quot;</span>,<span class="number">25</span>&#125;;</span><br><span class="line">    Worker* worker_p = &amp;worker;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 通过结构体指针去操控数据</span></span><br><span class="line">    <span class="comment">// worker.age = 24;</span></span><br><span class="line">    worker_p -&gt; age = <span class="number">24</span>; <span class="comment">// 结构体操作的另外一种方式， 一般这种比较常用</span></span><br><span class="line">    <span class="built_in">strcpy</span>(worker_p-&gt;name,<span class="string">&quot;Jack&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;name = %s, age = %d&quot;</span>,worker_p-&gt;name,worker_p-&gt;age);</span><br><span class="line"></span><br><span class="line">    getchar();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">name = Jack, age = 24</span><br></pre></td></tr></table></figure>
<h2 id="2-2-动态开辟结构体内存"><a href="#2-2-动态开辟结构体内存" class="headerlink" title="2.2.动态开辟结构体内存"></a>2.2.<strong>动态开辟结构体内存</strong></h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Worker</span>&#123; <span class="comment">// 定义一个结构体， 相当于 java 的 class</span></span><br><span class="line">    <span class="type">char</span> name[<span class="number">10</span>];</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">double</span> salary;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//工作, name gradle</span></span><br><span class="line">    <span class="comment">// Work work;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// Worker* worker;// 创建了一个结构体指针， 指针没有初始化？</span></span><br><span class="line">    Worker* worker = (Worker*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(Worker));</span><br><span class="line">    worker-&gt;age = <span class="number">26</span>;</span><br><span class="line">    <span class="built_in">strcpy</span>(worker-&gt;name,<span class="string">&quot;Jamin&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;name = %s, age = %d&quot;</span>,worker-&gt;name,worker-&gt;age);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 释放</span></span><br><span class="line">    <span class="keyword">if</span>(worker)&#123;</span><br><span class="line">        <span class="built_in">free</span>(worker);</span><br><span class="line">        worker = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">name = Jamin, age = 26</span><br></pre></td></tr></table></figure>
<p><strong>结构体的数组</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Worker</span>&#123;</span> <span class="comment">// 定义一个结构体， 相当于 java 的 class</span></span><br><span class="line">    <span class="type">char</span> name[<span class="number">20</span>];</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">double</span> salary;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 结构体的数组</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="comment">// 插件结构体数组，第一种</span></span><br><span class="line"><span class="comment">//    Worker worker[10] = &#123;&#123;&quot;Eastrise&quot;,25,1000&#125;,&#123;&#125;,&#123;&#125;,&#123;&#125;&#125;; // 创建了 10 个，静态开辟</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">//    worker[9] = &#123;&quot;Eastrise&quot;,26,1000&#125;;</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">//    printf(&quot;name = %s ,age = %d\n&quot;,worker[9].name,worker[9].age);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 动态内存开辟 Worker* 结构体的指针</span></span><br><span class="line">    Worker* worker = (Worker*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Worker)*<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">strcpy</span>(worker-&gt;name,<span class="string">&quot;Eastrise&quot;</span>);</span><br><span class="line">    worker-&gt;age = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 对第十个数据进行操作</span></span><br><span class="line">    worker += <span class="number">9</span>;</span><br><span class="line">    <span class="built_in">strcpy</span>(worker-&gt;name,<span class="string">&quot;Eastrise9&quot;</span>);</span><br><span class="line">    worker-&gt;age = <span class="number">9</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;name = %s，age = %d&quot;</span>,worker-&gt;name,worker-&gt;age);</span><br><span class="line">    <span class="built_in">free</span>(worker);</span><br><span class="line"></span><br><span class="line">    getchar();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">name = Eastrise9，age = 9</span><br></pre></td></tr></table></figure>
<h1 id="3-结构体大小计算（字节对齐）"><a href="#3-结构体大小计算（字节对齐）" class="headerlink" title="3.结构体大小计算（字节对齐）"></a>3.结构体大小计算（字节对齐）</h1><p>计算的规则：</p>
<ol>
<li>按照最大的字节去计算 </li>
<li>算得时候只会按照基本数据类型去算</li>
<li>首先会把所有字节数加起来， 是否能够整除最大属性(有其它结构体就按照其它结构体中所有的基本类型数据总和跟本身最大的基本数据类型比较大小)的字节数，如果不够会往上累加，一直加到能整除的位置</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Worker</span>&#123; <span class="comment">// 定义一个结构体， 相当于 java 的 class</span></span><br><span class="line">    <span class="type">char</span> name[<span class="number">10</span>]; <span class="comment">// 10</span></span><br><span class="line">    <span class="type">int</span> age; <span class="comment">// 4</span></span><br><span class="line">    <span class="type">double</span> salary; <span class="comment">// 8</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// char name[10] 24, char name[18] 32</span></span><br><span class="line">    <span class="comment">// 32 怎么来的？ 18 + 4 + 8 = 30, 按照能整除最大的基本数据类型来算：32/8=4</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> size = <span class="built_in">sizeof</span>(Worker); <span class="comment">// 24字节</span></span><br><span class="line">    <span class="comment">// 计算的规则：</span></span><br><span class="line">    <span class="comment">// 1. 按照最大的字节去计算</span></span><br><span class="line">    <span class="comment">// 2. 算得时候只会按照基本数据类型去算</span></span><br><span class="line">    <span class="comment">// 3. 首先会把所有字节数加起来， 是否能够整除最大属性(有其它结构体就按照其它结构体中所有的基本类型数据总和跟本身最大的基本数据类型比较大小)的字节数，如果不够会往上累加，一直加到能整除的位置</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;size = %d \n&quot;</span>,size);</span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">date</span>&#123; <span class="comment">// 12</span></span><br><span class="line">    <span class="type">int</span> year;</span><br><span class="line">    <span class="type">int</span> month;</span><br><span class="line">    <span class="type">int</span> day;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">student</span>&#123;</span><br><span class="line">    <span class="type">int</span> number; <span class="comment">// 4</span></span><br><span class="line">    <span class="type">char</span> sex; <span class="comment">// 1</span></span><br><span class="line">    <span class="type">int</span> age; <span class="comment">// 4</span></span><br><span class="line">    <span class="type">char</span> name[<span class="number">10</span>]; <span class="comment">// 10</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">date</span> birthday; <span class="comment">// 12</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 31%4  32%4 32个字节？</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> size = <span class="built_in">sizeof</span>(student); <span class="comment">// 36字节</span></span><br><span class="line">    <span class="comment">// 计算的规则：</span></span><br><span class="line">    <span class="comment">// 1. 按照最大的字节去计算</span></span><br><span class="line">    <span class="comment">// 2. 算得时候只会按照基本数据类型去算</span></span><br><span class="line">    <span class="comment">// 3. 首先会把所有字节数加起来， 是否能够整除最大属性(有其它结构体就按照其它结构体中所有的基本类型数据总和跟本身最大的基本数据类型比较大小)的字节数的字节数，如果不够会往上累加，一直加到能整除的位置</span></span><br><span class="line">    <span class="comment">// 4. 进阶，留下一个思考</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;size = %d \n&quot;</span>,size);</span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">24</span><br><span class="line"></span><br><span class="line">36</span><br></pre></td></tr></table></figure>
<h1 id="4-结构体和结构体指针取别名"><a href="#4-结构体和结构体指针取别名" class="headerlink" title="4.结构体和结构体指针取别名"></a>4.结构体和结构体指针取别名</h1><p>关键词：<strong>typedef</strong></p>
<p><code>typedef Worker_ Worker_;</code> :好处<strong><code>初始化时可以不用带 struct 关键字了</code></strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> Worker_ Worker_;</span><br><span class="line"><span class="comment">// 对结构体的指针取别名</span></span><br><span class="line"><span class="keyword">typedef</span> Worker_* Worker;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">	Worker worker = (Worker_*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Worker_));</span><br><span class="line"></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;%s,%d,%lf&quot;</span>,worker-&gt;name,worker-&gt;age,worker-&gt;salary);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">,--&gt; 0,---&gt; 0.000000</span><br></pre></td></tr></table></figure>
<h1 id="5-联合体的定义和使用"><a href="#5-联合体的定义和使用" class="headerlink" title="5.联合体的定义和使用"></a>5.联合体的定义和使用</h1><p><strong>联合体(共用体)</strong> 是一种特殊的数据类型，允许您在相同的内存位置存储不同的数据类型。</p>
<p><code>联合体因为是在相同的内存位置存储，所以下面代码中只能存在一个，要么是 age ，要么是 name</code> , 后定义的才存在</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">union</span> <span class="title">Person</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="comment">// 最能出现一个 ，不用累加，找最大值 10，12字节</span></span><br><span class="line">	<span class="type">char</span> name[<span class="number">10</span>]; <span class="comment">// 10 </span></span><br><span class="line">	<span class="type">int</span> age; <span class="comment">// 4 </span></span><br><span class="line">	<span class="type">double</span> salary;<span class="comment">// 8</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">	<span class="comment">// Person person = &#123;&quot;Darren&quot;&#125;;</span></span><br><span class="line">	 Person person;</span><br><span class="line">	 strcpy_s(person.name,<span class="string">&quot;Darren&quot;</span>);</span><br><span class="line">	 person.age = <span class="number">24</span>;</span><br><span class="line">	 <span class="built_in">printf</span>(<span class="string">&quot;name = %s,age = %d&quot;</span>,person.name,person.age); <span class="comment">// 只有 age 才有值，因为他是后定义的 </span></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, <span class="keyword">sizeof</span>(Person)); <span class="comment">// 12</span></span><br><span class="line">	getchar();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">name = ,age = 25</span><br><span class="line">16</span><br></pre></td></tr></table></figure>
<h1 id="6-联合体大小计算"><a href="#6-联合体大小计算" class="headerlink" title="6. 联合体大小计算"></a>6. 联合体大小计算</h1><p><code>不用累加，取的最大值，最终是否够基本数据类型整除</code></p>
<h1 id="7-枚举的定义和使用"><a href="#7-枚举的定义和使用" class="headerlink" title="7.枚举的定义和使用"></a>7.枚举的定义和使用</h1><p><code>枚举有点类似于 int</code>，没有指定的依照前面的数值累加</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">CommentType</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">// 后面的 = 只能是int类型,否则报错：Integral constant expression must have integral or unscoped enumeration type, not &#x27;const char [4]</span></span><br><span class="line">	TEXT = <span class="number">10</span>,TEXT_IMAGE =<span class="number">13</span>,IMAGE</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line"></span><br><span class="line">	CommentType commentType = TEXT;</span><br><span class="line">	CommentType commentType1 = TEXT_IMAGE;</span><br><span class="line">	CommentType commentType2 = IMAGE;</span><br><span class="line">	</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;%d,%d,%d&quot;</span>, commentType, commentType1, commentType2); <span class="comment">// 10,13,14</span></span><br><span class="line"></span><br><span class="line">	getchar();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C知识</category>
      </categories>
      <tags>
        <tag>C学习</tag>
        <tag>C基础</tag>
      </tags>
  </entry>
  <entry>
    <title>JNI:04.缓存策略和异常处理</title>
    <url>/10.JNI%E5%9F%BA%E7%A1%80-%E7%BC%93%E5%AD%98%E7%AD%96%E7%95%A5%E5%92%8C%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<p><strong>代码请看：<a href="https://github.com/EastUp/JavaJNITest/tree/master/src/com/east/jni10">10.JNI基础-缓存策略和异常处理</a></strong></p>
<h1 id="1-数组排序："><a href="#1-数组排序：" class="headerlink" title="1. 数组排序："></a>1. 数组排序：</h1><ul>
<li><code>void qsort(void * _Base, size_t _NumOfElements, size_t _SizeOfElements, int(*_PtFuncCompare)(const void *, const void *))</code> ：c中的排序方法</li>
<li><code>jxxx* = env-&gt;GetXXArrayElements(env,jarray,NULL);</code>：将java层的数组转换成c层的数组（指针）。</li>
<li><code>(*env)-&gt;ReleaseXXArrayElements(env, jarray, xxArray, JNI_OK);</code>：同步数组的数据给 java 数组 xxArray 并不是 jarray。 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// JNI_OK == 0:：既要同步数据给 jarray， 又要释放 intArray</span><br><span class="line">// JNI_COMMIT == 1：会同步数据给 jarray, 但是不会释放 intArray</span><br><span class="line">// JNI_ABORT == 2：不会同步数据给 jarray, 但是会释放 intArray</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>native层：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 4. 定义一个 compare 的方法</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">compare</span><span class="params">(<span class="type">const</span> <span class="type">int</span>* number1, <span class="type">const</span> <span class="type">int</span>* number2)</span>&#123;</span><br><span class="line">	<span class="keyword">return</span> *number1 - *number2;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 数组排序</span></span><br><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_jni10_Sample_sort</span></span><br><span class="line"><span class="params">(JNIEnv* env, jclass jclz, jintArray jarray)</span>&#123;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">// 2. 获取 jintArray 的首地址</span></span><br><span class="line">	jint* intArray = (*env)-&gt;GetIntArrayElements(env,jarray,<span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 3. 获取数组的长度</span></span><br><span class="line">	<span class="type">int</span> length = (*env)-&gt;GetArrayLength(env,jarray);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 1. 对 jarray 进行排序</span></span><br><span class="line">	<span class="comment">// void qsort(void * _Base, size_t _NumOfElements, size_t _SizeOfElements, int(*_PtFuncCompare)(const void *, const void *))</span></span><br><span class="line">	<span class="comment">// 第一个参数：void* 数组的首地址</span></span><br><span class="line">	<span class="comment">// 第二个参数：数组的长度</span></span><br><span class="line">	<span class="comment">// 第三个参数：数组元素数据类型的大小</span></span><br><span class="line">	<span class="comment">// 第四个参数：数组的一个比较方法指针（Comparable）</span></span><br><span class="line">	qsort(intArray,length,<span class="keyword">sizeof</span>(<span class="type">int</span>),compare);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 同步数组的数据给 java 数组 intArray 并不是 jarray， 可以简单理解为 copy</span></span><br><span class="line">	<span class="comment">// JNI_OK == 0:：既要同步数据给 jarray， 又要释放 intArray</span></span><br><span class="line">	<span class="comment">// JNI_COMMIT == 1：会同步数据给 jarray, 但是不会释放 intArray</span></span><br><span class="line">	<span class="comment">// JNI_ABORT == 2：不会同步数据给 jarray, 但是会释放 intArray</span></span><br><span class="line"></span><br><span class="line">	(*env)-&gt;ReleaseIntArrayElements(env, jarray, intArray, JNI_OK);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-局部引用和全局引用"><a href="#2-局部引用和全局引用" class="headerlink" title="2. 局部引用和全局引用"></a>2. 局部引用和全局引用</h1><ul>
<li><strong><code>jobject j_str = (*env)-&gt;NewObject(env,str_clz,jmid)</code></strong>：局部引用对象生成。</li>
<li><strong><code>(*env)-&gt;DeleteLocalRef(env,j_str);</code></strong> ：删除局部引用</li>
<li><strong><code>jstring globalStr =(*env)-&gt;NewGlobalRef(env,value);</code></strong> :创建全局引用</li>
<li><strong>(*env)-&gt;DeleteGlobalRef(env,globalStr)</strong>：删除全局引用</li>
</ul>
<h2 id="2-1-局部引用"><a href="#2-1-局部引用" class="headerlink" title="2.1. 局部引用"></a>2.1. 局部引用</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 局部引用</span></span><br><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_jni10_Sample_localRef</span></span><br><span class="line"><span class="params">(JNIEnv * env, jclass jclz)</span>&#123;</span><br><span class="line">	<span class="comment">// 在 native 层构建的 Java 对象，不用了应该怎么管理（应该进行回收删除）</span></span><br><span class="line">	<span class="comment">// native 层开辟的内存由谁管理，你能开辟多大</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 字符串截取，String 对象</span></span><br><span class="line">	jclass str_clz = (*env)-&gt;FindClass(env,<span class="string">&quot;java/lang/String&quot;</span>);</span><br><span class="line">	jmethodID jmid = (*env)-&gt;GetMethodID(env, str_clz, <span class="string">&quot;&lt;init&gt;&quot;</span>, <span class="string">&quot;()V&quot;</span>);</span><br><span class="line">	jobject j_str = (*env)-&gt;NewObject(env,str_clz,jmid);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 还有 100 行代码</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// jobject 不要再使用了，要回收 javaGC 的源码</span></span><br><span class="line">	(*env)-&gt;DeleteLocalRef(env,j_str);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 删除了就不能再使用了，C 和 C++ 都需要自己释放内存（静态开辟的不需要，动态开辟的需要）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-2-全局引用"><a href="#2-2-全局引用" class="headerlink" title="2.2 全局引用"></a>2.2 全局引用</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">// 全局引用</span></span><br><span class="line">jstring globalStr;</span><br><span class="line"></span><br><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_jni10_Sample_saveGlobalRef</span></span><br><span class="line"><span class="params">(JNIEnv * env, jclass j_clz, jstring value)</span>&#123;</span><br><span class="line">	<span class="comment">// 保存全局引用，其它方法需要用到</span></span><br><span class="line">	globalStr = (*env)-&gt;NewGlobalRef(env,value);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// NewWeakGlobalRef （java 中的软引用很像）无法保证对象不为空</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">JNIEXPORT jstring JNICALL <span class="title function_">Java_com_east_jni10_Sample_getGlobalRef</span></span><br><span class="line"><span class="params">(JNIEnv * env, jclass j_clz)</span>&#123;</span><br><span class="line">	<span class="keyword">return</span> globalStr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 删除全局引用</span></span><br><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_jni10_Sample_deleteGlobalRef</span></span><br><span class="line"><span class="params">(JNIEnv * env, jclass j_clz)</span>&#123;</span><br><span class="line">	(*env)-&gt;DeleteGlobalRef(env,globalStr);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-缓存策略-static-native层有一大堆方法需要去获取-name-属性-初始化全局静态缓存"><a href="#3-缓存策略-static-native层有一大堆方法需要去获取-name-属性-初始化全局静态缓存" class="headerlink" title="3. 缓存策略 static, native层有一大堆方法需要去获取 name 属性 // 初始化全局静态缓存"></a>3. 缓存策略 static, native层有一大堆方法需要去获取 name 属性 // 初始化全局静态缓存</h1><h2 id="3-1-局部静态缓存"><a href="#3-1-局部静态缓存" class="headerlink" title="3.1  局部静态缓存"></a>3.1  局部静态缓存</h2><p>局部静态缓存：即使方法被重复调用也不会反复的给<code>static</code>修饰的字段赋值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_jni10_Sample_staticLocalCache</span></span><br><span class="line"><span class="params">(JNIEnv * env, jclass j_clz, jstring value)</span>&#123;</span><br><span class="line">	<span class="comment">// name 属性，赋值操作</span></span><br><span class="line">	<span class="type">static</span> jfieldID jfid = <span class="literal">NULL</span>; <span class="comment">// 局部静态缓存，即使方法被重复调用也不会反复的去获取 jfieldID</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">if</span> (jfid)&#123;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">&quot;fieldID is not null\n&quot;</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span>&#123;</span><br><span class="line">		jfid = (*env)-&gt;GetStaticFieldID(env, j_clz, <span class="string">&quot;name&quot;</span>, <span class="string">&quot;Ljava/lang/String;&quot;</span>);<span class="comment">// 如果没有静态缓存 这个方法会被多次调用，会去反复的获取 jfieldID</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	(*env)-&gt;SetStaticObjectField(env,j_clz,jfid,value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="3-2-全局静态缓存"><a href="#3-2-全局静态缓存" class="headerlink" title="3.2 全局静态缓存"></a>3.2 全局静态缓存</h2><p>全局静态缓存：即使方法被重复调用也不会反复的给<code>static</code>修饰的字段赋值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 全局静态缓存</span></span><br><span class="line"><span class="type">static</span> jfieldID f_name_id = <span class="literal">NULL</span>;</span><br><span class="line"><span class="type">static</span> jfieldID f_name1_id = <span class="literal">NULL</span>;</span><br><span class="line"><span class="type">static</span> jfieldID f_name2_id = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_jni10_Sample_staticLocalCache</span></span><br><span class="line"><span class="params">(JNIEnv * env, jclass j_clz, jstring value)</span>&#123;</span><br><span class="line">	<span class="comment">// 因为是静态缓存，所以这个方法被反复调用，也不会反复的去获取 jfieldID</span></span><br><span class="line">	(*env)-&gt;SetStaticObjectField(env,j_clz,f_name_id,value);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_jni10_Sample_initStaticCache</span></span><br><span class="line"><span class="params">(JNIEnv *env, jclass j_clz)</span>&#123;</span><br><span class="line">	<span class="comment">// 初始化全局静态缓存</span></span><br><span class="line">	f_name_id = (*env)-&gt;GetStaticFieldID(env, j_clz, <span class="string">&quot;name&quot;</span>, <span class="string">&quot;Ljava/lang/String;&quot;</span>);</span><br><span class="line">	f_name1_id = (*env)-&gt;GetStaticFieldID(env, j_clz, <span class="string">&quot;name1&quot;</span>, <span class="string">&quot;Ljava/lang/String;&quot;</span>);</span><br><span class="line">	f_name2_id = (*env)-&gt;GetStaticFieldID(env, j_clz, <span class="string">&quot;name2&quot;</span>, <span class="string">&quot;Ljava/lang/String;&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-异常处理（简单讲，C-异常）"><a href="#4-异常处理（简单讲，C-异常）" class="headerlink" title="4. 异常处理（简单讲，C++异常）"></a>4. 异常处理（简单讲，C++异常）</h1><ul>
<li><code>jthrowable throwable = (*env)-&gt;ExceptionOccurred(env);</code>：正在抛出一个异常的本地引用</li>
<li><code>(*env)-&gt;ExceptionClear(env);</code>：清除异常</li>
<li><code>(*env)-&gt;Throw(env, throwable);</code>：将ExceptionOccurred获取到的异常直接抛给java层</li>
<li><code>(*env)-&gt;ThrowNew(env, no_such_clz, &quot;NoSuchFieldException name3&quot;);</code>：抛出自己想抛出的异常</li>
</ul>
<p>异常说明：</p>
<ol>
<li><p><code>ExceptionCheck</code> 用于检测如果一个异常已经抛出，则该方法将会返回JNI_TRUE就是typedef定义为1的布尔值。</p>
</li>
<li><p><code>ExceptionOccurred</code> 获取正在抛出一个异常的本地引用，Native或Java层必须处理该异常，并返回一个jthrowable对象。</p>
</li>
<li><p><code>ExceptionDescribe</code> 主要用于打印出异常的错误描述。</p>
</li>
<li><p><code>ExceptionClear</code> 清除刚刚抛出的异常。</p>
</li>
<li><p><code>FatalError</code> 的作用比较特殊，产生一个致命性的错误，Android123提示这样会导致JVM将关闭，就是程序停止运行了，所以使用时要谨慎。</p>
</li>
</ol>
<p>说明下：<code>ExceptionCheck</code> 用法</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">env-&gt;<span class="built_in">FindClass</span>(<span class="string">&quot;Android123CWJ&quot;</span>); <span class="comment">//假设这个类本身不存在</span></span><br><span class="line"> <span class="keyword">if</span>(env-&gt;<span class="built_in">ExceptionCheck</span>())</span><br><span class="line"> &#123;</span><br><span class="line">   env-&gt;<span class="built_in">ExceptionDescribe</span>();</span><br><span class="line">   env-&gt;<span class="built_in">ExceptionClear</span>();</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<p>下面是：<code>ExceptionOccurred</code> 用法</p>
<p>注意： 保存后在抛出异常后记得 <code>return</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 异常处理</span></span><br><span class="line">JNIEXPORT <span class="type">void</span> JNICALL <span class="title function_">Java_com_east_jni10_Sample_exception</span></span><br><span class="line"><span class="params">(JNIEnv *env, jclass j_clz)</span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 假设现在想给 ，name3 赋值</span></span><br><span class="line">	jfieldID jfid = (*env)-&gt;GetStaticFieldID(env, j_clz, <span class="string">&quot;name3&quot;</span>, <span class="string">&quot;Ljava/lang/String;&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 两种方式解决</span></span><br><span class="line">	<span class="comment">// 1. 补救措施， 不拿 name3 拿 name</span></span><br><span class="line">	<span class="comment">// 1.1 有没有异常</span></span><br><span class="line">	jthrowable throwable = (*env)-&gt;ExceptionOccurred(env);</span><br><span class="line">	<span class="comment">/*if (throwable)&#123;</span></span><br><span class="line"><span class="comment">		// 补救措施，先把异常清除</span></span><br><span class="line"><span class="comment">		printf(&quot;native have a exception&quot;);</span></span><br><span class="line"><span class="comment">		// 清除异常</span></span><br><span class="line"><span class="comment">		(*env)-&gt;ExceptionClear(env);</span></span><br><span class="line"><span class="comment">		// 重新获取 name 属性</span></span><br><span class="line"><span class="comment">		jfid = (*env)-&gt;GetStaticFieldID(env, j_clz, &quot;name&quot;, &quot;Ljava/lang/String;&quot;);</span></span><br><span class="line"><span class="comment">	&#125;*/</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2. 想给 java 层抛一个异常</span></span><br><span class="line">	<span class="keyword">if</span> (throwable)&#123;</span><br><span class="line">		<span class="comment">// 清除异常</span></span><br><span class="line">		(*env)-&gt;ExceptionClear(env);</span><br><span class="line">		<span class="comment">// 给 java 层抛 一个 Throwable 异常</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">// 第一种方式，直接把异常抛给 java 层</span></span><br><span class="line">		(*env)-&gt;Throw(env, throwable);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 第二种方式抛异常</span></span><br><span class="line">		<span class="comment">// (*env)-&gt;ThrowNew(env, no_such_clz, &quot;NoSuchFieldException name3&quot;);</span></span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span>; <span class="comment">//  必须 return 如果不的话，程序会接着往下运行，肯定还会crash</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	jstring name = (*env)-&gt;NewStringUTF(env, <span class="string">&quot;Eastrise&quot;</span>);</span><br><span class="line">	(*env)-&gt;SetStaticObjectField(env, j_clz, jfid, name);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>JNI</category>
      </categories>
      <tags>
        <tag>JNI学习</tag>
        <tag>异常</tag>
      </tags>
  </entry>
  <entry>
    <title>JNI:03.Android共享内存的序列化过程</title>
    <url>/09.JNI%E5%9F%BA%E7%A1%80-Android%E5%85%B1%E4%BA%AB%E5%86%85%E5%AD%98%E7%9A%84%E5%BA%8F%E5%88%97%E5%8C%96%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<p><strong>代码请看：<a href="https://github.com/EastUp/NDKPractice/tree/master/jni09">NDKPractice项目的jni09</a></strong></p>
<h1 id="1-面试题："><a href="#1-面试题：" class="headerlink" title="1. 面试题："></a>1. 面试题：</h1><ol>
<li>进程间的通信方式有哪些</li>
<li>binder 和 socket 通信的区别有哪些 <ul>
<li>从传输性能上说 ： <code>Socket</code>,<code>传输效率低</code>，<code>开销大</code>，主要用在<code>跨网络的进程间通信</code>;消息队列和管道采用存储-转发方式.数据先从发送方拷贝到内存开辟的缓存区中，<br>然后再从内核缓存区拷贝到接收方缓存区，至少有两次拷贝过程。<code>Binder</code>只需要拷贝一次（因为内存映射）</li>
<li>从安全性上来说： Socket 没有任何安全措施，完全依赖上层协议来确保。<code>Binder</code> 通过包名和类名在<code>serviceManager</code>中进行查找，<code>Binder.getCallingUID();</code> 就能取得对方UID来检验它的身分等。还可以进行 <code>checkCallingPermission()</code>等</li>
<li>总结:（binder 共享内存，Soket需要copy内存） Socket 远程，本地低速（zygote）</li>
</ul>
</li>
<li>Android 为什么在大部分场景下用 Binder 进行进程间通信 (效率高，开销小，安全性高)</li>
<li>Serializable 和 Parcelable 之间的区别<br> Serializable ：主要是IO操作，消耗较大 ，好处就是 <code>简单</code>。<strong>存储设置中或将对象序列化后通过网络传输时使用</strong><br> Parcelable ：因为原理是<code>共享内存</code>,所以效率高，内存消耗小。<strong>应用程序在内存间数据传输时推荐使用</strong></li>
<li>Parcelable 序列化和反序列化的具体过程</li>
</ol>
<h1 id="2-重点：-Native-构建对象如何与-Java-层对应"><a href="#2-重点：-Native-构建对象如何与-Java-层对应" class="headerlink" title="2. 重点： Native 构建对象如何与 Java 层对应"></a>2. <font color=red>重点：</font> Native 构建对象如何与 Java 层对应</h1><p><img src="/images/09共享内存.png" alt=""></p>
<h2 id="2-1-opencv-Mat-源码阅读"><a href="#2-1-opencv-Mat-源码阅读" class="headerlink" title="2.1 opencv Mat 源码阅读"></a>2.1 opencv Mat 源码阅读</h2><p>Java -&gt; Mat.java = C++ Mat.cpp 对象</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Mat &#123;// Java 对象</span><br><span class="line">    // </span><br><span class="line"></span><br><span class="line">    public final long nativeObj;</span><br><span class="line">    public Mat()&#123;</span><br><span class="line">        nativeObj = n_Mat(rows, cols, type);// c++ 创建一个对象，返回的是 long 类型（c++ 对象的指针地址）</span><br><span class="line">    &#125;</span><br><span class="line">    public static native long n_Mat(int rows, int cols, int type);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>c 层 构建的对象</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">JNIEXPORT jlong JNICALL <span class="title function_">Java_org_opencv_core_Mat_n_1Mat__DDI</span></span><br><span class="line">  <span class="params">(JNIEnv* env, jclass, jdouble size_width, jdouble size_height, jint type)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">static</span> <span class="type">const</span> <span class="type">char</span> method_name[] = <span class="string">&quot;Mat::n_1Mat__DDI()&quot;</span>;</span><br><span class="line">    try &#123;</span><br><span class="line">        LOGD(<span class="string">&quot;%s&quot;</span>, method_name);</span><br><span class="line">        Size <span class="title function_">size</span><span class="params">((<span class="type">int</span>)size_width, (<span class="type">int</span>)size_height)</span>;</span><br><span class="line">        <span class="keyword">return</span> (jlong) new Mat( size, type );</span><br><span class="line">    &#125; catch(<span class="type">const</span> <span class="built_in">std</span>::exception &amp;e) &#123;</span><br><span class="line">        throwJavaException(env, &amp;e, method_name);</span><br><span class="line">    &#125; catch (...) &#123;</span><br><span class="line">        throwJavaException(env, <span class="number">0</span>, method_name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-2-Parcel-java-的源码"><a href="#2-2-Parcel-java-的源码" class="headerlink" title="2.2 Parcel.java 的源码"></a>2.2 Parcel.java 的源码</h2><p><img src="/images/09Parcel原理.png" alt=""></p>
<p>java层 创建对象调用 c 层的创建对象并获取到 c 层的指针（地址）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">private void init(long nativePtr) &#123;</span><br><span class="line">    if (nativePtr != 0) &#123;</span><br><span class="line">        mNativePtr = nativePtr;</span><br><span class="line">        mOwnsNativeParcelObject = false;</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">        // 这里获取 c 层 parcel 对象的 指针（地址）</span><br><span class="line">        mNativePtr = nativeCreate();</span><br><span class="line">        mOwnsNativeParcelObject = true;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>c 层创建 Parcel 对象</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> jlong <span class="title function_">android_os_Parcel_create</span><span class="params">(JNIEnv* env, jclass clazz)</span></span><br><span class="line">&#123;</span><br><span class="line">    Parcel* parcel = new Parcel();</span><br><span class="line">    <span class="keyword">return</span> reinterpret_cast&lt;jlong&gt;(parcel);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>java 层调用 c 层写入Int</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// nativePtr 指针地址</span><br><span class="line">private static native void nativeWriteInt(long nativePtr, int val);</span><br></pre></td></tr></table></figure>
<p>c 层的写入： </p>
<ul>
<li>mData : 共享内存的首地址</li>
<li>mDataPos：内存首地址的当前逻动位置</li>
<li>mDataCapacity：共享内存的总大小</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">template&lt;class T&gt;</span><br><span class="line"><span class="type">status_t</span> <span class="title function_">Parcel::writeAligned</span><span class="params">(T val)</span> &#123;</span><br><span class="line">    COMPILE_TIME_ASSERT_FUNCTION_SCOPE(PAD_SIZE_UNSAFE(<span class="keyword">sizeof</span>(T)) == <span class="keyword">sizeof</span>(T));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((mDataPos+<span class="keyword">sizeof</span>(val)) &lt;= mDataCapacity) &#123;</span><br><span class="line">restart_write:</span><br><span class="line">        *reinterpret_cast&lt;T*&gt;(mData+mDataPos) = val;</span><br><span class="line">        <span class="keyword">return</span> finishWrite(<span class="keyword">sizeof</span>(val));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">status_t</span> err = growData(<span class="keyword">sizeof</span>(val));</span><br><span class="line">    <span class="keyword">if</span> (err == NO_ERROR) <span class="keyword">goto</span> restart_write;</span><br><span class="line">    <span class="keyword">return</span> err;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-手写个Native层的Parcel-cpp，便于理解"><a href="#3-手写个Native层的Parcel-cpp，便于理解" class="headerlink" title="3.手写个Native层的Parcel.cpp，便于理解"></a>3.手写个Native层的Parcel.cpp，便于理解</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;jni.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;malloc.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;com_east_jni09_Parcel.h&quot;</span></span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function">JNIEXPORT jstring JNICALL <span class="title">Java_com_east_jni09_MainActivity_stringFromJNI</span></span></span><br><span class="line"><span class="function"><span class="params">(JNIEnv* env,jobject jobj)</span></span>&#123;</span><br><span class="line">    std::string hello = <span class="string">&quot;Hello from C++&quot;</span>;</span><br><span class="line">    <span class="keyword">return</span> env-&gt;<span class="built_in">NewStringUTF</span>(hello.<span class="built_in">c_str</span>());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 结构体 == C++ 中对象 Parcel.cpp</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Parcel</span>&#123;</span><br><span class="line">    <span class="type">char</span>* mData; <span class="comment">// char 1 共享内存的首地址</span></span><br><span class="line">    <span class="type">int</span> mDataPos = <span class="number">0</span>; <span class="comment">// 必须初始化 结构体没有默认值，除非在 &#125; 后跟上几个默认的子项</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Parcel</span>()&#123;</span><br><span class="line">        mData = <span class="built_in">static_cast</span>&lt;<span class="type">char</span> *&gt;(<span class="built_in">malloc</span>(<span class="number">1024</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">writeInt</span><span class="params">(jint value)</span> </span>&#123;</span><br><span class="line">        *<span class="built_in">reinterpret_cast</span>&lt;<span class="type">int</span>*&gt;(mData+mDataPos) = value; <span class="comment">// 0 ,4</span></span><br><span class="line">        mDataPos += <span class="built_in">sizeof</span>(<span class="type">int</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setDataPosition</span><span class="params">(jint value)</span> </span>&#123;</span><br><span class="line">        mDataPos = value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">jint <span class="title">readInt</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> result = *<span class="built_in">reinterpret_cast</span>&lt;<span class="type">int</span>*&gt;(mData+mDataPos);</span><br><span class="line">        mDataPos += <span class="built_in">sizeof</span>(<span class="type">int</span>);</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="function">JNIEXPORT jlong JNICALL <span class="title">Java_com_east_jni09_Parcel_nativeCreate</span></span></span><br><span class="line"><span class="function">        <span class="params">(JNIEnv * env, jobject jobj)</span></span>&#123;</span><br><span class="line">    Parcel* parcel = <span class="keyword">new</span> <span class="built_in">Parcel</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">reinterpret_cast</span>&lt;jlong&gt;(parcel);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="function">JNIEXPORT <span class="type">void</span> JNICALL <span class="title">Java_com_east_jni09_Parcel_nativeWriteInt</span></span></span><br><span class="line"><span class="function">        <span class="params">(JNIEnv * env, jobject jobj,jlong nativePtr,jint value)</span></span>&#123;</span><br><span class="line">    Parcel* parcel = <span class="built_in">reinterpret_cast</span>&lt;Parcel*&gt;(nativePtr);</span><br><span class="line">    parcel -&gt; <span class="built_in">writeInt</span>(value);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="function">JNIEXPORT <span class="type">void</span> JNICALL <span class="title">Java_com_east_jni09_Parcel_nativeSetDataPosition</span></span></span><br><span class="line"><span class="function">        <span class="params">(JNIEnv * env, jobject jobj,jlong nativePtr,jint value)</span></span>&#123;</span><br><span class="line">    Parcel* parcel = <span class="built_in">reinterpret_cast</span>&lt;Parcel*&gt;(nativePtr);</span><br><span class="line">    parcel -&gt; <span class="built_in">setDataPosition</span>(value);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span>  <span class="function">JNIEXPORT jint JNICALL <span class="title">Java_com_east_jni09_Parcel_nativeReadInt</span></span></span><br><span class="line"><span class="function">        <span class="params">(JNIEnv * env, jobject jobj,jlong nativePtr)</span></span>&#123;</span><br><span class="line">    Parcel* parcel = <span class="built_in">reinterpret_cast</span>&lt;Parcel*&gt;(nativePtr);</span><br><span class="line">    <span class="keyword">return</span> parcel-&gt;<span class="built_in">readInt</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>java 层调用：</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> parcel = Parcel()</span><br><span class="line">       parcel.writeInt(<span class="number">12</span>)</span><br><span class="line">       parcel.writeInt(<span class="number">24</span>)</span><br><span class="line"></span><br><span class="line">       parcel.setDataPosition(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">       <span class="keyword">val</span> number1 = parcel.readInt()</span><br><span class="line">       <span class="keyword">val</span> number2 = parcel.readInt()</span><br><span class="line"></span><br><span class="line">       Log.e(<span class="string">&quot;TAG&quot;</span>,<span class="string">&quot;number1 = <span class="variable">$number1</span> , number2 = <span class="variable">$number2</span>&quot;</span>) <span class="comment">// 12,24</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>JNI</category>
      </categories>
      <tags>
        <tag>JNI学习</tag>
        <tag>Android</tag>
        <tag>面试</tag>
      </tags>
  </entry>
  <entry>
    <title>C进阶：07.内存四驱模型</title>
    <url>/11.C%E8%BF%9B%E9%98%B6-%E5%86%85%E5%AD%98%E5%9B%9B%E9%A9%B1%E6%A8%A1%E5%9E%8B/</url>
    <content><![CDATA[<h1 id="1-数组作为参数传递时获取不到长度的问题！！"><a href="#1-数组作为参数传递时获取不到长度的问题！！" class="headerlink" title="1. 数组作为参数传递时获取不到长度的问题！！"></a>1. 数组作为参数传递时获取不到长度的问题！！</h1><p>原因：数组作为参数传递，会退化成为一个指针，传递的是首地址 （高效）</p>
<h1 id="2-数据类型剖析"><a href="#2-数据类型剖析" class="headerlink" title="2. 数据类型剖析"></a>2. 数据类型剖析</h1><p>数据类型的本质：<code>一块连续大小的内存空间</code><br>数据类型的别名：int32_t<br>void指针数据类型：<code>void*</code> 代表任意的数据类型的指针  </p>
<h1 id="3-变量的本质"><a href="#3-变量的本质" class="headerlink" title="3. 变量的本质"></a>3. 变量的本质</h1><ul>
<li>变量的本质：固定内存块大小（一块连续大小的内存空间）的别名，通过变量可以去操作一块内存上的数据</li>
<li>变量的三要素：内存大小，名称，作用域</li>
</ul>
<h1 id="4-内存四驱模型"><a href="#4-内存四驱模型" class="headerlink" title="4. 内存四驱模型"></a>4. 内存四驱模型</h1><h2 id="4-1-程序运行的流程："><a href="#4-1-程序运行的流程：" class="headerlink" title="4.1 程序运行的流程："></a>4.1 程序运行的流程：</h2><ol>
<li>操作系统会把物理硬盘上的代码 load 到内存</li>
<li>加载到内存后会把 c 代码分成 4 个区</li>
<li>然后系统会找到 main 函数的入口去执行</li>
</ol>
<h2 id="4-2-四驱模型"><a href="#4-2-四驱模型" class="headerlink" title="4.2  四驱模型"></a>4.2  四驱模型</h2><ol>
<li>栈区：由编译器自动分配的，存放一些局部变量值和函数，这个里面内存是会自动进行回收的</li>
<li>堆区：一般都是由我们自己去开辟的，这个里面的内存需要手动进行释</li>
<li>全局区：静态的一些常量，字符串 等等</li>
<li>程序代码区：存放的是函数体的二进制代码</li>
</ol>
<h1 id="5-栈的开口方向"><a href="#5-栈的开口方向" class="headerlink" title="5. 栈的开口方向"></a>5. 栈的开口方向</h1><ul>
<li>debug模式下：向下</li>
<li>release模式下：向上</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">10</span>;</span><br><span class="line">    <span class="type">int</span> b = <span class="number">10</span>;</span><br><span class="line">    <span class="comment">// 0508 , 0496  a 的地址要大于 b 的地址 （debug）</span></span><br><span class="line">    <span class="comment">// 016  , 020   a 的地址要小于 b 的地址 （release）</span></span><br><span class="line">    <span class="comment">// buffer 数据类型跟栈的开口方向无关 （内部存储都是向上）</span></span><br><span class="line"></span><br><span class="line">    <span class="type">char</span> buff[<span class="number">10</span>];</span><br><span class="line">    <span class="comment">// buff+1 -&gt; buff</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%p , %p , %p , %p&quot;</span>,&amp;a,&amp;b,buff,buff+<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> a1 = <span class="number">100</span>;</span><br><span class="line">    <span class="type">int</span>* ff=  &amp;a1; <span class="comment">// 虽说是 a1 的地址，但是我也可以把 ff 看做一个数组</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// char** 二位数组 char** 二级指针</span></span><br><span class="line"></span><br><span class="line">    ff += <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    getchar();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="6-指针强化"><a href="#6-指针强化" class="headerlink" title="6. 指针强化"></a>6. 指针强化</h1><font color=red>**一个指针的大小始终是32位系统占4个字节，64位系统占8字节**</font>

<p>指针为什么要有类型：</p>
<ol>
<li>为了地址增量的方便：例如double型的地址增量为8，而int型指针的地址增量为4</li>
<li>为了类型安全：帮助编译器发现你是否用了错误的类型</li>
</ol>
<p><code>指针是以内存地址作为变量地址</code></p>
]]></content>
      <categories>
        <category>C知识</category>
      </categories>
      <tags>
        <tag>C学习</tag>
        <tag>C进阶</tag>
      </tags>
  </entry>
  <entry>
    <title>性能优化：1.App启动优化(源码)</title>
    <url>/1.App%E5%90%AF%E5%8A%A8%E4%BC%98%E5%8C%96(%E6%BA%90%E7%A0%81)/</url>
    <content><![CDATA[<h1 id="1-系统的启动"><a href="#1-系统的启动" class="headerlink" title="1.系统的启动"></a>1.系统的启动</h1><ol>
<li>打开电源  引导芯片代码加载引导程序Boot Loader到RAM中去执行</li>
<li>BootLoader把操作系统拉起来</li>
<li>Linux 内核启动开始系统设置，找到一个init.rc文件启动初始化进程</li>
<li>init进程初始化和启动属性服务，之后开启Zygote进程</li>
<li>Zygote开始创建JVM并注册JNI方法，开启SystemServer</li>
<li>启动Binder线程沲和SystemServiceManager,并启动各种服务</li>
<li>AMS启动Launcher</li>
</ol>
<p><img src="/images/系统的启动流程.png" alt=""></p>
<h1 id="2-Launcher启动应用程序"><a href="#2-Launcher启动应用程序" class="headerlink" title="2.Launcher启动应用程序"></a>2.Launcher启动应用程序</h1><p>当我们用手点击一个图标时，就到了这个类<code>public final class Launcher extends Activity</code>,<br>类路径在<code>E:\tools\android-src\android-6.0.1_r1\packages\apps\Launcher2\src\com\android\launcher2\Launcher.java</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">执行`onClick(View view)`方法，会把这个应用的相关信息传入  </span><br><span class="line">先获取一个intent---&gt;startActivitySafely(v, intent, tag)--&gt;startActivity(v, intent, tag);--&gt;startActivity(intent);</span><br><span class="line">startActivity(intent)会开一个APP进程</span><br><span class="line"></span><br><span class="line">ActivityThread.java的main方法作为入口     用attach开启app    再加载application和activity</span><br><span class="line">thread.attach(false);---&gt;mgr.attachApplication(mAppThread)会通过远端进程去回调private void handleBindApplication(AppBindData data)</span><br><span class="line">Application app = data.info.makeApplication()----创建Application对象</span><br><span class="line">mInstrumentation.callApplicationOnCreate(app);---- Application调用onCreate方法</span><br></pre></td></tr></table></figure>
<p><img src="/images/新应用的启动.png" alt=""></p>
<h1 id="3-adb一些命令说明"><a href="#3-adb一些命令说明" class="headerlink" title="3.adb一些命令说明"></a>3.adb一些命令说明</h1><ol>
<li>通过<code>adb shell dumpsys activity activities</code>查看当前的activity名</li>
<li>查看进程   <code>adb shell ps</code></li>
</ol>
<h1 id="4-黑白屏问题"><a href="#4-黑白屏问题" class="headerlink" title="4.黑白屏问题"></a>4.黑白屏问题</h1><p>原因：</p>
<pre><code>    在res/values/styles.xml文件中  
    白屏   `&lt;style name=&quot;AppTheme&quot; parent=&quot;Theme.AppCompat.Light&quot;&gt;`
    黑屏   ` &lt;style name=&quot;AppTheme&quot;&gt;（在以前的老版本上有效，现在的版本默认使用透明处理了）`
</code></pre><p>伪解决办法：</p>
<pre><code>    1.在自己的&lt;style name=&quot;AppTheme&quot; parent=&quot;Theme.AppCompat.Light&quot;&gt;中加入windowsbackground，可用于打广告
    2.设置windowbackground为透明的  &lt;item name=&quot;android:windowIsTranslucent&quot;&gt;true&lt;/item&gt;
    但这2种方法会有个问题，所有的activity启动都会显示
    3.单独做成一个主题
    &lt;style name=&quot;AppTheme.Launcher&quot;&gt;
            &lt;item name=&quot;android:windowBackground&quot;&gt;@drawable/bg&lt;/item&gt;
    &lt;/style&gt;
    再在功能清单中的单独activity下设置
    &lt;activity
          android:theme=&quot;@style/AppTheme.Launcher&quot;
    然后在程序中使用setTheme(R.style.AppTheme)
    让APP中所有的activity还是使用以前的样式，这样做就只有启动时才使用自己的样式
</code></pre><h1 id="5-启动时间查看"><a href="#5-启动时间查看" class="headerlink" title="5.启动时间查看"></a>5.启动时间查看</h1><p>4.4以前 <code>adb shell am start -W com.lqr.wechat/com.lqr.wechat.activity.SplashActivity</code></p>
<pre><code>ThisTime:最后一个启动的Activity的启动耗时；
TotalTime:自己的所有Activity的启动耗时；
WaitTime: ActivityManagerService启动App的Activity时的总时间（包括当前Activity的onPause()和自己Activity的启动）

AM路径：E:\tools\android-src\android-6.0.1_r1\frameworks\base\cmds\am\src\com\android\commands\am
Am.java   946行开始打印启动时间信息
其中一个result对象，在871行初始化 result = mAm.startActivityAndWait(。。。）
在这个初始化时就已经进行了时间的计算：
在android-src\android-6.0.1_r1\frameworks\base\services\core\java\com\android\server\am\ActivityRecord.java文件中计算
void windowsDrawnLocked() ---&gt;reportLaunchTimeLocked(SystemClock.uptimeMillis())中完成时间的统计;
</code></pre><p>4.4版本以后Logcat输入<code>Display</code>筛选系统日志  <code>不过滤信息No Filters</code></p>
<h1 id="6-TRACE工具分析代码执行时间"><a href="#6-TRACE工具分析代码执行时间" class="headerlink" title="6.TRACE工具分析代码执行时间"></a>6.TRACE工具分析代码执行时间</h1><h1 id="6-1-代码"><a href="#6-1-代码" class="headerlink" title="6.1.代码"></a>6.1.代码</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Debug.startMethodTracing(filePath);</span><br><span class="line">中间为需要统计执行时间的代码</span><br><span class="line">Debug.stopMethodTracing();</span><br></pre></td></tr></table></figure>
<h1 id="6-2-分析"><a href="#6-2-分析" class="headerlink" title="6.2. 分析"></a>6.2. 分析</h1><p><code>adb pull /storage/emulated/0/app1.trace</code>把文件拉出来分析，把pull到电脑上的文件拖到AS中就可以分析了</p>
<h1 id="6-3-优化方案"><a href="#6-3-优化方案" class="headerlink" title="6.3. 优化方案"></a>6.3. 优化方案</h1><ol>
<li>开线程  <code>没建handler    没操作UI   对异步要求不高</code></li>
<li>懒加载  <code>用到的时候再初始化，如网络，数据库操作</code></li>
</ol>
]]></content>
      <categories>
        <category>性能优化</category>
      </categories>
      <tags>
        <tag>源码</tag>
      </tags>
  </entry>
  <entry>
    <title>JNI:05.ndk调试</title>
    <url>/11.JNI%E5%9F%BA%E7%A1%80-ndk%E8%B0%83%E8%AF%95/</url>
    <content><![CDATA[<h1 id="1-ndk-stack-调试"><a href="#1-ndk-stack-调试" class="headerlink" title="1. ndk-stack 调试"></a>1. <code>ndk-stack</code> 调试</h1><p>条件：配置 <code>adb</code> 和 <code>ndk</code> 的环境变量.<br>命令：<code>adb logcat | ndk-stack -sym E:\AndroidStudioProjects\MyBintrayLib\NDKPractice\datastructure28bubbkeselectsort\build\intermediates\cmake\debug\obj\arm64-v8a</code> // 路径为有.so库的路径</p>
<p>如果出现：<code>&#39;ndk-stack&#39; 不是内部或外部命令，也不是可运行的程序</code>,则需要去ndk 目录下点击 <code>ndk-gdb.cmd</code>,再关闭程序重新打开尝试</p>
<h1 id="2-addr2line"><a href="#2-addr2line" class="headerlink" title="2. addr2line"></a>2. <code>addr2line</code></h1><p>条件：配置addr2line的环境变量：<br><code>D:\AndroidSdk\ndk\21.2.6472646\toolchains\aarch64-linux-android-4.9\prebuilt\windows-x86_64\bin</code></p>
<p>命令：<br><code>aarch64-linux-android-addr2line -e E:\AndroidStudioProjects\MyBintrayLib\NDKPractice\datastructure28bubbkeselectsort\build\intermediates\cmake\debu
g\obj\arm64-v8a\libnative-lib.so 000000000000fdb8 // 000000000000fdb8是(详细错误)</code></p>
<p>命令最后一位的地址编码请看：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">2020</span><span class="number">-07</span><span class="number">-02</span> <span class="number">16</span>:<span class="number">38</span>:<span class="number">57.553</span> <span class="number">18029</span><span class="number">-18029</span>/? A/DEBUG: backtrace:</span><br><span class="line"><span class="number">2020</span><span class="number">-07</span><span class="number">-02</span> <span class="number">16</span>:<span class="number">38</span>:<span class="number">57.554</span> <span class="number">18029</span><span class="number">-18029</span>/? A/DEBUG:     #<span class="number">00</span> pc <span class="number">000000000006</span>a784  /system/lib64/libc.<span class="built_in">so</span> (tgkill<span class="number">+8</span>)</span><br><span class="line"><span class="number">2020</span><span class="number">-07</span><span class="number">-02</span> <span class="number">16</span>:<span class="number">38</span>:<span class="number">57.554</span> <span class="number">18029</span><span class="number">-18029</span>/? A/DEBUG:     #<span class="number">01</span> pc <span class="number">000000000001</span>db50  /system/lib64/libc.<span class="built_in">so</span> (abort<span class="number">+88</span>)</span><br><span class="line"><span class="number">2020</span><span class="number">-07</span><span class="number">-02</span> <span class="number">16</span>:<span class="number">38</span>:<span class="number">57.554</span> <span class="number">18029</span><span class="number">-18029</span>/? A/DEBUG:     #<span class="number">02</span> pc <span class="number">000000000002532</span>c  /system/lib64/libc.<span class="built_in">so</span> (__libc_fatal<span class="number">+116</span>)</span><br><span class="line"><span class="number">2020</span><span class="number">-07</span><span class="number">-02</span> <span class="number">16</span>:<span class="number">38</span>:<span class="number">57.554</span> <span class="number">18029</span><span class="number">-18029</span>/? A/DEBUG:     #<span class="number">03</span> pc <span class="number">00000000000</span>91c6c  /system/lib64/libc.<span class="built_in">so</span> (ifree<span class="number">+812</span>)</span><br><span class="line"><span class="number">2020</span><span class="number">-07</span><span class="number">-02</span> <span class="number">16</span>:<span class="number">38</span>:<span class="number">57.554</span> <span class="number">18029</span><span class="number">-18029</span>/? A/DEBUG:     #<span class="number">04</span> pc <span class="number">00000000000</span>91eec  /system/lib64/libc.<span class="built_in">so</span> (je_free<span class="number">+120</span>)</span><br><span class="line"><span class="comment">// 主要是以下这行，复制他的地址编码到命令中</span></span><br><span class="line"><span class="number">2020</span><span class="number">-07</span><span class="number">-02</span> <span class="number">16</span>:<span class="number">38</span>:<span class="number">57.554</span> <span class="number">18029</span><span class="number">-18029</span>/? A/DEBUG:     #<span class="number">05</span> pc <span class="number">000000000000</span>fdb8  /data/app/com.east.datastructure28bubbkeselectsort-Rq_huZaLg_y9lQEFGFjFuw==/lib/arm64/libnative-lib.<span class="built_in">so</span> (Java_com_east_datastructure28bubbkeselectsort_MainActivity_stringFromJNI<span class="number">+192</span>)</span><br><span class="line"><span class="number">2020</span><span class="number">-07</span><span class="number">-02</span> <span class="number">16</span>:<span class="number">38</span>:<span class="number">57.554</span> <span class="number">18029</span><span class="number">-18029</span>/? A/DEBUG:     #<span class="number">06</span> pc <span class="number">000000000000</span>f09c  /data/app/com.east.datastructure28bubbkeselectsort-Rq_huZaLg_y9lQEFGFjFuw==/oat/arm64/base.<span class="built_in">odex</span> (offset <span class="number">0xf000</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure></p>
<p>坑: <code>如果addr2line的时候，报错 File format not recognized</code></p>
<p>解决:<br>　　1. <font color=red>addr2line的环境变量修改为 arm-linux-androideabi-4.9的：</font><code>D:\AndroidSdk\ndk\21.2.6472646\toolchains\arm-linux-androideabi-4.9\prebuilt\windows-x86_64\bin</code><br>　　<br>　　2. 命令修改为：<code>arm-linux-androideabi-addr2line -e E:\AndroidStudioProjects\MyBintrayLib\NDKPractice\datastructure28bubbkeselectsort\build\intermediates\cmake\debu
g\obj\arm64-v8a\libnative-lib.so 000000000000fdb8 // 000000000000fdb8是(详细错误)</code></p>
]]></content>
      <categories>
        <category>JNI</category>
      </categories>
      <tags>
        <tag>JNI学习</tag>
        <tag>异常</tag>
        <tag>ndk调试</tag>
      </tags>
  </entry>
  <entry>
    <title>C进阶：10.结构体赋值、套指针、深浅拷贝、大小</title>
    <url>/14.C%E8%BF%9B%E9%98%B6-%E7%BB%93%E6%9E%84%E4%BD%93%E8%B5%8B%E5%80%BC%E3%80%81%E5%A5%97%E6%8C%87%E9%92%88%E3%80%81%E6%B7%B1%E6%B5%85%E6%8B%B7%E8%B4%9D%E3%80%81%E5%A4%A7%E5%B0%8F/</url>
    <content><![CDATA[<h1 id="1-结构体赋值操作"><a href="#1-结构体赋值操作" class="headerlink" title="1. 结构体赋值操作"></a>1. 结构体赋值操作</h1><p><strong><code>结构体赋值相当于是（内容）赋值操作</code></strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">Student stu1 = &#123;<span class="string">&quot;Eastrise&quot;</span>,<span class="number">25</span>&#125;;</span><br><span class="line"></span><br><span class="line">Student stu2;</span><br><span class="line"></span><br><span class="line"><span class="comment">// strcpy(stu2.name,stu1.name);</span></span><br><span class="line"><span class="comment">// stu2.age = stu1.age;</span></span><br><span class="line"></span><br><span class="line">stu2 = stu1;<span class="comment">// = 赋值操作，java 中 stu2 对象就会变成 stu1</span></span><br><span class="line"><span class="comment">// 在 c 中相当于是（内容）赋值操作，是把 stu1 的值赋值给 stu2;</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%p,%p\n&quot;</span>,stu1,stu2);</span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s,%d&quot;</span>,stu2.name,stu2.age);</span><br></pre></td></tr></table></figure>
<h1 id="2-结构体套指针"><a href="#2-结构体套指针" class="headerlink" title="2. 结构体套指针"></a>2. 结构体套指针</h1><p>注意：  </p>
<ol>
<li><code>结构体套指针，我们必须对结构体指针进行初始化（赋值）</code></li>
<li><code>如果需要调用 strcpy 给结构体的char* 赋值就必须要开辟内存</code></li>
<li><code>将指针指向常量区的中Eastrise（OK）</code></li>
</ol>
<p>指针占多少个字节：  </p>
<ul>
<li>一个指针在<code>64位</code>的计算机上，占<code>8</code>个字节；</li>
<li>一个指针在<code>32位</code>的计算机上，占<code>4</code>个字节。</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">char</span>* name; <span class="comment">// 定义一个 name 指针，name 是可以给任何char*值，结构体的大小怎么算,64位计算机上8字节，32位上4个字节</span></span><br><span class="line">    <span class="type">int</span> age; <span class="comment">// 4个字节</span></span><br><span class="line">&#125;Student;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    Student stu;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// stu.name = &quot;Eastrise&quot;;// 将指针指向常量区的中Eastrise（OK）</span></span><br><span class="line"></span><br><span class="line">    stu.name = (<span class="type">char</span>*)<span class="built_in">malloc</span>(<span class="number">100</span>); <span class="comment">// 如果需要调用 strcpy 给结构体的char* 赋值就必须要开辟内存</span></span><br><span class="line">    <span class="comment">// 结构体套指针，我们必须对结构体指针进行初始化（赋值）</span></span><br><span class="line">    <span class="comment">// 指针可以指向任何地址，但在64位计算机上它的大小永远是 8 个字节，在32位计算机上它的大小永远是 4 字节</span></span><br><span class="line">    <span class="built_in">strcpy</span>(stu.name,<span class="string">&quot;Eastrise&quot;</span>);<span class="comment">// 往 stu.name 中拷贝数据 Eastrise（错误，必须先进行开辟内存才能拷贝）</span></span><br><span class="line"></span><br><span class="line">    stu.age = <span class="number">24</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s,%d,%d&quot;</span>,stu.name,stu.age,<span class="keyword">sizeof</span>(Student)); <span class="comment">// 16个字节？？因为要字节对齐，是8的倍数所以是 16</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Eastrise,24,16</span><br></pre></td></tr></table></figure>
<h1 id="3-深拷贝和浅拷贝"><a href="#3-深拷贝和浅拷贝" class="headerlink" title="3. 深拷贝和浅拷贝"></a>3. 深拷贝和浅拷贝</h1><ul>
<li><code>浅拷贝（shallowCopy）</code>：只是增加了一个指针指向已存在的内存地址，</li>
<li><code>深拷贝（deepCopy）</code>：是增加了一个指针并且申请了一个新的内存，使这个增加的指针指向这个新的内存，</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">char</span>* name; <span class="comment">// 定义一个 name 指针，name 是可以给任何char*值，结构体的大小怎么算,64位计算机上8字节，32位上4个字节</span></span><br><span class="line">    <span class="type">int</span> age; <span class="comment">// 4个字节</span></span><br><span class="line">&#125;Student;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">copyTo</span><span class="params">(Student* from,Student* to)</span>&#123;</span><br><span class="line">    *to = *from; <span class="comment">// 指针的赋值运算 是一个浅拷贝，都指向的是同一块内存地址</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 非得用，就将指针进行深拷贝（系统架构原型设计模式里面）</span></span><br><span class="line">    to-&gt;name = <span class="built_in">malloc</span>(<span class="number">100</span>); <span class="comment">// 开辟一块内存</span></span><br><span class="line">    <span class="built_in">strcpy</span>(to-&gt;name,from-&gt;name); <span class="comment">// 值的拷贝。</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    Student stu1;</span><br><span class="line"></span><br><span class="line">    stu1.name = (<span class="type">char</span>*)<span class="built_in">malloc</span>(<span class="number">100</span>);</span><br><span class="line">    <span class="built_in">strcpy</span>(stu1.name,<span class="string">&quot;Eastrise&quot;</span>);</span><br><span class="line">    stu1.age = <span class="number">24</span>;</span><br><span class="line"></span><br><span class="line">    Student stu2;</span><br><span class="line">    stu2.name = <span class="string">&quot;Jack&quot;</span>;</span><br><span class="line">    stu2.age = <span class="number">24</span>;</span><br><span class="line"></span><br><span class="line">    copyTo(&amp;stu1,&amp;stu2); <span class="comment">// 用方法进行拷贝，有问题吗？</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s,%d&quot;</span>,stu2.name,stu2.age);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 释放内存</span></span><br><span class="line">    <span class="keyword">if</span>(stu1.name)&#123;</span><br><span class="line">        <span class="built_in">free</span>(stu1.name);</span><br><span class="line">        stu1.name = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(stu2.name)&#123;</span><br><span class="line">        <span class="built_in">free</span>(stu2.name); <span class="comment">// 浅拷贝会释放出错</span></span><br><span class="line">        stu2.name = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Eastrise,24</span><br></pre></td></tr></table></figure>
<h1 id="4-结构体的偏移量强化"><a href="#4-结构体的偏移量强化" class="headerlink" title="4. 结构体的偏移量强化"></a>4. 结构体的偏移量强化</h1><p>结构体大小的偏移量问题：</p>
<ol>
<li>获取内存元素的偏移量 &amp;(stu-&gt;name) - (int)stu</li>
<li>获取结构体的大小 sizeof(student)</li>
<li>大小怎么来的，根据每个的偏移量计算来的 ， 结构体的大小跟内存位置摆放有关</li>
<li><code>把最小的字节数尽量往后靠</code>，结构体一旦定义了不要轻易的去挪动，因为我们只要知道内部任何一个变量的地址，那么就知道了整个结构体</li>
</ol>
<p>大部分情况下：内部偏移量 字节对齐 是针对后面的这个数据来看的</p>
]]></content>
      <categories>
        <category>C知识</category>
      </categories>
      <tags>
        <tag>C学习</tag>
        <tag>C进阶</tag>
      </tags>
  </entry>
  <entry>
    <title>C进阶：08.指针赋值、野指针、项目开发模型强化</title>
    <url>/12.C%E8%BF%9B%E9%98%B6-%E5%9B%9B%E9%A9%B1%E6%A8%A1%E5%9E%8B%E3%80%81%E9%87%8E%E6%8C%87%E9%92%88%E3%80%81%E9%A1%B9%E7%9B%AE%E5%BC%80%E5%8F%91%E6%A8%A1%E5%9E%8B%E5%BC%BA%E5%8C%96/</url>
    <content><![CDATA[<h1 id="1-内存四驱模型"><a href="#1-内存四驱模型" class="headerlink" title="1. 内存四驱模型"></a>1. 内存四驱模型</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">change1</span><span class="params">(<span class="type">int</span> number)</span></span>&#123; <span class="comment">// 不能修改</span></span><br><span class="line">    number = <span class="number">300</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%p\n&quot;</span>,&amp;number);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 修改 a 的值</span></span><br><span class="line"></span><br><span class="line">    a = <span class="number">200</span>;</span><br><span class="line">    <span class="built_in">change1</span>(a); <span class="comment">// a变量的值复制给了 number 变量，number 是在另一个函数中，number 是一个新的变量</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;change1：a = %d\n&quot;</span>,a); <span class="comment">// a = 200</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// getchar();</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/images/12.1间接赋值.png" alt=""></p>
<h1 id="2-指针间接赋值的意义"><a href="#2-指针间接赋值的意义" class="headerlink" title="2. 指针间接赋值的意义"></a>2. 指针间接赋值的意义</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">change2</span><span class="params">(<span class="type">int</span>* number)</span></span>&#123; <span class="comment">//  可以</span></span><br><span class="line">    *number = <span class="number">300</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">100</span>;</span><br><span class="line">    <span class="built_in">change2</span>(&amp;a); <span class="comment">//  a 的地址复制给了 number 变量，number 是在另一个函数中，number 是一个新的变量</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;change2：a = %d\n&quot;</span>,a); <span class="comment">// a = 300;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// getchar();</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/images/12.2通过指针间接赋值.png" alt=""></p>
<p>在做开发的时候，离不开指针，指针出现的意义有很多，其中一种就是通过方法去修改值 :</p>
<ol>
<li>获取Bitmap信息</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">AndroidBitmapInfo android_bitmap_info; <span class="comment">// 结构体</span></span><br><span class="line">AndroidBitmap_getInfo(env,bitmap,&amp;android_bitmap_info);<span class="comment">// 通过 getInfo 方法去获取 Bitmap 的属性</span></span><br><span class="line"><span class="comment">// 获取Bitmap的 宽，高，format</span></span><br><span class="line"><span class="type">int</span> bitmap_width = android_bitmap_info.width;</span><br><span class="line"><span class="type">int</span> bitmap_height = android_bitmap_info.height;</span><br></pre></td></tr></table></figure>
<p><code>c 和 java 的方法不同所在，c 可以传对象的地址在方法里面去赋值，java 一般都是通过返回值</code></p>
<p>java 写法：<br>　　<code>AndroidBitmapInfo android_bitmap_info = AndroidBitmap_getInfo(env,bitmap);</code></p>
<h1 id="3-野指针和-null-地址"><a href="#3-野指针和-null-地址" class="headerlink" title="3. 野指针和 null 地址"></a>3. 野指针和 null 地址</h1><p><code>野指针</code>:指 释放了指针对应的内存地址后，没有把指针置为 NULL</p>
<p><code>NULL</code> : NULL 相当于指向 <code>0x00000000</code>,这块地方是 c 和 c++ 编译器所持有的，不能在这块区域赋值</p>
<p>strcpy(p,”string”);<br>p=”string”;<br><code>第一个是把string赋值给p，第二个是p指向“string”的首地址。</code></p>
<p><img src="/images/12.3野指针.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 写入位置 0x00000000 时发生访问冲突</span></span><br><span class="line">    <span class="type">char</span>* p1 = <span class="literal">NULL</span>; <span class="comment">// p1 = NULL,NUll 也是地方，指针指向 NULL 相当于指向 0x00000000</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 但是我们不能对 0x00000000 去操作，这块地方是 c 和 c++ 编译器所持有的</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Student student = null; 不用纠结 null 是啥，跟 c 和 c++ 有点类似</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">strcpy</span>(p1,<span class="string">&quot;1122&quot;</span>); <span class="comment">// 把 1122 赋值给p1 NUll的位置，这是错误的</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;p1 = %s&quot;</span>,p1); <span class="comment">// Null的地址不能操作</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-字符串强化"><a href="#4-字符串强化" class="headerlink" title="4. 字符串强化"></a>4. 字符串强化</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="comment">// char buff[100] = &#123;&#x27;e&#x27;,&#x27;a&#x27;,&#x27;s&#x27;,&#x27;t&#x27;,&#x27;r&#x27;,&#x27;i&#x27;,&#x27;s&#x27;,&#x27;e&#x27;&#125;;// 后面 8 - 99 都是默认值 0</span></span><br><span class="line">    <span class="comment">// char buff[5] = &#123;&#x27;e&#x27;,&#x27;a&#x27;,&#x27;s&#x27;,&#x27;t&#x27;,&#x27;r&#x27;&#125;;</span></span><br><span class="line">    <span class="comment">// char buff[] = &#123;&#x27;e&#x27;,&#x27;a&#x27;,&#x27;s&#x27;,&#x27;t&#x27;,&#x27;r&#x27;&#125;; // 长度是 12（&#x27;\0&#x27;）, size 是 5（默认统计里面的个数）</span></span><br><span class="line">    <span class="comment">// char buff[2] = &#123;&#x27;e&#x27;,&#x27;a&#x27;,&#x27;s&#x27;,&#x27;t&#x27;,&#x27;r&#x27;&#125;; // 编译不通过，长度超出</span></span><br><span class="line">    <span class="comment">// char buff[100] = &#123; 0 &#125;; // 把数组初始化为 0</span></span><br><span class="line">    <span class="comment">// char buff[100] 数据都是默认值 -52</span></span><br><span class="line"></span><br><span class="line">      <span class="type">char</span> buff[] = <span class="string">&quot;123456&quot;</span>; <span class="comment">// len 是 6（&#x27;\0&#x27;）,size 是 7</span></span><br><span class="line">    <span class="comment">// 相当于 char buff[] = &#123;1,2,3,4,5,6,\0&#125;</span></span><br><span class="line"><span class="comment">//    char* buff= &quot;123456&quot;; // len==6, size==8（64位系统指针占8字节）</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 纠结一下 char buff[] = &quot;123456&quot; 和 char* buff= &quot;123456&quot;; malloc 的方式 啥区别 ？</span></span><br><span class="line">    <span class="comment">// 字符串可以在任何地方开辟内存，栈区，堆区，常量区</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 大小 size 100</span></span><br><span class="line">    <span class="type">int</span> len = <span class="built_in">strlen</span>(buff);<span class="comment">// len 5 碰到 &#x27;\0&#x27; 就结束了</span></span><br><span class="line">    <span class="type">int</span> size = <span class="keyword">sizeof</span>(buff);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;len = %d , size = %d\n&quot;</span>,len,size);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 内容</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d,%d,%d,%d&quot;</span>,buff[<span class="number">0</span>],buff[<span class="number">66</span>],buff[<span class="number">99</span>],buff[<span class="number">77</span>]);</span><br><span class="line"></span><br><span class="line">    getchar();</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/images/12.4字符串的不同之处.png" alt=""></p>
<h1 id="5-项目开发模型强化"><a href="#5-项目开发模型强化" class="headerlink" title="5. 项目开发模型强化"></a>5. 项目开发模型强化</h1><ol>
<li>确定你的参数，传递指针</li>
<li>一定要考虑健壮性</li>
<li>要异常错误进行抛出说明 萤视频 ffmpeg</li>
<li>不要直接轻易的去改变调用传递给你的指针</li>
</ol>
]]></content>
      <categories>
        <category>C知识</category>
      </categories>
      <tags>
        <tag>C学习</tag>
        <tag>C进阶</tag>
      </tags>
  </entry>
  <entry>
    <title>C++：02.类的构造函数、析构函数、拷贝函数</title>
    <url>/16.C++%E5%9F%BA%E7%A1%80-%E7%B1%BB%E7%9A%84%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E3%80%81%E6%9E%90%E6%9E%84%E5%87%BD%E6%95%B0%E3%80%81%E6%8B%B7%E8%B4%9D%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<h1 id="面试题：finalize-finally-final-之间的区别"><a href="#面试题：finalize-finally-final-之间的区别" class="headerlink" title="面试题：finalize , finally, final 之间的区别"></a>面试题：finalize , finally, final 之间的区别</h1><p><code>finalize</code> : java中垃圾回收回收该对象的时候会调用的方法   (c 中的析构函数)  </p>
<p><code>finally</code>： finally作为异常处理的一部分，它只能用在try/catch语句中，并且附带一个语句块，表示这段语句最终一定会被执行（不管有没有抛出异常,即使try语句块中有<code>return</code>）</p>
<p><code>final</code> ：  在java中，final可以用来修饰类，方法和变量（成员变量或局部变量）。  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">修饰类：当用final修饰类的时，表明该类不能被其他类所继承。</span><br><span class="line">修饰方法：当用final修饰方法时，把方法锁定，以防止继承类对其进行更改不能被重写。（注意：若父类中final方法的访问权限为private，</span><br><span class="line">        将导致子类中不能直接继承该方法，因此，此时可以在子类中定义相同方法名的函数，此时不会与重写final的矛盾，而是在子类中重新地定义了新方法。）</span><br><span class="line">修饰变量：final成员变量表示常量，只能被赋值一次，赋值后其值不再改变。类似于C++中的const。</span><br></pre></td></tr></table></figure>
<h1 id="1-构造函数（可以有很多）"><a href="#1-构造函数（可以有很多）" class="headerlink" title="1. 构造函数（可以有很多）"></a>1. 构造函数（可以有很多）</h1><h2 id="1-1-定义"><a href="#1-1-定义" class="headerlink" title="1.1.定义"></a>1.1.定义</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 构造函数</span></span><br><span class="line">    <span class="built_in">Student</span>()&#123;<span class="comment">// 空参构造函数</span></span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;空参构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*Student(char* name):age(0)&#123;//  一个参数构造函数，相当于 this-&gt;age = 0;</span></span><br><span class="line"><span class="comment">        cout &lt;&lt; &quot;一个参数构造函数&quot; &lt;&lt; endl;</span></span><br><span class="line"><span class="comment">        // this-&gt;name = name;</span></span><br><span class="line"><span class="comment">        // this-&gt;age = age;</span></span><br><span class="line"><span class="comment">    &#125;*/</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">Student</span>(<span class="type">char</span>* name):<span class="built_in">Student</span>(name,<span class="number">0</span>)&#123; <span class="comment">// 调用两个参数的构造函数，注意：先会调用两个参数的构造函数，然后才会执行当前构造函数</span></span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;一个参数构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">Student</span>(<span class="type">char</span>* name,<span class="type">int</span> age)&#123; <span class="comment">// 两个参数构造函数</span></span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;两个参数构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = (<span class="type">char</span>*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="type">char</span>)*<span class="number">100</span>);</span><br><span class="line">        <span class="built_in">strcpy</span>(<span class="keyword">this</span>-&gt;name,name);</span><br><span class="line">        <span class="comment">// this-&gt;name = name; // 这样赋值不行</span></span><br><span class="line">        <span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 析构函数，如果有在对象内部开辟堆内存，可以在析构函数中释放内存</span></span><br><span class="line">    ~<span class="built_in">Student</span>()&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;析构函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">        <span class="comment">// 临终遗言：对象被回收的时候会被调用</span></span><br><span class="line">        <span class="comment">// 释放内存</span></span><br><span class="line">        <span class="built_in">free</span>(<span class="keyword">this</span>-&gt;name);</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 拷贝构造函数，对象会有一个默认的拷贝构造函数，用来对象之间赋值</span></span><br><span class="line">    <span class="built_in">Student</span>(<span class="type">const</span> Student&amp; stu)&#123; <span class="comment">// 常量的引用</span></span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;拷贝构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">        <span class="comment">// this-&gt;name = stu.name;// 浅拷贝</span></span><br><span class="line">        <span class="comment">// 如果动态开辟内存，一定要采用深拷贝</span></span><br><span class="line">        <span class="keyword">this</span>-&gt;name = (<span class="type">char</span>*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="type">char</span>)*<span class="number">100</span>);</span><br><span class="line">        <span class="built_in">strcpy</span>(<span class="keyword">this</span>-&gt;name,stu.name);</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = stu.age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">char</span>* name;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getAge</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">char</span>* <span class="title">getName</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setAge</span><span class="params">(<span class="type">int</span> age)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setName</span><span class="params">(<span class="type">char</span>* name)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h2 id="1-2-调用"><a href="#1-2-调用" class="headerlink" title="1.2.调用"></a>1.2.调用</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. 默认调用空参的构造函数</span></span><br><span class="line">Student stu;</span><br><span class="line">stu.<span class="built_in">setAge</span>(<span class="number">24</span>);</span><br><span class="line">stu.<span class="built_in">setName</span>(<span class="string">&quot;Eastrise&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 调用两参构造函数</span></span><br><span class="line"><span class="function">Student <span class="title">stu</span><span class="params">(<span class="string">&quot;Eastrise&quot;</span>,<span class="number">24</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3.用 new 关键字，返回的是一个 Student 的一级指针</span></span><br><span class="line">Student* stu = <span class="keyword">new</span> <span class="built_in">Student</span>(<span class="string">&quot;eastrise&quot;</span>,<span class="number">25</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 4.用 malloc 的方式，并没有调用空参的构造函数</span></span><br><span class="line">Student* stu = (Student*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(Student));</span><br><span class="line">stu-&gt;<span class="built_in">setAge</span>(<span class="number">25</span>);</span><br><span class="line">stu-&gt;<span class="built_in">setName</span>(<span class="string">&quot;eastrise&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 构造函数相互调用</span></span><br><span class="line">Student* stu = <span class="keyword">new</span> <span class="built_in">Student</span>(<span class="string">&quot;eastrise&quot;</span>); <span class="comment">// 会先调用两参的再调用一参的</span></span><br></pre></td></tr></table></figure>
<h1 id="2-析构函数（只能有一个）"><a href="#2-析构函数（只能有一个）" class="headerlink" title="2. 析构函数（只能有一个）"></a>2. 析构函数（只能有一个）</h1><p><code>析构函数</code>:<code>如果有在对象内部开辟堆内存，可以在析构函数中释放内存</code></p>
<p><strong>如果是 <code>new</code> 的对象，则必须调用 <code>delete</code> 才会调用到<code>析构函数</code></strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">    <span class="comment">// 2. 析构函数，如果有在对象内部开辟堆内存，可以在析构函数中释放内存</span></span><br><span class="line">    ~<span class="built_in">Student</span>()&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;析构函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">        <span class="comment">// 临终遗言：对象被回收的时候会被调用</span></span><br><span class="line">        <span class="comment">// 释放内存</span></span><br><span class="line">        <span class="built_in">free</span>(<span class="keyword">this</span>-&gt;name);</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Student* stu = <span class="keyword">new</span> <span class="built_in">Student</span>(<span class="string">&quot;eastrise&quot;</span>);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; stu-&gt;<span class="built_in">getName</span>() &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; stu-&gt;<span class="built_in">getAge</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">delete</span>(stu);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-malloc、free、new、delete-区别"><a href="#3-malloc、free、new、delete-区别" class="headerlink" title="3.malloc、free、new、delete 区别"></a>3.malloc、free、new、delete 区别</h1><ol>
<li>malloc/free 他们是一套， new/delete 它们是一套</li>
<li>malloc/free 不会去调用构造函数和析构函数</li>
<li>new/delete 会调用构造函数和析构函数</li>
</ol>
<h1 id="4-拷贝构造函数和调用场景"><a href="#4-拷贝构造函数和调用场景" class="headerlink" title="4. 拷贝构造函数和调用场景"></a>4. 拷贝构造函数和调用场景</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct Person&#123;</span><br><span class="line">    int age;</span><br><span class="line">    char* name;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">int main()&#123;</span><br><span class="line">    Person person1 = &#123;25,&quot;east&quot;&#125;;</span><br><span class="line"></span><br><span class="line">    Person person2 = person1;// = 是赋值，把里面所有定义的属性赋值，c/c++ 编译器帮我们做的</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; &amp;person1 &lt;&lt; &quot; , &quot; &lt;&lt; &amp;person2 &lt;&lt; endl; // 地址不一样，是深拷贝</span><br><span class="line"></span><br><span class="line">    person1.name = &quot;sss&quot;;</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; person2.name &lt;&lt; &quot; , &quot; &lt;&lt; person2.age &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>总结：<font color=red>非指针运算的 = 应该都是深拷贝</font></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 4.拷贝构造函数，对象会有一个默认的拷贝构造函数，用来对象之间的赋值</span></span><br><span class="line"><span class="built_in">Student</span>(<span class="type">const</span> Student&amp; stu)&#123;<span class="comment">// 常量的引用</span></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;拷贝构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">	<span class="comment">// this-&gt;name = stu.name;// 浅拷贝</span></span><br><span class="line">	<span class="comment">// 如果动态开辟内存，一定要采用深拷贝</span></span><br><span class="line">	<span class="keyword">this</span>-&gt;name = (<span class="type">char</span>*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="type">char</span>)* <span class="number">100</span>);</span><br><span class="line">	<span class="built_in">strcpy</span>(<span class="keyword">this</span>-&gt;name, stu.name);</span><br><span class="line">	<span class="keyword">this</span>-&gt;age = stu.age;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/images/16拷贝构造函数赋值.png" alt=""></p>
<ol>
<li><p>= 会调用拷贝构造函数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Student <span class="title">stu1</span><span class="params">(<span class="string">&quot;Darren&quot;</span>, <span class="number">24</span>)</span></span>;</span><br><span class="line">Student stu2 = stu1; <span class="comment">// = 是赋值，把里面所有定义的属性赋值，c/c++ 编译器帮我们做的，其实会调用对象的拷贝构造</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">// 以下方式不会调用拷贝构造函数</span></span><br><span class="line">Student stu2;<span class="comment">// 声明变量，开辟变量内存</span></span><br><span class="line">stu2 = stu1; <span class="comment">// 这个不会去调用拷贝构造函数，但是会赋值 c 的类似</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>第二种场景 作为参数返回的时候会调用拷贝构造函数</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Student <span class="title">getStudent</span><span class="params">(<span class="type">char</span>* name)</span></span>&#123;</span><br><span class="line">    <span class="function">Student <span class="title">stu</span><span class="params">(name)</span></span>;<span class="comment">// 栈 ，方法执行完，这个对象会被回收，但是发现调用了拷贝构造函数</span></span><br><span class="line">    cout &lt;&lt; &amp;stu &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> stu;<span class="comment">// 会返回一个新的 Student 对象，而栈内开辟的 stu 是会被回收 </span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Student stu = <span class="built_in">getStudent</span>(<span class="string">&quot;Jack&quot;</span>);</span><br><span class="line">cout &lt;&lt; &amp;stu &lt;&lt; endl;</span><br></pre></td></tr></table></figure>
<ol>
<li><p>第三种场景 作为参数传递的时候会调用拷贝构造函数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">printStudent</span><span class="params">(Student stu)</span></span>&#123;<span class="comment">// stu 是该方法栈中一个新的对象，拷贝构造函数赋值，方法执行完会调用析构函数</span></span><br><span class="line">    cout &lt;&lt; stu.<span class="built_in">getName</span>() &lt;&lt; <span class="string">&quot; , &quot;</span> &lt;&lt; stu.<span class="built_in">getAge</span>() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">Student <span class="title">stu</span><span class="params">(<span class="string">&quot;Darren&quot;</span>, <span class="number">24</span>)</span></span>;</span><br><span class="line"><span class="built_in">printStudent</span>(stu);</span><br></pre></td></tr></table></figure>
</li>
</ol>
</li>
</ol>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>C++学习</tag>
      </tags>
  </entry>
  <entry>
    <title>C进阶：09.二级指针和多级指针</title>
    <url>/13.C%E8%BF%9B%E9%98%B6-%E4%BA%8C%E7%BA%A7%E6%8C%87%E9%92%88%E5%92%8C%E5%A4%9A%E7%BA%A7%E6%8C%87%E9%92%88/</url>
    <content><![CDATA[<h1 id="1-指针使用的注意事项"><a href="#1-指针使用的注意事项" class="headerlink" title="1. 指针使用的注意事项"></a>1. 指针使用的注意事项</h1><ol>
<li>不要轻易改变指针的位置</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span>* p = <span class="literal">NULL</span>;  </span><br><span class="line"><span class="built_in">strcpy</span>(p,<span class="string">&quot;string&quot;</span>);  <span class="comment">//  报错，无效（需要先动态给p开辟内存，或者定义时char p[num]这样使用才有效）</span></span><br><span class="line">p=<span class="string">&quot;string&quot;</span>;  <span class="comment">// 可以  </span></span><br></pre></td></tr></table></figure>
<p><code>第一个是把string赋值给p，第二个是p指向“string”的首地址。</code></p>
<h1 id="2-const-关键字"><a href="#2-const-关键字" class="headerlink" title="2. const 关键字"></a>2. const 关键字</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 常量（创建一个指针还是可以改变的）</span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> number = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 常量指针(常量指针的值是不能被修改的)</span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span>* numberP = &amp;number;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 指针常量（指针常量的地址是不能被修改的）</span></span><br><span class="line"><span class="type">int</span>* <span class="type">const</span> numberP = &amp;number2;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 值不能修改，指针也不能修改</span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> *<span class="type">const</span> numberP = &amp;number2;</span><br></pre></td></tr></table></figure>
<h1 id="3-二级指针的内存模式"><a href="#3-二级指针的内存模式" class="headerlink" title="3. 二级指针的内存模式"></a>3. 二级指针的内存模式</h1><h2 id="3-1-指针数组"><a href="#3-1-指针数组" class="headerlink" title="3.1 指针数组"></a>3.1 指针数组</h2><p><code>指针数组</code>：数组指针指向的是数组元素的首地址</p>
<h2 id="3-2-二维数组的几种定义方式"><a href="#3-2-二维数组的几种定义方式" class="headerlink" title="3.2 二维数组的几种定义方式"></a>3.2 二维数组的几种定义方式</h2><p>第一种定义方式：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span>* name[<span class="number">10</span>] = &#123;<span class="string">&quot;east&quot;</span>,<span class="string">&quot;rose&quot;</span>,<span class="string">&quot;jack&quot;</span>&#125;;<span class="comment">// 后面的默认指向的是 NULL 地址，都是在静态常量区</span></span><br><span class="line"></span><br><span class="line"><span class="type">char</span>** name = &#123;<span class="string">&quot;east&quot;</span>,<span class="string">&quot;rose&quot;</span>,<span class="string">&quot;jack&quot;</span>&#125;; <span class="comment">// 这种定义方式有问题,直接去赋值那么 c 和 c++ 会把这哥们当做 二级指针。而不是指针数组</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>第二种定义的方式：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> name[<span class="number">10</span>][<span class="number">30</span>] = &#123;<span class="string">&quot;east&quot;</span>,<span class="string">&quot;rose&quot;</span>,<span class="string">&quot;jack&quot;</span>&#125;; <span class="comment">// 这个几个数据都是从静态常量区copy 到 栈的 buffer里面的</span></span><br></pre></td></tr></table></figure>
<p>第三种定义方式（最常见的一种方式）动态开辟，不理解记住套路：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">print</span><span class="params">(<span class="type">char</span>** str,<span class="type">int</span> len)</span>&#123;</span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(; i&lt; len; i++)&#123;</span><br><span class="line"><span class="comment">//        printf(&quot;%s\n&quot;,str[i]); // [] 也相当于挪动指针</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>,*(str+i));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="type">int</span> number = <span class="number">4</span>;</span><br><span class="line">    <span class="type">char</span>** params = (<span class="type">char</span>**)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="type">char</span>*)*number); <span class="comment">// 开辟二位数组</span></span><br><span class="line">    <span class="comment">// 开辟一维数组</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i =<span class="number">0</span>; i&lt;number; i++)&#123;</span><br><span class="line">        params[i] = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="type">char</span>)*<span class="number">100</span>); <span class="comment">// 不会去开辟一维数组</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 写一些数据</span></span><br><span class="line">        <span class="built_in">sprintf</span>(params[i],<span class="string">&quot;i = %d&quot;</span>,i);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    print(params,number);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 释放数组</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i =<span class="number">0</span>; i&lt;number; i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(params[i])&#123;</span><br><span class="line">            <span class="built_in">free</span>(params[i]);</span><br><span class="line">            params[i] = <span class="literal">NULL</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 然后再释放 params 二维数组  (也可以看做一维数组，只不过这个一维数组里面存放的是元素是 一级数组指针)</span></span><br><span class="line">    <span class="keyword">if</span>(params)&#123;</span><br><span class="line">        <span class="built_in">free</span>(params);</span><br><span class="line">        params = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-多级指针"><a href="#4-多级指针" class="headerlink" title="4. 多级指针"></a>4. 多级指针</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">initParams</span><span class="params">(<span class="type">char</span>*** params,<span class="type">int</span> number)</span>&#123; <span class="comment">// 三级指针就出来了，最多也就二级指针（数组指针）</span></span><br><span class="line">    <span class="comment">// 开辟内存去赋值</span></span><br><span class="line">    <span class="type">char</span>** tempParams = (<span class="type">char</span>**)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="type">char</span>*)*number); <span class="comment">// 开辟二位数组</span></span><br><span class="line">    <span class="comment">// 开辟一维数组</span></span><br><span class="line">    <span class="type">int</span> i =<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(;i&lt;number; i++)&#123;</span><br><span class="line">        tempParams[i] = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="type">char</span>)*<span class="number">50</span>); <span class="comment">// 不会去开辟一维数组</span></span><br><span class="line">        <span class="built_in">sprintf</span>(tempParams[i],<span class="string">&quot;i = %d&quot;</span>,i);</span><br><span class="line">    &#125;</span><br><span class="line">    *params = tempParams;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="comment">// 我想给字符串数组开辟内存，开辟内存写到另一个方法</span></span><br><span class="line">    <span class="type">char</span>** params = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="type">int</span> number = <span class="number">4</span>;</span><br><span class="line">    initParams(&amp;params,number);</span><br><span class="line"></span><br><span class="line">    print(params,number);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 释放</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C知识</category>
      </categories>
      <tags>
        <tag>C学习</tag>
        <tag>C进阶</tag>
      </tags>
  </entry>
  <entry>
    <title>C++：01.引用和函数重载</title>
    <url>/15.C++%E5%9F%BA%E7%A1%80-%E5%BC%95%E7%94%A8%E5%92%8C%E5%87%BD%E6%95%B0%E9%87%8D%E8%BD%BD/</url>
    <content><![CDATA[<h1 id="1-c-和-c-的区别"><a href="#1-c-和-c-的区别" class="headerlink" title="1. c 和 c++ 的区别"></a>1. c 和 c++ 的区别</h1><ol>
<li>c++ 代码中可以混编 c 代码，c++ 里面写 c 代码也可以调用</li>
<li>c++ 面向对象（java）, c 面向过程</li>
<li>很多的开源框架都是用 c++ 写的（大部分基于 c++）</li>
</ol>
<p>打印：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span> <span class="comment">// c++ 的包</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std; <span class="comment">// 命名的空间， java中的内部类</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Hello World&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-常量"><a href="#2-常量" class="headerlink" title="2. 常量"></a>2. 常量</h1><p>c++ 中 通过指针修改常量，编译都不会通过。（在C中可以）</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> number = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// number = 20;</span></span><br><span class="line"></span><br><span class="line">     <span class="type">int</span>* numberP = &amp;number; <span class="comment">//  c++中不能通过地址去修改常量值（报错），但是某些编译器上面能通过，但是也不能修改值。</span></span><br><span class="line">    <span class="comment">// c 是能干修改的，在 c 中可以说是一个伪命题</span></span><br><span class="line"></span><br><span class="line">     *numberP = <span class="number">20</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-引用和常量引用"><a href="#3-引用和常量引用" class="headerlink" title="3. 引用和常量引用"></a>3. 引用和常量引用</h1><h2 id="3-1-引用"><a href="#3-1-引用" class="headerlink" title="3.1 引用"></a>3.1 引用</h2><p><strong><code>int &amp;b = a;</code></strong>：b 是 a 的引用可以直接根据地址改变a的数据</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">swap</span><span class="params">(<span class="type">int</span> &amp;number1,<span class="type">int</span> &amp;number2)</span></span>&#123; <span class="comment">// 引用</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;swap number1的地址&quot;</span> &lt;&lt; &amp;number1 &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> temp = <span class="number">0</span>;</span><br><span class="line">    temp = number1;</span><br><span class="line">    number1 = number2;</span><br><span class="line">    number2 = temp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> a  = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// int b = a; // 这个地址不一样</span></span><br><span class="line">    <span class="type">int</span> &amp;b = a; <span class="comment">// 给 a 指向的内存取另外一个别，名 b</span></span><br><span class="line"></span><br><span class="line">    b = <span class="number">20</span>;</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;a的地址&quot;</span> &lt;&lt; &amp;a &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;b的地址&quot;</span> &lt;&lt; &amp;b &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;a的值&quot;</span> &lt;&lt; a &lt;&lt;endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;b的值&quot;</span> &lt;&lt; b &lt;&lt;endl;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 交换值</span></span><br><span class="line">    <span class="type">int</span> number1 = <span class="number">10</span>;</span><br><span class="line">    <span class="type">int</span> number2 = <span class="number">20</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 引用，四驱模型值的拷贝，引用其实就是地址赋值，可以看成同一块内存的另外一个变量</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;main number1的地址&quot;</span> &lt;&lt; &amp;number1 &lt;&lt;endl;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">swap</span>(number1,number2);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;number1 = &quot;</span> &lt;&lt; number1 &lt;&lt; <span class="string">&quot;, number2 = &quot;</span> &lt;&lt; number2 &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a的地址0x61fe04</span><br><span class="line">b的地址0x61fe04</span><br><span class="line">a的值20</span><br><span class="line">b的值20</span><br><span class="line">main number1的地址0x61fe00</span><br><span class="line">swap number1的地址0x61fe00</span><br><span class="line">number1 = 20, number2 = 10</span><br></pre></td></tr></table></figure>
<p><img src="/images/15引用实例.png" alt=""></p>
<h2 id="3-2-常量引用"><a href="#3-2-常量引用" class="headerlink" title="3.2 常量引用"></a>3.2 常量引用</h2><p><code>const Student &amp;stu</code>：签名加一个const关键字就变成了只读，不能修改里面的任何值</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span>&#123;</span></span><br><span class="line">    <span class="type">char</span> name[<span class="number">20</span>];</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">&#125; Student;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">insertStu</span><span class="params">(<span class="type">const</span> Student &amp;stu)</span> &#123;<span class="comment">// &amp;stu  不想改，常量引用</span></span><br><span class="line">    <span class="comment">// 可以修改 stu 的值</span></span><br><span class="line">    <span class="comment">// strcpy(stu.name,&quot;Jack&quot;); // 引用加了const 所以不能修改</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 就变成了只读</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; stu.name &lt;&lt; <span class="string">&quot;,&quot;</span> &lt;&lt; stu.age &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-函数重载和默认参数"><a href="#4-函数重载和默认参数" class="headerlink" title="4. 函数重载和默认参数"></a>4. 函数重载和默认参数</h1><p> <code>c 不允许函数的重载,c++ 中可以</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> number1, <span class="type">int</span> number2 = <span class="number">200</span>, <span class="type">bool</span> cache = <span class="number">0</span>)</span> </span>&#123; <span class="comment">// number2 = 200 默认的参数 kotlin 很像</span></span><br><span class="line">    <span class="keyword">return</span> number1 + number2;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果在C中会报错：错误 1 error C2084：函数 &quot;int add(int,int)&quot; 已有主题， c 不允许函数的重载,c++ 中可以</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> number1, <span class="type">int</span> number2, <span class="type">int</span> number3)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> number1 + number2 + number3;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-类的初探"><a href="#5-类的初探" class="headerlink" title="5. 类的初探"></a>5. 类的初探</h1><ol>
<li>class 定义类，跟 java 几乎类似</li>
<li>真正的开发过程中我们的 cpp 或者 c 文件，最终 dll 或者 so 库供调用者使用，所以为了确保类能够正常被调用，我们一般需要定义 .h 头文件</li>
<li>实现方法的使用要用命名空间  xxx类名::xxx方法</li>
</ol>
<p>头文件：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123; <span class="comment">// 结构体类似</span></span><br><span class="line"><span class="keyword">private</span>: <span class="comment">// 私有，包装，影响下面所有的属性</span></span><br><span class="line">    <span class="type">char</span>* name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setAge</span><span class="params">(<span class="type">int</span> age)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setName</span><span class="params">(<span class="type">char</span>* name)</span></span>;</span><br><span class="line">    <span class="comment">// private： // 影响到下面的所有方法</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getAge</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">char</span>* <span class="title">getName</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>实现文件：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 写实现，定义了另外一个方法</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::setAge</span><span class="params">(<span class="type">int</span> age)</span></span>&#123; <span class="comment">// 实现 Student 的 setAge 方法</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Student::setName</span><span class="params">(<span class="type">char</span>* name)</span></span>&#123;</span><br><span class="line">    <span class="keyword">this</span> -&gt; name = name;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">Student::getAge</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>-&gt;age;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">char</span>* <span class="title">Student::getName</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>-&gt;name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++学习</tag>
      </tags>
  </entry>
  <entry>
    <title>C++：05.虚继承、虚函数、模板函数</title>
    <url>/19.C++%E5%9F%BA%E7%A1%80-%E8%99%9A%E7%BB%A7%E6%89%BF%E3%80%81%E8%99%9A%E5%87%BD%E6%95%B0%E3%80%81%E6%A8%A1%E6%9D%BF%E5%87%BD%E6%95%B0%E3%80%81%E6%A8%A1%E6%9D%BF%E7%B1%BB/</url>
    <content><![CDATA[<h1 id="1-属性初始化"><a href="#1-属性初始化" class="headerlink" title="1. 属性初始化"></a>1. 属性初始化</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>: <span class="keyword">public</span> Person&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// char* courseName;</span></span><br><span class="line">    Course course;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Student</span>(<span class="type">char</span>* name,<span class="type">int</span> age,<span class="type">char</span>* courseName):<span class="built_in">Person</span>(name,age),<span class="built_in">course</span>(courseName)&#123; <span class="comment">// 初始化父类的属性</span></span><br><span class="line">        <span class="comment">// this-&gt;courseName = courseName;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span></span>&#123;</span><br><span class="line">        cout  &lt;&lt; name &lt;&lt; <span class="string">&quot;,&quot;</span> &lt;&lt; age &lt;&lt; <span class="string">&quot;,&quot;</span> &lt;&lt; course._name() &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h1 id="2-多继承，（在-java-里面是不允许多继承的，-c-是可以的-但是你也不能有二义性（歧义））"><a href="#2-多继承，（在-java-里面是不允许多继承的，-c-是可以的-但是你也不能有二义性（歧义））" class="headerlink" title="2. 多继承，（在 java 里面是不允许多继承的， c++ 是可以的, 但是你也不能有二义性（歧义））"></a>2. 多继承，（在 java 里面是不允许多继承的， c++ 是可以的, 但是你也不能有二义性（歧义））</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Person</span>(<span class="type">char</span> *name) &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">char</span> *_name() &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Child</span> &#123;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Child</span>(<span class="type">int</span> age) &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> _age() &#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> : <span class="keyword">public</span> Person, <span class="keyword">public</span> Child &#123; <span class="comment">// 多继承，并没有实现</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Student</span>(<span class="type">char</span> *name, <span class="type">int</span> age) : <span class="built_in">Person</span>(name), <span class="built_in">Child</span>(age) &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h1 id="3-虚继承（二义性）"><a href="#3-虚继承（二义性）" class="headerlink" title="3. 虚继承（二义性）"></a>3. 虚继承（二义性）</h1><p>作用：<code>解决二义性</code>，<code>确保继承过来的相同属性或者函数，只存在一份拷贝</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span> : <span class="keyword">virtual</span> <span class="keyword">public</span> A &#123; <span class="comment">// 确保继承过来的相同属性或者函数，只存在一份拷贝</span></span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span> : <span class="keyword">virtual</span> <span class="keyword">public</span> A &#123;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">D</span> : <span class="keyword">public</span> B, <span class="keyword">public</span> C &#123;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    D d;</span><br><span class="line">    d.name = <span class="string">&quot;east&quot;</span>; <span class="comment">// 如果不用虚继承，会报错：不明确 name 是 B 的 还是 C 的</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-c-中的多态"><a href="#4-c-中的多态" class="headerlink" title="4. c++ 中的多态"></a>4. c++ 中的多态</h1><p><code>c++</code> 中的多态默认情况下<code>不存在</code>,如果需要使用多态，一定要加上<code>virtual</code> 关键字</p>
<p>c++中多态分两种：  </p>
<ol>
<li>动态多态：子父类继承</li>
<li>静态多态：方法的重载</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Activity</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 如果没有 virtual 关键字就没有多态</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">onCreate</span><span class="params">()</span> </span>&#123; <span class="comment">// 支持多态，虚函数</span></span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Activity 中的 onCreate&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="keyword">public</span> Activity &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">onCreate</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;MainActivity 中的 onCreate&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>调用：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Activity *activity1 = <span class="keyword">new</span> <span class="built_in">MainActivity</span>();</span><br><span class="line"><span class="built_in">startActivity</span>(activity1);</span><br></pre></td></tr></table></figure>
<h1 id="5-纯虚函数（类似于：java-中的-抽象类，接口）"><a href="#5-纯虚函数（类似于：java-中的-抽象类，接口）" class="headerlink" title="5. 纯虚函数（类似于：java 中的 抽象类，接口）"></a>5. 纯虚函数（类似于：java 中的 抽象类，接口）</h1><p>如果全部都是纯虚函数就类似 java 中的 <code>接口</code>,否则类似 <code>抽象类</code></p>
<p><code>virtual void click() = 0; // 必须要加 = 0</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">BaseActivity</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">onCreate</span><span class="params">()</span></span>&#123; <span class="comment">// 普通函数</span></span><br><span class="line">        <span class="built_in">initView</span>();</span><br><span class="line">        <span class="built_in">initData</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 子类必须要实现</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">initData</span><span class="params">()</span> </span>= <span class="number">0</span>; <span class="comment">// 虚函数，没有实现的，类似于 java 中的抽象方法，如果子类不实现会报错</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">initView</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果不实现父类的纯虚函数，那么 MainActivity 也会变成抽象类，抽象类不允许实例化</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span>:<span class="keyword">public</span> BaseActivity&#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">initData</span><span class="params">()</span> <span class="keyword">override</span></span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;initData&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">initView</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;initView&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ClickListener</span>&#123; <span class="comment">// 所有的函数都是虚函数，那么就可以认为是接口</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">click</span><span class="params">()</span> </span>= <span class="number">0</span>; <span class="comment">// 必须要加 = 0</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h1 id="6-构造函数和析构函数"><a href="#6-构造函数和析构函数" class="headerlink" title="6. 构造函数和析构函数"></a>6. 构造函数和析构函数</h1><ul>
<li>构造函数：先父类 -&gt; 再子类</li>
<li>析构函数: 先子类 -&gt; 再父类</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 构造函数：先父类 -&gt; 再子类</span></span><br><span class="line">    <span class="comment">// 析构函数: 先子类 -&gt; 再父类</span></span><br><span class="line">    Student* stu = <span class="keyword">new</span> <span class="built_in">Student</span>(<span class="string">&quot;East&quot;</span>,<span class="number">25</span>);</span><br><span class="line">    <span class="built_in">delete</span> (stu);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="7-模板函数（java-中的泛型）"><a href="#7-模板函数（java-中的泛型）" class="headerlink" title="7. 模板函数（java 中的泛型）"></a>7. 模板函数（java 中的泛型）</h1><h2 id="7-1-定义："><a href="#7-1-定义：" class="headerlink" title="7.1.定义："></a>7.1.定义：</h2><p><code>template &lt;typename T&gt;  // 模板函数的定义（不能有分号）</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;  <span class="comment">// 模板函数的定义（不能有分号）</span></span><br><span class="line"><span class="function">T <span class="title">add</span><span class="params">(T number1,T number2)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> number1+number2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="7-2-当普通函数和模板函数同时存在时，优先会调用普通函数"><a href="#7-2-当普通函数和模板函数同时存在时，优先会调用普通函数" class="headerlink" title="7.2. 当普通函数和模板函数同时存在时，优先会调用普通函数"></a>7.2. <strong><code>当普通函数和模板函数同时存在时，优先会调用普通函数</code></strong></h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function">T <span class="title">add</span><span class="params">(<span class="type">int</span> number1, <span class="type">int</span> number2)</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;模板函数被调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> number1+number2;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> number1,<span class="type">int</span> number2)</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;int add 被调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> number1+number2;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// so 是很难被反编译，比 java 安全为啥</span></span><br><span class="line"><span class="comment">// samll 语法</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> sum1 = <span class="built_in">add</span>(<span class="number">1</span>,<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">double</span> sum2 = <span class="built_in">add</span>(<span class="number">1.0</span>,<span class="number">2.0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> sum3 = <span class="built_in">add</span>(<span class="number">1.0f</span>,<span class="number">2.0f</span>);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; sum1 &lt;&lt; <span class="string">&quot; , &quot;</span> &lt;&lt; sum2 &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int add 被调用</span><br><span class="line">int add 被调用</span><br><span class="line">int add 被调用</span><br><span class="line">3 , 3</span><br></pre></td></tr></table></figure>
<h1 id="8-模板类"><a href="#8-模板类" class="headerlink" title="8.模板类"></a>8.模板类</h1><p><code>语法跟模板函数非常相似</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Callback</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">onError</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">onSucceed</span><span class="params">(T result)</span></span>&#123;</span><br><span class="line">        cout &lt;&lt; result &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 模板类继承</span></span><br><span class="line"><span class="comment">// 如果子类不是模板类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">HttpCallback</span> : <span class="keyword">public</span> Callback&lt;<span class="type">int</span>&gt;&#123;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 子类还是模板类</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">HttpCallback</span> : <span class="keyword">public</span> Callback&lt;T&gt;&#123;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++学习</tag>
      </tags>
  </entry>
  <entry>
    <title>性能优化：2.UI绘制优化</title>
    <url>/2.UI%E7%BB%98%E5%88%B6%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<h1 id="1-CPU与GPU工作流程"><a href="#1-CPU与GPU工作流程" class="headerlink" title="1.CPU与GPU工作流程"></a>1.CPU与GPU工作流程</h1><p>CPU 的任务繁多，做逻辑计算外，还要做内存管理、显示操作，因此在实际运算的时候性能会大打折扣，在没有 GPU 的时代，不能显示复<br>杂的图形，其运算速度远跟不上今天复杂三维游戏的要求。即使 CPU的工作频率超过 2GHz 或更高，对它绘制图形提高也不大。这时 GPU<br>的设计就出来了。</p>
<p><img src="/images/CPU、GPU传递工作.png" alt=""></p>
<p><img src="/images/CPU、GPU结构图.png" alt=""></p>
<ul>
<li>黄色的 Control 为控制器，用于协调控制整个 CPU 的运行，包括取出指令、控制其他模块的运行等；</li>
<li>绿色的 ALU （ Arithmetic Logic Unit ）是算术逻辑单元，用于进行数学、逻辑运算；</li>
<li>橙色的 Cache 和 DRAM 分别为缓存和 RAM ，用于存储信息。</li>
</ul>
<p>从结构图可以看出， CPU 的控制器较为复杂，而 ALU 数量较少。因此 CPU 擅长各种复杂的逻辑运算，但不擅长数学尤其是浮点运算。</p>
<p><img src="/images/CPU、GPU绘制的流程.png" alt=""></p>
<p><img src="/images/CPU、GPU绘制的流程2.png" alt=""></p>
<h1 id="2-60Hz-刷新频率由来"><a href="#2-60Hz-刷新频率由来" class="headerlink" title="2.60Hz 刷新频率由来"></a>2.60Hz 刷新频率由来</h1><ul>
<li>12 fps ：由于人类眼睛的特殊生理结构，如果所看画面之帧率高于每秒约 10-12 帧的时候，就会认为是连贯的</li>
<li>24 fps ：有声电影的拍摄及播放帧率均为每秒 24 帧，对一般人而言已算可接受</li>
<li>30 fps ：早期的高动态电子游戏，帧率少于每秒 30 帧的话就会显得不连贯，这是因为没有动态模糊使流畅度降低</li>
<li><code>60 fps</code> <code>在与手机交互过程中</code>，如触摸和反馈 60 帧以下人是能感觉出来的。 60 帧以上不能察觉变化当帧率<br>低于 60 fps 时感觉的画面的卡顿和迟滞现象</li>
</ul>
<p>Android 系统每隔 <code>16ms</code> 发出 <code>VSYNC(垂直同步信号)</code> 信号 (1000ms/60=16.66ms) ，触发对 UI 进行渲染， 如果每次渲染都成<br>功这样就能够达到流畅的画面所需要的 60fps ，为了能够实现 60fps ，这意味着计算渲染的大多数操作都必须在 16ms 内完成。</p>
<h1 id="3-卡顿原理分析和解决方案"><a href="#3-卡顿原理分析和解决方案" class="headerlink" title="3.卡顿原理分析和解决方案"></a>3.卡顿原理分析和解决方案</h1><p>当这一帧画面渲染时间超过16ms的时候,垂直同步机制会让显示器硬件 等待GPU完成栅格化渲染操作<br>这样会让湿一帧画面，多停留了 16ms,甚至更多.这样就这造成了用户看起来画面停顿.</p>
<p><code>16 毫秒的时间主要被两件事情所占用</code></p>
<ul>
<li>第一件：将 UI 对象转换为一系列多边形和纹理</li>
<li>第二件： CPU 传递处理数据到 GPU 。</li>
</ul>
<p>所以很明显，我们要缩短这两部分的时间，也就是说需要尽量减少对象转换的次数，以及Cpu传递到GPU数据的次数。</p>
<p><code>如何减少这两部分的时间以至于在16ms完成呢</code></p>
<ul>
<li>CPU减少xml转换成对象的时间(<code>布局优化</code>)</li>
<li>GPU减少重复绘制的时间（<code>减少过度绘制</code>）</li>
</ul>
<h1 id="4-减少过度绘制-减少GPU工作量"><a href="#4-减少过度绘制-减少GPU工作量" class="headerlink" title="4.减少过度绘制(减少GPU工作量)"></a><code>4.减少过度绘制(减少GPU工作量)</code></h1><h2 id="4-1-过度绘制定义"><a href="#4-1-过度绘制定义" class="headerlink" title="4.1.过度绘制定义"></a>4.1.过度绘制定义</h2><p>GPU的绘制过程，就跟刷墙一样,一层层的进行,16ms刷一次.这样就会造成图层覆盖的现象，即无用的图层还被绘制在底层,造成不必要的浪费.</p>
<h2 id="4-2-GPU过度绘制几种情况"><a href="#4-2-GPU过度绘制几种情况" class="headerlink" title="4.2.GPU过度绘制几种情况"></a>4.2.GPU过度绘制几种情况</h2><ol>
<li>自定义控件中onDraw方法做了过多重复绘制(可以使用裁剪，只绘制用户看的到的地方)</li>
<li>布局层次太深，重叠性太强。用户看不到的区域GPU也会渲染，导致耗时增加.</li>
</ol>
<h2 id="4-3-过度绘制査看工具"><a href="#4-3-过度绘制査看工具" class="headerlink" title="4.3.过度绘制査看工具"></a>4.3.过度绘制査看工具</h2><p>手机端的开发者选项—-&gt;<code>调试GPU过度绘制</code>——&gt;显示过度绘制区域</p>
<p><img src="/images/查看过度绘制区域.png" alt=""></p>
<p><img src="/images/查看过度绘制区域2.png" alt=""></p>
<ul>
<li>蓝色过度绘制一次无过度绘制</li>
<li>淡绿过度绘制两次</li>
<li>淡红过度绘制三次</li>
<li>深红过度绘制四次</li>
</ul>
<p>代表了4种不同程度Overdraw情况 我们的目标就是尽量减少红色Overdraw ,看到更多的蓝色区域。</p>
<h2 id="4-4-优化方案"><a href="#4-4-优化方案" class="headerlink" title="4.4.优化方案"></a><code>4.4.优化方案</code></h2><h3 id="4-4-1-减少背景重复"><a href="#4-4-1-减少背景重复" class="headerlink" title="4.4.1. 减少背景重复"></a>4.4.1. 减少背景重复</h3><p>去掉默认背景<br><img src="/images/去掉默认背景.png" alt=""></p>
<p>去掉二层容器背景<br><img src="/images/去掉容器背景.png" alt=""></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1.去掉所有activity主题设置中的属性</span><br><span class="line">直接在styles.xml中设置&lt;item name=&quot;android:windowBackground&quot;&gt;@null&lt;/item&gt;</span><br><span class="line"></span><br><span class="line">2.去掉单个activity的主题设置的属性</span><br><span class="line">可以在setContentView之前getWindow().setBackgroundDrawable(null); </span><br><span class="line"></span><br><span class="line">3.非业务需要，不要去设置背景 </span><br></pre></td></tr></table></figure>
<h3 id="4-4-2-使用裁减减少控件之间的重合部分"><a href="#4-4-2-使用裁减减少控件之间的重合部分" class="headerlink" title="4.4.2. 使用裁减减少控件之间的重合部分"></a>4.4.2. 使用裁减减少控件之间的重合部分</h3><p>Android7.0之后系统做出的优化,<code>invalidate()</code>不再执行测量和布局动作</p>
<p><img src="/images/重合区域过度绘制情况.png" alt=""></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">private void drawDroidCard(Canvas canvas,List&lt;DroidCard&gt; mDroidCards,int i) &#123;</span><br><span class="line">    DroidCard c = mDroidCards.get(i);</span><br><span class="line">    canvas.save();</span><br><span class="line">    canvas.clipRect((float)c.x,0f,(float)(mDroidCards.get(i+1).x),(float)c.height);</span><br><span class="line">    canvas.drawBitmap(c.bitmap,c.x,0f,paint);</span><br><span class="line">    canvas.restore();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-布局优化-减少CPU工作量"><a href="#5-布局优化-减少CPU工作量" class="headerlink" title="5.布局优化(减少CPU工作量)"></a><code>5.布局优化(减少CPU工作量)</code></h1><h2 id="5-1-布局查看常用工具"><a href="#5-1-布局查看常用工具" class="headerlink" title="5.1.布局查看常用工具"></a>5.1.布局查看常用工具</h2><ol>
<li>Tool—-&gt;<code>Layout Inspector</code>,<code>AndroidStudio自带</code></li>
<li>sdk目录/tools/bin/uiautomatorviewer.bat</li>
<li>sdk目录tools\monitor.bat打开<code>Device Monitor</code>中Hierarchy view，<code>AndroidStudio3.1过后已经遗弃，但是真的好用</code><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">三个点也是代表着View的Measure, Layout和Draw。</span><br><span class="line">绿: 表示该View的此项性能比该View Tree中超过50%的View都要快；例如,代表Measure的是绿点,意味着这个视图的测量时间快于树中的视图对象的50%。</span><br><span class="line">黄: 表示该View的此项性能比该View Tree中超过50%的View都要慢； </span><br><span class="line">红: 表示该View的此项性能是View Tree中最慢的；。</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p><img src="/images/AndroidDeviceMonitor.png" alt=""></p>
<h2 id="5-2-优化方案"><a href="#5-2-优化方案" class="headerlink" title="5.2.优化方案"></a><code>5.2.优化方案</code></h2><ol>
<li>能在一个平面显示的内容，尽量只用一个容器。</li>
<li>尽可能把相同的容器合并merge。</li>
<li>能复用的代码，用include处理，可以减少GPU重复工。</li>
</ol>
]]></content>
      <categories>
        <category>性能优化</category>
      </categories>
  </entry>
  <entry>
    <title>C++：04.操作符重载和类的继承</title>
    <url>/18.C++%E5%9F%BA%E7%A1%80-%E6%93%8D%E4%BD%9C%E7%AC%A6%E9%87%8D%E8%BD%BD%E5%92%8C%E7%B1%BB%E7%9A%84%E7%BB%A7%E6%89%BF/</url>
    <content><![CDATA[<h1 id="1-操作符重载"><a href="#1-操作符重载" class="headerlink" title="1. 操作符重载"></a>1. 操作符重载</h1><p><code>一般来讲我们定义在类的里面</code></p>
<p>格式：<strong>返回值 <code>operate</code>需要重载的操作符(参数){}</strong> 例如：void operator++(){}</p>
<h2 id="1-1-加减操作符"><a href="#1-1-加减操作符" class="headerlink" title="1.1 加减操作符"></a>1.1 加减操作符</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Vector</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Vector</span>(<span class="type">int</span> x, <span class="type">int</span> y) &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;x = x;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">Vector</span>(<span class="type">const</span> Vector &amp;vector) &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;x = vector.x;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = vector.y;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;拷贝构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">    <span class="type">int</span> y;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setX</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;x = x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setY</span><span class="params">(<span class="type">int</span> y)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getX</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getY</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 重载减号运算符</span></span><br><span class="line">    <span class="comment">// 为什么要用引用，为了防止重新创建对象</span></span><br><span class="line">    <span class="comment">// const 关键常量，为了防止去修改值</span></span><br><span class="line">    Vector <span class="keyword">operator</span>-(<span class="type">const</span> Vector &amp;vector) &#123;</span><br><span class="line">        <span class="type">int</span> x = <span class="keyword">this</span>-&gt;x - vector.x;</span><br><span class="line">        <span class="type">int</span> y = <span class="keyword">this</span>-&gt;y - vector.y;</span><br><span class="line">        <span class="function">Vector <span class="title">res</span><span class="params">(x, y)</span></span>;</span><br><span class="line">        <span class="keyword">return</span> res; <span class="comment">// 不建议返回引用</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Vector <span class="keyword">operator</span> + (<span class="type">const</span> Vector &amp;vector) &#123;</span><br><span class="line">        <span class="type">int</span> x = <span class="keyword">this</span>-&gt;x + vector.x;</span><br><span class="line">        <span class="type">int</span> y = <span class="keyword">this</span>-&gt;y + vector.y;</span><br><span class="line">        <span class="function">Vector <span class="title">vector</span><span class="params">(x,y)</span></span>;</span><br><span class="line">        <span class="keyword">return</span> vector;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">Vector <span class="title">vector1</span><span class="params">(<span class="number">2</span>, <span class="number">3</span>)</span></span>;</span><br><span class="line">    <span class="function">Vector <span class="title">vector2</span><span class="params">(<span class="number">2</span>, <span class="number">3</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// java 中 string + string;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// char* str = &quot;123&quot; + &quot;456&quot;;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 重载运算符 -</span></span><br><span class="line">    Vector vector = vector1 - vector2;</span><br><span class="line">    cout &lt;&lt; vector.<span class="built_in">getX</span>() &lt;&lt; <span class="string">&quot; , &quot;</span> &lt;&lt; vector.<span class="built_in">getY</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0,0</span><br></pre></td></tr></table></figure>
<h2 id="1-2-自增减运算符"><a href="#1-2-自增减运算符" class="headerlink" title="1.2 自增减运算符"></a>1.2 自增减运算符</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Vector</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Vector</span>(<span class="type">int</span> x, <span class="type">int</span> y) &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;x = x;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">Vector</span>(<span class="type">const</span> Vector &amp;vector) &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;x = vector.x;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = vector.y;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;拷贝构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">    <span class="type">int</span> y;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setX</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;x = x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setY</span><span class="params">(<span class="type">int</span> y)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getX</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getY</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 自增运算符</span></span><br><span class="line">    <span class="type">void</span> <span class="keyword">operator</span>++() &#123; <span class="comment">// ++X</span></span><br><span class="line">        <span class="keyword">this</span>-&gt;x = ++<span class="keyword">this</span>-&gt;x;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = ++<span class="keyword">this</span>-&gt;y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">void</span> <span class="keyword">operator</span>++(<span class="type">int</span>) &#123; <span class="comment">// X++</span></span><br><span class="line">        <span class="keyword">this</span>-&gt;x = <span class="keyword">this</span>-&gt;x++;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = <span class="keyword">this</span>-&gt;y++;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function">Vector <span class="title">vector</span><span class="params">(<span class="number">1</span>, <span class="number">2</span>)</span></span>;</span><br><span class="line">    <span class="comment">// vector++;</span></span><br><span class="line">    ++vector;</span><br><span class="line"></span><br><span class="line">     cout &lt;&lt; vector.<span class="built_in">getX</span>() &lt;&lt; <span class="string">&quot; , &quot;</span> &lt;&lt; vector.<span class="built_in">getY</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">2 , 3</span><br></pre></td></tr></table></figure>
<h2 id="1-3-输出运算符"><a href="#1-3-输出运算符" class="headerlink" title="1.3 输出运算符"></a>1.3 输出运算符</h2><p>定义：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Vector</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Vector</span>(<span class="type">int</span> x, <span class="type">int</span> y) &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;x = x;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">Vector</span>(<span class="type">const</span> Vector &amp;vector) &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;x = vector.x;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = vector.y;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;拷贝构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">    <span class="type">int</span> y;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setX</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;x = x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setY</span><span class="params">(<span class="type">int</span> y)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getX</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getY</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 输出运算符</span></span><br><span class="line">    <span class="keyword">friend</span> ostream &amp;<span class="keyword">operator</span>&lt;&lt;(ostream &amp;_Ostr, <span class="type">const</span> Vector vector) &#123;</span><br><span class="line">        _Ostr &lt;&lt; vector.x &lt;&lt; <span class="string">&quot; , &quot;</span> &lt;&lt; vector.y &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">return</span> _Ostr;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function">Vector <span class="title">vector</span><span class="params">(<span class="number">1</span>, <span class="number">2</span>)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 这么使用</span></span><br><span class="line">    cout &lt;&lt; vector &lt;&lt; vector;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">拷贝构造函数</span><br><span class="line">拷贝构造函数</span><br><span class="line">1 , 2</span><br><span class="line">1 , 2</span><br></pre></td></tr></table></figure>
<h2 id="1-4-条件运算符"><a href="#1-4-条件运算符" class="headerlink" title="1.4 条件运算符"></a>1.4 条件运算符</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 条件运算符</span></span><br><span class="line"><span class="type">bool</span> <span class="keyword">operator</span>==(<span class="type">const</span> Vector &amp;vector) &#123;</span><br><span class="line">    <span class="built_in">return</span> (<span class="keyword">this</span>-&gt;x == vector.x &amp;&amp; <span class="keyword">this</span>-&gt;y == vector.y);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="1-5-括号运算符"><a href="#1-5-括号运算符" class="headerlink" title="1.5 括号运算符"></a>1.5 括号运算符</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 操作符[]</span></span><br><span class="line"><span class="type">int</span> <span class="keyword">operator</span>[](<span class="type">int</span> index) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>-&gt;array[index];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-类继承"><a href="#2-类继承" class="headerlink" title="2. 类继承"></a>2. 类继承</h1><h2 id="2-1-变量修饰符和继承修饰符"><a href="#2-1-变量修饰符和继承修饰符" class="headerlink" title="2.1 变量修饰符和继承修饰符"></a>2.1 变量修饰符和继承修饰符</h2><p>变量修饰符：  </p>
<ol>
<li><code>private</code> ：本类中使用</li>
<li><code>protected</code> ：子类中能使用（默认）</li>
<li><code>public</code> ：公开，任何地方都可以</li>
</ol>
<p>类继承修饰符：  </p>
<ol>
<li><code>private</code> ：本类中使用</li>
<li><code>protected</code> ：子类中能使用（默认）</li>
<li><code>public</code> ：公开，任何地方都可以</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 加了 public 才可以访问 Person 中的公有属性和方法</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> : <span class="keyword">public</span> Person &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h2 id="2-2-初始化父类属性"><a href="#2-2-初始化父类属性" class="headerlink" title="2.2 初始化父类属性"></a>2.2 初始化父类属性</h2><p><code>不光可以给父类初始化属性，还可以给本类的属性进行初始化，用 , 隔开即可</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Student</span>(<span class="type">char</span> *name, <span class="type">int</span> age, <span class="type">char</span> *course) : <span class="built_in">Person</span>(name, age), <span class="built_in">course</span>(course) &#123;</span><br><span class="line">       cout &lt;&lt; <span class="string">&quot;Student 构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-3-初始化属性和函数重载"><a href="#2-3-初始化属性和函数重载" class="headerlink" title="2.3 初始化属性和函数重载"></a>2.3 初始化属性和函数重载</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="comment">//变量修饰符</span></span><br><span class="line"><span class="keyword">private</span>:<span class="comment">// 本类中使用</span></span><br><span class="line">    <span class="comment">// protected: 子类中能使用（默认）</span></span><br><span class="line">    <span class="comment">// public：公开，任何地方都可以</span></span><br><span class="line">    <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Person</span>(<span class="type">char</span> *name, <span class="type">int</span> age) &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = name;</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Person 构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="keyword">this</span>-&gt;name &lt;&lt; <span class="string">&quot; , &quot;</span> &lt;&lt; <span class="keyword">this</span>-&gt;age &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> : <span class="keyword">public</span> Person &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">char</span> *course;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// :Person(name,age) 调用构造函数初始化父类的属性</span></span><br><span class="line">    <span class="comment">// 不光可以给父类初始化属性，还可以给本类的属性进行初始化，用 , 隔开即可</span></span><br><span class="line">    <span class="built_in">Student</span>(<span class="type">char</span> *name, <span class="type">int</span> age, <span class="type">char</span> *course) : <span class="built_in">Person</span>(name, age), <span class="built_in">course</span>(course) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Student 构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;course： &quot;</span> &lt;&lt; course &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="function">Student <span class="title">stu</span><span class="params">(<span class="string">&quot;eastrise&quot;</span>,<span class="number">25</span>,<span class="string">&quot;数学&quot;</span>)</span></span>;</span><br><span class="line">    stu.<span class="built_in">print</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Person 构造函数</span><br><span class="line">Student 构造函数</span><br><span class="line">course： 数学</span><br></pre></td></tr></table></figure>
<h2 id="2-4-多继承"><a href="#2-4-多继承" class="headerlink" title="2.4 多继承"></a>2.4 多继承</h2><p>用 <code>,</code> 号 隔开</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> : <span class="keyword">public</span> Person , <span class="keyword">public</span> Vector&#123;</span><br><span class="line">    </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++学习</tag>
      </tags>
  </entry>
  <entry>
    <title>C++：03.可变参数、static、对象大小和友元类、函数</title>
    <url>/17.C++%E5%9F%BA%E7%A1%80-%E5%8F%AF%E5%8F%98%E5%8F%82%E6%95%B0%E3%80%81static%E3%80%81%E5%AF%B9%E8%B1%A1%E5%A4%A7%E5%B0%8F%E5%92%8C%E5%8F%8B%E5%85%83%E7%B1%BB%E3%80%81%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<h1 id="1-可变参数"><a href="#1-可变参数" class="headerlink" title="1. 可变参数"></a>1. 可变参数</h1><p>java中：<code>int ...</code><br>c++：<code>...</code></p>
<ul>
<li><code>va_start(vp,count);</code>：将可变参数放到va_list vp中去，这个宏必须在使用 va_arg 和 va_end 之前被调用。</li>
<li><code>int number = va_arg(vp,int);</code>: 获取可变参数，如果超出了可获取的范围默认就为0</li>
<li><code>va_end(vp);</code>：结束了释放内存。</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">sum</span><span class="params">(<span class="type">int</span> count,...)</span></span>&#123;<span class="comment">// java：int ..., c++： ...</span></span><br><span class="line">    va_list vp;</span><br><span class="line">    <span class="comment">// 可变参数开始方法， count 最后一个传递给函数的已知的固定参数。</span></span><br><span class="line">    <span class="built_in">va_start</span>(vp,count);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> number = <span class="built_in">va_arg</span>(vp,<span class="type">int</span>);</span><br><span class="line">    cout &lt;&lt; number &lt;&lt;endl;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 读取下一个</span></span><br><span class="line">    number = <span class="built_in">va_arg</span>(vp,<span class="type">int</span>);</span><br><span class="line">    cout &lt;&lt; number &lt;&lt;endl;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 读取下一个，超出了默认是 0</span></span><br><span class="line">    number = <span class="built_in">va_arg</span>(vp,<span class="type">int</span>);</span><br><span class="line">    cout &lt;&lt; number &lt;&lt;endl;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 结尾，释放内存</span></span><br><span class="line">    <span class="built_in">va_end</span>(vp);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">     <span class="built_in">sum</span>(<span class="number">5</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">4</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">4</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">sum</span><span class="params">(<span class="type">int</span> count,...)</span></span>&#123;<span class="comment">// java：int ..., c++： ...</span></span><br><span class="line">    va_list vp;</span><br><span class="line">    <span class="comment">// 可变参数开始方法， count 最后一个传递给函数的已知的固定参数。</span></span><br><span class="line">    <span class="built_in">va_start</span>(vp,count);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i&lt;count;i++)&#123;</span><br><span class="line">        <span class="type">int</span> number = <span class="built_in">va_arg</span>(vp,<span class="type">int</span>);</span><br><span class="line">        sum += number;</span><br><span class="line">        cout &lt;&lt; i &lt;&lt; <span class="string">&quot;：&quot;</span> &lt;&lt; number &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 结尾，释放内存</span></span><br><span class="line">    <span class="built_in">va_end</span>(vp);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> number = <span class="built_in">sum</span>(<span class="number">4</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">4</span>,<span class="number">0</span>); <span class="comment">// 7</span></span><br><span class="line">    cout &lt;&lt; number &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0：1</span><br><span class="line">1：2</span><br><span class="line">2：4</span><br><span class="line">3：0</span><br><span class="line">7</span><br></pre></td></tr></table></figure>
<h1 id="2-static-关键字"><a href="#2-static-关键字" class="headerlink" title="2. static 关键字"></a>2. static 关键字</h1><p>总结：</p>
<ol>
<li>静态的属性定义时<strong><code>必须要初始化</code></strong> （实现） <code>int Student::tag = 12;</code></li>
<li>静态 可以直接用类名去操作 ::   <code>Student::tag += 12;</code></li>
<li>静态的方法只能去操作静态的属性或者方法</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">char</span>* name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="comment">//静态</span></span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> tag; <span class="comment">// 定义</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">Student</span>()&#123;</span><br><span class="line">        tag = <span class="number">12</span>; <span class="comment">// 这里不算初始化</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="type">void</span> <span class="title">change</span><span class="params">()</span></span>&#123;</span><br><span class="line">        tag += <span class="number">12</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">change1</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span> -&gt; <span class="built_in">change</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 静态属性在 c++ 中必须要初始化，初始化必须这么写</span></span><br><span class="line"><span class="type">int</span> Student::tag = <span class="number">12</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 静态 可以直接用类名去操作 ：：</span></span><br><span class="line"><span class="comment">//      静态的属性必须初始化（实现）</span></span><br><span class="line"><span class="comment">//      静态的方法只能去操作静态的属性或方法</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Student stu;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// stu.tag = 12;</span></span><br><span class="line">    <span class="comment">// c++ 操作静态语法 ::</span></span><br><span class="line">    <span class="comment">// Student::tag += 12;</span></span><br><span class="line">    <span class="comment">// Student::change();</span></span><br><span class="line">    stu.<span class="built_in">change1</span>();</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt;Student::tag &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">24</span><br></pre></td></tr></table></figure>
<h1 id="3-对象大小"><a href="#3-对象大小" class="headerlink" title="3. 对象大小"></a>3. 对象大小</h1><ol>
<li>对象的大小与结构体的计算方式类似</li>
<li>static 静态变量和方法并没有算到类的大小中</li>
<li>栈，堆，全局（静态，常量，字符串），代码区 ，类的大小只与普通属性有关系</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">double</span> b;</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">    <span class="type">char</span> c;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">double</span> b;</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">    <span class="type">char</span> c;</span><br><span class="line">    <span class="type">static</span> <span class="type">double</span> d;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">double</span> b;</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">    <span class="type">char</span> c;</span><br><span class="line">    <span class="built_in">C</span>(<span class="type">int</span> a)&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;a = a;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getA</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;a;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 对象的大小与结构体的计算方式类似</span></span><br><span class="line"><span class="comment">// 2. static 静态变量和方法并没有算到类的大小中</span></span><br><span class="line"><span class="comment">// 3. 栈、堆、全局（静态、常量、字符串），代码区，类的大小只与普通属性有关系</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;A 的大小： &quot;</span> &lt;&lt; <span class="built_in">sizeof</span>(A) &lt;&lt;endl; <span class="comment">// 16</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;B 的大小： &quot;</span> &lt;&lt; <span class="built_in">sizeof</span>(B) &lt;&lt;endl; <span class="comment">// 16</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;C 的大小： &quot;</span> &lt;&lt; <span class="built_in">sizeof</span>(C) &lt;&lt;endl; <span class="comment">// 16</span></span><br><span class="line"></span><br><span class="line">    <span class="function">C <span class="title">c1</span><span class="params">(<span class="number">12</span>)</span></span>;</span><br><span class="line">    <span class="function">C <span class="title">c2</span><span class="params">(<span class="number">24</span>)</span></span>;</span><br><span class="line">    cout &lt;&lt; c<span class="number">1.</span><span class="built_in">getA</span>() &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; c<span class="number">2.</span><span class="built_in">getA</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">A 的大小： 16</span><br><span class="line">B 的大小： 16</span><br><span class="line">C 的大小： 16</span><br><span class="line">12</span><br><span class="line">24</span><br></pre></td></tr></table></figure>
<h1 id="4-this-指针"><a href="#4-this-指针" class="headerlink" title="4. this 指针"></a>4. this 指针</h1><p><code>this 指针：代表当前的对象，因为类的方法放在代码区，大家一起共享的，所以要有 this 做区分</code></p>
<h1 id="5-const-修饰函数"><a href="#5-const-修饰函数" class="headerlink" title="5.const 修饰函数"></a>5.const 修饰函数</h1><p><code>const 在 () 之后主要用来限制 this 关键字</code>,<code>this</code> 的值和地址都不能修改</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// this = const Student* const this</span></span><br><span class="line"><span class="comment">// 第一个 const : 常量指针， 代表值不能修改</span></span><br><span class="line"><span class="comment">// 第二个 const : 指针常量，代表指针的地址不能修改</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">change</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="comment">// const 在 () 之后主要用来限制 this 关键字</span></span><br><span class="line">    <span class="comment">// this-&gt;age + = 12;</span></span><br><span class="line">    <span class="comment">// this = (Student)0x0012;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="6-友元函数"><a href="#6-友元函数" class="headerlink" title="6.友元函数"></a>6.友元函数</h1><p>作用：<code>友元函数是可以在外部访问其私有属性的</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line"> <span class="keyword">private</span>:</span><br><span class="line">	 <span class="type">int</span> age = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="comment">// 如果有自己写构造函数，那么会默认覆盖无参的构造函数</span></span><br><span class="line">	<span class="built_in">Person</span>(<span class="type">int</span> age)&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 友元函数的声明</span></span><br><span class="line">	<span class="function"><span class="keyword">friend</span> <span class="type">void</span> <span class="title">friend_change</span><span class="params">(Person *person, <span class="type">int</span> age)</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 友元函数的实现</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">friend_change</span><span class="params">(Person *person, <span class="type">int</span> age)</span></span>&#123;</span><br><span class="line">	<span class="comment">// 修改一下 age 的值</span></span><br><span class="line">	<span class="comment">// 在类的内部才能访问私有属性</span></span><br><span class="line">	<span class="comment">// 如果该方法声明成友元函数那么是可以在外部访问其私有属性的</span></span><br><span class="line">	person-&gt;age = age;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Person person = <span class="built_in">Person</span>(<span class="number">24</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">friend_change</span>(&amp;person,<span class="number">36</span>);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; person.<span class="built_in">getAge</span>() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">36</span><br></pre></td></tr></table></figure>
<p>注意：调用的时候<code>不</code>使用 <code>类名::</code></p>
<h2 id="6-1-友元函数与普通函数的区别"><a href="#6-1-友元函数与普通函数的区别" class="headerlink" title="6.1 友元函数与普通函数的区别"></a>6.1 友元函数与普通函数的区别</h2><ul>
<li>友元函数：直接<code>方法名</code>调用。</li>
<li>普通函数：需要<code>对象名::</code>+<code>方法名</code> 调用</li>
</ul>
<h1 id="7-友元类"><a href="#7-友元类" class="headerlink" title="7.友元类"></a>7.友元类</h1><p>作用：<code>友元类是可以在访问其附属类的私有属性</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span>  <span class="title class_">ImageView</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>: <span class="comment">// B 是 A 的友元类</span></span><br><span class="line">	<span class="keyword">friend</span> <span class="keyword">class</span> <span class="title class_">Class</span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="type">int</span> a;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Class</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	ImageView aObj;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">changeA</span><span class="params">(<span class="type">int</span> number)</span></span>&#123;</span><br><span class="line">		aObj.a = number;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="type">int</span> <span class="title">getA</span><span class="params">()</span></span>&#123;</span><br><span class="line">		<span class="keyword">return</span> aObj.a;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Class b;</span><br><span class="line">    b.<span class="built_in">changeA</span>(<span class="number">12</span>);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; b.<span class="built_in">getA</span>() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">12</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++学习</tag>
      </tags>
  </entry>
  <entry>
    <title>C++：07.类型转换、实现System.arrayCopy、异常处理</title>
    <url>/21.C++%E5%9F%BA%E7%A1%80-%E7%B1%BB%E5%9E%8B%E8%BD%AC%E6%8D%A2%E3%80%81%E5%AE%9E%E7%8E%B0System.arrayCopy%E3%80%81%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h1 id="1-类型转换"><a href="#1-类型转换" class="headerlink" title="1. 类型转换"></a>1. 类型转换</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Student *stu = <span class="built_in">static_cast</span>&lt;Student*&gt;(person); <span class="comment">// 正确</span></span><br><span class="line">Student stu = <span class="built_in">static_cast</span>&lt;Student&gt;(person); <span class="comment">// 错误</span></span><br><span class="line"></span><br><span class="line">Student *stu = <span class="built_in">reinterpret_cast</span>&lt;Student*&gt;(person); <span class="comment">// 正确</span></span><br><span class="line">Student stu = <span class="built_in">reinterpret_cast</span>&lt;Student&gt;(person); <span class="comment">// 错误</span></span><br></pre></td></tr></table></figure>
<h2 id="1-1-static-cast-静态转换"><a href="#1-1-static-cast-静态转换" class="headerlink" title="1.1 static_cast 静态转换"></a>1.1 static_cast <code>静态转换</code></h2><ul>
<li><code>非指针情况下：子类可以转换成父类，父类不能转换成子类，指针情况下可以</code></li>
<li><code>用于基本数据类型之间的转换，如把int转换成char</code></li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    string name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Person</span>(string name,<span class="type">int</span> age):<span class="built_in">name</span>(name),<span class="built_in">age</span>(age)&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> : <span class="keyword">public</span> Person&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Student</span>(string name,<span class="type">int</span> age):<span class="built_in">Person</span>(name,age)&#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Worker</span>: <span class="keyword">public</span> Person&#123;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// static_cast(静态转换 类似于 直接转换)</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">double</span> number1 = <span class="number">20.02</span>;</span><br><span class="line">    <span class="comment">// 直接转换</span></span><br><span class="line">    <span class="comment">// int number2 = number1;</span></span><br><span class="line">    <span class="comment">// 用于基本数据类型之间的转换，例如把 int 转 char</span></span><br><span class="line">    <span class="comment">// int number2 = static_cast&lt;int&gt;(number1);</span></span><br><span class="line">    <span class="comment">// cout &lt;&lt; number2 &lt;&lt; endl;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 把类型转换成另一种类型，用于类层次结构中基类和派生类之间指针或引用的转换</span></span><br><span class="line">    <span class="comment">// Student *stu = new Student(&quot;eastrise&quot;,26);</span></span><br><span class="line">    <span class="comment">// Person *person = stu;</span></span><br><span class="line">    <span class="comment">// Person *person = static_cast&lt;Person*&gt;(stu); // ok的</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 待会再试</span></span><br><span class="line">    Person person = <span class="built_in">Person</span>(<span class="string">&quot;eastrise&quot;</span>,<span class="number">26</span>);</span><br><span class="line">    <span class="comment">// 转成 Student</span></span><br><span class="line">    <span class="comment">// Student stu = person; // 错误</span></span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; person.name.<span class="built_in">c_str</span>() &lt;&lt; <span class="string">&quot; , &quot;</span> &lt;&lt; person.age &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="1-2-const-cast-常量转换"><a href="#1-2-const-cast-常量转换" class="headerlink" title="1.2. const_cast 常量转换"></a>1.2. const_cast <code>常量转换</code></h2><p>用于修改常量的值（<code>转换成了不是常量的指针然后去修改值</code>）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">const</span> Person* person = <span class="keyword">new</span> <span class="built_in">Person</span>(<span class="string">&quot;eastrise&quot;</span>,<span class="number">26</span>);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; person-&gt;name.<span class="built_in">c_str</span>() &lt;&lt; <span class="string">&quot; , &quot;</span> &lt;&lt; person-&gt;age &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// person-&gt;age = 24;</span></span><br><span class="line">    <span class="comment">// const_cast</span></span><br><span class="line">    Person* person1 = <span class="built_in">const_cast</span>&lt;Person*&gt;(person);</span><br><span class="line">    person1-&gt;age = <span class="number">25</span>;</span><br><span class="line">    person1-&gt;name = <span class="string">&quot;Jack&quot;</span>;</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; person1-&gt;name.<span class="built_in">c_str</span>() &lt;&lt; <span class="string">&quot; , &quot;</span> &lt;&lt; person1-&gt;age &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="1-3-reinterpret-cast-强制类型转换"><a href="#1-3-reinterpret-cast-强制类型转换" class="headerlink" title="1.3. reinterpret_cast 强制类型转换"></a>1.3. reinterpret_cast <code>强制类型转换</code></h2><p>用于转换任意类型</p>
<ul>
<li><code>非指针情况下：子类可以转换成父类，父类不能转换成子类，指针情况下可以</code></li>
<li><code>jni</code> 中常见的是 <code>long</code> 转 <code>指针</code></li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 待会再试</span></span><br><span class="line">    Person *person = <span class="keyword">new</span> <span class="built_in">Person</span>(<span class="string">&quot;eastrise&quot;</span>,<span class="number">26</span>);</span><br><span class="line">    <span class="comment">// 转成 Student</span></span><br><span class="line">    <span class="comment">// Student stu = person; // 错误</span></span><br><span class="line">    <span class="comment">// Student *stu = static_cast&lt;Student*&gt;(person); // 正确</span></span><br><span class="line"></span><br><span class="line">    <span class="type">long</span> mPtr = (<span class="type">long</span>)person; <span class="comment">// NDK中可以</span></span><br><span class="line">    <span class="comment">// 除了字符类，各种类型的转换  long -&gt; 对象的指针* ，用到reinterpret_cast</span></span><br><span class="line">    <span class="comment">// 与 static_Cast 区别在于 static_cast 一般用于转换有继承关系的类型 reinterpret_cast 也能转换有继承关系的类型</span></span><br><span class="line">    Student *stu = <span class="built_in">reinterpret_cast</span>&lt;Student*&gt;(person);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; stu-&gt;name.<span class="built_in">c_str</span>() &lt;&lt; <span class="string">&quot; , &quot;</span> &lt;&lt; stu-&gt;age &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="1-4-dynamic-cast-动态转换"><a href="#1-4-dynamic-cast-动态转换" class="headerlink" title="1.4. dynamic_cast 动态转换"></a>1.4. dynamic_cast <code>动态转换</code></h2><p><code>必须要包含多态类型</code> ,虽然<code>更安全</code>，<code>转换成功返回类型，失败返回空</code> ，<br>      和 <code>static_cast</code> 很类似，但是更安全</p>
<p>static_cast <code>一般用于转换有继承关系的类型</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Student *stu = <span class="keyword">new</span> <span class="built_in">Student</span>(<span class="string">&quot;&quot;</span>,<span class="number">26</span>);</span><br><span class="line"></span><br><span class="line">    Person *worker = <span class="built_in">dynamic_cast</span>&lt;Person* &gt;(stu);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-native-层实现-System-arrayCopy-的功能"><a href="#2-native-层实现-System-arrayCopy-的功能" class="headerlink" title="2. native 层实现 System.arrayCopy  的功能"></a>2. native 层实现 System.arrayCopy  的功能</h1><p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/cpp21">NDKPractice项目的cpp21</a></strong></p>
<ul>
<li><code>jobject  obj = env-&gt;GetObjectArrayElement(src_array,srcPos+i);</code>：获取jobjectArray中下标为srcPos+i的数据。</li>
<li><code>env-&gt;SetObjectArrayElement(dest_array,destPos+i,obj);</code>：设置jobjectArray中下标为destPos+i的数据为obj</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span></span><br><span class="line"><span class="function">JNIEXPORT <span class="type">void</span> JNICALL</span></span><br><span class="line"><span class="function"><span class="title">Java_com_darren_ndk_day26_MainActivity_arraycopy</span><span class="params">(JNIEnv *env, jclass type, jobject src, jint srcPos,</span></span></span><br><span class="line"><span class="params"><span class="function">                                                 jobject dest, jint destPos, jint length)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 做一些列的判断 ，是不是数组</span></span><br><span class="line">    <span class="comment">// 做一系列的准备工作 ，如果有异常，挂了，程序是会崩掉</span></span><br><span class="line">    <span class="comment">// buggly  so库报错 ，如果有抛 java 层的异常，是可以 try ，但是很多情况下并不会抛java层的异常</span></span><br><span class="line">    <span class="comment">// 如果没抛 java 层的异常，在 java 层 try 了 依旧会蹦</span></span><br><span class="line">    <span class="comment">// src</span></span><br><span class="line">    <span class="comment">// dest</span></span><br><span class="line">    <span class="comment">// 都是 java 数组，需要类型转换 reinterpret_cast static_cast dynamic_cast(最佳)</span></span><br><span class="line">    <span class="comment">// dynamic_cast 但是必须包含多态类型</span></span><br><span class="line">    jobjectArray src_array = <span class="built_in">reinterpret_cast</span>&lt;jobjectArray&gt;(src);</span><br><span class="line">    jobjectArray dest_array = <span class="built_in">reinterpret_cast</span>&lt;jobjectArray&gt;(dest);</span><br><span class="line">    <span class="keyword">if</span>(src_array &amp;&amp; dest_array)&#123;</span><br><span class="line">        __android_log_print(ANDROID_LOG_ERROR,<span class="string">&quot;TAG&quot;</span>,<span class="string">&quot;转换成功&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置值</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i&lt; length ; i ++)&#123;</span><br><span class="line">            jobject  obj = env-&gt;<span class="built_in">GetObjectArrayElement</span>(src_array,srcPos+i);</span><br><span class="line">            <span class="comment">// 放到新数组里面</span></span><br><span class="line">            env-&gt;<span class="built_in">SetObjectArrayElement</span>(dest_array,destPos+i,obj);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 完毕</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-异常处理"><a href="#3-异常处理" class="headerlink" title="3. 异常处理"></a>3. 异常处理</h1><ul>
<li>c++ 中有自己一套异常的体系，不要去强记 <a href="https://www.cnblogs.com/QG-whz/p/5136883.html">c++自己的异常</a></li>
<li>但是 c++ 可以抛任何数据类型 <code>try&#123;&#125;catch(数据类型 变量名)&#123;&#125;</code></li>
<li>throw 抛异常</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// c++ 中有自己一套异常的体系，不要去强记</span></span><br><span class="line">    <span class="comment">// 但是 c++ 可以抛任何数据类型 try&#123;&#125;catch(数据类型 变量名)&#123;&#125;</span></span><br><span class="line">    <span class="comment">// throw 抛异常</span></span><br><span class="line">    <span class="keyword">try</span>&#123;</span><br><span class="line">        <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(i == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="built_in">Exception</span>(<span class="string">&quot;出异常了&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(i &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="number">12.5f</span>;</span><br><span class="line">    &#125; <span class="built_in">catch</span> (<span class="type">int</span> number) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;捕捉到异常&quot;</span> &lt;&lt; number &lt;&lt; endl;</span><br><span class="line">    &#125; <span class="built_in">catch</span> (Exception e) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;捕捉到异常：&quot;</span> &lt;&lt; e.<span class="built_in">what</span>() &lt;&lt; endl;</span><br><span class="line">    &#125; <span class="built_in">catch</span> (...) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;捕捉到其它异常&quot;</span>&lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>NDK 异常总结:  </p>
<ol>
<li>在 c++ 层如果是自己写的代码或者调用别人的方法，记得要 try 住， 如果不 try 在 java 层 try 是没有意义的</li>
<li>如果异常需要往外抛给 java 层，一定要按照java层抛异常的方式</li>
<li>如果是自己写的 NDK 层的代码，最好抛自己写的异常，声明异常</li>
<li>如果是做 c++/c , 或者是帮 c/c++ 写代码，最好抛系统定义好的异常或者继承系统的异常</li>
<li>系统异常的体系  exception 基类  <a href="https://www.cnblogs.com/QG-whz/p/5136883.html">https://www.cnblogs.com/QG-whz/p/5136883.html</a></li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">c_method</span><span class="params">()</span> <span class="title">throw</span><span class="params">(Exception,<span class="type">int</span>)</span></span>&#123; <span class="comment">// 如果是自己写的， 1000 多行</span></span><br><span class="line">    <span class="keyword">throw</span> <span class="built_in">Exception</span>(<span class="string">&quot;异常了&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="built_in">c_method</span>();</span><br><span class="line">    &#125;<span class="built_in">catch</span> (Exception exception)&#123;</span><br><span class="line">        <span class="comment">// 这个异常可以抛给 java 层，但是要抛成 java 层的异常</span></span><br><span class="line">        jclass  je = env-&gt;<span class="built_in">FindClass</span>(<span class="string">&quot;java/lang/Exception&quot;</span>);</span><br><span class="line">        env -&gt;<span class="built_in">ThrowNew</span>(je,exception.<span class="built_in">what</span>());</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-NDK异常处理增强"><a href="#4-NDK异常处理增强" class="headerlink" title="4.NDK异常处理增强"></a>4.NDK异常处理增强</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Exception</span> : <span class="keyword">public</span> out_of_range &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Exception</span>(string mag) : <span class="built_in">out_of_range</span>(mag) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">Exception</span>() &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;析构函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">c_method</span><span class="params">()</span> </span>&#123; <span class="comment">// 如果是自己的写，1000 多行</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;抛异常&quot;</span> &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Exception</span>(<span class="string">&quot;异常了&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="built_in">c_method</span>();</span><br><span class="line">    &#125; <span class="built_in">catch</span> (Exception *e) &#123;</span><br><span class="line">        <span class="comment">// Exception：多次构造函数和析构函数</span></span><br><span class="line">        <span class="comment">// Exception&amp;：避免了多次创建对象（最多的）</span></span><br><span class="line">        <span class="comment">// Exception*：创建的对象会被析构，如果使用局部函数或者成员就会是一个野指针</span></span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;try异常&quot;</span> &lt;&lt; e-&gt;<span class="built_in">what</span>() &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">delete</span> e;</span><br><span class="line">    &#125; <span class="built_in">catch</span> (...) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;其它异常&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>异常</tag>
        <tag>C++学习</tag>
      </tags>
  </entry>
  <entry>
    <title>C++：08.string、c++const增强和引用本质分析</title>
    <url>/22.C++%E5%9F%BA%E7%A1%80-string%E3%80%81c++const%E5%A2%9E%E5%BC%BA%E5%92%8C%E5%BC%95%E7%94%A8%E6%9C%AC%E8%B4%A8%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="1-string-常见操作"><a href="#1-string-常见操作" class="headerlink" title="1. string 常见操作"></a>1. string 常见操作</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;algorithm&gt;</span> <span class="comment">// STL 算法包</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cctype&gt;</span>   <span class="comment">// 用到一个函数指针，回调函数</span></span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h2 id="1-1-string创建"><a href="#1-1-string创建" class="headerlink" title="1.1.string创建"></a>1.1.string创建</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">string str1 = <span class="string">&quot;123&quot;</span>;</span><br><span class="line"><span class="function">string <span class="title">str2</span><span class="params">(<span class="string">&quot;123&quot;</span>)</span></span>;</span><br><span class="line"><span class="function">string <span class="title">str3</span><span class="params">(<span class="number">5</span>,<span class="string">&#x27;A&#x27;</span>)</span></span>; <span class="comment">// 5 个 A = AAAAA</span></span><br><span class="line">string *str4 = <span class="keyword">new</span> <span class="built_in">string</span> (<span class="string">&quot;123&quot;</span>);</span><br></pre></td></tr></table></figure>
<h2 id="1-2-string-与-char-互转"><a href="#1-2-string-与-char-互转" class="headerlink" title="1.2. string 与 char* 互转"></a>1.2. string 与 char* 互转</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// string 转 char* c_str();</span></span><br><span class="line"><span class="type">const</span> <span class="type">char</span>* c_str1 = str<span class="number">1.</span><span class="built_in">c_str</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// char* 转 string</span></span><br><span class="line"><span class="type">char</span>* c_str = <span class="string">&quot;east&quot;</span>;</span><br><span class="line"><span class="function">string <span class="title">str</span><span class="params">(c_str)</span></span>; <span class="comment">// 对象</span></span><br></pre></td></tr></table></figure>
<h2 id="1-3-遍历"><a href="#1-3-遍历" class="headerlink" title="1.3. 遍历"></a>1.3. 遍历</h2><p><code>str.at(i)</code> 和 <code>str[i]</code>，区别在于 <code>str.at(i)</code>如果数组越界会报异常，而<code>str[i]</code>不一定</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; str.<span class="built_in">length</span>() + <span class="number">2</span>; i++)&#123;</span><br><span class="line">        cout &lt;&lt; str.<span class="built_in">at</span>(i) &lt;&lt;endl; <span class="comment">// 如果越界会抛异常</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; str.<span class="built_in">length</span>() + <span class="number">2</span>; i++)&#123;</span><br><span class="line">        cout &lt;&lt; str[i] &lt;&lt;endl; <span class="comment">// 不会报错！！！，会导致程序宕机，AS里面是可以的</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="built_in">catch</span> (...) &#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;异常了&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">string <span class="title">str</span><span class="params">(<span class="string">&quot;1234567&quot;</span>)</span></span>;</span><br><span class="line"><span class="comment">// 1. 字符串的遍历</span></span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; str.<span class="built_in">length</span>(); i++)&#123;</span><br><span class="line">    cout &lt;&lt; str[i] &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 迭代器遍历</span></span><br><span class="line"><span class="keyword">for</span>(string::iterator it = str.<span class="built_in">begin</span>(); it &lt; str.<span class="built_in">end</span>(); it++)&#123;</span><br><span class="line">    cout &lt;&lt; *it &lt;&lt;endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="1-4-添加"><a href="#1-4-添加" class="headerlink" title="1.4. 添加"></a>1.4. 添加</h2><ol>
<li><code>str1 = str1 + str2;</code></li>
<li><code>str1 = str1.append(str2);</code></li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    string str1 = <span class="string">&quot;123&quot;</span>;</span><br><span class="line">    string str2 = <span class="string">&quot;456&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//     str1 = str1 + str2;    </span></span><br><span class="line">     str1 = str<span class="number">1.</span><span class="built_in">append</span>(str2);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; str1 &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">123456</span><br></pre></td></tr></table></figure>
<h2 id="1-5-删除"><a href="#1-5-删除" class="headerlink" title="1.5. 删除"></a>1.5. 删除</h2><ul>
<li><code>str1.erase(it);</code></li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">string str1 = <span class="string">&quot;123 abc 123 abc 123&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// str1.erase(0,3);// 第一个参数：从哪里开始，第二个参数：删除几个（默认值，字符串结尾）</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 迭代器删除 2 bc 123 abc 123 解释</span></span><br><span class="line"><span class="keyword">for</span>(string::iterator it = str<span class="number">1.</span><span class="built_in">begin</span>(); it&lt;str<span class="number">1.</span><span class="built_in">begin</span>()<span class="number">+3</span>; it++)&#123; <span class="comment">// 删除一个节后都会从头开始计算</span></span><br><span class="line">    str<span class="number">1.</span><span class="built_in">erase</span>(it);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 删除一个节后都会从头开始计算</span><br><span class="line">2 bc 123 abc 123</span><br></pre></td></tr></table></figure>
<h2 id="1-6-替换"><a href="#1-6-替换" class="headerlink" title="1.6. 替换"></a>1.6. 替换</h2><ul>
<li><code>str1.replace(0,6,&quot;1234&quot;);</code>：从0开始，替换6个字符</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">string str1 = <span class="string">&quot;123 abc 123 abc 123&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 第一个参数：从哪里开始</span></span><br><span class="line"><span class="comment">// 第二个参数：替换几个</span></span><br><span class="line"><span class="comment">// 第三个参数：替换成谁</span></span><br><span class="line">str<span class="number">1.</span><span class="built_in">replace</span>(<span class="number">0</span>,<span class="number">6</span>,<span class="string">&quot;1234&quot;</span>);</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1234c 123 abc 123</span><br></pre></td></tr></table></figure>
<h2 id="1-7-查找"><a href="#1-7-查找" class="headerlink" title="1.7. 查找"></a>1.7. 查找</h2><ul>
<li><code>str1.find(&quot;123&quot;,0);</code>：// 查找谁，从哪里开始</li>
<li><code>str1.rfind(&quot;123&quot;);</code>：// 从后面往前找</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">string str1 = <span class="string">&quot;123 abc 123 abc 123&quot;</span>;</span><br><span class="line"><span class="comment">// 查找谁，从哪里开始</span></span><br><span class="line"><span class="comment">// int position = str1.find(&quot;123&quot;,0);</span></span><br><span class="line"><span class="comment">// 从后面往前找</span></span><br><span class="line"><span class="type">int</span> position = str<span class="number">1.</span><span class="built_in">rfind</span>(<span class="string">&quot;123&quot;</span>);</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0,16</span><br></pre></td></tr></table></figure>
<h2 id="1-8-大小写转换"><a href="#1-8-大小写转换" class="headerlink" title="1.8. 大小写转换"></a>1.8. 大小写转换</h2><ul>
<li><code>transform(str1.begin(),str1.end(),str1.begin(),::toupper);</code>：转大写</li>
<li><code>transform(str1.begin(),str1.end(),str1.begin(),::tolower);</code>：转小写</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    string str1 = <span class="string">&quot;AAA abc BBB abc 123&quot;</span>;</span><br><span class="line">    <span class="comment">// 转换成大写</span></span><br><span class="line">    <span class="built_in">transform</span>(str<span class="number">1.</span><span class="built_in">begin</span>(),str<span class="number">1.</span><span class="built_in">end</span>(),str<span class="number">1.</span><span class="built_in">begin</span>(),::toupper);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 转小写</span></span><br><span class="line">    <span class="built_in">transform</span>(str<span class="number">1.</span><span class="built_in">begin</span>(),str<span class="number">1.</span><span class="built_in">end</span>(),str<span class="number">1.</span><span class="built_in">begin</span>(),::tolower);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; str<span class="number">1.</span><span class="built_in">c_str</span>() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-c-相对-c-的一些增强"><a href="#2-c-相对-c-的一些增强" class="headerlink" title="2. c++ 相对 c 的一些增强"></a>2. c++ 相对 c 的一些增强</h1><h2 id="2-1-实用性增强"><a href="#2-1-实用性增强" class="headerlink" title="2.1 实用性增强"></a>2.1 实用性增强</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line"></span><br><span class="line">a=<span class="number">10</span>;</span><br><span class="line"><span class="type">int</span> b; <span class="comment">// 有的编译器不通过， c++ 中肯定能通过</span></span><br><span class="line">b = <span class="number">10</span>;</span><br></pre></td></tr></table></figure>
<h2 id="2-2-register-增强-c中报错，c-可以通过"><a href="#2-2-register-增强-c中报错，c-可以通过" class="headerlink" title="2.2 register 增强(c中报错，c++可以通过)"></a>2.2 register 增强(c中报错，c++可以通过)</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 寄存器上面的变量不能取地址，register 快速</span></span><br><span class="line"><span class="keyword">register</span> <span class="type">int</span> a = <span class="number">10</span>;  <span class="comment">// c中报错，c++可以通过</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,&amp;a);</span><br><span class="line"><span class="built_in">getchar</span>();</span><br></pre></td></tr></table></figure>
<h2 id="2-3-参数增强-以下代码，c中没问题，c-中会检测报错"><a href="#2-3-参数增强-以下代码，c中没问题，c-中会检测报错" class="headerlink" title="2.3 参数增强 (以下代码，c中没问题，c++中会检测报错)"></a>2.3 参数增强 (以下代码，c中没问题，c++中会检测报错)</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 参数处理增强</span></span><br><span class="line">    <span class="type">int</span> a = <span class="number">10</span>;</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,a);<span class="comment">// c中没问题，c++中会检测报错</span></span><br><span class="line">    </span><br><span class="line">        <span class="built_in">getchar</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h2 id="2-4-检测增强-以下代码，c中没问题，c-中会检测报错"><a href="#2-4-检测增强-以下代码，c中没问题，c-中会检测报错" class="headerlink" title="2.4 检测增强 (以下代码，c中没问题，c++中会检测报错)"></a>2.4 检测增强 (以下代码，c中没问题，c++中会检测报错)</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">f</span>()&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 检测增强</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">mian</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,<span class="built_in">f</span>()); <span class="comment">// c中没问题，c++中会检测报错</span></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-const-关键字"><a href="#3-const-关键字" class="headerlink" title="3. const 关键字"></a>3. const 关键字</h1><p>const 使用场景：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. 修饰变量不可改变</span></span><br><span class="line"><span class="comment">// const int number = 1;</span></span><br><span class="line"><span class="comment">// 修饰指针 常量指针，指针常量</span></span><br><span class="line"><span class="type">int</span> number = <span class="number">1</span>;</span><br><span class="line"><span class="comment">// const int *n_p = &amp;number;</span></span><br><span class="line"><span class="comment">// *n_p = 3; // 常量指针不能改变值</span></span><br><span class="line"><span class="comment">// 指针常量</span></span><br><span class="line"><span class="type">int</span> *<span class="type">const</span> n_p = &amp;number;</span><br><span class="line"><span class="type">int</span> a = <span class="number">200</span>;</span><br><span class="line"><span class="comment">// n_p = &amp;a; // 指针常量不能修改值</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 拷贝构造函数，操作符重载，方法后面（都是由于它的成员不可改变）</span></span><br></pre></td></tr></table></figure>
<p>区别：</p>
<ul>
<li>c 里面 const 是一个伪命题，可以通过指针去操作，编译时期的检测</li>
<li>c++ 里面不光在编译时期做处理，还在运行时做了处理</li>
</ul>
<h1 id="4-引用加强"><a href="#4-引用加强" class="headerlink" title="4. 引用加强"></a>4. 引用加强</h1><p>其实引用的本质就是 <code>指针</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    string name;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Student</span>(string name) : <span class="built_in">name</span>(name) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">string &amp;<span class="title">getName</span><span class="params">()</span> </span>&#123;<span class="comment">// java 想都别想</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Student stu = <span class="built_in">Student</span>(<span class="string">&quot;eastrise&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 改不动</span></span><br><span class="line">    <span class="comment">// stu.getName() = &quot;Jack&quot;;</span></span><br><span class="line"></span><br><span class="line">    stu.<span class="built_in">getName</span>() = <span class="string">&quot;Jack&quot;</span>;</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; stu.<span class="built_in">getName</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// vector front back</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++学习</tag>
      </tags>
  </entry>
  <entry>
    <title>C++：06.反转单链表、实现ArrayList</title>
    <url>/20.C++%E5%9F%BA%E7%A1%80-%E5%8F%8D%E8%BD%AC%E5%8D%95%E9%93%BE%E8%A1%A8%E3%80%81%E5%AE%9E%E7%8E%B0ArrayList/</url>
    <content><![CDATA[<h1 id="1-腾讯面试题："><a href="#1-腾讯面试题：" class="headerlink" title="1. 腾讯面试题："></a>1. 腾讯面试题：</h1><ol>
<li><p>用 c/c++ 反转单链表   </p>
<ul>
<li>输入: 1-&gt;2-&gt;3-&gt;4-&gt;5-&gt;NULL</li>
<li><p>输出: 5-&gt;4-&gt;3-&gt;2-&gt;1-&gt;NULL </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">ListNode</span> *<span class="built_in">reverseList</span>(<span class="keyword">struct</span> ListNode* head) &#123;</span><br><span class="line">	<span class="keyword">struct</span> <span class="title class_">ListNode</span> *newHead = <span class="literal">NULL</span>;</span><br><span class="line">	<span class="keyword">struct</span> <span class="title class_">ListNode</span> *node;</span><br><span class="line">	<span class="keyword">while</span> (head != <span class="literal">NULL</span>) &#123;</span><br><span class="line">		<span class="comment">//1. 对之前的链表做头删</span></span><br><span class="line">		node = head;</span><br><span class="line">		head = head-&gt;next;</span><br><span class="line">		</span><br><span class="line">		<span class="comment">//2. 对新链表做头插</span></span><br><span class="line">		node-&gt;next = newHead;</span><br><span class="line">		newHead = node;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> newHead;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>用 c/c++ 判断一棵树是否为平衡二叉树 （<strong>可以是一棵空树，左右子树的高度差不会超过 1 ，并且左右两棵子树都是一棵平衡二叉树</strong>）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *  判断一棵树是否是平衡二叉树</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isBalanceTree</span><span class="params">(TreeNode&lt;T&gt; *pNode)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 可以是一棵空树，左右子树的高度差不会超过 1 ，并且左右两棵子树都是一棵平衡二叉树</span></span><br><span class="line">    <span class="keyword">if</span>(!pNode)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 左右子树的高度差不会超过 1</span></span><br><span class="line">    <span class="type">int</span> left = <span class="built_in">getDepthTree</span>(pNode-&gt;left);</span><br><span class="line">    <span class="type">int</span> right = <span class="built_in">getDepthTree</span>(pNode-&gt;right);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 并且左右两棵子树都是一棵平衡二叉树</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">abs</span>(left-right) &lt;= <span class="number">1</span> &amp;&amp; <span class="built_in">isBalanceTree</span>(pNode-&gt;left) &amp;&amp; <span class="built_in">isBalanceTree</span>(pNode-&gt;right);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h1 id="2-ArrayList-源码分析"><a href="#2-ArrayList-源码分析" class="headerlink" title="2. ArrayList 源码分析"></a>2. ArrayList 源码分析</h1><p>重点：<code>如果开发中涉及到模板类，声明和实现要写到同一个类中，hpp = h + cpp/c (编译)</code></p>
<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/cpp20">NDKPractice项目的cpp20</a></strong></p>
<p>或者请看： jni/<code>20.C++基础-实现Native层的ArrayList</code></p>
<p><code>java中ArrayList</code>知识点：  </p>
<ol>
<li><strong>如果未指定容量大小的话，默认容量是 <code>10</code></strong></li>
<li><strong>每次扩容都是扩充的原来的一半 <code>oldCapacity + (oldCapacity &gt;&gt; 1);</code></strong></li>
</ol>
<p>代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifndef</span> JNI20_ARRAYLIST_H</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> JNI20_ARRAYLIST_H</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;malloc.h&gt;</span></span></span><br><span class="line"><span class="comment">//------------------------ 声明 ------------------------//</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ArrayList</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// 长度，数组，当前交表</span></span><br><span class="line">    E *array = <span class="literal">NULL</span>;      <span class="comment">// 当前数组指针</span></span><br><span class="line">    <span class="type">int</span> len = <span class="number">0</span>;   <span class="comment">// 数组大小</span></span><br><span class="line">    <span class="type">int</span> index = <span class="number">0</span>; <span class="comment">// 当前角标</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">ArrayList</span>();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">ArrayList</span>(<span class="type">int</span> len);</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">ArrayList</span>();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">ArrayList</span>(<span class="type">const</span> ArrayList&amp; list);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">add</span><span class="params">(E e)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">E <span class="title">remove</span><span class="params">(<span class="type">int</span> index)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">E <span class="title">get</span><span class="params">(<span class="type">int</span> index)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">size</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">ensureCapacityInternal</span><span class="params">(<span class="type">int</span> capacity)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">grow</span><span class="params">(<span class="type">int</span> capacity)</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//------------------------ 实现 ------------------------//</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">ArrayList&lt;E&gt;::<span class="built_in">ArrayList</span>() &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 每个方法都得添加</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">ArrayList&lt;E&gt;::<span class="built_in">ArrayList</span>(<span class="type">int</span> len) &#123;</span><br><span class="line">    <span class="keyword">if</span>(len &lt;= <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">this</span>-&gt;len = len;</span><br><span class="line">    <span class="keyword">this</span>-&gt;array = (E*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(E)*len);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">ArrayList&lt;E&gt;::~<span class="built_in">ArrayList</span>() &#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">this</span>-&gt;array)&#123;</span><br><span class="line">        <span class="built_in">free</span>(<span class="keyword">this</span>-&gt;array);</span><br><span class="line">        <span class="keyword">this</span>-&gt;array = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">ArrayList&lt;E&gt;::<span class="built_in">ArrayList</span>(<span class="type">const</span> ArrayList &amp;list) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;len = list.len;</span><br><span class="line">    <span class="keyword">this</span>-&gt;index = list.index;</span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">this</span>-&gt;array)&#123;</span><br><span class="line">       <span class="built_in">free</span>(<span class="keyword">this</span>-&gt;array);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">this</span>-&gt;array = <span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(E)*list.len);</span><br><span class="line">    <span class="built_in">memcpy</span>(<span class="keyword">this</span>-&gt;array,list.array, <span class="built_in">sizeof</span>(E)*index);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">int</span> ArrayList&lt;E&gt;::<span class="built_in">size</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>-&gt;index;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> ArrayList&lt;E&gt;::<span class="built_in">add</span>(E e) &#123;</span><br><span class="line">    <span class="built_in">ensureCapacityInternal</span>(index<span class="number">+1</span>); <span class="comment">// Increments modCount!!</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;array[index++] = e;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> ArrayList&lt;E&gt;::<span class="built_in">ensureCapacityInternal</span>(<span class="type">int</span> capacity) &#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">this</span>-&gt;array == <span class="literal">NULL</span>)</span><br><span class="line">        capacity = <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">if</span>(capacity &gt; <span class="keyword">this</span>-&gt;len)&#123;</span><br><span class="line">        <span class="comment">// 创建新数组</span></span><br><span class="line">        <span class="built_in">grow</span>(capacity);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> ArrayList&lt;E&gt;::<span class="built_in">grow</span>(<span class="type">int</span> capacity) &#123;</span><br><span class="line">    <span class="type">int</span> new_len = len + (len &gt;&gt; <span class="number">1</span>); <span class="comment">// 扩容 len的一半</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(capacity &gt; new_len)&#123;</span><br><span class="line">        new_len = capacity;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建新的数组</span></span><br><span class="line">    E* new_array = (E*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(E)*new_len);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">this</span>-&gt;array)&#123;</span><br><span class="line">        <span class="built_in">memcpy</span>(new_array,<span class="keyword">this</span>-&gt;array,<span class="built_in">sizeof</span>(E)* index); <span class="comment">// sizeof(E)*index 字节</span></span><br><span class="line">        <span class="comment">// 释放旧的array</span></span><br><span class="line">        <span class="built_in">free</span>(<span class="keyword">this</span>-&gt;array);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">this</span>-&gt;array = new_array;</span><br><span class="line">    <span class="keyword">this</span>-&gt;len = new_len;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E ArrayList&lt;E&gt;::<span class="built_in">get</span>(<span class="type">int</span> index) &#123;</span><br><span class="line">    <span class="keyword">return</span> array[index];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E ArrayList&lt;E&gt;::<span class="built_in">remove</span>(<span class="type">int</span> index) &#123;</span><br><span class="line">    E e = array[index];</span><br><span class="line">    <span class="type">int</span> numMoved = <span class="keyword">this</span>-&gt;index - index - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = index; i &lt; index + numMoved ;i++)&#123;</span><br><span class="line">        array[i] = array[i<span class="number">+1</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    array[--<span class="keyword">this</span>-&gt;index] = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> e;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span> <span class="comment">// JNI20_ARRAYLIST_H</span></span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++学习，面试</tag>
      </tags>
  </entry>
  <entry>
    <title>C++：09.STL容器的基本介绍和使用</title>
    <url>/23.C++%E5%9F%BA%E7%A1%80-STL%E5%AE%B9%E5%99%A8%E7%9A%84%E5%9F%BA%E6%9C%AC%E4%BB%8B%E7%BB%8D%E5%92%8C%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p><a href="https://en.cppreference.com/w/cpp/container/sets">学习精华：集合底层的数据结构原理</a></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stack&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;list&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;set&gt;</span></span></span><br></pre></td></tr></table></figure>
<h1 id="1-vector-容器"><a href="#1-vector-容器" class="headerlink" title="1. vector 容器"></a>1. vector 容器</h1><p>(1). 容量  </p>
<ul>
<li>向量大小： vec.size();</li>
<li>向量真实大小： vec.capacity();</li>
<li>向量判空： vec.empty();</li>
</ul>
<p>(2). 修改  </p>
<ul>
<li>末尾添加元素： vec.push_back();</li>
<li>末尾删除元素： vec.pop_back();</li>
<li>任意位置插入元素： vec.insert();</li>
<li>任意位置删除元素： vec.erase();</li>
<li>清空向量元素： vec.clear();</li>
</ul>
<p>(3)迭代器</p>
<ul>
<li>开始指针：vec.begin();</li>
<li>末尾指针：vec.end(); //指向最后一个元素的下一个位置</li>
<li>指向常量的末尾指针： vec.cend();</li>
</ul>
<p>(4)元素的访问</p>
<ul>
<li>下标访问： vec[1]; //并不会检查是否越界</li>
<li>at方法访问： vec.at(1); //以上两者的区别就是at会检查是否越界，是则抛出out of range异常</li>
<li>访问第一个元素： vec.front();</li>
<li>访问最后一个元素： vec.back();</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123; <span class="comment">// vector 数组</span></span><br><span class="line">    <span class="comment">// vector</span></span><br><span class="line">    <span class="comment">// 1. vector&lt;int&gt; v;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. vector&lt;int&gt; v(10);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. vector&lt;int&gt; v(10,0);</span></span><br><span class="line"></span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 插入数据</span></span><br><span class="line">    <span class="comment">// v.begin(); // 迭代器的开始位置</span></span><br><span class="line">    v.<span class="built_in">insert</span>(v.<span class="built_in">begin</span>(),<span class="number">12</span>);</span><br><span class="line">    v.<span class="built_in">insert</span>(v.<span class="built_in">begin</span>(),<span class="number">22</span>);</span><br><span class="line">    v.<span class="built_in">insert</span>(v.<span class="built_in">begin</span>(),<span class="number">32</span>);</span><br><span class="line"></span><br><span class="line">    v.<span class="built_in">insert</span>(v.<span class="built_in">end</span>(),<span class="number">42</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 引用当左值当右值（修改）</span></span><br><span class="line">    v.<span class="built_in">front</span>() = <span class="number">33</span>;</span><br><span class="line">    v.<span class="built_in">back</span>() = <span class="number">44</span>;</span><br><span class="line"></span><br><span class="line">    v.<span class="built_in">push_back</span>(<span class="number">55</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 移除最后的元素，并没有返回值</span></span><br><span class="line">    <span class="comment">// v.pop_back();</span></span><br><span class="line">    <span class="comment">// 通过迭代器位置进行移除</span></span><br><span class="line">    <span class="comment">// v.erase(v.begin());</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取数据 for 循环</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; v.<span class="built_in">size</span>(); i++)&#123;</span><br><span class="line">        cout &lt;&lt; v[i] &lt;&lt; <span class="string">&quot;\t&quot;</span>; <span class="comment">// 越界程序宕机,clion 和 AS中不一定出错</span></span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; endl;</span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; v.<span class="built_in">size</span>(); i++)&#123;</span><br><span class="line">        cout &lt;&lt; v.<span class="built_in">at</span>(i) &lt;&lt; <span class="string">&quot;\t&quot;</span>; <span class="comment">// 越界抛异常 out_of_range</span></span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 通过迭代器</span></span><br><span class="line">    <span class="keyword">for</span>(vector&lt;<span class="type">int</span>&gt;::iterator it = v.<span class="built_in">begin</span>(); it != v.<span class="built_in">end</span>(); it++)&#123;</span><br><span class="line">        cout &lt;&lt; *it &lt;&lt; <span class="string">&quot;\t&quot;</span>; <span class="comment">// 越界抛异常 out_of_range</span></span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-stack-容器（先进后出）"><a href="#2-stack-容器（先进后出）" class="headerlink" title="2. stack 容器（先进后出）"></a>2. stack 容器（先进后出）</h1><p><code>不能通过角标或迭代器去循环值</code></p>
<ul>
<li>压栈：s.push(12);</li>
<li>获取顶部元素：s.top(); </li>
<li>弹栈顶部元素：s.pop();</li>
<li>判空： s.empty();</li>
</ul>
<h1 id="3-queue-容器（先进先出）"><a href="#3-queue-容器（先进先出）" class="headerlink" title="3. queue 容器（先进先出）"></a>3. queue 容器（先进先出）</h1><ul>
<li>添加元素：s.push(12);</li>
<li>访问第一个元素： q.front();</li>
<li>访问最后一个元素： q.back();</li>
<li>弹出第一个元素：q.pop();</li>
</ul>
<h1 id="4-优先级队列-priority-queue"><a href="#4-优先级队列-priority-queue" class="headerlink" title="4. 优先级队列 priority_queue"></a>4. 优先级队列 priority_queue</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// int 存放的数据 vector&lt;int&gt; 数据类型（数组） greater 从大到小 less 从小到大</span></span><br><span class="line">priority_queue&lt;<span class="type">int</span>,vector&lt;<span class="type">int</span>&gt;,greater&lt;<span class="type">int</span>&gt;&gt; pq;</span><br><span class="line">pq.<span class="built_in">push</span>(<span class="number">12</span>);</span><br><span class="line">pq.<span class="built_in">push</span>(<span class="number">44</span>);</span><br><span class="line">pq.<span class="built_in">push</span>(<span class="number">32</span>);</span><br><span class="line">pq.<span class="built_in">push</span>(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 最大值 </span></span><br><span class="line">cout &lt;&lt; pq.<span class="built_in">top</span>() &lt;&lt; endl;</span><br></pre></td></tr></table></figure>
<h1 id="5-list-容器-链表，双向链表"><a href="#5-list-容器-链表，双向链表" class="headerlink" title="5. list 容器(链表，双向链表)"></a>5. list 容器(链表，双向链表)</h1><p><code>不能通过角标去访问</code></p>
<p>插入：  </p>
<ul>
<li>开头插入：l.push_front(11);</li>
<li>末尾插入：l.push_back(22);</li>
<li>指定的iterator位置插入：l.insert(l.begin(),10);</li>
</ul>
<p>修改：  </p>
<ul>
<li>修改最后一个元素：l.back() = 33;</li>
<li>修改最开头的元素：l.front() = 44;</li>
</ul>
<p>移除：  </p>
<ul>
<li>移除指定位置上元素：l.erase(l.begin());</li>
<li>移除开头元素：l.pop_front();</li>
<li>移除末尾元素：l.pop_back();</li>
</ul>
<p>循环：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 循环</span></span><br><span class="line"><span class="keyword">for</span> (list&lt;<span class="type">int</span>&gt;::iterator it = l.<span class="built_in">begin</span>(); it != l.<span class="built_in">end</span>(); it++)</span><br><span class="line">&#123;</span><br><span class="line">	cout &lt;&lt; *it &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="6-set-容器"><a href="#6-set-容器" class="headerlink" title="6. set 容器"></a>6. set 容器</h1><p><code>set 容器（红黑树结构），会对你存入的数据进行排序，但是不允许元素相同</code></p>
<ul>
<li><code>set&lt;int, greater&lt;int&gt;&gt; s;</code>：排序从大到小</li>
<li><code>set&lt;int, less&lt;int&gt;&gt; s;</code>：排序从大到小</li>
<li><code>pair&lt;set&lt;int,greater&lt;int&gt;&gt;::iterator ,bool&gt; res = s.insert(5);</code>：插入</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// set&lt;int, less&lt;int&gt;&gt; s; // 从小到大排序，默认就是 less</span></span><br><span class="line">   set&lt;<span class="type">int</span>, greater&lt;<span class="type">int</span>&gt;&gt; s; <span class="comment">// 从大到小</span></span><br><span class="line"></span><br><span class="line">   <span class="comment">// 添加参数， 不需要用迭代器，也不需要指定位置</span></span><br><span class="line">   s.<span class="built_in">insert</span>(<span class="number">3</span>);</span><br><span class="line">   s.<span class="built_in">insert</span>(<span class="number">5</span>);</span><br><span class="line">   s.<span class="built_in">insert</span>(<span class="number">4</span>);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// 重复的插入，并不会报错，返回两个值 插入迭代器的位置，是否插入功</span></span><br><span class="line">   pair&lt;set&lt;<span class="type">int</span>,greater&lt;<span class="type">int</span>&gt;&gt;::iterator ,<span class="type">bool</span>&gt; res = s.<span class="built_in">insert</span>(<span class="number">5</span>);</span><br><span class="line">   <span class="comment">// res.first;// 获取第一个参数</span></span><br><span class="line">   <span class="type">bool</span> insert_succeed = res.second;</span><br><span class="line">   <span class="keyword">if</span>(insert_succeed)&#123;</span><br><span class="line">       cout &lt;&lt; <span class="string">&quot;插入成功&quot;</span> &lt;&lt; endl;</span><br><span class="line">   &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">       cout &lt;&lt; <span class="string">&quot;插入失败&quot;</span> &lt;&lt; endl;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> count = s.<span class="built_in">count</span>(<span class="number">5</span>);</span><br><span class="line">   <span class="comment">// s.find();</span></span><br><span class="line"></span><br><span class="line">   <span class="keyword">for</span>(set&lt;<span class="type">int</span>&gt; :: iterator it = s.<span class="built_in">begin</span>(); it != s.<span class="built_in">end</span>(); it ++)&#123;</span><br><span class="line">       cout &lt;&lt; *it &lt;&lt; endl;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<h1 id="7-函数谓词和函数对象"><a href="#7-函数谓词和函数对象" class="headerlink" title="7.函数谓词和函数对象"></a>7.函数谓词和函数对象</h1><p>函数谓词：<code>按照特定的规则所表写的函数谓词</code></p>
<p>函数对象：有函数重载了<code>（）</code>运算符</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    string name;</span><br><span class="line">    <span class="type">int</span> grade;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Student</span>(string name,<span class="type">int</span> grade):<span class="built_in">name</span>(name),<span class="built_in">grade</span>(grade)&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 谓词（函数谓词）：按照特定的规则所表写的函数谓词</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">compare</span><span class="params">(<span class="type">const</span> Student&amp; _Left,<span class="type">const</span> Student&amp; _Right)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> _Left.grade &gt; _Right.grade;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数对象 仿函数</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">comparefuction</span>&#123;</span><br><span class="line">    <span class="comment">// 函数重载了（）运算符，函数对象，仿函数</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">const</span> Student&amp; _Left,<span class="type">const</span> Student&amp; _Right)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> _Left.grade &gt; _Right.grade; <span class="comment">// 从大到小</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 基本数据类型，对象数据类型</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    set&lt;Student,comparefuction&gt; s;</span><br><span class="line"></span><br><span class="line">    <span class="function">Student <span class="title">s1</span><span class="params">(<span class="string">&quot;eastrise&quot;</span>,<span class="number">2</span>)</span></span>;</span><br><span class="line">    <span class="function">Student <span class="title">s2</span><span class="params">(<span class="string">&quot;eastrise&quot;</span>,<span class="number">9</span>)</span></span>;</span><br><span class="line">    <span class="function">Student <span class="title">s3</span><span class="params">(<span class="string">&quot;eastrise&quot;</span>,<span class="number">5</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    s.<span class="built_in">insert</span>(s1);</span><br><span class="line">    s.<span class="built_in">insert</span>(s2);</span><br><span class="line">    s.<span class="built_in">insert</span>(s3);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(set&lt;Student&gt;::iterator it = s.<span class="built_in">begin</span>(); it!=s.<span class="built_in">end</span>(); it ++)&#123;</span><br><span class="line">        cout &lt;&lt; it-&gt;name &lt;&lt; <span class="string">&quot;,&quot;</span> &lt;&lt; it-&gt;grade &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="8-multiset容器"><a href="#8-multiset容器" class="headerlink" title="8. multiset容器"></a>8. multiset容器</h1><p><code>multiset容器 , 允许重复 ，用法和 set 一样</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// set&lt;int,less&lt;int&gt;&gt;;// 从小到大排序，默认就是 less</span></span><br><span class="line">multiset&lt;<span class="type">int</span>,greater&lt;<span class="type">int</span>&gt;&gt; ms;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 添加参数， 不需要用迭代器，也不需要指定位置</span></span><br><span class="line">ms.<span class="built_in">insert</span>(<span class="number">3</span>);</span><br><span class="line">ms.<span class="built_in">insert</span>(<span class="number">5</span>);</span><br><span class="line">ms.<span class="built_in">insert</span>(<span class="number">4</span>);</span><br><span class="line">ms.<span class="built_in">insert</span>(<span class="number">4</span>);</span><br><span class="line">ms.<span class="built_in">insert</span>(<span class="number">3</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span>(set&lt;<span class="type">int</span>&gt;::iterator it = ms.<span class="built_in">begin</span>(); it != ms.<span class="built_in">end</span>(); it++)&#123;</span><br><span class="line">    cout &lt;&lt; *it &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++学习</tag>
      </tags>
  </entry>
  <entry>
    <title>C++：10.map、函数对象、一元、二元谓词</title>
    <url>/24.C++%E5%9F%BA%E7%A1%80-map%E3%80%81%E5%87%BD%E6%95%B0%E5%AF%B9%E8%B1%A1%E3%80%81%E4%B8%80%E5%85%83%E3%80%81%E4%BA%8C%E5%85%83%E8%B0%93%E8%AF%8D/</url>
    <content><![CDATA[<h1 id="1-map"><a href="#1-map" class="headerlink" title="1. map"></a>1. <a href="https://en.cppreference.com/w/cpp/container/map">map</a></h1><p><code>map 不能存 key 重复的数据，要么不生效，要么覆盖</code></p>
<p><code>map 会对 key 排序，二叉数算法</code></p>
<h2 id="1-1-添加数据："><a href="#1-1-添加数据：" class="headerlink" title="1.1. 添加数据："></a>1.1. 添加数据：</h2><ul>
<li><code>map1.insert(pair&lt;int,string&gt;(01,&quot;01&quot;));</code> // 重复添加<code>不会覆盖值</code></li>
<li><code>map1.insert(make_pair(02,&quot;02&quot;));</code> // 重复添加<code>不会覆盖值</code></li>
<li><code>map1.insert(map&lt;int,string&gt;::value_type(03,&quot;03&quot;));</code> // 重复添加<code>不会覆盖值</code></li>
<li><code>map1[04] = &quot;04&quot;</code> // 重复添加<code>会覆盖</code> </li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">map&lt;<span class="type">int</span>,string&gt; map1;</span><br><span class="line"></span><br><span class="line">map<span class="number">1.</span><span class="built_in">insert</span>(<span class="built_in">pair</span>&lt;<span class="type">int</span>,string&gt;(<span class="number">01</span>,<span class="string">&quot;01&quot;</span>));</span><br><span class="line">map<span class="number">1.</span><span class="built_in">insert</span>(<span class="built_in">make_pair</span>(<span class="number">02</span>,<span class="string">&quot;02&quot;</span>));</span><br><span class="line">map<span class="number">1.</span><span class="built_in">insert</span>(map&lt;<span class="type">int</span>,string&gt;::<span class="built_in">value_type</span>(<span class="number">03</span>,<span class="string">&quot;03&quot;</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">//区别，如果用前面三种 key 重复添加，不生效</span></span><br><span class="line"><span class="comment">// map1.insert(map&lt;int,string&gt;::value_type(03,&quot;30&quot;));// 无效</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 第四种方式 = map[key] = value</span></span><br><span class="line">map1[<span class="number">04</span>] = <span class="string">&quot;04&quot;</span>;</span><br><span class="line"><span class="comment">// 这种方式会覆盖</span></span><br><span class="line">map1[<span class="number">04</span>] = <span class="string">&quot;40&quot;</span>;</span><br></pre></td></tr></table></figure>
<h2 id="1-2-判断是否添加成功"><a href="#1-2-判断是否添加成功" class="headerlink" title="1.2.判断是否添加成功:"></a>1.2.判断是否添加成功:</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">pair&lt;map&lt;<span class="type">int</span>,string&gt;::iterator,<span class="type">bool</span>&gt; p = map<span class="number">1.</span><span class="built_in">insert</span>(<span class="built_in">make_pair</span>(<span class="number">06</span>,<span class="string">&quot;06&quot;</span>));</span><br><span class="line"><span class="keyword">if</span>(p.second)&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;添加成功&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;添加失败&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="1-3-循环："><a href="#1-3-循环：" class="headerlink" title="1.3.循环："></a>1.3.循环：</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 循环迭代器</span></span><br><span class="line">   <span class="keyword">for</span>(map&lt;<span class="type">int</span>,string&gt;::iterator it = map<span class="number">1.</span><span class="built_in">begin</span>(); it != map<span class="number">1.</span><span class="built_in">end</span>(); it++)&#123;</span><br><span class="line">       cout &lt;&lt; it-&gt;first &lt;&lt; <span class="string">&quot; - &quot;</span> &lt;&lt; it-&gt;second.<span class="built_in">c_str</span>() &lt;&lt; endl;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<h2 id="1-4-删除："><a href="#1-4-删除：" class="headerlink" title="1.4.删除："></a>1.4.删除：</h2><p><code>auto</code>：可以推到出类型</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::map&lt;<span class="type">int</span>, std::string&gt; c = &#123;&#123;<span class="number">1</span>, <span class="string">&quot;one&quot;</span>&#125;, &#123;<span class="number">2</span>, <span class="string">&quot;two&quot;</span>&#125;, &#123;<span class="number">3</span>, <span class="string">&quot;three&quot;</span>&#125;,</span><br><span class="line">                                    &#123;<span class="number">4</span>, <span class="string">&quot;four&quot;</span>&#125;, &#123;<span class="number">5</span>, <span class="string">&quot;five&quot;</span>&#125;, &#123;<span class="number">6</span>, <span class="string">&quot;six&quot;</span>&#125;&#125;;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// erase all odd numbers from c</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> it = c.<span class="built_in">begin</span>(); it != c.<span class="built_in">end</span>(); ) &#123;</span><br><span class="line">        <span class="keyword">if</span>(it-&gt;first % <span class="number">2</span> == <span class="number">1</span>)</span><br><span class="line">            it = c.<span class="built_in">erase</span>(it);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            ++it;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span>&amp; p : c) &#123;</span><br><span class="line">        std::cout &lt;&lt; p.second &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="1-5查找："><a href="#1-5查找：" class="headerlink" title="1.5查找："></a>1.5查找：</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">map&lt;<span class="type">int</span>,string&gt;::iterator find_it = map<span class="number">1.f</span>ind(<span class="number">0</span>); <span class="comment">// 0指的是key</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// cout &lt;&lt; find_it-&gt;first &lt;&lt; &quot; - &quot; &lt;&lt; find_it-&gt;second.c_str() &lt;&lt; endl;</span></span><br><span class="line"><span class="keyword">if</span>(find_it!=map<span class="number">1.</span><span class="built_in">end</span>())&#123;</span><br><span class="line">    cout &lt;&lt; find_it-&gt;first &lt;&lt; <span class="string">&quot; - &quot;</span> &lt;&lt; find_it-&gt;second.<span class="built_in">c_str</span>() &lt;&lt; endl;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot; 找不到 &quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-multimap"><a href="#2-multimap" class="headerlink" title="2. multimap"></a>2. <a href="https://en.cppreference.com/w/cpp/container/multimap">multimap</a></h1><p><code>multimap 可存 key 重复的数据</code>, 基本操作与 <code>map</code> 类似<br><code>multimap</code> 是没有排序的</p>
<p>遍历和分组查询：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 遍历</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">auto</span> it = map<span class="number">1.</span><span class="built_in">begin</span>(); it != map<span class="number">1.</span><span class="built_in">end</span>(); it++)&#123;</span><br><span class="line">    cout &lt;&lt; it-&gt;first &lt;&lt; <span class="string">&quot; - &quot;</span> &lt;&lt; it-&gt;second.<span class="built_in">c_str</span>() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line">cout &lt;&lt; <span class="string">&quot; 遍历结束 &quot;</span> &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 分组查询 多个数据</span></span><br><span class="line">multimap&lt;<span class="type">int</span>,string&gt;::iterator find_it = map<span class="number">1.f</span>ind(<span class="number">3</span>);</span><br><span class="line"><span class="keyword">while</span>(find_it!=map<span class="number">1.</span><span class="built_in">end</span>())&#123;</span><br><span class="line">    cout &lt;&lt; find_it-&gt;first &lt;&lt; <span class="string">&quot; - &quot;</span> &lt;&lt; find_it-&gt;second.<span class="built_in">c_str</span>() &lt;&lt; endl;</span><br><span class="line">    find_it ++;</span><br><span class="line">    <span class="comment">// 不是我们要找的内容</span></span><br><span class="line">    <span class="keyword">if</span>(find_it == map<span class="number">1.</span><span class="built_in">end</span>() || find_it -&gt; first != <span class="number">3</span>)&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;到底了&quot;</span> &lt;&lt;endl;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-容器存对象"><a href="#3-容器存对象" class="headerlink" title="3. 容器存对象"></a>3. 容器存对象</h1><p><code>java</code> 和 <code>c++</code> 的区别：<br>java: <code>java</code> 中把对象添加到了集合,这个对象发送了改变，集合中的对象也会发生改变，他们是同一块地址<br>c++：中会调用对象的拷贝构造函数，<code>存进去的是另一个对象</code></p>
<p>c++ 中容器中存对象需要注意的点：  </p>
<ol>
<li>没有默认的构造函数</li>
<li>析构函数也可能回调多次，如果说在析构函数中释放内存，需要在拷贝构造函数中进行深拷贝</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    vector&lt;Person&gt; vector1;</span><br><span class="line"></span><br><span class="line">    <span class="function">Person <span class="title">person</span><span class="params">(<span class="string">&quot;eastrise&quot;</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 会调用对象的拷贝构造函数，存进去的是另一个对象</span></span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(person);</span><br><span class="line"></span><br><span class="line">    person.<span class="built_in">setName</span>(<span class="string">&quot;Jack&quot;</span>);</span><br><span class="line"></span><br><span class="line">    Person p1 = vector<span class="number">1.</span><span class="built_in">back</span>();</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; p<span class="number">1.</span>name.<span class="built_in">c_str</span>() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-函数对象"><a href="#4-函数对象" class="headerlink" title="4. 函数对象"></a>4. 函数对象</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Compare</span>&#123;</span><br><span class="line">    <span class="comment">// 重载了括号操作符</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">()</span></span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;仿函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">compare1</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;普通函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 4. 函数对象（仿函数） 一元谓词，二元谓词</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Compare compare;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 跟函数非常类似</span></span><br><span class="line">    <span class="built_in">compare</span>();</span><br><span class="line">    <span class="comment">// 普通函数调用</span></span><br><span class="line">    <span class="built_in">compare1</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-一元谓词"><a href="#5-一元谓词" class="headerlink" title="5. 一元谓词"></a>5. 一元谓词</h1><p>定义：重载<code>()</code>，并且需要传递<code>1</code>个参数,常返回int值用于计数</p>
<ul>
<li><code>printObj = for_each(set1.begin(),set1.end(),printObj);</code>：最后一个参数传一元函数对象</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">(<span class="type">int</span> number)</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; number &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 仿函数 - 一元谓词（能够记录状态）</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">PrintObj</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span> number)</span></span>&#123;</span><br><span class="line">        cout &lt;&lt; number &lt;&lt; endl;</span><br><span class="line">        count ++;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 回调函数和仿函数的区别</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    set&lt;<span class="type">int</span>&gt; set1;</span><br><span class="line"></span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="number">1</span>);</span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="number">2</span>);</span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="number">3</span>);</span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="number">4</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// for_each 迭代器，非常重要的一点就是：仿函数如果要保存记录状态，要确保对象一致，可以用返回值</span></span><br><span class="line">    <span class="comment">// for_each(set1.begin(),set1.end(),print);</span></span><br><span class="line">    PrintObj printObj;</span><br><span class="line">    <span class="comment">// 如果不用返回值，那么printObj 和 </span></span><br><span class="line">    printObj = for_each(set<span class="number">1.</span><span class="built_in">begin</span>(),set<span class="number">1.</span><span class="built_in">end</span>(),printObj);</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;个数：&quot;</span> &lt;&lt; printObj.count &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="6-二元谓词"><a href="#6-二元谓词" class="headerlink" title="6. 二元谓词"></a>6. 二元谓词</h1><p>定义：重载<code>()</code>，并且需要传递<code>2</code>个参数,常返回bool值用于算法判断条件</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">CompareObj</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">const</span> string str1,<span class="type">const</span> string str2)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> str1 &lt; str2;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 二元谓词的仿函数</span></span><br><span class="line">    set&lt;string,CompareObj&gt; set1;</span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="string">&quot;aaa&quot;</span>);</span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="string">&quot;aAa&quot;</span>);</span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="string">&quot;ccc&quot;</span>);</span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="string">&quot;ddd&quot;</span>);</span><br><span class="line">    <span class="comment">// 是否包含 aaa ， 遍历比较 ， 找方法</span></span><br><span class="line">    <span class="keyword">for</span>(set&lt;string&gt;::iterator it = set<span class="number">1.</span><span class="built_in">begin</span>();it != set<span class="number">1.</span><span class="built_in">end</span>(); it ++)&#123;</span><br><span class="line">        cout &lt;&lt; (*it).<span class="built_in">c_str</span>() &lt;&lt;  endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++学习</tag>
      </tags>
  </entry>
  <entry>
    <title>C++：11.C++中常用函数对象</title>
    <url>/25.C++%E5%9F%BA%E7%A1%80-C++%E4%B8%AD%E5%B8%B8%E7%94%A8%E5%87%BD%E6%95%B0%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<p><a href="https://www.jianshu.com/p/6badcff31c50">源码阅读和追踪</a></p>
<h1 id="仿函数"><a href="#仿函数" class="headerlink" title="仿函数"></a>仿函数</h1><p><code>必须重载()运算符</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Equal</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> equal_number;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Equal</span>(<span class="type">int</span> equal_number):<span class="built_in">equal_number</span>(equal_number)&#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">const</span> <span class="type">int</span>&amp; number)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> equal_number == number;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h1 id="1-预定义函数对象和函数适配器-bind2nd"><a href="#1-预定义函数对象和函数适配器-bind2nd" class="headerlink" title="1. 预定义函数对象和函数适配器(bind2nd)"></a>1. 预定义函数对象和函数适配器(bind2nd)</h1><p>c/c++ 提供了很多定义好的函数对象,常见的几个 <code>less</code>, <code>greater</code>, <code>plus</code>, <code>equal_to</code>，这些都是二元函数</p>
<p><strong>bind1st和bind2nd是将二元函数转换为一元函数，比如一个比较大小的函数是二元函数。</strong> 当在某些情况下我们想要固定第一个参数或者第二个参数时，就成了一元函数</p>
<ul>
<li><code>bind2nd(equal_to&lt;string&gt;(),&quot;aaa&quot;)</code></li>
<li><code>set&lt;string&gt; :: iterator  find_it = find_if(set1.begin(),set1.end(),bind2nd(equal_to&lt;string&gt;(),&quot;aaa&quot;));</code>// 查找是否包含<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// c/c++ 提供了很多定义好的函数对象</span></span><br><span class="line">    <span class="comment">// 常见的几个 less, greater , plus , equal_to</span></span><br><span class="line">    plus&lt;string&gt; strAdd;</span><br><span class="line">    string str = <span class="built_in">strAdd</span>(<span class="string">&quot;aaa&quot;</span>,<span class="string">&quot;bbb&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// cout &lt;&lt; str.c_str() &lt;&lt; endl; // aaabbb</span></span><br><span class="line"></span><br><span class="line">    set&lt;string,greater&lt;string&gt;&gt; set1;</span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="string">&quot;aaa&quot;</span>);</span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="string">&quot;bbb&quot;</span>);</span><br><span class="line">    set<span class="number">1.</span><span class="built_in">insert</span>(<span class="string">&quot;ccc&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 判断是不是包含 aaa</span></span><br><span class="line">    <span class="comment">// 怎么写仿函数，一定要确定好你的仿函数的参数</span></span><br><span class="line">    <span class="comment">// bind2nd 函数适配器， aaa 相当于 equal_to 中的 right</span></span><br><span class="line"></span><br><span class="line">    set&lt;string&gt; :: iterator  find_it = <span class="built_in">find_if</span>(set<span class="number">1.</span><span class="built_in">begin</span>(),set<span class="number">1.</span><span class="built_in">end</span>(),<span class="built_in">bind2nd</span>(<span class="built_in">equal_to</span>&lt;string&gt;(),<span class="string">&quot;aaa&quot;</span>));</span><br><span class="line">    <span class="keyword">if</span>(find_it!=set<span class="number">1.</span><span class="built_in">end</span>())&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;找到了&quot;</span> &lt;&lt; (*find_it).<span class="built_in">c_str</span>() &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h1 id="2-自定义仿函数（函数对象）-和-count-if"><a href="#2-自定义仿函数（函数对象）-和-count-if" class="headerlink" title="2.自定义仿函数（函数对象） 和 count_if"></a>2.自定义仿函数（函数对象） 和 count_if</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Equal</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> equal_number;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Equal</span>(<span class="type">int</span> equal_number):<span class="built_in">equal_number</span>(equal_number)&#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">const</span> <span class="type">int</span>&amp; number)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> equal_number == number;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// count_if</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; vector1;</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">1</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">3</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">4</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 找集合中 等于 2 的个数</span></span><br><span class="line">    <span class="type">int</span> count = <span class="built_in">count_if</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),<span class="built_in">Equal</span>(<span class="number">2</span>));</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;count = &quot;</span> &lt;&lt; count &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 预定义好的函数对象 +  函数适配器</span></span><br><span class="line">    count = <span class="built_in">count_if</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),<span class="built_in">bind2nd</span>(<span class="built_in">equal_to</span>&lt;<span class="type">int</span>&gt;(),<span class="number">2</span>));</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;count = &quot;</span> &lt;&lt; count &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">count = 3</span><br><span class="line">count = 3</span><br></pre></td></tr></table></figure>
<h1 id="3-foreach-transform"><a href="#3-foreach-transform" class="headerlink" title="3. foreach, transform"></a>3. foreach, transform</h1><ul>
<li><code>transform(vector1.begin(),vector1.end(),vector2.begin(),transform_print);</code>：修改</li>
<li><code>for_each(vector2.begin(),vector2.end(),print);</code>：循环遍历元素</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">(<span class="type">int</span> number)</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; number &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 进行修改</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">transform_print</span><span class="params">(<span class="type">int</span> number)</span></span>&#123;</span><br><span class="line">    <span class="comment">// cout &lt;&lt; number &lt;&lt; endl;</span></span><br><span class="line">    <span class="keyword">return</span> number + <span class="number">3</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; vector1;</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">1</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">3</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">4</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// for_each(vector1.begin(),vector1.end(),print);</span></span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; vector2;</span><br><span class="line">    vector<span class="number">2.</span><span class="built_in">resize</span>(vector<span class="number">1.</span><span class="built_in">size</span>());</span><br><span class="line"></span><br><span class="line">    <span class="built_in">transform</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),vector<span class="number">2.</span><span class="built_in">begin</span>(),transform_print);</span><br><span class="line"></span><br><span class="line">    for_each(vector<span class="number">2.</span><span class="built_in">begin</span>(),vector<span class="number">2.</span><span class="built_in">end</span>(),print);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td></tr></table></figure>
<h1 id="4-find-find-if"><a href="#4-find-find-if" class="headerlink" title="4. find, find_if"></a>4. find, find_if</h1><ul>
<li><code>find(first, end, value);</code>：返回区间[first,end)中第一个值等于value的元素的位置。如果没有找到匹配元素，则返回end。(只能判断等于)</li>
<li><code>find_if (begin, end, func)</code>：从begin开始 ，到end为止，返回第一个让 func这个函数返回true的iterator。（所有判断都可以）</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="type">int</span>&gt; vector1;</span><br><span class="line">vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">1</span>);</span><br><span class="line">vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line">vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">3</span>);</span><br><span class="line">vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">4</span>);</span><br><span class="line"></span><br><span class="line">vector&lt;<span class="type">int</span>&gt;::iterator find_it = <span class="built_in">find</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),<span class="number">2</span>);</span><br><span class="line"><span class="keyword">if</span>(find_it!=vector<span class="number">1.</span><span class="built_in">end</span>())&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;包含&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;不包含&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 有没有大于2的，自定义函数对象，预定义函数对象+函数适配器，省略</span></span><br><span class="line">vector&lt;<span class="type">int</span>&gt;::iterator find_it1 = <span class="built_in">find_if</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),<span class="built_in">bind2nd</span>(<span class="built_in">less</span>&lt;<span class="type">int</span>&gt;(),<span class="number">4</span>));</span><br><span class="line"><span class="keyword">while</span>(find_it1!=vector<span class="number">1.</span><span class="built_in">end</span>())&#123;</span><br><span class="line">    <span class="keyword">if</span>(*find_it1&gt;=<span class="number">4</span>)&#123;</span><br><span class="line">        find_it1++;</span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; *find_it1 &lt;&lt; endl;</span><br><span class="line">    find_it1++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">包含</span><br><span class="line">1</span><br><span class="line">3</span><br><span class="line">2</span><br></pre></td></tr></table></figure>
<h1 id="5-count，count-if"><a href="#5-count，count-if" class="headerlink" title="5. count，count_if"></a>5. count，count_if</h1><ul>
<li><code>count(first, end, value);</code>：返回区间[first,end)中值等于value的元素的个数。(只能判断等于)</li>
<li><code>count_if(begin, end, func)</code>：从begin开始 ，到end为止，返回让 func这个函数返回true的元素个数。（所有判断都可以）</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="type">int</span>&gt; vector1;</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">1</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">3</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">4</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> number = <span class="built_in">count</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;等于2的个数：&quot;</span> &lt;&lt; number &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    number = <span class="built_in">count_if</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),<span class="built_in">bind2nd</span>(<span class="built_in">less</span>&lt;<span class="type">int</span>&gt;(),<span class="number">2</span>));</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;小于2的个数：&quot;</span> &lt;&lt; number &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    number = <span class="built_in">count_if</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),<span class="built_in">bind2nd</span>(<span class="built_in">greater</span>&lt;<span class="type">int</span>&gt;(),<span class="number">2</span>));</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;大于2的个数：&quot;</span> &lt;&lt; number &lt;&lt; endl;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">等于2的个数：2</span><br><span class="line">小于2的个数：1</span><br><span class="line">大于2的个数：2</span><br></pre></td></tr></table></figure>
<h1 id="6-merge"><a href="#6-merge" class="headerlink" title="6. merge"></a>6. merge</h1><ul>
<li><code>merge(vector1.begin(),vector1.end(),vector2.begin(),vector2.end(),vector3.begin());</code>：合并元素</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">_merge</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span> number1,<span class="type">int</span> number2)</span></span>&#123;</span><br><span class="line">       <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">(<span class="type">int</span> number)</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; number &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// merge</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 两个有序数组进行合并 - 归并排序</span></span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; vector1;</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">1</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; vector2;</span><br><span class="line">    vector<span class="number">2.</span><span class="built_in">push_back</span>(<span class="number">4</span>);</span><br><span class="line">    vector<span class="number">2.</span><span class="built_in">push_back</span>(<span class="number">5</span>);</span><br><span class="line">    vector<span class="number">2.</span><span class="built_in">push_back</span>(<span class="number">6</span>);</span><br><span class="line"></span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; vector3;</span><br><span class="line">    vector<span class="number">3.</span><span class="built_in">resize</span>(<span class="number">6</span>);</span><br><span class="line">    <span class="built_in">merge</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),vector<span class="number">2.</span><span class="built_in">begin</span>(),vector<span class="number">2.</span><span class="built_in">end</span>(),vector<span class="number">3.</span><span class="built_in">begin</span>());</span><br><span class="line"></span><br><span class="line">    for_each(vector<span class="number">3.</span><span class="built_in">begin</span>(),vector<span class="number">3.</span><span class="built_in">end</span>(),print);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td></tr></table></figure>
<h1 id="7-sort"><a href="#7-sort" class="headerlink" title="7. sort"></a>7. sort</h1><ul>
<li><code>sort(vector1.begin(),vector1.end(),less&lt;int&gt;());</code>：按照从小到大的方式排序</li>
<li><code>random_shuffle(vector1.begin(),vector1.end());</code>：打乱 vector1的顺序</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="type">int</span>&gt; vector1;</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">1</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">3</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">4</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">sort</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),<span class="built_in">less</span>&lt;<span class="type">int</span>&gt;());</span><br><span class="line">    for_each(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),print);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 打乱顺序</span></span><br><span class="line">    <span class="built_in">random_shuffle</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>());</span><br><span class="line">    for_each(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),print);</span><br></pre></td></tr></table></figure>
<h1 id="8-copy，replace"><a href="#8-copy，replace" class="headerlink" title="8. copy，replace"></a>8. copy，replace</h1><ul>
<li><code>copy(vector1.begin(),vector1.begin()+2,vector2.begin());</code>：将vector1的前两位拷贝到vector2中，从vector2的开始位置拷入。</li>
<li><code>replace(vector1.begin(),vector1.end(),2,22);</code>：将vector1中等于2的值替换成22</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="type">int</span>&gt; vector1;</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">1</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">3</span>);</span><br><span class="line">    vector<span class="number">1.</span><span class="built_in">push_back</span>(<span class="number">4</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">vector2</span><span class="params">(<span class="number">2</span>)</span></span>;</span><br><span class="line">    <span class="built_in">copy</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">begin</span>()<span class="number">+2</span>,vector<span class="number">2.</span><span class="built_in">begin</span>());</span><br><span class="line">    <span class="comment">// for_each(vector2.begin(),vector2.end(),print);</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">replace</span>(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),<span class="number">2</span>,<span class="number">22</span>);</span><br><span class="line">    for_each(vector<span class="number">1.</span><span class="built_in">begin</span>(),vector<span class="number">1.</span><span class="built_in">end</span>(),print);</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++学习</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：06.递归和数组实现栈结构</title>
    <url>/31.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E9%80%92%E5%BD%92%E5%92%8C%E6%95%B0%E7%BB%84%E5%AE%9E%E7%8E%B0%E6%A0%88%E7%BB%93%E6%9E%84/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure31recursionandstack">NDKPractice项目的datastructure31recursionandstack</a></strong></p>
<h1 id="1-递归和循环的区别："><a href="#1-递归和循环的区别：" class="headerlink" title="1. 递归和循环的区别："></a>1. 递归和循环的区别：</h1><ul>
<li>循环： <code>高效一些</code>,<code>循环能解决的问题递归也能解决</code></li>
<li>递归： <code>低效一些</code>，<code>递归能解决的问题循环不一定能解决</code></li>
</ul>
<h1 id="2-java-中的-stack（继承至-Vector）"><a href="#2-java-中的-stack（继承至-Vector）" class="headerlink" title="2. java 中的 stack（继承至 Vector）"></a>2. java 中的 stack（继承至 Vector）</h1><ul>
<li>vector ：没指定大小的情况下，初始大小为 <code>10</code>,扩容为当前大小的<code>一倍</code></li>
<li>ArrayList ： 没指定大小的情况下，初始大小为 <code>10</code>,扩容为当前大小的<code>二分之一</code></li>
</ul>
<h1 id="2-数组实现栈结构"><a href="#2-数组实现栈结构" class="headerlink" title="2. 数组实现栈结构"></a>2. 数组实现栈结构</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ArrayStack</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// 栈顶元素的角标位置</span></span><br><span class="line">    <span class="type">int</span> top = <span class="number">-1</span>;</span><br><span class="line">    E *array = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="type">int</span> size = <span class="number">10</span>; <span class="comment">// 栈的初始大小</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 构造和析构</span></span><br><span class="line">    <span class="built_in">ArrayStack</span>();</span><br><span class="line">    ~<span class="built_in">ArrayStack</span>();</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  将元素压入栈中</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push</span><span class="params">(E e)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  获取栈顶的元素，不弹出</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function">E <span class="title">peek</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 弹出栈顶元素</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function">E <span class="title">pop</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  栈是否为空</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isEmpty</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">growArray</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">ArrayStack&lt;E&gt;::<span class="built_in">ArrayStack</span>() &#123;</span><br><span class="line">    array = (E*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(E)*size);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">ArrayStack&lt;E&gt;::~<span class="built_in">ArrayStack</span>() &#123;</span><br><span class="line">    <span class="keyword">delete</span>[] array;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> ArrayStack&lt;E&gt;::<span class="built_in">growArray</span>()&#123;</span><br><span class="line">    size = size &lt;&lt; <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// 改变内存空间大小</span></span><br><span class="line">    array = (E*) <span class="built_in">realloc</span>(array,size * <span class="built_in">sizeof</span>(E));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> ArrayStack&lt;E&gt;::<span class="built_in">push</span>(E e) &#123;</span><br><span class="line">    <span class="keyword">if</span>(top + <span class="number">1</span> &gt;= size)</span><br><span class="line">        <span class="built_in">growArray</span>(); <span class="comment">// 扩容</span></span><br><span class="line">    array[top++] = e;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E ArrayStack&lt;E&gt;::<span class="built_in">peek</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> array[top];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E ArrayStack&lt;E&gt;::<span class="built_in">pop</span>() &#123;</span><br><span class="line">    <span class="built_in">assert</span>(top &gt;= <span class="number">0</span>);</span><br><span class="line">    E e = <span class="built_in">peek</span>();</span><br><span class="line">    array[top--] = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">return</span> e;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">bool</span> ArrayStack&lt;E&gt;::<span class="built_in">isEmpty</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> top &lt; <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-链表实现栈结构"><a href="#3-链表实现栈结构" class="headerlink" title="3. 链表实现栈结构"></a>3. 链表实现栈结构</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Node</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    E value = <span class="literal">NULL</span>;</span><br><span class="line">    Node&lt;E&gt; *next = <span class="literal">NULL</span>;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Node</span>(E value, Node&lt;E&gt; *next) : <span class="built_in">value</span>(value), <span class="built_in">next</span>(next) &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">LinkedStack</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Node&lt;E&gt; *first = <span class="literal">NULL</span>; <span class="comment">// 头结点</span></span><br><span class="line">    <span class="type">int</span> index = <span class="number">-1</span>;</span><br><span class="line">    Node&lt;E&gt; *top = <span class="literal">NULL</span>; <span class="comment">// 栈顶元素</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">LinkedStack</span>();</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">LinkedStack</span>();</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  将元素压入栈中</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push</span><span class="params">(E e)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  获取栈顶的元素，不弹出</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function">E <span class="title">peek</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 弹出栈顶元素</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function">E <span class="title">pop</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  栈是否为空</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isEmpty</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function">Node&lt;E&gt; *<span class="title">node</span><span class="params">(<span class="type">int</span> index)</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">Node&lt;E&gt; *LinkedStack&lt;E&gt;::<span class="built_in">node</span>(<span class="type">int</span> index) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (index == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> first;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (index &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    Node&lt;E&gt; *h = first;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; index; ++i) &#123;</span><br><span class="line">        h = h-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> h;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> LinkedStack&lt;E&gt;::<span class="built_in">push</span>(E e) &#123;</span><br><span class="line">    Node&lt;E&gt; *new_node = <span class="keyword">new</span> <span class="built_in">Node</span>&lt;E&gt;(e, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span> (!first)</span><br><span class="line">        first = new_node;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        top-&gt;next = new_node;</span><br><span class="line">    top = new_node;</span><br><span class="line">    index++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E LinkedStack&lt;E&gt;::<span class="built_in">peek</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> top;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E LinkedStack&lt;E&gt;::<span class="built_in">pop</span>() &#123;</span><br><span class="line">    <span class="built_in">assert</span>(index &gt;= <span class="number">0</span>);</span><br><span class="line">    E e = top-&gt;value;</span><br><span class="line">    <span class="keyword">delete</span> top;</span><br><span class="line"></span><br><span class="line">    top = <span class="built_in">node</span>(--index);</span><br><span class="line">    <span class="keyword">return</span> e;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">bool</span> LinkedStack&lt;E&gt;::<span class="built_in">isEmpty</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> index &lt; <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">LinkedStack&lt;E&gt;::<span class="built_in">LinkedStack</span>() &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">LinkedStack&lt;E&gt;::~<span class="built_in">LinkedStack</span>() &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; index; ++i) &#123;</span><br><span class="line">        <span class="built_in">delete</span>(<span class="built_in">node</span>(i));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    first = <span class="literal">NULL</span>;</span><br><span class="line">    top = <span class="literal">NULL</span>;</span><br><span class="line">    index = <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
        <category>Stack</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>Stack</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：03.冒泡、选择、插入排序</title>
    <url>/28.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E5%86%92%E6%B3%A1%E3%80%81%E9%80%89%E6%8B%A9%E3%80%81%E6%8F%92%E5%85%A5%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure28bubbleselectsort">NDKPractice项目的datastructure</a></strong></p>
<h1 id="1-Ndk中，宏定义Log打印"><a href="#1-Ndk中，宏定义Log打印" class="headerlink" title="1. Ndk中，宏定义Log打印"></a>1. Ndk中，宏定义<code>Log</code>打印</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> TAG <span class="string">&quot;TAG&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LOGE(...) __android_log_print(ANDROID_LOG_ERROR,TAG,__VA_ARGS__)</span></span><br><span class="line"><span class="comment">// 其他方法的定义 __android_log_print 取了一个别名（参数固定，可变）</span></span><br></pre></td></tr></table></figure>
<h1 id="2-c-中的rand-随机"><a href="#2-c-中的rand-随机" class="headerlink" title="2. c++ 中的rand() 随机"></a>2. c++ 中的rand() 随机</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//  输出 0 到 49 之间的随机数</span></span><br><span class="line"> <span class="built_in">rand</span>() % <span class="number">50</span></span><br></pre></td></tr></table></figure>
<h1 id="3-冒泡排序"><a href="#3-冒泡排序" class="headerlink" title="3. 冒泡排序"></a>3. 冒泡排序</h1><ul>
<li><code>思想</code>：相邻两个数进行比较，如果前面的比后面的大，就进行交换，否则不需要交换。<strong><code>每次循环完毕都会在末尾选出一个最大的数</code></strong>  </li>
<li><code>时间复杂度</code>：<strong>O($n^2$)</strong></li>
<li><code>空间复杂度</code>：<strong>O(1)</strong></li>
</ul>
<p>看图：<br><img src="/images/冒泡思想.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">bubbleSort</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> len)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; len - <span class="number">1</span>; ++i) &#123; <span class="comment">// 外循环代表循环次数 n - 1 步</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; len - i - <span class="number">1</span>; ++j) &#123; <span class="comment">// 内循环代表一次循环对比的次数 n-1,n-2,n-3,1</span></span><br><span class="line">            <span class="keyword">if</span>(arr[j] &gt; arr[j<span class="number">+1</span>])&#123;</span><br><span class="line">                <span class="comment">// 交换 一次交换是三次赋值</span></span><br><span class="line">                <span class="built_in">swap</span>(arr[j],arr[j<span class="number">+1</span>]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-选择排序"><a href="#4-选择排序" class="headerlink" title="4. 选择排序"></a>4. 选择排序</h1><ul>
<li><code>思想</code>： 遍历找出最小的位置，最后与第一个位置交换。<strong><code>每次循环完毕都会在开始位置选出一个最小的数</code></strong>  </li>
<li><code>时间复杂度</code>：<strong>O($n^2$)</strong></li>
<li><code>空间复杂度</code>：<strong>O(1)</strong></li>
</ul>
<p>耗费的时间比上边未优化的冒泡排序要少很多</p>
<p>看图：<br><img src="/images/选择排序思想.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">selectSort</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> len)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; len - <span class="number">1</span>; ++i) &#123; <span class="comment">// 外循环代表循环次数 n - 1 步</span></span><br><span class="line">        <span class="type">int</span> min = i;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = i<span class="number">+1</span>; j &lt; len; ++j) &#123;  <span class="comment">// 内循环代表一次循环对比的次数</span></span><br><span class="line">            <span class="keyword">if</span>(arr[min] &gt; arr[j])&#123;</span><br><span class="line">                min = j;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">swap</span>(arr[min],arr[i]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-插入排序"><a href="#5-插入排序" class="headerlink" title="5. 插入排序:"></a>5. 插入排序:</h1><p><img src="/images/插入排序.png" alt=""></p>
<p><strong>像打扑克牌摸排后插入一样</strong></p>
<ul>
<li><code>思想</code>： <strong><code>每次循环完毕index在(循环次数+1)前面的数都是排好序的</code></strong>  </li>
<li><code>时间复杂度</code>：最差情况：<strong>O($n^2$)</strong>，最好情况：<strong>O(n)</strong></li>
<li><code>空间复杂度</code>：<strong>O(1)</strong></li>
</ul>
<p>特点：<code>适用于数组中大部分是排好序的数组</code>,如果大部分都没排好序，那么花费的时间比原来的冒泡排序还多</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">insertSort</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> len)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt; len; ++i) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = i; j &gt;<span class="number">0</span> &amp;&amp; arr[j] &lt; arr[j<span class="number">-1</span>] ; --j) &#123;</span><br><span class="line">            <span class="built_in">swap</span>(arr[j],arr[j<span class="number">-1</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">insertSort</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> len)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt; len; ++i) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = i; j &gt;<span class="number">0</span>; --j) &#123;</span><br><span class="line">            <span class="keyword">if</span>(arr[j] &lt; arr[j<span class="number">-1</span>] )</span><br><span class="line">                <span class="built_in">swap</span>(arr[j],arr[j<span class="number">-1</span>]);</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="6-两个整数交换不额外开辟新内存"><a href="#6-两个整数交换不额外开辟新内存" class="headerlink" title="6.两个整数交换不额外开辟新内存"></a>6.两个整数交换不额外开辟新内存</h1><p>有以下两种方法</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">100</span>;</span><br><span class="line">    <span class="type">int</span> b = <span class="number">200</span>;</span><br><span class="line">    <span class="comment">// 交换</span></span><br><span class="line"></span><br><span class="line">    a = a + b;<span class="comment">// a = 300</span></span><br><span class="line">    b = a - b;<span class="comment">// b = 200</span></span><br><span class="line">    a = a - b;<span class="comment">// a = 100</span></span><br><span class="line"></span><br><span class="line">    System.out.<span class="built_in">println</span>(<span class="string">&quot;交换后ab的值分别是：%d,%d&quot;</span>,a,b); <span class="comment">// 交换后ab的值分别是：200,100</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">2</span>;  <span class="comment">// 二进制 10</span></span><br><span class="line">    <span class="type">int</span> b = <span class="number">3</span>;  <span class="comment">// 二进制 11</span></span><br><span class="line">    <span class="comment">// 交换</span></span><br><span class="line"></span><br><span class="line">    a = a ^ b; <span class="comment">// 二进制：10 ^ 11 = 01 = a = 十进制：1</span></span><br><span class="line">    b = a ^ b; <span class="comment">// 二进制：01 ^ 11 = 10 = b = 十进制：2</span></span><br><span class="line">    a = a ^ b; <span class="comment">// 二进制：01 ^ 10 = 11 = a = 十进制：3</span></span><br><span class="line"></span><br><span class="line">    System.out.<span class="built_in">println</span>(<span class="string">&quot;交换后ab的值分别是：%d,%d&quot;</span>,a,b); <span class="comment">// 交换后ab的值分别是：3,2</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>排序</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：04.冒泡优化、插入和希尔排序</title>
    <url>/29.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E5%86%92%E6%B3%A1%E4%BC%98%E5%8C%96%E3%80%81%E6%8F%92%E5%85%A5%E4%BC%98%E5%8C%96%E5%92%8C%E5%B8%8C%E5%B0%94%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<h1 id="冒泡优化、插入优化和希尔排序"><a href="#冒泡优化、插入优化和希尔排序" class="headerlink" title="冒泡优化、插入优化和希尔排序"></a><center>冒泡优化、插入优化和希尔排序<center></h1><p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure28bubbleselectsort">NDKPractice项目的datastructure</a></strong></p>
<h1 id="1-冒泡排序优化"><a href="#1-冒泡排序优化" class="headerlink" title="1. 冒泡排序优化"></a>1. 冒泡排序优化</h1><p>引用个<a href="https://blog.csdn.net/hansionz/article/details/80822494">冒泡排序优化</a> 的链接</p>
<p>思维：<br><img src="/images/冒泡排序优化.png" alt=""></p>
<p>特点：<code>适用于数组中大部分是排好序的数组</code>,如果大部分都没排好序，那么花费的时间比原来的冒泡排序还多</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 冒泡排序的优化（适用于数组中大部分是排好序的数组）</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">optimizeBubbleSort</span><span class="params">(<span class="type">int</span> arr[], <span class="type">int</span> len)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 记录上一次最后遍历的位置</span></span><br><span class="line">    <span class="type">int</span> n = len;</span><br><span class="line">    <span class="type">int</span> lastchangeIndex = <span class="number">0</span>; <span class="comment">//  最后交换的位置，控制位置</span></span><br><span class="line">    <span class="keyword">do</span>&#123;</span><br><span class="line">        lastchangeIndex = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt; n; ++i) &#123;</span><br><span class="line">            <span class="keyword">if</span>(arr[i<span class="number">-1</span>] &gt; arr[i])&#123; <span class="comment">// 后一个跟前一个对比</span></span><br><span class="line">                <span class="built_in">swap</span>(arr[i<span class="number">-1</span>],arr[i]);</span><br><span class="line">                <span class="comment">// 记录交换的位置，</span></span><br><span class="line">                lastchangeIndex = i;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        n = lastchangeIndex; <span class="comment">// 记录最后一次交换的位置，证明了后面的不用交换，顺序是对的，可以避免下次循环的判断</span></span><br><span class="line">    &#125;<span class="keyword">while</span> (n &gt; <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BubbleSort</span><span class="params">(<span class="type">int</span> arr[], <span class="type">int</span> len)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">	<span class="type">int</span> tmp = <span class="number">0</span>;</span><br><span class="line">	<span class="type">int</span> flag = <span class="number">0</span>;</span><br><span class="line">	<span class="type">int</span> pos = <span class="number">0</span>;<span class="comment">//用来记录最后一次交换的位置</span></span><br><span class="line">	<span class="type">int</span> k = len - <span class="number">1</span>;</span><br><span class="line">	<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; len - <span class="number">1</span>; i++)<span class="comment">//确定排序趟数</span></span><br><span class="line">	&#123;</span><br><span class="line">		pos = <span class="number">0</span>;</span><br><span class="line">		<span class="type">int</span> j = <span class="number">0</span>;</span><br><span class="line">		flag = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; k; j++)<span class="comment">//确定比较次数</span></span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">if</span> (arr[j]&gt;arr[j + <span class="number">1</span>])</span><br><span class="line">			&#123;</span><br><span class="line">				<span class="comment">//交换</span></span><br><span class="line">				<span class="built_in">swap</span>(arr[j],arr[j<span class="number">+1</span>]);</span><br><span class="line">				flag = <span class="number">1</span>;<span class="comment">//加入标记</span></span><br><span class="line">				pos = j;<span class="comment">//交换元素，记录最后一次交换的位置</span></span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (flag == <span class="number">0</span>)<span class="comment">//如果没有交换过元素，则已经有序</span></span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		k = pos;<span class="comment">//下一次比较到记录位置即可</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h1 id="2-插入排序优化"><a href="#2-插入排序优化" class="headerlink" title="2. 插入排序优化"></a>2. 插入排序优化</h1><p><img src="/images/插入排序优化.png" alt=""></p>
<p>特点:<code>适用于数组中大部分是排好序的数组</code>,比选择排序快很多(时间复杂度上是 O(n)级别)</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">insertSort</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> len)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> temp,j,i;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">1</span>; i &lt; len; ++i) &#123;</span><br><span class="line">        <span class="comment">// 当前的位置</span></span><br><span class="line">        temp = arr[i];</span><br><span class="line">        <span class="keyword">for</span> (j = i; j &gt;<span class="number">0</span> &amp;&amp; arr[j<span class="number">-1</span>] &gt;temp ; --j) &#123;</span><br><span class="line">            arr[j] = arr[j<span class="number">-1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 插入合适的位置 (这个时候的j 是 -- 了的哦 注意看循环的最后一个条件)</span></span><br><span class="line">        arr[j] = temp;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 插入排序优化1(这样会跳出循环更快)</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">insertSort1</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> len)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> temp,j,i;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">1</span>; i &lt; len; ++i) &#123;</span><br><span class="line">        <span class="comment">// 当前的位置</span></span><br><span class="line">        temp = arr[i];</span><br><span class="line">        <span class="keyword">for</span> (j = i; j &gt;<span class="number">0</span>; --j) &#123;</span><br><span class="line">            <span class="keyword">if</span>(arr[j<span class="number">-1</span>] &gt;temp)</span><br><span class="line">                arr[j] = arr[j<span class="number">-1</span>];</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 插入合适的位置 (这个时候的j 是 -- 了的哦 注意看循环的最后一个条件)</span></span><br><span class="line">        arr[j] = temp;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-对比插入排序和选择排序"><a href="#3-对比插入排序和选择排序" class="headerlink" title="3.对比插入排序和选择排序"></a>3.对比插入排序和选择排序</h1><div class="table-container">
<table>
<thead>
<tr>
<th></th>
<th>最好情况</th>
<th>最坏情况</th>
</tr>
</thead>
<tbody>
<tr>
<td>选择排序</td>
<td>比较N*（N-1）/2次，交换0次</td>
<td>比较N<em>（N-1）/2次，交换N</em>（N-1）/2次，赋值N*（N-1）/2次</td>
</tr>
<tr>
<td>插入排序</td>
<td>比较N-1次，交换0次</td>
<td>比较N<em>（N-1）/2次，交换N</em>（N-1）/2次，赋值3<em>N</em>（N-1）/2次</td>
</tr>
<tr>
<td>改进插入排序</td>
<td>比较N-1次，交换0次</td>
<td>比较N<em>（N-1）/2次，交换N</em>（N-1）/2次，赋值N*（N-1）/2 + 2（N-1）次</td>
</tr>
</tbody>
</table>
</div>
<h1 id="4-希尔排序-插入排序优化的再次优化"><a href="#4-希尔排序-插入排序优化的再次优化" class="headerlink" title="4.希尔排序(插入排序优化的再次优化)"></a>4.希尔排序(插入排序优化的再次优化)</h1><p>引用个<a href="(https://www.cnblogs.com/chengxiao/p/6104371.html">希尔排序链接</a>)</p>
<p>希尔排序在 <code>数据非常无序时</code>是<code>首选</code>,<code>数据很有序</code>的时候还是没有<code>改进插入排序</code> 快</p>
<p>思想: <code>分治</code><br><img src="/images/希尔排序.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 希尔排序思想：对插入排序分组</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">shellInsertSort</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> len)</span></span>&#123; <span class="comment">// 8</span></span><br><span class="line">    <span class="comment">// 思考 ： 求算法的复杂度</span></span><br><span class="line">    <span class="type">int</span> increment = len / <span class="number">2</span>; <span class="comment">// 4组</span></span><br><span class="line">    <span class="type">int</span> i, j ,k;</span><br><span class="line">    <span class="keyword">while</span>(increment &gt;= <span class="number">1</span>)&#123;</span><br><span class="line">        <span class="comment">// 希尔排序</span></span><br><span class="line">        <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; increment; ++i)&#123; <span class="comment">// i = 0, increment = 2</span></span><br><span class="line">            <span class="keyword">for</span>(j = i + increment; j &lt; len; j += increment)&#123;</span><br><span class="line">                <span class="type">int</span> tmp = arr[j]; <span class="comment">// 5</span></span><br><span class="line">                <span class="comment">// k = j = 6;</span></span><br><span class="line">                <span class="keyword">for</span>(k = j; k &gt; i  &amp;&amp; arr[k - increment] &gt; tmp; k -= increment)&#123;</span><br><span class="line">                    <span class="comment">// 往后挪动</span></span><br><span class="line">                    arr[k] = arr[k - increment];</span><br><span class="line">                &#125;</span><br><span class="line">                arr[k] = tmp;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        increment /= <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">shellInsertSort1</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> len)</span></span>&#123; <span class="comment">// 8</span></span><br><span class="line">    <span class="comment">// 思考 ： 求算法的复杂度</span></span><br><span class="line">    <span class="type">int</span> i, j ;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> increment = len / <span class="number">2</span>;increment &gt;<span class="number">0</span>;increment /= <span class="number">2</span>)&#123; <span class="comment">// 4组</span></span><br><span class="line">        <span class="comment">// 希尔排序</span></span><br><span class="line">        <span class="keyword">for</span>(i = increment; i &lt; len; ++i)&#123; <span class="comment">// i = 0, increment = 2</span></span><br><span class="line">            <span class="type">int</span> tmp = arr[i]; <span class="comment">// 5</span></span><br><span class="line">            <span class="keyword">for</span>(j = i ; j - increment &gt;= <span class="number">0</span> &amp;&amp; tmp &lt; arr[j - increment]; j -= increment)&#123;</span><br><span class="line">                arr[j] = arr[j - increment];</span><br><span class="line">            &#125;</span><br><span class="line">            arr[j] = tmp;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>排序</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：05.稳定和不稳定排序、归并排序和快速排序、三路快排</title>
    <url>/30.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E7%A8%B3%E5%AE%9A%E5%92%8C%E4%B8%8D%E7%A8%B3%E5%AE%9A%E6%8E%92%E5%BA%8F%E3%80%81%E5%BD%92%E5%B9%B6%E6%8E%92%E5%BA%8F%E5%92%8C%E5%BF%AB%E9%80%9F%E6%8E%92%E5%BA%8F%E3%80%81%E4%B8%89%E8%B7%AF%E5%BF%AB%E6%8E%92/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure28bubbleselectsort">NDKPractice项目的datastructure</a></strong></p>
<h1 id="1-稳定排序和不稳定排序："><a href="#1-稳定排序和不稳定排序：" class="headerlink" title="1. 稳定排序和不稳定排序："></a>1. 稳定排序和不稳定排序：</h1><p>稳定排序概念：<code>通俗地讲就是能保证排序前两个相等的数其在序列的前后位置顺序和排序后它们两个的前后位置顺序相同。在简单形式化一下，如果Ai = Aj，Ai原来在位置前，排序后Ai还是要在Aj位置前。</code></p>
<p>代表：</p>
<ul>
<li>稳定排序：<code>冒泡</code>、<code>插入</code>、<code>归并</code> </li>
<li>不稳定排序：<code>选择</code>、<code>希尔</code>、<code>快速排序(可以做到稳定但是比较难)</code>、<code>堆排序</code></li>
</ul>
<h1 id="2-归并排序"><a href="#2-归并排序" class="headerlink" title="2. 归并排序"></a>2. <a href="https://www.cnblogs.com/chengxiao/p/6194356.html">归并排序</a></h1><p>步骤：</p>
<ol>
<li>递归，分治的算法，每次都从中间分隔，然后排序。</li>
<li>最后把分别排好序的两个子数组合并，利用一个临时数据作为中转。</li>
</ol>
<ul>
<li><code>思想</code>： <strong><code>每次循环完毕index在(循环次数+1)前面的数都是排好序的</code></strong>  </li>
<li><code>时间复杂度</code>：<strong>O(N&#42;$\log_2 N$)</strong></li>
<li><code>空间复杂度</code>：<strong>O(N)</strong></li>
</ul>
<p>每次合并操作的平均时间复杂度为O(n)，而完全二叉树的深度为|log2n|。总的平均时间复杂度为O(nlogn)。而且，归并排序的最好，最坏，平均时间复杂度均为O(nlogn)。</p>
<p>思想：<br><img src="/images/归并排序1.png" alt=""><br><img src="/images/归并排序2.png" alt=""><br><img src="/images/归并排序3.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 对数组区间 [l,mid] 和 [mid+1,r] 进行归并</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">merge_</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> l,<span class="type">int</span> mid,<span class="type">int</span> r)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 1. 对数组进行一次拷贝</span></span><br><span class="line">    <span class="type">int</span> temp[r - l + <span class="number">1</span>];</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = l ;i &lt;= r; ++i)&#123;</span><br><span class="line">        temp[i -l] = arr[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 2. 确定好分析之后的变量</span></span><br><span class="line">    <span class="type">int</span> i = l;</span><br><span class="line">    <span class="type">int</span> j = mid + <span class="number">1</span>;</span><br><span class="line">    <span class="type">int</span> k = l;</span><br><span class="line">    <span class="keyword">for</span> (; k &lt;= r; ++k) &#123;</span><br><span class="line">        <span class="keyword">if</span>(i &gt; mid)&#123;   <span class="comment">// 左边的数组已经全部填充好了，那么剩下的应该填充右边的数组了</span></span><br><span class="line">            arr[k] = temp[j - l];</span><br><span class="line">            j++;</span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">if</span>( j &gt; r)&#123; <span class="comment">// 右边的数组已经全部填充好了，那么剩下的应该填充左边的数组了</span></span><br><span class="line">            arr[k] = temp[i -l];</span><br><span class="line">            i++;</span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">if</span>(temp[i - l] &lt; temp[j - l])&#123; <span class="comment">// 临时数据里面的 i 位置和 j 位置去比较</span></span><br><span class="line">            arr[k] = temp[i - l];</span><br><span class="line">            i++;</span><br><span class="line">        &#125; <span class="keyword">else</span>&#123;</span><br><span class="line">            arr[k] = temp[j - l];</span><br><span class="line">            j++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 对数组的 [l,r] 区间进行归并排序</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">mergeSort_</span><span class="params">(<span class="type">int</span> arr[], <span class="type">int</span> l, <span class="type">int</span> r)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 递归到底的情况</span></span><br><span class="line">    <span class="keyword">if</span>(l &gt;= r)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    <span class="type">int</span> mid = ( l + r) &gt;&gt; <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">mergeSort_</span>(arr,l,mid);<span class="comment">//左边归并排序，使得左子序列有序</span></span><br><span class="line">    <span class="built_in">mergeSort_</span>(arr,mid + <span class="number">1</span>,r);<span class="comment">//右边归并排序，使得右子序列有序</span></span><br><span class="line">    <span class="comment">// 优化要根据具体的场景去做（因为mid前面和后面都是排好序的！！）</span></span><br><span class="line">    <span class="keyword">if</span>(arr[mid] &gt; arr[mid + <span class="number">1</span>])&#123;</span><br><span class="line">        <span class="built_in">merge_</span>(arr, l, mid, r);<span class="comment">//将两个有序子数组合并操作</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 归并排序</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">mergeSort</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> len)</span></span>&#123;</span><br><span class="line">    <span class="built_in">mergeSort_</span>(arr,<span class="number">0</span>,len - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-快速排序"><a href="#2-快速排序" class="headerlink" title="2. 快速排序"></a>2. <a href="https://www.cnblogs.com/MOBIN/p/4681369.html">快速排序</a></h1><p>以数组的第一个元素为基准元素(v)进行排序，<code>大于等于v的排在数组右边，小于v的排在数组左边</code>。最后返回v的下标，再次分为两个数组进行递归操作。</p>
<p><strong>如果&gt;v，和&lt;v 的左右规模接近，那么这个排序就类似归并排序，时间复杂度最优</strong></p>
<ul>
<li><code>思想</code>： <strong><code>每次返回排序后基准元素在数组的下标，也就是说每次只排好了一个数。</code></strong>  </li>
<li><code>时间复杂度</code>：最差情况：<strong>O($n^2$)</strong>，最好情况（随机快排）：<strong>O(N&#42;$\log_2 N$)</strong></li>
<li><code>空间复杂度</code>：最差情况：<strong>O(n)</strong>，最好情况（随机快排）：<strong>O($\log_2 N$)</strong></li>
</ul>
<p><img src="/images/快速排序.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 对数组 arr 区间[l,r] 进行分割操作</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">partition_</span><span class="params">(<span class="type">int</span> arr[], <span class="type">int</span> l, <span class="type">int</span> r)</span> </span>&#123;<span class="comment">// 10 , 20</span></span><br><span class="line">    <span class="comment">// 优化，跟区间[l,r]随机位置进行比较。(如果数组本身就是有序的，那么每次都拿第一个数作为基准元素，算出来的p就是在头尾位置，小于区域和等于区域就不是接近等规模的，这样还要递归N次，复杂度就是O(n2))</span></span><br><span class="line">    <span class="built_in">swap</span>(arr[l], arr[<span class="built_in">rand</span>() % (r - l + <span class="number">1</span>) + l]);</span><br><span class="line">    <span class="type">int</span> v = arr[l];</span><br><span class="line">    <span class="comment">// 以 p 为分割，[l+1,p]&lt;v 和  [p+1,r] &gt; v</span></span><br><span class="line">    <span class="type">int</span> p = l;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = l; i &lt;= r; ++i) &#123;</span><br><span class="line">        <span class="keyword">if</span> (arr[i] &lt; v) &#123;</span><br><span class="line">            <span class="comment">// 只需要处理小于的情况</span></span><br><span class="line">            <span class="built_in">swap</span>(arr[p + <span class="number">1</span>], arr[i]);</span><br><span class="line">            p++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">swap</span>(arr[l], arr[p]);</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 对数组 arr 区间[l,r] 进行快速排序</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">quickSort_</span><span class="params">(<span class="type">int</span> arr[], <span class="type">int</span> l, <span class="type">int</span> r)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 递归到底的情况</span></span><br><span class="line">    <span class="keyword">if</span> (l &gt;= r) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> p = <span class="built_in">partition_</span>(arr, l, r);</span><br><span class="line">    <span class="built_in">quickSort_</span>(arr, l, p - <span class="number">1</span>); <span class="comment">// 对基准元素左边的元素进行递归排序（如果，左边规模没有，那么这个递归就无效）</span></span><br><span class="line">    <span class="built_in">quickSort_</span>(arr, p + <span class="number">1</span>, r); <span class="comment">// 对基准元素右边的进行递归排序（如果，右边规模没有，那么这个递归就无效）</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 快速排序</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">quickSort</span><span class="params">(<span class="type">int</span> arr[], <span class="type">int</span> len)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">srand</span>(<span class="built_in">time</span>(<span class="literal">NULL</span>)); <span class="comment">// 初始化随机数发生器</span></span><br><span class="line">    <span class="built_in">quickSort_</span>(arr, <span class="number">0</span>, len - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-三路快排"><a href="#3-三路快排" class="headerlink" title="3. 三路快排"></a>3. 三路快排</h1><p>以数组的第一个元素为基准元素(v)进行排序，<code>大于v的排在数组右边，小于v的排在数组左边，剩下的=v在中间</code>。最后返回<code>=v的数组头尾下标</code>，再次分为两个数组进行递归操作。</p>
<ul>
<li><code>思想</code>： <strong><code>每次返回排序后，=基准元素v在数组的头尾下标，也就是说每次只排好了所有=v的数</code></strong>  </li>
<li><code>时间复杂度</code>：最差情况：<strong>O($n^2$)</strong>，最好情况（随机快排）：<strong>O(N&#42;$\log_2 N$)</strong></li>
<li><code>空间复杂度</code>：最差情况：<strong>O(n)</strong>，最好情况（随机快排）：<strong>O($\log_2 N$)</strong></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Code_04_QuickSort</span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">quickSort</span><span class="params">(<span class="type">int</span>[] arr)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (arr == <span class="literal">null</span> || arr.length &lt; <span class="number">2</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		quickSort(arr, <span class="number">0</span>, arr.length - <span class="number">1</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">quickSort</span><span class="params">(<span class="type">int</span>[] arr, <span class="type">int</span> l, <span class="type">int</span> r)</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (l &lt; r) &#123;</span><br><span class="line">			swap(arr, l + (<span class="type">int</span>) (Math.random() * (r - l + <span class="number">1</span>)), r);</span><br><span class="line">			<span class="type">int</span>[] p = partition(arr, l, r);</span><br><span class="line">			quickSort(arr, l, p[<span class="number">0</span>] - <span class="number">1</span>);</span><br><span class="line">			quickSort(arr, p[<span class="number">1</span>] + <span class="number">1</span>, r);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 以最后一个元素为基准元素。</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="type">int</span>[] partition(<span class="type">int</span>[] arr, <span class="type">int</span> l, <span class="type">int</span> r) &#123;</span><br><span class="line">		<span class="type">int</span> <span class="variable">less</span> <span class="operator">=</span> l - <span class="number">1</span>; </span><br><span class="line">		<span class="type">int</span> <span class="variable">more</span> <span class="operator">=</span> r;</span><br><span class="line">		<span class="keyword">while</span> (l &lt; more) &#123;</span><br><span class="line">			<span class="keyword">if</span> (arr[l] &lt; arr[r]) &#123;</span><br><span class="line">				swap(arr, ++less, l++);</span><br><span class="line">			&#125; <span class="keyword">else</span> <span class="keyword">if</span> (arr[l] &gt; arr[r]) &#123;</span><br><span class="line">				swap(arr, --more, l);</span><br><span class="line">			&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">				l++;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		swap(arr, more, r);</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">int</span>[] &#123; less + <span class="number">1</span>, more &#125;;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">quickSort3ways_</span><span class="params">(<span class="type">int</span> arr[], <span class="type">int</span> l, <span class="type">int</span> r)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 递归到底的情况</span></span><br><span class="line">    <span class="keyword">if</span> (l &gt;= r) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义变量</span></span><br><span class="line">    <span class="built_in">swap</span>(arr[l], arr[<span class="built_in">rand</span>() % (r - l + <span class="number">1</span>) + l]);</span><br><span class="line">    <span class="type">int</span> v = arr[l];</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> lt = l;<span class="comment">// 左边指针 lt [l+1, lt] &lt; v  </span></span><br><span class="line">    <span class="type">int</span> gt = r + <span class="number">1</span>;<span class="comment">// 右边指针 gt [gt,r] &gt;v</span></span><br><span class="line">    <span class="type">int</span> i = l + <span class="number">1</span>;<span class="comment">// 循环开始位置 [lt + 1,i) = v</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (gt &gt; i) &#123;</span><br><span class="line">        <span class="keyword">if</span> (arr[i] &gt; v) &#123;</span><br><span class="line">            <span class="built_in">swap</span>(arr[i], arr[gt - <span class="number">1</span>]);</span><br><span class="line">            gt--;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (arr[i] &lt; v) &#123;</span><br><span class="line">            <span class="built_in">swap</span>(arr[i], arr[lt + <span class="number">1</span>]); <span class="comment">// i左边的一定是小于或等于v的  </span></span><br><span class="line">            i++;</span><br><span class="line">            lt++;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            i++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">swap</span>(arr[l], arr[lt]); <span class="comment">// 交换完毕后 [l,lt-1] &lt; v,[gt,r] &gt; v</span></span><br><span class="line">    <span class="built_in">quickSort3ways_</span>(arr, l, lt - <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">quickSort3ways_</span>(arr, gt, r);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">quickSort3ways</span><span class="params">(<span class="type">int</span> arr[], <span class="type">int</span> len)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">srand</span>(<span class="built_in">time</span>(<span class="literal">NULL</span>)); <span class="comment">// 初始化随机数发生器</span></span><br><span class="line">    <span class="built_in">quickSort3ways_</span>(arr, <span class="number">0</span>, len - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>排序</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：01.定义</title>
    <url>/26.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E5%AE%9A%E4%B9%89/</url>
    <content><![CDATA[<h1 id="1-数据结构算法基本概念"><a href="#1-数据结构算法基本概念" class="headerlink" title="1.数据结构算法基本概念"></a>1.数据结构算法基本概念</h1><h2 id="1-1-数据结构："><a href="#1-1-数据结构：" class="headerlink" title="1.1 数据结构："></a>1.1 数据结构：</h2><p>指相互之间存在着一种或多种关系的数据元素的集合和该集合中数据元素之间的关系，简单的理解就是<code>元素之间的相互关系</code>。  </p>
<p>其中逻辑结构分为四类：  </p>
<ol>
<li>集合结构</li>
<li>线性结构</li>
<li>树形结构</li>
<li>图形结构</li>
</ol>
<p>存储结构一般分为：  </p>
<ol>
<li>顺序存储</li>
<li>链式存储</li>
</ol>
<h2 id="1-2-算法"><a href="#1-2-算法" class="headerlink" title="1.2 算法"></a>1.2 算法</h2><p>指特定问题求解步骤的描述。算法的特性有：<code>输入，输出，有穷性，确定性和可行性</code>。</p>
<p>算法的优劣主要从算法的<code>执行时间</code>和所需要<code>占用的存储空间</code>两个方面衡量，也称之为<code>时间复杂度</code>和<code>空间复杂度</code>。</p>
<h1 id="2-算法的时间复杂度和空间复杂度"><a href="#2-算法的时间复杂度和空间复杂度" class="headerlink" title="2. 算法的时间复杂度和空间复杂度"></a>2. 算法的时间复杂度和空间复杂度</h1><p><code>时间复杂度</code>是一个函数，它定性描述了该算法的运行时间，时间复杂度常用大<code>O</code>符号表述，不包括这个函数的低阶项和首项系数。<br><code>空间复杂度(Space Complexity)</code>是对一个算法在运行过程中<code>临时占用存储空间大小</code>的量度。</p>
<p>常见的时间复杂度：</p>
<ul>
<li>常数阶O(1)，</li>
<li>对数阶O($\log_2 n$)，</li>
<li>线性阶O(n)，</li>
<li>线性对数阶O(n* $\log_2 n$)，</li>
<li>平方阶O($n^2$)，</li>
<li>立方阶O($n^3$)，</li>
<li>k次方阶O($n^K$)，</li>
<li>指数阶O ($2^n$)。</li>
</ul>
<p>举几个列：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 比如我们现在 求 1+2+3+4+***+n</span></span><br><span class="line"><span class="comment">// 任何算法在特定执行的 n 步骤下，我们都可以推演出算法的复杂度（时间，空间）</span></span><br><span class="line"><span class="comment">// 简单的理解为执行的步骤</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">sum1</span><span class="params">(<span class="type">int</span> n)</span></span>&#123;<span class="comment">// n + 2 步 O(n)</span></span><br><span class="line">    <span class="type">int</span> sum = <span class="number">0</span>; <span class="comment">// 1步</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt;= n ; ++i) &#123; <span class="comment">// n 步</span></span><br><span class="line">        sum += i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sum;<span class="comment">// 1步</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">sum2</span><span class="params">(<span class="type">int</span> n)</span></span>&#123; <span class="comment">// O(1)</span></span><br><span class="line">    <span class="keyword">return</span> (<span class="number">1</span>+n)*n / <span class="number">2</span>; <span class="comment">// 1步</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 空间复杂度 反转一个字符串 aaa222bbb -&gt; bbb222aaa</span></span><br><span class="line"><span class="function"><span class="type">char</span>* <span class="title">reverse1</span><span class="params">(<span class="type">char</span> str1[],<span class="type">int</span> n)</span></span>&#123; <span class="comment">// O(n)</span></span><br><span class="line">    <span class="comment">// 第一种写法</span></span><br><span class="line">    <span class="comment">// 创建一个新的数组</span></span><br><span class="line">    <span class="type">char</span>* res = (<span class="type">char</span>*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="type">char</span>)*n);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 倒序循环</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = n - <span class="number">1</span>; i &gt;=<span class="number">0</span>; i--)&#123;</span><br><span class="line">        <span class="comment">// 赋值</span></span><br><span class="line">        res[n - <span class="number">1</span> - i] = str1[i];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> res; <span class="comment">// 1</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">reverse2</span><span class="params">(<span class="type">char</span> str[],<span class="type">int</span> n)</span></span>&#123;<span class="comment">// 空间复杂度是 O(1) 时间复杂度是 O(n)</span></span><br><span class="line">    <span class="type">int</span> mid = n / <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; mid; i++)&#123;</span><br><span class="line">        <span class="comment">// i 的位置和 i+mid 的位置进行交换</span></span><br><span class="line">        <span class="comment">// 交换 1 次交换是两次赋值</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>作业：读一篇英文文档统计字符出现的个数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="comment">//统计各字符的出现频bai率，只统du计ASC[0，127]的字符，中文字符不参加统计 </span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">tongji</span><span class="params">(<span class="type">int</span> *c,FILE *fp)</span></span>&#123;</span><br><span class="line"> zhiintd;</span><br><span class="line">  <span class="keyword">while</span>((d=<span class="built_in">fgetc</span>(fp))!=EOF)&#123;</span><br><span class="line">    <span class="keyword">if</span> (d&gt;=<span class="number">0</span> &amp;&amp; d&lt;<span class="number">128</span>) c[d]++;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    FILE *fp;</span><br><span class="line">    <span class="type">int</span> c[<span class="number">128</span>],i;</span><br><span class="line">    <span class="keyword">if</span> ((fp=<span class="built_in">fopen</span>(<span class="string">&quot;d:\\temp.txt&quot;</span>,<span class="string">&quot;r&quot;</span>))==<span class="literal">NULL</span>)&#123; <span class="comment">//自行dao指定打开的文件 </span></span><br><span class="line">     <span class="built_in">printf</span>(<span class="string">&quot;打开文件失败\n&quot;</span>);</span><br><span class="line">     <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;<span class="number">128</span>;i++)&#123;</span><br><span class="line">        c[i]=<span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">tongji</span>(c,fp);</span><br><span class="line">    <span class="built_in">fclose</span>(fp);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;字符\tASC码\t频率\n&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">32</span>;i&lt;<span class="number">128</span>;i++)&#123; <span class="comment">//显示ASC字符是32以上的统计结果 </span></span><br><span class="line">    <span class="keyword">if</span>(c[i]!=<span class="number">0</span>)</span><br><span class="line">          <span class="built_in">printf</span>(<span class="string">&quot;%c\t%d\t%d\n&quot;</span>,i,i,c[i]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：02.单链表与双链表</title>
    <url>/27.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E5%8D%95%E9%93%BE%E8%A1%A8%E4%B8%8E%E5%8F%8C%E9%93%BE%E8%A1%A8(LinkedList)/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure27">NDKPractice项目的datastructure</a></strong></p>
<h1 id="1-单链表和双链表的定义"><a href="#1-单链表和双链表的定义" class="headerlink" title="1.单链表和双链表的定义"></a>1.单链表和双链表的定义</h1><ul>
<li>单链表：<strong>只有一个指向下一结点的指针，也就是只能next</strong></li>
<li>双链表：<strong>除了有一个指向下一结点的指针外，还有一个指向前一结点的指针，可以通过prev()快速找到前一结点，顾名思义，单链表只能单向读取</strong></li>
</ul>
<h1 id="2-需要注意的地方"><a href="#2-需要注意的地方" class="headerlink" title="2.需要注意的地方:"></a>2.需要注意的地方:</h1><p><code>结构体或class 属性必须指定或者定义的时候给个默认对象这样才会有默认值,否则没有</code></p>
<h1 id="3-检测耗费的时间："><a href="#3-检测耗费的时间：" class="headerlink" title="3.检测耗费的时间："></a>3.检测耗费的时间：</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">time_t</span> start = <span class="built_in">clock</span>();</span><br><span class="line">... <span class="comment">// 这块是需要检测耗费时间的步骤</span></span><br><span class="line"><span class="type">time_t</span> end = <span class="built_in">clock</span>();</span><br><span class="line"><span class="built_in">LOGE</span>(<span class="string">&quot;耗费时间:%d&quot;</span>,(end-start) / CLOCKS_PER_SEC);</span><br></pre></td></tr></table></figure>
<h1 id="4-单链表和双链表的关系"><a href="#4-单链表和双链表的关系" class="headerlink" title="4.单链表和双链表的关系"></a>4.单链表和双链表的关系</h1><ol>
<li>都是属于增删快的</li>
<li>双链表比单链表数据量大时查询要快一倍.(插入在链表中间时,可以根据需要从first,还是last查询,这样要快一倍)</li>
</ol>
<h1 id="5-手写c-中的LinkedList"><a href="#5-手写c-中的LinkedList" class="headerlink" title="5.手写c++中的LinkedList"></a>5.手写c++中的LinkedList</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Created by 123 on 2020/7/1.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifndef</span> NDKPRACTICE_LINKEDLIST_HPP</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> NDKPRACTICE_LINKEDLIST_HPP</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;android/log.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 单链表节点</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Node</span> &#123;</span><br><span class="line">    Node&lt;E&gt; *pre;</span><br><span class="line">    Node&lt;E&gt; *next;</span><br><span class="line">    E value;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Node</span>(E value, Node&lt;E&gt; *pre, Node&lt;E&gt; *next) : <span class="built_in">value</span>(value), <span class="built_in">pre</span>(pre), <span class="built_in">next</span>(next) &#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// list</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">LinkedList</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// 头节点</span></span><br><span class="line">    Node&lt;E&gt; *first = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="comment">// 集合的长度</span></span><br><span class="line">    <span class="type">int</span> len = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 尾节点</span></span><br><span class="line">    Node&lt;E&gt; *last = <span class="literal">NULL</span>;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push</span><span class="params">(E e)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">size</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">E <span class="title">get</span><span class="params">(<span class="type">int</span> index)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// remove insert</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">insert</span><span class="params">(<span class="type">int</span> index, E e)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">E <span class="title">remove</span><span class="params">(<span class="type">int</span> index)</span></span>;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">LinkedList</span>();</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function">Node&lt;E&gt; *<span class="title">node</span><span class="params">(<span class="type">int</span> index)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">linkLast</span><span class="params">(E e)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">linkBefore</span><span class="params">(Node&lt;E&gt; *pNode, E e)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">E <span class="title">unlink</span><span class="params">(Node&lt;E&gt; *pNode)</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> LinkedList&lt;E&gt;::<span class="built_in">push</span>(E e) &#123;</span><br><span class="line">    <span class="comment">// 添加一个数据在列表的后面</span></span><br><span class="line">    <span class="built_in">linkLast</span>(e);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 下面是单链表的添加</span></span><br><span class="line">    <span class="comment">/*Node&lt;E&gt; *new_node = new Node&lt;E&gt;(e, NULL);</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    if (head) &#123;// 0 -&gt; 1 -&gt; 2 -&gt; 3</span></span><br><span class="line"><span class="comment">        // ?</span></span><br><span class="line"><span class="comment">        // head-&gt;next = new_node;</span></span><br><span class="line"><span class="comment">        // 找到尾巴节点，有一个特定就是 next 节点为空</span></span><br><span class="line"><span class="comment">        */</span><span class="comment">/*Node&lt;E&gt;* h = head;</span></span><br><span class="line"><span class="comment">        while(h)&#123;</span></span><br><span class="line"><span class="comment">            if(h-&gt;next == NULL)&#123;</span></span><br><span class="line"><span class="comment">                break;</span></span><br><span class="line"><span class="comment">            &#125;</span></span><br><span class="line"><span class="comment">            h = h-&gt;next;</span></span><br><span class="line"><span class="comment">        &#125;</span></span><br><span class="line"><span class="comment">        h-&gt;next = new_node;*/</span><span class="comment">/*</span></span><br><span class="line"><span class="comment">        // 每一次都需要找到最后一个节点  50000</span></span><br><span class="line"><span class="comment">        // 记录 last 节点</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">        // Node&lt;E&gt; *last = node(len - 1);</span></span><br><span class="line"><span class="comment">        last-&gt;next = new_node;// O(1)</span></span><br><span class="line"><span class="comment">    &#125; else &#123;</span></span><br><span class="line"><span class="comment">        head = new_node;</span></span><br><span class="line"><span class="comment">    &#125;</span></span><br><span class="line"><span class="comment">    last = new_node;*/</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">int</span> LinkedList&lt;E&gt;::<span class="built_in">size</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> len;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">Node&lt;E&gt;* LinkedList&lt;E&gt;::<span class="built_in">node</span>(<span class="type">int</span> index) &#123; <span class="comment">// O(n)</span></span><br><span class="line">    <span class="keyword">if</span>(index &lt; (len &gt;&gt; <span class="number">1</span>))&#123; <span class="comment">// 从开头开始找要快点</span></span><br><span class="line">        Node&lt;E&gt; *x = first;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i&lt; index; i++)&#123;</span><br><span class="line">            x = x-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="comment">// 从后往前遍历</span></span><br><span class="line">        Node&lt;E&gt; *x = last;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = len<span class="number">-1</span>; i &gt; index; i--)&#123;</span><br><span class="line">            x = x-&gt;pre;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E LinkedList&lt;E&gt;::<span class="built_in">get</span>(<span class="type">int</span> index) &#123;</span><br><span class="line">    <span class="built_in">assert</span>(index&gt;=<span class="number">0</span> &amp;&amp; index &lt; len);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">node</span>(index)-&gt;value;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> LinkedList&lt;E&gt;::<span class="built_in">insert</span>(<span class="type">int</span> index, E e) &#123; <span class="comment">// len = 4;</span></span><br><span class="line">    <span class="built_in">assert</span>(index&gt;=<span class="number">0</span> &amp;&amp; index &lt;= len);</span><br><span class="line">    <span class="comment">// 考虑边界 0</span></span><br><span class="line">    <span class="keyword">if</span>(index == len)&#123;</span><br><span class="line">        <span class="built_in">linkLast</span>(e);</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="built_in">linkBefore</span>(<span class="built_in">node</span>(index),e);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 下面是单链表的插入</span></span><br><span class="line">    <span class="comment">/*Node&lt;E&gt; *new_node = new Node&lt;E&gt;(e, NULL);</span></span><br><span class="line"><span class="comment">    if (index == 0) &#123;</span></span><br><span class="line"><span class="comment">        Node&lt;E&gt; *h = head;</span></span><br><span class="line"><span class="comment">        head = new_node;</span></span><br><span class="line"><span class="comment">        new_node-&gt;next = h;</span></span><br><span class="line"><span class="comment">    &#125; else &#123;</span></span><br><span class="line"><span class="comment">        // 考虑最后一个位置</span></span><br><span class="line"><span class="comment">        Node&lt;E&gt; *prev = node(index - 1);</span></span><br><span class="line"><span class="comment">        Node&lt;E&gt; *next = prev-&gt;next;// NULL</span></span><br><span class="line"><span class="comment">        prev-&gt;next = new_node;</span></span><br><span class="line"><span class="comment">        new_node-&gt;next = next;</span></span><br><span class="line"><span class="comment">    &#125;*/</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E LinkedList&lt;E&gt;::<span class="built_in">remove</span>(<span class="type">int</span> index) &#123;</span><br><span class="line">    <span class="comment">// 考虑边界问题 0 , len ，mid</span></span><br><span class="line">    <span class="built_in">assert</span>(index&gt;=<span class="number">0</span> &amp;&amp; index &lt; len);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">unlink</span>(<span class="built_in">node</span>(index));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 单链表的移除</span></span><br><span class="line">    <span class="comment">/*if (index == 0) &#123;</span></span><br><span class="line"><span class="comment">    Node&lt;E&gt; *h = head;</span></span><br><span class="line"><span class="comment">    head = h-&gt;next;</span></span><br><span class="line"><span class="comment">    // 释放</span></span><br><span class="line"><span class="comment">    delete h;</span></span><br><span class="line"><span class="comment">    &#125; else &#123;</span></span><br><span class="line"><span class="comment">        Node&lt;E&gt; *prev = node(index - 1);</span></span><br><span class="line"><span class="comment">        // 删除的节点</span></span><br><span class="line"><span class="comment">        Node&lt;E&gt; *cur = prev-&gt;next;</span></span><br><span class="line"><span class="comment">        prev-&gt;next = cur-&gt;next;</span></span><br><span class="line"><span class="comment">        delete cur;</span></span><br><span class="line"><span class="comment">    &#125;*/</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">LinkedList&lt;E&gt;::~<span class="built_in">LinkedList</span>() &#123;</span><br><span class="line">    <span class="comment">// 析构释放内存 ？</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>; i &lt; len; i++)&#123;</span><br><span class="line">        <span class="built_in">delete</span>(<span class="built_in">node</span>(i));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 头指针和尾指针置为NULL</span></span><br><span class="line">    first = <span class="literal">NULL</span>;</span><br><span class="line">    last = <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> LinkedList&lt;E&gt;::<span class="built_in">linkLast</span>(E e) &#123;</span><br><span class="line">    Node&lt;E&gt; *l = last;</span><br><span class="line">    Node&lt;E&gt; *new_node = <span class="keyword">new</span> <span class="built_in">Node</span>&lt;E&gt;(e,l,<span class="literal">NULL</span>);</span><br><span class="line">    last = new_node;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(l)&#123;</span><br><span class="line">        l-&gt;next = new_node;</span><br><span class="line">    &#125; <span class="keyword">else</span></span><br><span class="line">        first = new_node;</span><br><span class="line">    len ++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> LinkedList&lt;E&gt;::<span class="built_in">linkBefore</span>(Node&lt;E&gt; *pNode, E e) &#123;</span><br><span class="line">    Node&lt;E&gt; *pre = pNode-&gt;pre;<span class="comment">// NULL</span></span><br><span class="line">    Node&lt;E&gt; *new_node = <span class="keyword">new</span> <span class="built_in">Node</span>&lt;E&gt;(e,pre,pNode);</span><br><span class="line">    <span class="comment">// 当前节点的上一个节点 = 新增的节点</span></span><br><span class="line">    pNode-&gt;pre = new_node;</span><br><span class="line">    <span class="comment">// 上一个节点的 next -&gt; 新增的节点 ， 0 特殊处理</span></span><br><span class="line">    <span class="keyword">if</span>(pre)&#123;</span><br><span class="line">        pre-&gt;next = new_node;</span><br><span class="line">    &#125; <span class="keyword">else</span></span><br><span class="line">        first = new_node;</span><br><span class="line">    len ++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E LinkedList&lt;E&gt;::<span class="built_in">unlink</span>(Node&lt;E&gt; *pNode) &#123;</span><br><span class="line">    E e = pNode-&gt;value;</span><br><span class="line">    <span class="comment">// 左右两个节点</span></span><br><span class="line">    Node&lt;E&gt; *pre = pNode-&gt;pre;</span><br><span class="line">    Node&lt;E&gt; *next = pNode-&gt;next;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 有两个为空的情况，严谨，思维灵活</span></span><br><span class="line">    <span class="keyword">if</span>(pre)&#123;</span><br><span class="line">        pre-&gt;next = next;</span><br><span class="line">        pNode-&gt;pre = <span class="literal">NULL</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span></span><br><span class="line">        first = next;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(next)&#123;</span><br><span class="line">        next-&gt;pre = pre;</span><br><span class="line">        pNode-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span></span><br><span class="line">        last = pre;</span><br><span class="line"></span><br><span class="line">    pNode -&gt; value = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">delete</span> pNode;</span><br><span class="line">    len --;</span><br><span class="line">    <span class="keyword">return</span> e;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span> <span class="comment">//NDKPRACTICE_LINKEDLIST_HPP</span></span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
        <category>LinkedList</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>LinkedList</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：07.汉诺塔、数组实现队列和双向队列</title>
    <url>/32.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E6%B1%89%E8%AF%BA%E5%A1%94%E3%80%81%E6%95%B0%E7%BB%84%E5%AE%9E%E7%8E%B0%E9%98%9F%E5%88%97%E5%92%8C%E5%8F%8C%E5%90%91%E9%98%9F%E5%88%97/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure32queue">NDKPractice项目的datastructure32queue</a></strong></p>
<h1 id="1-汉诺塔："><a href="#1-汉诺塔：" class="headerlink" title="1. 汉诺塔："></a>1. 汉诺塔：</h1><p>如下图所示，从左到右有A、B、C三根柱子，其中A柱子上面有从小叠到大的n个圆盘，现要求将A柱子上的圆盘移到C柱子上去，<br>期间只有一个原则：一次只能移到一个盘子且大盘子不能在小盘子上面，求移动的步骤和移动的次数</p>
<p><img src="/images/32.汉诺塔.jpg" alt=""></p>
<p>步骤：</p>
<ol>
<li>n-1个盘子借助 C 从 A 挪动到 B 上面</li>
<li>直接把 n 盘子从 A 挪动到 C</li>
<li>n-1 个盘子借助A 从 B 挪动到 C</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">hannuota</span><span class="params">(<span class="type">int</span> n,<span class="type">char</span> start,<span class="type">char</span> help,<span class="type">char</span> end)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(n == <span class="number">1</span>)&#123;</span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;把第 %d 个盘子从 %c 挪动到 %c&quot;</span>,n,start,end);</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="comment">// 又是 n-1 汉诺塔</span></span><br><span class="line">        <span class="built_in">hannuota</span>(n<span class="number">-1</span>,start,end,help);</span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;把第 %d 个盘子从 %c 挪动到 %c&quot;</span>,n,start,end);</span><br><span class="line">        <span class="built_in">hannuota</span>(n<span class="number">-1</span>,help,start,end);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">hannuota</span>(<span class="number">3</span>,<span class="string">&#x27;A&#x27;</span>,<span class="string">&#x27;B&#x27;</span>,<span class="string">&#x27;C&#x27;</span>);</span><br></pre></td></tr></table></figure>
<p>打印结果:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">E/TAG: 把第 1 个盘子从 A 挪动到 C</span><br><span class="line">E/TAG: 把第 2 个盘子从 A 挪动到 B</span><br><span class="line">E/TAG: 把第 1 个盘子从 C 挪动到 B</span><br><span class="line">E/TAG: 把第 3 个盘子从 A 挪动到 C</span><br><span class="line">E/TAG: 把第 1 个盘子从 B 挪动到 A</span><br><span class="line">E/TAG: 把第 2 个盘子从 B 挪动到 C</span><br><span class="line">E/TAG: 把第 1 个盘子从 A 挪动到 C</span><br></pre></td></tr></table></figure>
<h1 id="2-位运算知识："><a href="#2-位运算知识：" class="headerlink" title="2. 位运算知识："></a>2. 位运算知识：</h1><p>负数转二进制 = 原码的补码</p>
<ul>
<li><p>原码：<code>一个正数，按照绝对值大小转换成的二进制数；一个负数按照绝对值大小转换成的二进制数，然后最高位补1，称为原码。</code></p>
<p>  比如 00000000 00000000 00000000 00000101 是 5的 原码；10000000 00000000 00000000 00000101 是 -5的 原码。 　</p>
</li>
<li><p>反码：<code>正数的反码与原码相同，负数的反码为对该数的原码除符号位外各位取反。</code></p>
<p>  取反操作指：原为1，得0；原为0，得1。（1变0; 0变1）</p>
<p>  比如：正数00000000 00000000 00000000 00000101 的反码还是 00000000 00000000 00000000 00000101 ；</p>
<p>  负数10000000 00000000 00000000 00000101每一位取反（除符号位），得11111111 11111111 11111111 11111010。</p>
<p>  称：10000000 00000000 00000000 00000101 和 11111111 11111111 11111111 11111010互为反码。　</p>
</li>
<li><p>补码：<code>正数的补码与原码相同，负数的补码为对该数的原码除符号位外各位取反，然后在最后一位加1.</code></p>
<p>  比如：10000000 00000000 00000000 00000101 的反码是：11111111 11111111 11111111 11111010。</p>
<p>  那么，补码为：</p>
<p>  11111111 11111111 11111111 11111010 + 1 = 11111111 11111111 11111111 11111011</p>
<p>  所以，-5 在计算机中表达为：11111111 11111111 11111111 11111011。转换为十六进制：0xFFFFFFFB。</p>
</li>
</ul>
<h1 id="3-数组实现队列："><a href="#3-数组实现队列：" class="headerlink" title="3. 数组实现队列："></a>3. 数组实现队列：</h1><p><code>cap</code> 可以乱传，以下代码，保证size 为 2 的 幂次</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">  <span class="comment">// -1 二进制为：-1的源码的补码</span></span><br><span class="line">head = (head - <span class="number">1</span>) &amp; (size - <span class="number">1</span>); <span class="comment">//(-1 &amp; size -1  = size - 1) 这样head的取值才是：size-1,size-2,size-3</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">ArrayQueue&lt;E&gt;::<span class="built_in">ArrayQueue</span>(<span class="type">int</span> cap)&#123;</span><br><span class="line">    <span class="comment">// cap 可以乱传，以下代码保证是 2 的幂次</span></span><br><span class="line">    <span class="type">int</span> n = cap - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span>(cap &gt;= <span class="number">2</span>)&#123;</span><br><span class="line">        n |= n &gt;&gt; <span class="number">1</span>;</span><br><span class="line">        n |= n &gt;&gt; <span class="number">2</span>;</span><br><span class="line">        n |= n &gt;&gt; <span class="number">4</span>;</span><br><span class="line">        n |= n &gt;&gt; <span class="number">8</span>;</span><br><span class="line">        n |= n &gt;&gt; <span class="number">16</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    n += <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">LOGE</span>(<span class="string">&quot;size = %d&quot;</span>,n);</span><br><span class="line">    size = n;</span><br><span class="line">    array = (E*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(E) * size);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; size; ++i) &#123;</span><br><span class="line">        array[i] = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>思想：<code>head，和tail</code>用来标记队列的头尾，<code>从尾部开始插入修改head，从尾部拿去修改tail</code>。</p>
<p>完整代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ArrayQueue</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// 必须是 2 的幂次</span></span><br><span class="line">    <span class="type">int</span> size = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 队列的头索引（是反向的添加，从 size - 1 ,size -2,...添加）</span></span><br><span class="line">    <span class="type">int</span> head = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 队尾索引</span></span><br><span class="line">    <span class="type">int</span> tail = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 数组地址</span></span><br><span class="line">    E *array = <span class="literal">NULL</span>;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">ArrayQueue</span>();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">ArrayQueue</span>(<span class="type">int</span> size);</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">ArrayQueue</span>();</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 添加到队尾巴</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">enqueue</span><span class="params">(E e)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 移除队首的元素（指的是 tail的位置）</span></span><br><span class="line">    <span class="function">E <span class="title">dequeue</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取队首的位置，但不移除</span></span><br><span class="line">    <span class="function">E <span class="title">peek</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isEmpty</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">growArray</span><span class="params">()</span></span>; <span class="comment">// 扩容数组</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">copyElement</span><span class="params">(E *src,<span class="type">int</span> sPo, E *dest,<span class="type">int</span> dPo,<span class="type">int</span> len)</span></span>; <span class="comment">// 拷贝数组元素到新的数组</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">ArrayQueue&lt;E&gt;::<span class="built_in">ArrayQueue</span>():<span class="built_in">ArrayQueue</span>(<span class="number">4</span>)&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">ArrayQueue&lt;E&gt;::<span class="built_in">ArrayQueue</span>(<span class="type">int</span> cap)&#123;</span><br><span class="line">    <span class="comment">// cap 可以乱传，以下代码保证是 2 的幂次</span></span><br><span class="line">    <span class="type">int</span> n = cap - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span>(cap &gt;= <span class="number">2</span>)&#123;</span><br><span class="line">        n |= n &gt;&gt; <span class="number">1</span>;</span><br><span class="line">        n |= n &gt;&gt; <span class="number">2</span>;</span><br><span class="line">        n |= n &gt;&gt; <span class="number">4</span>;</span><br><span class="line">        n |= n &gt;&gt; <span class="number">8</span>;</span><br><span class="line">        n |= n &gt;&gt; <span class="number">16</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    n += <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">LOGE</span>(<span class="string">&quot;size = %d&quot;</span>,n);</span><br><span class="line">    size = n;</span><br><span class="line">    array = (E*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(E) * size);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; size; ++i) &#123;</span><br><span class="line">        array[i] = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">ArrayQueue&lt;E&gt;::~<span class="built_in">ArrayQueue</span>() &#123;</span><br><span class="line">    <span class="built_in">free</span>(array);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> ArrayQueue&lt;E&gt;::<span class="built_in">enqueue</span>(E e) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(head == tail &amp;&amp; array[head]!=<span class="literal">NULL</span>)&#123; <span class="comment">// 弹出的时候把对应的元素置为 NULL,这样好判断能否扩容</span></span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;扩容了 head = %d,tail = %d&quot;</span>,head,tail);</span><br><span class="line">        <span class="comment">// 扩容，双端队列，头部增加删除，也可以在尾部增加删除</span></span><br><span class="line">        <span class="built_in">growArray</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// -1 二进制为：-1的源码的补码</span></span><br><span class="line">    head = (head - <span class="number">1</span>) &amp; (size - <span class="number">1</span>); <span class="comment">//(-1 &amp; size -1  = size - 1) 这样head的取值才是：size-1,size-2,size-3</span></span><br><span class="line">    array[head] = e;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E ArrayQueue&lt;E&gt;::<span class="built_in">peek</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> array[(tail - <span class="number">1</span>) &amp; (size - <span class="number">1</span>)];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line">E ArrayQueue&lt;E&gt;::<span class="built_in">dequeue</span>() &#123;</span><br><span class="line">    tail = (tail - <span class="number">1</span>) &amp; (size - <span class="number">1</span>);</span><br><span class="line">    E value = array[tail];</span><br><span class="line">    array[tail] = <span class="literal">NULL</span>; <span class="comment">// 弹出去的元素置为 NULL;</span></span><br><span class="line">    <span class="keyword">return</span> value;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">bool</span> ArrayQueue&lt;E&gt;::<span class="built_in">isEmpty</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> head == tail &amp;&amp; array[head]==<span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> ArrayQueue&lt;E&gt;::<span class="built_in">growArray</span>() &#123;</span><br><span class="line">    <span class="type">int</span> new_size = size &lt;&lt; <span class="number">1</span>; <span class="comment">// 扩容为原来的两倍</span></span><br><span class="line"></span><br><span class="line">    E *new_array = (E*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(E) * new_size);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 对数据进行 copy, 将 tail 后面的拷贝到前面，将tail 前面的拷贝到后面</span></span><br><span class="line">    <span class="type">int</span> rightLen = size - tail;</span><br><span class="line">    <span class="built_in">copyElement</span>(array,tail,new_array,<span class="number">0</span>,rightLen);</span><br><span class="line">    <span class="built_in">copyElement</span>(array,<span class="number">0</span>,new_array,rightLen,tail);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">free</span>(array);</span><br><span class="line">    head = <span class="number">0</span>;</span><br><span class="line">    tail = size;</span><br><span class="line">    size = new_size;</span><br><span class="line">    array = new_array;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="type">void</span> ArrayQueue&lt;E&gt;::<span class="built_in">copyElement</span>(E *src, <span class="type">int</span> sPo, E *dest, <span class="type">int</span> dPo, <span class="type">int</span> len) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt;len; ++i) &#123;</span><br><span class="line">        dest[dPo+i] = src[sPo+i];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
        <category>Queue</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>Queue</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：08.HashMap 源码分析和手写</title>
    <url>/33.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-HashMap%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E5%92%8C%E6%89%8B%E5%86%99/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/JavaJNITest/tree/master/src/com/east/datastructure33hashmap">JavaJniTest项目的datastructure33hashmap</a></strong></p>
<p>常见面试题：</p>
<ul>
<li>equals 和 == 的区别，hashCode 与它们之间的联系？</li>
<li>HashMap 的长度为什么是 2 的幂次？</li>
<li>五个线程同时往 HashMap 中 put 数据会发生什么？</li>
<li>ConcurrentHashMap 是怎么保证线程安全的？</li>
<li>HashMap在jdk1.8之后为何引入了红黑树</li>
</ul>
<h1 id="1-HashMap默认的一些参数："><a href="#1-HashMap默认的一些参数：" class="headerlink" title="1. HashMap默认的一些参数："></a>1. HashMap默认的一些参数：</h1><ul>
<li>HashMap <code>数组长度默认大小：16</code>、<code>默认阈值：12</code>、<code>加载因子：0.75f</code>，扩容为当前数组长度大小的<code>一倍</code></li>
<li>vector ：没指定大小的情况下，初始大小为 <code>10</code>,扩容为当前大小的<code>一倍</code></li>
<li>ArrayList ： 没指定大小的情况下，初始大小为 <code>10</code>,扩容为当前大小的<code>二分之一</code></li>
</ul>
<p>为什么加载因子是 <code>0.75</code></p>
<ul>
<li><p>加载因子越大，填满的元素越多，空间利用率越高，但发生冲突的机会变大了；</p>
</li>
<li><p>加载因子越小，填满的元素越少，冲突发生的机会减小，但空间浪费了更多了，而且还会提高扩容rehash操作的次数。</p>
</li>
</ul>
<h1 id="2-和-equals"><a href="#2-和-equals" class="headerlink" title="2. == 和 equals"></a>2. == 和 equals</h1><p>== 判断的是地址 </p>
<p>equals 也是判断两个对象相等，但是更多的是判断内容相等，多用于集合判断</p>
<h1 id="3-hashCode："><a href="#3-hashCode：" class="headerlink" title="3. hashCode："></a>3. hashCode：</h1><p><code>如果你复写了 equals 一般要复写 hashCode</code>,一般集合中都是通过<code>hashcode</code>去减少时间复杂度,获取的时候也会判断<code>hashcode是否相等</code></p>
<p><code>hashcode = 地址值 &gt;&gt; 16</code></p>
<p>总结:    </p>
<ol>
<li>hash 值相等两个对象不一定相等</li>
<li>两个对象不相等 hash 值有可能相等</li>
<li>hash 值不相等的两个对象，这两个对象肯定不相等</li>
</ol>
<h1 id="4-HashMap-的长度为什么是-2-的幂次？"><a href="#4-HashMap-的长度为什么是-2-的幂次？" class="headerlink" title="4. HashMap 的长度为什么是 2 的幂次？"></a>4. HashMap 的长度为什么是 2 的幂次？</h1><p>为了能让HashMap存取高效，尽量减少碰撞，也就是要尽量把数据分配均匀，Hash值的范围是-2147483648到2147483647，前后加起来有40亿的映射空间。</p>
<p>这个散列值是不能直接拿来用的。用之前需要<code>先对数组长度取模运算</code>，得到的才是在数组中存储的位置。</p>
<p>总结：<code>hash%length=hash&amp;(length-1)，但前提是length是2的n次方，并且采用&amp;运算比%运算效率高，且为了减少碰撞，分配均匀，所以长度才会是 2的幂次。</code></p>
<p>举例：<br>    例如长度为9时候，3&amp;(9-1)=0  2&amp;(9-1)=0 ，都在0上，碰撞了；<br>    例如长度为8时候，3&amp;(8-1)=3  2&amp;(8-1)=2 ，不同位置上，不碰撞；  </p>
<h1 id="5-手写HashMap时关键代码分析："><a href="#5-手写HashMap时关键代码分析：" class="headerlink" title="5. 手写HashMap时关键代码分析："></a>5. 手写HashMap时关键代码分析：</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *  手写 HashMap 一些关键性的代码</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HashMap</span>&lt;K,V&gt; &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">MapEntry</span>&lt;K,V&gt;&#123;</span><br><span class="line">        K key;</span><br><span class="line">        V value;</span><br><span class="line">        MapEntry&lt;K,V&gt; next;</span><br><span class="line">        <span class="type">int</span> hash; <span class="comment">// Key 的 hash 值</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="title function_">MapEntry</span><span class="params">(<span class="type">int</span> hash,K key, V value,MapEntry&lt;K, V&gt; next)</span> &#123;</span><br><span class="line">            <span class="built_in">this</span>.hash = hash;</span><br><span class="line">            <span class="built_in">this</span>.key = key;</span><br><span class="line">            <span class="built_in">this</span>.value = value;</span><br><span class="line">            <span class="built_in">this</span>.next = next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  散列 table （桶）</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">transient</span> MapEntry[] table;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  总共多少的键值对</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="type">int</span> size;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  初始大小为 16</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">final</span> <span class="type">int</span> <span class="variable">DEFAULT_INITIAL_CAPACITY</span> <span class="operator">=</span>  <span class="number">1</span> &lt;&lt; <span class="number">4</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  扩容阈值（满足这个条件时扩容）</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="type">int</span> threshold;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  扩容因子，如何扩容</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">final</span> <span class="type">float</span> <span class="variable">loadFactory</span> <span class="operator">=</span> <span class="number">0.75f</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> V <span class="title function_">put</span><span class="params">(K key,V value)</span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(table == <span class="literal">null</span>)&#123;</span><br><span class="line">            table = <span class="keyword">new</span> <span class="title class_">MapEntry</span>[DEFAULT_INITIAL_CAPACITY];</span><br><span class="line">            threshold = (<span class="type">int</span>) (DEFAULT_INITIAL_CAPACITY * loadFactory);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 是不是空</span></span><br><span class="line">        <span class="keyword">if</span>(key == <span class="literal">null</span>)&#123;</span><br><span class="line">            <span class="comment">// 自行看 hashMap 的源码</span></span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 1. 找到 table 的位置</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">hash</span> <span class="operator">=</span> hash(key);</span><br><span class="line">        <span class="type">int</span> <span class="variable">index</span> <span class="operator">=</span> getIndex(hash,table.length);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 判断有没有存在该key</span></span><br><span class="line">        <span class="keyword">for</span> (MapEntry&lt;K,V&gt; e = table[index]; e!=<span class="literal">null</span>; e = e.next)&#123;</span><br><span class="line">            K k;</span><br><span class="line">            <span class="keyword">if</span>(e.hash == hash &amp;&amp; ((k = e.key) == key || (key!=<span class="literal">null</span> &amp;&amp; key.equals(k))))&#123;</span><br><span class="line">                <span class="type">V</span> <span class="variable">oldValue</span> <span class="operator">=</span> e.value;</span><br><span class="line">                e.value = value;</span><br><span class="line">                <span class="keyword">return</span> oldValue;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 3. 添加一个新的 MapEntry</span></span><br><span class="line">        addEntry(hash,key,value,index);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 添加一个新的 Entry</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> hash</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> key</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> value</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> index</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">addEntry</span><span class="params">(<span class="type">int</span> hash, K key, V value, <span class="type">int</span> index)</span> &#123;</span><br><span class="line">        <span class="comment">// hash_shift 16 value() &gt;&gt; 16</span></span><br><span class="line">        <span class="comment">// 判断要不要扩容 jni 源码记住 mask_bits(value() &gt;&gt; hash_shift,hash_mask)</span></span><br><span class="line">        <span class="comment">// 1. hash 值相等两个对象不一定相等，（两个对象不相等，hash值可能相等）</span></span><br><span class="line">        <span class="comment">// 2. hash 值不相等的两个对象肯定不相等</span></span><br><span class="line">        <span class="keyword">if</span>(size &gt;= threshold &amp;&amp; table[index] != <span class="literal">null</span>)&#123;</span><br><span class="line">            resize(table.length &lt;&lt; <span class="number">1</span>);</span><br><span class="line">            <span class="comment">// 重新计算 index</span></span><br><span class="line">            index = getIndex(hash,table.length);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 添加</span></span><br><span class="line">        createEntry(hash,key,value,index);</span><br><span class="line"></span><br><span class="line">        size++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">resize</span><span class="params">(<span class="type">int</span> newCapacity)</span> &#123;</span><br><span class="line">        MapEntry&lt;K,V&gt;[] newTable = <span class="keyword">new</span> <span class="title class_">MapEntry</span>[newCapacity];</span><br><span class="line">        <span class="comment">// 直接把之前的数组搬过来，不行！！ 扩容之后 Index 会变 复杂度 O(n)</span></span><br><span class="line">        transform(newTable);</span><br><span class="line">        table = newTable;</span><br><span class="line">        threshold = (<span class="type">int</span>)(newCapacity * loadFactory);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 重新计算 挪动，散列</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> newTable</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">transform</span><span class="params">(MapEntry&lt;K, V&gt;[] newTable)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">newCapacity</span> <span class="operator">=</span> newTable.length;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (MapEntry&lt;K, V&gt; entry : table) &#123;</span><br><span class="line">            <span class="keyword">while</span> (entry!=<span class="literal">null</span>)&#123;</span><br><span class="line">                <span class="comment">// 从原来的数组中获取数据 Entry , 保证新的数组能链上</span></span><br><span class="line">                MapEntry&lt;K,V&gt; next = entry.next;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 重新计算 index</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">index</span> <span class="operator">=</span> getIndex(entry.hash,newCapacity);</span><br><span class="line">                <span class="comment">// 保证新的数组能链上</span></span><br><span class="line">                entry.next = newTable[index];</span><br><span class="line">                newTable[index] = entry;</span><br><span class="line"></span><br><span class="line">                entry = next;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">createEntry</span><span class="params">(<span class="type">int</span> hash, K key, V value, <span class="type">int</span> index)</span> &#123;</span><br><span class="line">        MapEntry&lt;K,V&gt; newEntry = <span class="keyword">new</span> <span class="title class_">MapEntry</span>&lt;&gt;(hash,key,value,table[index]);</span><br><span class="line">        table[index] = newEntry;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="title function_">hash</span><span class="params">(K key)</span>&#123;</span><br><span class="line">        <span class="type">int</span> h;</span><br><span class="line">        <span class="keyword">return</span> (key == <span class="literal">null</span>) ? <span class="number">0</span> : (h=key.hashCode()) ^ (h &gt;&gt;&gt; <span class="number">16</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="title function_">getIndex</span><span class="params">(<span class="type">int</span> hash,<span class="type">int</span> length)</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> hash &amp; length -<span class="number">1</span>; <span class="comment">// - 运算符比 &amp; 优先级高</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> V <span class="title function_">get</span><span class="params">(K key)</span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(key == <span class="literal">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        MapEntry&lt;K,V&gt; entry = getEntry(key);</span><br><span class="line">        <span class="keyword">return</span> entry == <span class="literal">null</span> ? <span class="literal">null</span> : entry.value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> MapEntry&lt;K, V&gt; <span class="title function_">getEntry</span><span class="params">(K key)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 找到 table 的位置</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">hash</span> <span class="operator">=</span> hash(key);</span><br><span class="line">        <span class="type">int</span> <span class="variable">index</span> <span class="operator">=</span> getIndex(hash, table.length);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 判断有没有存在该 key</span></span><br><span class="line">        <span class="keyword">for</span>(MapEntry&lt;K,V&gt; e = table[index]; e!=<span class="literal">null</span>; e = e.next)&#123;</span><br><span class="line">            K k;</span><br><span class="line">            <span class="keyword">if</span>(hash == e.hash &amp;&amp; ((k = e.key) == key || (key != <span class="literal">null</span> &amp;&amp; key.equals(k))))&#123;</span><br><span class="line">                <span class="keyword">return</span> e;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">size</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> size;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
        <category>HashMap</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>数据结构和算法学习</tag>
        <tag>HashMap</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：09.树、二叉树和森林</title>
    <url>/34.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E6%A0%91%E3%80%81%E4%BA%8C%E5%8F%89%E6%A0%91%E5%92%8C%E6%A3%AE%E6%9E%97/</url>
    <content><![CDATA[<h1 id="1-工作中的一个小例子：登录后自动跳转到目标页面"><a href="#1-工作中的一个小例子：登录后自动跳转到目标页面" class="headerlink" title="1. 工作中的一个小例子：登录后自动跳转到目标页面"></a>1. 工作中的一个小例子：登录后自动跳转到目标页面</h1><p>需求例子：跳转到直播页，首先跳转到登录页，登录成功之后再跳转到直播页<br>比较好的解决方案，随着业务逻辑越来越复杂的情况下，不需要去刻意的去修改原来的代码  </p>
<p>解决办法：<br>1、传参数和Intent（复杂）<br>2、使用路由：Arouter、Drouter<br>3、拦截 Activity 的 <code>startActivityForResult</code> 方法（最好），使用 aspectJ 面向切面</p>
<h1 id="2-树的定义"><a href="#2-树的定义" class="headerlink" title="2. 树的定义"></a>2. 树的定义</h1><ol>
<li>有且只有一个根节点。</li>
<li>有若干互不相交的子树，这些子树本身也是一棵树（所有：树的算法求解一般可以采用<code>递归</code>）</li>
</ol>
<h1 id="3-树的名词解释"><a href="#3-树的名词解释" class="headerlink" title="3. 树的名词解释"></a>3. 树的名词解释</h1><ol>
<li><code>双亲节点（父节点）</code>：有子节点的位置</li>
<li><code>子节点</code>：有父节点</li>
<li><code>兄弟节点</code>：同一个父类下有很多的子节点，他们互相成为兄弟节点</li>
<li><code>度</code>：一个父节点下有多少个子节点</li>
<li><code>叶子节点</code>：没有子节点的节点</li>
</ol>
<h1 id="4-树的分类"><a href="#4-树的分类" class="headerlink" title="4. 树的分类"></a>4. 树的分类</h1><ol>
<li><code>一般树</code>:任意一个节点的子节点都不受限制。</li>
<li><code>二叉树</code>：任意一个节点的子节点最多<code>2</code>个，且子节点位置不能更改（左右子节点）。</li>
<li><code>森林</code>：n 棵互补相交的树的集合。</li>
</ol>
<h1 id="5-树的操作（树的存储）"><a href="#5-树的操作（树的存储）" class="headerlink" title="5. 树的操作（树的存储）"></a>5. 树的操作（树的存储）</h1><p>树的存储：<code>顺序存储</code>、<code>链式存储</code>，总共有以下几种方法：</p>
<ol>
<li>双亲表示法（找父亲比较简单，找孩子比较麻烦）</li>
<li>孩子表示法（找孩子比较简单，找父亲比较麻烦）</li>
<li>孩子双亲表示法（找父亲和孩子都比较简单）</li>
<li>二叉树表示法（一般树、森林与二叉树之间互转）</li>
</ol>
<h2 id="5-1-孩子双亲表示法"><a href="#5-1-孩子双亲表示法" class="headerlink" title="5.1 孩子双亲表示法"></a>5.1 孩子双亲表示法</h2><p><img src="/images/34.孩子双亲表示法.png" alt=""></p>
<h2 id="5-2-二叉树表示法（一般树、森林与二叉树之间互转）"><a href="#5-2-二叉树表示法（一般树、森林与二叉树之间互转）" class="headerlink" title="5.2 二叉树表示法（一般树、森林与二叉树之间互转）"></a>5.2 二叉树表示法（一般树、森林与二叉树之间互转）</h2><h3 id="5-2-1一般树-转-二叉树"><a href="#5-2-1一般树-转-二叉树" class="headerlink" title="5.2.1一般树 转 二叉树"></a>5.2.1一般树 转 二叉树</h3><p>第一种方式：<br>　　1. 左节点当做左节点<br>　　2. <code>谁</code>的兄弟节点，就当做<code>谁</code>的右节点  </p>
<p>第二种方式：<br><img src="/images/34.树转二叉树.png" alt=""></p>
<h3 id="5-2-2-二叉树-转-树"><a href="#5-2-2-二叉树-转-树" class="headerlink" title="5.2.2 二叉树 转 树"></a>5.2.2 二叉树 转 树</h3><p><img src="/images/34.二叉树转树.png" alt=""></p>
<h3 id="5-2-3-森林转二叉树"><a href="#5-2-3-森林转二叉树" class="headerlink" title="5.2.3 森林转二叉树"></a>5.2.3 森林转二叉树</h3><p><img src="/images/34.森林转二叉树.png" alt=""></p>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>树</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：10.二叉树常见操作、平衡二叉树</title>
    <url>/35.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E4%BA%8C%E5%8F%89%E6%A0%91%E5%B8%B8%E8%A7%81%E6%93%8D%E4%BD%9C%E3%80%81%E5%B9%B3%E8%A1%A1%E4%BA%8C%E5%8F%89%E6%A0%91/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure35binarytree">NDKPractice项目的datastructure35binarytree</a></strong></p>
<h1 id="1-二叉树分类"><a href="#1-二叉树分类" class="headerlink" title="1. 二叉树分类"></a>1. 二叉树分类</h1><ol>
<li><code>普通二叉树</code></li>
<li><code>完全二叉树</code></li>
<li><code>满二叉树</code></li>
</ol>
<h2 id="完全二叉树："><a href="#完全二叉树：" class="headerlink" title="完全二叉树："></a><code>完全二叉树</code>：</h2><p>概念：<code>叶结点只能出现在最底层的两层，且最底层叶结点均处于次底层叶结点的左侧。</code><br><img src="/images/35.完全二叉树.png" alt=""></p>
<h2 id="满二叉树："><a href="#满二叉树：" class="headerlink" title="满二叉树："></a><code>满二叉树</code>：</h2><p>概念：<code>所有叶结点同处于最底层（非底层结点均是内部结点），一个深度为k(&gt;=-1)且有2^(k+1) - 1个结点</code><br><img src="/images/35.满二叉树.png" alt=""></p>
<h2 id="1-1-常用的操作树："><a href="#1-1-常用的操作树：" class="headerlink" title="1.1 常用的操作树："></a>1.1 常用的操作树：</h2><ol>
<li>二叉搜索树（Binary Search Tree）又称 B 树 （SQL）</li>
<li>哈夫曼树</li>
<li><font color = red>平衡二叉树(AVL树)：`可以是一棵空树，左右子树的高度差不会超过 1 ，并且左右两棵子树都是一棵平衡二叉树`</font></li>
<li>红黑树</li>
</ol>
<h1 id="2-二叉树的遍历"><a href="#2-二叉树的遍历" class="headerlink" title="2. 二叉树的遍历"></a>2. 二叉树的遍历</h1><p><img src="/images/35.二叉树遍历.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">visit</span><span class="params">(<span class="type">char</span> data)</span></span>&#123;</span><br><span class="line">    <span class="built_in">LOGE</span>(<span class="string">&quot;%c&quot;</span>,data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-1-前序遍历"><a href="#2-1-前序遍历" class="headerlink" title="2.1 前序遍历"></a>2.1 前序遍历</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">preOrderTraverse</span><span class="params">(TreeNode&lt;T&gt; *pNode,<span class="type">void</span>(*visit)(T))</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!pNode)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 先根节点</span></span><br><span class="line">    <span class="built_in">visit</span>(pNode-&gt;data);</span><br><span class="line">    <span class="comment">// 再左节点</span></span><br><span class="line">    <span class="built_in">preOrderTraverse</span>(pNode-&gt;left,visit);</span><br><span class="line">    <span class="comment">// 再右节点</span></span><br><span class="line">    <span class="built_in">preOrderTraverse</span>(pNode-&gt;right,visit);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-2-中序遍历"><a href="#2-2-中序遍历" class="headerlink" title="2.2 中序遍历"></a>2.2 中序遍历</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">infixOrderTraverse</span><span class="params">(TreeNode&lt;T&gt; *pNode,<span class="type">void</span>(*visit)(T))</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!pNode)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 先左节点</span></span><br><span class="line">    <span class="built_in">infixOrderTraverse</span>(pNode-&gt;left,visit);</span><br><span class="line">    <span class="comment">// 再根节点</span></span><br><span class="line">    <span class="built_in">visit</span>(pNode-&gt;data);</span><br><span class="line">    <span class="comment">// 再右节点</span></span><br><span class="line">    <span class="built_in">infixOrderTraverse</span>(pNode-&gt;right,visit);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-3-后序遍历"><a href="#2-3-后序遍历" class="headerlink" title="2.3 后序遍历"></a>2.3 后序遍历</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">afterOrderTraverse</span><span class="params">(TreeNode&lt;T&gt; *pNode,<span class="type">void</span>(*visit)(T))</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!pNode)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 先左节点</span></span><br><span class="line">    <span class="built_in">afterOrderTraverse</span>(pNode-&gt;left,visit);</span><br><span class="line">    <span class="comment">// 再右节点</span></span><br><span class="line">    <span class="built_in">afterOrderTraverse</span>(pNode-&gt;right,visit);</span><br><span class="line">    <span class="comment">// 再根节点</span></span><br><span class="line">    <span class="built_in">visit</span>(pNode-&gt;data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-4-层序遍历"><a href="#2-4-层序遍历" class="headerlink" title="2.4 层序遍历"></a>2.4 层序遍历</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">levelOrderTraverse</span><span class="params">(TreeNode&lt;T&gt; *pNode,<span class="type">void</span>(*visit)(T))</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!pNode)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    queue&lt;TreeNode&lt;T&gt;*&gt; queue;</span><br><span class="line">    queue.<span class="built_in">push</span>(pNode);</span><br><span class="line">    <span class="keyword">while</span>(!queue.<span class="built_in">empty</span>())&#123;</span><br><span class="line">        TreeNode&lt;T&gt; *front = queue.<span class="built_in">front</span>();</span><br><span class="line">        queue.<span class="built_in">pop</span>();</span><br><span class="line">        <span class="built_in">visit</span>(front-&gt;data);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(front-&gt;left)</span><br><span class="line">            queue.<span class="built_in">push</span>(front-&gt;left);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(front-&gt;right)</span><br><span class="line">            queue.<span class="built_in">push</span>(front-&gt;right);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-根据前序和中序遍历-或-后序和中序遍历还原二叉树"><a href="#3-根据前序和中序遍历-或-后序和中序遍历还原二叉树" class="headerlink" title="3. 根据前序和中序遍历 或 后序和中序遍历还原二叉树"></a>3. 根据<code>前序和中序遍历</code> 或 <code>后序和中序遍历</code>还原二叉树</h1><p>中序遍历 和 前序或后序任一种都可还原二叉树</p>
<p>前序：ABCDEFGH<br>中序：BDCEAFHG</p>
<p><img src="/images/35.前序和中序遍历还原二叉树.png" alt=""></p>
<h1 id="4-二叉树的深度："><a href="#4-二叉树的深度：" class="headerlink" title="4. 二叉树的深度："></a>4. 二叉树的深度：</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *  获取树的深度</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">getDepthTree</span><span class="params">(TreeNode&lt;T&gt; *pNode)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!pNode)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> left = <span class="built_in">getDepthTree</span>(pNode-&gt;left);</span><br><span class="line">    <span class="type">int</span> right = <span class="built_in">getDepthTree</span>(pNode-&gt;right);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">max</span>(left,right) + <span class="number">1</span>; <span class="comment">// 需要加上自己</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-判断是否是平衡二叉树"><a href="#5-判断是否是平衡二叉树" class="headerlink" title="5. 判断是否是平衡二叉树"></a>5. 判断是否是平衡二叉树</h1><p><code>可以是一棵空树，左右子树的高度差不会超过 1 ，并且左右两棵子树都是一棵平衡二叉树</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *  判断一棵树是否是平衡二叉树</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isBalanceTree</span><span class="params">(TreeNode&lt;T&gt; *pNode)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 可以是一棵空树，左右子树的高度差不会超过 1 ，并且左右两棵子树都是一棵平衡二叉树</span></span><br><span class="line">    <span class="keyword">if</span>(!pNode)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 左右子树的高度差不会超过 1</span></span><br><span class="line">    <span class="type">int</span> left = <span class="built_in">getDepthTree</span>(pNode-&gt;left);</span><br><span class="line">    <span class="type">int</span> right = <span class="built_in">getDepthTree</span>(pNode-&gt;right);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 并且左右两棵子树都是一棵平衡二叉树</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">abs</span>(left-right) &lt;= <span class="number">1</span> &amp;&amp; <span class="built_in">isBalanceTree</span>(pNode-&gt;left) &amp;&amp; <span class="built_in">isBalanceTree</span>(pNode-&gt;right);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>树</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：12.二叉搜索树</title>
    <url>/37.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E4%BA%8C%E5%8F%89%E6%90%9C%E7%B4%A2%E6%A0%91/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure37binarysearchtree">NDKPractice项目的datastructure37binarysearchtree</a></strong></p>
<h1 id="1-二叉搜索树的定义："><a href="#1-二叉搜索树的定义：" class="headerlink" title="1. 二叉搜索树的定义："></a>1. 二叉搜索树的定义：</h1><p>定义：<code>比它（当前根节点）小的放左边，比它（当前根节点）大的放右边</code></p>
<p><code>普通二叉搜索树的中序遍历，就是从小到大的排序  （数据排序）</code></p>
<h1 id="2-删除"><a href="#2-删除" class="headerlink" title="2.删除"></a>2.删除</h1><p>当删除节点的左右两个子节点都不为 NULL 的情况下，<code>从左边找最大值来替代</code>，<code>或者从右边找最小值</code></p>
<h1 id="3-代码"><a href="#3-代码" class="headerlink" title="3.代码"></a>3.代码</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">K</span>, <span class="keyword">class</span> <span class="title class_">V</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">TreeNode</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    TreeNode&lt;K,V&gt; *left = <span class="literal">NULL</span>;</span><br><span class="line">    TreeNode&lt;K,V&gt; *right = <span class="literal">NULL</span>;</span><br><span class="line">    K key = <span class="literal">NULL</span>;</span><br><span class="line">    V value = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">TreeNode</span>(TreeNode&lt;K,V&gt; *pNode)&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;left = pNode-&gt;left;</span><br><span class="line">        <span class="keyword">this</span>-&gt;right = pNode-&gt;right;</span><br><span class="line">        <span class="keyword">this</span>-&gt;key = pNode-&gt;key;</span><br><span class="line">        <span class="keyword">this</span>-&gt;value = pNode-&gt;value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">TreeNode</span>(K key, V value):<span class="built_in">key</span>(key),<span class="built_in">value</span>(value)&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 二叉搜索树</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">K</span>, <span class="keyword">class</span> <span class="title class_">V</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BST</span> &#123;</span><br><span class="line">    <span class="type">int</span> count = <span class="number">0</span>; <span class="comment">// 总个数</span></span><br><span class="line">    TreeNode&lt;K,V&gt; *root = <span class="literal">NULL</span>; <span class="comment">// 根节点</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">BST</span>()&#123;&#125;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">BST</span>()&#123;</span><br><span class="line">        <span class="built_in">deleteAllNode</span>(root);</span><br><span class="line">        root = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 新增</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">put</span><span class="params">(K key,V value)</span></span>&#123;</span><br><span class="line">        root = <span class="built_in">addNode</span>(root,key,value);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取</span></span><br><span class="line">    <span class="function">V <span class="title">get</span><span class="params">(K key)</span></span>&#123;</span><br><span class="line">        TreeNode&lt;K,V&gt; *node = root;</span><br><span class="line">        <span class="keyword">while</span> (node)&#123;</span><br><span class="line">            <span class="keyword">if</span>(node-&gt;key == key)</span><br><span class="line">                <span class="keyword">return</span> node-&gt;value;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(node-&gt;key &gt; key)</span><br><span class="line">                node = node-&gt;left;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(node-&gt;key &lt; key)</span><br><span class="line">                node = node-&gt;right;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">size</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 是否包含</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">contains</span><span class="params">(K key)</span></span>&#123;</span><br><span class="line">        TreeNode&lt;K,V&gt; *node = root;</span><br><span class="line">        <span class="keyword">while</span> (node)&#123;</span><br><span class="line">            <span class="keyword">if</span>(node-&gt;key == key)</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(node-&gt;key &gt; key)</span><br><span class="line">                node = node-&gt;left;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(node-&gt;key &lt; key)</span><br><span class="line">                node = node-&gt;right;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 移除</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">remove</span><span class="params">(K key)</span></span>&#123;</span><br><span class="line">        root = <span class="built_in">removeNode</span>(root,key);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 中序遍历 就是相当于从小到大的升序遍历了</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">infixOrderTraverse</span><span class="params">(<span class="type">void</span>(*log)(<span class="type">int</span>,<span class="type">int</span>))</span></span>&#123;</span><br><span class="line">        <span class="built_in">infixOrderTraverse</span>(root,log);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// 删除所有的数据</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">deleteAllNode</span><span class="params">(TreeNode&lt;K,V&gt; *pNode)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(pNode-&gt;left)</span><br><span class="line">            <span class="built_in">deleteAllNode</span>(pNode-&gt;left);</span><br><span class="line">        <span class="keyword">if</span>(pNode-&gt;right)</span><br><span class="line">            <span class="built_in">deleteAllNode</span>(pNode-&gt;right);</span><br><span class="line">        <span class="built_in">delete</span>(pNode);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode&lt;K,V&gt; *<span class="title">addNode</span><span class="params">(TreeNode&lt;K,V&gt; *pNode,K key,V value)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(!pNode)&#123;</span><br><span class="line">            count++;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">TreeNode</span>&lt;K,V&gt;(key,value);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(pNode-&gt;key &gt; key)&#123;</span><br><span class="line">            pNode-&gt;left = <span class="built_in">addNode</span>(pNode-&gt;left,key,value);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pNode-&gt;key &lt; key)&#123;</span><br><span class="line">            pNode-&gt;right = <span class="built_in">addNode</span>(pNode-&gt;right,key,value);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            pNode-&gt;value = value;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> pNode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode&lt;K,V&gt; *<span class="title">removeNode</span><span class="params">(TreeNode&lt;K,V&gt; *pNode,K key)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(pNode-&gt;key &gt; key)</span><br><span class="line">            pNode-&gt;left = <span class="built_in">removeNode</span>(pNode-&gt;left,key);</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(pNode-&gt;key &lt; key)</span><br><span class="line">            pNode-&gt;right = <span class="built_in">removeNode</span>(pNode-&gt;right);</span><br><span class="line">        <span class="keyword">else</span>&#123; <span class="comment">// 相等找到了</span></span><br><span class="line">            count --;</span><br><span class="line">            <span class="keyword">if</span>(pNode-&gt;left == <span class="literal">NULL</span> &amp;&amp; pNode-&gt;right == <span class="literal">NULL</span>)&#123;</span><br><span class="line">                <span class="built_in">delete</span>(pNode);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">            &#125;<span class="keyword">else</span> <span class="keyword">if</span>(pNode-&gt;left == <span class="literal">NULL</span>)&#123;</span><br><span class="line">                TreeNode&lt;K, V&gt; *right = pNode-&gt;right;</span><br><span class="line">                <span class="built_in">delete</span> (pNode);</span><br><span class="line">                <span class="keyword">return</span> right;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pNode-&gt;right == <span class="literal">NULL</span>) &#123;</span><br><span class="line">                TreeNode&lt;K, V&gt; *left = pNode-&gt;left;</span><br><span class="line">                <span class="built_in">delete</span> (pNode);</span><br><span class="line">                <span class="keyword">return</span> left;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 左右两子树都不为空（把左子树的最大值作为根，或者右子树的最小值作为根）</span></span><br><span class="line">                TreeNode&lt;K, V&gt; *successor = <span class="keyword">new</span> <span class="built_in">TreeNode</span>&lt;K, V&gt;(<span class="built_in">maximum</span>(pNode-&gt;left));</span><br><span class="line">                successor-&gt;left = <span class="built_in">deleteMax</span>(pNode-&gt;left);</span><br><span class="line">                count++;</span><br><span class="line">                successor-&gt;right = pNode-&gt;right;</span><br><span class="line">                <span class="built_in">delete</span> (pNode);</span><br><span class="line">                <span class="keyword">return</span> successor;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> pNode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode&lt;K, V&gt; *<span class="title">deleteMax</span><span class="params">(TreeNode&lt;K, V&gt; *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (pNode-&gt;right == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            TreeNode&lt;K, V&gt; *left = pNode-&gt;left;</span><br><span class="line">            <span class="built_in">delete</span> (pNode);</span><br><span class="line">            count--;</span><br><span class="line">            <span class="keyword">return</span> left;</span><br><span class="line">        &#125;</span><br><span class="line">        pNode-&gt;right = <span class="built_in">deleteMax</span>(pNode-&gt;right);</span><br><span class="line">        <span class="keyword">return</span> pNode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 查找当前树的左边的最大值 （右边的最小值怎么找？）</span></span><br><span class="line">    <span class="function">TreeNode&lt;K, V&gt; *<span class="title">maximum</span><span class="params">(TreeNode&lt;K, V&gt; *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 不断的往右边找，直到找到右子树为空节点</span></span><br><span class="line">        <span class="keyword">if</span> (pNode-&gt;right == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> pNode;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">maximum</span>(pNode-&gt;left);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">infixOrderTraverse</span><span class="params">(TreeNode&lt;K,V&gt; *pNode,<span class="type">void</span>(*log)(<span class="type">int</span>,<span class="type">int</span>))</span></span>&#123;</span><br><span class="line">        <span class="comment">// 先左孩子</span></span><br><span class="line">        <span class="built_in">infixOrderTraverse</span>(pNode-&gt;left,log);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 再根节点</span></span><br><span class="line">        <span class="built_in">log</span>(pNode-&gt;key,pNode-&gt;value);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 再右孩子</span></span><br><span class="line">        <span class="built_in">infixOrderTraverse</span>(pNode-&gt;right,log);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>树</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：14.红黑树</title>
    <url>/40%E3%80%81%2041.%20%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E7%BA%A2%E9%BB%91%E6%A0%91/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure40redblacktree">NDKPractice项目的datastructure40redblacktree</a></strong></p>
<h1 id="1-红黑树的定义："><a href="#1-红黑树的定义：" class="headerlink" title="1. 红黑树的定义："></a>1. 红黑树的定义：</h1><p>红黑树是一棵自平衡的二叉搜索树</p>
<p>特性：</p>
<ol>
<li>每个节点或者是黑色，或者是红色。</li>
<li>根节点是黑色。</li>
<li>每个叶子节点（NIL）是黑色。 [注意：这里叶子节点，是指为空(NIL或NULL)的叶子节点！]</li>
<li>如果一个节点是红色的，则它的子节点必须是黑色的。</li>
<li>从一个节点到该节点的子孙节点的所有路径上包含相同数目的黑节点。</li>
</ol>
<h1 id="2-新增逻辑："><a href="#2-新增逻辑：" class="headerlink" title="2. 新增逻辑："></a>2. 新增逻辑：</h1><p>分情况分析：</p>
<ol>
<li>如果没有出现双红现象，父亲是黑色的不需要修正；</li>
<li>叔叔是红色的 ，将叔叔和父亲染黑，然后爷爷染红；指针回到爷爷</li>
<li>叔叔是黑色的，父亲是爷爷的左节点，且当前节点是其父节点的右孩子，将“父节点”作为“新的当前节点”，以“新的当前节点”为支点进行左旋。然后将“父节点”设为“黑色”，将“祖父节点”设为“红色”，以“祖父节点”为支点进行右旋；</li>
<li>叔叔是黑色的，父亲是爷爷的左节点，且当前节点是其父节点的左孩子，将“父节点”设为“黑色”，将“祖父节点”设为“红色”，以“祖父节点”为支点进行右旋；</li>
<li>叔叔是黑色的，父亲是爷爷的右节点，且当前节点是其父节点的左孩子，将“父节点”作为“新的当前节点”，以“新的当前节点”为支点进行右旋。然后将“父节点”设为“黑色”，将“祖父节点”设为“红色”，以“祖父节点”为支点进行左旋；</li>
<li>叔叔是黑色的，父亲是爷爷的右节点，且当前节点是其父节点的右孩子，将“父节点”设为“黑色”，将“祖父节点”设为“红色”，以“祖父节点”为支点进行左旋；</li>
</ol>
<p>上面的双红修正现象看似比较复杂，但实际上只有三种情况，一种是没有双红现象，另一种是父亲和叔叔都是红色的，最后一种是叔叔是黑色的。我们来画个实例看下：以 [3,2,1,4,5] 为例</p>
<p><img src="/images/40.红黑树新增.png" alt=""></p>
<h1 id="3-删除逻辑："><a href="#3-删除逻辑：" class="headerlink" title="3. 删除逻辑："></a><a href="https://blog.csdn.net/a944750471/article/details/92384553">3. 删除逻辑：</a></h1><p>讨论下：</p>
<ol>
<li>删除一个红色节点不影响；</li>
<li>加入删除一个黑色节点那么肯定会破坏性质 5</li>
<li>假设左右子树都不为空的情况下，需要找到后继节点来代替，其实删除的就是后继节点</li>
<li>假设删除的左右两棵子树上有一颗不为空的情况，会找左右的一棵子树来代替</li>
</ol>
<p><code>左右子树不为null的情况下实际删除的是后继节点</code></p>
<ol>
<li>如果兄弟节点是红色的，把兄弟节点染黑，父节点染红，左/右旋父节点；</li>
<li>如果兄弟节点是黑色的，并且两个侄子节点都是黑色的，将兄弟节点染红，指针回溯至父亲节点；</li>
<li>如果兄弟节点是黑色，的并且远侄子是黑色的，近侄子是红色的，将进侄子染黑，兄弟染红，左/右旋兄弟节点，进入下面情况 4 ；</li>
<li>如果兄弟节点是黑色的，并且远侄子是红色的，近侄子随意，将兄弟节点染成父亲节点的颜色，父亲节点染黑，远侄子染黑，左/右旋父亲节点。</li>
</ol>
<p><img src="/images/40.红黑树删除.png" alt=""></p>
<h1 id="4-红黑树与-AVL-树的对比"><a href="#4-红黑树与-AVL-树的对比" class="headerlink" title="4.红黑树与 AVL 树的对比"></a>4.红黑树与 AVL 树的对比</h1><p>AVL : 插入的思想，在插入的过程中不断去调整树，最坏的情况下可能每次回溯都需要调整（回溯到父亲节点）<br>红黑树：可能会出现双红现象，如果叔叔是黑色的只需要调整一次或者两次 ，最坏的情况下就是叔叔节点是红色，但每次回溯的时候是两层（回溯到爷爷节点）</p>
<p>查找：AVL 复杂度是 log(N)，红黑树复杂度是 Log(2N)<br>新增：AVL 复杂度是 log(N)，红黑树复杂度是 Log(N) / 2<br>删除：AVL 复杂度是 log(N)，红黑树复杂度是 Log(N) / 2 </p>
<p><code>红黑树的查询性能略微逊色于AVL树</code>，因为其比AVL树会稍微不平衡最多一层，也就是说红黑树的查询性能只比相同内容的AVL树最多多一次比较。<br>但是，<code>红黑树在插入和删除上优于AVL树</code>，AVL树每次插入删除会进行大量的平衡度计算，而红黑树为了维持红黑性质所做的红黑变换和旋转的开销，相较于AVL树为了维持平衡的开销要小得多。</p>
<h1 id="5-总结"><a href="#5-总结" class="headerlink" title="5. 总结"></a>5. 总结</h1><p>总结：实际应用中，若搜索的次数远远大于插入和删除，那么选择AVL，如果搜索，插入删除次数几乎差不多，应该选择RB。</p>
<h1 id="6-代码："><a href="#6-代码：" class="headerlink" title="6. 代码："></a>6. 代码：</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;queue&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="type">bool</span> rb_color;</span><br><span class="line"><span class="meta">#<span class="keyword">define</span> black false <span class="comment">// 黑色  define 后面一定不能带分号</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> red true <span class="comment">// 红色</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">K</span>, <span class="keyword">class</span> <span class="title class_">V</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">map</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">TreeNode</span>;</span><br><span class="line">    <span class="type">int</span> count = <span class="number">0</span>;</span><br><span class="line">    TreeNode *root = <span class="literal">NULL</span>;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">map</span>() &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">iterator</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode *<span class="title">parent</span><span class="params">(TreeNode *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> pNode ? pNode-&gt;parent : <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode *<span class="title">left</span><span class="params">(TreeNode *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> pNode ? pNode-&gt;left : <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode *<span class="title">right</span><span class="params">(TreeNode *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> pNode ? pNode-&gt;right : <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode *<span class="title">brother</span><span class="params">(TreeNode *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">left</span>(<span class="built_in">parent</span>(pNode)) == pNode ? <span class="built_in">right</span>(<span class="built_in">parent</span>(pNode)) : <span class="built_in">left</span>(<span class="built_in">parent</span>(pNode));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">rb_color <span class="title">getColor</span><span class="params">(TreeNode *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> pNode ? pNode-&gt;color : black;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setColor</span><span class="params">(TreeNode *pNode, rb_color color)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (pNode)</span><br><span class="line">            pNode-&gt;color = color;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 左旋</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">l_rotation</span><span class="params">(TreeNode *pNode)</span> </span>&#123;</span><br><span class="line">        TreeNode *right = pNode-&gt;right;</span><br><span class="line">        TreeNode *left = right-&gt;left;</span><br><span class="line">        pNode-&gt;right = left;</span><br><span class="line">        right-&gt;left = pNode;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 调整pNode 父亲指向</span></span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">parent</span>(pNode)) &#123;</span><br><span class="line">            root = right;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pNode-&gt;parent-&gt;left == pNode) &#123;</span><br><span class="line">            <span class="built_in">parent</span>(pNode)-&gt;left = right;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">parent</span>(pNode)-&gt;right = right;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 修改父亲</span></span><br><span class="line">        right-&gt;parent = <span class="built_in">parent</span>(pNode);</span><br><span class="line">        pNode-&gt;parent = right;</span><br><span class="line">        <span class="keyword">if</span> (left)</span><br><span class="line">            left-&gt;parent = pNode;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 左旋</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">r_rotation</span><span class="params">(TreeNode *pNode)</span> </span>&#123;</span><br><span class="line">        TreeNode *left = pNode-&gt;left;</span><br><span class="line">        TreeNode *right = left-&gt;right;</span><br><span class="line">        pNode-&gt;left = right;</span><br><span class="line">        left-&gt;right = pNode;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 调整pNode 父亲指向</span></span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">parent</span>(pNode)) &#123;</span><br><span class="line">            root = left;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pNode-&gt;parent-&gt;left == pNode) &#123;</span><br><span class="line">            <span class="built_in">parent</span>(pNode)-&gt;left = left;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">parent</span>(pNode)-&gt;right = left;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 修改父亲</span></span><br><span class="line">        left-&gt;parent = <span class="built_in">parent</span>(pNode);</span><br><span class="line">        pNode-&gt;parent = left;</span><br><span class="line">        <span class="keyword">if</span> (right)</span><br><span class="line">            right-&gt;parent = pNode;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 调整顺序</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">solveDoubleRed</span><span class="params">(TreeNode *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (pNode-&gt;parent &amp;&amp; pNode-&gt;parent-&gt;color == red) &#123; <span class="comment">// 父亲是红色</span></span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">getColor</span>(<span class="built_in">brother</span>(<span class="built_in">parent</span>(pNode))) == red) &#123; <span class="comment">// 情况2：叔叔是红色的 ，将叔叔和父亲染黑，然后爷爷染红；</span></span><br><span class="line">                <span class="built_in">setColor</span>(<span class="built_in">parent</span>(pNode), black);</span><br><span class="line">                <span class="built_in">setColor</span>(<span class="built_in">brother</span>(<span class="built_in">parent</span>(pNode)), black);</span><br><span class="line">                <span class="built_in">setColor</span>(<span class="built_in">parent</span>(<span class="built_in">parent</span>(pNode)), red);</span><br><span class="line">                pNode = <span class="built_in">parent</span>(<span class="built_in">parent</span>(pNode));</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123; <span class="comment">// 叔叔是黑色</span></span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">left</span>(<span class="built_in">parent</span>(<span class="built_in">parent</span>(pNode))) == <span class="built_in">parent</span>(pNode)) &#123;</span><br><span class="line">                    <span class="comment">// 情况3：叔叔是黑色的，父亲是爷爷的左节点，且当前节点是其父节点的右孩子，将“父节点”作为“新的当前节点”，以“新的当前节点”为支点进行左旋。</span></span><br><span class="line">                    <span class="keyword">if</span> (<span class="built_in">right</span>(<span class="built_in">parent</span>(pNode)) == pNode) &#123;</span><br><span class="line">                        pNode = <span class="built_in">parent</span>(pNode);</span><br><span class="line">                        <span class="built_in">l_rotation</span>(pNode);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 情况4：叔叔是黑色的，父亲是爷爷的左节点，且当前节点是其父节点的左孩子，将“父节点”设为“黑色”，将“祖父节点”设为“红色”，以“祖父节点”为支点进行右旋；</span></span><br><span class="line">                    <span class="built_in">setColor</span>(<span class="built_in">parent</span>(pNode), black);</span><br><span class="line">                    <span class="built_in">setColor</span>(<span class="built_in">parent</span>(<span class="built_in">parent</span>(pNode)), red);</span><br><span class="line">                    <span class="built_in">r_rotation</span>(<span class="built_in">parent</span>(<span class="built_in">parent</span>(pNode)));</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 情况3：叔叔是黑色的，父亲是爷爷的左节点，且当前节点是其父节点的右孩子，将“父节点”作为“新的当前节点”，以“新的当前节点”为支点进行左旋。</span></span><br><span class="line">                    <span class="keyword">if</span> (<span class="built_in">left</span>(<span class="built_in">parent</span>(pNode)) == pNode) &#123;</span><br><span class="line">                        pNode = <span class="built_in">parent</span>(pNode);</span><br><span class="line">                        <span class="built_in">r_rotation</span>(pNode);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 情况4：叔叔是黑色的，父亲是爷爷的左节点，且当前节点是其父节点的左孩子，将“父节点”设为“黑色”，将“祖父节点”设为“红色”，以“祖父节点”为支点进行右旋；</span></span><br><span class="line">                    <span class="built_in">setColor</span>(<span class="built_in">parent</span>(pNode), black);</span><br><span class="line">                    <span class="built_in">setColor</span>(<span class="built_in">parent</span>(<span class="built_in">parent</span>(pNode)), red);</span><br><span class="line">                    <span class="built_in">l_rotation</span>(<span class="built_in">parent</span>(<span class="built_in">parent</span>(pNode)));</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        root-&gt;color = black;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function">iterator <span class="title">insert</span><span class="params">(K key, V value)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!root) &#123;</span><br><span class="line">            root = <span class="keyword">new</span> <span class="built_in">TreeNode</span>(<span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>, key, value, black);</span><br><span class="line">            count = <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">iterator</span>(root);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 最好我们插入红色节点，它不会违反性质 5 ，但是有可能违反性质 4</span></span><br><span class="line">        TreeNode *node = root;</span><br><span class="line">        TreeNode *parent = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="keyword">while</span> (node) &#123;</span><br><span class="line">            parent = node;</span><br><span class="line">            <span class="keyword">if</span> (node-&gt;key &gt; key)</span><br><span class="line">                node = node-&gt;left;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (node-&gt;key &lt; key)</span><br><span class="line">                node = node-&gt;right;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                node-&gt;value = value;</span><br><span class="line">                <span class="keyword">return</span> <span class="built_in">iterator</span>(node);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        TreeNode *new_node = <span class="keyword">new</span> <span class="built_in">TreeNode</span>(<span class="literal">NULL</span>, <span class="literal">NULL</span>, parent, key, value, red);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (key &lt; parent-&gt;key)</span><br><span class="line">            parent-&gt;left = new_node;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            parent-&gt;right = new_node;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 调整顺序</span></span><br><span class="line">        <span class="built_in">solveDoubleRed</span>(new_node);</span><br><span class="line"></span><br><span class="line">        count++;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">iterator</span>(new_node);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode *<span class="title">findTree</span><span class="params">(K key)</span> </span>&#123;</span><br><span class="line">        TreeNode *node = root;</span><br><span class="line">        <span class="keyword">while</span> (node) &#123;</span><br><span class="line">            <span class="keyword">if</span> (key == node-&gt;key) &#123;</span><br><span class="line">                <span class="keyword">return</span> node;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (key &gt; node-&gt;key) &#123;</span><br><span class="line">                node = node-&gt;right;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                node = node-&gt;left;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 关键代码</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">solveLostBlack</span><span class="params">(TreeNode *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (pNode != root &amp;&amp; pNode-&gt;color == black) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">left</span>(<span class="built_in">parent</span>(pNode)) == pNode) &#123;<span class="comment">// 当前节点是父亲节点的左节点</span></span><br><span class="line">                TreeNode *sib = <span class="built_in">brother</span>(pNode);</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">getColor</span>(sib) == red) &#123; <span class="comment">// 想办法把兄弟节点变成黑色  情况 1</span></span><br><span class="line">                    <span class="built_in">setColor</span>(sib, black);</span><br><span class="line">                    <span class="built_in">setColor</span>(<span class="built_in">parent</span>(pNode), red);</span><br><span class="line">                    <span class="built_in">l_rotation</span>(<span class="built_in">parent</span>(pNode));</span><br><span class="line">                    sib = <span class="built_in">brother</span>(pNode);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">getColor</span>(<span class="built_in">left</span>(sib)) == black &amp;&amp; <span class="built_in">getColor</span>(<span class="built_in">right</span>(sib)) == black) &#123; <span class="comment">// 情况2</span></span><br><span class="line">                    <span class="built_in">setColor</span>(sib, red);</span><br><span class="line">                    pNode = <span class="built_in">parent</span>(pNode);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 情况 3</span></span><br><span class="line">                    <span class="keyword">if</span> (<span class="built_in">getColor</span>(<span class="built_in">right</span>(sib)) == black) &#123;</span><br><span class="line">                        <span class="built_in">setColor</span>(sib, red);</span><br><span class="line">                        <span class="built_in">setColor</span>(<span class="built_in">left</span>(sib), black);</span><br><span class="line">                        <span class="built_in">r_rotation</span>(sib);</span><br><span class="line">                        sib = <span class="built_in">brother</span>(pNode);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 情况 4</span></span><br><span class="line">                    <span class="built_in">setColor</span>(sib, <span class="built_in">getColor</span>(<span class="built_in">parent</span>(pNode)));</span><br><span class="line">                    <span class="built_in">setColor</span>(<span class="built_in">parent</span>(pNode), black);</span><br><span class="line">                    <span class="built_in">setColor</span>(<span class="built_in">right</span>(sib), black);</span><br><span class="line">                    <span class="built_in">l_rotation</span>(<span class="built_in">parent</span>(pNode));</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 相当于两行代码 ：break ，将根节点染黑</span></span><br><span class="line">                    pNode = root;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;<span class="comment">// 当前节点是父亲节点的右节点</span></span><br><span class="line">                TreeNode *sib = <span class="built_in">brother</span>(pNode);</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">getColor</span>(sib) == red) &#123; <span class="comment">// 想办法把兄弟节点变成黑色</span></span><br><span class="line">                    <span class="built_in">setColor</span>(sib, black);</span><br><span class="line">                    <span class="built_in">setColor</span>(<span class="built_in">parent</span>(pNode), red);</span><br><span class="line">                    <span class="built_in">r_rotation</span>(<span class="built_in">parent</span>(pNode));</span><br><span class="line">                    sib = <span class="built_in">brother</span>(pNode);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">getColor</span>(<span class="built_in">left</span>(sib)) == black &amp;&amp; <span class="built_in">getColor</span>(<span class="built_in">right</span>(sib)) == black) &#123;</span><br><span class="line">                    <span class="built_in">setColor</span>(sib, red);</span><br><span class="line">                    pNode = <span class="built_in">parent</span>(pNode);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 情况 3  远侄子是 黑</span></span><br><span class="line">                    <span class="keyword">if</span> (<span class="built_in">getColor</span>(<span class="built_in">left</span>(sib)) == black) &#123;</span><br><span class="line">                        <span class="built_in">setColor</span>(sib, red);</span><br><span class="line">                        <span class="built_in">setColor</span>(<span class="built_in">right</span>(sib), black);</span><br><span class="line">                        <span class="built_in">l_rotation</span>(sib);</span><br><span class="line">                        sib = <span class="built_in">brother</span>(pNode);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 情况 4</span></span><br><span class="line">                    <span class="built_in">setColor</span>(sib, <span class="built_in">getColor</span>(<span class="built_in">parent</span>(pNode)));</span><br><span class="line">                    <span class="built_in">setColor</span>(<span class="built_in">parent</span>(pNode), black);</span><br><span class="line">                    <span class="built_in">setColor</span>(<span class="built_in">left</span>(sib), black);</span><br><span class="line">                    <span class="built_in">r_rotation</span>(<span class="built_in">parent</span>(pNode));</span><br><span class="line"></span><br><span class="line">                    pNode = root;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 当遇到一个红色节点，把红色节点染黑即可</span></span><br><span class="line">        pNode-&gt;color = black;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">remove</span><span class="params">(K key)</span> </span>&#123;</span><br><span class="line">        TreeNode *current = <span class="built_in">findTree</span>(key);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (current == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (current-&gt;left != <span class="literal">NULL</span> &amp;&amp; current-&gt;right != <span class="literal">NULL</span>) &#123;</span><br><span class="line">            TreeNode *successor = current-&gt;<span class="built_in">successor</span>();</span><br><span class="line">            current-&gt;key = successor-&gt;key;</span><br><span class="line">            current-&gt;value = successor-&gt;value;</span><br><span class="line">            current = successor;</span><br><span class="line">        &#125;</span><br><span class="line">        TreeNode *replace = current-&gt;left ? current-&gt;left : current-&gt;right;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (replace != <span class="literal">NULL</span>) &#123;</span><br><span class="line">            <span class="comment">// 父亲  , current-&gt;parent 会不会有空的情况？</span></span><br><span class="line">            <span class="keyword">if</span> (current-&gt;parent == <span class="literal">NULL</span>) &#123; <span class="comment">// 根节点</span></span><br><span class="line">                root = replace;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (current-&gt;parent-&gt;left == current) &#123;</span><br><span class="line">                current-&gt;parent-&gt;left = replace;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                current-&gt;parent-&gt;right = replace;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            replace-&gt;parent = current-&gt;parent;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (current-&gt;color == black) &#123;</span><br><span class="line">                <span class="built_in">solveLostBlack</span>(replace);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="built_in">delete</span> (current);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (current-&gt;parent == <span class="literal">NULL</span>) &#123; <span class="comment">// 删除根节点的情况</span></span><br><span class="line">            <span class="built_in">delete</span> (root);</span><br><span class="line">            root = <span class="literal">NULL</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 为什么不先移除，是因为在修正的时候需要去获取叔叔和侄子节点来分情况判断</span></span><br><span class="line">            <span class="keyword">if</span> (current-&gt;color == black) &#123;</span><br><span class="line">                <span class="built_in">solveLostBlack</span>(current);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 再来移除</span></span><br><span class="line">            <span class="keyword">if</span> (current-&gt;parent) &#123;</span><br><span class="line">                <span class="keyword">if</span> (current-&gt;parent-&gt;left == current) &#123;</span><br><span class="line">                    current-&gt;parent-&gt;left = <span class="literal">NULL</span>;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    current-&gt;parent-&gt;right = <span class="literal">NULL</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">delete</span> (current);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        count--;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">size</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isEmpty</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> count == <span class="number">0</span>;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">levelTraverse</span><span class="params">(<span class="type">void</span>(*log)(K, V, <span class="type">bool</span>))</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="literal">NULL</span>)</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">        TreeNode *node = root;</span><br><span class="line">        queue&lt;TreeNode *&gt; nodes;</span><br><span class="line">        nodes.<span class="built_in">push</span>(node);</span><br><span class="line">        <span class="keyword">while</span> (!nodes.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">            node = nodes.<span class="built_in">front</span>();</span><br><span class="line">            <span class="built_in">log</span>(node-&gt;key, node-&gt;value, node-&gt;color);</span><br><span class="line">            nodes.<span class="built_in">pop</span>();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (node-&gt;left) &#123;</span><br><span class="line">                nodes.<span class="built_in">push</span>(node-&gt;left);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (node-&gt;right) &#123;</span><br><span class="line">                nodes.<span class="built_in">push</span>(node-&gt;right);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">K</span>, <span class="keyword">class</span> <span class="title class_">V</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">map</span>&lt;K, V&gt;::TreeNode &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    TreeNode *left;</span><br><span class="line">    TreeNode *right;</span><br><span class="line">    TreeNode *parent;</span><br><span class="line">    K key;</span><br><span class="line">    V value;</span><br><span class="line">    <span class="comment">// 颜色</span></span><br><span class="line">    rb_color color;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">TreeNode</span>(TreeNode *left, TreeNode *right, TreeNode *parent, K key, V value, rb_color color)</span><br><span class="line">            : <span class="built_in">left</span>(left), <span class="built_in">right</span>(right), <span class="built_in">parent</span>(parent), <span class="built_in">key</span>(key), <span class="built_in">value</span>(value), <span class="built_in">color</span>(color) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 找到前驱</span></span><br><span class="line">    <span class="function">TreeNode *<span class="title">precursor</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (left) &#123;</span><br><span class="line">            TreeNode *node = left;</span><br><span class="line">            <span class="keyword">while</span> (node-&gt;right) &#123;</span><br><span class="line">                node = node-&gt;right;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> node;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (right) &#123; <span class="comment">// 左子树为NULL，右子树不为NULL</span></span><br><span class="line">                <span class="keyword">return</span> parent;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123; <span class="comment">// 左右子树都为 NULL 的情况</span></span><br><span class="line">                <span class="keyword">if</span> (parent-&gt;right = <span class="keyword">this</span>) &#123;</span><br><span class="line">                    <span class="keyword">return</span> parent;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    TreeNode *myp = <span class="keyword">this</span>;</span><br><span class="line">                    <span class="keyword">while</span> (myp) &#123;</span><br><span class="line">                        <span class="keyword">if</span> (myp-&gt;parent-&gt;left = myp)<span class="comment">// 在左子树</span></span><br><span class="line">                            myp = myp-&gt;parent;</span><br><span class="line">                        <span class="keyword">else</span> &#123; <span class="comment">// 在根节点的右子树中</span></span><br><span class="line">                            <span class="keyword">return</span> myp-&gt;parent;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">return</span> myp;</span><br><span class="line"></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 找后驱</span></span><br><span class="line">    <span class="function">TreeNode *<span class="title">successor</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (right) &#123;</span><br><span class="line">            TreeNode *node = right;</span><br><span class="line">            <span class="keyword">while</span> (node-&gt;left) &#123;</span><br><span class="line">                node = node-&gt;left;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> node;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (left) &#123; <span class="comment">// 左子树不为NULL，右子树为NULL</span></span><br><span class="line">                <span class="keyword">return</span> parent;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123; <span class="comment">// 左右子树都为 NULL 的情况</span></span><br><span class="line">                <span class="keyword">if</span> (parent-&gt;left = <span class="keyword">this</span>) &#123;</span><br><span class="line">                    <span class="keyword">return</span> parent;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    TreeNode *myp = <span class="keyword">this</span>;</span><br><span class="line">                    <span class="keyword">while</span> (myp) &#123;</span><br><span class="line">                        <span class="keyword">if</span> (myp-&gt;parent-&gt;right = myp)<span class="comment">// 在右子树</span></span><br><span class="line">                            myp = myp-&gt;parent;</span><br><span class="line">                        <span class="keyword">else</span> &#123; <span class="comment">// 在根节点的左子树中</span></span><br><span class="line">                            <span class="keyword">return</span> myp-&gt;parent;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">return</span> myp;</span><br><span class="line"></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">K</span>, <span class="keyword">class</span> <span class="title class_">V</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">map</span>&lt;K, V&gt;::iterator &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    TreeNode *node;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">iterator</span>(TreeNode *node) : <span class="built_in">node</span>(node) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 找前驱</span></span><br><span class="line">    iterator &amp;<span class="keyword">operator</span>--() &#123;</span><br><span class="line">        node = node-&gt;<span class="built_in">precursor</span>();</span><br><span class="line">        <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 找后继</span></span><br><span class="line">    iterator &amp;<span class="keyword">operator</span>++() &#123;</span><br><span class="line">        node = node-&gt;<span class="built_in">successor</span>();</span><br><span class="line">        <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    V <span class="keyword">operator</span>*() &#123;</span><br><span class="line">        <span class="keyword">return</span> node-&gt;value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>树</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：13.AVL树</title>
    <url>/38%E3%80%8139.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-AVL%E6%A0%91/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure38avl">NDKPractice项目的datastructure38avl</a></strong></p>
<h1 id="1-AVL树的定义："><a href="#1-AVL树的定义：" class="headerlink" title="1. AVL树的定义："></a>1. AVL树的定义：</h1><p>定义：<code>左右两个子树高度不超过1</code></p>
<p>也称为<code>平衡二叉搜索树</code>,意义：</p>
<ol>
<li>普通二叉搜索树会出现极度不平衡的情况，复杂度有可能退化成O(n)</li>
<li>平衡二叉搜索树可确保复杂度在O(logn)</li>
</ol>
<h1 id="2-旋转操作"><a href="#2-旋转操作" class="headerlink" title="2.旋转操作"></a>2.旋转操作</h1><p>[3,2,1,4,5,6,7,10,9,8]</p>
<h2 id="2-1-左旋"><a href="#2-1-左旋" class="headerlink" title="2.1 左旋"></a>2.1 左旋</h2><p><img src="/images/38.左旋.png" alt=""></p>
<h2 id="2-2-右旋"><a href="#2-2-右旋" class="headerlink" title="2.2 右旋"></a>2.2 右旋</h2><p><img src="/images/38.右旋.png" alt=""></p>
<h2 id="2-3-先左旋再右旋"><a href="#2-3-先左旋再右旋" class="headerlink" title="2.3 先左旋再右旋"></a>2.3 先左旋再右旋</h2><p><img src="/images/38.先左旋再右旋.png" alt=""></p>
<h2 id="2-4-先右旋再左旋"><a href="#2-4-先右旋再左旋" class="headerlink" title="2.4 先右旋再左旋"></a>2.4 先右旋再左旋</h2><p><img src="/images/38.先右旋再左旋.png" alt=""></p>
<h1 id="3-移除"><a href="#3-移除" class="headerlink" title="3.移除"></a>3.移除</h1><p><code>比较复杂： 1. 当左右不为空，最大或者最小值会作为后继，要确保在删除最大或者最小值的时候要更新节点的高度</code></p>
<p>看项目工程代码的 <code>removeNode</code> 方法</p>
<h1 id="4-具体代码"><a href="#4-具体代码" class="headerlink" title="4. 具体代码"></a>4. 具体代码</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// c++ map multiMap 红黑树</span></span><br><span class="line"><span class="comment">// java Map 和 c++ Map 比较</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">K</span>, <span class="keyword">class</span> <span class="title class_">V</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">TreeNode</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    TreeNode&lt;K,V&gt; *left = <span class="literal">NULL</span>;</span><br><span class="line">    TreeNode&lt;K,V&gt; *right = <span class="literal">NULL</span>;</span><br><span class="line">    K key = <span class="literal">NULL</span>;</span><br><span class="line">    V value = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="type">int</span> height = <span class="number">1</span>; <span class="comment">//自己算上，所以为1</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">TreeNode</span>(TreeNode&lt;K,V&gt; *pNode):<span class="built_in">height</span>(pNode-&gt;height)&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;left = pNode-&gt;left;</span><br><span class="line">        <span class="keyword">this</span>-&gt;right = pNode-&gt;right;</span><br><span class="line">        <span class="keyword">this</span>-&gt;key = pNode-&gt;key;</span><br><span class="line">        <span class="keyword">this</span>-&gt;value = pNode-&gt;value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">TreeNode</span>(K key, V value):<span class="built_in">key</span>(key),<span class="built_in">value</span>(value),<span class="built_in">height</span>(<span class="number">1</span>)&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 二叉搜索树</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">K</span>, <span class="keyword">class</span> <span class="title class_">V</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">AVL</span> &#123;</span><br><span class="line">    <span class="type">int</span> count = <span class="number">0</span>; <span class="comment">// 总个数</span></span><br><span class="line">    TreeNode&lt;K,V&gt; *root = <span class="literal">NULL</span>; <span class="comment">// 根节点</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">AVL</span>()&#123;&#125;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">AVL</span>()&#123;</span><br><span class="line">        <span class="built_in">deleteAllNode</span>(root);</span><br><span class="line">        root = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 新增</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">put</span><span class="params">(K key,V value)</span></span>&#123;</span><br><span class="line">        root = <span class="built_in">addNode</span>(root,key,value);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取</span></span><br><span class="line">    <span class="function">V <span class="title">get</span><span class="params">(K key)</span></span>&#123;</span><br><span class="line">        TreeNode&lt;K,V&gt; *node = root;</span><br><span class="line">        <span class="keyword">while</span> (node)&#123;</span><br><span class="line">            <span class="keyword">if</span>(node-&gt;key == key)</span><br><span class="line">                <span class="keyword">return</span> node-&gt;value;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(node-&gt;key &gt; key)</span><br><span class="line">                node = node-&gt;left;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(node-&gt;key &lt; key)</span><br><span class="line">                node = node-&gt;right;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">size</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 是否包含</span></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">contains</span><span class="params">(K key)</span></span>&#123;</span><br><span class="line">        TreeNode&lt;K,V&gt; *node = root;</span><br><span class="line">        <span class="keyword">while</span> (node)&#123;</span><br><span class="line">            <span class="keyword">if</span>(node-&gt;key == key)</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(node-&gt;key &gt; key)</span><br><span class="line">                node = node-&gt;left;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(node-&gt;key &lt; key)</span><br><span class="line">                node = node-&gt;right;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 移除</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">remove</span><span class="params">(K key)</span></span>&#123;</span><br><span class="line">        root = <span class="built_in">removeNode</span>(root,key);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 中序遍历 就是相当于从小到大的升序遍历了</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">infixOrderTraverse</span><span class="params">(<span class="type">void</span>(*log)(K,V))</span></span>&#123;</span><br><span class="line">        <span class="built_in">infixOrderTraverse</span>(root,log);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 层序排列</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">levelOrderTraverse</span><span class="params">(<span class="type">void</span>(*log)(K,V))</span></span>&#123;</span><br><span class="line">        <span class="built_in">levelOrderTraverse</span>(root,log);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getHeight</span><span class="params">(TreeNode&lt;K,V&gt; *pNode)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> pNode ? pNode-&gt;height : <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 右旋</span></span><br><span class="line">    <span class="function">TreeNode&lt;K,V&gt;* <span class="title">r_rotation</span><span class="params">(TreeNode&lt;K,V&gt; *pNode)</span></span>&#123;</span><br><span class="line">        TreeNode&lt;K,V&gt; *left = pNode-&gt;left;</span><br><span class="line">        TreeNode&lt;K,V&gt; *right = left-&gt;right;</span><br><span class="line"></span><br><span class="line">        left-&gt;right = pNode;</span><br><span class="line">        pNode-&gt;left = right;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 更新高度</span></span><br><span class="line">        pNode-&gt;height = <span class="built_in">max</span>(<span class="built_in">getHeight</span>(pNode-&gt;left),<span class="built_in">getHeight</span>(pNode-&gt;right))<span class="number">+1</span>;</span><br><span class="line">        left-&gt;height = <span class="built_in">max</span>(<span class="built_in">getHeight</span>(left-&gt;left),<span class="built_in">getHeight</span>(left-&gt;right))<span class="number">+1</span>;</span><br><span class="line">        <span class="keyword">return</span> left;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 左旋</span></span><br><span class="line">    <span class="function">TreeNode&lt;K,V&gt;* <span class="title">l_rotation</span><span class="params">(TreeNode&lt;K,V&gt; *pNode)</span></span>&#123;</span><br><span class="line">        TreeNode&lt;K,V&gt; *right = pNode-&gt;right;</span><br><span class="line">        TreeNode&lt;K,V&gt; *left = right-&gt;left;</span><br><span class="line"></span><br><span class="line">        right-&gt;left = pNode;</span><br><span class="line">        pNode-&gt;right = left;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 更新高度</span></span><br><span class="line">        pNode-&gt;height = <span class="built_in">max</span>(<span class="built_in">getHeight</span>(pNode-&gt;left),<span class="built_in">getHeight</span>(pNode-&gt;right))<span class="number">+1</span>;</span><br><span class="line">        right-&gt;height = <span class="built_in">max</span>(<span class="built_in">getHeight</span>(right-&gt;left),<span class="built_in">getHeight</span>(right-&gt;right))<span class="number">+1</span>;</span><br><span class="line">        <span class="keyword">return</span> right;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 先左旋再右旋</span></span><br><span class="line">    <span class="function">TreeNode&lt;K,V&gt;* <span class="title">l_r_rotation</span><span class="params">(TreeNode&lt;K,V&gt; *pNode)</span></span>&#123;</span><br><span class="line">        pNode-&gt;left = <span class="built_in">l_rotation</span>(pNode-&gt;left);</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">r_rotation</span>(pNode);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 先右旋再左旋</span></span><br><span class="line">    <span class="function">TreeNode&lt;K,V&gt;* <span class="title">r_l_rotation</span><span class="params">(TreeNode&lt;K,V&gt; *pNode)</span></span>&#123;</span><br><span class="line">        pNode-&gt;right = <span class="built_in">r_rotation</span>(pNode-&gt;right);</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">l_rotation</span>(pNode);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 删除所有的数据</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">deleteAllNode</span><span class="params">(TreeNode&lt;K,V&gt; *pNode)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(pNode-&gt;left)</span><br><span class="line">            <span class="built_in">deleteAllNode</span>(pNode-&gt;left);</span><br><span class="line">        <span class="keyword">if</span>(pNode-&gt;right)</span><br><span class="line">            <span class="built_in">deleteAllNode</span>(pNode-&gt;right);</span><br><span class="line">        <span class="built_in">delete</span>(pNode);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode&lt;K,V&gt; *<span class="title">addNode</span><span class="params">(TreeNode&lt;K,V&gt; *pNode,K key,V value)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(!pNode)&#123;</span><br><span class="line">            count++;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">TreeNode</span>&lt;K,V&gt;(key,value);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(pNode-&gt;value &gt; key)&#123;</span><br><span class="line">            pNode-&gt;left = <span class="built_in">addNode</span>(pNode-&gt;left,key,value);</span><br><span class="line">            <span class="keyword">if</span>(<span class="built_in">getHeight</span>(pNode-&gt;left) - <span class="built_in">getHeight</span>(pNode-&gt;right) == <span class="number">2</span>)&#123;</span><br><span class="line">                <span class="comment">// 调整</span></span><br><span class="line">                <span class="keyword">if</span>(<span class="built_in">getHeight</span>(pNode-&gt;left-&gt;right) &gt; <span class="built_in">getHeight</span>(pNode-&gt;left-&gt;left))&#123;</span><br><span class="line">                    <span class="comment">// 先左旋再右旋</span></span><br><span class="line">                    pNode = <span class="built_in">l_r_rotation</span>(pNode);</span><br><span class="line">                &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                    pNode = <span class="built_in">r_rotation</span>(pNode);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pNode-&gt;value &lt; key)&#123;</span><br><span class="line">            pNode-&gt;right = <span class="built_in">addNode</span>(pNode-&gt;right,key,value);</span><br><span class="line">            <span class="keyword">if</span>(<span class="built_in">getHeight</span>(pNode-&gt;right) - <span class="built_in">getHeight</span>(pNode-&gt;left) == <span class="number">2</span>)&#123;</span><br><span class="line">                <span class="comment">// 调整</span></span><br><span class="line">                <span class="keyword">if</span>(<span class="built_in">getHeight</span>(pNode-&gt;right-&gt;left)&gt;<span class="built_in">getHeight</span>(pNode-&gt;right-&gt;right))&#123;</span><br><span class="line">                    <span class="comment">// 先右旋再左旋</span></span><br><span class="line">                    pNode = <span class="built_in">r_l_rotation</span>(pNode);</span><br><span class="line">                &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                    pNode = <span class="built_in">l_rotation</span>(pNode);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            pNode-&gt;value = value;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 更新二叉树高度</span></span><br><span class="line">        pNode-&gt;height = <span class="built_in">max</span>(<span class="built_in">getHeight</span>(pNode-&gt;left),<span class="built_in">getHeight</span>(pNode-&gt;right))<span class="number">+1</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> pNode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode&lt;K,V&gt; *<span class="title">removeNode</span><span class="params">(TreeNode&lt;K,V&gt; *pNode,K key)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(!pNode)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(pNode-&gt;key &gt; key)&#123;</span><br><span class="line">            pNode-&gt;left = <span class="built_in">removeNode</span>(pNode-&gt;left,key);</span><br><span class="line">            <span class="keyword">if</span>(<span class="built_in">getHeight</span>(pNode-&gt;right) - <span class="built_in">getHeight</span>(pNode-&gt;left) == <span class="number">2</span>)&#123;</span><br><span class="line">                <span class="comment">// 调整</span></span><br><span class="line">                <span class="keyword">if</span>(<span class="built_in">getHeight</span>(pNode-&gt;right-&gt;left)&gt;<span class="built_in">getHeight</span>(pNode-&gt;right-&gt;right))&#123;</span><br><span class="line">                    <span class="comment">// 先右旋再左旋</span></span><br><span class="line">                    pNode = <span class="built_in">r_l_rotation</span>(pNode);</span><br><span class="line">                &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                    pNode = <span class="built_in">l_rotation</span>(pNode);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(pNode-&gt;key &lt; key)&#123;</span><br><span class="line">            pNode-&gt;right = <span class="built_in">removeNode</span>(pNode-&gt;right,key);</span><br><span class="line">            <span class="keyword">if</span>(<span class="built_in">getHeight</span>(pNode-&gt;left) - <span class="built_in">getHeight</span>(pNode-&gt;right) == <span class="number">2</span>)&#123;</span><br><span class="line">                <span class="comment">// 调整</span></span><br><span class="line">                <span class="keyword">if</span>(<span class="built_in">getHeight</span>(pNode-&gt;left-&gt;right) &gt; <span class="built_in">getHeight</span>(pNode-&gt;left-&gt;left))&#123;</span><br><span class="line">                    <span class="comment">// 先左旋再右旋</span></span><br><span class="line">                    pNode = <span class="built_in">l_r_rotation</span>(pNode);</span><br><span class="line">                &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                    pNode = <span class="built_in">r_rotation</span>(pNode);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123; <span class="comment">// 相等找到了</span></span><br><span class="line">            count --;</span><br><span class="line">            <span class="keyword">if</span>(pNode-&gt;left == <span class="literal">NULL</span> &amp;&amp; pNode-&gt;right == <span class="literal">NULL</span>)&#123;</span><br><span class="line">                <span class="built_in">delete</span>(pNode);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">            &#125;<span class="keyword">else</span> <span class="keyword">if</span>(pNode-&gt;left == <span class="literal">NULL</span>)&#123;</span><br><span class="line">                TreeNode&lt;K, V&gt; *right = pNode-&gt;right;</span><br><span class="line">                <span class="built_in">delete</span> (pNode);</span><br><span class="line">                <span class="keyword">return</span> right;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pNode-&gt;right == <span class="literal">NULL</span>) &#123;</span><br><span class="line">                TreeNode&lt;K, V&gt; *left = pNode-&gt;left;</span><br><span class="line">                <span class="built_in">delete</span> (pNode);</span><br><span class="line">                <span class="keyword">return</span> left;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 左右子树都不为 NULL</span></span><br><span class="line">                <span class="keyword">if</span>(<span class="built_in">getHeight</span>(pNode-&gt;left) &gt; <span class="built_in">getHeight</span>(pNode-&gt;right))&#123;</span><br><span class="line">                    <span class="comment">// 左右两子树都不为空（把左子树的最大值作为根，或者右子树的最小值作为根），因为下面会删除这个节点所以需要重新赋值创建</span></span><br><span class="line">                    TreeNode&lt;K, V&gt; *max = <span class="built_in">maximum</span>(pNode-&gt;left);</span><br><span class="line">                    TreeNode&lt;K, V&gt; *successor = <span class="keyword">new</span> <span class="built_in">TreeNode</span>&lt;K, V&gt;(max);</span><br><span class="line">                    <span class="comment">// 删除左边最大的node，并更新高度</span></span><br><span class="line">                    successor-&gt;left = <span class="built_in">removeNode</span>(pNode-&gt;left,max-&gt;key);</span><br><span class="line">                    count++;</span><br><span class="line">                    successor-&gt;right = pNode-&gt;right;</span><br><span class="line">                    <span class="built_in">delete</span> (pNode);</span><br><span class="line">                    pNode = successor;</span><br><span class="line">                &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                    <span class="comment">// 左右两子树都不为空（把左子树的最大值作为根，或者右子树的最小值作为根），因为下面会删除这个节点所以需要重新赋值创建</span></span><br><span class="line">                    TreeNode&lt;K, V&gt; *min = <span class="built_in">minimum</span>(pNode-&gt;right);</span><br><span class="line">                    TreeNode&lt;K, V&gt; *successor = <span class="keyword">new</span> <span class="built_in">TreeNode</span>&lt;K, V&gt;(min);</span><br><span class="line">                    <span class="comment">// 删除右边最小的node，并更新高度</span></span><br><span class="line">                    successor-&gt;right = <span class="built_in">removeNode</span>(pNode-&gt;right,min-&gt;key);</span><br><span class="line">                    count++;</span><br><span class="line">                    successor-&gt;left = pNode-&gt;left;</span><br><span class="line">                    <span class="built_in">delete</span> (pNode);</span><br><span class="line">                    pNode = successor;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        pNode-&gt;height = <span class="built_in">max</span>(<span class="built_in">getHeight</span>(pNode-&gt;left),<span class="built_in">getHeight</span>(pNode-&gt;right))<span class="number">+1</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> pNode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function">TreeNode&lt;K, V&gt; *<span class="title">deleteMax</span><span class="params">(TreeNode&lt;K, V&gt; *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (pNode-&gt;right == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            TreeNode&lt;K, V&gt; *left = pNode-&gt;left;</span><br><span class="line">            <span class="built_in">delete</span> (pNode);</span><br><span class="line">            count--;</span><br><span class="line">            <span class="keyword">return</span> left;</span><br><span class="line">        &#125;</span><br><span class="line">        pNode-&gt;right = <span class="built_in">deleteMax</span>(pNode-&gt;right);</span><br><span class="line">        <span class="keyword">return</span> pNode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 查找当前树的左边的最大值 （右边的最小值怎么找？）</span></span><br><span class="line">    <span class="function">TreeNode&lt;K, V&gt; *<span class="title">maximum</span><span class="params">(TreeNode&lt;K, V&gt; *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 不断的往右边找，直到找到右子树为空节点</span></span><br><span class="line">        <span class="keyword">if</span> (pNode-&gt;right == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> pNode;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">maximum</span>(pNode-&gt;right);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 查找当前树的右边的最小值 （右边的最小值怎么找？）</span></span><br><span class="line">    <span class="function">TreeNode&lt;K, V&gt; *<span class="title">minimum</span><span class="params">(TreeNode&lt;K, V&gt; *pNode)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 不断的往右边找，直到找到右子树为空节点</span></span><br><span class="line">        <span class="keyword">if</span> (pNode-&gt;left == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> pNode;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">maximum</span>(pNode-&gt;left);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">infixOrderTraverse</span><span class="params">(TreeNode&lt;K,V&gt; *pNode,<span class="type">void</span>(*log)(K,V))</span></span>&#123;</span><br><span class="line">        <span class="comment">// 所有递归都应有递归到底的情况</span></span><br><span class="line">        <span class="keyword">if</span> (pNode == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 先左孩子</span></span><br><span class="line">        <span class="built_in">infixOrderTraverse</span>(pNode-&gt;left,log);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 再根节点</span></span><br><span class="line">        <span class="built_in">log</span>(pNode-&gt;key,pNode-&gt;value);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 再右孩子</span></span><br><span class="line">        <span class="built_in">infixOrderTraverse</span>(pNode-&gt;right,log);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">levelOrderTraverse</span><span class="params">(TreeNode&lt;K,V&gt; *pNode,<span class="type">void</span>(*log)(K,V))</span></span>&#123;</span><br><span class="line">        queue&lt;TreeNode&lt;K,V&gt; *&gt; que;</span><br><span class="line">        que.<span class="built_in">push</span>(pNode);</span><br><span class="line">        <span class="keyword">while</span> (!que.<span class="built_in">empty</span>())&#123;</span><br><span class="line">            TreeNode&lt;K,V&gt; *node = que.<span class="built_in">front</span>();</span><br><span class="line">            <span class="built_in">log</span>(node-&gt;key,node-&gt;value);</span><br><span class="line">            que.<span class="built_in">pop</span>(); <span class="comment">// 弹出</span></span><br><span class="line">            <span class="keyword">if</span>(node-&gt;left)</span><br><span class="line">                que.<span class="built_in">push</span>(node-&gt;left);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span>(node-&gt;right)</span><br><span class="line">                que.<span class="built_in">push</span>(node-&gt;right);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>树</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：11.二叉树序列化、优先级队列和堆排序</title>
    <url>/36.%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E4%BA%8C%E5%8F%89%E6%A0%91%E5%BA%8F%E5%88%97%E5%8C%96%E3%80%81%E4%BC%98%E5%85%88%E7%BA%A7%E9%98%9F%E5%88%97%E5%92%8C%E5%A0%86%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure36heapsorting">NDKPractice项目的datastructure36heapsorting</a></strong></p>
<h1 id="1-二叉树的序列化和反序列化"><a href="#1-二叉树的序列化和反序列化" class="headerlink" title="1. 二叉树的序列化和反序列化"></a>1. 二叉树的序列化和反序列化</h1><p><img src="/images/36.二叉树的序列化和反序列化.png" alt=""></p>
<h2 id="1-1-序列化"><a href="#1-1-序列化" class="headerlink" title="1.1 序列化"></a>1.1 序列化</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 序列化</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">serializeTree</span><span class="params">(TreeNode&lt;<span class="type">char</span>&gt; *pNode, string &amp;str)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!pNode)&#123;</span><br><span class="line">        str.<span class="built_in">append</span>(<span class="string">&quot;#&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 先添加根节点</span></span><br><span class="line">    str.<span class="built_in">append</span>(<span class="built_in">string</span>(<span class="number">1</span>,pNode-&gt;data));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 再左节点</span></span><br><span class="line">    <span class="built_in">serializeTree</span>(pNode-&gt;left,str);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 再右节点</span></span><br><span class="line">    <span class="built_in">serializeTree</span>(pNode-&gt;right,str);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="1-2-反序列化"><a href="#1-2-反序列化" class="headerlink" title="1.2 反序列化"></a>1.2 反序列化</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 反序列化 (ABD##E##C#F##)</span></span><br><span class="line"><span class="comment"> * 注意：必须要传 2 级指针，如果是一级指针这只是传过来的数组，++的时候是不会对数组有影响的</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function">TreeNode&lt;<span class="type">char</span>&gt; *<span class="title">deserializeTree</span><span class="params">(<span class="type">char</span> **str)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(**str == <span class="string">&#x27;#&#x27;</span>)&#123;</span><br><span class="line">        *str += <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    TreeNode&lt;<span class="type">char</span>&gt; *node = <span class="keyword">new</span> <span class="built_in">TreeNode</span>&lt;<span class="type">char</span>&gt;(**str);</span><br><span class="line">    *str += <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 解析左边的</span></span><br><span class="line">    node-&gt;left = <span class="built_in">deserializeTree</span>(str);</span><br><span class="line">    <span class="comment">// 解析右边的</span></span><br><span class="line">    node-&gt;right = <span class="built_in">deserializeTree</span>(str);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-优先级队列"><a href="#2-优先级队列" class="headerlink" title="2. 优先级队列"></a>2. 优先级队列</h1><p>数组转二叉树规律(前提：<code>index 从 1 开始</code>)：</p>
<p>left(左节点) = 父节点角标 <em> 2<br>right（右节点） = 父节点角标 </em> 2 + 1</p>
<ul>
<li><code>最大堆</code>：父节点永远比子节点大。</li>
<li><code>最小堆</code>：父节点永远比子节点小。</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">E</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">PriorityQueue</span> &#123;</span><br><span class="line">    <span class="type">int</span> count = <span class="number">0</span>; <span class="comment">// 数组的大小，不够要扩容</span></span><br><span class="line">    <span class="type">int</span> index = <span class="number">0</span>; <span class="comment">// 当前数组的角标位置</span></span><br><span class="line">    E *array = <span class="literal">NULL</span>; <span class="comment">// 数据数组</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 向上调整为大根堆</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">shiftUp</span><span class="params">(<span class="type">int</span> index)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(index &gt; <span class="number">1</span> &amp;&amp; array[index] &gt; array[index/<span class="number">2</span>])&#123;</span><br><span class="line">            <span class="built_in">swap</span>(array[index],array[index/<span class="number">2</span>]);</span><br><span class="line">            <span class="built_in">shiftUp</span>(index/<span class="number">2</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 向下调整为大根堆</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">shiftDown</span><span class="params">(<span class="type">int</span> k)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (k * <span class="number">2</span> &lt;= index)&#123; <span class="comment">// 到底的情况</span></span><br><span class="line">            <span class="type">int</span> max = k*<span class="number">2</span>; <span class="comment">// 先假设左孩子是最大的</span></span><br><span class="line">            <span class="keyword">if</span>(max + <span class="number">1</span> &lt;= index &amp;&amp; array[max] &lt; array[max + <span class="number">1</span>])&#123;</span><br><span class="line">                max = max + <span class="number">1</span>; <span class="comment">// 右孩子比左孩子大</span></span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 因为没弹出之前就是大根堆，所以如果比子孩子最大的都还要大就不用排序了</span></span><br><span class="line">            <span class="keyword">if</span>(array[k] &gt; array[max])</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 交换，最大的往上冒</span></span><br><span class="line">            <span class="built_in">swap</span>(array[k],array[max]);</span><br><span class="line"></span><br><span class="line">            k = max;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">PriorityQueue</span>(<span class="type">int</span> count) : <span class="built_in">count</span>(count) &#123;</span><br><span class="line">        array = <span class="keyword">new</span> E[count];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isEmpty</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> index == <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">E <span class="title">pop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        E max = array[<span class="number">1</span>];</span><br><span class="line">        <span class="comment">// 把最后一个赋值到第一个</span></span><br><span class="line">        array[<span class="number">1</span>] = array[index];</span><br><span class="line">        array[index] = <span class="literal">NULL</span>;</span><br><span class="line">        index --;</span><br><span class="line">        <span class="built_in">shiftDown</span>(<span class="number">1</span>);</span><br><span class="line">        <span class="keyword">return</span> max;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push</span><span class="params">(E e)</span></span>&#123;</span><br><span class="line">        array[index + <span class="number">1</span>] = e;</span><br><span class="line">        index ++;</span><br><span class="line">        <span class="comment">// 不断的调整堆</span></span><br><span class="line">        <span class="built_in">shiftUp</span>(index);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h1 id="3-堆排序-升序采用大根堆，降序采用小根堆"><a href="#3-堆排序-升序采用大根堆，降序采用小根堆" class="headerlink" title="3. 堆排序(升序采用大根堆，降序采用小根堆)"></a>3. 堆排序(升序采用大根堆，降序采用小根堆)</h1><p>将一个数组想象成一棵完全二叉树：</p>
<p>如果把一个数组排成完全二叉树，则是下标循环时，是从上到下，<code>从左到右</code>,进行填充</p>
<ol>
<li><code>i</code>的左子节点下标：<code>2*i+1</code> ,如果越界，证明左孩子不存在</li>
<li><code>i</code>的右子节点下标：<code>2*i+2</code>，如果越界，证明右孩子不存在</li>
<li><code>i</code>的父节点下标：<code>(i-1)/2</code>,注意下标0位置求父节点，(0-1)/2 = 0</li>
<li>第一个非叶子结点 <code>arr.length/2-1</code></li>
</ol>
<p>堆就是一棵完全二叉树。</p>
<ul>
<li><code>最大堆</code>：父节点永远比子节点大。</li>
<li><p><code>最小堆</code>：父节点永远比子节点小。</p>
</li>
<li><p>思想是：<code>将待排序序列构造成一个大根堆，此时，整个序列的最大值就是堆顶的根节点。  
将其与末尾元素进行交换，此时末尾就为最大值。然后将剩余n-1个元素重新构造成一个大根堆，再将堆顶的根节点和最后一个元素n-1交换。如此反复执行，便能得到一个有序序列了</code></p>
</li>
<li><code>时间复杂度</code>：<strong>O(N&#42;$\log_2 N$)</strong></li>
<li><code>空间复杂度</code>：<strong>O(1)</strong></li>
</ul>
<p><img src="/images/堆排序.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 调整为大根堆</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">adjuastHeap</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> k, <span class="type">int</span> n)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> left = k * <span class="number">2</span> + <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span> (left &lt; n)&#123; <span class="comment">// 到底的情况</span></span><br><span class="line">        <span class="comment">// 先假设左孩子最大</span></span><br><span class="line">        <span class="type">int</span> max = left;</span><br><span class="line">        <span class="comment">// 有右孩子且右孩子大于左孩子</span></span><br><span class="line">        <span class="keyword">if</span>(max + <span class="number">1</span> &lt; n &amp;&amp; arr[max + <span class="number">1</span>] &gt; arr[max])&#123;</span><br><span class="line">            max = max + <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(arr[k] &gt; arr[max]) <span class="comment">// 左右两个孩子都比父亲小，不用调整了</span></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 将最大的和父亲进行交换</span></span><br><span class="line">        <span class="built_in">swap</span>(arr[k],arr[max]);</span><br><span class="line">        k = max; <span class="comment">// 交换后接着往下调整</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *  堆排序</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">heapSort</span><span class="params">(<span class="type">int</span> arr[],<span class="type">int</span> len)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 1. 从最后一个不是叶子节点的节点（叶结点自然不用调整），开始调整为大根堆</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = len/<span class="number">2</span><span class="number">-1</span>; i&gt;=<span class="number">0</span>; i--)&#123;</span><br><span class="line">        <span class="built_in">adjuastHeap</span>(arr,i,len);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 第一个与最后一个进行交换，最后一个值自然就是最大值了，接着把剩下的再进行大根堆排序</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = len - <span class="number">1</span>; i &gt; <span class="number">0</span>; --i) &#123;</span><br><span class="line">        <span class="built_in">swap</span>(arr[<span class="number">0</span>],arr[i]);<span class="comment">// 将最大的与末尾交换，这时末尾的就是最大值了</span></span><br><span class="line">        <span class="built_in">adjuastHeap</span>(arr,<span class="number">0</span>,i); <span class="comment">// 对 第 0 个位置进行调整。</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>排序</tag>
        <tag>树</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：16.ConcurrentHashMap分析</title>
    <url>/43.%20%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-ConcurrentHashMap%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="1-HashMap-5-个线程-put-数据会发生什么？-（阿里面试第一面）"><a href="#1-HashMap-5-个线程-put-数据会发生什么？-（阿里面试第一面）" class="headerlink" title="1. HashMap 5 个线程 put 数据会发生什么？ （阿里面试第一面）"></a>1. HashMap 5 个线程 put 数据会发生什么？ （阿里面试第一面）</h1><p><img src="/images/43.多线程操作HashMap.png" alt=""></p>
<p><code>数据丢失</code>，<code>死循环 （扩容）</code></p>
<p>解决方案：</p>
<ol>
<li>hashTable</li>
<li>Collections.synchronizedMap()</li>
<li>ConcurrentHashMap</li>
</ol>
<p>前两种是锁着整个方法，分段锁 Lock , 分段锁 （synchronized，CAS）<br>先来分析一下 java的多线程内存模型</p>
<h1 id="2-Java-多线程的内存模型"><a href="#2-Java-多线程的内存模型" class="headerlink" title="2. Java 多线程的内存模型"></a>2. Java 多线程的内存模型</h1><p><img src="/images/43.java多线程的内存模型.png" alt=""></p>
<p>Java内存模型中规定了所有的变量都存储在主内存中，每条线程还有自己的工作内存，线程的工作内存中保存了该线程使用到的变量到主内存副本拷贝，线程对变量的所有操作（读取、赋值）都必须在工作内存中进行，<br>而不能直接读写主内存中的变量。不同线程之间无法直接访问对方工作内存中的变量，线程间变量值的传递均需要在主内存来完成，线程、主内存和工作内存的交互关系如上图所示。</p>
<p>现在我们来想象一下，假设线程 1 把数据读到了自己的工作内存中，在 tab 角标为 1 的链表头插入了一条新的数据，倘若这时还没来得及将新增的数据刷新到主内中。接着线程 2 就把数据读到了自己的工作内存中，在 tab 角标为 1 的链表头插入了一条新的数据。接着线程 1 把新增数据刷新到主内存中，<br>线程 2 也把数据新增数据刷新到主内存中，那么线程 2 就会覆盖线程 1 的新增数据，从而导致数据丢失的情况。这里需要注意的是，只有两个线程都是操作 tab 的同一个 index 链表才会导致数据丢失的情况，如果不是同一个 index 链表就不会有覆盖和丢失这一说。</p>
<h1 id="3-synchronized-的底层实现原理"><a href="#3-synchronized-的底层实现原理" class="headerlink" title="3. synchronized 的底层实现原理"></a>3. synchronized 的底层实现原理</h1><p><img src="/images/43.synchronized底层实现原理.png" alt=""></p>
<p>关于 HashMap 的线程不安全问题，Java 给我们提供了三种方案，第一种是 HashTable ，第二种是 Collections.synchronizedMap() ，第三种是 ConcurrentHashMap 。而第一种和第二种都是通过用 synchronized 同步方法来保证线程安全，性能上有所欠缺不推荐大家使用。<br>ConcurrentHashMap 在 JDK 1.8 之前采用的是 Segment 分段锁来实现的，而 JDK 1.8 之后则采用 synchronized 和 CAS 来实现。</p>
<p>HashTable 通过锁住整个 put 和 get 方法来实现线程安全并不是很合理，因为一个线程在 put 的时候，另外一个线程不能再 put 和 get 必须进入等待状态。同理一个线程在 get 的时候，另外一个线程也不能再 get 和 put 。<br>上面通过分析只有两个线程都是操作 tab 的同一个 index 链表才会导致数据丢失的情况，如果不是同一个 index 链表就不会有覆盖和丢失这一说。因此也没必要锁住整个方法，只需要锁住每个 tab 的 index 链即可。</p>
<p>ConcurrentHashMap 在 JDK 1.8 之前采用的是 Segment 继承自 ReentrantLock 来锁住 tab 的 index 链，而 JDK 1.8 之后则采用 synchronized 来实现</p>
<h1 id="4-ConcurrentHashMap-源码分析"><a href="#4-ConcurrentHashMap-源码分析" class="headerlink" title="4. ConcurrentHashMap 源码分析"></a>4. ConcurrentHashMap 源码分析</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// volatile 保证可见性</span></span><br><span class="line">transient <span class="keyword">volatile</span> Node&lt;K,V&gt;[] table;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 新增元素的方法</span></span><br><span class="line"><span class="function"><span class="keyword">final</span> V <span class="title">putVal</span><span class="params">(K key, V value, boolean onlyIfAbsent)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (key == null || value == null) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">NullPointerException</span>();</span><br><span class="line">        <span class="comment">// 二次 hash </span></span><br><span class="line">        <span class="type">int</span> hash = <span class="built_in">spread</span>(key.<span class="built_in">hashCode</span>());</span><br><span class="line">        <span class="type">int</span> binCount = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (Node&lt;K,V&gt;[] tab = table;;) &#123;</span><br><span class="line">            Node&lt;K,V&gt; f; <span class="type">int</span> n, i, fh;</span><br><span class="line">            <span class="comment">// 如果 tab 为空，初始化 tab</span></span><br><span class="line">            <span class="keyword">if</span> (tab == null || (n = tab.length) == <span class="number">0</span>)&#123;</span><br><span class="line">                tab = <span class="built_in">initTable</span>();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 当前 tab 的 index 链表为 null</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((f = <span class="built_in">tabAt</span>(tab, i = (n - <span class="number">1</span>) &amp; hash)) == null) &#123;</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">casTabAt</span>(tab, i, null, <span class="keyword">new</span> <span class="built_in">Node</span>&lt;K,V&gt;(hash, key, value, null)))</span><br><span class="line">                    <span class="keyword">break</span>;                   <span class="comment">// no lock when adding to empty bin</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((fh = f.hash) == MOVED)</span><br><span class="line">                tab = <span class="built_in">helpTransfer</span>(tab, f);</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                V oldVal = null;</span><br><span class="line">                <span class="comment">// 锁住当前 tab 的 index 链表（分段锁）</span></span><br><span class="line">                <span class="built_in">synchronized</span> (f) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (<span class="built_in">tabAt</span>(tab, i) == f) &#123;</span><br><span class="line">                        <span class="keyword">if</span> (fh &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                            binCount = <span class="number">1</span>;</span><br><span class="line">                            <span class="comment">// ......</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">get</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">        Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; e, p; <span class="type">int</span> n, eh; K ek;</span><br><span class="line">        <span class="type">int</span> h = <span class="built_in">spread</span>(key.<span class="built_in">hashCode</span>());</span><br><span class="line">        <span class="keyword">if</span> ((tab = table) != null &amp;&amp; (n = tab.length) &gt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">            <span class="comment">// CAS 操作</span></span><br><span class="line">            (e = <span class="built_in">tabAt</span>(tab, (n - <span class="number">1</span>) &amp; h)) != null) &#123;</span><br><span class="line">            <span class="keyword">if</span> ((eh = e.hash) == h) &#123;</span><br><span class="line">                <span class="keyword">if</span> ((ek = e.key) == key || (ek != null &amp;&amp; key.<span class="built_in">equals</span>(ek)))</span><br><span class="line">                    <span class="keyword">return</span> e.val;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (eh &lt; <span class="number">0</span>)</span><br><span class="line">                <span class="built_in">return</span> (p = e.<span class="built_in">find</span>(h, key)) != null ? p.val : null;</span><br><span class="line">            <span class="comment">// 遍历当前列表</span></span><br><span class="line">            <span class="keyword">while</span> ((e = e.next) != null) &#123;</span><br><span class="line">                <span class="keyword">if</span> (e.hash == h &amp;&amp;</span><br><span class="line">                    ((ek = e.key) == key || (ek != null &amp;&amp; key.<span class="built_in">equals</span>(ek))))</span><br><span class="line">                    <span class="keyword">return</span> e.val;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> null;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
        <category>ConcurrentHashMap</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>数据结构和算法学习</tag>
        <tag>ConcurrentHashMap</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：19.leetcode刷题-常用算法思想</title>
    <url>/47.leetcode%E5%88%B7%E9%A2%98-%E5%B8%B8%E7%94%A8%E7%AE%97%E6%B3%95%E6%80%9D%E6%83%B3/</url>
    <content><![CDATA[<h1 id="1-贪心算法思想"><a href="#1-贪心算法思想" class="headerlink" title="1. 贪心算法思想"></a>1. 贪心算法思想</h1><p>每次都选择正确的最优的解，不断反复</p>
<ul>
<li><p><a href="https://leetcode-cn.com/problems/assign-cookies/">分发饼干-455</a></p>
</li>
<li><p><a href="https://leetcode-cn.com/problems/wiggle-subsequence/">摆动序列-376</a></p>
</li>
</ul>
<h1 id="2-回溯算法思想"><a href="#2-回溯算法思想" class="headerlink" title="2. 回溯算法思想"></a>2. 回溯算法思想</h1><p>就是不断的试探，试探完就退回去重新选择，回溯  （都与递归有关系）</p>
<ul>
<li><a href="https://leetcode-cn.com/problems/subsets/">子集-78</a></li>
<li><a href="https://leetcode-cn.com/problems/subsets-ii/">子集2-90</a></li>
</ul>
<h1 id="3-分治算法思想"><a href="#3-分治算法思想" class="headerlink" title="3. 分治算法思想"></a>3. 分治算法思想</h1><p>把 n 的问题分解成 K 个较小的子问题，这些问题是相互独立，求出子问题的解进行合并，就可以得到原问题的解 （归并排序，快速排序）</p>
<ul>
<li><a href="https://leetcode-cn.com/problems/count-of-smaller-numbers-after-self/">计算右侧小于当前元素的个数-315</a></li>
</ul>
<h1 id="4-动态规划算法思想"><a href="#4-动态规划算法思想" class="headerlink" title="4. 动态规划算法思想"></a>4. 动态规划算法思想</h1><p>多阶段过程转化为一系列单阶段问题，利用各阶段之间的关系，逐个求解 ， 原问题和子问题的问题 ，但是相互依赖   dp 求解</p>
<ul>
<li><p><a href="https://leetcode-cn.com/problems/climbing-stairs/">爬楼梯-70</a></p>
</li>
<li><p><a href="https://leetcode-cn.com/problems/house-robber/">打家劫舍-198</a></p>
</li>
</ul>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>leetcode刷题</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：17.图</title>
    <url>/43.%20%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E5%9B%BE/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure43graph">NDKPractice项目的datastructure43graph</a></strong></p>
<h1 id="1-图的基本概念："><a href="#1-图的基本概念：" class="headerlink" title="1. 图的基本概念："></a>1. 图的基本概念：</h1><p>图（Graph）是由顶点的有穷非空集合和顶点之间边的集合组成，通常表示为：G（V，E），其中，G表示一个图，V是图G中顶点的集合，E是图G中边的集合。</p>
<p>图就是由 点 和 边组成。</p>
<p><code>注意：线性表中可以没有元素，称为空表。树中可以没有结点，叫做空树。但是在图中不允许没有顶点，可以没有边。</code></p>
<h1 id="2-基本术语："><a href="#2-基本术语：" class="headerlink" title="2. 基本术语："></a>2. 基本术语：</h1><ul>
<li><p>无向边：若顶点V1和V2之间的边没有方向，称这条边为无向边（Edge），用（V1，V2）来表示。</p>
</li>
<li><p>无向图（Undirected graphs）：图中任意两个顶点的边都是无向边。</p>
</li>
</ul>
<p><img src="/images/43.无向图.png" alt=""></p>
<ul>
<li><p>有向边：若从顶点V1到V2的边有方向，称这条边为有向边，也称为弧（Arc），用<V2, V1>来表示，其中V1称为弧尾（Tail），V2称为弧头（Head）。</p>
</li>
<li><p>有向图（Directed graphs）：图中任意两个顶点的边都是有向边。</p>
</li>
</ul>
<p><img src="/images/43.有向图.png" alt=""></p>
<ul>
<li><p>无向完全图：无向图中，任意两个顶点之间都存在边。</p>
</li>
<li><p>有向完全图：有向图中，任意两个顶点之间都存在方向相反的两条弧。</p>
</li>
<li><p>稀疏图；有很少条边或弧的图称为稀疏图 n*log(n)，反之称为稠密图。  相对的概念  （头发稀疏） 完全图肯定是稠密</p>
</li>
<li><p>权（Weight）：表示从图中一个顶点到另一个顶点的距离或耗费。</p>
</li>
<li><p>网：带有权重的图</p>
</li>
<li><p>度：与特定顶点相连接的边数；</p>
</li>
<li><p>出度、入度：有向图中的概念，出度表示以此顶点为起点的边的数目，入度表示以此顶点为终点的边的数目；</p>
</li>
<li><p>连通图：任意两个顶点都相互连通的图；</p>
</li>
<li><p>生成树：n个顶点，n-1条边 ，的图可以看成树 （生成树）</p>
</li>
<li><p>最小生成树：此生成树的边的权重之和是所有生成树中最小的；</p>
</li>
</ul>
<h1 id="3-图的存储结构"><a href="#3-图的存储结构" class="headerlink" title="3. 图的存储结构"></a>3. 图的存储结构</h1><h2 id="3-1-无向边存储图如下："><a href="#3-1-无向边存储图如下：" class="headerlink" title="3.1 无向边存储图如下："></a>3.1 无向边存储图如下：</h2><p><img src="/images/43.无向边存储方式.png" alt=""></p>
<h2 id="3-2-有向边存储图如下："><a href="#3-2-有向边存储图如下：" class="headerlink" title="3.2 有向边存储图如下："></a>3.2 有向边存储图如下：</h2><p><img src="/images/43.有向边存储方式.png" alt=""></p>
<h2 id="3-3-邻接表"><a href="#3-3-邻接表" class="headerlink" title="3.3  邻接表"></a>3.3  邻接表</h2><p><img src="/images/43.邻接表.png" alt=""></p>
<h2 id="3-4-十字链表"><a href="#3-4-十字链表" class="headerlink" title="3.4 十字链表"></a>3.4 十字链表</h2><p><img src="/images/43.十字链表.png" alt=""></p>
<h1 id="4-深度优先遍历和广度优先遍历"><a href="#4-深度优先遍历和广度优先遍历" class="headerlink" title="4. 深度优先遍历和广度优先遍历"></a>4. 深度优先遍历和广度优先遍历</h1><p><img src="/images/43.广度优先遍历.png" alt=""></p>
<h1 id="5-最小生成树和最短路径"><a href="#5-最小生成树和最短路径" class="headerlink" title="5. 最小生成树和最短路径"></a>5. 最小生成树和最短路径</h1><p><code>普里姆算法</code> : 每次都找最近的，与它相连的考虑进去，边修路边淘汰</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">    // 最小生成树（普里姆算法）</span><br><span class="line">    public void prim()&#123;</span><br><span class="line">        // 定义一个数组内存，当前修好村庄，lowcost = 0 代表已经修了</span><br><span class="line">        int[] lowcost = new int[vertexSize];</span><br><span class="line"></span><br><span class="line">        // 第一行的数据先放到 lowcost</span><br><span class="line">        for (int i = 0; i &lt; vertexSize; i++) &#123;</span><br><span class="line">            lowcost[i] = matrix[0][i];        // [0,1,5,max,max,max,max,max,max]</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        int sum = 0;</span><br><span class="line"></span><br><span class="line">        for (int i = 1; i &lt; vertexSize; i++) &#123;</span><br><span class="line">            int min = MAX_WEIGHT;</span><br><span class="line">            int minId = 0;</span><br><span class="line"></span><br><span class="line">            // 找最小的，之间是否连通</span><br><span class="line">            for (int j = 1; j &lt; vertexSize; j++) &#123;</span><br><span class="line">                if(lowcost[j] &lt; min &amp;&amp; lowcost[j] != 0)&#123;</span><br><span class="line">                    min = lowcost[j];</span><br><span class="line">                    minId = j;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            // min = 1, minId = 1;</span><br><span class="line"></span><br><span class="line">            Log.e(&quot;TAG&quot;,&quot;找到村庄：&quot;+vertexs[minId] + &quot;, 修路距离：&quot; + min);</span><br><span class="line">            lowcost[minId] = 0;</span><br><span class="line">            sum += min;</span><br><span class="line"></span><br><span class="line">            for (int k = 0; k &lt; vertexSize; k++) &#123;</span><br><span class="line">                // 边考虑边淘汰</span><br><span class="line">                if(matrix[minId][k] &lt; lowcost[k] &amp;&amp; lowcost[k] &gt; 0)&#123;</span><br><span class="line">                    lowcost[k] = matrix[minId][k];</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">//            for (int o = 0; o&lt; vertexSize; o++)&#123;</span><br><span class="line">//                Log.e(&quot;TAG&quot;,&quot;i = &quot;+o+&quot;,  number = &quot; + lowcost[o]); //[0,0,3,7,5,max,max,max,max]</span><br><span class="line">//            &#125;</span><br><span class="line">//            Log.e(&quot;TAG&quot;,&quot;==================================== &quot;);</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Log.e(&quot;TAG&quot;,&quot;最短路径是：&quot; + sum);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p><code>迪杰斯特拉算法</code>:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 生成最短路径（迪杰斯特拉算法）</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">dijstra</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 有没找到最短路径</span></span><br><span class="line">    <span class="type">boolean</span>[] isPath = <span class="keyword">new</span> <span class="title class_">boolean</span>[vertexSize];</span><br><span class="line">    <span class="comment">// 存放每个村庄的最短路径</span></span><br><span class="line">    <span class="type">int</span>[] shortPath = <span class="keyword">new</span> <span class="title class_">int</span>[vertexSize];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt;vertexSize; i++) &#123;</span><br><span class="line">        shortPath[i] = matrix[<span class="number">0</span>][i];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    shortPath[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">    isPath[<span class="number">0</span>] = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt; vertexSize; i++) &#123; <span class="comment">// 外循环每次找到下一个村庄最短的路径</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">minId</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">min</span> <span class="operator">=</span> MAX_WEIGHT;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">1</span>; j &lt; vertexSize; j++) &#123;</span><br><span class="line">            <span class="keyword">if</span>(shortPath[j] &lt; min &amp;&amp; !isPath[j])&#123;</span><br><span class="line">                min = shortPath[j];   <span class="comment">// 1 4</span></span><br><span class="line">                minId = j;            <span class="comment">// 1 2</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        isPath[minId] = <span class="literal">true</span>; <span class="comment">// 已经找到了最短的路径</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">k</span> <span class="operator">=</span> <span class="number">0</span>; k &lt; vertexSize; k++) &#123;</span><br><span class="line">            <span class="keyword">if</span>(!isPath[k] &amp;&amp; (matrix[minId][k] + min) &lt; shortPath[k])&#123;</span><br><span class="line">                shortPath[k] = matrix[minId][k] + min;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">k</span> <span class="operator">=</span> <span class="number">0</span>; k &lt; vertexSize; k++) &#123;</span><br><span class="line">            Log.e(<span class="string">&quot;TAG&quot;</span>, shortPath[k]+<span class="string">&quot; &quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        Log.e(<span class="string">&quot;TAG&quot;</span>, <span class="string">&quot;===================&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; vertexSize; i++) &#123;</span><br><span class="line">        Log.e(<span class="string">&quot;TAG&quot;</span>, <span class="string">&quot;顶点 0 到顶点 &quot;</span> + i + <span class="string">&quot; 的最短距离为：&quot;</span> + shortPath[i]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>图</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：01.OpenCV安装和转灰度图提升饱和度</title>
    <url>/48.OpenCV-%E5%AE%89%E8%A3%85%E5%92%8C%E8%BD%AC%E7%81%B0%E5%BA%A6%E5%9B%BE%E6%8F%90%E5%8D%87%E9%A5%B1%E5%92%8C%E5%BA%A6/</url>
    <content><![CDATA[<h1 id="1-VS-安装、配置Opencv的环境"><a href="#1-VS-安装、配置Opencv的环境" class="headerlink" title="1. VS 安装、配置Opencv的环境"></a>1. VS 安装、配置Opencv的环境</h1><p>我们是在 windows的<code>VS</code>中开发 使用功能的方式是：<code>头文件+dll文件</code>的方式来学习opencv的一些常见使用方法。</p>
<ol>
<li>下载 <a href="https://github.com/opencv/opencv/releases">opencv.exe</a> 并解压</li>
<li>生成-&gt;配置管理器-&gt;平台修改为<code>x64</code></li>
<li>配置windows环境变量：path : <code>opencv解压目录\build\x64\vc14\bin</code></li>
<li>配置头文件包含目录：<code>调试</code>-&gt;<code>xx属性</code>-&gt; <code>配置属性</code> -&gt;<code>VC++ 目录</code>-&gt; <code>包含目录</code>: <code>opencv解压目录\build\include</code></li>
<li>配置库目录：<code>调试</code>-&gt;<code>xx属性</code> -&gt; <code>配置属性</code> -&gt;<code>VC++ 目录</code>-&gt; <code>库目录</code>：<code>opencv解压目录\build\x64\vc14\lib</code></li>
<li>链接器：<code>opencv解压目录\build\x64\vc14\lib</code> 中的 <code>opencv_world440d.lib</code> 复制文件名至<br> <code>调试</code>-&gt;<code>xx属性</code> -&gt; <code>配置属性</code> -&gt;<code>VC++ 目录</code>-&gt; <code>链接器</code> -&gt; <code>输入</code> -&gt; <code>附加依赖项</code>。  <font color = red>注意：带d的适用于debug模式，不带d的使用于release模式</font></li>
<li>.dll 系统适配，如果第6步完成后报错<code>xxx.dll</code>未找到，则先把<code>opencv解压目录\build\x64\vc14\bin</code>中的dll文件<br> 复制到<code>系统盘\windows\System32</code>和<code>系统盘\windows\SysWOW54</code>,如果还是不是，则将第6步的文件换成release模式下的文件</li>
</ol>
<h1 id="c-中-uchar"><a href="#c-中-uchar" class="headerlink" title="c++ 中 uchar"></a>c++ 中 uchar</h1><p>char 是有符号的 unsigned char 是无符号的,char 整数范围为-128到127( 0x80<strong>0x7F)， 而unsigned char 整数范围为0到255( 0</strong>0xFF )<br>有时候想把整数数值限在255范围内，也用unsigned char，这个类型在嵌入式用的多,<code>所以 uchar 一般用于图片处理</code></p>
<h1 id="2-opencv方法介绍："><a href="#2-opencv方法介绍：" class="headerlink" title="2. opencv方法介绍："></a>2. opencv方法介绍：</h1><ul>
<li>Mat src = cv::imread(“图片路径”) ：读取读片，注意 windows 中路径分隔符一定得修改为：<code>/</code></li>
<li>cv::imwrite(“保存路径”,mat); 把 mat 写入文件,注意 windows 中路径分隔符一定得修改为：<code>/</code></li>
<li>cv::imshow(“test pic”,mat); 显示图片</li>
<li>int height = cv::mat.rows  // 获取 mat 的行高</li>
<li>int width = cv::mat.cols  // 获取 mat 的列宽</li>
<li>int channels= mat.channels() // 颜色通道，一个像素点几个信息 3（BGR） 4（BGRA）</li>
<li>saturate_cast<uchar>(b_w); // 大于 255 就变成 255 ，如果小于 0 就变成 0 </li>
</ul>
<h1 id="3-彩色图转灰度图及黑白图（手写）"><a href="#3-彩色图转灰度图及黑白图（手写）" class="headerlink" title="3. 彩色图转灰度图及黑白图（手写）"></a>3. 彩色图转灰度图及黑白图（手写）</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; src.rows; i++)&#123;</span><br><span class="line">	<span class="comment">// 读取当前行的首地址</span></span><br><span class="line">	uchar* start_pixels = src.<span class="built_in">ptr</span>&lt;uchar&gt;(i);</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; src.cols; j++)&#123;</span><br><span class="line">		uchar b = start_pixels[<span class="number">0</span>];</span><br><span class="line">		uchar g = start_pixels[<span class="number">1</span>];</span><br><span class="line">		uchar r = start_pixels[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 彩色转灰度</span></span><br><span class="line">		<span class="comment">// 公式计算 f =  0.11R + 0.59G + 0.30B   gery 最大255 ，其他处理可能会超过 255</span></span><br><span class="line">		<span class="comment">// uchar gery = 0.11f*r + 0.59f*g + 0.30f*b;</span></span><br><span class="line">		uchar gray = <span class="number">0.11f</span>*r + <span class="number">0.59f</span>*g + <span class="number">0.30f</span>*b;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 彩色转黑白</span></span><br><span class="line">		uchar bw = (b + g + r) / <span class="number">3</span> &gt; <span class="number">125</span> ? <span class="number">255</span> : <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// saturate_cast 大于 255就变成255 ，小于0 就变成0</span></span><br><span class="line">		<span class="comment">/* start_pixels[0] = saturate_cast&lt;uchar&gt;(bw);</span></span><br><span class="line"><span class="comment">		start_pixels[1] = saturate_cast&lt;uchar&gt;(bw);</span></span><br><span class="line"><span class="comment">		start_pixels[2] = saturate_cast&lt;uchar&gt;(bw);*/</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">// 饱和度提升会更亮些</span></span><br><span class="line">		start_pixels[<span class="number">0</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(<span class="number">1.2f</span> * b);</span><br><span class="line">		start_pixels[<span class="number">1</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(<span class="number">1.2f</span> * g);</span><br><span class="line">		start_pixels[<span class="number">2</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(<span class="number">1.2f</span> * r);</span><br><span class="line"></span><br><span class="line">		start_pixels += <span class="number">3</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：18.leetcode刷题-数字和位运算、树</title>
    <url>/44.45.46.leetcode%E5%88%B7%E9%A2%98-%E6%95%B0%E5%AD%97%E5%92%8C%E4%BD%8D%E8%BF%90%E7%AE%97%E3%80%81%E6%A0%91/</url>
    <content><![CDATA[<h1 id="1-数字篇"><a href="#1-数字篇" class="headerlink" title="1. 数字篇"></a>1. 数字篇</h1><ol>
<li><a href="https://leetcode-cn.com/problems/two-sum/">两数之和-1</a></li>
<li><a href="https://leetcode.com/problems/reverse-integer/">整数反转-7</a></li>
</ol>
<h1 id="2-位运算"><a href="#2-位运算" class="headerlink" title="2. 位运算"></a>2. 位运算</h1><ol>
<li><a href="https://leetcode-cn.com/problems/add-two-numbers/">两数相加-2</a></li>
<li><a href="https://leetcode-cn.com/problems/power-of-two/">2的幂-231</a></li>
<li><a href="https://leetcode-cn.com/problems/power-of-three/">3的幂-326</a></li>
<li><a href="https://leetcode-cn.com/problems/number-of-1-bits/">位1的个数-191</a></li>
<li><a href="https://leetcode-cn.com/problems/factorial-trailing-zeroes/">阶乘后的零-172</a></li>
</ol>
<h1 id="3-树"><a href="#3-树" class="headerlink" title="3. 树"></a>3. 树</h1><ol>
<li><a href="https://leetcode-cn.com/problems/binary-tree-inorder-traversal/">二叉树的中序遍历-94</a></li>
</ol>
<p>递归方式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">inorderTraversal</span><span class="params">(TreeNode* root, vector&lt;<span class="type">int</span>&gt;* res)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root == <span class="literal">NULL</span>)&#123;           </span><br><span class="line">         <span class="keyword">return</span>;    </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 访问</span></span><br><span class="line">    <span class="built_in">inorderTraversal</span>(root-&gt;left,res);</span><br><span class="line">    <span class="comment">// 打印</span></span><br><span class="line">    res-&gt;<span class="built_in">push_back</span>(root-&gt;val);</span><br><span class="line">    <span class="built_in">inorderTraversal</span>(root-&gt;right,res);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">inorderTraversal</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; res;</span><br><span class="line">    <span class="built_in">inorderTraversal</span>(root,&amp;res);</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>非递归方式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TreeNode</span> &#123;</span><br><span class="line">   <span class="type">int</span> val;</span><br><span class="line">   TreeNode left;</span><br><span class="line">   TreeNode right;</span><br><span class="line">   <span class="built_in">TreeNode</span>(<span class="type">int</span> x) &#123; val = x; &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Integer&gt; <span class="title">inorderTraversal</span><span class="params">(TreeNode root)</span> </span>&#123; </span><br><span class="line">        Stack&lt;TraversalOperate&gt; operates = <span class="keyword">new</span> Stack&lt;&gt;();</span><br><span class="line">        List&lt;Integer&gt; res = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line"> </span><br><span class="line">        operates.<span class="built_in">push</span>(<span class="keyword">new</span> <span class="built_in">TraversalOperate</span>(<span class="number">1</span>,root));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span>(!operates.<span class="built_in">isEmpty</span>())&#123;</span><br><span class="line">            TraversalOperate top = operates.<span class="built_in">pop</span>();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span>(top.root == null)</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span>(top.operate == <span class="number">0</span>)</span><br><span class="line">                res.<span class="built_in">add</span>(top.root.val);</span><br><span class="line">            <span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="comment">// 从下往上看</span></span><br><span class="line">                operates.<span class="built_in">add</span>(<span class="keyword">new</span> <span class="built_in">TraversalOperate</span>(<span class="number">1</span>,top.root.right));</span><br><span class="line">                operates.<span class="built_in">add</span>(<span class="keyword">new</span> <span class="built_in">TraversalOperate</span>(<span class="number">0</span>,top.root));</span><br><span class="line">                operates.<span class="built_in">add</span>(<span class="keyword">new</span> <span class="built_in">TraversalOperate</span>(<span class="number">1</span>,top.root.left));                </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">TraversalOperate</span>&#123;</span><br><span class="line">        <span class="type">int</span> operate; <span class="comment">// 0 print  1 visit    </span></span><br><span class="line">        TreeNode root; <span class="comment">// 节点</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="title">TraversalOperate</span><span class="params">(<span class="type">int</span> operate,TreeNode root)</span></span>&#123;</span><br><span class="line">            <span class="keyword">this</span>.operate = operate;</span><br><span class="line">            <span class="keyword">this</span>.root = root;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>几个题</p>
<ul>
<li><a href="https://leetcode.com/problems/path-sum-ii/">第一题</a></li>
<li><a href="https://leetcode.com/problems/binary-tree-level-order-traversal/">第二题</a></li>
<li><a href="https://leetcode.com/problems/maximum-depth-of-binary-tree/">第三题</a></li>
<li><a href="https://leetcode.com/problems/minimum-depth-of-binary-tree/">第四题</a></li>
</ul>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
        <tag>leetcode刷题</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：02.android滤镜(不用opencv来写)</title>
    <url>/49.%20opencv-android%E6%BB%A4%E9%95%9C(%E4%B8%8D%E7%94%A8opencv%E6%9D%A5%E5%86%99)/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/opencv49androidfiltereffects">NDKPractice项目的opencv49androidfiltereffects</a></strong></p>
<h1 id="1-使用ColorMatrix颜色矩阵："><a href="#1-使用ColorMatrix颜色矩阵：" class="headerlink" title="1. 使用ColorMatrix颜色矩阵："></a>1. 使用ColorMatrix颜色矩阵：</h1><p>看下如何运算的：<br><img src="/images/49.ColorMatrix矩阵运算.png" alt=""></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@Nullable</span><br><span class="line">public static Bitmap gray(@Nullable Bitmap src) &#123;</span><br><span class="line"></span><br><span class="line">    Bitmap dst = Bitmap.createBitmap(src.getWidth(),src.getHeight(),src.getConfig());</span><br><span class="line">    Canvas canvas = new Canvas(dst);</span><br><span class="line"></span><br><span class="line">    // 通过 ColorMatrix 改变颜色矩阵</span><br><span class="line">    Paint paint = new Paint();</span><br><span class="line">    paint.setDither(true);</span><br><span class="line">    paint.setAntiAlias(true);</span><br><span class="line"></span><br><span class="line">    // ColorMatrix colorMatrix = new ColorMatrix();</span><br><span class="line">    // colorMatrix.setSaturation(0); // 修改饱和度为0 也就是把颜色矩阵转成了灰度图,相当于下面的矩阵</span><br><span class="line"></span><br><span class="line">    /*ColorMatrix colorMatrix = new ColorMatrix(new float[]&#123; // 灰度图效果</span><br><span class="line">            0.213f,0.715f,0.072f,0,0,</span><br><span class="line">            0.213f,0.715f,0.072f,0,0,</span><br><span class="line">            0.213f,0.715f,0.072f,0,0,</span><br><span class="line">            0,0,0,1,0</span><br><span class="line">    &#125;);*/</span><br><span class="line"></span><br><span class="line">    /*ColorMatrix colorMatrix = new ColorMatrix(new float[]&#123; // 原图效果</span><br><span class="line">            1,0,0,0,0,</span><br><span class="line">            0,1,0,0,0,</span><br><span class="line">            0,0,1,0,0,</span><br><span class="line">            0,0,0,1,0</span><br><span class="line">    &#125;);*/</span><br><span class="line"></span><br><span class="line">    /*ColorMatrix colorMatrix = new ColorMatrix(new float[]&#123; // 底片效果</span><br><span class="line">            -1,0,0,0,255,</span><br><span class="line">            0,-1,0,0,255,</span><br><span class="line">            0,0,-1,0,255,</span><br><span class="line">            0,0,0,1,0</span><br><span class="line">    &#125;);*/</span><br><span class="line"></span><br><span class="line">    ColorMatrix colorMatrix = new ColorMatrix(new float[]&#123; // 提高亮度</span><br><span class="line">            1.2f,0,0,0,0,</span><br><span class="line">            0,1.2f,0,0,0,</span><br><span class="line">            0,0,1.2f,0,0,</span><br><span class="line">            0,0,0,1,0</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    paint.setColorFilter(new ColorMatrixColorFilter(colorMatrix)); // 设置颜色矩阵</span><br><span class="line">    </span><br><span class="line">    canvas.drawBitmap(src,0,0,paint);</span><br><span class="line">    return dst;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-使用Bitmap的getPixels-和setPixels修改像素"><a href="#2-使用Bitmap的getPixels-和setPixels修改像素" class="headerlink" title="2. 使用Bitmap的getPixels()和setPixels修改像素"></a>2. 使用Bitmap的<code>getPixels()</code>和<code>setPixels</code>修改像素</h1><p>如果需要修改Bitmap本身的像素，复用则需要做调整：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var options = BitmapFactory.Options()</span><br><span class="line">options.inMutable = true // 是否能修改</span><br><span class="line"></span><br><span class="line">var bitmap  = BitmapFactory.decodeResource(resources,R.mipmap.copy,options)</span><br></pre></td></tr></table></figure>
<p>具体代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public static Bitmap gray2(@Nullable Bitmap src) &#123;</span><br><span class="line">    // Bitmap dst = Bitmap.createBitmap(src.getWidth(), src.getHeight(), src.getConfig());</span><br><span class="line"></span><br><span class="line">    // java层像素操作</span><br><span class="line">    int[] pixels = new int[src.getWidth() * src.getHeight()];</span><br><span class="line"></span><br><span class="line">    src.getPixels(pixels, 0, src.getWidth(), 0, 0, src.getWidth(), src.getHeight());</span><br><span class="line"></span><br><span class="line">    for (int i = 0; i &lt; pixels.length; i++) &#123;</span><br><span class="line">        int pixel = pixels[i];</span><br><span class="line"></span><br><span class="line">        int a = pixel &gt;&gt; 24 &amp; 0xFF;</span><br><span class="line">        int r = pixel &gt;&gt; 16 &amp; 0xFF;</span><br><span class="line">        int g = pixel &gt;&gt; 8 &amp; 0xFF;</span><br><span class="line">        int b = pixel &amp; 0xFF;</span><br><span class="line"></span><br><span class="line">        // 灰色</span><br><span class="line">        int gray = (int) (0.213f * r + 0.715f * g + 0.072f * b);</span><br><span class="line">        // pixels[i] = a &lt;&lt; 24 | gray &lt;&lt; 16 | gray &lt;&lt; 8 | gray;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        // 黑白</span><br><span class="line">        int bw = (r + g + b) / 3 &gt; 125 ? 255 : 0;</span><br><span class="line">        pixels[i] = a &lt;&lt; 24 | bw &lt;&lt; 16 | bw &lt;&lt; 8 | bw;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // dst.setPixels(pixels, 0, src.getWidth(), 0, 0, src.getWidth(), src.getHeight());</span><br><span class="line">    // return dst;</span><br><span class="line"></span><br><span class="line">    // 自己设置给自己，能复用不？,需要 BitmapFactory.Options.inMutable = true</span><br><span class="line">    src.setPixels(pixels, 0, src.getWidth(), 0, 0, src.getWidth(), src.getHeight());</span><br><span class="line">    return src;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-jni中操作（最好的方法）"><a href="#3-jni中操作（最好的方法）" class="headerlink" title="3. jni中操作（最好的方法）"></a>3. jni中操作（最好的方法）</h1><p>BitmapFactory.Options可以修改图片的颜色属性</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var options = BitmapFactory.Options()</span><br><span class="line">options.inMutable = true // 是否能修改</span><br><span class="line">options.inPreferredConfig = Bitmap.Config.RGB_565 // 将图片转为 rgb_565格式</span><br><span class="line">// RGB_565 747  ARGB_8888 1494  1 倍</span><br><span class="line">// ARGB_8888 -&gt; RGB 565 RGB 5位R 6位G 5位B   16位</span><br><span class="line">var src  = BitmapFactory.decodeResource(resources,R.mipmap.copy,options)</span><br></pre></td></tr></table></figure>
<p>jni中修改图片为黑白或者灰度的代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Java_com_east_opencv49androidfiltereffects_BitmapUtils_gray3(JNIEnv *env, jclass clazz,</span><br><span class="line">                                                             jobject src) &#123;</span><br><span class="line"></span><br><span class="line">    AndroidBitmapInfo info;</span><br><span class="line"></span><br><span class="line">    int result = AndroidBitmap_getInfo(env,src,&amp;info);</span><br><span class="line">    if(result!=0)</span><br><span class="line">        return -1;</span><br><span class="line"></span><br><span class="line">    void *pixels;</span><br><span class="line">    // 锁定画布</span><br><span class="line">    AndroidBitmap_lockPixels(env,src,&amp;pixels);</span><br><span class="line"></span><br><span class="line">    if(info.format == ANDROID_BITMAP_FORMAT_RGBA_8888)&#123;</span><br><span class="line">        uint32_t *pixels_p = reinterpret_cast&lt;uint32_t*&gt;(pixels);</span><br><span class="line">        for (int i = 0; i &lt; info.width*info.height; ++i) &#123;</span><br><span class="line">            if(i != 0)</span><br><span class="line">                pixels_p+=1;</span><br><span class="line"></span><br><span class="line">            int a = *pixels_p &gt;&gt; 24 &amp; 0xFF;</span><br><span class="line">            int r = *pixels_p &gt;&gt; 16 &amp; 0xFF;</span><br><span class="line">            int g = *pixels_p &gt;&gt; 8 &amp; 0xFF;</span><br><span class="line">            int b = *pixels_p &amp; 0xFF;</span><br><span class="line"></span><br><span class="line">            int gray = (int) (0.213f * r + 0.715f * g + 0.072f * b);</span><br><span class="line"></span><br><span class="line">            *pixels_p = a&lt;&lt;24 | gray &lt;&lt; 16 | gray &lt;&lt; 8 | gray;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;else if(info.format == ANDROID_BITMAP_FORMAT_RGB_565)&#123;</span><br><span class="line">        uint16_t *pixels_p = reinterpret_cast&lt;uint16_t*&gt;(pixels);</span><br><span class="line">        for (int i = 0; i &lt; info.width*info.height; ++i) &#123;</span><br><span class="line">            if(i != 0)</span><br><span class="line">                pixels_p+=1;</span><br><span class="line">            // 8888 -&gt; 565</span><br><span class="line">            int r = *pixels_p &gt;&gt; 11 &lt;&lt; 3 &amp; 0xFF;</span><br><span class="line">            int g = *pixels_p &gt;&gt; 5 &lt;&lt; 2 &amp; 0xFF;</span><br><span class="line">            int b = *pixels_p &lt;&lt; 3 &amp; 0xFF;</span><br><span class="line"></span><br><span class="line">            // f = 0.213f * r + 0.715f * g + 0.072f * b  (32位来讲的  255,255,255),所以需要补齐8位</span><br><span class="line">            int gray = (int) (0.213f * r + 0.715f * g + 0.072f * b);// 8位</span><br><span class="line"></span><br><span class="line">            *pixels_p = gray &gt;&gt;3 &lt;&lt; 11 | gray &gt;&gt;2 &lt;&lt;5 | gray &gt;&gt; 3;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // 解锁画布</span><br><span class="line">    AndroidBitmap_unlockPixels(env,src);</span><br><span class="line"></span><br><span class="line">    return 1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构和算法：15.哈夫曼编码</title>
    <url>/42.%20%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95-%E5%93%88%E5%A4%AB%E6%9B%BC%E7%BC%96%E7%A0%81/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/datastructure42huffman">NDKPractice项目的datastructure42huffman</a></strong></p>
<ul>
<li>定长编码： 1 字节 8 位代表一个字符   a</li>
<li>变长编码： utf   哈夫曼编码</li>
</ul>
<h1 id="1-哈夫曼树定义："><a href="#1-哈夫曼树定义：" class="headerlink" title="1. 哈夫曼树定义："></a>1. 哈夫曼树定义：</h1><p>哈夫曼树（霍夫曼树）：外国大学生发现的，主要用于压缩，变长的编码，最优的前缀码二叉树</p>
<ul>
<li>路径长度：根节点到该节点的边的条数 = 节点个数 - 1</li>
<li>节点的权：出现的次数</li>
<li>带权路径长度：= 权（次数） * 路径的长度</li>
<li>树的带权路径长度（WLP）： = 所有节点的带权路径长度之和</li>
</ul>
<h1 id="2-如何构建一棵最优的哈夫曼树"><a href="#2-如何构建一棵最优的哈夫曼树" class="headerlink" title="2. 如何构建一棵最优的哈夫曼树"></a>2. 如何构建一棵最优的哈夫曼树</h1><p><code>怎样才是最优的？ 就是树的带权路径长度最小</code></p>
<p><code>aaaabbcccccccd</code></p>
<h2 id="2-1-统计字符出现的个数"><a href="#2-1-统计字符出现的个数" class="headerlink" title="2.1 统计字符出现的个数"></a>2.1 统计字符出现的个数</h2><p>a -&gt; 4， d -&gt; 1 , b -&gt; 2 , c -&gt; 7,</p>
<h2 id="2-2-构建赫夫曼树"><a href="#2-2-构建赫夫曼树" class="headerlink" title="2.2 构建赫夫曼树"></a>2.2 构建赫夫曼树</h2><p>首先拿两个频率出现最低的出来，然后再拿两个出现频率最小的出来</p>
<p>a -&gt; 4，  (d -&gt; 1 , b-&gt;2) -&gt; 3 , c -&gt; 7,<br>(d -&gt; 1 , b-&gt;2, a-&gt;4) -&gt; 7, c -&gt; 7,</p>
<h2 id="2-3-前缀码"><a href="#2-3-前缀码" class="headerlink" title="2.3. 前缀码"></a>2.3. 前缀码</h2><p>前缀编码 是指对字符集进行编码时，要求字符集中任一字符的编码都不是其它字符的编码的前缀。<br>例如：设有abcd需要编码表示（其中，a=0、b=10、c=110、d=11,则表示110的前缀可以是c或者da，不唯一）</p>
<p>很多地方用到：图片，音视频 ，霍夫曼</p>
<h2 id="2-4-哈夫曼编码和解码实现"><a href="#2-4-哈夫曼编码和解码实现" class="headerlink" title="2.4. 哈夫曼编码和解码实现"></a>2.4. 哈夫曼编码和解码实现</h2><p>思想：</p>
<ol>
<li>统计字符出现的次数</li>
<li>构建出这棵最优的前缀码二叉树</li>
<li>统计字符的编码，生成映射表</li>
<li>根据字符去映射表中查找生成最终的霍夫曼编码</li>
</ol>
<p><img src="/images/42.霍夫曼编码.png" alt=""></p>
]]></content>
      <categories>
        <category>数据结构和算法</category>
      </categories>
      <tags>
        <tag>数据结构和算法学习</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：04.图像的形状绘制和卷积掩膜操作</title>
    <url>/51.OpenCV-%E5%9B%BE%E5%83%8F%E7%9A%84%E5%BD%A2%E7%8A%B6%E7%BB%98%E5%88%B6%E5%92%8C%E5%8D%B7%E7%A7%AF%E6%8E%A9%E8%86%9C%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<h1 id="1-inline-和-define"><a href="#1-inline-和-define" class="headerlink" title="1. inline 和 define"></a>1. inline 和 define</h1><p>inline是内联函数，类似于 define 都可以做到<code>编译时替换</code></p>
<p>区别：<code>define 并没有严格的检测,inline函数的参数有确切的类型所以会有严格的类型检测</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> Max(a,b) a&gt;b ? a :b </span></span><br><span class="line"><span class="comment">// inline 内联函数，类似于 define 做到编译时替换</span></span><br><span class="line"><span class="comment">// 区别：define 并没有严格的检测,inline函数的参数有确切的类型所以会有严格的类型检测</span></span><br><span class="line"><span class="comment">// inline 使用场景：不涉及过于复杂的函数</span></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="type">int</span> <span class="title">max1</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> a &gt; b ? a : b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/test.jpg&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    cout &lt;&lt; <span class="built_in">max1</span>(<span class="number">2</span>, <span class="number">3</span>) &lt;&lt; endl;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line">    <span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-形状和文件绘制"><a href="#2-形状和文件绘制" class="headerlink" title="2.形状和文件绘制"></a>2.形状和文件绘制</h1><ul>
<li>线 line</li>
<li>椭圆 ellipse</li>
<li>矩形 rectangle</li>
<li>圆 cicle</li>
<li>填充 fillPloy</li>
<li>文字 putText </li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/test.jpg&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 判断读取是否正确</span></span><br><span class="line">	<span class="keyword">if</span> (src.<span class="built_in">empty</span>())&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;src read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2.1 绘制线</span></span><br><span class="line">	<span class="comment">// LINE_8: 8联通线（8-connected）,即下一个点连接上一个点的边或角</span></span><br><span class="line">	<span class="comment">// LINE_4: 下一个点和上一个点边相连（没有角了）</span></span><br><span class="line">	<span class="built_in">line</span>(src,<span class="built_in">Point</span>(<span class="number">10</span>,<span class="number">10</span>),<span class="built_in">Point</span>(<span class="number">300</span>,<span class="number">300</span>),<span class="built_in">Scalar</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">255</span>),<span class="number">5</span>,LINE_8);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2.2 绘制矩形</span></span><br><span class="line">	<span class="built_in">rectangle</span>(src, <span class="built_in">Point</span>(<span class="number">20</span>, <span class="number">20</span>), <span class="built_in">Point</span>(<span class="number">100</span>, <span class="number">100</span>), <span class="built_in">Scalar</span>(<span class="number">0</span>, <span class="number">0</span>, <span class="number">255</span>), <span class="number">5</span>, LINE_8);</span><br><span class="line">	<span class="built_in">rectangle</span>(src, <span class="built_in">Rect</span>(<span class="number">30</span>,<span class="number">30</span>,<span class="number">20</span>,<span class="number">50</span>), <span class="built_in">Scalar</span>(<span class="number">0</span>, <span class="number">0</span>, <span class="number">255</span>), <span class="number">5</span>, LINE_8);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2.3 绘制椭圆</span></span><br><span class="line">	<span class="comment">// 第二个参数是：椭圆的中心点</span></span><br><span class="line">	<span class="comment">// 第三个参数: Size 第一个值是椭圆 x width 的半径， 第二个是 y height 的半径</span></span><br><span class="line">	<span class="built_in">ellipse</span>(src,<span class="built_in">Point</span>(src.cols/<span class="number">2</span>,src.rows/<span class="number">4</span>),<span class="built_in">Size</span>(src.cols/<span class="number">8</span>,src.rows/<span class="number">4</span>),<span class="number">180</span>,<span class="number">0</span>,<span class="number">360</span>,<span class="built_in">Scalar</span>(<span class="number">255</span>,<span class="number">0</span>,<span class="number">0</span>),<span class="number">3</span>,LINE_AA);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2.4 园</span></span><br><span class="line">	<span class="built_in">circle</span>(src,<span class="built_in">Point</span>(src.cols<span class="number">-500</span>,src.rows<span class="number">-500</span>),<span class="number">50</span>,<span class="built_in">Scalar</span>(<span class="number">0</span>,<span class="number">255</span>,<span class="number">255</span>),<span class="number">5</span>,LINE_AA);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2.5 填充 fillpoly 多边形</span></span><br><span class="line">	<span class="comment">/*CV_EXPORTS void fillPoly(InputOutputArray img, const Point** pts,</span></span><br><span class="line"><span class="comment">		const int* npts, int ncontours,</span></span><br><span class="line"><span class="comment">		const Scalar&amp; color, int lineType = LINE_8, int shift = 0,</span></span><br><span class="line"><span class="comment">		Point offset = Point());*/</span></span><br><span class="line">	Point pots[] = &#123; <span class="built_in">Point</span>(<span class="number">100</span>, <span class="number">100</span>), <span class="built_in">Point</span>(<span class="number">100</span>, <span class="number">200</span>), <span class="built_in">Point</span>(<span class="number">200</span>, <span class="number">200</span>), <span class="built_in">Point</span>(<span class="number">100</span>, <span class="number">100</span>) &#125;;</span><br><span class="line"></span><br><span class="line">	<span class="type">const</span> Point* potss[] = &#123; pots &#125;;</span><br><span class="line"></span><br><span class="line">	<span class="type">const</span> <span class="type">int</span> npts[] = &#123; <span class="number">4</span> &#125;;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">fillPoly</span>(src, potss, npts, <span class="number">1</span>, <span class="built_in">Scalar</span>(<span class="number">255</span>, <span class="number">0</span>, <span class="number">0</span>), LINE_8);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2,6 画文字</span></span><br><span class="line">	<span class="comment">// fontFace 字体</span></span><br><span class="line">	<span class="comment">// fontScale 缩放，大小</span></span><br><span class="line">	<span class="built_in">putText</span>(src, <span class="string">&quot;HI，OpenCV&quot;</span>, <span class="built_in">Point</span>(<span class="number">100</span>, <span class="number">100</span>), QT_FONT_BLACK,<span class="number">1</span>,<span class="built_in">Scalar</span>(<span class="number">255</span>,<span class="number">0</span>,<span class="number">255</span>),<span class="number">1</span>,LINE_AA);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2.7 随机画 srand 画线</span></span><br><span class="line">	<span class="comment">// opencv 做随机 srand random 效果一样</span></span><br><span class="line">	<span class="function">RNG <span class="title">rng</span><span class="params">(time(<span class="literal">NULL</span>))</span></span>;</span><br><span class="line">	<span class="comment">// 创建一张图片,与 src 的宽高和颜色通道一直，所有的点都是 0</span></span><br><span class="line">	Mat dst = Mat::<span class="built_in">zeros</span>(src.<span class="built_in">size</span>(),src.<span class="built_in">type</span>());</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		Point sp;</span><br><span class="line">		sp.x = rng.<span class="built_in">uniform</span>(<span class="number">0</span>,dst.cols);</span><br><span class="line">		sp.y = rng.<span class="built_in">uniform</span>(<span class="number">0</span>, dst.rows);</span><br><span class="line">		Point ep;</span><br><span class="line">		ep.x = rng.<span class="built_in">uniform</span>(<span class="number">0</span>, dst.cols);</span><br><span class="line">		ep.y = rng.<span class="built_in">uniform</span>(<span class="number">0</span>, dst.rows);</span><br><span class="line">		<span class="built_in">line</span>(dst, sp, ep, <span class="built_in">Scalar</span>(rng.<span class="built_in">uniform</span>(<span class="number">0</span>, <span class="number">255</span>), rng.<span class="built_in">uniform</span>(<span class="number">0</span>, <span class="number">255</span>),rng.<span class="built_in">uniform</span>(<span class="number">0</span>, <span class="number">255</span>)));</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, dst);</span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-图像卷积和滤波操作"><a href="#3-图像卷积和滤波操作" class="headerlink" title="3.图像卷积和滤波操作"></a>3.图像卷积和滤波操作</h1><p><img src="/images/51.卷积操作.png" alt=""></p>
<h2 id="3-1-图像矩阵掩膜操作-（可以过滤图像信息）"><a href="#3-1-图像矩阵掩膜操作-（可以过滤图像信息）" class="headerlink" title="3.1 图像矩阵掩膜操作 （可以过滤图像信息）"></a>3.1 图像矩阵掩膜操作 （可以过滤图像信息）</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (src.<span class="built_in">empty</span>())&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;src read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 下面是掩膜操作</span></span><br><span class="line">	<span class="comment">// 生成一个和源图像大小相等类型相同的全 0 矩阵</span></span><br><span class="line">	<span class="comment">/*Mat dst = Mat::zeros(src.size(),src.type());</span></span><br><span class="line"><span class="comment">	int rows = src.rows;</span></span><br><span class="line"><span class="comment">	int cols = src.cols;</span></span><br><span class="line"><span class="comment">	int channels = src.channels();</span></span><br><span class="line"><span class="comment">	for (int i = 1; i &lt; rows - 1; i++)</span></span><br><span class="line"><span class="comment">	&#123;</span></span><br><span class="line"><span class="comment">		// 上一行</span></span><br><span class="line"><span class="comment">		uchar* previous = src.ptr&lt;uchar&gt;(i-1);</span></span><br><span class="line"><span class="comment">		// 当前行</span></span><br><span class="line"><span class="comment">		uchar* current = src.ptr&lt;uchar&gt;(i);</span></span><br><span class="line"><span class="comment">		// 下一行</span></span><br><span class="line"><span class="comment">		uchar* next = src.ptr&lt;uchar&gt;(i+1);</span></span><br><span class="line"><span class="comment">		// 输出</span></span><br><span class="line"><span class="comment">		uchar* output = dst.ptr&lt;uchar&gt;(i);</span></span><br><span class="line"><span class="comment">		for (int j = channels; j &lt; (cols-1)*channels; j++)</span></span><br><span class="line"><span class="comment">		&#123;</span></span><br><span class="line"><span class="comment">			output[j] = saturate_cast&lt;uchar&gt;(5 * current[j] - (previous[j] + next[j] + current[j - channels] + current[j + channels]));</span></span><br><span class="line"><span class="comment">		&#125;</span></span><br><span class="line"><span class="comment">	&#125;*/</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="comment">// opencv 自带的掩膜操作</span></span><br><span class="line">	Mat dst;</span><br><span class="line">	Mat kernel = (<span class="built_in">Mat_</span>&lt;<span class="type">char</span>&gt;(<span class="number">3</span>, <span class="number">3</span>) &lt;&lt; <span class="number">0</span>, <span class="number">-1</span>, <span class="number">0</span>, <span class="number">-1</span>, <span class="number">5</span>, <span class="number">-1</span>, <span class="number">0</span>, <span class="number">-1</span>, <span class="number">0</span>);</span><br><span class="line">	<span class="built_in">filter2D</span>(src,dst,src.<span class="built_in">depth</span>(),kernel);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>, dst);</span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：07.形态学，验证码，提取水平线，上、降采样</title>
    <url>/55.OpenCV-%E5%BD%A2%E6%80%81%E5%AD%A6%EF%BC%8C%E9%AA%8C%E8%AF%81%E7%A0%81%EF%BC%8C%E6%8F%90%E5%8F%96%E6%B0%B4%E5%B9%B3%E7%BA%BF%EF%BC%8C%E4%B8%8A%E3%80%81%E9%99%8D%E9%87%87%E6%A0%B7/</url>
    <content><![CDATA[<h1 id="1-形态学操作"><a href="#1-形态学操作" class="headerlink" title="1. 形态学操作"></a>1. 形态学操作</h1><p><code>morphologyEx</code></p>
<p>腐蚀：erode 取最小值<br>膨胀：dilate 取最大值  </p>
<ul>
<li>MORPH_OPEN 开：先腐蚀后膨胀</li>
<li>MORPH_CLOSE 闭：先膨胀后腐蚀</li>
<li>MORPH_GRADIENT 梯度：膨胀 - 腐蚀</li>
<li>MORPH_TOPHAT 顶帽：原图像 - 开图像</li>
<li>MORPH_BLACKHAT 黑帽：闭图像 - 原图像</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;imread error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	Mat kernel = <span class="built_in">getStructuringElement</span>(MORPH_RECT,<span class="built_in">Size</span>(<span class="number">25</span>,<span class="number">25</span>));</span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="comment">// 腐蚀</span></span><br><span class="line">	<span class="comment">// erode(src, dst, kernel);</span></span><br><span class="line">	<span class="comment">// 膨胀</span></span><br><span class="line">	<span class="comment">//dilate(src,dst,kernel);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// MORPH_OPEN 开：先腐蚀后膨胀</span></span><br><span class="line">	<span class="comment">// morphologyEx(src, dst,MORPH_OPEN,kernel);</span></span><br><span class="line">	</span><br><span class="line">	<span class="comment">// MORPH_CLOSE 闭：先膨胀后腐蚀</span></span><br><span class="line">	<span class="comment">// morphologyEx(src,dst,MORPH_CLOSE,kernel);</span></span><br><span class="line">	</span><br><span class="line">	<span class="comment">// MORPH_GRADIENT 梯度：膨胀 - 腐蚀</span></span><br><span class="line">	<span class="comment">//morphologyEx(src, dst, MORPH_GRADIENT, kernel);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// MORPH_TOPHAT 顶帽：原图像 - 开图像</span></span><br><span class="line">	<span class="comment">// morphologyEx(src,dst,MORPH_TOPHAT,kernel);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// MORPH_BLACKHAT 黑帽：闭图像 - 原图像</span></span><br><span class="line">	<span class="built_in">morphologyEx</span>(src,dst,MORPH_BLACKHAT,kernel);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>,dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-验证码识别"><a href="#2-验证码识别" class="headerlink" title="2. 验证码识别"></a>2. 验证码识别</h1><p><code>~ 符号表示 = （255 - 原本的像素值） 等同于 bitwise_not</code><br><code>adaptiveThreshold 二值化， 比如 &gt; 125 的像素 设置为 255, 小于=125 的像素设置为 0</code> </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;imread error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2.1 把彩色变成 黑白 二值化</span></span><br><span class="line">	<span class="comment">// 灰白</span></span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src,gray,COLOR_BGR2GRAY);</span><br><span class="line">	<span class="comment">// imshow(&quot;gray&quot;,gray);</span></span><br><span class="line">	<span class="comment">// 二值化方法，自动阈值</span></span><br><span class="line">	<span class="comment">// ~ 符号表示 = （255 - 原本的像素值） 等同于 bitwise_not</span></span><br><span class="line">	Mat binary;</span><br><span class="line">	<span class="built_in">adaptiveThreshold</span>(~gray,binary,<span class="number">255</span>,ADAPTIVE_THRESH_MEAN_C,THRESH_BINARY,<span class="number">15</span>,<span class="number">0</span>);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;binary&quot;</span>,binary);</span><br><span class="line"></span><br><span class="line">	Mat kernel = <span class="built_in">getStructuringElement</span>(MORPH_RECT,<span class="built_in">Size</span>(<span class="number">5</span>,<span class="number">5</span>));</span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="comment">// 腐蚀</span></span><br><span class="line">	<span class="built_in">erode</span>(src,dst,kernel);</span><br><span class="line">	<span class="comment">// 膨胀</span></span><br><span class="line">	<span class="built_in">dilate</span>(src, dst, kernel);</span><br><span class="line">	<span class="comment">// morphologyEx(src,dst,MORPH_CLOSE,kernel);</span></span><br><span class="line">	<span class="built_in">bitwise_not</span>(dst, dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>,dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-提取水平线"><a href="#3-提取水平线" class="headerlink" title="3. 提取水平线"></a>3. 提取水平线</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;imread error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src, gray, COLOR_BGR2GRAY);</span><br><span class="line">	<span class="comment">// imshow(&quot;gray&quot;,gray);</span></span><br><span class="line">	<span class="comment">// 二值化方法，自动阈值</span></span><br><span class="line">	<span class="comment">// ~ 符号表示 = （255 - 原本的像素值） 等同于 bitwise_not</span></span><br><span class="line">	Mat binary;</span><br><span class="line">	<span class="built_in">adaptiveThreshold</span>(~gray, binary, <span class="number">255</span>, ADAPTIVE_THRESH_MEAN_C, THRESH_BINARY, <span class="number">15</span>, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 二值化有些小问题，做一下弥补</span></span><br><span class="line">	Mat kernel = <span class="built_in">getStructuringElement</span>(MORPH_RECT, <span class="built_in">Size</span>(<span class="number">9</span>, <span class="number">9</span>));</span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="built_in">dilate</span>(binary, dst, kernel);</span><br><span class="line">	<span class="built_in">erode</span>(dst, dst, kernel);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 取水平</span></span><br><span class="line">	Mat lLine = <span class="built_in">getStructuringElement</span>(MORPH_RECT, <span class="built_in">Size</span>(src.cols/<span class="number">16</span>,<span class="number">1</span>));</span><br><span class="line">	<span class="built_in">erode</span>(dst,dst,lLine);</span><br><span class="line">	<span class="built_in">dilate</span>(dst,dst,lLine);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>,dst);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-上采样与降采样"><a href="#4-上采样与降采样" class="headerlink" title="4. 上采样与降采样"></a>4. 上采样与降采样</h1><p>图片采样：采样（sampling）也称取样，指把时间域或空间域的连续量转化成离散量的过程。在数字图像处理领域中，定义为图像空间坐标的数字化操作。</p>
<ul>
<li>pyrUp 上采样 放大  拉普拉斯：预计算（推算）值</li>
<li>pyrDown 降采样 缩小  高斯 ，1. 对图像进行高斯模糊，2.再过滤左右临近点</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;imread error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="comment">// 上采样 拉普拉斯 双线性插值，预计算（推算）值</span></span><br><span class="line">	<span class="comment">// pyrUp(src,dst,Size(src.cols*2,src.rows*2));</span></span><br><span class="line">	<span class="comment">// 降采样：1. 对图像进行高斯模糊，2.再过滤左右临近点。 采用的高斯 所以比较慢</span></span><br><span class="line">	<span class="built_in">pyrDown</span>(src,dst,<span class="built_in">Size</span>(src.cols/<span class="number">2</span>,src.rows/<span class="number">2</span>));</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>,dst);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// imwrite(&quot;E:/copy_pyrUp.png&quot;, dst);</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：05.Bitmap性能优化(LruCache)和高斯模糊</title>
    <url>/52.OpenCV-Bitmap%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96(LruCache)%E5%92%8C%E9%AB%98%E6%96%AF%E6%A8%A1%E7%B3%8A/</url>
    <content><![CDATA[<h1 id="1-Bitmap-占用内存分析及常见优化"><a href="#1-Bitmap-占用内存分析及常见优化" class="headerlink" title="1. Bitmap 占用内存分析及常见优化"></a>1. Bitmap 占用内存分析及常见优化</h1><p>分析工具：AS3.0以上使用 <code>Android Profile</code>,以下可以使用 <code>Android Device Monitor</code></p>
<p>优化：  </p>
<ol>
<li>耗时操作可以通过 <code>Rxjava</code> 去切换到子线程处理</li>
<li>如果涉及到高斯模糊，从网络上下载的大图，根据ui需求，通过 Glide 重新调整其大小（这样减少高斯模糊的计算时间）</li>
<li>使用LruCache 缓存，根据不同机型分配 不同的缓存 大小</li>
</ol>
<h2 id="1-1-Lrucache-两个点记录"><a href="#1-1-Lrucache-两个点记录" class="headerlink" title="1.1 Lrucache 两个点记录"></a>1.1 Lrucache 两个点记录</h2><ol>
<li>记得重写 Lrucache 的 <code>sizeOf(K key, V value)</code>方法为缓存的单个对象的大小，不然默认为 1</li>
<li>内存大小可以根据不同手机等条件动态设置</li>
</ol>
<p>Glide 的 <code>MemoryCache</code>的缓存大小为：<code>(一个屏幕图片大小=widthPixels*heightPixels*4)* 2</code>  </p>
<p>LruCache实际上就是一个 <code>LinkedHashMap( 补充知识：LinkedHashMap是一个双向循环列表，不支持线程安全，LruCache对它进行了封装添加了线程安全操作)，里面保存了一定数量的对象强引用</code>，<br>每次添加的新对象都是在链表的头，当分配的空间用完的时候会把末尾的对象移除，移除的对象就可以被gc回收了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 缓存图片</span></span><br><span class="line">Lrucache cache = <span class="built_in">Lrucache</span>(<span class="number">4</span>*<span class="number">1024</span>*<span class="number">1024</span>); </span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果不重写 Lrucache 的 `sizeOf(K key, V value)`方法 ，则默认可以缓存 4*1024*1024 张图片</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"><span class="keyword">private</span> LruCache&lt;String, Bitmap&gt; mMemoryCache;<span class="comment">//声明缓存空间</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">final</span> <span class="type">int</span> maxMemory = (<span class="type">int</span>) (Runtime.<span class="built_in">getRuntime</span>().<span class="built_in">maxMemory</span>() / <span class="number">1024</span>);<span class="comment">//获取应用在系统中的最大内存分配</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">//分配1/8的应用内存作为缓存空间</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">final</span> <span class="type">int</span> cacheSize = maxMemory / <span class="number">8</span>;</span><br><span class="line"> </span><br><span class="line">mMemoryCache = <span class="keyword">new</span> <span class="built_in">LruCache</span>&lt;String, Bitmap&gt;(cacheSize) &#123;</span><br><span class="line"> </span><br><span class="line">@Override</span><br><span class="line"><span class="keyword">protected</span> <span class="type">int</span> <span class="built_in">sizeOf</span>(String key, Bitmap bitmap) &#123;</span><br><span class="line">    <span class="comment">//重写sizeOf方法，返回图片的占用字节数而不是图片的个数，每次添加图片是会被调用</span></span><br><span class="line">    <span class="keyword">return</span> bitmap.<span class="built_in">getByteCount</span>() / <span class="number">1024</span>;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h1 id="2-图像模糊"><a href="#2-图像模糊" class="headerlink" title="2. 图像模糊"></a>2. 图像模糊</h1><p><img src="/images/52.高斯模糊和均值模糊.png" alt=""></p>
<h2 id="2-1-均值模糊"><a href="#2-1-均值模糊" class="headerlink" title="2.1 均值模糊"></a>2.1 均值模糊</h2><p>操作：对当前像素点的周边半径，进行相加然后取平均值，赋值给当前像素点。</p>
<h2 id="2-2-高斯模糊"><a href="#2-2-高斯模糊" class="headerlink" title="2.2 高斯模糊"></a>2.2 高斯模糊</h2><p>操作： 对当前像素点的周边半径，求权重计算周边像素值，然后相加赋值给当前像素点。</p>
<font color="red">每个像素点都要处理，还有就是要去计算我们的正太分布的值（卷积核）</font>  
<font color="red">如果半径越大，算法的复杂度越大，如果图片的宽高像素点越多，算法的复杂度也越大</font>

<p>高斯和均值模糊的区别：<code>高斯保留了一些轮廓，毛玻璃的效果</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (src.<span class="built_in">empty</span>())&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;src read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 均值模糊</span></span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="comment">// Size（w,h）只能是基数，这样才能找到中心点</span></span><br><span class="line">	<span class="comment">// Point(-1,-1) 代表中心店</span></span><br><span class="line">	<span class="built_in">blur</span>(src,dst,<span class="built_in">Size</span>(<span class="number">151</span>,<span class="number">151</span>),<span class="built_in">Point</span>(<span class="number">-1</span>,<span class="number">-1</span>));</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>, dst);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 高斯模糊</span></span><br><span class="line">	Mat gaussian;</span><br><span class="line">	<span class="comment">// sigmaX sigmaY 代表 作用</span></span><br><span class="line">	<span class="comment">// sigmaY 不传代表和 sigmaX 一样</span></span><br><span class="line">	<span class="comment">// 如果 sigmaX &lt;= 0 ,自己会计算 0.3*((ksize-1)*0.5 - 1) + 0.8</span></span><br><span class="line">	<span class="comment">// sigmaX 越小越没效果</span></span><br><span class="line">	<span class="built_in">GaussianBlur</span>(src, gaussian, <span class="built_in">Size</span>(<span class="number">151</span>, <span class="number">151</span>), <span class="number">0</span>);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;gaussian&quot;</span>, gaussian);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
        <category>LruCache</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>OpenCV</tag>
        <tag>LruCache</tag>
      </tags>
  </entry>
  <entry>
    <title>Bitmap全面解析</title>
    <url>/53.OpenCV-Bitmap%E5%85%A8%E9%9D%A2%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<h1 id="1-dp-dip-dpi-px"><a href="#1-dp-dip-dpi-px" class="headerlink" title="1. dp dip  dpi  px"></a>1. dp dip  dpi  px</h1><ul>
<li>dp = dip：Android 为了适配不同的设备弄出来的单位 1dp = dpi（像素密度） / 160</li>
<li>dpi(dots per inch)：像素密度  = px / 英寸</li>
<li>px：像素</li>
</ul>
<p>他们之间的换算：<code>屏幕的总 dp = px / (dpi/160)</code></p>
<h1 id="2-Bitmap-大小计算-看源码"><a href="#2-Bitmap-大小计算-看源码" class="headerlink" title="2. Bitmap 大小计算 看源码"></a>2. Bitmap 大小计算 看源码</h1><p>以下是 java 层的源码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">类:`BitmapFactory`.`decodeResource`(Resources res, int id)</span><br><span class="line"></span><br><span class="line">-&gt; 类:`BitmapFactory`.decodeResource(Resources res, int id, Options opts)</span><br><span class="line"></span><br><span class="line">-&gt; 类:`BitmapFactory`.decodeResourceStream(@Nullable Resources res, @Nullable TypedValue value,</span><br><span class="line">            @Nullable InputStream is, @Nullable Rect pad, @Nullable Options opts)&#123;</span><br><span class="line">                        if (opts == null) &#123;</span><br><span class="line">                            opts = new Options();</span><br><span class="line">                        &#125;</span><br><span class="line">                </span><br><span class="line">                        if (opts.inDensity == 0 &amp;&amp; value != null) &#123;</span><br><span class="line">                            final int density = value.density;</span><br><span class="line">                            if (density == TypedValue.DENSITY_DEFAULT) &#123;</span><br><span class="line">                                opts.inDensity = DisplayMetrics.DENSITY_DEFAULT;  // 根据放到哪个文件夹决定 density 的大小</span><br><span class="line">                            &#125; else if (density != TypedValue.DENSITY_NONE) &#123;</span><br><span class="line">                                opts.inDensity = density;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                        // 获取当前手机设备的 dpi </span><br><span class="line">                        if (opts.inTargetDensity == 0 &amp;&amp; res != null) &#123;</span><br><span class="line">                            opts.inTargetDensity = res.getDisplayMetrics().densityDpi;</span><br><span class="line">                        &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">-&gt; 类:`BitmapFactory`.decodeStream(@Nullable InputStream is, @Nullable Rect outPadding,</span><br><span class="line">            @Nullable Options opts)</span><br><span class="line">            </span><br><span class="line">-&gt; 类:`BitmapFactory`.decodeStreamInternal(@NonNull InputStream is,</span><br><span class="line">            @Nullable Rect outPadding, @Nullable Options opts)</span><br><span class="line">            </span><br><span class="line">   // 调用 native 层的方法解析图片</span><br><span class="line">-&gt; 类:`BitmapFactory`.nativeDecodeStream(InputStream is, byte[] storage,</span><br><span class="line">            Rect padding, Options opts, long inBitmapHandle, long colorSpaceHandle)</span><br><span class="line"></span><br><span class="line">            </span><br><span class="line">private static native Bitmap nativeDecodeStream(InputStream is, byte[] storage,</span><br><span class="line">            Rect padding, Options opts, long inBitmapHandle, long colorSpaceHandle);</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<font color = red>
opts.inDensity = DisplayMetrics.DENSITY_DEFAULT;  // 根据放到哪个文件夹决定 density 的大小

- mdpi = 160
- hdpi = 240
- xhdpi = 320
- xxhdpi = 480
- xxxhdpi = 640

</font>



<p>以下是 native 层的源码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">类 `BitmapFactory.cpp`.nativeDecodeStream(JNIEnv* env, jobject clazz, jobject is, jbyteArray storage,</span><br><span class="line">        jobject padding, jobject options, jlong inBitmapHandle, jlong colorSpaceHandle)</span><br><span class="line"></span><br><span class="line">-&gt; 类 `BitmapFactory.cpp`.doDecode(JNIEnv* env, std::unique_ptr&lt;SkStreamRewindable&gt; stream,</span><br><span class="line">                        jobject padding, jobject options, jlong inBitmapHandle,</span><br><span class="line">                        jlong colorSpaceHandle)</span><br><span class="line">                        </span><br><span class="line">// 分析下这个方法</span><br><span class="line"></span><br><span class="line">static jobject doDecode(JNIEnv* env, std::unique_ptr&lt;SkStreamRewindable&gt; stream,</span><br><span class="line">                        jobject padding, jobject options, jlong inBitmapHandle,</span><br><span class="line">                        jlong colorSpaceHandle) &#123;</span><br><span class="line">    // Set default values for the options parameters.</span><br><span class="line">    int sampleSize = 1;    // option里面要大小设置的参数，代表压缩几倍  BitmapFactory.Options. `inSampleSize`</span><br><span class="line">    bool onlyDecodeSize = false;  // 是否只获取图片的大小 BitmapFactory.Options. `inJustDecodeBounds` </span><br><span class="line">    SkColorType prefColorType = kN32_SkColorType; </span><br><span class="line">    bool isHardware = false; // 是否硬件加速</span><br><span class="line">    bool isMutable = false; // 是否可以复用</span><br><span class="line">    float scale = 1.0f;  // 缩放</span><br><span class="line">    bool requireUnpremultiplied = false;</span><br><span class="line">    jobject javaBitmap = NULL;</span><br><span class="line">    sk_sp&lt;SkColorSpace&gt; prefColorSpace = GraphicsJNI::getNativeColorSpace(colorSpaceHandle);</span><br><span class="line">    </span><br><span class="line">    // 获取 Options 里面的属性</span><br><span class="line">    if (options != NULL) &#123;</span><br><span class="line">        sampleSize = env-&gt;GetIntField(options, gOptions_sampleSizeFieldID);</span><br><span class="line">        // Correct a non-positive sampleSize.  sampleSize defaults to zero within the</span><br><span class="line">        // options object, which is strange.</span><br><span class="line">        if (sampleSize &lt;= 0) &#123;</span><br><span class="line">            sampleSize = 1;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        if (env-&gt;GetBooleanField(options, gOptions_justBoundsFieldID)) &#123;</span><br><span class="line">            onlyDecodeSize = true;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        // initialize these, in case we fail later on</span><br><span class="line">        env-&gt;SetIntField(options, gOptions_widthFieldID, -1);</span><br><span class="line">        env-&gt;SetIntField(options, gOptions_heightFieldID, -1);</span><br><span class="line">        env-&gt;SetObjectField(options, gOptions_mimeFieldID, 0);</span><br><span class="line">        env-&gt;SetObjectField(options, gOptions_outConfigFieldID, 0);</span><br><span class="line">        env-&gt;SetObjectField(options, gOptions_outColorSpaceFieldID, 0);</span><br><span class="line"></span><br><span class="line">        jobject jconfig = env-&gt;GetObjectField(options, gOptions_configFieldID);</span><br><span class="line">        prefColorType = GraphicsJNI::getNativeBitmapColorType(env, jconfig);</span><br><span class="line">        isHardware = GraphicsJNI::isHardwareConfig(env, jconfig);</span><br><span class="line">        isMutable = env-&gt;GetBooleanField(options, gOptions_mutableFieldID);</span><br><span class="line">        requireUnpremultiplied = !env-&gt;GetBooleanField(options, gOptions_premultipliedFieldID);</span><br><span class="line">        javaBitmap = env-&gt;GetObjectField(options, gOptions_bitmapFieldID);</span><br><span class="line"></span><br><span class="line">        if (env-&gt;GetBooleanField(options, gOptions_scaledField  ID)) &#123;</span><br><span class="line">            const int density = env-&gt;GetIntField(options, gOptions_densityFieldID);</span><br><span class="line">            const int targetDensity = env-&gt;GetIntField(options, gOptions_targetDensityFieldID);</span><br><span class="line">            const int screenDensity = env-&gt;GetIntField(options, gOptions_screenDensityFieldID);</span><br><span class="line">            if (density != 0 &amp;&amp; targetDensity != 0 &amp;&amp; density != screenDensity) &#123;</span><br><span class="line">                // 获取 scale 图片存放的文件夹不一样 scale 的值就不一样</span><br><span class="line">                scale = (float) targetDensity / density;   // scale = 420/480 = 0.875</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    SkISize size = codec-&gt;getSampledDimensions(sampleSize);</span><br><span class="line">    int scaledWidth = size.width();  // 864</span><br><span class="line">    int scaledHeight = size.height(); // 582</span><br><span class="line">    bool willScale = false;</span><br><span class="line">    </span><br><span class="line">    // Apply a fine scaling step if necessary. 如果 sampleSize != 1 的话就会进入到这个方法中 进行尺寸压缩</span><br><span class="line">    if (needsFineScale(codec-&gt;getInfo().dimensions(), size, sampleSize)) &#123;</span><br><span class="line">        willScale = true;</span><br><span class="line">        scaledWidth = codec-&gt;getInfo().width() / sampleSize;</span><br><span class="line">        scaledHeight = codec-&gt;getInfo().height() / sampleSize;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">    // Scale is necessary due to density differences.  scale = 420/480 = 0.875</span><br><span class="line">    if (scale != 1.0f) &#123; // 0.833333  </span><br><span class="line">        willScale = true;</span><br><span class="line">        // 0.5f 是很重要的</span><br><span class="line">        scaledWidth = static_cast&lt;int&gt;(scaledWidth * scale + 0.5f); // 756</span><br><span class="line">        scaledHeight = static_cast&lt;int&gt;(scaledHeight * scale + 0.5f); // 509 </span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/images/Native层解析Bitmap的流程.png" alt=""></p>
<h1 id="3-Bitmap-的内存开辟"><a href="#3-Bitmap-的内存开辟" class="headerlink" title="3. Bitmap 的内存开辟"></a>3. Bitmap 的内存开辟</h1><p>内存申请因为版本不同而有所区别：</p>
<p>Bitmap 的内存申请不同版本间有些许差异，在 <code>3.0-7.0</code> 的 bitmap 像素内存都是存放在 <code>Java heap</code> 中的，而 <code>8.0</code> 以后则是放在 <code>Native heap</code> 中的</p>
<p>看下面代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@Override</span><br><span class="line">protected void onCreate(Bundle savedInstanceState) &#123;</span><br><span class="line">    super.onCreate(savedInstanceState);</span><br><span class="line">    setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">    logMemory();</span><br><span class="line"></span><br><span class="line">    Bitmap bitmap = Bitmap.createBitmap(1024, 1024 * 500, Bitmap.Config.ARGB_8888);</span><br><span class="line"></span><br><span class="line">    logMemory();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">private void logMemory() &#123;</span><br><span class="line">    ActivityManager activityManager = (ActivityManager) getSystemService(Context.ACTIVITY_SERVICE);</span><br><span class="line">    ActivityManager.MemoryInfo memoryInfo = new ActivityManager.MemoryInfo();</span><br><span class="line">    activityManager.getMemoryInfo(memoryInfo);</span><br><span class="line">    Log.e(&quot;TAG&quot;, &quot;AvailMem :&quot; + memoryInfo.availMem / 1024 / 1024);</span><br><span class="line">    Log.e(&quot;TAG&quot;, &quot;lowMemory：&quot; + memoryInfo.lowMemory);</span><br><span class="line">    Log.e(&quot;TAG&quot;, &quot;NativeHeapAllocatedSize :&quot; + Debug.getNativeHeapAllocatedSize() / 1024 / 1024);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面我们创建了一张 2G 大小的 bitmap 我们在 8.0 以下的版本运行是会 OOM 的，而我们在 8.0 以上的版本运行是完全没问题，但 Native 内存多了 2G 的内存。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">E/TAG: AvailMem :1654</span><br><span class="line">E/TAG: lowMemory：false</span><br><span class="line">E/TAG: NativeHeapAllocatedSize :4</span><br><span class="line"></span><br><span class="line">E/TAG: AvailMem :1656</span><br><span class="line">E/TAG: lowMemory：false</span><br><span class="line">E/TAG: NativeHeapAllocatedSize :2052</span><br></pre></td></tr></table></figure>
<p>通过之前的源码分析可知 bitmap 的内存创建都是通过 <code>tryAllocPixels</code> 方法来申请的</p>
<p>8.0 以上像素内存在 Native 内存中</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">auto wrapper = alloc(size, info, rowBytes, ctable);</span><br></pre></td></tr></table></figure>
<p>看一下源码（8.0以上）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">类 `BitmapFactory.cpp`.doDecode(JNIEnv* env, std::unique_ptr&lt;SkStreamRewindable&gt; stream,</span><br><span class="line">                        jobject padding, jobject options, jlong inBitmapHandle,</span><br><span class="line">                        jlong colorSpaceHandle)</span><br><span class="line"></span><br><span class="line">static jobject doDecode(JNIEnv* env, std::unique_ptr&lt;SkStreamRewindable&gt; stream,</span><br><span class="line">                        jobject padding, jobject options, jlong inBitmapHandle,</span><br><span class="line">                        jlong colorSpaceHandle) &#123;</span><br><span class="line">    </span><br><span class="line">    // 原图</span><br><span class="line">    SkBitmap decodingBitmap;</span><br><span class="line">    if (!decodingBitmap.setInfo(bitmapInfo) ||</span><br><span class="line">            !decodingBitmap.tryAllocPixels(decodeAllocator)) &#123; // tryAllocPixels 原图加载在了 native 层</span><br><span class="line">        // SkAndroidCodec should recommend a valid SkImageInfo, so setInfo()</span><br><span class="line">        // should only only fail if the calculated value for rowBytes is too</span><br><span class="line">        // large.</span><br><span class="line">        // tryAllocPixels() can fail due to OOM on the Java heap, OOM on the</span><br><span class="line">        // native heap, or the recycled javaBitmap being too small to reuse.</span><br><span class="line">        return nullptr;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // Use SkAndroidCodec to perform the decode.</span><br><span class="line">    SkAndroidCodec::AndroidOptions codecOptions;</span><br><span class="line">    codecOptions.fZeroInitialized = decodeAllocator == &amp;defaultAllocator ?</span><br><span class="line">            SkCodec::kYes_ZeroInitialized : SkCodec::kNo_ZeroInitialized;</span><br><span class="line">    codecOptions.fSampleSize = sampleSize;</span><br><span class="line">    SkCodec::Result result = codec-&gt;getAndroidPixels(decodeInfo, decodingBitmap.getPixels(),</span><br><span class="line">            decodingBitmap.rowBytes(), &amp;codecOptions);</span><br><span class="line">    switch (result) &#123;</span><br><span class="line">        case SkCodec::kSuccess:</span><br><span class="line">        case SkCodec::kIncompleteInput:</span><br><span class="line">            break;</span><br><span class="line">        default:</span><br><span class="line">            return nullObjectReturn(&quot;codec-&gt;getAndroidPixels() failed.&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    ...</span><br><span class="line">    </span><br><span class="line">    // 返回的图片</span><br><span class="line">    SkBitmap outputBitmap;</span><br><span class="line">    if (willScale) &#123; // 如果有压缩</span><br><span class="line">        // Set the allocator for the outputBitmap.</span><br><span class="line">        SkBitmap::Allocator* outputAllocator;</span><br><span class="line">        if (javaBitmap != nullptr) &#123;</span><br><span class="line">            outputAllocator = &amp;recyclingAllocator;</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            outputAllocator = &amp;defaultAllocator;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        SkColorType scaledColorType = decodingBitmap.colorType();</span><br><span class="line">        // FIXME: If the alphaType is kUnpremul and the image has alpha, the</span><br><span class="line">        // colors may not be correct, since Skia does not yet support drawing</span><br><span class="line">        // to/from unpremultiplied bitmaps.</span><br><span class="line">        outputBitmap.setInfo(</span><br><span class="line">                bitmapInfo.makeWH(scaledWidth, scaledHeight).makeColorType(scaledColorType));</span><br><span class="line">        if (!outputBitmap.tryAllocPixels(outputAllocator)) &#123; // 输出的图片加载在 navtive 层</span><br><span class="line">            // This should only fail on OOM.  The recyclingAllocator should have</span><br><span class="line">            // enough memory since we check this before decoding using the</span><br><span class="line">            // scaleCheckingAllocator.</span><br><span class="line">            return nullObjectReturn(&quot;allocation failed for scaled bitmap&quot;);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        SkPaint paint;</span><br><span class="line">        // kSrc_Mode instructs us to overwrite the uninitialized pixels in</span><br><span class="line">        // outputBitmap.  Otherwise we would blend by default, which is not</span><br><span class="line">        // what we want.</span><br><span class="line">        paint.setBlendMode(SkBlendMode::kSrc);</span><br><span class="line">        paint.setFilterQuality(kLow_SkFilterQuality); // bilinear filtering</span><br><span class="line"></span><br><span class="line">        SkCanvas canvas(outputBitmap, SkCanvas::ColorBehavior::kLegacy);</span><br><span class="line">        canvas.scale(scaleX, scaleY);</span><br><span class="line">        canvas.drawBitmap(decodingBitmap, 0.0f, 0.0f, &amp;paint);</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">        outputBitmap.swap(decodingBitmap); // 否则返回原图的复制对象，也是 native 层</span><br><span class="line">    &#125;</span><br><span class="line">       </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-Bitmap-内存的回收"><a href="#4-Bitmap-内存的回收" class="headerlink" title="4. Bitmap 内存的回收"></a>4. Bitmap 内存的回收</h1><h2 id="4-1-recycle-方法"><a href="#4-1-recycle-方法" class="headerlink" title="4.1 recycle 方法"></a>4.1 recycle 方法</h2><p>如果是 <code>8.0</code> 我们手动调用 <code>recycle</code> 方法，<code>数据是会立即释放的</code>，因为像素数据本身就是在 Native 层开辟的。但如果是在 <code>8.0 以下</code>，就算我们手动调用 recycle 方法，<code>数据也是不会立即释放的</code>，<br>而是 DeleteWeakGlobalRef <code>交由 Java GC 来回收</code>。建议大家翻译一下 recycle 方法注释。注意：以上的所说的<code>释放数据仅代表释放像素数据</code>，并未释放 Native 层的 Bitmap 对象。</p>
<h2 id="4-2-不手动调用-recycle-方法时Native-层的-Bitmap-是怎么回收"><a href="#4-2-不手动调用-recycle-方法时Native-层的-Bitmap-是怎么回收" class="headerlink" title="4.2 不手动调用 recycle 方法时Native 层的 Bitmap 是怎么回收"></a>4.2 不手动调用 recycle 方法时Native 层的 Bitmap 是怎么回收</h2><ul>
<li>Android M（6.0） 版本及以前的版本, Bitmap 的内存回收主要是通过 BitmapFinalizer 来完成的(类似于 GC 时 Object 的finalize()方法)</li>
<li>Android M（6.0） 以上版本 虽然没有了 BitmapFinalizer 类,但在 new Bitmap 时会注册 native 的 Finalizer 方法</li>
</ul>
<p>总结：其实无论是 Android M 前还是之后，释放 Native 层的 Bitmap 对象的思想都是去监听 Java 层的 Bitmap 是否被释放，一旦当 Java 层的 Bitmap 对象被释放则立即去释放 Native 层的 Bitmap 。只不过 Android M 前是基于 Java 的 GC 机制，而 Android M 后是注册 native 的 Finalizer 方法。</p>
<h1 id="5-Bitmap-的内存复用"><a href="#5-Bitmap-的内存复用" class="headerlink" title="5.Bitmap 的内存复用"></a>5.Bitmap 的内存复用</h1><p>内存复用好处：<code>防止反复的去开辟和释放内存，内存抖动 -&gt; GC -&gt; 卡顿</code></p>
<p>Bitmap 绝对是我们 Android 开发中最容易引起 OOM 的对象之一，因为其占用的像素数据内存比较大，而加载图片又是很常见的操作。如果不断反复的去开辟和销毁 Bitmap 数据内存，势必可能会引起应用的内存抖动，因此 Google 的开发者也为我们想了一些办法，那就是允许 Bitmap 内存复用，具体如下：</p>
<ul>
<li>被复用的 Bitmap <code>必须为 Mutable（通过 BitmapFactory.Options 设置），然后 options.inBitmap = bitmap1，设置 inBitmap复用的Bitmap是谁</code></li>
<li>4.4 之前，将要解码的图像（无论是资源还是流）必须是 jpeg 或 png 格式且和被复用的 Bitmap 大小一样，其中BitmapFactory.Options#inSampleSize 字段必须设置为 1，要求比较严苛</li>
<li>4.4 以后，将要解码的图像的内存需要小于等于要复用的 Bitmap 的内存</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 不复用的写法，消耗内存 32 M</span><br><span class="line">logMemory();</span><br><span class="line">Bitmap bitmap1 = BitmapFactory.decodeResource(getResources(), R.drawable.test2);</span><br><span class="line">Bitmap bitmap2 = BitmapFactory.decodeResource(getResources(), R.drawable.test2);</span><br><span class="line">logMemory();</span><br><span class="line">// 复用的写法，消耗内存 16 M</span><br><span class="line">logMemory();</span><br><span class="line">BitmapFactory.Options options = new BitmapFactory.Options();</span><br><span class="line">options.inMutable = true;</span><br><span class="line">Bitmap bitmap1 = BitmapFactory.decodeResource(getResources(), R.drawable.test2, options);</span><br><span class="line">options.inBitmap = bitmap1;</span><br><span class="line">Bitmap bitmap2 = BitmapFactory.decodeResource(getResources(), R.drawable.test2, options);</span><br><span class="line">logMemory();</span><br></pre></td></tr></table></figure>
<h1 id="6-xh，xxh，xxxh-放哪个文件夹更高效"><a href="#6-xh，xxh，xxxh-放哪个文件夹更高效" class="headerlink" title="6. xh，xxh，xxxh 放哪个文件夹更高效"></a>6. xh，xxh，xxxh 放哪个文件夹更高效</h1><ul>
<li>放当前主流的  xxh , native 源码中只会开辟一次内存</li>
</ul>
<h1 id="7-Bitmap-的内存优化与适配"><a href="#7-Bitmap-的内存优化与适配" class="headerlink" title="7.Bitmap 的内存优化与适配"></a>7.Bitmap 的内存优化与适配</h1><p>做效果的时候，最好按比例去计算</p>
<h1 id="8-线上-OOM-的-dump-收集"><a href="#8-线上-OOM-的-dump-收集" class="headerlink" title="8.线上 OOM 的 dump 收集"></a>8.线上 OOM 的 dump 收集</h1><p>首先 <code>OOM</code> 是可以 <code>try catch</code> 的,但是一般不这么做。<br>可以在 崩溃日志收集那儿 之前进行 dump 内存，怎么 dump 内存 可以参考 leakCanny</p>
]]></content>
      <categories>
        <category>Bitmap</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>Bitmap</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：06.中值滤波、腐蚀、膨胀、实现图片的美容效果-双边滤波</title>
    <url>/54.OpenCV-%E4%B8%AD%E5%80%BC%E6%BB%A4%E6%B3%A2%E3%80%81%E8%85%90%E8%9A%80%E3%80%81%E8%86%A8%E8%83%80%E3%80%81%E5%AE%9E%E7%8E%B0%E5%9B%BE%E7%89%87%E7%9A%84%E7%BE%8E%E5%AE%B9%E6%95%88%E6%9E%9C-%E5%8F%8C%E8%BE%B9%E6%BB%A4%E6%B3%A2/</url>
    <content><![CDATA[<h1 id="1-中值滤波与双边滤波"><a href="#1-中值滤波与双边滤波" class="headerlink" title="1. 中值滤波与双边滤波"></a>1. 中值滤波与双边滤波</h1><ul>
<li><code>medianBlur</code> 中值 对所有考虑点进行排序，取中间的那个值（替换到矩阵的中心点）   1,2,”3”,4,5  去掉噪音（很多 黑白点的情况）</li>
<li><code>bilateralFilter</code> 双边  可以用作美容 ，基于高斯，高斯保留的轮廓并不强，双边保留的轮廓信息会增强，基于高斯再增加像素差</li>
</ul>
<p>使用到的 api</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// @params d : 滤波过程中使用的每个像素邻域的直径。如果它是非正的，它是由sigmaSpace计算的。</span></span><br><span class="line"><span class="comment">// @param sigmaColor ： 滤色空间中的。参数的值越大意味着，像素附近的更远的颜色(参见sigmaSpace)将被混合在一起，得到结果 在更大的区域半相等的颜色。</span></span><br><span class="line"><span class="comment">// @param sigmaSpace: 在坐标空间中过滤。参数的值越大意味着距离较远的像素会相互影响，只要它们的颜色足够接近(参见sigmaColor))。当d &gt; 0时，它指定邻域大小，而与sigmaSpace无关。否则,d是与sigmaSpace成正比。</span></span><br><span class="line"><span class="function">CV_EXPORTS_W <span class="type">void</span> <span class="title">bilateralFilter</span><span class="params">( InputArray src, OutputArray dst, <span class="type">int</span> d,</span></span></span><br><span class="line"><span class="params"><span class="function">                                   <span class="type">double</span> sigmaColor, <span class="type">double</span> sigmaSpace,</span></span></span><br><span class="line"><span class="params"><span class="function">                                   <span class="type">int</span> borderType = BORDER_DEFAULT )</span></span>;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;src read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	Mat dst;</span><br><span class="line">	 <span class="built_in">blur</span>(src, dst, <span class="built_in">Size</span>(<span class="number">15</span>, <span class="number">15</span>));</span><br><span class="line"></span><br><span class="line">	 中值滤波 对所有考虑点进行排序，取中间的那个值   <span class="number">1</span>,<span class="number">2</span>,“<span class="number">3</span>”,<span class="number">4</span>,<span class="number">5</span>  去掉噪音</span><br><span class="line">	 <span class="built_in">medianBlur</span>(src, dst,<span class="number">5</span>);</span><br><span class="line"></span><br><span class="line">	 双边滤波 可以用作美容 ，基于高斯，高斯保留的轮廓并不强，双边保留的轮廓信息会增强，基于高斯再增加像素差</span><br><span class="line">	<span class="built_in">bilateralFilter</span>(src,dst,<span class="number">15</span>,<span class="number">100</span>,<span class="number">5</span>);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>, dst);</span><br><span class="line"></span><br><span class="line">	 掩膜操作</span><br><span class="line">	Mat <span class="keyword">final</span>;</span><br><span class="line">	Mat kernal = (<span class="built_in">Mat_</span>&lt;<span class="type">int</span>&gt;(<span class="number">3</span>, <span class="number">3</span>) &lt;&lt; <span class="number">0</span>, <span class="number">-1</span>, <span class="number">0</span>, <span class="number">-1</span>, <span class="number">5</span>, <span class="number">-1</span>, <span class="number">0</span>, <span class="number">-1</span>, <span class="number">0</span>);</span><br><span class="line">	<span class="built_in">filter2D</span>(dst,<span class="keyword">final</span>,dst.<span class="built_in">depth</span>(),kernal);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;final&quot;</span>,<span class="keyword">final</span>);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-腐蚀与膨胀"><a href="#2-腐蚀与膨胀" class="headerlink" title="2. 腐蚀与膨胀"></a>2. 腐蚀与膨胀</h1><p>数学形态学，与卷积操作类似，用的矩形 （E 求和），用的是任意形状，矩形，十字形，椭圆形，等等 最大值，最小值，中值，最大值-最小值</p>
<p>膨胀定义：<br>　　膨胀—-图像膨胀的过程类似于一个卷积的过程，假设有图像矩阵A以及结构元素B（注意，B的形状、尺寸没有限制），B在A矩阵上依次移动，每个位置上B所覆盖元素的<code>最大值</code>替换B的中心位置值（即锚点处），即为膨胀的过程。</p>
<p>腐蚀定义：<br>    腐蚀—-图像膨胀的过程类似于一个卷积的过程，假设有图像矩阵A以及结构元素B（注意，B的形状、尺寸没有限制），B在A矩阵上依次移动，每个位置上B所覆盖元素的<code>最小值</code>替换B的中心位置值（即锚点处），即为腐蚀的过程。</p>
<ul>
<li>erode 腐蚀 ，最小值（去白点）</li>
<li>dilate 膨胀 ， 最大值(去黑点)</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="type">int</span> element_size = <span class="number">1</span>;</span><br><span class="line"><span class="type">int</span> max_size = <span class="number">21</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 膨胀与腐蚀，处理黑白图片</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">trackbarCallback</span><span class="params">(<span class="type">int</span> pos, <span class="type">void</span> *userdata)</span></span>&#123;</span><br><span class="line">	<span class="type">int</span> size = element_size * <span class="number">2</span> + <span class="number">1</span>;</span><br><span class="line">	<span class="comment">// 创建一个 kernel</span></span><br><span class="line">	Mat kernel = <span class="built_in">getStructuringElement</span>(MORPH_RECT, <span class="built_in">Size</span>(size, size));</span><br><span class="line">	Mat dst;</span><br><span class="line"></span><br><span class="line">	<span class="comment">//膨胀 ， 最大值(去黑点)</span></span><br><span class="line">	<span class="built_in">dilate</span>(src, dst, kernel);</span><br><span class="line">	<span class="comment">// 腐蚀 ，最小值 （去白点）</span></span><br><span class="line">	<span class="comment">// erode(src,dst,kernel);</span></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;output image&quot;</span>, dst);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;src read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line">	</span><br><span class="line">	<span class="comment">// 创建一个 window</span></span><br><span class="line">	<span class="built_in">namedWindow</span>(<span class="string">&quot;output image&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 动态控制</span></span><br><span class="line">	<span class="built_in">createTrackbar</span>(<span class="string">&quot;Trackbar&quot;</span>,<span class="string">&quot;output image&quot;</span>,&amp;element_size,max_size,trackbarCallback);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：03.Mat对象和图像混合</title>
    <url>/50.OpenCV-Mat%E5%AF%B9%E8%B1%A1%E5%92%8C%E5%9B%BE%E5%83%8F%E6%B7%B7%E5%90%88/</url>
    <content><![CDATA[<h1 id="1-mat-常见用法"><a href="#1-mat-常见用法" class="headerlink" title="1. mat 常见用法"></a>1. mat 常见用法</h1><p>Mat src</p>
<ul>
<li>src.empty() 判断mat是否为空，搭配<code>imread</code>判断文件是否存在</li>
<li>src.data 头指针，通过判断是否为 NULL 也能判断图片是否存在</li>
</ul>
<p>Mat的构造函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Mat的构造函数</span></span><br><span class="line"><span class="comment">// CV_8UC1 1个颜色的通道  (0-255)</span></span><br><span class="line"><span class="comment">// CV_8UC2 2个颜色的通道  16位 RGB565</span></span><br><span class="line"><span class="comment">// CV_8UC3 3个颜色的通道  24位</span></span><br><span class="line"><span class="comment">// CV_8UC4 4个颜色的通道  32位 ARGB8888</span></span><br><span class="line"><span class="comment">// 匹配上 Java Bitmap 的颜色通道 RGB565 ARGB8888</span></span><br><span class="line"><span class="comment">// Scalar 指定颜色</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 第一个参数 rows 高，第二个参数 cols 宽</span></span><br><span class="line"><span class="function">Mat <span class="title">mat</span><span class="params">(<span class="number">500</span>,<span class="number">300</span>,CV_8UC1,Scalar(<span class="number">255</span>))</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Size 第一个参数是 width , 第二个参数是 height</span></span><br><span class="line"><span class="function">Mat <span class="title">mat1</span><span class="params">(Size(<span class="number">300</span>,<span class="number">500</span>),CV_8UC1,Scalar(<span class="number">255</span>))</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//三颜色通道</span></span><br><span class="line"><span class="function">Mat <span class="title">mat2</span><span class="params">(Size(<span class="number">300</span>,<span class="number">500</span>),CV_8UC3,Scalar(<span class="number">255</span>,<span class="number">0</span>,<span class="number">255</span>))</span></span>;</span><br></pre></td></tr></table></figure>
<p>注意：以下几个拷贝的区别</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 拷贝构造函数，但是不会去拷贝内容操作的还是原来的图</span></span><br><span class="line"><span class="comment">// Mat dst(src);</span></span><br><span class="line"><span class="comment">// Mat dst = src;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 会拷贝内容</span></span><br><span class="line">Mat dst;</span><br><span class="line"><span class="comment">// src.copyTo(dst);</span></span><br><span class="line">dst = src.<span class="built_in">clone</span>();</span><br></pre></td></tr></table></figure>
<h1 id="2-图像操作"><a href="#2-图像操作" class="headerlink" title="2. 图像操作"></a>2. 图像操作</h1><p>图像截取(不涉及创建新的内容)</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 区域截取(不涉及创建新的内容)</span></span><br><span class="line">Mat srcROT = <span class="built_in">src</span>(<span class="built_in">Rect</span>(<span class="number">20</span>,<span class="number">20</span>,<span class="number">400</span>,<span class="number">400</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 不改变原图</span></span><br><span class="line">Mat dstROI = srcROT.<span class="built_in">clone</span>();</span><br></pre></td></tr></table></figure>
<p>读取像素</p>
<p><code>mat.at&lt;Vec3b&gt;(i,j)</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/test.jpg&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 区域截取(不涉及创建新的内容)</span></span><br><span class="line">	Mat srcROT = <span class="built_in">src</span>(<span class="built_in">Rect</span>(<span class="number">20</span>,<span class="number">20</span>,<span class="number">400</span>,<span class="number">400</span>));</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 不改变原图</span></span><br><span class="line">	Mat dstROI = srcROT.<span class="built_in">clone</span>();</span><br><span class="line"></span><br><span class="line">	<span class="type">int</span> rows = dstROI.rows;</span><br><span class="line">	<span class="type">int</span> cols = dstROI.cols;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; rows; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; cols; j++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="comment">// 获取像素 at Vec3b 3个字节</span></span><br><span class="line">			<span class="type">int</span> b = dstROI.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">0</span>];</span><br><span class="line">			<span class="type">int</span> g = dstROI.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">1</span>];</span><br><span class="line">			<span class="type">int</span> r = dstROI.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">			<span class="comment">// 底片效果</span></span><br><span class="line">			dstROI.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">0</span>] = <span class="number">255</span> - b;</span><br><span class="line">			dstROI.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">1</span>] = <span class="number">255</span> - g;</span><br><span class="line">			dstROI.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">2</span>] = <span class="number">255</span> - r;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src,gray,COLOR_BGR2GRAY); <span class="comment">// 这样转过来是1个通道</span></span><br><span class="line">	<span class="comment">// 获取信息</span></span><br><span class="line">	<span class="type">int</span> cols = gray.cols; <span class="comment">// 宽</span></span><br><span class="line">	<span class="type">int</span> rows = gray.rows; <span class="comment">// 高</span></span><br><span class="line">	<span class="type">int</span> chnnales = gray.<span class="built_in">channels</span>();</span><br><span class="line">	cout &lt;&lt; cols &lt;&lt; <span class="string">&quot;,&quot;</span> &lt;&lt; rows &lt;&lt; <span class="string">&quot;,&quot;</span> &lt;&lt; chnnales &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// Bitmap 里面转的是 4 通道，一个通道就可以代表灰度</span></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; rows; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; cols; j++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">if</span> (chnnales == <span class="number">3</span>)&#123;</span><br><span class="line">				<span class="type">int</span> b = src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">0</span>];</span><br><span class="line">				<span class="type">int</span> g = src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">1</span>];</span><br><span class="line">				<span class="type">int</span> r = src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">				<span class="comment">// 底片效果</span></span><br><span class="line">				gray.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">0</span>] = <span class="number">255</span> - b;</span><br><span class="line">				gray.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">1</span>] = <span class="number">255</span> - g;</span><br><span class="line">				gray.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">2</span>] = <span class="number">255</span> - r;</span><br><span class="line">			&#125;<span class="keyword">else</span> <span class="keyword">if</span> (chnnales == <span class="number">1</span>)&#123;</span><br><span class="line">				uchar pixels = gray.<span class="built_in">at</span>&lt;uchar&gt;(i, j);</span><br><span class="line">				gray.<span class="built_in">at</span>&lt;uchar&gt;(i, j) = <span class="number">255</span> - pixels;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 手写一个通道的灰度图</span></span><br><span class="line">	<span class="function">Mat <span class="title">gray</span><span class="params">(src.rows, src.cols, CV_8UC1)</span></span>;</span><br><span class="line">	<span class="type">int</span> cols = gray.cols; <span class="comment">// 宽</span></span><br><span class="line">	<span class="type">int</span> rows = gray.rows; <span class="comment">// 高</span></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; rows; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; cols; j++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="type">int</span> b = src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">0</span>];</span><br><span class="line">			<span class="type">int</span> g = src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">1</span>];</span><br><span class="line">			<span class="type">int</span> r = src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">			<span class="comment">// 底片效果</span></span><br><span class="line">			gray.<span class="built_in">at</span>&lt;uchar&gt;(i, j) = <span class="number">0.11f</span>*r + <span class="number">0.59f</span>*g + <span class="number">0.3f</span>*b;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imwrite</span>(<span class="string">&quot;E:/test_onegray.jpg&quot;</span>,gray);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;test&quot;</span>, gray);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-图像混合"><a href="#3-图像混合" class="headerlink" title="3. 图像混合"></a>3. 图像混合</h1><p><code>mat.add(src1,src2,dst)</code>  注意两张图片的大小必须一致（效果很生硬，直接是像素相加）</p>
<p>不一定非得是 alpha + beta = 1; dst(x) = saturate_cast(src(x)<em>alpha + logo(x)</em>beta + gamma)<br><code>addWeighted(src,0.8,logo,0.2,0.0,dst)</code> //注意两张图片的大小必须一致</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/test.jpg&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (src.<span class="built_in">empty</span>())&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;src read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	Mat logo = <span class="built_in">imread</span>(<span class="string">&quot;E:/android.png&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (logo.<span class="built_in">empty</span>())&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;logo read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="comment">// add 方法：注意两张图片的大小必须一致（效果很生硬，直接是像素相加）</span></span><br><span class="line">	<span class="comment">// add(src,logo,dst);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 不一定非得是 alpha + beta = 1; dst(x) = saturate_cast(src(x)*alpha + logo(x)*beta + gamma)</span></span><br><span class="line">	<span class="comment">// addWeighted 方法：注意两张图片的大小必须一致</span></span><br><span class="line">	<span class="comment">// addWeighted(src,0.8,logo,0.2,0.0,dst);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 非得加图片不一样的怎么办，通过截取的方式进行图片合并</span></span><br><span class="line">	Mat srcROI = <span class="built_in">src</span>(<span class="built_in">Rect</span>(<span class="number">0</span>, <span class="number">0</span>, logo.cols, logo.rows));</span><br><span class="line">	<span class="comment">// 并不适合加水印，只适合做图片的混合</span></span><br><span class="line">	<span class="built_in">addWeighted</span>(srcROI, <span class="number">0.8</span>, logo, <span class="number">1</span>, <span class="number">0.0</span>, srcROI);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;test&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-饱和度、亮度和对比度"><a href="#4-饱和度、亮度和对比度" class="headerlink" title="4.饱和度、亮度和对比度"></a>4.饱和度、亮度和对比度</h1><p>概念</p>
<ul>
<li>饱和度：指的其实是色彩的纯度，纯度越高，表现越鲜明，纯度较低，表现则较黯淡。 红色，淡红，鲜红，红得发紫等等   R -&gt; 1.2R B -&gt; 1.2B G -&gt; 1.2G</li>
<li>亮度：亮暗的程度</li>
<li>对比度: 图像对比度指的是 [1]  一幅图像中明暗区域最亮的白和最暗的黑之间不同亮度层级的测量，即指一幅图像灰度反差的大小</li>
</ul>
<p>修改像素：<code>alpha*R(G B) + beta;</code>   alpha控制饱和度和对比度，beta控制亮度</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/test.jpg&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (src.<span class="built_in">empty</span>())&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;src read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 滤镜 UI设计师 再调亮一点 ，饱和一点，对比度再调高一点？ alpha 增大成比例去增 1:500  10:5000  beta增</span></span><br><span class="line">	<span class="comment">// alpha 饱和度 , 对比度</span></span><br><span class="line">	<span class="comment">// beta 亮度</span></span><br><span class="line">	<span class="comment">// F(R) = alpha*R + beta;</span></span><br><span class="line">	<span class="comment">// F(G) = alpha*G + beta;</span></span><br><span class="line">	<span class="comment">// F(B) = alpha*B + beta;</span></span><br><span class="line">	<span class="comment">// 获取信息</span></span><br><span class="line">	<span class="type">int</span> cols = src.cols;<span class="comment">// 宽</span></span><br><span class="line">	<span class="type">int</span> rows = src.rows;<span class="comment">// 高</span></span><br><span class="line">	<span class="type">int</span> chnnales = src.<span class="built_in">channels</span>();<span class="comment">// 1 </span></span><br><span class="line"></span><br><span class="line">	<span class="type">int</span> alpha = <span class="number">3</span>;</span><br><span class="line">	<span class="type">int</span> beta = <span class="number">-50</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; rows; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; cols; j++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="type">int</span> b = src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">0</span>];</span><br><span class="line">			<span class="type">int</span> g = src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">1</span>];</span><br><span class="line">			<span class="type">int</span> r = src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">			</span><br><span class="line">			src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">0</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(alpha*b + beta);</span><br><span class="line">			src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">1</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(alpha*g + beta);</span><br><span class="line">			src.<span class="built_in">at</span>&lt;Vec3b&gt;(i, j)[<span class="number">2</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(alpha*r + beta);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;test&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：12.直方图比较和匹配</title>
    <url>/60.OpenCV-%E7%9B%B4%E6%96%B9%E5%9B%BE%E6%AF%94%E8%BE%83%E5%92%8C%E5%8C%B9%E9%85%8D/</url>
    <content><![CDATA[<h1 id="1-直方图比较-（compareHist-2组数据去对比）"><a href="#1-直方图比较-（compareHist-2组数据去对比）" class="headerlink" title="1. 直方图比较 （compareHist 2组数据去对比）"></a>1. 直方图比较 （compareHist 2组数据去对比）</h1><p><code>compareHist( const SparseMat&amp; H1, const SparseMat&amp; H2, int method )</code></p>
<h2 id="1-1-相关性比较-HISTCMP-CORREL-（多少的时候是最好的-1）-N-bins-直方图的等级"><a href="#1-1-相关性比较-HISTCMP-CORREL-（多少的时候是最好的-1）-N-bins-直方图的等级" class="headerlink" title="1.1 相关性比较 HISTCMP_CORREL （多少的时候是最好的  1）  N bins  直方图的等级"></a>1.1 相关性比较 HISTCMP_CORREL （多少的时候是最好的  1）  N bins  直方图的等级</h2><p><img src="/images/60.相关性比较.png" alt="">  <img src="/images/60.相关性比较2.png" alt=""></p>
<h2 id="1-2-卡方比较-HISTCMP-CHISQR-多少的时候是最好的-0"><a href="#1-2-卡方比较-HISTCMP-CHISQR-多少的时候是最好的-0" class="headerlink" title="1.2 卡方比较 HISTCMP_CHISQR    (多少的时候是最好的  0)"></a>1.2 卡方比较 HISTCMP_CHISQR    (多少的时候是最好的  0)</h2><p><img src="/images/60.卡方比较.png" alt=""></p>
<h2 id="1-3-十字交叉性-HISTCMP-INTERSECT"><a href="#1-3-十字交叉性-HISTCMP-INTERSECT" class="headerlink" title="1.3 十字交叉性 HISTCMP_INTERSECT"></a>1.3 十字交叉性 HISTCMP_INTERSECT</h2><p><img src="/images/60.十字交叉性比较.png" alt=""></p>
<h2 id="1-4-巴氏距离-CV-COMP-BHATTACHARYYA-多少的时候是最好的-0"><a href="#1-4-巴氏距离-CV-COMP-BHATTACHARYYA-多少的时候是最好的-0" class="headerlink" title="1.4 巴氏距离 CV_COMP_BHATTACHARYYA  (多少的时候是最好的  0)"></a>1.4 巴氏距离 CV_COMP_BHATTACHARYYA  (多少的时候是最好的  0)</h2><p><img src="/images/60.巴式距离.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	<span class="comment">// 三张图</span></span><br><span class="line">	Mat src1 = <span class="built_in">imread</span>(<span class="string">&quot;E:/test.jpg&quot;</span>);</span><br><span class="line">	Mat src2 = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line">	Mat src3 = <span class="built_in">imread</span>(<span class="string">&quot;E:/test3.jpg&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src<span class="number">1.</span>data || !src<span class="number">2.</span>data || !src<span class="number">3.</span>data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// RGB -&gt; HSV , 计算 HS 直方图3</span></span><br><span class="line">	Mat hsv1, hsv2, hsv3;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src1, hsv1, COLOR_BGR2HSV);</span><br><span class="line">	<span class="built_in">cvtColor</span>(src2, hsv2, COLOR_BGR2HSV);</span><br><span class="line">	<span class="built_in">cvtColor</span>(src3, hsv3, COLOR_BGR2HSV);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 计算直方图3（取 h 和 s 两个）</span></span><br><span class="line">	<span class="type">int</span> channels[] = &#123; <span class="number">0</span>, <span class="number">1</span> &#125;;</span><br><span class="line">	<span class="comment">// 复数（h的直方图放一个  s的直方图放一个）</span></span><br><span class="line">	MatND hist1, hist2, hist3;</span><br><span class="line">	<span class="type">int</span> h_bins = <span class="number">50</span>; <span class="comment">// h 的等级个数 </span></span><br><span class="line">	<span class="type">int</span> s_bins = <span class="number">50</span>; <span class="comment">// s 的等级个数</span></span><br><span class="line">	<span class="type">int</span> hist_size[] = &#123; h_bins, s_bins &#125;;</span><br><span class="line">	<span class="type">float</span> h_rangs[] = &#123; <span class="number">0</span>, <span class="number">180</span> &#125;;<span class="comment">// h 通道 最大不会超过 180</span></span><br><span class="line">	<span class="type">float</span> s_rangs[] = &#123; <span class="number">0</span>, <span class="number">255</span> &#125;;<span class="comment">// s 通过 最大不超过 255</span></span><br><span class="line">	<span class="type">const</span> <span class="type">float</span> *rangs[] = &#123; h_rangs, s_rangs &#125;;</span><br><span class="line">	<span class="built_in">calcHist</span>(&amp;hsv1, <span class="number">1</span>, channels, <span class="built_in">Mat</span>(), hist1, <span class="number">2</span>, hist_size, rangs);</span><br><span class="line">	<span class="built_in">calcHist</span>(&amp;hsv2, <span class="number">1</span>, channels, <span class="built_in">Mat</span>(), hist2, <span class="number">2</span>, hist_size, rangs);</span><br><span class="line">	<span class="built_in">calcHist</span>(&amp;hsv3, <span class="number">1</span>, channels, <span class="built_in">Mat</span>(), hist3, <span class="number">2</span>, hist_size, rangs);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 归一化  0 - 1</span></span><br><span class="line">	<span class="built_in">normalize</span>(hist1, hist1, <span class="number">0</span>, <span class="number">1</span>, NORM_MINMAX);</span><br><span class="line">	<span class="built_in">normalize</span>(hist2, hist2, <span class="number">0</span>, <span class="number">1</span>, NORM_MINMAX);</span><br><span class="line">	<span class="built_in">normalize</span>(hist2, hist2, <span class="number">0</span>, <span class="number">1</span>, NORM_MINMAX);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 两个直方图比较</span></span><br><span class="line">	<span class="comment">// compareHist( const SparseMat&amp; H1, const SparseMat&amp; H2, int method )</span></span><br><span class="line">	<span class="type">double</span> hist1_hist1 = <span class="built_in">compareHist</span>(hist1, hist1, HISTCMP_CORREL); <span class="comment">// 最好的</span></span><br><span class="line">	<span class="type">double</span> hist1_hist2 = <span class="built_in">compareHist</span>(hist1, hist2, HISTCMP_CORREL); <span class="comment">// 接近于 1</span></span><br><span class="line">	<span class="type">double</span> hist1_hist3 = <span class="built_in">compareHist</span>(hist1, hist3, HISTCMP_CORREL); <span class="comment">// &lt; 0.8</span></span><br><span class="line"></span><br><span class="line">	cout &lt;&lt; hist1_hist1 &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; hist1_hist2 &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; hist1_hist3 &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src1&quot;</span>, src1);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src2&quot;</span>, src2);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src3&quot;</span>, src3);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-直方图反向投射-反射投影的次数-（不是像素值）"><a href="#2-直方图反向投射-反射投影的次数-（不是像素值）" class="headerlink" title="2. 直方图反向投射 (反射投影的次数 （不是像素值）)"></a>2. 直方图反向投射 (反射投影的次数 （不是像素值）)</h1><p><code>可用于截取</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">calcBackProject</span>(<span class="type">const</span> Mat* images, <span class="type">int</span> nimages,</span><br><span class="line">		<span class="type">const</span> <span class="type">int</span>* channels, InputArray hist,</span><br><span class="line">		OutputArray backProject, <span class="type">const</span> <span class="type">float</span>** ranges,</span><br><span class="line">		<span class="type">double</span> scale = <span class="number">1</span>, <span class="type">bool</span> uniform = <span class="literal">true</span>);</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Mat hueImage;</span><br><span class="line"><span class="type">int</span> bins = <span class="number">2</span>;</span><br><span class="line"><span class="type">int</span> max_bins = <span class="number">180</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">hist_backprojection</span><span class="params">(<span class="type">int</span>, <span class="type">void</span>*)</span></span>&#123;</span><br><span class="line">	<span class="comment">// 直方图反向投影到 Mat</span></span><br><span class="line">	<span class="comment">// calcBackProject</span></span><br><span class="line">	<span class="comment">//calcBackProject(const Mat* images, int nimages,</span></span><br><span class="line">	<span class="comment">//	const int* channels, InputArray hist,</span></span><br><span class="line">	<span class="comment">//	OutputArray backProject, const float** ranges,</span></span><br><span class="line">	<span class="comment">//	double scale = 1, bool uniform = true);</span></span><br><span class="line"></span><br><span class="line">	Mat hist;</span><br><span class="line">	<span class="type">int</span> hist_size = <span class="built_in">MAX</span>(bins,<span class="number">2</span>);</span><br><span class="line">	<span class="type">float</span> hue_rang[] = &#123; <span class="number">0</span>, <span class="number">180</span> &#125;;</span><br><span class="line">	<span class="type">const</span> <span class="type">float</span> *ranges[] = &#123; hue_rang &#125;;</span><br><span class="line">	<span class="built_in">calcHist</span>(&amp;hueImage, <span class="number">1</span>, <span class="number">0</span>, <span class="built_in">Mat</span>(), hist, <span class="number">1</span>, &amp;hist_size, ranges);</span><br><span class="line">	<span class="built_in">normalize</span>(hist, hist, <span class="number">0</span>, <span class="number">255</span>, NORM_MINMAX);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 反射投影的次数 （不是像素值）</span></span><br><span class="line">	Mat backProject;</span><br><span class="line">	<span class="built_in">calcBackProject</span>(&amp;hueImage, <span class="number">1</span>, <span class="number">0</span>, hist, backProject, ranges);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;BackProject&quot;</span>, backProject);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;C:/Users/hcDarren/Desktop/android/hand.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;读取文件出错&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	Mat hsv;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src, hsv, COLOR_BGR2HSV);</span><br><span class="line">	<span class="comment">// 作业，明天讲</span></span><br><span class="line">	vector&lt;Mat&gt; hsv_s;</span><br><span class="line">	<span class="built_in">split</span>(hsv, hsv_s);</span><br><span class="line">	hueImage = hsv_s[<span class="number">0</span>];</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 创建一个窗口</span></span><br><span class="line">	<span class="built_in">namedWindow</span>(<span class="string">&quot;BackProject&quot;</span>);</span><br><span class="line">	<span class="built_in">createTrackbar</span>(<span class="string">&quot;backprojection&quot;</span>, <span class="string">&quot;BackProject&quot;</span>, &amp;bins, max_bins, hist_backprojection);</span><br><span class="line">	<span class="built_in">hist_backprojection</span>(bins, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-直方图模板匹配"><a href="#3-直方图模板匹配" class="headerlink" title="3.直方图模板匹配"></a>3.<a href="http://www.opencv.org.cn/opencvdoc/2.3.2/html/doc/tutorials/imgproc/histograms/template_matching/template_matching.html">直方图模板匹配</a></h1><p><code>用于找寻相似的部分</code></p>
<p><code>matchTemplate(InputArray image, InputArray templ,
         OutputArray result, int method, InputArray mask = noArray());</code></p>
<ul>
<li>TM_SQDIFF : 平方差匹配。这类方法利用平方差来进行匹配,最好匹配为0.匹配越差,匹配值越大.</li>
<li>TM_SQDIFF_NORMED：标准平方差匹配</li>
<li>TM_CCORR：相关匹配。这类方法采用模板和图像间的乘法操作,所以较大的数表示匹配程度较高,0标识最坏的匹配效果.</li>
<li>TM_CCORR_NORMED：标准相关匹配</li>
<li>TM_CCOEFF：相关匹配。这类方法将模版对其均值的相对值与图像对其均值的相关值进行匹配,1表示完美匹配,-1表示糟糕的匹配,0表示没有任何相关性(随机序列).</li>
<li>TM_CCOEFF_NORMED：标准相关匹配。</li>
</ul>
<p><code>CV_EXPORTS_W void minMaxLoc(InputArray src, CV_OUT double* minVal,
         CV_OUT double* maxVal = 0, CV_OUT Point* minLoc = 0,
         CV_OUT Point* maxLoc = 0, InputArray mask = noArray());</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;C:/Users/hcDarren/Desktop/android/qd.jpg&quot;</span>);</span><br><span class="line">	Mat tmpl = <span class="built_in">imread</span>(<span class="string">&quot;C:/Users/hcDarren/Desktop/android/template.png&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;读取文件出错&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*matchTemplate(InputArray image, InputArray templ,</span></span><br><span class="line"><span class="comment">		OutputArray result, int method, InputArray mask = noArray());*/</span></span><br><span class="line"></span><br><span class="line">	<span class="function">Mat <span class="title">result</span><span class="params">(src.rows - tmpl.rows + <span class="number">1</span>, src.cols - tmpl.cols + <span class="number">1</span>, CV_32FC1)</span></span>;</span><br><span class="line">	<span class="comment">// 模板匹配   result  匹配计算的结果 ， 大小应该是多少 image.cols image.rows</span></span><br><span class="line">	<span class="built_in">matchTemplate</span>(src, tmpl, result, TM_CCORR_NORMED);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 从结果里面去找最小值，for , 找到那个最小值的点 Point， for 去找 </span></span><br><span class="line">	<span class="function">CV_EXPORTS_W <span class="type">void</span> <span class="title">minMaxLoc</span><span class="params">(InputArray src, CV_OUT <span class="type">double</span>* minVal,</span></span></span><br><span class="line"><span class="params"><span class="function">		CV_OUT <span class="type">double</span>* maxVal = <span class="number">0</span>, CV_OUT Point* minLoc = <span class="number">0</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">		CV_OUT Point* maxLoc = <span class="number">0</span>, InputArray mask = noArray())</span></span>;</span><br><span class="line">	<span class="comment">// 传出参数：minVal 在函数中通过指针赋值，当做返回值用</span></span><br><span class="line">	<span class="type">double</span> minVal = <span class="number">0</span>;</span><br><span class="line">	<span class="type">double</span> maxVal = <span class="number">0</span>;</span><br><span class="line">	Point minLoc = <span class="number">0</span>;</span><br><span class="line">	Point maxLoc = <span class="number">0</span>;</span><br><span class="line">	<span class="built_in">minMaxLoc</span>(result, &amp;minVal, &amp;maxVal, &amp;minLoc, &amp;maxLoc);</span><br><span class="line"></span><br><span class="line">	cout &lt;&lt; maxVal &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 画出来</span></span><br><span class="line">	<span class="built_in">rectangle</span>(src, maxLoc, <span class="built_in">Point</span>(maxLoc.x + tmpl.cols, maxLoc.y + tmpl.rows), <span class="built_in">Scalar</span>(<span class="number">0</span>, <span class="number">0</span>, <span class="number">255</span>), <span class="number">2</span>, <span class="number">8</span>);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：08.边缘类型、自定义线性滤波、阈值</title>
    <url>/56.OpenCV-%E8%BE%B9%E7%BC%98%E7%B1%BB%E5%9E%8B%E3%80%81%E8%87%AA%E5%AE%9A%E4%B9%89%E7%BA%BF%E6%80%A7%E6%BB%A4%E6%B3%A2%E3%80%81%E9%98%88%E5%80%BC/</url>
    <content><![CDATA[<h1 id="1-了解下卷积操作"><a href="#1-了解下卷积操作" class="headerlink" title="1. 了解下卷积操作"></a>1. 了解下卷积操作</h1><p>概念：<code>把kernel（卷积核）放到我们图像的矩阵之上，求锚点周围覆盖的像素乘积之和（包括锚点），用来计算锚点的像素值覆盖图片下面的像素值，称之为卷积操作。</code></p>
<p><img src="/images/56.卷积操作.png" alt=""></p>
<h1 id="2-处理边缘类型"><a href="#2-处理边缘类型" class="headerlink" title="2. 处理边缘类型"></a>2. 处理边缘类型</h1><p><code>copyMakeBorder(src,dst,src.rows/8,src.rows/8,src.cols/8,src.cols/8,BORDER_DEFAULT);</code></p>
<ul>
<li>BORDER_DEFAULT：用周边边缘区域来填充</li>
<li>BORDER_WRAP：用对边来填充</li>
<li>BORDER_REPLICATE: 用周边像素来填充</li>
<li>BORDER_CONSTANT: 用一个指定颜色来填充，默认是黑色。</li>
</ul>
<h1 id="3-自定义线性滤波与图片模糊-算子也叫作-核"><a href="#3-自定义线性滤波与图片模糊-算子也叫作-核" class="headerlink" title="3. 自定义线性滤波与图片模糊(算子也叫作 核)"></a>3. 自定义线性滤波与图片模糊(算子也叫作 核)</h1><p>介绍几种算子</p>
<ul>
<li>Robert 算子：<code>Mat kernelRobert = (Mat_&lt;int&gt;(2, 2) &lt;&lt; 1, 0, 0, -1);</code></li>
<li>Sobel 算子：x方向：<code>(Mat_&lt;int&gt;(3, 3) &lt;&lt; -1, 0, 1, -2,0,2,-1,0,1);</code>，y方向：<code>(Mat_&lt;int&gt;(3, 3) &lt;&lt; -1, -2, -1, 0, 0, 0, 1, 2, 1);</code></li>
<li>拉普拉斯算子：它对图片的要求比较高 <code>Mat kernelLpls= (Mat_&lt;int&gt;(3, 3) &lt;&lt; 0, -1, 0, -1, 4, -1, 0, -1, 0);</code></li>
</ul>
<p><img src="/images/56.自定义算子.png" alt=""></p>
<p>depth:（opencv中的概念）<br>　　矩阵中元素的一个通道的数据类型，这个值和type是相关的。例如 type为 CV_16SC2，一个2通道的16位的有符号整数。那么，depth则是CV_16S。depth也是一系列的预定义值，<br>将type的预定义值去掉通道信息就是depth值: 0 - 6 , type 的精度的层度<br>　　CV_8U 0  CV_8S 1 CV_16U 2 CV_16S 3 CV_32S 4 CV_32F 5 CV_64F 6</p>
<p>图像深度：像素的 bit 位数</p>
<p><code>自定义模糊：</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 自定义模糊</span></span><br><span class="line"><span class="type">int</span> size = <span class="number">15</span>;</span><br><span class="line">Mat kernel = Mat::<span class="built_in">ones</span>(size,size,CV_32F)/(size*size);</span><br><span class="line"></span><br><span class="line">Mat dst;</span><br><span class="line"><span class="built_in">filter2D</span>(src,dst,src.<span class="built_in">depth</span>(),kernel);</span><br><span class="line"><span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>,dst);</span><br></pre></td></tr></table></figure>
<h1 id="4-阈值（图像二值化）"><a href="#4-阈值（图像二值化）" class="headerlink" title="4.阈值（图像二值化）"></a>4.阈值（图像二值化）</h1><p><code>threshold(gray,dst,100,255,type);</code></p>
<p><code>当使用了THRESH_OTSU和THRESH_TRIANGLE两个标志时，输入图像必须为单通道。</code></p>
<p>type 有以下值：</p>
<ul>
<li>THRESH_BINARY：当前像素点超过 thresh = 100 ,当前像素取 maxValue = 255 , 否则取最小值 0</li>
<li>THRESH_BINARY_INV：当前像素点超过 thresh = 100 ,当前像素取 0 , 否则取最大值 maxValue = 255</li>
<li>THRESH_TRUNC：当前像素点超过 thresh = 100 ,当前像素取 maxValue = 255 , 否则取原来的值</li>
<li>THRESH_TOZERO：当前像素点超过 thresh = 100 ,当前像素取原来的值，否则取最小值 0</li>
<li>THRESH_TOZERO_INV：当前像素点超过 thresh = 100，当前像素取 0 ，否则取原来的值</li>
<li>THRESH_OTSU：自动阈值：取每个像素点（整个图像）计算一个 thresh 值</li>
<li>THRESH_TRIANGLE：自动阈值：取每个像素点（整个图像）计算一个 thresh 值</li>
</ul>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：10.霍夫检测(直线、圆检测)与重映射(镜像翻转)</title>
    <url>/58.OpenCV-%E9%9C%8D%E5%A4%AB%E6%A3%80%E6%B5%8B(%E7%9B%B4%E7%BA%BF%E3%80%81%E5%9C%86%E6%A3%80%E6%B5%8B)%E4%B8%8E%E9%87%8D%E6%98%A0%E5%B0%84(%E9%95%9C%E5%83%8F%E7%BF%BB%E8%BD%AC)/</url>
    <content><![CDATA[<h1 id="1-霍夫直线检测"><a href="#1-霍夫直线检测" class="headerlink" title="1. 霍夫直线检测"></a>1. 霍夫直线检测</h1><p>作用：检测图片当中点集合中的直线。（比如提取图片中的直线：车道线）</p>
<p>先来看下原理图：<br><img src="/images/58.霍夫直线检测.png" alt=""></p>
<p>再来看下<code>opencv</code>中的代码</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/houghLine.jpg&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>,src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 1. Canny 边缘检测</span></span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src,gray,COLOR_BGR2GRAY);</span><br><span class="line"></span><br><span class="line">	Mat cannyImage;</span><br><span class="line">	<span class="built_in">Canny</span>(gray,cannyImage,<span class="number">200</span>,<span class="number">250</span>,<span class="number">3</span>);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;cannyImage&quot;</span>, cannyImage);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2. HoughLineP</span></span><br><span class="line">	vector&lt;Vec4f&gt; plines;</span><br><span class="line">	<span class="comment">// lines 信息里面包含多条直线，每条直线两个点</span></span><br><span class="line">	<span class="comment">// rho 像素间隙扫描</span></span><br><span class="line">	<span class="comment">// theta 每次增加的角度 CV_PI / 180 = 1度</span></span><br><span class="line">	<span class="comment">// threshold 低阈值</span></span><br><span class="line">	<span class="comment">// minLineLength 线的最小长度</span></span><br><span class="line">	<span class="comment">// maxLineGap 线点之间的间隙</span></span><br><span class="line">	<span class="built_in">HoughLinesP</span>(cannyImage, plines, <span class="number">1</span>, CV_PI / <span class="number">360</span>, <span class="number">170</span>, <span class="number">30</span>, <span class="number">15</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; plines.<span class="built_in">size</span>(); i++)</span><br><span class="line">	&#123;</span><br><span class="line">		Vec4f pline = plines[i];</span><br><span class="line"></span><br><span class="line">		<span class="built_in">line</span>(src, <span class="built_in">Point</span>(pline[<span class="number">0</span>], pline[<span class="number">1</span>]), <span class="built_in">Point</span>(pline[<span class="number">1</span>], pline[<span class="number">2</span>]), <span class="built_in">Scalar</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">255</span>));</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;result&quot;</span>,src);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="comment">//HoughLines</span></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-霍夫圆检测"><a href="#2-霍夫圆检测" class="headerlink" title="2. 霍夫圆检测"></a>2. 霍夫圆检测</h1><p>先来看下原理图：<br><img src="/images/58.霍夫圆检测.png" alt=""></p>
<p>解释：  </p>
<ul>
<li>梯度的求取：类似于 x、y 轴的垂直，梯度求取就是<code>取各个点各个方向直线的垂直线</code> ，投票将 相交的次数大于多少的才算作圆</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/HoughCircle.jpg&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 1. 转灰度图</span></span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src, gray, COLOR_BGR2GRAY);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 2. HoughLineP</span></span><br><span class="line">	vector&lt;Vec3f&gt; circles;</span><br><span class="line">	<span class="comment">// minDist:10 两个圆之间的最小距离 越大越能减少同心圆的现象</span></span><br><span class="line">	<span class="comment">// param1:100 投票累加结果超过多少才能算圆</span></span><br><span class="line">	<span class="comment">// params2:30 低阈值</span></span><br><span class="line">	<span class="comment">// minRadius: 5 最小的半径</span></span><br><span class="line">	<span class="comment">// maxRadius: 50 最大的半径</span></span><br><span class="line">	<span class="built_in">HoughCircles</span>(gray, circles, HOUGH_GRADIENT, <span class="number">1</span>, <span class="number">40</span>, <span class="number">100</span>, <span class="number">30</span>,<span class="number">5</span>,<span class="number">50</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; circles.<span class="built_in">size</span>(); i++)</span><br><span class="line">	&#123;</span><br><span class="line">		Vec3f cc = circles[i];</span><br><span class="line">		<span class="built_in">circle</span>(src, <span class="built_in">Point</span>(cc[<span class="number">0</span>], cc[<span class="number">1</span>]), cc[<span class="number">2</span>], <span class="built_in">Scalar</span>(<span class="number">0</span>, <span class="number">0</span>, <span class="number">255</span>),<span class="number">1</span>,LINE_AA);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;result&quot;</span>, src);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="comment">//HoughLines</span></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-重映射"><a href="#3-重映射" class="headerlink" title="3.重映射"></a>3.重映射</h1><p>remap 根据原图按照新定的满足一定规律重新赋值的操作。<br>g(x,y) = f(h(x,y))  g 新的图片 = h 原图 ， f 函数，</p>
<p>可用于<code>镜像翻转</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="function">Mat <span class="title">map_x</span><span class="params">(src.size(),CV_32F)</span></span>;</span><br><span class="line">	<span class="function">Mat <span class="title">map_y</span><span class="params">(src.size(), CV_32F)</span></span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src.rows; row++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src.cols; col++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="comment">// x 轴的翻转</span></span><br><span class="line">			<span class="comment">//map_x.at&lt;float&gt;(row, col) = src.cols - col - 1;</span></span><br><span class="line">			<span class="comment">//map_y.at&lt;float&gt;(row, col) = row;</span></span><br><span class="line"></span><br><span class="line">			<span class="comment">// y 轴的翻转</span></span><br><span class="line">			map_x.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(row, col) = col;</span><br><span class="line">			map_y.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(row, col) = src.rows - row;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="built_in">remap</span>(src, dst, map_x, map_y, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>, dst);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="comment">//HoughLines</span></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：11.色彩空间与直方图均衡化</title>
    <url>/59.OpenCV-%E8%89%B2%E5%BD%A9%E7%A9%BA%E9%97%B4%E4%B8%8E%E7%9B%B4%E6%96%B9%E5%9B%BE%E5%9D%87%E8%A1%A1%E5%8C%96/</url>
    <content><![CDATA[<h1 id="1-RGB与HSV"><a href="#1-RGB与HSV" class="headerlink" title="1. RGB与HSV"></a>1. RGB与HSV</h1><p>rgb请看下图:</p>
<p><img src="/images/59.rgb.jpg" alt=""></p>
<p>HSV 分析：  </p>
<ul>
<li>H ： Hue 色相 色调，色度 0~360度  360种</li>
<li>S ：Saturation 饱和度</li>
<li>V ：Value 亮度</li>
</ul>
<p><img src="/images/59.hsv.jpg" alt=""></p>
<p>作用：证件照背景替换，扣头发等等</p>
<h1 id="2-归一化-normalize"><a href="#2-归一化-normalize" class="headerlink" title="2.归一化 normalize"></a>2.归一化 normalize</h1><p>归一化：就是把需要处理的数据经过处理后，限制在你需要的一定范围内。例如<code>假设我们把训练数据的第一个属性从[-10,+10]缩放到[-1,+1],那么如果测试数据的第一个属性属于区间[-11,+8],我们必须将测试数据转变为[-1.1,+0.8]</code></p>
<h1 id="3-直方图均衡化-equalizeHistogram"><a href="#3-直方图均衡化-equalizeHistogram" class="headerlink" title="3.直方图均衡化 equalizeHistogram"></a>3.直方图均衡化 equalizeHistogram</h1><p><code>单通道的图片</code>，统计其每个像素值的个数。</p>
<p>概念：直方图均衡化是一种简单有效的图像增强技术，通过改变图像的直方图来改变图像中各像素的灰度，主要用于增强动态范围偏小的图像的对比度。<br>把原始图像的直方图变换为均匀分布（均衡）的形式，这样就增加了像素之间灰度值差别的动态范围，从而达到增强图像整体对比度的效果</p>
<p><img src="/images/59.直方图均衡化.png" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">// 单通道均衡化</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/2_04.png&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src,gray,COLOR_BGR2GRAY);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>,src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// Histogram 直方图</span></span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="built_in">equalizeHist</span>(gray, dst);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>, dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 彩色图均衡化</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/2_05.jpg&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>,src);</span><br><span class="line">	</span><br><span class="line">	vector&lt;Mat&gt; channels;</span><br><span class="line">	<span class="built_in">split</span>(src,channels);</span><br><span class="line"></span><br><span class="line">	Mat blue, green, red;</span><br><span class="line">	blue = channels.<span class="built_in">at</span>(<span class="number">0</span>);</span><br><span class="line">	green = channels.<span class="built_in">at</span>(<span class="number">1</span>);</span><br><span class="line">	red = channels.<span class="built_in">at</span>(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 直方均衡</span></span><br><span class="line">	<span class="built_in">equalizeHist</span>(blue, blue);</span><br><span class="line">	<span class="built_in">equalizeHist</span>(green, green);</span><br><span class="line">	<span class="built_in">equalizeHist</span>(red, red);</span><br><span class="line"></span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="built_in">merge</span>(channels,dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>,dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-直方图的计算和绘制"><a href="#4-直方图的计算和绘制" class="headerlink" title="4.直方图的计算和绘制"></a>4.直方图的计算和绘制</h1><p>api</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">// NORM_MINMAX中alpha、beta都起作用，同时需要注意的是alpha和beta的取值顺序与归一化结果无关。即alpha=255,beta=0和alpha=0,beta=255最后的归一化结果是相同的。</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// @param alpha 在范围的情况下，标准化到或较低范围边界的范数值规范化。</span></span><br><span class="line"><span class="comment">// @param beta 范围归一化时的上限范围边界;它不用于规范规范化。</span></span><br><span class="line"><span class="function">CV_EXPORTS_W <span class="type">void</span> <span class="title">normalize</span><span class="params">( InputArray src, InputOutputArray dst, <span class="type">double</span> alpha = <span class="number">1</span>, <span class="type">double</span> beta = <span class="number">0</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">                             <span class="type">int</span> norm_type = NORM_L2, <span class="type">int</span> dtype = <span class="number">-1</span>, InputArray mask = noArray())</span></span>;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 获取直方图，B G R 每个都单独分离出来（自己写个代码去分离）</span></span><br><span class="line">	vector&lt;Mat&gt; bgr_s;</span><br><span class="line">	<span class="built_in">split</span>(src,bgr_s);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// b 通道，但为什么不是 蓝色，而是一个灰度图 ?   </span></span><br><span class="line">	<span class="comment">// imshow(&quot;bgr_s&quot;, bgr_s[0]);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 计算获得直方图的数据</span></span><br><span class="line">	<span class="comment">// images 输入的图像</span></span><br><span class="line">	<span class="comment">// nimages 输入图像的个数 </span></span><br><span class="line">	<span class="comment">// channels 第几通道  下标为 0</span></span><br><span class="line">	<span class="comment">// mask 掩模</span></span><br><span class="line">	<span class="comment">// dims 需要统计的通道个数</span></span><br><span class="line">	<span class="comment">// histSize 等级的个数  0-255，每个等级占的范围就是 256/256 = 1</span></span><br><span class="line">	<span class="comment">// ranges 数据的范围  0-255</span></span><br><span class="line">	<span class="comment">// uniform true 是否对得到的图片进行归一化处理</span></span><br><span class="line">	<span class="comment">// accumulate 在多个图像时，是否累计计算像素值的个数 false</span></span><br><span class="line">	<span class="comment">/*calcHist(const Mat* images, int nimages,</span></span><br><span class="line"><span class="comment">	const int* channels, InputArray mask,</span></span><br><span class="line"><span class="comment">	OutputArray hist, int dims, const int* histSize,</span></span><br><span class="line"><span class="comment">	const float** ranges, bool uniform = true, bool accumulate = false);*/</span></span><br><span class="line"></span><br><span class="line">	<span class="type">int</span> histSize = <span class="number">256</span>;</span><br><span class="line">	<span class="type">float</span> range[] = &#123; <span class="number">0</span>, <span class="number">256</span> &#125;;</span><br><span class="line">	<span class="type">const</span> <span class="type">float</span> * ranges = &#123; range &#125;;</span><br><span class="line">	Mat hist_b, hist_g, hist_r;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">calcHist</span>(&amp;bgr_s[<span class="number">0</span>], <span class="number">1</span>, <span class="number">0</span>, <span class="built_in">Mat</span>(), hist_b, <span class="number">1</span>, &amp;histSize, &amp;ranges, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">	<span class="built_in">calcHist</span>(&amp;bgr_s[<span class="number">1</span>], <span class="number">1</span>, <span class="number">0</span>, <span class="built_in">Mat</span>(), hist_g, <span class="number">1</span>, &amp;histSize, &amp;ranges, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">	<span class="built_in">calcHist</span>(&amp;bgr_s[<span class="number">2</span>], <span class="number">1</span>, <span class="number">0</span>, <span class="built_in">Mat</span>(), hist_r, <span class="number">1</span>, &amp;histSize, &amp;ranges, <span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// Mat 存的是像素值，不一定，存任意值</span></span><br><span class="line">	<span class="comment">// 画出来，hist_b 存的是什么？存的是各个灰度值的个数，hist_b 最小值 0 ，最大值 图片的宽*高</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 归一化</span></span><br><span class="line">	<span class="type">int</span> hist_h = <span class="number">400</span>; <span class="comment">// 直方图的图像的高</span></span><br><span class="line">	<span class="type">int</span> hist_w = <span class="number">512</span>; <span class="comment">// 256 * 2 直方图的图像的宽（2 代表画笔的大小）</span></span><br><span class="line">	<span class="type">int</span> bin_w = hist_w / histSize; <span class="comment">// 画笔大小</span></span><br><span class="line">	<span class="comment">// alpha 最小值 </span></span><br><span class="line">	<span class="comment">// beta 最大值</span></span><br><span class="line">	<span class="comment">/*normalize(InputArray src, InputOutputArray dst, double alpha = 1, double beta = 0,</span></span><br><span class="line"><span class="comment">	int norm_type = NORM_L2, int dtype = -1, InputArray mask = noArray());*/</span></span><br><span class="line">	<span class="comment">// NORM_MINMAX 缩放到一定区域</span></span><br><span class="line">	<span class="built_in">normalize</span>(hist_b, hist_b, <span class="number">0</span>, hist_h, NORM_MINMAX, <span class="number">-1</span>, <span class="built_in">Mat</span>());</span><br><span class="line">	<span class="built_in">normalize</span>(hist_g, hist_g, <span class="number">0</span>, hist_h, NORM_MINMAX, <span class="number">-1</span>, <span class="built_in">Mat</span>());</span><br><span class="line">	<span class="built_in">normalize</span>(hist_r, hist_r, <span class="number">0</span>, hist_h, NORM_MINMAX, <span class="number">-1</span>, <span class="built_in">Mat</span>());</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 画到一张图中</span></span><br><span class="line">	<span class="function">Mat <span class="title">histImage</span><span class="params">(hist_h, hist_w, CV_8SC4, Scalar())</span></span>; </span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt; histSize; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="comment">// 开始点，结束点 255</span></span><br><span class="line">		<span class="built_in">line</span>(</span><br><span class="line">			histImage,</span><br><span class="line">			<span class="built_in">Point</span>((i - <span class="number">1</span>)*bin_w, hist_h - hist_b.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(i - <span class="number">1</span>)),<span class="comment">// 图片的像素点 左上角才是 0,0</span></span><br><span class="line">			<span class="built_in">Point</span>(i*bin_w, hist_h - hist_b.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(i)),</span><br><span class="line">			<span class="built_in">Scalar</span>(<span class="number">255</span>, <span class="number">0</span>, <span class="number">0</span>), bin_w, LINE_AA);</span><br><span class="line">		<span class="built_in">line</span>(</span><br><span class="line">			histImage,</span><br><span class="line">			<span class="built_in">Point</span>((i - <span class="number">1</span>)*bin_w, hist_h - hist_g.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(i - <span class="number">1</span>)),</span><br><span class="line">			<span class="built_in">Point</span>(i*bin_w, hist_h - hist_g.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(i)),</span><br><span class="line">			<span class="built_in">Scalar</span>(<span class="number">0</span>, <span class="number">255</span>, <span class="number">0</span>), bin_w, LINE_AA);</span><br><span class="line">		<span class="built_in">line</span>(</span><br><span class="line">			histImage,</span><br><span class="line">			<span class="built_in">Point</span>((i - <span class="number">1</span>)*bin_w, hist_h - hist_r.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(i - <span class="number">1</span>)),</span><br><span class="line">			<span class="built_in">Point</span>(i*bin_w, hist_h - hist_r.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(i)),</span><br><span class="line">			<span class="built_in">Scalar</span>(<span class="number">0</span>, <span class="number">0</span>, <span class="number">255</span>), bin_w, LINE_AA);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;histImage&quot;</span>, histImage);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：09.图片边缘检测</title>
    <url>/57.OpenCV-%E5%9B%BE%E7%89%87%E8%BE%B9%E7%BC%98%E6%A3%80%E6%B5%8B/</url>
    <content><![CDATA[<h1 id="1-Sobel-算子-检测图片边缘"><a href="#1-Sobel-算子-检测图片边缘" class="headerlink" title="1. Sobel 算子 检测图片边缘"></a>1. Sobel 算子 检测图片边缘</h1><p>先来看下微积分求导:</p>
<p><img src="/images/57.微积分求导.png" alt=""></p>
<p>上代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/card1.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">//Mat kernel = (Mat_&lt;int&gt;(3, 3) &lt;&lt; -1, 0, 1, -2, 0, 2, -1, 0, 1); </span></span><br><span class="line">	<span class="comment">//Mat dst;</span></span><br><span class="line">	<span class="comment">//filter2D(src,dst,src.depth(),kernel);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 1. 降噪高斯</span></span><br><span class="line">	Mat gaussian;</span><br><span class="line">	<span class="built_in">GaussianBlur</span>(src,gaussian,<span class="built_in">Size</span>(<span class="number">3</span>,<span class="number">3</span>),<span class="number">0</span>);</span><br><span class="line">	<span class="comment">// 2. 转灰度</span></span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(gaussian, gray, COLOR_BGR2GRAY);</span><br><span class="line">	<span class="comment">// 3. 求梯度</span></span><br><span class="line">	<span class="comment">// 参数 ：delta -&gt; 在计算结果的基础上再加上 delta</span></span><br><span class="line">	<span class="comment">// ddepth : -1代表与 gray.depth()相同的值，但是应该传比 gray 精度更高的值(1 / 3=0,1f/3 = 0.3333)</span></span><br><span class="line">	<span class="comment">// x y ，求梯度一般不用索贝尔，Scharr 增强</span></span><br><span class="line">	Mat sobel_x, sobel_y;</span><br><span class="line">	<span class="built_in">Sobel</span>(gray, sobel_x, CV_32F, <span class="number">1</span>, <span class="number">0</span>, <span class="number">3</span>);</span><br><span class="line">	<span class="built_in">Sobel</span>(gray, sobel_y, CV_32F, <span class="number">0</span>, <span class="number">1</span>, <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 4. 求绝对值 (因为有的像素值求出来为负数)</span></span><br><span class="line">	<span class="built_in">convertScaleAbs</span>(sobel_x, sobel_x);</span><br><span class="line">	<span class="built_in">convertScaleAbs</span>(sobel_y, sobel_y);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 5. 合并两个方向的图片</span></span><br><span class="line">	Mat sobel;</span><br><span class="line">	<span class="built_in">addWeighted</span>(sobel_x,<span class="number">0.5</span>,sobel_y,<span class="number">0.5</span>,<span class="number">0</span>,sobel);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;sobel_x&quot;</span>, sobel_x);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;sobel_y&quot;</span>, sobel_y);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;sobel&quot;</span>, sobel);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>sobel x y方向两张图的合并</p>
<p><img src="/images/57.sobel%20合并.png" alt=""></p>
<h1 id="2-Scharr-增强求边缘"><a href="#2-Scharr-增强求边缘" class="headerlink" title="2. Scharr 增强求边缘"></a>2. Scharr 增强求边缘</h1><p>除了求梯度那儿跟 <code>sobel</code> 不一样以外其它代码都类似。下面着重上<code>求梯度</code>的代码</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 3. 求梯度</span></span><br><span class="line"><span class="comment">// 参数 ：delta -&gt; 在计算结果的基础上再加上 delta</span></span><br><span class="line"><span class="comment">// ddepth : -1代表与 gray.depth()相同的值，但是应该传比 gray 精度更高的值(1 / 3=0,1f/3 = 0.3333)</span></span><br><span class="line"><span class="comment">// x y ，求梯度一般不用索贝尔，Scharr 增强</span></span><br><span class="line">Mat scharr_x, scharr_y;</span><br><span class="line"><span class="built_in">Scharr</span>(gray, scharr_x, CV_32F, <span class="number">1</span>, <span class="number">0</span>, <span class="number">3</span>);</span><br><span class="line"><span class="built_in">Scharr</span>(gray, scharr_y, CV_32F, <span class="number">0</span>, <span class="number">1</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure>
<h1 id="3-Laplance-图片边缘检测"><a href="#3-Laplance-图片边缘检测" class="headerlink" title="3. Laplance 图片边缘检测"></a>3. Laplance 图片边缘检测</h1><p>这用到了二阶导数，所以只处理一次图片，不需要像 <code>sobel</code> 那样处理两次图片再合并</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/card1.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">//Mat kernel = (Mat_&lt;int&gt;(3, 3) &lt;&lt; -1, 0, 1, -2, 0, 2, -1, 0, 1);</span></span><br><span class="line">	<span class="comment">//Mat dst;</span></span><br><span class="line">	<span class="comment">//filter2D(src,dst,src.depth(),kernel);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 1. 降噪高斯</span></span><br><span class="line">	Mat gaussian;</span><br><span class="line">	<span class="built_in">GaussianBlur</span>(src, gaussian, <span class="built_in">Size</span>(<span class="number">3</span>, <span class="number">3</span>), <span class="number">0</span>);</span><br><span class="line">	<span class="comment">// 2. 转灰度</span></span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(gaussian, gray, COLOR_BGR2GRAY);</span><br><span class="line">	<span class="comment">// 3. laplance</span></span><br><span class="line">	Mat lpls;</span><br><span class="line">	<span class="built_in">Laplacian</span>(gray, lpls, CV_16S, <span class="number">5</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 4.求绝对值</span></span><br><span class="line">	<span class="built_in">convertScaleAbs</span>(lpls, lpls);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 5. 二值化</span></span><br><span class="line">	Mat thresh;</span><br><span class="line">	<span class="built_in">threshold</span>(lpls,thresh,<span class="number">0</span>,<span class="number">255</span>,THRESH_BINARY|THRESH_OTSU);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;lpls&quot;</span>, lpls);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-Canny-图片边缘检测"><a href="#4-Canny-图片边缘检测" class="headerlink" title="4.Canny 图片边缘检测"></a>4.Canny 图片边缘检测</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/card1.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// Canny 的原理</span></span><br><span class="line">	<span class="comment">// 1. 高斯去噪声 </span></span><br><span class="line">	<span class="comment">// 2. 灰度转换</span></span><br><span class="line">	<span class="comment">// 3. 计算梯度 Sobel/Scharr</span></span><br><span class="line">	<span class="comment">// 4. 非最大信号抑制</span></span><br><span class="line">	<span class="comment">// 5. 高低阈值输出二值图像（0,255） threshold1 低阈值 threshold2 高阈值</span></span><br><span class="line"></span><br><span class="line">	Mat dst;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 参数：threshold1 - threshold2 ，在 threshold1 - threshold2 之间，取最大值 255，否则取 0  尽量 1 ：2  1 ：3   30-60  30-90  50-100 50-150</span></span><br><span class="line">	<span class="comment">// 参数：L2gradient  falsee类似于:绝对值相加，true类似于:开根号(sobel_x^2 + soble_y^2)</span></span><br><span class="line">	<span class="built_in">Canny</span>(src,dst,<span class="number">30</span>,<span class="number">90</span>,<span class="number">3</span>,<span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>, dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：14.说说图片效果</title>
    <url>/62.OpenCV-%E8%AF%B4%E8%AF%B4%E5%9B%BE%E7%89%87%E6%95%88%E6%9E%9C/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/opencv62">NDKPractice项目的opencv62</a></strong></p>
<h1 id="1-opencv-的用处"><a href="#1-opencv-的用处" class="headerlink" title="1. opencv 的用处"></a>1. opencv 的用处</h1><ul>
<li>提高图片的视觉，图片变换效果</li>
<li>提取图像的特征，用来分析</li>
<li>图像识别，直播，人脸，指纹，二维码，汽车牌照识别</li>
</ul>
<p><img src="/images/62.图形图像处理.png" alt=""></p>
<h1 id="2-Bitmap-的解封装与编解码"><a href="#2-Bitmap-的解封装与编解码" class="headerlink" title="2. Bitmap 的解封装与编解码"></a>2. Bitmap 的解封装与编解码</h1><p>你给一个错误地址，或者给一个文本地址，Glide 是如何判断图片格式，是否错误之类的呢？</p>
<p><code>根据流读出来的头几个字节判断格式</code>。</p>
<p>先说下 <code>PNG、JPEG、WEBP 区别</code> :<br><code>PNG (无损压缩)，JPEG（有损压缩），WEBP  区别？ 有损压缩和无损压缩</code></p>
<p>面试中常问问题：<code>WEBP</code> 有什么好处？ : </p>
<ul>
<li><code>存储空间更小还原度更高</code>，</li>
<li>版本区别：支持 4.0 开始支持，4.2 正式没有问题（alpha颜色通道）</li>
</ul>
<p><img src="/images/62.图片%20Bitmap%20的解压缩和编解码.png" alt=""></p>
<h1 id="3-逆世界效果"><a href="#3-逆世界效果" class="headerlink" title="3. 逆世界效果"></a>3. 逆世界效果</h1><p>用图片说下原理：</p>
<p><img src="/images/62.逆世界.png" alt=""></p>
<p>代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Java_com_east_opencv62_NDKBitmapUtils_againstWorld</span>(JNIEnv *env, jclass clazz, jobject bitmap) &#123;</span><br><span class="line">    Mat src;</span><br><span class="line">    cv_helper::<span class="built_in">bitmap2mat</span>(env, bitmap, src);</span><br><span class="line"></span><br><span class="line">    <span class="function">Mat <span class="title">res</span><span class="params">(src.size(), src.type())</span></span>;</span><br><span class="line">    <span class="comment">// 获取图片宽高</span></span><br><span class="line">    <span class="type">int</span> src_w = src.cols; <span class="comment">// 图片的宽</span></span><br><span class="line">    <span class="type">int</span> src_h = src.rows; <span class="comment">// 图片的高</span></span><br><span class="line">    <span class="type">int</span> mid_h = src_h &gt;&gt; <span class="number">1</span>; <span class="comment">// 逆向世界的一般一般</span></span><br><span class="line">    <span class="type">int</span> a_h = src_h &gt;&gt; <span class="number">2</span>; <span class="comment">// 原图像的4/1</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 处理输出图像的下半部分</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; mid_h; ++row) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src_w; ++col) &#123;</span><br><span class="line">            <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC4)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec4b&gt;(row + mid_h, col) = src.<span class="built_in">at</span>&lt;Vec4b&gt;(row + a_h, col);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC2)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec3b&gt;(row + mid_h, col) = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row + a_h, col);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC1)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;uchar&gt;(row + mid_h, col) = src.<span class="built_in">at</span>&lt;uchar&gt;(row + a_h, col);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 处理输出图像的上半部分</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; mid_h; ++row) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src_w; ++col) &#123;</span><br><span class="line">            <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC4)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec4b&gt;(row, col) = src.<span class="built_in">at</span>&lt;Vec4b&gt;(src_h - row - a_h, col);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC2)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col) = src.<span class="built_in">at</span>&lt;Vec3b&gt;(src_h - row - a_h, col);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC1)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;uchar&gt;(row, col) = src.<span class="built_in">at</span>&lt;uchar&gt;(src_h - row - a_h, col);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cv_helper::<span class="built_in">mat2bitmap</span>(env, res, bitmap);</span><br><span class="line">    <span class="keyword">return</span> bitmap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-浮雕效果"><a href="#4-浮雕效果" class="headerlink" title="4.浮雕效果"></a>4.浮雕效果</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Java_com_east_opencv62_NDKBitmapUtils_anaglyph</span>(JNIEnv *env, jclass clazz, jobject bitmap) &#123;</span><br><span class="line">    <span class="comment">// 有立体感，突出了轮廓信息，opencv  卷积</span></span><br><span class="line">    <span class="comment">// [1,0]</span></span><br><span class="line">    <span class="comment">// [0,1]</span></span><br><span class="line">    Mat src;</span><br><span class="line">    cv_helper::<span class="built_in">bitmap2mat</span>(env, bitmap, src);</span><br><span class="line">    <span class="function">Mat <span class="title">res</span><span class="params">(src.size(), src.type())</span></span>;</span><br><span class="line">    src.<span class="built_in">copyTo</span>(res);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取图片宽高</span></span><br><span class="line">    <span class="type">int</span> src_w = src.cols; <span class="comment">// 图片的宽</span></span><br><span class="line">    <span class="type">int</span> src_h = src.rows; <span class="comment">// 图片的高</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src_h - <span class="number">1</span>; ++row) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src_w - <span class="number">1</span>; ++col) &#123;</span><br><span class="line">            <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC4) &#123;</span><br><span class="line">                Vec4b pixels_p = src.<span class="built_in">at</span>&lt;Vec4b&gt;(row, col);</span><br><span class="line">                Vec4b pixels_n = src.<span class="built_in">at</span>&lt;Vec4b&gt;(row + <span class="number">1</span>, col + <span class="number">1</span>);</span><br><span class="line">                <span class="comment">// bgra</span></span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec4b&gt;(row, col)[<span class="number">0</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(pixels_p[<span class="number">0</span>] - pixels_n[<span class="number">0</span>] + <span class="number">128</span>);</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec4b&gt;(row, col)[<span class="number">1</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(pixels_p[<span class="number">1</span>] - pixels_n[<span class="number">1</span>] + <span class="number">128</span>);</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec4b&gt;(row, col)[<span class="number">2</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(pixels_p[<span class="number">2</span>] - pixels_n[<span class="number">2</span>] + <span class="number">128</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC2) &#123;</span><br><span class="line">                Vec3b pixels_p = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col);</span><br><span class="line">                Vec3b pixels_n = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row + <span class="number">1</span>, col + <span class="number">1</span>);</span><br><span class="line">                <span class="comment">// bgr</span></span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">0</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(pixels_p[<span class="number">0</span>] - pixels_n[<span class="number">0</span>] + <span class="number">128</span>);</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">1</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(pixels_p[<span class="number">1</span>] - pixels_n[<span class="number">1</span>] + <span class="number">128</span>);</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">2</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(pixels_p[<span class="number">2</span>] - pixels_n[<span class="number">2</span>] + <span class="number">128</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC1) &#123;</span><br><span class="line">                uchar pixels_p = src.<span class="built_in">at</span>&lt;uchar&gt;(row, col);</span><br><span class="line">                uchar pixels_n = src.<span class="built_in">at</span>&lt;uchar&gt;(row + <span class="number">1</span>, col + <span class="number">1</span>);</span><br><span class="line">                res.<span class="built_in">at</span>&lt;uchar&gt;(row, col) = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(pixels_p - pixels_n + <span class="number">128</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    jobject newBitmap = cv_helper::<span class="built_in">createBitmap</span>(env, res.cols, res.rows, res.<span class="built_in">type</span>());</span><br><span class="line"></span><br><span class="line">    cv_helper::<span class="built_in">mat2bitmap</span>(env, res, newBitmap);</span><br><span class="line">    <span class="keyword">return</span> newBitmap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-马赛克"><a href="#5-马赛克" class="headerlink" title="5.马赛克"></a>5.马赛克</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Java_com_east_opencv62_NDKBitmapUtils_mosaic</span>(JNIEnv *env, jclass clazz, jobject bitmap) &#123;</span><br><span class="line">    Mat src;</span><br><span class="line">    cv_helper::<span class="built_in">bitmap2mat</span>(env, bitmap, src);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取图片宽高</span></span><br><span class="line">    <span class="type">int</span> src_w = src.cols; <span class="comment">// 图片的宽</span></span><br><span class="line">    <span class="type">int</span> src_h = src.rows; <span class="comment">// 图片的高</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 省略人脸识别</span></span><br><span class="line">    <span class="type">int</span> row_start = src_h &gt;&gt; <span class="number">2</span>;</span><br><span class="line">    <span class="type">int</span> row_end = src_h * <span class="number">3</span> / <span class="number">4</span>;</span><br><span class="line">    <span class="type">int</span> col_start = src_w &gt;&gt; <span class="number">2</span>;</span><br><span class="line">    <span class="type">int</span> col_end = src_w * <span class="number">3</span> / <span class="number">4</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> size_h = <span class="number">50</span>;</span><br><span class="line">    <span class="type">int</span> size_w = <span class="number">50</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = row_start; row &lt; row_end; row += size_h) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = col_start; col &lt; col_end; col += size_w) &#123;</span><br><span class="line">            <span class="type">int</span> pixel = src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row, col);</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> m_row = <span class="number">0</span>; m_row &lt; size_w; m_row++) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> m_col = <span class="number">0</span>; m_col &lt; size_h; m_col++) &#123;</span><br><span class="line">                    src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row + m_row, col + m_col) = pixel;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 当还没到结束的区域时，剩下的区域也需要修改像素</span></span><br><span class="line">            <span class="keyword">if</span> (col &lt; col_end &amp;&amp; col + size_w &gt; col_end) &#123;</span><br><span class="line">                size_w = col_end - col - <span class="number">1</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                size_w = <span class="number">50</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 当还没到结束的区域时，剩下的区域也需要修改像素</span></span><br><span class="line">        <span class="keyword">if</span> (row &lt; row_end &amp;&amp; row + size_h &gt; row_end) &#123;</span><br><span class="line">            size_h = row_end - row - <span class="number">1</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            size_h = <span class="number">50</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cv_helper::<span class="built_in">mat2bitmap</span>(env, src, bitmap);</span><br><span class="line">    <span class="keyword">return</span> bitmap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="6-毛玻璃效果"><a href="#6-毛玻璃效果" class="headerlink" title="6.毛玻璃效果"></a>6.毛玻璃效果</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Java_com_east_opencv62_NDKBitmapUtils_groundGlass</span>(JNIEnv *env, jclass clazz, jobject bitmap) &#123;</span><br><span class="line">    Mat src;</span><br><span class="line">    cv_helper::<span class="built_in">bitmap2mat</span>(env, bitmap, src);</span><br><span class="line">    <span class="comment">// 不同于高斯模糊，毛玻璃（对某个区域取随机像素）</span></span><br><span class="line">    <span class="comment">// 获取图片宽高</span></span><br><span class="line">    <span class="type">int</span> src_w = src.cols; <span class="comment">// 图片的宽</span></span><br><span class="line">    <span class="type">int</span> src_h = src.rows; <span class="comment">// 图片的高</span></span><br><span class="line">    <span class="type">int</span> size = <span class="number">30</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">RNG <span class="title">rng</span><span class="params">(time(<span class="literal">NULL</span>))</span></span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src_h; ++row) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src_w; ++col) &#123;</span><br><span class="line">            <span class="type">int</span> random = rng.<span class="built_in">uniform</span>(<span class="number">0</span>, size<span class="number">+1</span>);</span><br><span class="line">            <span class="keyword">if</span>(row+random &gt;= src_h)&#123;</span><br><span class="line">                <span class="keyword">if</span>(col + random &gt;= src_w)&#123;</span><br><span class="line">                    src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row, col) = src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row - random, col - random);</span><br><span class="line">                &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                    src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row, col) = src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row - random, col + random);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="keyword">if</span>(col + random &gt;= src_w)&#123;</span><br><span class="line">                    src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row, col) = src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row + random, col - random);</span><br><span class="line">                &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                    src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row, col) = src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row + random, col + random);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cv_helper::<span class="built_in">mat2bitmap</span>(env, src, bitmap);</span><br><span class="line">    <span class="keyword">return</span> bitmap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="7-油画特效效果"><a href="#7-油画特效效果" class="headerlink" title="7. 油画特效效果"></a>7. 油画特效效果</h1><p>注意：<code>下面的size 越大效果越明显，但是计算速度越慢</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Java_com_east_opencv62_NDKBitmapUtils_oilPainting</span>(JNIEnv *env, jclass clazz, jobject bitmap) &#123;</span><br><span class="line">    <span class="comment">// 油画基于直方统计</span></span><br><span class="line">    <span class="comment">// 1. 每个点需要分成 n*n 小块</span></span><br><span class="line">    <span class="comment">// 2. 统计灰度等级</span></span><br><span class="line">    <span class="comment">// 3. 选择灰度等级中最多的值</span></span><br><span class="line">    <span class="comment">// 4. 找到所有的像素取平均值</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//    1、获取图像的灰度(gray)图片</span></span><br><span class="line"><span class="comment">//    2、设计一个小方框（4x4 or 8x8 or 10x10等），统计每个小方框的像素值</span></span><br><span class="line"><span class="comment">//    3、将0-255的灰度值划分成几个等级，并把第二步处理的结果映射到所设置的各个等级中，并计数</span></span><br><span class="line"><span class="comment">//    4、找到每个方框中灰度等级最多的所有的像素，并且求取这些像素的均值</span></span><br><span class="line"><span class="comment">//    5、用统计出来的平均值来替代原来的像素值</span></span><br><span class="line"></span><br><span class="line">    Mat src;</span><br><span class="line">    cv_helper::<span class="built_in">bitmap2mat</span>(env, bitmap, src);</span><br><span class="line">    Mat gray;</span><br><span class="line">    <span class="built_in">cvtColor</span>(src,gray,COLOR_BGRA2GRAY);</span><br><span class="line">    <span class="function">Mat <span class="title">res</span><span class="params">(src.size(),src.type())</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> src_w = src.cols; <span class="comment">// 图片的宽</span></span><br><span class="line">    <span class="type">int</span> src_h = src.rows; <span class="comment">// 图片的高</span></span><br><span class="line">    <span class="type">int</span> size = <span class="number">15</span>;</span><br><span class="line">    <span class="comment">// 知识不是用来背的 20% ，用来唤醒大家的</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src_h - size; ++row) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src_w - size; ++col) &#123;</span><br><span class="line">            <span class="type">int</span> g[<span class="number">20</span>] = &#123;<span class="number">0</span>&#125;, b_g[<span class="number">20</span>] = &#123;<span class="number">0</span>&#125;, g_g[<span class="number">20</span>] = &#123;<span class="number">0</span>&#125;, r_g[<span class="number">20</span>] = &#123;<span class="number">0</span>&#125;;<span class="comment">// 255/8</span></span><br><span class="line">            <span class="comment">// 这个位置  64 循环 -&gt; 1 个像素点 ， 高斯模糊 ，想想怎么优化</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> o_rows = <span class="number">0</span>; o_rows &lt; size; ++o_rows) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> o_cols = <span class="number">0</span>; o_cols &lt; size; ++o_cols) &#123;</span><br><span class="line">                    uchar gery = gray.<span class="built_in">at</span>&lt;uchar&gt;(row + o_rows,col + o_cols);</span><br><span class="line">                    uchar index = gery / (<span class="number">255</span> / (size - <span class="number">1</span>)); <span class="comment">// 254*7/254</span></span><br><span class="line">                    g[index] += <span class="number">1</span>;</span><br><span class="line">                    <span class="comment">// 等级的像素之和</span></span><br><span class="line">                    b_g[index] += src.<span class="built_in">at</span>&lt;Vec4b&gt;(row + o_rows,col + o_cols)[<span class="number">0</span>];</span><br><span class="line">                    g_g[index] += src.<span class="built_in">at</span>&lt;Vec4b&gt;(row + o_rows,col + o_cols)[<span class="number">1</span>];</span><br><span class="line">                    r_g[index] += src.<span class="built_in">at</span>&lt;Vec4b&gt;(row + o_rows,col + o_cols)[<span class="number">2</span>];</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 最大的角标找出来</span></span><br><span class="line">            <span class="type">int</span> max_index = <span class="number">0</span>;</span><br><span class="line">            <span class="type">int</span> max = g[<span class="number">0</span>];</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt; size; ++i) &#123;</span><br><span class="line">                <span class="keyword">if</span>(g[max_index] &lt; g[i])&#123;</span><br><span class="line">                    max_index = i;</span><br><span class="line">                    max = g[i];</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 会超过 255，超过了的话 会截取最高位</span></span><br><span class="line">            res.<span class="built_in">at</span>&lt;Vec4b&gt;(row,col)[<span class="number">0</span>] = b_g[max_index] / max;</span><br><span class="line">            res.<span class="built_in">at</span>&lt;Vec4b&gt;(row,col)[<span class="number">1</span>] = g_g[max_index] / max;</span><br><span class="line">            res.<span class="built_in">at</span>&lt;Vec4b&gt;(row,col)[<span class="number">2</span>] = r_g[max_index] / max;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cv_helper::<span class="built_in">mat2bitmap</span>(env, res, bitmap);</span><br><span class="line">    <span class="keyword">return</span> bitmap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="8-处理速度性能优化"><a href="#8-处理速度性能优化" class="headerlink" title="8. 处理速度性能优化"></a>8. 处理速度性能优化</h1><p>运算速度：位运算 &gt; +- &gt;  */ 。 int &gt; float</p>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：17.手写直方图和直方均衡</title>
    <url>/65.OpenCV-%E6%89%8B%E5%86%99%E7%9B%B4%E6%96%B9%E5%9B%BE%E5%92%8C%E7%9B%B4%E6%96%B9%E5%9D%87%E8%A1%A1/</url>
    <content><![CDATA[<h1 id="1-亮度增强-线性方式"><a href="#1-亮度增强-线性方式" class="headerlink" title="1.亮度增强(线性方式)"></a>1.亮度增强(线性方式)</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/2_05.jpg&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>,src);</span><br><span class="line"></span><br><span class="line">	cout &lt;&lt; src.<span class="built_in">channels</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	Mat dst = src.<span class="built_in">clone</span>();</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src.rows; row++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src.cols; col++)</span><br><span class="line">		&#123;</span><br><span class="line">			Vec3b pixels = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row,col);</span><br><span class="line">			dst.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">0</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(pixels[<span class="number">0</span>] + <span class="number">30</span>);</span><br><span class="line">			dst.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">1</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(pixels[<span class="number">1</span>] + <span class="number">30</span>);</span><br><span class="line">			dst.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">2</span>] = <span class="built_in">saturate_cast</span>&lt;uchar&gt;(pixels[<span class="number">2</span>] + <span class="number">30</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>,dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-直方均衡，提升对比度"><a href="#2-直方均衡，提升对比度" class="headerlink" title="2.直方均衡，提升对比度"></a>2.直方均衡，提升对比度</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/2_05.jpg&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	Mat hsv;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src,hsv,COLOR_BGR2HSV);</span><br><span class="line"></span><br><span class="line">	vector&lt;Mat&gt; hsv_s;</span><br><span class="line">	<span class="built_in">split</span>(hsv,hsv_s);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">equalizeHist</span>(hsv_s[<span class="number">2</span>],hsv_s[<span class="number">2</span>]);</span><br><span class="line">	<span class="built_in">merge</span>(hsv_s,hsv);</span><br><span class="line">	<span class="built_in">cvtColor</span>(hsv,src,COLOR_HSV2BGR);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>,src);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-手写直方图计算源码"><a href="#3-手写直方图计算源码" class="headerlink" title="3. 手写直方图计算源码"></a>3. 手写直方图计算源码</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 手写直方图计算源码</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">calcHist</span><span class="params">(<span class="type">const</span> Mat &amp;mat,Mat &amp;hist)</span></span>&#123;</span><br><span class="line">	<span class="comment">// int 存</span></span><br><span class="line">	hist.<span class="built_in">create</span>(<span class="number">1</span>, <span class="number">256</span>, CV_32S);</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; hist.cols; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		hist.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(<span class="number">0</span>, i) = <span class="number">0</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; mat.rows; row++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; mat.cols; col++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="comment">// 灰度等级的角标</span></span><br><span class="line">			<span class="type">int</span> index = mat.<span class="built_in">at</span>&lt;uchar&gt;(row, col);</span><br><span class="line">			hist.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(<span class="number">0</span>, index) += <span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">normalize</span><span class="params">(<span class="type">const</span> Mat &amp;src, Mat &amp;dst, <span class="type">int</span> n_max)</span></span>&#123;</span><br><span class="line">	<span class="comment">// 0 ~ n_max;</span></span><br><span class="line">	<span class="type">int</span> max_value = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src.rows; row++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src.cols; col++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="type">int</span> value = src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row, col);</span><br><span class="line">			max_value = <span class="built_in">max</span>(value, max_value);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	dst.<span class="built_in">create</span>(src.<span class="built_in">size</span>(),src.<span class="built_in">type</span>());</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src.rows; row++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src.cols; col++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="type">int</span> value = src.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row, col);</span><br><span class="line">			dst.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(row, col) = <span class="number">1.0</span> * value / max_value * n_max;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 手写直方图计算源码</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/2_05.jpg&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src,gray,COLOR_BGR2GRAY);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;gray&quot;</span>,gray);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// hist 没有宽高，是生成了一个空的数组</span></span><br><span class="line">	Mat hist;</span><br><span class="line">	<span class="built_in">calcHist</span>(gray,hist);</span><br><span class="line">	cout &lt;&lt; hist &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 画直方图，归一化</span></span><br><span class="line">	<span class="built_in">normalize</span>(hist,hist,<span class="number">255</span>);</span><br><span class="line">	cout &lt;&lt; hist &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 画直方图</span></span><br><span class="line">	<span class="type">int</span> bin_w = <span class="number">2</span>;</span><br><span class="line">	<span class="type">int</span> grad = <span class="number">256</span>;</span><br><span class="line">	<span class="function">Mat <span class="title">hist_mat</span><span class="params">(grad,bin_w * <span class="number">256</span>,CV_8UC3)</span></span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; grad; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="function">Point <span class="title">start</span><span class="params">(i*bin_w, hist_mat.rows)</span></span>;</span><br><span class="line">		<span class="function">Point <span class="title">end</span><span class="params">(i*bin_w, hist_mat.rows - hist.at&lt;<span class="type">int</span>&gt;(<span class="number">0</span>,i))</span></span>;</span><br><span class="line">		<span class="built_in">line</span>(hist_mat, start, end, <span class="built_in">Scalar</span>(<span class="number">255</span>, <span class="number">0</span>, <span class="number">255</span>), bin_w, LINE_AA);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;hist_mat&quot;</span>,hist_mat);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-手写直方图均衡"><a href="#4-手写直方图均衡" class="headerlink" title="4. 手写直方图均衡"></a>4. 手写直方图均衡</h1><p>假设有如下图像：</p>
<p><img src="/images/65.1.jpg" alt=""></p>
<p>得图像的统计信息如下图所示，并根据统计信息完成灰度值映射:  </p>
<p><img src="/images/65.2.jpg" alt=""></p>
<p>映射后的图像如下所示：  </p>
<p><img src="/images/65.3.jpg" alt=""></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 4.1 直方图的统计</span></span><br><span class="line"><span class="comment">// 4.2 计算直方图中像素的概率</span></span><br><span class="line"><span class="comment">// 4.3 生成一张映射表(255 * 累加概率)</span></span><br><span class="line"><span class="comment">// 4.4 从映射表中查找赋值</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">equalizeHist</span><span class="params">(Mat &amp;src,Mat &amp;dst)</span></span>&#123;</span><br><span class="line">	Mat hist;</span><br><span class="line">	<span class="comment">// 1.直方图的统计</span></span><br><span class="line">	<span class="built_in">calcHist</span>(src,hist);</span><br><span class="line">	<span class="comment">// 2.计算直方图中像素的概率</span></span><br><span class="line">	<span class="function">Mat <span class="title">prob_mat</span><span class="params">(hist.size(),CV_32FC1)</span></span>;</span><br><span class="line"></span><br><span class="line">	<span class="type">float</span> image_size = src.rows * src.cols; <span class="comment">// 这个就是总的次数，因为每个像素值都算一次</span></span><br><span class="line">	<span class="comment">/*float image_size = 0;</span></span><br><span class="line"><span class="comment">	for (int i = 0; i &lt; hist.cols; i++)</span></span><br><span class="line"><span class="comment">	&#123;</span></span><br><span class="line"><span class="comment">		image_size += hist.at&lt;int&gt;(0, i);</span></span><br><span class="line"><span class="comment">	&#125;*/</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; hist.cols; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> times = hist.<span class="built_in">at</span>&lt;<span class="type">int</span>&gt;(<span class="number">0</span>, i);</span><br><span class="line">		<span class="type">float</span> prob = times / image_size;</span><br><span class="line">		prob_mat.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(<span class="number">0</span>, i) = prob;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 3. 计算累加概率 256 （可读）</span></span><br><span class="line">	<span class="type">float</span> prob_sum = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; hist.cols; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		prob_sum += prob_mat.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(<span class="number">0</span>, i);</span><br><span class="line">		prob_mat.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(<span class="number">0</span>, i) = prob_sum;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 生成映射表</span></span><br><span class="line">	<span class="function">Mat <span class="title">map</span><span class="params">(hist.size(), CV_32FC1)</span></span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; prob_mat.cols; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		map.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(<span class="number">0</span>, i) = <span class="number">255</span> * prob_mat.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(<span class="number">0</span>, i); <span class="comment">// 255 * 概率</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	dst.<span class="built_in">create</span>(src.<span class="built_in">size</span>(), src.<span class="built_in">type</span>());</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src.rows; row++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src.cols; col++)</span><br><span class="line">		&#123;</span><br><span class="line">			uchar pixels = src.<span class="built_in">at</span>&lt;uchar&gt;(row, col);</span><br><span class="line">			dst.<span class="built_in">at</span>&lt;uchar&gt;(row, col) = map.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(<span class="number">0</span>,pixels);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/2_05.jpg&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src, gray, COLOR_BGR2GRAY);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;gray&quot;</span>, gray);</span><br><span class="line">	<span class="built_in">equalizeHist</span>(gray,gray);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>,gray);</span><br><span class="line">	</span><br><span class="line">	<span class="comment">//vector&lt;Mat&gt; channels;</span></span><br><span class="line">	<span class="comment">//split(src,channels);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">//Mat blue = channels.at(0);</span></span><br><span class="line">	<span class="comment">//Mat green = channels.at(0);</span></span><br><span class="line">	<span class="comment">//Mat red = channels.at(0);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">//equalizeHist(blue, blue);</span></span><br><span class="line">	<span class="comment">//equalizeHist(green, green);</span></span><br><span class="line">	<span class="comment">//equalizeHist(red, red);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">//Mat dst;</span></span><br><span class="line">	<span class="comment">//merge(channels, dst);</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">//imshow(&quot;dst&quot;, dst);</span></span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：13.轮廓查找、图像矩、多边形测试、图像分水岭</title>
    <url>/61.OpenCV-%E8%BD%AE%E5%BB%93%E6%9F%A5%E6%89%BE%E3%80%81%E5%9B%BE%E5%83%8F%E7%9F%A9%E3%80%81%E5%A4%9A%E8%BE%B9%E5%BD%A2%E6%B5%8B%E8%AF%95%E3%80%81%E5%9B%BE%E5%83%8F%E5%88%86%E6%B0%B4%E5%B2%AD/</url>
    <content><![CDATA[<h1 id="1-银行卡轮廓查找与绘制"><a href="#1-银行卡轮廓查找与绘制" class="headerlink" title="1. 银行卡轮廓查找与绘制"></a>1. 银行卡轮廓查找与绘制</h1><p>使用到的api</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">参数说明：https://blog.csdn.net/guduruyu/article/details/69220296</span></span><br><span class="line"><span class="comment">输入图像image必须为一个2值单通道图像</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">contours参数为检测的轮廓数组，每一个轮廓用一个point类型的vector表示</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">hiararchy参数和轮廓个数相同，每个轮廓contours[ i ]对应4个hierarchy元素hierarchy[ i ][ 0 ] ~hierarchy[ i ][ 3 ]，</span></span><br><span class="line"><span class="comment">分别表示后一个轮廓、前一个轮廓、父轮廓、内嵌轮廓的索引编号，如果没有对应项，该值设置为负数。</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">mode表示轮廓的检索模式</span></span><br><span class="line"><span class="comment">CV_RETR_EXTERNAL 表示只检测外轮廓</span></span><br><span class="line"><span class="comment">CV_RETR_LIST 检测的轮廓不建立等级关系</span></span><br><span class="line"><span class="comment">CV_RETR_CCOMP 建立两个等级的轮廓，上面的一层为外边界，里面的一层为内孔的边界信息。如果内孔内还有一个连通物体，这个物体的边界也在顶层。</span></span><br><span class="line"><span class="comment">CV_RETR_TREE 建立一个等级树结构的轮廓。具体参考contours.c这个demo</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">method为轮廓的近似办法</span></span><br><span class="line"><span class="comment">CV_CHAIN_APPROX_NONE 存储所有的轮廓点，相邻的两个点的像素位置差不超过1，即max（abs（x1-x2），abs（y2-y1））==1</span></span><br><span class="line"><span class="comment">CV_CHAIN_APPROX_SIMPLE 压缩水平方向，垂直方向，对角线方向的元素，只保留该方向的终点坐标，例如一个矩形轮廓只需4个点来保存轮廓信息</span></span><br><span class="line"><span class="comment">CV_CHAIN_APPROX_TC89_L1，CV_CHAIN_APPROX_TC89_KCOS 使用teh-Chinl chain 近似算法</span></span><br><span class="line"><span class="comment">offset表示代表轮廓点的偏移量，可以设置为任意值。对ROI图像中找出的轮廓，并要在整个图像中进行分析时，这个参数还是很有用的。</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function">CV_EXPORTS_W <span class="type">void</span> <span class="title">findContours</span><span class="params">( InputArray image, OutputArrayOfArrays contours,</span></span></span><br><span class="line"><span class="params"><span class="function">                              OutputArray hierarchy, <span class="type">int</span> mode,</span></span></span><br><span class="line"><span class="params"><span class="function">                              <span class="type">int</span> method, Point offset = Point())</span></span>;</span><br><span class="line"><span class="comment">// 查找轮廓</span></span><br><span class="line"><span class="function">CV_EXPORTS <span class="type">void</span> <span class="title">findContours</span><span class="params">( InputArray image, OutputArrayOfArrays contours,</span></span></span><br><span class="line"><span class="params"><span class="function">                               <span class="type">int</span> mode, <span class="type">int</span> method, Point offset = Point())</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取可以包围轮廓的外层的矩形</span></span><br><span class="line"><span class="function">CV_EXPORTS_W Rect <span class="title">boundingRect</span><span class="params">( InputArray array )</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 绘制轮廓</span></span><br><span class="line"></span><br><span class="line"><span class="function">CV_EXPORTS_W <span class="type">void</span> <span class="title">drawContours</span><span class="params">( InputOutputArray image, InputArrayOfArrays contours,</span></span></span><br><span class="line"><span class="params"><span class="function">                              <span class="type">int</span> contourIdx, <span class="type">const</span> Scalar&amp; color,</span></span></span><br><span class="line"><span class="params"><span class="function">                              <span class="type">int</span> thickness = <span class="number">1</span>, <span class="type">int</span> lineType = LINE_8,</span></span></span><br><span class="line"><span class="params"><span class="function">                              InputArray hierarchy = noArray(),</span></span></span><br><span class="line"><span class="params"><span class="function">                              <span class="type">int</span> maxLevel = INT_MAX, Point offset = Point() )</span></span>;</span><br></pre></td></tr></table></figure>
<p>以下是银行卡的轮廓查找与绘制</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/card.jpg&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 梯度和二值化</span></span><br><span class="line">	Mat binary;</span><br><span class="line">	<span class="built_in">Canny</span>(src,binary,<span class="number">50</span>,<span class="number">150</span>);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;binary&quot;</span>, binary);</span><br><span class="line"></span><br><span class="line">	<span class="comment">//findContours(InputArray image, OutputArrayOfArrays contours,</span></span><br><span class="line">	<span class="comment">//	int mode, int method, Point offset = Point())</span></span><br><span class="line"></span><br><span class="line">	vector&lt;vector&lt;Point&gt;&gt; contours;</span><br><span class="line">	<span class="built_in">findContours</span>(binary,contours,RETR_EXTERNAL,CHAIN_APPROX_SIMPLE);</span><br><span class="line"></span><br><span class="line">	Mat contours_mat = Mat::<span class="built_in">zeros</span>(src.<span class="built_in">size</span>(),CV_8UC3);</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; contours.<span class="built_in">size</span>(); i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="comment">// 画轮廓 </span></span><br><span class="line">		<span class="comment">// boundingRect 获取可以包围轮廓的外层的矩形</span></span><br><span class="line">		Rect rect = <span class="built_in">boundingRect</span>(contours[i]);</span><br><span class="line">		<span class="keyword">if</span> (rect.width &gt; src.cols / <span class="number">2</span> &amp;&amp; rect.height &gt; src.rows / <span class="number">2</span>)&#123;</span><br><span class="line">			<span class="comment">// 画轮廓</span></span><br><span class="line">			<span class="built_in">drawContours</span>(contours_mat,contours,i,<span class="built_in">Scalar</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">255</span>),<span class="number">1</span>);</span><br><span class="line">			<span class="built_in">rectangle</span>(contours_mat, <span class="built_in">Point</span>(rect.x, rect.y), <span class="built_in">Point</span>(rect.x + rect.width, rect.y + rect.height), <span class="built_in">Scalar</span>(<span class="number">255</span>, <span class="number">255</span>, <span class="number">255</span>), <span class="number">2</span>);</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;contours_mat&quot;</span>, contours_mat);</span><br><span class="line">	</span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-图像矩-多边形测试"><a href="#2-图像矩-多边形测试" class="headerlink" title="2. 图像矩,多边形测试"></a>2. <a href="http://www.opencv.org.cn/opencvdoc/2.3.2/html/doc/tutorials/imgproc/shapedescriptors/point_polygon_test/point_polygon_test.html#point-polygon-test">图像矩,多边形测试</a></h1><p>使用到的api</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// measureDist: 为`true`的情况下, `&lt;0:在外面，&gt;0：在里面，==0：在轮廓上`。</span></span><br><span class="line"><span class="comment">// 为`false`的情况,`-1:在外面，1：在里面，0：在轮廓上`</span></span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">pointPolygonTest</span><span class="params">( InputArray contour, Point2f pt, <span class="type">bool</span> measureDist )</span></span></span><br><span class="line"><span class="function"></span></span><br></pre></td></tr></table></figure>
<p>图像矩的一个小例子</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	<span class="comment">/// 创建一个图形     </span></span><br><span class="line">	<span class="type">const</span> <span class="type">int</span> r = <span class="number">100</span>;</span><br><span class="line">	Mat src = Mat::<span class="built_in">zeros</span>(<span class="built_in">Size</span>(<span class="number">4</span> * r, <span class="number">4</span> * r), CV_8UC1);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/// 绘制一系列点创建一个轮廓:</span></span><br><span class="line">	<span class="function">vector&lt;Point2f&gt; <span class="title">vert</span><span class="params">(<span class="number">6</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">	vert[<span class="number">0</span>] = <span class="built_in">Point</span>(<span class="number">1.5</span>*r, <span class="number">1.34</span>*r);</span><br><span class="line">	vert[<span class="number">1</span>] = <span class="built_in">Point</span>(<span class="number">1</span> * r, <span class="number">2</span> * r);</span><br><span class="line">	vert[<span class="number">2</span>] = <span class="built_in">Point</span>(<span class="number">1.5</span>*r, <span class="number">2.866</span>*r);</span><br><span class="line">	vert[<span class="number">3</span>] = <span class="built_in">Point</span>(<span class="number">2.5</span>*r, <span class="number">2.866</span>*r);</span><br><span class="line">	vert[<span class="number">4</span>] = <span class="built_in">Point</span>(<span class="number">3</span> * r, <span class="number">2</span> * r);</span><br><span class="line">	vert[<span class="number">5</span>] = <span class="built_in">Point</span>(<span class="number">2.5</span>*r, <span class="number">1.34</span>*r);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/// 在src内部绘制</span></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; <span class="number">6</span>; j++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">line</span>(src, vert[j], vert[(j + <span class="number">1</span>) % <span class="number">6</span>], <span class="built_in">Scalar</span>(<span class="number">255</span>), <span class="number">3</span>, <span class="number">8</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 查找轮廓</span></span><br><span class="line">	vector&lt;vector&lt;Point&gt;&gt; contours;</span><br><span class="line">	<span class="built_in">findContours</span>(src,contours,RETR_EXTERNAL,CHAIN_APPROX_SIMPLE);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 计算到轮廓的距离</span></span><br><span class="line">	<span class="function">Mat <span class="title">raw_dist</span><span class="params">(src.size(),CV_32FC1)</span></span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src.rows; row++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src.cols; col++)</span><br><span class="line">		&#123;</span><br><span class="line">			raw_dist.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(row, col) = <span class="built_in">pointPolygonTest</span>(contours[<span class="number">0</span>], <span class="built_in">Point2f</span>(col, row), <span class="literal">true</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 优化</span></span><br><span class="line">	Mat drawing = Mat::<span class="built_in">zeros</span>(src.<span class="built_in">size</span>(),CV_8UC3);</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src.rows; row++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src.cols; col++)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="comment">// raw_dist.at&lt;float&gt;(row, col) = pointPolygonTest(contours[0], Point2f(col, row), true);</span></span><br><span class="line">			<span class="type">float</span> value = raw_dist.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(row, col);</span><br><span class="line">			<span class="keyword">if</span> (value &lt; <span class="number">0</span>)&#123; <span class="comment">// 外面</span></span><br><span class="line">				drawing.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">0</span>] = <span class="built_in">saturate_cast</span>&lt;<span class="type">int</span>&gt;((<span class="type">int</span>)<span class="built_in">abs</span>(value));</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> <span class="keyword">if</span> (value &gt; <span class="number">0</span>)&#123; <span class="comment">// 里面</span></span><br><span class="line">				drawing.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">2</span>] = <span class="built_in">saturate_cast</span>&lt;<span class="type">int</span>&gt;((<span class="type">int</span>)value);</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span>&#123;<span class="comment">// 矩形上面</span></span><br><span class="line">				drawing.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">0</span>] = <span class="number">255</span>;</span><br><span class="line">				drawing.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">1</span>] = <span class="number">255</span>;</span><br><span class="line">				drawing.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">2</span>] = <span class="number">255</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;drawing&quot;</span>, drawing);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-图像分水岭"><a href="#3-图像分水岭" class="headerlink" title="3. 图像分水岭"></a>3. 图像分水岭</h1><p>使用到的api</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">CV_EXPORTS_W <span class="type">void</span> <span class="title">watershed</span><span class="params">( InputArray image, InputOutputArray markers )</span></span>;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：16.封装 java 层 sdk 模型</title>
    <url>/64.OpenCV-%E5%B0%81%E8%A3%85%20java%20%E5%B1%82%20sdk%20%E6%A8%A1%E5%9E%8B/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/opencv64">NDKPractice项目的opencv64</a></strong></p>
<p>小的需求：<code>优化下Bitmap加载图片的源码（严格控制下内存）</code></p>
<h1 id="1-苏宁体育面试题讲解（一面）"><a href="#1-苏宁体育面试题讲解（一面）" class="headerlink" title="1. 苏宁体育面试题讲解（一面）"></a>1. 苏宁体育面试题讲解（一面）</h1><ul>
<li>HashMap 的实现，如何判断超出了原存储空间，超出了如何扩容，如果存入 key 相同，值如何存入。（之前手写过）</li>
<li>你所了解的设计模式，谈谈你对工厂设计模式的理解，单例设计模式如何避免内存泄露</li>
<li>事件分发机制</li>
<li>二叉树找最大</li>
<li>Activity 的启动模式，几种模式具体的应用。</li>
<li>静态方法存储在哪儿，堆和栈的垃圾回收机制</li>
</ul>
<p>面试考两点：1.<code>基础牢不牢</code>　2.<code>能不能搞定目前的项目</code></p>
<h1 id="2-NDK-学到什么程度"><a href="#2-NDK-学到什么程度" class="headerlink" title="2. NDK 学到什么程度"></a>2. NDK 学到什么程度</h1><p>c/c++ 少不了，基础，进阶，linux内核，shell脚本，cmake语法<br>opencv,ffmpeg<br>android开发：c/c++工程师开发（精通），java 工程师(精通)，native 和 java 层互通 NDK 开发(熟悉c/c++,熟悉android)  </p>
<ul>
<li>知道：有这么个东西，模糊  </li>
<li>熟悉：知道这个东西，讲明白，项目中也能直接用上  </li>
<li>精通：知道这个东西，讲明白，项目中也能直接用，知道原理（四种启动模式，怎么解析，怎么压栈弹栈，声明周期怎么调用）    </li>
</ul>
<p>读：第三方的源码，opencv，ffmpeg,openGL,android native.<br>写：那么多套路    </p>
<p>部门：引擎部门（.so库，java的sdk）有专门的 c/c++ 工程师，android 应用层开发</p>
<h1 id="3-如何封装-java-层-sdk-模型"><a href="#3-如何封装-java-层-sdk-模型" class="headerlink" title="3.如何封装 java 层 sdk 模型"></a>3.如何封装 java 层 sdk 模型</h1><p>假设我对一张图片做操作  </p>
<ul>
<li>掩膜操作</li>
<li>模糊操作</li>
<li>又来一个关于 filter2D 的操作</li>
</ul>
<p>规则考虑周到</p>
<ul>
<li>细节拆分</li>
<li>尽量不要改 native 层的代码，可以改 java 层的（<code>针对上面的需求可以把 filter2D 方法提到 java 层</code>）</li>
<li>思考：时间空间复杂度</li>
</ul>
<h1 id="4-动手封装OpenCV的Mat-java-对象"><a href="#4-动手封装OpenCV的Mat-java-对象" class="headerlink" title="4.动手封装OpenCV的Mat.java 对象"></a>4.动手封装OpenCV的Mat.java 对象</h1><p>filter2D -&gt; Mat  </p>
<p>Java层的Mat.java 对应 native 层的 Mat.cpp  </p>
<p>Java层写 Mat.java -&gt; Bitmap.java, Bitmap.java -&gt; Mat.java</p>
<h1 id="5-NDK异常处理需要注意的地方"><a href="#5-NDK异常处理需要注意的地方" class="headerlink" title="5.NDK异常处理需要注意的地方"></a>5.NDK异常处理需要注意的地方</h1><p>在前面有篇博客讲过 NDK 中的异常处理</p>
<h1 id="6-java和C-的回收机制"><a href="#6-java和C-的回收机制" class="headerlink" title="6.java和C++的回收机制"></a>6.java和C++的回收机制</h1><p>java的 GC 会调用 <code>finalize</code> 方法。</p>
<p>c 中需要自己回收</p>
]]></content>
      <categories>
        <category>OpenCV</category>
        <category>面试</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：18.人脸美容效果-积分图</title>
    <url>/66.OpenCV-%E4%BA%BA%E8%84%B8%E7%BE%8E%E5%AE%B9%E6%95%88%E6%9E%9C-%E7%A7%AF%E5%88%86%E5%9B%BE/</url>
    <content><![CDATA[<h1 id="1-双边滤波美容"><a href="#1-双边滤波美容" class="headerlink" title="1. 双边滤波美容"></a>1. 双边滤波美容</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/meirong.jpg&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>,src);</span><br><span class="line"></span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="built_in">bilateralFilter</span>(src,dst,<span class="number">0</span>,<span class="number">100</span>,<span class="number">5</span>);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>, dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-卷积模糊算法，时间复杂度分析"><a href="#2-卷积模糊算法，时间复杂度分析" class="headerlink" title="2.卷积模糊算法，时间复杂度分析"></a>2.卷积模糊算法，时间复杂度分析</h1><p><code>时间复杂度 = src.rows * src.cols * size * size</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/meirong.jpg&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 高斯（模糊），计算高斯卷积核，卷积操作，考虑像素之间的差值（更好的保留图像的边缘）</span></span><br><span class="line"></span><br><span class="line">	Mat dst;</span><br><span class="line"></span><br><span class="line">	<span class="type">int</span> size = <span class="number">10000</span>;</span><br><span class="line">	Mat kernel = Mat::<span class="built_in">ones</span>(size, size, CV_32FC1) / (size * size);</span><br><span class="line"></span><br><span class="line">	<span class="type">long</span> st = <span class="built_in">clock</span>();</span><br><span class="line">	<span class="built_in">filter2D</span>(src,dst,src.<span class="built_in">depth</span>(),kernel);</span><br><span class="line">	<span class="type">long</span> et = <span class="built_in">clock</span>();</span><br><span class="line">	<span class="type">double</span> time = <span class="built_in">static_cast</span>&lt;<span class="type">double</span>&gt;((et - st) / CLOCKS_PER_SEC);</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;time = &quot;</span> &lt;&lt; time &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="comment">//时间复杂度 = src.rows * src.cols * size * size</span></span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>, dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-积分图算法介绍"><a href="#3-积分图算法介绍" class="headerlink" title="3. 积分图算法介绍"></a>3. 积分图算法介绍</h1><p>看图更明白些：</p>
<p><img src="/images/66.积分图计算.png" alt=""></p>
<h1 id="4-使用积分图算法对卷积模糊进行优化"><a href="#4-使用积分图算法对卷积模糊进行优化" class="headerlink" title="4. 使用积分图算法对卷积模糊进行优化"></a>4. 使用积分图算法对卷积模糊进行优化</h1><p>使用到的 api</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// @param sum ：积分和表</span></span><br><span class="line"><span class="comment">// @param sqsum ：平方和表</span></span><br><span class="line"><span class="comment">// @param sdepth ：积分和表的深度</span></span><br><span class="line"><span class="comment">// @param sqdepth ：平方和表的深度 如果是：CV_32F，计算的时候就以 float 计算</span></span><br><span class="line"><span class="built_in">CV_EXPORTS_AS</span>(integral2) <span class="function"><span class="type">void</span> <span class="title">integral</span><span class="params">( InputArray src, OutputArray sum,</span></span></span><br><span class="line"><span class="params"><span class="function">                                        OutputArray sqsum, <span class="type">int</span> sdepth = <span class="number">-1</span>, <span class="type">int</span> sqdepth = <span class="number">-1</span> )</span></span>;</span><br></pre></td></tr></table></figure>
<p><img src="/images/66.积分图的模糊算法.png" alt=""></p>
<p><code>Vec3b:vector&lt;uchar, 3&gt; 不能超过 255</code></p>
<p><code>Vec3i:vector&lt;int, 3&gt;</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">meanBlur</span><span class="params">(Mat &amp;src, Mat &amp;dst, <span class="type">int</span> size)</span></span>&#123;</span><br><span class="line">	<span class="comment">// 卷积的 size 必须确保为基数 size % 2 == 1</span></span><br><span class="line">	<span class="comment">// 填充周围，方便计算</span></span><br><span class="line">	Mat mat;</span><br><span class="line">	<span class="type">int</span> radius = size / <span class="number">2</span>;</span><br><span class="line">	<span class="built_in">copyMakeBorder</span>(src,mat,radius,radius,radius,radius,BORDER_DEFAULT);</span><br><span class="line">	<span class="comment">// 求积分图</span></span><br><span class="line">	Mat sum_mat, sqsum_mat;</span><br><span class="line">	<span class="built_in">integral</span>(mat,sum_mat,sqsum_mat,CV_32S,CV_32S);</span><br><span class="line"></span><br><span class="line">	dst.<span class="built_in">create</span>(src.<span class="built_in">size</span>(),src.<span class="built_in">type</span>());</span><br><span class="line">	<span class="type">int</span> channels = src.<span class="built_in">channels</span>();</span><br><span class="line">	<span class="type">int</span> area = size * size;</span><br><span class="line">	<span class="comment">// 求四个点，左上，左下，右上，右下</span></span><br><span class="line">	<span class="type">int</span> x0 = <span class="number">0</span>, x1 = <span class="number">0</span>, y0 = <span class="number">0</span>, y1 = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src.rows; row++)</span><br><span class="line">	&#123;	</span><br><span class="line">		y0 = row;</span><br><span class="line">		y1 = y0 + size;</span><br><span class="line">		<span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src.cols; col++)</span><br><span class="line">		&#123;</span><br><span class="line">			x0 = col;</span><br><span class="line">			x1 = x0 + size;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; channels; i++)</span><br><span class="line">			&#123;</span><br><span class="line">				<span class="comment">// 获取四个点的值</span></span><br><span class="line">				<span class="type">int</span> lt = sum_mat.<span class="built_in">at</span>&lt;Vec3i&gt;(y0, x0)[i];</span><br><span class="line">				<span class="type">int</span> lb = sum_mat.<span class="built_in">at</span>&lt;Vec3i&gt;(y1, x0)[i];</span><br><span class="line">				<span class="type">int</span> rt = sum_mat.<span class="built_in">at</span>&lt;Vec3i&gt;(y0, x1)[i];</span><br><span class="line">				<span class="type">int</span> rb = sum_mat.<span class="built_in">at</span>&lt;Vec3i&gt;(y1, x1)[i];</span><br><span class="line"></span><br><span class="line">				<span class="comment">// 区块的合</span></span><br><span class="line">				<span class="type">int</span> sum = rb - lb - rt + lt;</span><br><span class="line">				dst.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[i] = sum / area;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/meirong.jpg&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 高斯（模糊），计算高斯卷积核，卷积操作，考虑像素之间的差值（更好的保留图像的边缘）</span></span><br><span class="line"></span><br><span class="line">	Mat dst;</span><br><span class="line">	<span class="type">int</span> size = <span class="number">15</span>; <span class="comment">// size == 10001 时 time = 1 秒</span></span><br><span class="line">	<span class="type">long</span> st = <span class="built_in">clock</span>();</span><br><span class="line">	<span class="built_in">meanBlur</span>(src,dst,size);</span><br><span class="line">	<span class="type">long</span> et = <span class="built_in">clock</span>();</span><br><span class="line">	<span class="type">double</span> time = <span class="built_in">static_cast</span>&lt;<span class="type">double</span>&gt;((et - st) / CLOCKS_PER_SEC);</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;time = &quot;</span> &lt;&lt; time &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dst&quot;</span>, dst);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-快速边缘保留算法"><a href="#5-快速边缘保留算法" class="headerlink" title="5.快速边缘保留算法"></a>5.快速边缘保留算法</h1><p>先看两张图：</p>
<p><img src="/images/66.快速边缘保留算法.png" alt=""></p>
<p><img src="/images/66.局部方差公式推导.png" alt=""></p>
<p>具体的请看<a href="https://github.com/EastUp/CPractice/blob/master/markdown/67.OpenCV-%E4%BA%BA%E8%84%B8%E7%BE%8E%E5%AE%B9%E6%95%88%E6%9E%9C2-%E8%BE%B9%E7%BC%98%E5%A4%84%E7%90%86.md">67.人脸美容效果2-边缘处理</a></p>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：15.图片的几何变换</title>
    <url>/63.OpenCV-%E5%9B%BE%E7%89%87%E7%9A%84%E5%87%A0%E4%BD%95%E5%8F%98%E6%8D%A2/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/opencv63">NDKPractice项目的opencv63</a></strong></p>
<h1 id="1-图片旋转"><a href="#1-图片旋转" class="headerlink" title="1. 图片旋转"></a>1. 图片旋转</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Java_com_east_opencv63_OpenCvUtils_rotation</span>(JNIEnv *env, jclass clazz, jobject bitmap) &#123;</span><br><span class="line">    Mat src;</span><br><span class="line">    cv_helper::<span class="built_in">bitmap2mat</span>(env, bitmap, src);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> res_w = src.rows; <span class="comment">// 图片的宽</span></span><br><span class="line">    <span class="type">int</span> res_h = src.cols; <span class="comment">// 图片的高</span></span><br><span class="line"></span><br><span class="line">    <span class="function">Mat <span class="title">res</span><span class="params">(res_h, res_w, src.type())</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 处理输出图像的下半部分</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; res_h; ++row) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; res_w; ++col) &#123;</span><br><span class="line">            <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC4)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec4b&gt;(row, col) = src.<span class="built_in">at</span>&lt;Vec4b&gt;(src.rows - col, row);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC2)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col) = src.<span class="built_in">at</span>&lt;Vec3b&gt;(src.rows - col, row);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC1)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;uchar&gt;(row, col) = src.<span class="built_in">at</span>&lt;uchar&gt;(src.rows - col, row);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建一个新的bitmap 宽是原来的高，高是原来的宽</span></span><br><span class="line">    jobject newBitmap = bitmap_util::<span class="built_in">create_bitmap</span>(env, res_w, res_h, res.<span class="built_in">type</span>());</span><br><span class="line">    cv_helper::<span class="built_in">mat2bitmap</span>(env, res, newBitmap);</span><br><span class="line">    <span class="keyword">return</span> newBitmap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-矩阵仿射变换"><a href="#2-矩阵仿射变换" class="headerlink" title="2. 矩阵仿射变换"></a>2. 矩阵仿射变换</h1><p>使用到的 api </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// center 旋转中心点  angle 逆时针 scale 缩放比例</span></span><br><span class="line"><span class="function">Mat <span class="title">getRotationMatrix2D</span><span class="params">(Point2f center, <span class="type">double</span> angle, <span class="type">double</span> scale)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="comment">// 图像仿射变换的 用到的 api</span></span></span><br><span class="line"><span class="function">CV_EXPORTS_W <span class="type">void</span> <span class="title">warpAffine</span><span class="params">( InputArray src, OutputArray dst,</span></span></span><br><span class="line"><span class="params"><span class="function">                              InputArray M, Size dsize,</span></span></span><br><span class="line"><span class="params"><span class="function">                              <span class="type">int</span> flags = INTER_LINEAR,</span></span></span><br><span class="line"><span class="params"><span class="function">                              <span class="type">int</span> borderMode = BORDER_CONSTANT,</span></span></span><br><span class="line"><span class="params"><span class="function">                              <span class="type">const</span> Scalar&amp; borderValue = Scalar())</span></span>;</span><br></pre></td></tr></table></figure>
<p>代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 图片仿射变换</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span></span><br><span class="line"><span class="function">JNIEXPORT jobject JNICALL</span></span><br><span class="line"><span class="function"><span class="title">Java_com_east_opencv63_OpenCvUtils_warpAffine</span><span class="params">(JNIEnv *env, jclass clazz, jobject bitmap)</span> </span>&#123;</span><br><span class="line">    Mat src;</span><br><span class="line">    cv_helper::<span class="built_in">bitmap2mat</span>(env, bitmap, src);</span><br><span class="line"></span><br><span class="line">    <span class="function">Mat <span class="title">res</span><span class="params">(src.size(), src.type())</span></span>;</span><br><span class="line">    <span class="comment">/*Mat M(2,3,CV_32FC1);</span></span><br><span class="line"><span class="comment">    // 这几个值应该怎么确定？</span></span><br><span class="line"><span class="comment">    // [a0,a1,a2]     两个矩阵    [a0,a1]    [a2]       =     [ x ]     *     [a0,a1]    +   [a2]    =   a0*x+b0*x + a2</span></span><br><span class="line"><span class="comment">    // [b0,b1,b2]                 [b0,b1]    [b2]             [ y ]           [b0,b1]        [b2]    =   b1*y+b1*y + b2</span></span><br><span class="line"><span class="comment">    M.at&lt;float&gt;(0,0) = 1;// a0</span></span><br><span class="line"><span class="comment">    M.at&lt;float&gt;(0,1) = 0;// a1</span></span><br><span class="line"><span class="comment">    M.at&lt;float&gt;(0,2) = 0;// a2</span></span><br><span class="line"><span class="comment">    M.at&lt;float&gt;(1,0) = 0; // b1</span></span><br><span class="line"><span class="comment">    M.at&lt;float&gt;(1,1) = 1; // b2</span></span><br><span class="line"><span class="comment">    M.at&lt;float&gt;(1,2) = 0;*/</span> <span class="comment">//b3</span></span><br><span class="line"></span><br><span class="line">    <span class="function">Point2f <span class="title">center</span><span class="params">(src.cols / <span class="number">2</span>, src.rows / <span class="number">2</span>)</span></span>;</span><br><span class="line">    Mat M = <span class="built_in">getRotationMatrix2D</span>(center, <span class="number">45</span>, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">warpAffine</span>(src, res, M, src.<span class="built_in">size</span>());</span><br><span class="line"></span><br><span class="line">    cv_helper::<span class="built_in">mat2bitmap</span>(env, res, bitmap);</span><br><span class="line">    <span class="keyword">return</span> bitmap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-reSize-图片缩放"><a href="#3-reSize-图片缩放" class="headerlink" title="3. reSize() 图片缩放"></a>3. reSize() 图片缩放</h1><ul>
<li>INTER_NEAREST - 最近邻插值</li>
<li>INTER_LINEAR - 线性插值（默认值）</li>
<li>INTER_AREA - 区域插值（利用像素区域关系的重采样插值）</li>
<li>INTER_CUBIC –三次样条插值（超过4×4像素邻域内的双三次插值）</li>
<li>INTER_LANCZOS4 -Lanczos插值（超过8×8像素邻域的Lanczos插值）</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 图片缩放</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span></span><br><span class="line"><span class="function">JNIEXPORT jobject JNICALL</span></span><br><span class="line"><span class="function"><span class="title">Java_com_east_opencv63_OpenCvUtils_resize</span><span class="params">(JNIEnv *env, jclass clazz, jobject bitmap, jint width,</span></span></span><br><span class="line"><span class="params"><span class="function">                                          jint height)</span> </span>&#123;</span><br><span class="line">    Mat src;</span><br><span class="line">    cv_helper::<span class="built_in">bitmap2mat</span>(env, bitmap, src);</span><br><span class="line">    <span class="function">Mat <span class="title">res</span><span class="params">(height, width, src.type())</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> src_w = src.cols;</span><br><span class="line">    <span class="type">float</span> src_h = src.rows;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; height; ++row) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; width; ++col) &#123;</span><br><span class="line"></span><br><span class="line">            <span class="type">int</span> src_row = row * (src_h / height);</span><br><span class="line">            <span class="type">int</span> src_col = col * (src_w / width);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC4)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec4b&gt;(row, col) = src.<span class="built_in">at</span>&lt;Vec4b&gt;(src_row, src_col);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC2)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col) = src.<span class="built_in">at</span>&lt;Vec3b&gt;(src_row, src_col);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (src.<span class="built_in">type</span>() == CV_8UC1)</span><br><span class="line">                res.<span class="built_in">at</span>&lt;uchar&gt;(row, col) = src.<span class="built_in">at</span>&lt;uchar&gt;(src_row, src_col);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    jobject newBitmap = bitmap_util::<span class="built_in">create_bitmap</span>(env, width, height, res.<span class="built_in">type</span>());</span><br><span class="line">    cv_helper::<span class="built_in">mat2bitmap</span>(env, res, newBitmap);</span><br><span class="line">    <span class="keyword">return</span> newBitmap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-remap-重映射-自己实现"><a href="#4-remap-重映射-自己实现" class="headerlink" title="4.remap 重映射  (自己实现)"></a>4.remap 重映射  (自己实现)</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">remap</span><span class="params">(Mat &amp;src,Mat &amp;res,Mat &amp;matX,Mat &amp;matY)</span></span>&#123;</span><br><span class="line">    res.<span class="built_in">create</span>(src.<span class="built_in">size</span>(),src.<span class="built_in">type</span>());</span><br><span class="line">    <span class="type">int</span> res_w = res.cols;</span><br><span class="line">    <span class="type">int</span> res_h = res.rows;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; res_h; ++row) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; res_w; ++col) &#123;</span><br><span class="line">            <span class="type">int</span> x = matX.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(row,col);</span><br><span class="line">            <span class="type">int</span> y = matY.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(row,col);</span><br><span class="line">            res.<span class="built_in">at</span>&lt;Vec4b&gt;(row,col) = src.<span class="built_in">at</span>&lt;Vec4b&gt;(y,x);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 手写 Remap 重映射</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span></span><br><span class="line"><span class="function">JNIEXPORT jobject JNICALL</span></span><br><span class="line"><span class="function"><span class="title">Java_com_east_opencv63_OpenCvUtils_reMap</span><span class="params">(JNIEnv *env, jclass clazz, jobject bitmap)</span> </span>&#123;</span><br><span class="line">    Mat src;</span><br><span class="line">    cv_helper::<span class="built_in">bitmap2mat</span>(env, bitmap, src);</span><br><span class="line">    Mat res;</span><br><span class="line"></span><br><span class="line">    <span class="function">Mat <span class="title">matX</span><span class="params">(src.size(), CV_32F)</span></span>;</span><br><span class="line">    <span class="function">Mat <span class="title">matY</span><span class="params">(src.size(), CV_32F)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src.rows; ++row) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src.cols; ++col) &#123;</span><br><span class="line">            matX.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(row, col) = src.cols - col;</span><br><span class="line">            matY.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(row, col) = src.rows - row;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">remap</span>(src, res, matX, matY);</span><br><span class="line">    cv_helper::<span class="built_in">mat2bitmap</span>(env,res,bitmap);</span><br><span class="line">    <span class="keyword">return</span> bitmap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：20.二维码检测</title>
    <url>/68.OpenCV-%E4%BA%8C%E7%BB%B4%E7%A0%81%E6%A3%80%E6%B5%8B/</url>
    <content><![CDATA[<h1 id="1-二维码检测"><a href="#1-二维码检测" class="headerlink" title="1.二维码检测"></a>1.二维码检测</h1><p>思路：</p>
<p><img src="/images/68.二维码检测思路.png" alt=""></p>
<p>代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 如果是倾斜的 变换菱形 放正</span></span><br><span class="line"><span class="function">Mat <span class="title">wrapTransfrom</span><span class="params">(<span class="type">const</span> Mat &amp;gray,<span class="type">const</span> RotatedRect &amp;rect)</span></span>&#123;</span><br><span class="line">	<span class="type">int</span> width = rect.size.width;</span><br><span class="line">	<span class="type">int</span> height = rect.size.height;</span><br><span class="line">	<span class="function">Mat <span class="title">result</span><span class="params">(Size(width,height),gray.type())</span></span>;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 矩阵怎么获取</span></span><br><span class="line">	vector&lt;Point&gt; srcPoints;</span><br><span class="line">	Point2f pts[<span class="number">4</span>];</span><br><span class="line">	rect.<span class="built_in">points</span>(pts);</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">4</span>; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		srcPoints.<span class="built_in">push_back</span>(pts[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	vector&lt;Point&gt; dstPoints;</span><br><span class="line">	dstPoints.<span class="built_in">push_back</span>(<span class="built_in">Point</span>(<span class="number">0</span>,<span class="number">0</span>));</span><br><span class="line">	dstPoints.<span class="built_in">push_back</span>(<span class="built_in">Point</span>(width, <span class="number">0</span>));</span><br><span class="line">	dstPoints.<span class="built_in">push_back</span>(<span class="built_in">Point</span>(width, height));</span><br><span class="line">	dstPoints.<span class="built_in">push_back</span>(<span class="built_in">Point</span>(<span class="number">0</span>, height));</span><br><span class="line">	</span><br><span class="line">	Mat M = <span class="built_in">findHomography</span>(srcPoints,dstPoints);</span><br><span class="line">	<span class="built_in">warpPerspective</span>(gray,result,M,result.<span class="built_in">size</span>());</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isXCorner</span><span class="params">(<span class="type">const</span> Mat &amp;qrROI)</span></span>&#123;</span><br><span class="line">	<span class="comment">// 四个值</span></span><br><span class="line">	<span class="comment">//cb:中心的黑色块数量</span></span><br><span class="line">	<span class="comment">//lw:左边的白色块数量</span></span><br><span class="line">	<span class="comment">//rw:右边的白色块数量</span></span><br><span class="line">	<span class="comment">//lb:左边的黑色块数量</span></span><br><span class="line">	<span class="comment">//rb:右边的黑色块数量</span></span><br><span class="line">	<span class="type">int</span> cb = <span class="number">0</span>, lw = <span class="number">0</span>, rw = <span class="number">0</span>, lb = <span class="number">0</span>, rb = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">	<span class="type">int</span> width = qrROI.cols; <span class="comment">//36</span></span><br><span class="line">	<span class="type">int</span> height = qrROI.rows; <span class="comment">//36</span></span><br><span class="line">	<span class="type">int</span> cx = width / <span class="number">2</span>;  <span class="comment">// 18</span></span><br><span class="line">	<span class="type">int</span> cy = height / <span class="number">2</span>; <span class="comment">// 18</span></span><br><span class="line">	uchar pixels = qrROI.<span class="built_in">at</span>&lt;uchar&gt;(cy, cx);</span><br><span class="line">	<span class="keyword">if</span> (pixels == <span class="number">255</span>)&#123; <span class="comment">// 中间肯定是黑色的色块</span></span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 求中心黑色</span></span><br><span class="line">	<span class="type">int</span> start = <span class="number">0</span>, end = <span class="number">0</span>, offset = <span class="number">0</span>;</span><br><span class="line">	<span class="type">bool</span> findleft = <span class="literal">false</span>, findright = <span class="literal">false</span>;</span><br><span class="line">	<span class="keyword">while</span> (<span class="literal">true</span>)&#123;</span><br><span class="line">		offset++;</span><br><span class="line">		<span class="keyword">if</span> (offset &gt;= cx)</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 中心点往左边扫</span></span><br><span class="line">		pixels = qrROI.<span class="built_in">at</span>&lt;uchar&gt;(cy, cx - offset);</span><br><span class="line">		<span class="keyword">if</span> (!findleft &amp;&amp; pixels == <span class="number">255</span>)&#123;</span><br><span class="line">			start = cx - offset; <span class="comment">// 左边白色的像素位置 12</span></span><br><span class="line">			findleft = <span class="literal">true</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 中心点往右边扫</span></span><br><span class="line">		pixels = qrROI.<span class="built_in">at</span>&lt;uchar&gt;(cy, cx + offset);</span><br><span class="line">		<span class="keyword">if</span> (!findright &amp;&amp; pixels == <span class="number">255</span>)&#123;</span><br><span class="line">			end = cx + offset; <span class="comment">// 右边白色的像素位置  25</span></span><br><span class="line">			findright = <span class="literal">true</span>;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (findleft &amp;&amp; findright)</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (start == <span class="number">0</span> || end == <span class="number">0</span>)</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">	cb = end - start;</span><br><span class="line">	<span class="comment">// 相间的白色</span></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> col = end; col &lt; width - <span class="number">1</span>; col++)</span><br><span class="line">	&#123;</span><br><span class="line">		pixels = qrROI.<span class="built_in">at</span>&lt;uchar&gt;(cy, col);</span><br><span class="line">		<span class="keyword">if</span> (pixels == <span class="number">0</span>)</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		rw++;  <span class="comment">// 5</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> col = start; col &gt; <span class="number">0</span>; col--)</span><br><span class="line">	&#123;</span><br><span class="line">		pixels = qrROI.<span class="built_in">at</span>&lt;uchar&gt;(cy, col);</span><br><span class="line">		<span class="keyword">if</span> (pixels == <span class="number">0</span>)</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		lw++;  <span class="comment">// 6</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (rw == <span class="number">0</span> || lw == <span class="number">0</span>)</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">	</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 两边的黑色</span></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> col = end+rw; col &lt; width - <span class="number">1</span>; col++)</span><br><span class="line">	&#123;</span><br><span class="line">		pixels = qrROI.<span class="built_in">at</span>&lt;uchar&gt;(cy, col);</span><br><span class="line">		<span class="keyword">if</span> (pixels == <span class="number">255</span>)</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		rb++;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> col = start - lw; col &gt; <span class="number">0</span>; col--)</span><br><span class="line">	&#123;</span><br><span class="line">		pixels = qrROI.<span class="built_in">at</span>&lt;uchar&gt;(cy, col);</span><br><span class="line">		<span class="keyword">if</span> (pixels == <span class="number">255</span>)</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		lb++;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (rb == <span class="number">0</span> || lb == <span class="number">0</span>)</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">	<span class="type">float</span> sum = cb + lb + rb + lw + rw;</span><br><span class="line"></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;cx = &quot;</span> &lt;&lt; cx &lt;&lt; <span class="string">&quot;,,,cy =&quot;</span> &lt;&lt; cy &lt;&lt; <span class="string">&quot;,,,width = &quot;</span> &lt;&lt; width &lt;&lt; <span class="string">&quot;,,,height =&quot;</span> &lt;&lt; height &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;start = &quot;</span> &lt;&lt; start &lt;&lt; <span class="string">&quot;,,,end =&quot;</span> &lt;&lt; end &lt;&lt; <span class="string">&quot;,,,cb =&quot;</span> &lt;&lt; cb &lt;&lt; <span class="string">&quot;,,,lw = &quot;</span> &lt;&lt; lw &lt;&lt; <span class="string">&quot;,,,rw =&quot;</span> &lt;&lt; rw &lt;&lt; <span class="string">&quot;,,,lb =&quot;</span> &lt;&lt; lb &lt;&lt; <span class="string">&quot;,,,rb =&quot;</span> &lt;&lt; rb &lt;&lt; <span class="string">&quot;,,,sum =&quot;</span> &lt;&lt; sum &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 求比例 3:1:1:1:1</span></span><br><span class="line">	cb = (cb / sum)*<span class="number">7.0</span> + <span class="number">0.5</span>;</span><br><span class="line">	lb = (lb / sum)*<span class="number">7.0</span> + <span class="number">0.5</span>;</span><br><span class="line">	rb = (rb / sum)*<span class="number">7.0</span> + <span class="number">0.5</span>;</span><br><span class="line">	lw = (lw / sum)*<span class="number">7.0</span> + <span class="number">0.5</span>;</span><br><span class="line">	rw = (rw / sum)*<span class="number">7.0</span> + <span class="number">0.5</span>;</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;start = &quot;</span> &lt;&lt; start &lt;&lt; <span class="string">&quot;,,,end =&quot;</span> &lt;&lt; end &lt;&lt; <span class="string">&quot;,,,cb =&quot;</span> &lt;&lt; cb &lt;&lt; <span class="string">&quot;,,,lw = &quot;</span> &lt;&lt; lw &lt;&lt; <span class="string">&quot;,,,rw =&quot;</span> &lt;&lt; rw &lt;&lt; <span class="string">&quot;,,,lb =&quot;</span> &lt;&lt; lb &lt;&lt; <span class="string">&quot;,,,rb =&quot;</span> &lt;&lt; rb &lt;&lt; <span class="string">&quot;,,,sum =&quot;</span> &lt;&lt; sum &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> ((cb == <span class="number">3</span> || cb == <span class="number">4</span>) &amp;&amp; (lw == rw) &amp;&amp; (lb == rb) &amp;&amp; (lw == <span class="number">1</span>))&#123;</span><br><span class="line">		<span class="comment">// 3:1:1:1:1 或 4:1:1:1:1</span></span><br><span class="line">		<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 最好还是加上</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isYCorner</span><span class="params">(<span class="type">const</span> Mat&amp; qrROI)</span></span>&#123;</span><br><span class="line">	<span class="comment">// 统计白色像素点和黑色像素点</span></span><br><span class="line">	<span class="type">int</span> bp = <span class="number">0</span>, wp = <span class="number">0</span>;</span><br><span class="line">	<span class="type">int</span> width = qrROI.cols;</span><br><span class="line">	<span class="type">int</span> height = qrROI.rows;</span><br><span class="line">	<span class="type">int</span> cx = width / <span class="number">2</span>;  <span class="comment">// 3</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 中心点是黑色</span></span><br><span class="line">	<span class="type">int</span> pv = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; height; row++)</span><br><span class="line">	&#123;</span><br><span class="line">		pv = qrROI.<span class="built_in">at</span>&lt;uchar&gt;(row, cx);</span><br><span class="line">		<span class="keyword">if</span> (pv == <span class="number">0</span>)&#123;</span><br><span class="line">			bp++;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span>(pv == <span class="number">255</span>)&#123;</span><br><span class="line">			wp++;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (bp == <span class="number">0</span> || wp == <span class="number">0</span>)</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (wp * <span class="number">2</span> &gt; bp || bp &gt; <span class="number">4</span> * wp)&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/qcode1.jpg&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>,src);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 1.对图像进行灰度图处理</span></span><br><span class="line">	Mat gray;</span><br><span class="line">	<span class="built_in">cvtColor</span>(src,gray,COLOR_BGR2GRAY);</span><br><span class="line">	<span class="comment">// 2. 二值化处理</span></span><br><span class="line">	<span class="built_in">threshold</span>(gray,gray,<span class="number">0</span>,<span class="number">255</span>,THRESH_BINARY | THRESH_OTSU);</span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;threshold&quot;</span>, gray);</span><br><span class="line">	<span class="comment">// 轮廓发现</span></span><br><span class="line">	vector&lt;vector&lt;Point&gt;&gt; contours;</span><br><span class="line">	<span class="built_in">findContours</span>(gray,contours,RETR_LIST,CHAIN_APPROX_SIMPLE);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; contours.<span class="built_in">size</span>(); i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="comment">// 过滤面积</span></span><br><span class="line">		<span class="type">double</span> area = <span class="built_in">contourArea</span>(contours[i]);</span><br><span class="line">		<span class="keyword">if</span> (area &lt; <span class="number">49</span>)</span><br><span class="line">			<span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 过滤宽高比和宽高大小</span></span><br><span class="line">		RotatedRect rRect = <span class="built_in">minAreaRect</span>(contours[i]);</span><br><span class="line">		<span class="type">float</span> w = rRect.size.width;</span><br><span class="line">		<span class="type">float</span> h = rRect.size.height;</span><br><span class="line">		<span class="type">float</span> ratio = <span class="built_in">min</span>(w,h) / <span class="built_in">max</span>(w,h);</span><br><span class="line">		<span class="keyword">if</span> (ratio &gt; <span class="number">0.9</span> &amp;&amp; w &lt; gray.cols / <span class="number">2</span> &amp;&amp; h &lt; gray.rows / <span class="number">2</span>)&#123;</span><br><span class="line">			<span class="comment">// 去分析，找到满足宽高比的，满足宽高大小的</span></span><br><span class="line">			Mat qrROI = <span class="built_in">wrapTransfrom</span>(gray, rRect);</span><br><span class="line">			<span class="keyword">if</span> (<span class="built_in">isYCorner</span>(qrROI) &amp;&amp; <span class="built_in">isXCorner</span>(qrROI))&#123;</span><br><span class="line">				<span class="built_in">drawContours</span>(src, contours, i, <span class="built_in">Scalar</span>(<span class="number">0</span>, <span class="number">0</span>, <span class="number">255</span>), <span class="number">4</span>);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">imshow</span>(<span class="string">&quot;dstsrc&quot;</span>,src);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：22.实时人脸检测</title>
    <url>/70.OpenCV-%E5%AE%9E%E6%97%B6%E4%BA%BA%E8%84%B8%E6%A3%80%E6%B5%8B/</url>
    <content><![CDATA[<h1 id="1-相机实时人脸检测"><a href="#1-相机实时人脸检测" class="headerlink" title="1. 相机实时人脸检测"></a>1. 相机实时人脸检测</h1><p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/opencv70">NDKPractice项目的opencv70</a></strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// @param 1.1 会采用上采样、下采样、缩放比例</span></span><br><span class="line"><span class="comment">// @param 3 会检测多少次才能判断是个人脸</span></span><br><span class="line"><span class="comment">// @param Size(width/4,height/4) 最小的人脸大小</span></span><br><span class="line"><span class="comment">// @param Size(width,height) 最大的人脸</span></span><br><span class="line">cascadeClassifier.<span class="built_in">detectMultiScale</span>(gray,faces,<span class="number">1.1</span>,<span class="number">3</span>,<span class="number">0</span>,<span class="built_in">Size</span>(width/<span class="number">4</span>,height/<span class="number">4</span>),<span class="built_in">Size</span>(width,height));</span><br></pre></td></tr></table></figure>
<h1 id="2-人脸识别相关：-均值，方差与协方差、特征值与特征向量"><a href="#2-人脸识别相关：-均值，方差与协方差、特征值与特征向量" class="headerlink" title="2. 人脸识别相关： 均值，方差与协方差、特征值与特征向量"></a>2. 人脸识别相关： 均值，方差与协方差、特征值与特征向量</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">	Mat src = (<span class="built_in">Mat_</span>&lt;<span class="type">double</span>&gt;(<span class="number">3</span>, <span class="number">3</span>) &lt;&lt; <span class="number">50</span>, <span class="number">50</span>, <span class="number">50</span>, <span class="number">60</span>, <span class="number">60</span>, <span class="number">60</span>, <span class="number">70</span>, <span class="number">70</span>, <span class="number">70</span>);</span><br><span class="line">	<span class="comment">// 平均值 ：(50+50+50+...+70) / 9 = 60;</span></span><br><span class="line">	<span class="comment">// 标准方差（均方差）：各个数据与平均值之差的平方的和的平均数，再开根号</span></span><br><span class="line">	<span class="comment">// ((50-60)的平方 + (50-60)的平方 + (50-60)的平方 + (60-60)的平方... + (70-60)的平方)/9 = 开根号(66.6666666667) = 8.1649658093 </span></span><br><span class="line">	Mat mean, stddev; <span class="comment">// mean 平均值，stddev 标准方差</span></span><br><span class="line">	<span class="built_in">meanStdDev</span>(src, mean, stddev);</span><br><span class="line">	cout &lt;&lt; mean &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; stddev &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;========================================&quot;</span> &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 协方差矩阵</span></span><br><span class="line">	Mat covar;</span><br><span class="line">	<span class="built_in">calcCovarMatrix</span>(src,covar,mean,COVAR_NORMAL | COVAR_ROWS);</span><br><span class="line">	cout &lt;&lt; mean &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; covar &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;========================================&quot;</span> &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 协方差矩阵再去求特征和特征向量</span></span><br><span class="line">	src = (<span class="built_in">Mat_</span>&lt;<span class="type">double</span>&gt;(<span class="number">2</span>, <span class="number">2</span>) &lt;&lt; <span class="number">100</span>, <span class="number">100</span>, <span class="number">100</span>, <span class="number">100</span>);</span><br><span class="line">	Mat eigenvalues, eigenvectors;</span><br><span class="line">	<span class="built_in">eigen</span>(src, eigenvalues, eigenvectors);</span><br><span class="line">	cout &lt;&lt; eigenvalues &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; eigenvectors &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="built_in">getchar</span>();</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-人脸识别相关：PCA-原理与应用-（降维）"><a href="#3-人脸识别相关：PCA-原理与应用-（降维）" class="headerlink" title="3.人脸识别相关：PCA 原理与应用 （降维）"></a>3.人脸识别相关：PCA 原理与应用 （降维）</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 人脸识别相关：PCA 原理与应用 （降维）</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">	Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/copy.png&quot;</span>);</span><br><span class="line">	</span><br><span class="line">	<span class="type">int</span> size = src.rows * src.cols;</span><br><span class="line">	<span class="function">Mat <span class="title">data</span><span class="params">(size,<span class="number">3</span>,CV_8UC1)</span></span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; size; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> row = i / src.cols;</span><br><span class="line">		<span class="type">int</span> col = i - row * src.cols;</span><br><span class="line"></span><br><span class="line">		data.<span class="built_in">at</span>&lt;uchar&gt;(i, <span class="number">0</span>) = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">0</span>];</span><br><span class="line">		data.<span class="built_in">at</span>&lt;uchar&gt;(i, <span class="number">1</span>) = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">1</span>];</span><br><span class="line">		data.<span class="built_in">at</span>&lt;uchar&gt;(i, <span class="number">2</span>) = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">2</span>];</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 最终降维的数据</span></span><br><span class="line">	<span class="function">PCA <span class="title">pca_analyze</span><span class="params">(data, Mat(), PCA::Flags::DATA_AS_ROW)</span></span>;</span><br><span class="line"></span><br><span class="line">	cout &lt;&lt; pca_analyze.mean &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; pca_analyze.eigenvalues &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; pca_analyze.eigenvectors &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="built_in">getchar</span>();</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：21.人脸识别 - HOG 和 LBP 特征检测</title>
    <url>/69.OpenCV-%E4%BA%BA%E8%84%B8%E8%AF%86%E5%88%AB%20-%20HOG%20%E5%92%8C%20LBP%20%E7%89%B9%E5%BE%81%E6%A3%80%E6%B5%8B/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/opencv69">NDKPractice项目的opencv69</a></strong></p>
<p>人脸检测分为以下几步</p>
<ol>
<li>特征提取：边缘，角点，点，线，平坦区</li>
<li>训练样本：正向样，负向样本 （xml文件）</li>
<li>特征匹配：DDM , detection (发现)  description (描述)  matching (匹配) </li>
</ol>
<h1 id="1-HOG-Histogram-of-Oriented-Gradient-特征检测的实现原理"><a href="#1-HOG-Histogram-of-Oriented-Gradient-特征检测的实现原理" class="headerlink" title="1. HOG(Histogram of Oriented Gradient) 特征检测的实现原理"></a>1. HOG(Histogram of Oriented Gradient) 特征检测的实现原理</h1><p><img src="/images/69.hog特征.png" alt=""><br><img src="/images/69.hog分网格的梯度方向直方图.png" alt=""><br><img src="/images/69.hog块描述子.png" alt=""></p>
<p>HOG特征提取流程可分为5个部分：检测窗口、归一化图像、计算梯度、统计直方图、梯度直方图归一化、得到HOG特征向量，以下介绍下检测窗口</p>
<h2 id="1-1-检测窗口："><a href="#1-1-检测窗口：" class="headerlink" title="1.1 检测窗口："></a>1.1 检测窗口：</h2><p>HOG通过窗口（window)和块(block)将图像进行分割。通过以细胞(cell)为单位，对图像某一区域的像素值进行数学计算处理。在此先介绍窗口(window)、块(block)和细胞（cell）的概念及之间的联系。</p>
<ul>
<li>窗口(window)：将图像按一定大小分割成多个相同的窗口，滑动。</li>
<li>块（block)：将每个窗口按一定大小分割成多个相同的块，滑动。</li>
<li>细胞（cell)：将每个窗口按一定大小分割成多个相同的细胞，属于特征提取的单元，静止不动。</li>
<li>图像(image)-&gt;检测窗口(win)-&gt;图像块(block)-&gt;细胞单元(cell)</li>
</ul>
<p><img src="/images/69.hog检测窗口.jpg" alt=""></p>
<h1 id="2-实现行人检测"><a href="#2-实现行人检测" class="headerlink" title="2. 实现行人检测"></a>2. 实现行人检测</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 实现行人检测</span></span><br><span class="line"><span class="comment">// 训练样本，直接拿过来用</span></span><br><span class="line">Mat bgr; <span class="comment">// 只能用 bgr 格式的</span></span><br><span class="line"><span class="built_in">cvtColor</span>(src,bgr,COLOR_BGRA2BGR);</span><br><span class="line">HOGDescriptor descriptor;</span><br><span class="line">descriptor.<span class="built_in">setSVMDetector</span>(descriptor.<span class="built_in">getDefaultPeopleDetector</span>());</span><br><span class="line">vector&lt;Rect&gt; foundLocations;</span><br><span class="line">descriptor.<span class="built_in">detectMultiScale</span>(bgr,foundLocations,<span class="number">0</span>,<span class="built_in">Size</span>(<span class="number">12</span>,<span class="number">12</span>));</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; foundLocations.<span class="built_in">size</span>(); ++i) &#123;</span><br><span class="line">    <span class="built_in">rectangle</span>(src,foundLocations[i],<span class="built_in">Scalar</span>(<span class="number">255</span>,<span class="number">0</span>,<span class="number">0</span>),<span class="number">2</span>,LINE_AA);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-LBP-（Local-Binary-Patterns）特征介绍"><a href="#3-LBP-（Local-Binary-Patterns）特征介绍" class="headerlink" title="3.LBP （Local Binary Patterns）特征介绍"></a>3.LBP （Local Binary Patterns）特征介绍</h1><p><img src="/images/69.lbp.特征.png" alt=""><br><img src="/images/69.lbp表达.png" alt=""></p>
<h1 id="4-手写-LBP-特征算法实现"><a href="#4-手写-LBP-特征算法实现" class="headerlink" title="4.手写 LBP 特征算法实现"></a>4.手写 LBP 特征算法实现</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Java_com_east_opencv69_MainActivity_lbpBitmap</span>(JNIEnv *env, jobject thiz, jobject bitmap) &#123;</span><br><span class="line">    <span class="comment">// 自己手写 两套 3*3 考虑步长和角度</span></span><br><span class="line">    Mat src;</span><br><span class="line">    cv_helper::<span class="built_in">bitmap2mat</span>(env,bitmap,src);</span><br><span class="line">    Mat gray;</span><br><span class="line">    <span class="built_in">cvtColor</span>(src,gray,COLOR_BGRA2GRAY);</span><br><span class="line">    <span class="comment">// 3*3 lbp 计算的特征数据</span></span><br><span class="line">    Mat result = Mat::<span class="built_in">zeros</span>(<span class="built_in">Size</span>(src.cols - <span class="number">2</span>,src.rows - <span class="number">2</span>),CV_8UC1);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 有点类似卷积操作</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; gray.rows - <span class="number">1</span>; ++row) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; gray.cols - <span class="number">1</span>; ++col) &#123;</span><br><span class="line">            uchar pixels = gray.<span class="built_in">at</span>&lt;uchar&gt;(row,col); <span class="comment">// 中心点</span></span><br><span class="line">            <span class="type">int</span> rPixels = <span class="number">0</span>;</span><br><span class="line">            rPixels |= (pixels &lt;= gray.<span class="built_in">at</span>&lt;uchar&gt;(row<span class="number">-1</span>,col<span class="number">-1</span>)) &lt;&lt; <span class="number">0</span>;</span><br><span class="line">            rPixels |= (pixels &lt;= gray.<span class="built_in">at</span>&lt;uchar&gt;(row<span class="number">-1</span>,col)) &lt;&lt; <span class="number">1</span>;</span><br><span class="line">            rPixels |= (pixels &lt;= gray.<span class="built_in">at</span>&lt;uchar&gt;(row<span class="number">-1</span>,col<span class="number">+1</span>)) &lt;&lt; <span class="number">2</span>;</span><br><span class="line">            rPixels |= (pixels &lt;= gray.<span class="built_in">at</span>&lt;uchar&gt;(row,col<span class="number">-1</span>)) &lt;&lt; <span class="number">7</span>;</span><br><span class="line">            rPixels |= (pixels &lt;= gray.<span class="built_in">at</span>&lt;uchar&gt;(row,col<span class="number">+1</span>)) &lt;&lt; <span class="number">3</span>;</span><br><span class="line">            rPixels |= (pixels &lt;= gray.<span class="built_in">at</span>&lt;uchar&gt;(row<span class="number">+1</span>,col<span class="number">-1</span>)) &lt;&lt; <span class="number">6</span>;</span><br><span class="line">            rPixels |= (pixels &lt;= gray.<span class="built_in">at</span>&lt;uchar&gt;(row<span class="number">+1</span>,col)) &lt;&lt; <span class="number">5</span>;</span><br><span class="line">            rPixels |= (pixels &lt;= gray.<span class="built_in">at</span>&lt;uchar&gt;(row<span class="number">+1</span>,col<span class="number">+1</span>)) &lt;&lt; <span class="number">4</span>;</span><br><span class="line"></span><br><span class="line">            result.<span class="built_in">at</span>&lt;uchar&gt;(row<span class="number">-1</span>,col<span class="number">-1</span>) = rPixels;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    jobject  resBitmap = cv_helper::<span class="built_in">createBitmap</span>(env,result.cols,result.rows,src.<span class="built_in">type</span>());</span><br><span class="line">    cv_helper::<span class="built_in">mat2bitmap</span>(env,result,resBitmap);</span><br><span class="line">    <span class="keyword">return</span> resBitmap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-haar-特征介绍"><a href="#5-haar-特征介绍" class="headerlink" title="5.haar 特征介绍"></a>5.haar 特征介绍</h1><p><img src="/images/69.Haar特征.png" alt=""></p>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：23.实时人脸检测和识别</title>
    <url>/71.OpenCV-%E5%AE%9E%E6%97%B6%E4%BA%BA%E8%84%B8%E6%A3%80%E6%B5%8B%E5%92%8C%E8%AF%86%E5%88%AB/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/opencv71">NDKPractice项目的opencv71</a></strong></p>
<h1 id="1-训练人脸样本"><a href="#1-训练人脸样本" class="headerlink" title="1. 训练人脸样本"></a>1. 训练人脸样本</h1><p>三种训练方法：  </p>
<ul>
<li>eigenFace 算法 : EigenFaceRecognizer</li>
<li>FisherFace 算法：FisherFaceRecognizer</li>
<li>LBPH 算法：LBPHFaceRecognizer</li>
</ul>
<p><a href="https://blog.csdn.net/akenseren/article/details/80647276">网站1</a></p>
<p><a href="https://www.cnblogs.com/little-monkey/p/8118938.html">网站2</a></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Java_com_east_opencv71_FaceDetection_trainingPattern</span>(JNIEnv *env, jobject instance) &#123;</span><br><span class="line">    <span class="comment">// 训练样本，这一步是在数据采集做的</span></span><br><span class="line">    <span class="comment">// train it</span></span><br><span class="line">    vector&lt;Mat&gt; faces;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; labels;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 样本比较少</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">5</span>; ++i) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">1</span>; j &lt;= <span class="number">5</span>; ++j) &#123;</span><br><span class="line">            Mat face = <span class="built_in">imread</span>(format(<span class="string">&quot;/storage/emulated/0/s%d/%d.pgm&quot;</span>, i, j), <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">if</span> (face.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">                <span class="built_in">LOGE</span>(<span class="string">&quot;face mat is empty&quot;</span>);</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 确保大小一致</span></span><br><span class="line">            <span class="built_in">resize</span>(face, face, <span class="built_in">Size</span>(<span class="number">128</span>, <span class="number">128</span>));</span><br><span class="line">            faces.<span class="built_in">push_back</span>(face);</span><br><span class="line">            labels.<span class="built_in">push_back</span>(i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">8</span>; ++i) &#123;</span><br><span class="line">        Mat face = <span class="built_in">imread</span>(format(<span class="string">&quot;/storage/emulated/0/face_%d.png&quot;</span>, i), <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span> (face.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">            <span class="built_in">LOGE</span>(<span class="string">&quot;face mat is empty&quot;</span>);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">resize</span>(face, face, <span class="built_in">Size</span>(<span class="number">128</span>, <span class="number">128</span>));</span><br><span class="line">        faces.<span class="built_in">push_back</span>(face);</span><br><span class="line">        labels.<span class="built_in">push_back</span>(<span class="number">11</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 训练方法</span></span><br><span class="line">    Ptr&lt;BasicFaceRecognizer&gt; model = EigenFaceRecognizer::<span class="built_in">create</span>();</span><br><span class="line">    <span class="comment">// 采集了八张，同一个人 label 一样</span></span><br><span class="line">    model-&gt;<span class="built_in">train</span>(faces, labels);</span><br><span class="line">    <span class="comment">// 训练样本是 xml ，本地</span></span><br><span class="line">    model-&gt;<span class="built_in">save</span>(<span class="string">&quot;/storage/emulated/0/face_darren_pattern.xml&quot;</span>);<span class="comment">// 存的是处理的特征数据</span></span><br><span class="line">    <span class="built_in">LOGE</span>(<span class="string">&quot;樣本訓練成功&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-识别人脸"><a href="#2-识别人脸" class="headerlink" title="2. 识别人脸"></a>2. 识别人脸</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Java_com_east_opencv71_FaceDetection_loadPattern</span>(JNIEnv *env, jobject instance,</span><br><span class="line">                                                    jstring patternPath_) &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> *patternPath = env-&gt;<span class="built_in">GetStringUTFChars</span>(patternPath_, <span class="number">0</span>);</span><br><span class="line">    <span class="comment">// 加载样本数据，Error:(142, 12) error: no matching member function for call to &#x27;load&#x27; 怎么搞？</span></span><br><span class="line">    <span class="comment">// model-&gt;load(patternPath);</span></span><br><span class="line">    <span class="function">FileStorage <span class="title">fs</span><span class="params">(patternPath, FileStorage::READ)</span></span>;</span><br><span class="line">    <span class="comment">// 报错，没有提示</span></span><br><span class="line">    <span class="comment">// CV_Assert(fs.isOpened());</span></span><br><span class="line">    <span class="comment">// 抛个 java 异常</span></span><br><span class="line">    FileNode fn = fs.<span class="built_in">getFirstTopLevelNode</span>();</span><br><span class="line">    model-&gt;<span class="built_in">read</span>(fn);</span><br><span class="line">    env-&gt;<span class="built_in">ReleaseStringUTFChars</span>(patternPath_, patternPath);</span><br><span class="line">    <span class="built_in">LOGE</span>(<span class="string">&quot;训练样本加载成功&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：02.linux系统-常用命令</title>
    <url>/73.linux%E7%B3%BB%E7%BB%9F-%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h1 id="1-常用快捷键"><a href="#1-常用快捷键" class="headerlink" title="1. 常用快捷键"></a>1. 常用快捷键</h1><ul>
<li><code>table</code> 补齐</li>
<li><code>ctrl + a</code> : 光标移动到最前</li>
<li><code>ctrl + e</code>：光标移动到最后</li>
<li><code>ctrl + u</code>：清空</li>
<li><code>上，下键</code>：切换命令</li>
</ul>
<h1 id="2-文件操作"><a href="#2-文件操作" class="headerlink" title="2. 文件操作"></a>2. 文件操作</h1><h2 id="2-1-文件基本操作指令"><a href="#2-1-文件基本操作指令" class="headerlink" title="2.1 文件基本操作指令"></a>2.1 文件基本操作指令</h2><ul>
<li><code>cd xxx</code>：进入到某个目录</li>
<li><code>cd ..</code>：回退到上一级目录</li>
<li><code>ls</code> : 当前文件夹下面的所有文件</li>
<li><code>./date</code> ：执行当前目录下的可执行文件</li>
</ul>
<p>linux 系统中所见即文件（驱动）  , bin 目录一些执行文件，home 目录用户， lib 目录常用的 so ，opt 和 proc 是与进程相关的</p>
<ul>
<li><code>mkdir xxx</code>：创建文件夹</li>
<li><code>touch xxx.xxx</code>： 创建文件</li>
<li><code>ls -l</code> : 打印详细信息</li>
<li><code>ls -R</code> ：递归文件夹输出</li>
</ul>
<h2 id="2-2-文件类型"><a href="#2-2-文件类型" class="headerlink" title="2.2 文件类型"></a>2.2 文件类型</h2><p><code>drwxr-xr-x 1 ubuntu ubuntu 0 Apr 14 20:37 hello.c</code><br>代表：文件权限，硬链接计数，所有者，所属组，大小，时间，文件名</p>
<ul>
<li><code>d</code>:表示文件类型；</li>
<li><code>rwx</code>：表示文件所有者的对该文件所拥有的权限；</li>
</ul>
<p><code>-rw-rw-r--：- 文件类型，rw- 所有者可读可写，rw- 同一组用户可读可写，r-- 其他人可读</code></p>
<p><img src="/images/73.linux文件权限.jpg" alt=""></p>
<p>linux 系统文件类型：</p>
<ul>
<li>普通文件：-</li>
<li>目录文件：d</li>
<li>软连接：l</li>
<li>字符设备文件：c</li>
<li>块设备文件：b</li>
<li>管道文件：p</li>
<li>套接字：s</li>
</ul>
<h2 id="2-3-文件删除、拷贝、读取等"><a href="#2-3-文件删除、拷贝、读取等" class="headerlink" title="2.3 文件删除、拷贝、读取等"></a>2.3 文件删除、拷贝、读取等</h2><ul>
<li><code>rmdir ‘文件夹’</code> : 只能清空空目录文件夹</li>
<li><code>rm -r ‘文件夹’</code>：递归清空目录</li>
<li><code>rm ‘文件’</code>：删除文件</li>
<li><code>cp &#39;文件&#39; ‘文件夹路径’</code> ： 拷贝文件 -r 递归赋值</li>
<li><code>cat ‘文件’</code> ：查看文件内容</li>
<li><code>tac ‘文件’</code>：倒着查看文件</li>
<li><code>more ‘文件’</code>：每次只查看一页，空格查看下一页</li>
<li><code>less ‘文件’</code>：与 more 不同地方是, 回车是下一行，空格翻一页，没有百分百，退出 ctrl + c 不行，ESC之后  :q</li>
<li><code>head -2 ‘文件’</code>：查看前面2行</li>
<li><code>tail -20 ‘文件’</code>：查看后面20行</li>
</ul>
<p>删除文件时<code>rm</code>的参数</p>
<ul>
<li>-i 删除前逐一询问确认。</li>
<li>-f 即使原档案属性设为唯读，亦直接删除，无需逐一确认。</li>
<li>-r 将目录及以下之档案亦逐一删除。</li>
</ul>
<h1 id="3-用户与用户组"><a href="#3-用户与用户组" class="headerlink" title="3. 用户与用户组"></a>3. 用户与用户组</h1><ul>
<li>sudo：代表使用 root 权限</li>
<li><code>sudo su ‘用户’</code>：切换到 root 用户</li>
<li><code>exit</code> : 退出 当前 用户</li>
<li><code>whoami</code>：查看当前用户</li>
</ul>
<h2 id="3-1-修改用户文件权限"><a href="#3-1-修改用户文件权限" class="headerlink" title="3.1 修改用户文件权限"></a>3.1 修改用户文件权限</h2><p>新建的文件默认是不能执行的，我们需要修改用户文件权限</p>
<h3 id="3-1-1-文字设定法"><a href="#3-1-1-文字设定法" class="headerlink" title="3.1.1. 文字设定法"></a>3.1.1. 文字设定法</h3><p><code>chmod 用户的标识 +/- 权限 文件名：chmod u+x hello.sh</code><br>用户的标识：u(当前用户) ，g(同组)，o(other)，a(all)</p>
<h3 id="3-1-2-数字设定法"><a href="#3-1-2-数字设定法" class="headerlink" title="3.1.2. 数字设定法"></a>3.1.2. 数字设定法</h3><p><code>chmod nnn 文件名</code>  ,第一个n所属用户，第二个n所属用户组，第三个n其它用户<br>n = 4+2+1 = r+w+x </p>
<h2 id="3-2-用户和用户组的创建、修改、删除"><a href="#3-2-用户和用户组的创建、修改、删除" class="headerlink" title="3.2 用户和用户组的创建、修改、删除"></a>3.2 用户和用户组的创建、修改、删除</h2><ul>
<li><code>sudo adduser &#39;用户&#39;</code>： 创建了一个用户</li>
<li><code>sudo chown &#39;用户&#39; &#39;文件&#39;</code> ：修改文件的所属用户</li>
<li><code>sudo chgrp &#39;用户组&#39; &#39;文件&#39;</code> ：修改文件的所属组</li>
<li><code>sudo chown &#39;用户&#39;:&#39;用户组&#39; &#39;文件&#39;</code>：同时修改文件所属组：所属用户</li>
<li><code>sudo deluser &#39;用户&#39;</code>：删除用户，同时需要再删除“/home/用户名”的文件夹</li>
</ul>
<h1 id="4-搜索查找"><a href="#4-搜索查找" class="headerlink" title="4.搜索查找"></a>4.搜索查找</h1><h2 id="4-1-find-查找文件"><a href="#4-1-find-查找文件" class="headerlink" title="4.1 find 查找文件"></a>4.1 find 查找文件</h2><ul>
<li><code>find ./ -type ‘文件类型（文件是 -，目录是 d）’</code>（递归）</li>
<li><code>find ./ -name &#39;*.so&#39;</code> ： 根据文件名去查找</li>
<li><code>find ./ -maxdepth 1 -name &#39;*.so&#39;</code> ： 根据文件名去查找，只在当前文件夹查找，-maxdepth ：递归的层级</li>
<li><code>find ./ -maxdepth 1 -ctime -20</code>：将目前目录及其子目录下所有最近 20 天内更新过的文件列出</li>
</ul>
<p>再回顾下 文件类型</p>
<ul>
<li>d: 目录</li>
<li>c: 字型装置文件</li>
<li>b: 区块装置文件</li>
<li>p: 具名贮列</li>
<li>f: 一般文件</li>
<li>l: 符号连结</li>
<li>s: socket</li>
</ul>
<h2 id="4-2-grep-查找内容"><a href="#4-2-grep-查找内容" class="headerlink" title="4.2 grep 查找内容"></a>4.2 grep 查找内容</h2><ul>
<li><code>grep -r &quot;查询内容&quot; 文件目录</code>：grep -r ‘jaminniubi’ ./ 找当前文件夹中文件内容有jamin 的文件</li>
<li><code>grep -r -l   &quot;查询内容&quot;</code>  文件目录  ：  #这样只显示包含内容的文件名</li>
<li><code>ps aux</code>: 类似于任务管理器，把所有的进程信息都列举出来</li>
<li><code>ps aux | grep ‘内容’</code> : 找到了所有的任务，交给 grep 去执行搜索 ，如果只有一个代表没有找到，两个以上才算有</li>
</ul>
<p>| ： 管道 （进程间的通信），相当于 | 前的结果，作为输出给到 | 后</p>
<p>常用选项：</p>
<pre><code>　　-e ：开启扩展（Extend）的正则表达式。

　　-i ：忽略大小写（ignore case）。

　　-v ：反过来（invert），只打印没有匹配的，而匹配的反而不打印。

　　-n ：显示行号

　　-w ：被匹配的文本只能是单词，而不能是单词中的某一部分，如文本中有liker，而我搜寻的只是like，就可以使用-w选项来避免匹配liker

　　-c ：显示总共有多少行被匹配到了，而不是显示被匹配到的内容，注意如果同时使用-cv选项是显示有多少行没有被匹配到。

　　-o ：只显示被模式匹配到的字符串。

　　--color :将匹配到的内容以颜色高亮显示。

　　-A  n：显示匹配到的字符串所在的行及其后n行，after

　　-B  n：显示匹配到的字符串所在的行及其前n行，before

　　-C  n：显示匹配到的字符串所在的行及其前后各n行，context
</code></pre><h1 id="5-软件安装"><a href="#5-软件安装" class="headerlink" title="5.软件安装"></a>5.软件安装</h1><h2 id="5-1-命令行安装"><a href="#5-1-命令行安装" class="headerlink" title="5.1 命令行安装"></a>5.1 命令行安装</h2><ul>
<li><code>sudo apt-get install tree</code>：tree 是要安装的程序</li>
<li><code>sudo apt-get update</code>:更新程序</li>
<li><code>sudo apt-get remove tree</code>:软件卸载</li>
</ul>
<h2 id="5-2-压缩包安装"><a href="#5-2-压缩包安装" class="headerlink" title="5.2 压缩包安装"></a>5.2 压缩包安装</h2><p>这种格式一般会采用源代码编译安装，或是解压完直接就可以运行的方式，可以通过查看目录内是否有源代码或是configure文件来确实是不是源代码。</p>
<ul>
<li><code>./configure                       //配置</code></li>
<li><code>make                              //编译</code></li>
<li><code>make install                      //安装</code></li>
</ul>
<h2 id="5-3-源码安装"><a href="#5-3-源码安装" class="headerlink" title="5.3 源码安装"></a>5.3 源码安装</h2><ul>
<li><code>./configure                       //配置</code></li>
<li><code>make                              //编译</code></li>
<li><code>make install                      //安装</code></li>
</ul>
<h1 id="6-压缩和解压缩"><a href="#6-压缩和解压缩" class="headerlink" title="6.压缩和解压缩"></a>6.压缩和解压缩</h1><h2 id="6-1-tar"><a href="#6-1-tar" class="headerlink" title="6.1 tar"></a>6.1 tar</h2><p><code>tar -zcvf &#39;压缩到的文件&#39; ‘需要压缩的文件’ ‘需要压缩的文件’</code><br><code>tar -jcvf test.tar.zip a.c file</code><br><code>tar -zxvf test.tar.zip</code> // 解压文件  </p>
<ul>
<li><code>-zcvf</code>　<code>z:gzip</code> ,　<code>c: create压缩</code>　<code>f: file</code>　<code>v:显示过程</code> <code>x:解压缩</code> </li>
<li><p><code>-jcvf</code>　<code>j:bizp2</code> ,　<code>c: create压缩</code>　<code>f: file</code>　<code>v:显示过程</code> <code>x:解压缩</code></p>
</li>
<li><p><code>gzip ‘需要压缩的文件’</code> 只能压缩一个文件</p>
</li>
<li><code>gunzip a.c.gz</code>：解压文件</li>
</ul>
<h2 id="6-2-rar和zip"><a href="#6-2-rar和zip" class="headerlink" title="6.2 rar和zip"></a>6.2 rar和zip</h2><ul>
<li><code>rar a -r test.rar</code> ：rar 压缩</li>
<li><code>unrar x test.rar</code>：rar 解压缩</li>
<li><code>zip -r test.zip a.c file</code> ：zip 压缩</li>
<li><code>unzip test.zip</code>：zip解压缩</li>
</ul>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：03.linux系统-文档编辑、应用编译运行</title>
    <url>/74.linux%E7%B3%BB%E7%BB%9F-%E6%96%87%E6%A1%A3%E7%BC%96%E8%BE%91%E3%80%81%E5%BA%94%E7%94%A8%E7%BC%96%E8%AF%91%E8%BF%90%E8%A1%8C/</url>
    <content><![CDATA[<p>注意：<code>未做说明以下都是在命令模式下进行</code></p>
<p>编辑过程中遇到的问题：</p>
<p>误按了：<code>ctrl + z</code> ： 可以将一个正在前台执行的命令放到后台，并且暂停</p>
<p>再次打开就变成了以下这样</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">E325: 注意</span><br><span class="line">发现交换文件 &quot;.t.txt.swp&quot;</span><br><span class="line">            所有者: root    日期: Sun Aug 25 23:27:33 2019</span><br><span class="line">            文件名: ~root/test/t.txt</span><br><span class="line">            修改过: 否</span><br><span class="line">            用户名: root      主机名: master</span><br><span class="line">           进程 ID: 4803 (仍在运行)</span><br><span class="line">正在打开文件 &quot;t.txt&quot;</span><br><span class="line">              日期: Sun Aug 25 22:17:30 2019</span><br><span class="line"></span><br><span class="line">(1) Another program may be editing the same file.  If this is the case,</span><br><span class="line">    be careful not to end up with two different instances of the same</span><br><span class="line">    file when making changes.  Quit, or continue with caution.</span><br><span class="line">(2) An edit session for this file crashed.</span><br><span class="line">    如果是这样，请用 &quot;:recover&quot; 或 &quot;vim -r t.txt&quot;</span><br><span class="line">    恢复修改的内容 (请见 &quot;:help recovery&quot;)。</span><br><span class="line">    如果你已经进行了恢复，请删除交换文件 &quot;.t.txt.swp&quot;</span><br><span class="line">    以避免再看到此消息。</span><br><span class="line"></span><br><span class="line">交换文件 &quot;.t.txt.swp&quot; 已存在！</span><br><span class="line">以只读方式打开([O]), 直接编辑((E)), 恢复((R)), 退出((Q)), 中止((A)):</span><br></pre></td></tr></table></figure>
<p>解决办法：</p>
<p>使用 <code>fg</code> :将后台中的命令调至前台继续运行</p>
<h1 id="1-vim-工作模式"><a href="#1-vim-工作模式" class="headerlink" title="1. vim 工作模式"></a>1. vim 工作模式</h1><p><img src="/images/74.vim三种工作模式.png" alt=""></p>
<ul>
<li><code>gcc hello.c -o a</code>：编译成可执行文件 <code>a</code></li>
<li><code>./a</code>：运行可执行文件 a</li>
</ul>
<h1 id="2-vim-命令模式进入输入模式的快捷键"><a href="#2-vim-命令模式进入输入模式的快捷键" class="headerlink" title="2 vim 命令模式进入输入模式的快捷键"></a>2 vim 命令模式进入输入模式的快捷键</h1><ul>
<li><code>i</code>: 输入,光标前面</li>
<li><code>I</code>: 输入,光标前面</li>
<li><code>a</code>: 输入,光标会逻动一个位置</li>
<li><code>A</code>:输入,光标会逻动到最后</li>
<li><code>o</code>:输入,光标会逻动到下一行</li>
<li><code>O</code>:输入,光标会逻动到上一行</li>
<li><code>s</code>:输入,会删除光标所在字符</li>
<li><p><code>S</code>: 输入,会删除光标所在行</p>
</li>
<li><p><code>h</code>: 向左移动光标</p>
</li>
<li><code>l</code>: 向右移动光标</li>
<li><code>j</code>: 向下移动光标</li>
<li><code>k</code>: 向上移动光标</li>
</ul>
<h2 id="2-1-跳转和删除"><a href="#2-1-跳转和删除" class="headerlink" title="2.1.跳转和删除"></a>2.1.跳转和删除</h2><ul>
<li><code>5G</code>：跳转到第 5 行</li>
<li><code>5回车</code>或者<code>G</code>：跳转到末行行首</li>
<li><code>gg</code>: 跳转到首行行首</li>
<li><code>$</code>: 跳转到当行行尾</li>
<li><code>0</code>: 跳转到当行行首</li>
<li><p><code>gg=G</code>: 格式化代码</p>
</li>
<li><p><code>dw</code>：删除单词从光标到单词结束的地方</p>
</li>
<li><code>x</code>: 删除某个字母</li>
<li><code>d0</code>: 删除光标至行首</li>
<li><code>v+h/j/k/l+d</code> 或者 <code>v+上/下/左/右+d</code>：删除选中区域</li>
<li><code>dd</code>: 剪切整行，可以当删除</li>
<li><code>3dd</code>: 从当前行剪切 3 行，可以当删除</li>
</ul>
<h2 id="2-2-复制、粘贴"><a href="#2-2-复制、粘贴" class="headerlink" title="2.2. 复制、粘贴"></a>2.2. 复制、粘贴</h2><ul>
<li><code>yy</code>: 复制当前行</li>
<li><code>3yy</code>: 从当前行复制 3 行</li>
<li><code>p</code>: 粘贴到当前行的<code>下一行</code></li>
<li><code>P</code>: 粘贴到当前行的<code>上一行</code></li>
<li><code>dd</code>: 剪切整行</li>
<li><code>3dd</code>: 从当前行剪切 3 行</li>
</ul>
<h2 id="2-3-查找替换"><a href="#2-3-查找替换" class="headerlink" title="2.3 查找替换"></a>2.3 查找替换</h2><ul>
<li><code>/ + &#39;要查找的内容&#39; +回车</code>:  查找 , ‘*’或‘n’ 跳转到下一个， ‘N’ 是查看上一个</li>
<li><code>r + &#39;要替换的字符&#39;</code>：把当前光标字符替换成 ‘要替换的字符’</li>
<li><code>:s /printf/println</code> : 将当前光标所在行中的<code>第一个</code>printf替换成println</li>
<li><code>:%s /printf/println</code> : 将每一行中的<code>第一个</code>printf替换成println</li>
<li><code>:s /printf/println/g</code> ：将这一行的所有printf替换成println</li>
<li><code>:%s /printf/println/g</code> : 将所有地方的printf替换成println</li>
<li><code>:1,3s /printf/println/g</code>: 将1-3行的所有printf替换成println</li>
</ul>
<h2 id="2-4-其它命令"><a href="#2-4-其它命令" class="headerlink" title="2.4.其它命令"></a>2.4.其它命令</h2><ul>
<li><code>u</code>: 撤销</li>
<li><code>ctrl+r</code>: 反撤销</li>
<li><code>:sp</code>: 上下分屏</li>
<li><code>:vsp</code>: 左右分屏</li>
<li><code>ctrl + ww</code> : 切换分屏</li>
<li><code>wqall</code>: 保存退出所有</li>
<li><code>3+K或2+K</code>: 跳转到光标所指向的函数声明</li>
</ul>
<h1 id="3-gcc-编译的四个步骤"><a href="#3-gcc-编译的四个步骤" class="headerlink" title="3. gcc 编译的四个步骤"></a>3. gcc 编译的四个步骤</h1><p>.c 文件 -&gt; 可执行文件，经历了四个步骤</p>
<h2 id="3-1-预处理阶段"><a href="#3-1-预处理阶段" class="headerlink" title="3.1 预处理阶段"></a>3.1 预处理阶段</h2><p><code>将所有的宏定义的替换，类似于 java 的 final</code>,展开include的文件 </p>
<p><code>gcc -E -o hello.i hello.c</code>: 将 hello.c 文件预处理成 hello.i 文件</p>
<h2 id="3-2-预编译阶段"><a href="#3-2-预编译阶段" class="headerlink" title="3.2 预编译阶段"></a>3.2 预编译阶段</h2><p>在这个阶段，GCC 才会去检测你代码的规范性，语法是否有错误，gcc 会把代码翻译成汇编</p>
<p><code>gcc -S -o hello.s hello.i</code>:将 hello.i 文件预编译成 hello.i 文件</p>
<h2 id="3-3-汇编阶段"><a href="#3-3-汇编阶段" class="headerlink" title="3.3 汇编阶段"></a>3.3 汇编阶段</h2><p>把 .s 文件翻译成二进制.o文件（机器的指令）,文本打开的是乱码</p>
<p><code>gcc -c -o hello.o hello.s</code>:将 hello.s 汇编成 hello.o 文件</p>
<h3 id="3-4-链接阶段"><a href="#3-4-链接阶段" class="headerlink" title="3.4 链接阶段"></a>3.4 链接阶段</h3><p>计算逻辑地址，合并数据段，有些函数是在另外一个 so 库文件中的。</p>
<p><code>gcc -o hello hello.o</code>：这个时候就可以执行<code>hello</code>了</p>
<h1 id="4-动态库和静态库"><a href="#4-动态库和静态库" class="headerlink" title="4.动态库和静态库"></a>4.动态库和静态库</h1><p><code>静态库</code>：其实是内容的拷贝(代码)，运行阶段<code>没有 .a 文件</code>了也是可以执行的。<br><code>动态库</code>：不会拷贝内容，运行阶段起的作用（加载），生成 so 的时候要生成与位置无关的代码，没有 so 不能运行。</p>
<h2 id="4-1-静态库的生成"><a href="#4-1-静态库的生成" class="headerlink" title="4.1 静态库的生成:"></a>4.1 静态库的生成:</h2><h3 id="4-1-1-将-c-生成-o"><a href="#4-1-1-将-c-生成-o" class="headerlink" title="4.1.1 将 c 生成 .o"></a>4.1.1 将 c 生成 .o</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">gcc -c add.c -o add.o  <span class="comment">// 将 add.c 文件输出为 .o文件</span></span><br></pre></td></tr></table></figure>
<h3 id="4-1-2-使用-ar-工具制作静态库"><a href="#4-1-2-使用-ar-工具制作静态库" class="headerlink" title="4.1.2. 使用 ar 工具制作静态库"></a>4.1.2. 使用 ar 工具制作静态库</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ar rcs libmath.a add.o xxx.o <span class="comment">// 将 add.o 和 xxx.o 文件制作到 libmath.a 中</span></span><br></pre></td></tr></table></figure>
<h3 id="4-1-3-编译静态库到可执行文件"><a href="#4-1-3-编译静态库到可执行文件" class="headerlink" title="4.1.3. 编译静态库到可执行文件"></a>4.1.3. 编译静态库到可执行文件</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">gcc a.c libmath.a -o hello <span class="comment">// 将 a.c 和 libmath.a 输出到可执行文件 hello 中</span></span><br></pre></td></tr></table></figure>
<h2 id="4-2-动态库生成"><a href="#4-2-动态库生成" class="headerlink" title="4.2 动态库生成"></a>4.2 动态库生成</h2><h3 id="4-2-1-将-c-生成-o-（要生成与位置无关的代码）"><a href="#4-2-1-将-c-生成-o-（要生成与位置无关的代码）" class="headerlink" title="4.2.1 将 c 生成 .o （要生成与位置无关的代码）"></a>4.2.1 将 c 生成 .o （要生成与位置无关的代码）</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">gcc -c add.c -o add.o -fPIC  <span class="comment">// // 将 add.c 文件输出为 .o文件 要加 -fPIC</span></span><br></pre></td></tr></table></figure>
<h3 id="4-2-2-使用-gcc-shared-制作动态库"><a href="#4-2-2-使用-gcc-shared-制作动态库" class="headerlink" title="4.2.2 使用 gcc -shared 制作动态库"></a>4.2.2 使用 gcc -shared 制作动态库</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">gcc -shared -o libmath.so add.o xxx.o <span class="comment">// // 将 add.o 和 xxx.o 文件制作到 libmath.so 中</span></span><br></pre></td></tr></table></figure>
<h3 id="4-2-3-编译动态库到可执行文件，需要指定动态库的名称-l-指定库路径-L"><a href="#4-2-3-编译动态库到可执行文件，需要指定动态库的名称-l-指定库路径-L" class="headerlink" title="4.2.3 编译动态库到可执行文件，需要指定动态库的名称 -l , 指定库路径 -L"></a>4.2.3 编译动态库到可执行文件，需要指定动态库的名称 -l , 指定库路径 -L</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">gcc a.c -o a -lmath -L./   <span class="comment">// libmath.so 要写成math， ./ 代表当前文件夹</span></span><br></pre></td></tr></table></figure>
<h1 id="5-虚拟内存与物理内存"><a href="#5-虚拟内存与物理内存" class="headerlink" title="5.虚拟内存与物理内存"></a>5.虚拟内存与物理内存</h1><p><code>虚拟内存</code> 是为了缓解<code>物理内存</code>的压力而创建的，他们之间是通过<code>映射</code>来进行的联系</p>
<p>每个进程都有4G的虚拟地址空间，其中3G用户空间，1G内核空间（linux），每个进程共享内核空间，独立的用户空间：</p>
<p><img src="/images/74.虚拟内存布局图.png" alt=""></p>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：01.音视频技术-启动篇</title>
    <url>/72.%E9%9F%B3%E8%A7%86%E9%A2%91%E6%8A%80%E6%9C%AF%E5%AD%A6%E4%B9%A0-%E5%90%AF%E5%8A%A8%E7%AF%87/</url>
    <content><![CDATA[<h1 id="1-音视频学习"><a href="#1-音视频学习" class="headerlink" title="1. 音视频学习"></a>1. 音视频学习</h1><ul>
<li>视音频播放器（华为视频，腾讯视频，喜马拉雅，QQ音乐）</li>
<li>短视频（抖音，小火山，微视）</li>
<li>视音频的直播（有信，QQ音乐的电台，视频直播）+ OpengGl </li>
</ul>
<p><a href="https://blog.csdn.net/leixiaohua1020">雷霄骅</a></p>
<p>Android + NDK　　FFmpeg + SurfaceView + AudioTrack </p>
<h1 id="2-音视频编解码"><a href="#2-音视频编解码" class="headerlink" title="2. 音视频编解码"></a>2. 音视频编解码</h1><p>先来看下视频的播放流程：</p>
<p><img src="/images/72.视音频播放流程.png" alt=""></p>
<ul>
<li>RGB：8字节，24字节，32字节（RGBA）</li>
<li>YUV：Y黑白电视，亮度；U 色度，V 饱和度  ， YUV440，YUV420，YUV444 </li>
</ul>
<p>YUV：发现比例可以不一样，为什么可以，人脸对亮度比较敏感，对色度没这么敏感</p>
<p>YUV444 : Y：U：V 比例 1:1:1，YUV420 比例 2:1:0   </p>
<p>H264 编解码码的思想，认为每一帧每一帧都有相同的数据，就充分利用这个特性  </p>
<p>涉计到的算法：熵编码，帧内预测，帧间预测，环形滤波… </p>
<p>补充：H264码流怎么存？一个一个的 <code>NALU</code> 存起来的，大小是不固定的，一个  <code>NALU</code> 代表的是一帧画面，区分每个 <code>NALU</code> 的办法就是设置两个标志位 </p>
<p>PCM 采样：麦克风，  </p>
<ul>
<li>声道　　　　　: 2声道 （左右声道，立体声） 5point1 环绕声</li>
<li>采样率　　　　: 48.0 KHz      1秒钟采样多少个点 48*1000 ， 44100HZ 人耳</li>
<li>位深度　　　　: 16位              16byte 存的   最大值？   8 位   </li>
</ul>
<h1 id="3-FFmpeg"><a href="#3-FFmpeg" class="headerlink" title="3. FFmpeg"></a>3. FFmpeg</h1><ul>
<li>ijkplayer ： ffmpeg ， libyuv ， opensl es , 自己代码</li>
<li>微视：基于这个 ffmpeg </li>
<li><p>音视频通话：基于 webrtc</p>
<p>ffmpeg 是一个 c 库，要用你的编译 so , 自己去编译，你可以按需加载，减少so库的体积<br>shell 脚本，cmake 语法，linux ， 买最便宜，ubuntu ，mac 可以不用买，终端 -&gt; shell<br>inux 内核基础，android 底层基于 linux 内核 mmap() 等等  </p>
</li>
</ul>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCV：19.人脸美容效果2-边缘处理</title>
    <url>/67.OpenCV-%E4%BA%BA%E8%84%B8%E7%BE%8E%E5%AE%B9%E6%95%88%E6%9E%9C2-%E8%BE%B9%E7%BC%98%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h1 id="1-快速边缘保留算法"><a href="#1-快速边缘保留算法" class="headerlink" title="1.快速边缘保留算法"></a>1.快速边缘保留算法</h1><p>先看两张图：</p>
<p><img src="/images/66.快速边缘保留算法.png" alt=""></p>
<p><img src="/images/66.局部方差公式推导.png" alt=""></p>
<p>具体代码</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. 快速边缘保留</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;opencv2/opencv.hpp&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> cv;</span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 快速边缘保留</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">getBlockSum</span><span class="params">(Mat &amp;sum_mat,<span class="type">int</span> x0,<span class="type">int</span> y0,<span class="type">int</span> x1,<span class="type">int</span> y1,<span class="type">int</span> ch)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 获取四个点的值（因为肯定会超过 255 所以需要用Vec3i 来装）</span></span><br><span class="line">    <span class="type">int</span> lt = sum_mat.<span class="built_in">at</span>&lt;Vec3i&gt;(y0, x0)[ch];</span><br><span class="line">    <span class="type">int</span> lb = sum_mat.<span class="built_in">at</span>&lt;Vec3i&gt;(y1, x0)[ch];</span><br><span class="line">    <span class="type">int</span> rt = sum_mat.<span class="built_in">at</span>&lt;Vec3i&gt;(y0, x1)[ch];</span><br><span class="line">    <span class="type">int</span> rb = sum_mat.<span class="built_in">at</span>&lt;Vec3i&gt;(y1, x1)[ch];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 区块的合</span></span><br><span class="line">    <span class="type">int</span> sum = rb - lb - rt + lt;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">float</span> <span class="title">getBlockSqSum</span><span class="params">(Mat &amp;sqsum_mat, <span class="type">int</span> x0, <span class="type">int</span> y0, <span class="type">int</span> x1, <span class="type">int</span> y1, <span class="type">int</span> ch)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 获取四个点的值（因为肯定会超过 255 所以需要用Vec3f 来装）</span></span><br><span class="line">    <span class="type">float</span> lt = sqsum_mat.<span class="built_in">at</span>&lt;Vec3f&gt;(y0, x0)[ch];</span><br><span class="line">    <span class="type">float</span> lb = sqsum_mat.<span class="built_in">at</span>&lt;Vec3f&gt;(y1, x0)[ch];</span><br><span class="line">    <span class="type">float</span> rt = sqsum_mat.<span class="built_in">at</span>&lt;Vec3f&gt;(y0, x1)[ch];</span><br><span class="line">    <span class="type">float</span> rb = sqsum_mat.<span class="built_in">at</span>&lt;Vec3f&gt;(y1, x1)[ch];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 区块的合</span></span><br><span class="line">    <span class="type">float</span> sqsum = rb - lb - rt + lt;</span><br><span class="line">    <span class="keyword">return</span> sqsum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 积分图的模糊算法 size 模糊的直径</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">fastBlateralBlur</span><span class="params">(Mat &amp;src, Mat &amp;dst, <span class="type">int</span> size,<span class="type">int</span> sigma)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 卷积的 size 必须确保为基数 size % 2 == 1</span></span><br><span class="line">    <span class="comment">// 填充周围，方便计算</span></span><br><span class="line">    Mat mat;</span><br><span class="line">    <span class="type">int</span> radius = size / <span class="number">2</span>;</span><br><span class="line">    <span class="built_in">copyMakeBorder</span>(src,mat,radius,radius,radius,radius,BORDER_DEFAULT);</span><br><span class="line">    <span class="comment">// 求积分图</span></span><br><span class="line">    Mat sum_mat, sqsum_mat;</span><br><span class="line">    <span class="built_in">integral</span>(mat,sum_mat,sqsum_mat,CV_32S,CV_32F);</span><br><span class="line"></span><br><span class="line">    dst.<span class="built_in">create</span>(src.<span class="built_in">size</span>(),src.<span class="built_in">type</span>());</span><br><span class="line">    <span class="type">int</span> channels = src.<span class="built_in">channels</span>();</span><br><span class="line">    <span class="type">int</span> area = size * size;</span><br><span class="line">    <span class="comment">// 求四个点，左上，左下，右上，右下</span></span><br><span class="line">    <span class="type">int</span> x0 = <span class="number">0</span>, x1 = <span class="number">0</span>, y0 = <span class="number">0</span>, y1 = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; src.rows; row++)</span><br><span class="line">    &#123;</span><br><span class="line">        y0 = row;</span><br><span class="line">        y1 = y0 + size;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; src.cols; col++)</span><br><span class="line">        &#123;</span><br><span class="line">            x0 = col;</span><br><span class="line">            x1 = x0 + size;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; channels; i++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 区块的合</span></span><br><span class="line">                <span class="type">int</span> sum = <span class="built_in">getBlockSum</span>(sum_mat,x0,y0,x1,y1,i);</span><br><span class="line">                <span class="type">float</span> sqsum = <span class="built_in">getBlockSqSum</span>(sqsum_mat, x0, y0, x1, y1, i);</span><br><span class="line"></span><br><span class="line">                <span class="type">float</span> diff_sq = (sqsum - (sum*sum) / area) / area;</span><br><span class="line">                <span class="type">float</span> k = diff_sq / (diff_sq + sigma);</span><br><span class="line"></span><br><span class="line">                <span class="type">int</span> pixels = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[i];</span><br><span class="line">                pixels = (<span class="number">1</span> - k)*(sum / area) + k * pixels;</span><br><span class="line"></span><br><span class="line">                dst.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[i] = pixels;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">skinDetect</span><span class="params">(Mat &amp;src, Mat &amp;skinMask)</span></span>&#123;</span><br><span class="line">    skinMask.<span class="built_in">create</span>(src.<span class="built_in">size</span>(),CV_8UC1);</span><br><span class="line">    <span class="type">int</span> rows = src.rows;</span><br><span class="line">    <span class="type">int</span> cols = src.cols;</span><br><span class="line"></span><br><span class="line">    Mat ycrcb;</span><br><span class="line">    <span class="built_in">cvtColor</span>(src,ycrcb,COLOR_BGR2YCrCb);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; rows; row++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; cols; col++)</span><br><span class="line">        &#123;</span><br><span class="line">            Vec3b pixels = ycrcb.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col);</span><br><span class="line">            uchar y = pixels[<span class="number">0</span>];</span><br><span class="line">            uchar cr = pixels[<span class="number">1</span>];</span><br><span class="line">            uchar cb = pixels[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (y&gt;<span class="number">80</span> &amp;&amp; <span class="number">85</span> &lt; cb &lt; <span class="number">135</span> &amp;&amp; <span class="number">135</span> &lt; cr &lt; <span class="number">180</span>)&#123;</span><br><span class="line">                <span class="comment">// 255 代表人脸区域</span></span><br><span class="line">                skinMask.<span class="built_in">at</span>&lt;uchar&gt;(row,col) = <span class="number">255</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span>&#123;</span><br><span class="line">                skinMask.<span class="built_in">at</span>&lt;uchar&gt;(row, col) = <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">fuseSkin</span><span class="params">(Mat &amp;src,Mat &amp;blur_mat, Mat &amp;dst, Mat &amp;skinMask)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 融合</span></span><br><span class="line">    dst.<span class="built_in">create</span>(src.<span class="built_in">size</span>(),src.<span class="built_in">type</span>());</span><br><span class="line">    <span class="comment">// 处理一些区域，平滑点(所以有些地方的数值就不是0 或者 255了)</span></span><br><span class="line">    <span class="built_in">GaussianBlur</span>(skinMask, skinMask, <span class="built_in">Size</span>(<span class="number">3</span>, <span class="number">3</span>), <span class="number">0.0</span>);</span><br><span class="line"></span><br><span class="line">    Mat skinMask_f;</span><br><span class="line">    skinMask.<span class="built_in">convertTo</span>(skinMask_f, CV_32F);</span><br><span class="line">	</span><br><span class="line">    <span class="built_in">normalize</span>(skinMask_f, skinMask_f, <span class="number">0.0</span>, <span class="number">1.0</span>, NORM_MINMAX);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> rows = src.rows;</span><br><span class="line">    <span class="type">int</span> cols = src.cols;</span><br><span class="line">    <span class="type">int</span> ch = src.<span class="built_in">channels</span>();</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> row = <span class="number">0</span>; row &lt; rows; row++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> col = <span class="number">0</span>; col &lt; cols; col++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 有的区域还是有可能不够平滑</span></span><br><span class="line">            <span class="comment">/*uchar mask_pixels = skinMask.at&lt;uchar&gt;(row, col);</span></span><br><span class="line"><span class="comment">            // 皮肤位置</span></span><br><span class="line"><span class="comment">            if (mask_pixels == 255)&#123;</span></span><br><span class="line"><span class="comment">                dst.at&lt;Vec3b&gt;(row, col) = blur_mat.at&lt;Vec3b&gt;(row,col);</span></span><br><span class="line"><span class="comment">            &#125;</span></span><br><span class="line"><span class="comment">            else&#123;</span></span><br><span class="line"><span class="comment">                dst.at&lt;Vec3b&gt;(row, col) = src.at&lt;Vec3b&gt;(row, col);</span></span><br><span class="line"><span class="comment">            &#125;*/</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">// src ，通过指针去获取， 指针 -&gt; Vec3b -&gt; 获取</span></span><br><span class="line">            uchar b_src = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">0</span>];</span><br><span class="line">            uchar g_src = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">1</span>];</span><br><span class="line">            uchar r_src = src.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">            <span class="comment">// blur_mat</span></span><br><span class="line">            uchar b_blur = blur_mat.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">0</span>];</span><br><span class="line">            uchar g_blur = blur_mat.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">1</span>];</span><br><span class="line">            uchar r_blur = blur_mat.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">            <span class="type">float</span> k = skinMask_f.<span class="built_in">at</span>&lt;<span class="type">float</span>&gt;(row, col); <span class="comment">// k == 1的时候，人脸区域需要取b_blur，否则取b_src</span></span><br><span class="line"></span><br><span class="line">            dst.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">0</span>] = b_blur*k + (<span class="number">1</span> - k)*b_src;</span><br><span class="line">            dst.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">1</span>] = g_blur*k + (<span class="number">1</span> - k)*g_src;</span><br><span class="line">            dst.<span class="built_in">at</span>&lt;Vec3b&gt;(row, col)[<span class="number">2</span>] = r_blur*k + (<span class="number">1</span> - k)*r_src;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Mat src = <span class="built_in">imread</span>(<span class="string">&quot;E:/meirong.jpg&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!src.data)&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;read error&quot;</span> &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">imshow</span>(<span class="string">&quot;src&quot;</span>, src);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 高斯（模糊），计算高斯卷积核，卷积操作，考虑像素之间的差值（更好的保留图像的边缘）</span></span><br><span class="line"></span><br><span class="line">    Mat blur_mat;</span><br><span class="line">    <span class="type">int</span> size = <span class="number">12</span>; <span class="comment">// size == 10001 时 time = 1 秒</span></span><br><span class="line">    <span class="built_in">fastBlateralBlur</span>(src, blur_mat, size, size*size);</span><br><span class="line">    <span class="built_in">imshow</span>(<span class="string">&quot;fastBlateralBlur&quot;</span>, blur_mat);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 皮肤区域检测</span></span><br><span class="line">    Mat skinMask;</span><br><span class="line">    <span class="built_in">skinDetect</span>(src, skinMask);</span><br><span class="line">    <span class="built_in">imshow</span>(<span class="string">&quot;skinDetect&quot;</span>, skinMask);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//皮肤区域融合</span></span><br><span class="line">    Mat fuseDst;</span><br><span class="line">    <span class="built_in">fuseSkin</span>(src, blur_mat, fuseDst, skinMask);</span><br><span class="line">    <span class="built_in">imshow</span>(<span class="string">&quot;fuseSkin&quot;</span>, fuseDst);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 边缘的提升（可有可无）</span></span><br><span class="line">    Mat cannyMask;</span><br><span class="line">    <span class="built_in">Canny</span>(src,cannyMask,<span class="number">150</span>,<span class="number">300</span>,<span class="number">3</span>,<span class="literal">false</span>);</span><br><span class="line">    <span class="built_in">imshow</span>(<span class="string">&quot;cannyMask&quot;</span>, cannyMask);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 相当于 &amp; 运算</span></span><br><span class="line">    <span class="built_in">bitwise_and</span>(src,src,fuseDst,cannyMask);</span><br><span class="line">    <span class="built_in">imshow</span>(<span class="string">&quot;bitwise_and&quot;</span>, fuseDst);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 稍微提升一下对比度（亮度）</span></span><br><span class="line">    <span class="built_in">add</span>(fuseDst,<span class="built_in">Scalar</span>(<span class="number">10</span>,<span class="number">10</span>,<span class="number">10</span>),fuseDst);</span><br><span class="line">    <span class="built_in">imshow</span>(<span class="string">&quot;add&quot;</span>, fuseDst);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">waitKey</span>(<span class="number">0</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>OpenCV</category>
      </categories>
      <tags>
        <tag>OpenCV</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：04.linux系统-内核编程，文件IO与文件描述符</title>
    <url>/75.linux%E7%B3%BB%E7%BB%9F-%E5%86%85%E6%A0%B8%E7%BC%96%E7%A8%8B%EF%BC%8C%E6%96%87%E4%BB%B6IO%E4%B8%8E%E6%96%87%E4%BB%B6%E6%8F%8F%E8%BF%B0%E7%AC%A6/</url>
    <content><![CDATA[<h1 id="1-系统内核调用"><a href="#1-系统内核调用" class="headerlink" title="1. 系统内核调用"></a>1. 系统内核调用</h1><p>3-4G 是系统的空间（<code>内核空间</code>），这部分是由操作系统来管理，那么如果我们要操作这部分数据，只能借助系统提供的一些函数（系统函数），用户调用这些函数就称之为系统调用。</p>
<p><img src="/images/75.系统调用.png" alt=""></p>
<h1 id="2-普通函数与内核函数"><a href="#2-普通函数与内核函数" class="headerlink" title="2.普通函数与内核函数"></a>2.普通函数与内核函数</h1><p>系统函数：<code>open , close , write , read</code></p>
<p><code>man 2 ‘系统函数’</code>：查看系统函数<br><code>man 3 ‘普通函数’</code>：查看系统函数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// open</span><br><span class="line">int open(const char *pathname, int flags, mode_t mode);</span><br><span class="line">pathname : 文件的路径  </span><br><span class="line">flags : O_APPEND 追加，O_CREAT 没有的时候会创建，O_TRUNC 会截断（清空文件）  </span><br><span class="line">mode：就是创建时的文件权限 ， 只是当前用户可读可写，S_IRUSR|S_IWUSR  </span><br><span class="line">返回的是一个文件句柄 fd , 我们直接通过 fd 去做操作  </span><br><span class="line"></span><br><span class="line"></span><br><span class="line">int close(int fd); 关闭文件</span><br><span class="line"></span><br><span class="line">//  read</span><br><span class="line">ssize_t read(int fd, void *buf, size_t count);</span><br><span class="line">fd：文件句柄</span><br><span class="line">void *buf：读入的缓冲区</span><br><span class="line">count：每次读多少字节数</span><br><span class="line">ssize_t ：返回的是读到真实个数</span><br><span class="line"></span><br><span class="line">ssize_t write(int fd, const void *buf, size_t count);</span><br><span class="line">fd：文件句柄</span><br><span class="line">void *buf：写入的缓冲区</span><br><span class="line">count：每次写入的字节数</span><br><span class="line">ssize_t ：返回的是写入真实个数</span><br></pre></td></tr></table></figure>
<p>文件操作的错误怎么提示？会有一个宏定义 errno  , 通过 strerror(errno) 获取错误信息，需要打印错误快捷的方式 perror<br>void perror(const char *s); s 代表的是错误信息的描述 s: strerror(errno)</p>
<p>普通函数：<code>fopen，fclose ， fread/fputc ，fwrite/fgetc</code></p>
<h1 id="3-普通函数和系统函数拷贝文件的区别"><a href="#3-普通函数和系统函数拷贝文件的区别" class="headerlink" title="3.普通函数和系统函数拷贝文件的区别"></a>3.普通函数和系统函数拷贝文件的区别</h1><p>fputc 和 fgetc 内部是封装了 read , wirte , 如果拷贝文件，速度区别如下：</p>
<ul>
<li>系统函数每次只读取一个 Byte，速度很慢</li>
<li>普通函数每次读取 4096 个 byte，所以很快</li>
</ul>
<p>Java write 数据，需要 flush 的原理一样，数据还在缓冲区(数据不会自动发送出去)，需要flush一起给到内核空间，在内核空间中调用 write.</p>
<p><img src="/images/75.系统函数与普通函数.png" alt=""></p>
<h1 id="4-文件描述符（文件句柄）"><a href="#4-文件描述符（文件句柄）" class="headerlink" title="4.文件描述符（文件句柄）"></a>4.文件描述符（文件句柄）</h1><p><img src="/images/75.文件描述符.png" alt=""></p>
<h1 id="5-lseek-与-fseek"><a href="#5-lseek-与-fseek" class="headerlink" title="5.lseek 与 fseek"></a>5.lseek 与 fseek</h1><p>用来获取偏移量 SEEK_SET , SEEK_END, SEEK_CUR<br>lseek 他的偏移量，读和写是同一个</p>
<h1 id="6-stat-和-lstat"><a href="#6-stat-和-lstat" class="headerlink" title="6.stat 和 lstat"></a>6.stat 和 lstat</h1><p>Inode: 370887 ， 文件的权限，盘符的位置。<br>硬链接计数就充分的利用了 inode<br>硬链接：文件快捷键<br>软链接：类似于文件的快捷键，但是你操作任何一个都会被改变  </p>
<p>软连接和硬链接的特点：</p>
<p>软链接：</p>
<ul>
<li>软链接是存放另一个文件的路径的形式存在。</li>
<li>软链接可以 跨文件系统 ，硬链接不可以。</li>
<li>软链接可以对一个不存在的文件名进行链接，硬链接必须要有源文件。</li>
<li>软链接可以对目录进行链接。</li>
</ul>
<p>硬链接：</p>
<ul>
<li>硬链接，以文件副本的形式存在。但不占用实际空间。</li>
<li>不允许给目录创建硬链接。</li>
<li>硬链接只有在同一个文件系统中才能创建。</li>
<li>删除其中一个硬链接文件并不影响其他有相同 inode 号的文件。</li>
</ul>
<p>不论是硬链接或软链接都不会将原本的档案复制一份，只会占用非常少量的磁碟空间。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int stat(const char *pathname, struct stat *buf);</span><br><span class="line">pathname：文件路径</span><br><span class="line"></span><br><span class="line">struct stat *buf：stat 结构体指针</span><br><span class="line"></span><br><span class="line">作业：stat 获取文件的属性，获取文件的大小就可以</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">stat 和 lstat 有什么区别？stat 是会穿透的，lstat 不会穿透，穿透软连接</span><br><span class="line"></span><br><span class="line">ln -s f_read_write f_read_write.soft ： 创建软连接</span><br></pre></td></tr></table></figure>
<h1 id="7-目录操作"><a href="#7-目录操作" class="headerlink" title="7.目录操作"></a>7.目录操作</h1><p>递归操作</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;dirent.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stat.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isdir</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* fileName)</span></span>&#123;</span><br><span class="line">    <span class="comment">// int stat(const char *pathname,struct stat *buf);</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">stat</span>* buf; <span class="comment">// 作为传入参数</span></span><br><span class="line">    <span class="built_in">stat</span>(fileName,&amp;buf);</span><br><span class="line">    <span class="type">mode_t</span> st_mode = buf.st_mode;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">S_ISDIR</span>(st_mode);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span>&#123;</span><br><span class="line">    <span class="comment">// 打开目录</span></span><br><span class="line">    DIR *dir = <span class="built_in">opendir</span>(argv[<span class="number">1</span>]);</span><br><span class="line">    <span class="keyword">if</span>(dir == <span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;open dir fail&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// struct dirent *readdir(DIR *dirp);</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">dirent</span> *dirp;</span><br><span class="line">    <span class="keyword">while</span>((dirp = <span class="built_in">readdir</span>(dir)) != <span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="comment">// printf(&quot;%s\t&quot;,dirp-&gt;d_name);</span></span><br><span class="line">        <span class="comment">// 一个递归</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：06.linux系统-IPC进程间通信</title>
    <url>/77.linux%E7%B3%BB%E7%BB%9F-IPC%E8%BF%9B%E7%A8%8B%E9%97%B4%E9%80%9A%E4%BF%A1/</url>
    <content><![CDATA[<h1 id="1-进程间通信的方式"><a href="#1-进程间通信的方式" class="headerlink" title="1.进程间通信的方式"></a>1.进程间通信的方式</h1><p>文件，管道，信号、信号量，共享映射区（共享内存），消息队列，管道，套接字（socket）</p>
<p>着重讲下：</p>
<ul>
<li>管道（使用简单）</li>
<li>信号（开销比较小）</li>
<li><font color=red>共享映射区</font></li>
<li>套接字（低速稳定）</li>
</ul>
<p>一般都是通过内核共享空间进行通信</p>
<p><img src="/images/77.进程间通信.png" alt=""></p>
<h1 id="2-管道-pipe"><a href="#2-管道-pipe" class="headerlink" title="2.管道(pipe)"></a>2.管道(pipe)</h1><p><img src="/images/77.管道.png" alt=""><br><img src="/images/77.管道的通信机制.png" alt=""></p>
<ol>
<li>无名管道，<font color=red>半双工的通信方式(数据流向固定：只能 a 的数据写给 b ，或者 b 的数据写给 a )</font></li>
<li>管道大家可以简单的理解为一种特殊的文件形式，我们可以用文件的读写方法，不是一种简单的文件，只会占用内存。</li>
<li>管道只能是有血缘关系的进程间通信</li>
<li>数据不能自己写自己读</li>
<li>数据一旦读走，不能再次读</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> fd[<span class="number">2</span>];</span><br><span class="line">    <span class="type">pid_t</span>  pid;</span><br><span class="line">    <span class="type">char</span> buf[<span class="number">20</span>];</span><br><span class="line">    <span class="comment">// 创建管道</span></span><br><span class="line">    <span class="type">int</span> ret = <span class="built_in">pipe</span>(pipefd);</span><br><span class="line">    <span class="keyword">if</span>(ret != <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;create pipe error!\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">    pid = fork();</span><br><span class="line">    <span class="keyword">if</span>(pid &lt; <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;fork error!\n&quot;</span>);</span><br><span class="line">    &#125;<span class="keyword">else</span> <span class="keyword">if</span>(pid == <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="comment">// 子进程</span></span><br><span class="line">        <span class="built_in">close</span>(fd[<span class="number">1</span>]); <span class="comment">// 关闭写端</span></span><br><span class="line">        <span class="comment">// 等等在读</span></span><br><span class="line">        <span class="built_in">sleep</span>(<span class="number">1</span>);</span><br><span class="line">        <span class="built_in">read</span>(fd[<span class="number">0</span>],buf,<span class="number">11</span>);</span><br><span class="line">        <span class="comment">// 没有其它读写操作了可以关闭 fd[0]</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;pipe read : %s&quot;</span>,buf);</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="comment">// 父进程</span></span><br><span class="line">        <span class="built_in">close</span>(fd[<span class="number">0</span>]); <span class="comment">// 关闭读端</span></span><br><span class="line">        <span class="built_in">write</span>(fd[<span class="number">1</span>],<span class="string">&quot;hello pipe\n&quot;</span>,<span class="number">11</span>);</span><br><span class="line">        <span class="comment">// 没有其它读写操作了可以关闭 fd[1]</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>非法操作：</p>
<ol>
<li><p>读管道：<br> 1.1 管道中有数据，read 返回实际读到的数据</p>
<p> 1.2 管道中无数据：    </p>
<pre><code> 1.2.1 管道写端被关闭，read 返回 0

 1.2.2 管道写端如果没有被关闭，会阻塞等待
</code></pre></li>
<li><p>写管道：<br> 2.1 读端被关闭，异常终止（信号导致）</p>
<p> 2.2 读端未关闭：</p>
<pre><code> 2.2.1 管道数据已满，阻塞等待写入数据（其他人读走）

 2.2.2 管道未满，直接将数据写入
</code></pre></li>
</ol>
<h1 id="3-共享映射区"><a href="#3-共享映射区" class="headerlink" title="3.共享映射区"></a><font color=red>3.共享映射区</font></h1><p><img src="/images/77.mmap的实现原理.png" alt=""></p>
<p>binder 驱动，腾讯 MMKV，xlog （日志库）中都会有 <code>mmap</code>函数</p>
<p>以下是binder驱动的流程</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1、注册建立ServiceManager表。</span><br><span class="line">2、Service1、Service2通过Binder驱动申请注册到ServiceManager中。</span><br><span class="line">3、Client需要与Service2进行通信,跨进程无法直接通信，需要先通过ServiceManger中查询到Service2。借助Binder驱动</span><br><span class="line">返回一个Service2的代理对象。Client直接跟Service2代理对象进行操作，通过Binder驱动映射操作Service2真实对象，从而完成通信。</span><br><span class="line"></span><br><span class="line">- Binder是一种通信机制；</span><br><span class="line">- 对于Service来说Binder指的是Binder本地对象，对于Client来说Binder指的是Binder的代理对象；</span><br><span class="line">- 对于传输过程而言，Binder是可以跨进程传递的对象。</span><br><span class="line"></span><br><span class="line">为什么要使用Binder？</span><br><span class="line">性能方面</span><br><span class="line"></span><br><span class="line">在移动设备上（性能受限制的设备，比如要省电），广泛地使用跨进程通信对通信机制的性能有严格的要求，Binder相对出传统的Socket方式，</span><br><span class="line">更加高效。Binder数据拷贝只需要一次，而管道、消息队列、Socket都需要2次，共享内存方式一次内存拷贝都不需要，但实现方式又比较复杂。</span><br><span class="line"></span><br><span class="line">安全方面</span><br><span class="line"></span><br><span class="line">传统的进程通信方式对于通信双方的身份并没有做出严格的验证，比如Socket通信ip地址是客户端手动填入，很容易进行伪造，而Binder机制从协</span><br><span class="line">议本身就支持对通信双方做身份校检，因而大大提升了安全性。</span><br></pre></td></tr></table></figure>
<p>mmap 注意事项  </p>
<ol>
<li>指定映射区的大小，必须要小于文件大小</li>
<li>映射区大小必须要大于 0 ，否则会异常</li>
<li>创建映射区需要 read 权限，mmap 的读写权限应该 &lt;= 文件的权限</li>
<li>文件可以先关闭</li>
<li>偏移量必须是 4k 的整数倍</li>
<li>MAP_PRIVATE 只会对内存进行修改，不会反应到磁盘上</li>
</ol>
<p>mmap 万能调用方法：<br>open(O_RDWR);<br>mmap(NULL, 有效大小，PROT_READ|PROT_WRITE,MAP_SHARED,fd,0)  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">sys_err</span><span class="params">(<span class="type">const</span> <span class="type">char</span> * msg)</span></span>&#123;</span><br><span class="line">    <span class="built_in">perror</span>(msg);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">/*void *mmap(void *addr, size_t length, int prot, int flags,</span></span><br><span class="line"><span class="comment">              int fd, off_t offset);</span></span><br><span class="line"><span class="comment">   int munmap(void *addr, size_t length);*/</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     *  addr：地址，一般可以不传（NULL），传入传出参数</span></span><br><span class="line"><span class="comment">     *  length：内存大小</span></span><br><span class="line"><span class="comment">     *  prot：</span></span><br><span class="line"><span class="comment">     *       PROT_EXEC 内容可以被执行</span></span><br><span class="line"><span class="comment">     *       PROT_READ 内容可以被读取</span></span><br><span class="line"><span class="comment">     *       PROT_WRITE 内容可以被写</span></span><br><span class="line"><span class="comment">     *       PROT_NONE 内容不可访问</span></span><br><span class="line"><span class="comment">     *  flags:</span></span><br><span class="line"><span class="comment">     *       MAP_SHARED 共享</span></span><br><span class="line"><span class="comment">     *       MAP_PRIVATE 私有f</span></span><br><span class="line"><span class="comment">     *       MAP_ANONYMOUS 匿名映射（不基于文件）,fd 参数就传 -1</span></span><br><span class="line"><span class="comment">     *  fd: 文件句柄</span></span><br><span class="line"><span class="comment">     *  offset：偏移大小，必须是 4K 的整数倍，一个物理页映射是 4K。     </span></span><br><span class="line"><span class="comment">     *  </span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">// 创建打开一个文件</span></span><br><span class="line">    <span class="type">int</span> fd;</span><br><span class="line">    <span class="type">char</span> *p;</span><br><span class="line">    fd = <span class="built_in">open</span>(<span class="string">&quot;test_map&quot;</span>,O_ROWR|O_CREAT|O_TRUNC,<span class="number">0644</span>);</span><br><span class="line">    <span class="keyword">if</span>(fd == <span class="number">-1</span>)&#123;</span><br><span class="line">        <span class="built_in">sys_err</span>(<span class="string">&quot;open file error&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 文件变大（不增大的话，映射的是 0）</span></span><br><span class="line">    <span class="built_in">ftruncate</span>(fd,<span class="number">20</span>);</span><br><span class="line"></span><br><span class="line">    p = (<span class="type">char</span>*)<span class="built_in">mmap</span>(<span class="literal">NULL</span>,<span class="number">20</span>, PROT_READ|PROT_WRITE,MAP_SHARED,fd,<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 关闭了文件，那么后面通过操作 mmap 的空间，能不能写入文件? 能</span></span><br><span class="line">    <span class="built_in">close</span>(fd);<span class="comment">// 关闭的是系统函数到文件的操作，而 mmap 映射不受影响</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(p == MAP_FAILED)&#123;</span><br><span class="line">        <span class="built_in">sys_err</span>(<span class="string">&quot;mmap error&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">strcpy</span>(p,<span class="string">&quot;hello mmap\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;---&gt;%s&quot;</span>,p);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 释放 mmap</span></span><br><span class="line">    <span class="built_in">munmap</span>(p,<span class="number">20</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>还有示例请可以看<a href="https://github.com/EastUp/CPractice/tree/master/linux/77.linux%E7%B3%BB%E7%BB%9F-IPC%E8%BF%9B%E7%A8%8B%E9%97%B4%E9%80%9A%E4%BF%A1">这儿</a></p>
<h1 id="4-信号"><a href="#4-信号" class="headerlink" title="4.信号"></a>4.信号</h1><p>进程间通信的机制，内核自带的，信号不能携带大量的数据，一般就是一个数字，只有在特定场景下才调用。</p>
<p>怎样才会产生信号：</p>
<ol>
<li>按键产生: ctrl + c, ctrl+z</li>
<li>系统调用 kill ， raise， abort</li>
<li>软件条件产生， alarm</li>
<li>硬件异常产生，非法访问内存，除 0 ，内存对齐等等</li>
<li>命令产生</li>
</ol>
<p>信号的处理方式：</p>
<ol>
<li>忽略此信号</li>
<li>执行系统默认动作</li>
<li>捕捉用户希望动作</li>
</ol>
<p>信号处理的原理：  </p>
<ul>
<li>信号屏蔽字: 用于屏蔽x信号，内部实现就是一个集合，当 x 信号屏蔽后，再收到该信号，信号处理将推后</li>
<li>未决信号集: 信号抵达 x 信号位反转为 1 ， 当信号被处理后重新置为 0 </li>
</ul>
<p><code>man 7 signal</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">       Signal     Value     Action   Comment</span><br><span class="line">       ──────────────────────────────────────────────────────────────────────</span><br><span class="line">       SIGHUP        1       Term    Hangup detected on controlling terminal</span><br><span class="line">                                     or death of controlling process</span><br><span class="line">       SIGINT        2       Term    Interrupt from keyboard</span><br><span class="line">       SIGQUIT       3       Core    Quit from keyboard</span><br><span class="line">       SIGILL        4       Core    Illegal Instruction</span><br><span class="line">       SIGABRT       6       Core    Abort signal from abort(3)</span><br><span class="line">       SIGFPE        8       Core    Floating point exception</span><br><span class="line">       SIGKILL       9       Term    Kill signal</span><br><span class="line">       SIGSEGV      11       Core    Invalid memory reference</span><br><span class="line">       SIGPIPE      13       Term    Broken pipe: write to pipe with no</span><br><span class="line">                                     readers</span><br><span class="line">       SIGALRM      14       Term    Timer signal from alarm(2)</span><br><span class="line">       SIGTERM      15       Term    Termination signal</span><br><span class="line">       SIGUSR1   30,10,16    Term    User-defined signal 1</span><br><span class="line">       SIGUSR2   31,12,17    Term    User-defined signal 2</span><br><span class="line">       SIGCHLD   20,17,18    Ign     Child stopped or terminated</span><br><span class="line">       SIGCONT   19,18,25    Cont    Continue if stopped</span><br><span class="line">       SIGSTOP   17,19,23    Stop    Stop process</span><br><span class="line">       SIGTSTP   18,20,24    Stop    Stop typed at tty</span><br><span class="line">       SIGTTIN   21,21,26    Stop    tty input for background process</span><br><span class="line">       SIGTTOU   22,22,27    Stop    tty output for background process</span><br><span class="line"></span><br><span class="line">       Signal       Value     Action   Comment</span><br><span class="line">       ────────────────────────────────────────────────────────────────────</span><br><span class="line">       SIGBUS      10,7,10     Core    Bus error (bad memory access)</span><br><span class="line">       SIGPOLL                 Term    Pollable event (Sys V).</span><br><span class="line">                                       Synonym for SIGIO</span><br><span class="line">       SIGPROF     27,27,29    Term    Profiling timer expired</span><br><span class="line">       SIGSYS      12,31,12    Core    Bad argument to routine (SVr4)</span><br><span class="line">       SIGTRAP        5        Core    Trace/breakpoint trap</span><br><span class="line">       SIGURG      16,23,21    Ign     Urgent condition on socket (4.2BSD)</span><br><span class="line">       SIGVTALRM   26,26,28    Term    Virtual alarm clock (4.2BSD)</span><br><span class="line">       SIGXCPU     24,24,30    Core    CPU time limit exceeded (4.2BSD)</span><br><span class="line">       SIGXFSZ     25,25,31    Core    File size limit exceeded (4.2BSD)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Signal     Value     Action   Comment</span><br><span class="line">名称        编号       事件      默认执行动作</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Term ： 终止进程</span><br><span class="line">Ign ： 忽略</span><br><span class="line">Core：终止进程，生成 Core 文件（可以帮助开发者检测）</span><br><span class="line">Stop：停止进程</span><br><span class="line">Cont：继续运行</span><br></pre></td></tr></table></figure>
<p>Process.killProcess(Process.myPid());</p>
<p><code>kill pid （发送信号）</code> : 第一个参数，，&lt; 0 往 |pid| 发个信息，0 杀死进程组，不要传 -1 </p>
<ul>
<li>pid &gt; 0 往 pid 进程发个信号</li>
<li>pid &lt; 0 往 |pid| 发个信息</li>
<li>pid = 0 杀死进程组</li>
<li>不要传 -1 ,会杀死所有能杀死的进程</li>
</ul>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：08.Cmake语法-makefile、cmake、shell 自动编译和链接</title>
    <url>/79.Cmake%E8%AF%AD%E6%B3%95-makefile%E3%80%81cmake%E3%80%81shell%20%E8%87%AA%E5%8A%A8%E7%BC%96%E8%AF%91%E5%92%8C%E9%93%BE%E6%8E%A5/</url>
    <content><![CDATA[<h1 id="1-makefile、cmake-与-shell-之间的联系"><a href="#1-makefile、cmake-与-shell-之间的联系" class="headerlink" title="1.makefile、cmake 与 shell 之间的联系"></a>1.makefile、cmake 与 shell 之间的联系</h1><ul>
<li><p>makefile ： 帮助我们管理编译项目   </p>
</li>
<li><p>cmake：是一个跨平台的安装（编译）工具，可以用简单的语句来描述所有平台的安装(编译过程)。他能够输出各种各样的 Makefile 或者 project 文件，CMake 并不直接建构出最终的软件，而是产生标准的建构档（如 Makefile 或 projects）。</p>
</li>
<li><p>shell 脚本：一系列命令和语法</p>
</li>
</ul>
<h1 id="2-makefile-自动编译和链接"><a href="#2-makefile-自动编译和链接" class="headerlink" title="2.makefile 自动编译和链接"></a>2.makefile 自动编译和链接</h1><h2 id="2-1-gcc-如何编译相互依赖，却没直接引入的文件"><a href="#2-1-gcc-如何编译相互依赖，却没直接引入的文件" class="headerlink" title="2.1 gcc 如何编译相互依赖，却没直接引入的文件"></a>2.1 gcc 如何编译相互依赖，却没直接引入的文件</h2><p>Android.mk  Application.mk </p>
<p><code>gcc hello.cpp div.cpp sub.cpp add.cpp -o hello</code>// hello.cpp 中并没引入 div.cpp等文件，但是这样编译会自动找寻到依赖的文件</p>
<p>gcc 四步骤：生成 .o 文件比较耗时（<code>汇编阶段</code>）</p>
<p>上面的编译步骤其实就是省略了编译四步骤，假设现在我改了一个文件，需要重新编译，那么每次都会重新生成 .o 文件，因此需要把上面的步骤进行拆分</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">gcc -c sub.cpp -o sub.o</span><br><span class="line">gcc -c add.cpp -o add.o</span><br><span class="line">gcc -c div.cpp -o div.o</span><br><span class="line">gcc -c hello.cpp -o hello.o</span><br><span class="line">gcc add.o sub.o div.o hello.o -o hello</span><br></pre></td></tr></table></figure>
<p>这样如何只修改了 sub.cpp ，就只需要重新生成 sub.o 文件，然后重新生成最终的可执行文件</p>
<h2 id="2-2-如何编写Makefile文件"><a href="#2-2-如何编写Makefile文件" class="headerlink" title="2.2 如何编写Makefile文件"></a><font color=red>2.2 如何编写Makefile文件</font></h2><p>执行 Makefile 文件，只需要在Makefile 的目录下 执行<code>make</code>指令就行</p>
<h3 id="2-2-1-一个规则（观看makefile、cmake、shell目录下的m1）"><a href="#2-2-1-一个规则（观看makefile、cmake、shell目录下的m1）" class="headerlink" title="2.2.1. 一个规则（观看makefile、cmake、shell目录下的m1）"></a>2.2.1. 一个规则（观看makefile、cmake、shell目录下的m1）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">目标：依赖条件</span><br><span class="line">（一个tab缩进） 命令</span><br><span class="line"></span><br><span class="line">比如：</span><br><span class="line">hello.out:hello.o sub.o add.o div.o</span><br><span class="line">	gcc hello.o sub.o add.o div.o -o hello.out</span><br><span class="line">div.o:div.cpp</span><br><span class="line">	gcc -c div.cpp -o div.o</span><br></pre></td></tr></table></figure>
<p><code>默认情况下只会执行 MakeFile 中的第一个命令。</code></p>
<ol>
<li><p>但若想生成目标文件，则会检查命令中的依赖是否存在，如果不存在就会往下查找是否有生成该依赖条件的命令，如有就会执行生成依赖再生成目标文件。</p>
</li>
<li><p>会检测规则中的目标是否需要更新，必须先检测它的依赖性（通过更新时间判断），只需要重新生成依赖中被修改的依赖，然后再更新目标文件</p>
</li>
</ol>
<h3 id="2-2-2-两个函数（观看makefile、cmake、shell目录下的m2）"><a href="#2-2-2-两个函数（观看makefile、cmake、shell目录下的m2）" class="headerlink" title="2.2.2. 两个函数（观看makefile、cmake、shell目录下的m2）"></a>2.2.2. 两个函数（观看makefile、cmake、shell目录下的m2）</h3><ol>
<li><code>src = $(wildcard *.cpp)</code>　// 找到当前目录下所有后缀为.cpp的文件，然后赋值给src <code>$(src) 取变量src的值</code></li>
<li><code>obj = $(patsubst %cpp,%o,$(src))</code>　//把src变量中所有后缀为.cpp的文件替换成.o文件 <code>$(obj) 取变量obj的值</code></li>
</ol>
<p>make clean -n 注意加上 -n 预执行</p>
<p>make clean 执行 clean 命令</p>
<p>如果目录下有 clean 文件夹，则会有冲突，这时只需要生成伪目标就行</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">src = $(wildcard *.cpp)</span><br><span class="line">obj = $(patsubst %cpp,%o,$(src))</span><br><span class="line"></span><br><span class="line"># 默认情况下只会执行第一个</span><br><span class="line">hello.out:$(obj)</span><br><span class="line">	gcc $(obj) -o hello.out</span><br><span class="line"></span><br><span class="line">div.o:div.cpp</span><br><span class="line">	gcc -c div.cpp -o div.o</span><br><span class="line"></span><br><span class="line">add.o:add.cpp</span><br><span class="line">	gcc -c add.cpp -o add.o</span><br><span class="line"></span><br><span class="line">sub.o:sub.cpp</span><br><span class="line">	gcc -c sub.cpp -o sub.o</span><br><span class="line"></span><br><span class="line">hello.o:hello.cpp</span><br><span class="line">	gcc -c hello.cpp -o hello.o</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"># $(xxx) 取变量的值</span><br><span class="line">clean:</span><br><span class="line">	rm -rf $(obj) hello.out	</span><br><span class="line"></span><br><span class="line"># 生成伪目标</span><br><span class="line">.PHONY: clean</span><br></pre></td></tr></table></figure>
<h3 id="2-2-3-三个自动变量（观看makefile、cmake、shell目录下的m3）"><a href="#2-2-3-三个自动变量（观看makefile、cmake、shell目录下的m3）" class="headerlink" title="2.2.3. 三个自动变量（观看makefile、cmake、shell目录下的m3）"></a>2.2.3. 三个自动变量（观看makefile、cmake、shell目录下的m3）</h3><p>多行注释 <code>:&#39;</code></p>
<ul>
<li>$@：表示规则中的目标</li>
<li>$^：表示规则中所有的依赖条件,组成一个列表，以空格隔开，如果这个列表有重复项则消除重复项</li>
<li>$&lt;：表示规则中的第一个依赖条件，如果运行在模式套用中，相当于取出依赖条件套用在该模式中</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">src = $(wildcard *.cpp)</span><br><span class="line">obj = $(patsubst %cpp,%o,$(src))</span><br><span class="line"></span><br><span class="line"># 默认情况下只会执行第一个</span><br><span class="line">hello.out:$(obj)</span><br><span class="line">	gcc $^ -o $@ </span><br><span class="line"></span><br><span class="line">:&#x27;</span><br><span class="line">div.o:div.cpp</span><br><span class="line">	gcc -c $&lt; -o $@</span><br><span class="line"></span><br><span class="line">add.o:add.cpp</span><br><span class="line">	gcc -c $&lt; -o $@</span><br><span class="line"></span><br><span class="line">sub.o:sub.cpp</span><br><span class="line">	gcc -c $&lt; -o $@</span><br><span class="line"></span><br><span class="line">hello.o:hello.cpp</span><br><span class="line">	gcc -c $&lt; -o $@</span><br><span class="line">:&#x27;</span><br><span class="line"># 模式规则</span><br><span class="line"># gcc -c $(src) -o $(obj)</span><br><span class="line">%o:%cpp</span><br><span class="line">	gcc -c $&lt; -o $@ </span><br><span class="line"></span><br><span class="line"># $(xxx) 取变量的值</span><br><span class="line">clean:</span><br><span class="line">	rm -rf $(obj) hello.out	</span><br><span class="line"></span><br><span class="line"># 生成伪目标</span><br><span class="line">.PHONY: clean</span><br></pre></td></tr></table></figure>
<h1 id="3-cmake简介和安装"><a href="#3-cmake简介和安装" class="headerlink" title="3.cmake简介和安装"></a>3.<a href="https://www.jianshu.com/p/528eeb266f83">cmake简介和安装</a></h1><p>写 CMakeLists.txt 里面用 CMake 语法写，会帮我们生成 Makefile 用于编译管理项目</p>
<p><a href="https://blog.csdn.net/qq_27525611/article/details/104651517">安装：</a></p>
<ol>
<li>去<a href="https://cmake.org/download/">官网</a> 下载cmake的最新linux版本，这里下载的是<code>cmake-3.18.3.tar.gz</code></li>
<li><code>拷贝</code>到linux系统的 <code>/lib</code>目录下<code>新建的cmake目录</code>并<code>解压</code>,需要root权限</li>
<li>cd到解压后的cmake目录中运行<code>sudo ./bootstrap</code>命令进行安装</li>
<li>安装完成执行<code>sudo make</code></li>
<li>最后执行<code>sudo make install</code></li>
<li>可以在任一位置 使用<code>cmake --version</code> 查看安装的版本</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">如果不能在任一位置 使用`cmake --version` 查看安装的版本,则配置环境变量</span><br><span class="line"></span><br><span class="line">配置环境变量 vim /etc/profile</span><br><span class="line">文件最后面输入：</span><br><span class="line">export CMAKE_PATH=/lib/cmake/cmake-3.18.3/bin</span><br><span class="line">export PATH=$CMAKE_PATH:$PATH</span><br><span class="line">使环境变量生效 source /etc/profile</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>安装问题：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">有的会出现没安装 make的情况,使用以下命令</span><br><span class="line">sudo apt-get install make</span><br><span class="line"></span><br><span class="line">Could not find OpenSSL.  Install an OpenSSL development package or</span><br><span class="line">  configure CMake with -DCMAKE_USE_OPENSSL=OFF to build without OpenSSL.</span><br><span class="line"></span><br><span class="line">解决：sudo apt-get install libssl-dev</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Error when bootstrapping CMake:</span><br><span class="line">Cannot find a C++ compiler that supports both C++11 and the specified C++ flags.</span><br><span class="line"></span><br><span class="line">1.没有装gcc 和 g++。使用 sudo apt-get install g++ 安装</span><br><span class="line"></span><br><span class="line">2. gcc 或者 g++ 版本过低，需要安装高版本的。升级风险较大，可以采用新的编译环境安装新版本的 gcc和g++ 。</span><br></pre></td></tr></table></figure>
<p><a href="https://blog.csdn.net/jiangjiang_jian/article/details/80694799">gcc升级</a></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo add-apt-repository ppa:ubuntu-toolchain-r/test</span><br><span class="line">sudo apt-get update</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">sudo apt-get install gcc-7 g++-7</span><br></pre></td></tr></table></figure>
<p><a href="https://www.cnblogs.com/uestc-mm/p/7511063.html">升级后切换 gcc 和 g++ 版本</a></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">你在安装完之后可以采用下面的指令来查看当前系统中安装的所有的gcc和g++的版本：</span><br><span class="line">ls /usr/bin/gcc*</span><br><span class="line">ls /usr/bin/g++*</span><br><span class="line"></span><br><span class="line">将gcc-6加入gcc候选中，最后的数字是优先级，我自己是直接设为100（测试没有问题）</span><br><span class="line">sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-6 100</span><br><span class="line">sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-6 100</span><br><span class="line"></span><br><span class="line">完成上面的操作之后，我们就可以通过下面的指令来选择不同的gcc和g++的版本了</span><br><span class="line">sudo update-alternatives --config gcc</span><br><span class="line">sudo update-alternatives --config g++</span><br><span class="line"></span><br><span class="line">注意一个问题：</span><br><span class="line">  当切换使用了其他版本的gcc时，请务必保持g++的版本和gcc版本的一致性，否则用cmake配置出来的项目遇到c++代码还是会用之前版本的gcc。</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：07.linux系统-线程和多线程同步</title>
    <url>/78.linux%E7%B3%BB%E7%BB%9F-%E7%BA%BF%E7%A8%8B%E5%92%8C%E5%A4%9A%E7%BA%BF%E7%A8%8B%E5%90%8C%E6%AD%A5/</url>
    <content><![CDATA[<h1 id="1-线程的本质"><a href="#1-线程的本质" class="headerlink" title="1.线程的本质"></a>1.线程的本质</h1><p>线程与进程的区别：线程大家可以简单理解为一个轻量级的进程，线程共享了栈和堆（变量），没有复制 0-3 G 的进程空间，<br>但线程会有自己的工作空间，会有自己的 pcb 块。跟 Java 是类似的。</p>
<ol>
<li>linux线程执行和windows不同，pthread有两种状态<code>joinable</code>状态和<code>unjoinable</code>状态，<br>如果线程是<code>joinable</code>状态，当<code>线程函数自己返回退出时或pthread_exit</code>时都不会释放线程所占用堆栈和线程描述符（总计8K多）。只有当你调用了<code>pthread_join</code>之后这些资源才会被释放。<br>若是<code>unjoinable</code>状态的线程，这些资源在<code>线程函数退出时或pthread_exit时</code>自动会被释放。</li>
<li><code>unjoinable</code>属性可以在<code>pthread_create时指定</code>，或在线程创建后在线程中pthread_detach自己, 如：<code>pthread_detach(pthread_self())，将状态改为unjoinable状态</code>，确保资源的释放。或者将线程置为 joinable,然后适时调用pthread_join.</li>
<li>其实简单的说就是在线程函数头加上 pthread_detach(pthread_self())的话，线程状态改变，在函数尾部直接 pthread_exit线程就会自动退出。省去了给线程擦屁股的麻烦。</li>
</ol>
<h1 id="2-创建线程"><a href="#2-创建线程" class="headerlink" title="2.创建线程"></a>2.创建线程</h1><p><code>pthread_create(&amp;tid,NULL,thread_run,NULL);</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// void* 是可以任意类型，指针、变量都可以</span></span><br><span class="line"><span class="function"><span class="type">void</span>* <span class="title">thread_run</span><span class="params">(<span class="type">void</span>* arg)</span></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i am a thread\n&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 线程创建</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*int pthread_create(pthread_t *thread, const pthread_attr_t *attr,</span></span><br><span class="line"><span class="comment">                              void *(*start_routine) (void *), void *arg);*/</span></span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    <span class="built_in">pthread_create</span>(&amp;tid,<span class="literal">NULL</span>,thread_run,<span class="literal">NULL</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i am  main thread\n&quot;</span>);</span><br><span class="line">    <span class="built_in">sleep</span>(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 编译时调用 gcc thread1.cpp -o thread1 -lpthread  // -l 链接 pthread 的库 </span></span><br></pre></td></tr></table></figure>
<h1 id="3-退出线程"><a href="#3-退出线程" class="headerlink" title="3.退出线程"></a>3.退出线程</h1><p><code>pthread_exit(-1);</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">(<span class="type">int</span> no)</span></span>&#123;</span><br><span class="line">        <span class="comment">// 碰到 2 不打印</span></span><br><span class="line">        <span class="keyword">if</span>(no == <span class="number">2</span>)&#123;</span><br><span class="line">                <span class="comment">// 退出线程</span></span><br><span class="line">                <span class="built_in">pthread_exit</span>(<span class="number">-1</span>);</span><br><span class="line">                <span class="comment">// return;</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;i am a thread，no = %d\n&quot;</span>,no);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// void* 是可以任意类型，指针、变量都可以</span></span><br><span class="line"><span class="function"><span class="type">void</span>* <span class="title">thread_run</span><span class="params">(<span class="type">void</span>* arg)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> no = (<span class="type">int</span>)arg;</span><br><span class="line">    <span class="built_in">print</span>(no);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 线程创建</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*int pthread_create(pthread_t *thread, const pthread_attr_t *attr,</span></span><br><span class="line"><span class="comment">                              void *(*start_routine) (void *), void *arg);*/</span></span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>; i&lt;<span class="number">5</span>; i++)&#123;</span><br><span class="line">        <span class="built_in">pthread_create</span>(&amp;tid,<span class="literal">NULL</span>,thread_run,(<span class="type">void</span>*)i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i am  main thread\n&quot;</span>);</span><br><span class="line">    <span class="built_in">sleep</span>(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 编译时调用 gcc thread1.cpp -o thread1 -lpthread  // -l 链接 pthread 的库 </span></span><br></pre></td></tr></table></figure>
<h1 id="4-回收线程"><a href="#4-回收线程" class="headerlink" title="4.回收线程"></a>4.回收线程</h1><p><code>pthread_join(tid,&amp;retval); // retval = 0代表回收成功，-1 代表线程已经退出了</code></p>
<p>pthread_join()即是子线程合入主线程，<code>主线程阻塞等待子线程结束</code>，然后回收子线程资源。</p>
<p>pthread_join()函数，<code>以阻塞的方式</code>等待thread指定的线程结束。当函数返回时，<code>被等待线程的资源被收回</code>。如果线程已经结束，那么该函数会立即返回。并且thread指定的线程必须是joinable的。</p>
<p>一般情况下需要回收线程，retval 用来接收线程的返回值，tid 是线程的id，该方法会阻塞等待<br>什么情况下不需要回收呢？除非调用了分离线程</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">(<span class="type">int</span> no)</span></span>&#123;</span><br><span class="line">        <span class="comment">// 碰到 2 不打印</span></span><br><span class="line">        <span class="keyword">if</span>(no == <span class="number">2</span>)&#123;</span><br><span class="line">                <span class="comment">// 退出线程</span></span><br><span class="line">                <span class="built_in">pthread_exit</span>(<span class="number">-1</span>);</span><br><span class="line">                <span class="comment">// return;</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;i am a thread，no = %d\n&quot;</span>,no);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// void* 是可以任意类型，指针、变量都可以</span></span><br><span class="line"><span class="function"><span class="type">void</span>* <span class="title">thread_run</span><span class="params">(<span class="type">void</span>* arg)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> no = (<span class="type">int</span>)arg;</span><br><span class="line">    <span class="built_in">print</span>(no);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 线程创建</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*int pthread_create(pthread_t *thread, const pthread_attr_t *attr,</span></span><br><span class="line"><span class="comment">                              void *(*start_routine) (void *), void *arg);*/</span></span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    <span class="built_in">pthread_create</span>(&amp;tid,<span class="literal">NULL</span>,thread_run,<span class="number">2</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i am  main thread\n&quot;</span>);</span><br><span class="line">    <span class="comment">// linux 中需要回收线程,等待回收</span></span><br><span class="line">    <span class="comment">/*int pthread_join(pthread_t thread, void **retval);*/</span></span><br><span class="line">    <span class="type">int</span> *retval;</span><br><span class="line">    <span class="built_in">pthread_join</span>(tid,&amp;retval);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;retval = %d\n&quot;</span>,retval);</span><br><span class="line">    <span class="comment">// sleep(1);</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 编译时调用 gcc thread1.cpp -o thread1 -lpthread  // -l 链接 pthread 的库 </span></span><br></pre></td></tr></table></figure>
<h1 id="5-杀死（取消）线程"><a href="#5-杀死（取消）线程" class="headerlink" title="5.杀死（取消）线程"></a>5.杀死（取消）线程</h1><p><code>pthread_cancel(tid);</code> // 取消线程需要有函数进入内核，也就是说需要调用系统函数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">(<span class="type">int</span> no)</span></span>&#123;</span><br><span class="line">        <span class="comment">// 碰到 2 不打印</span></span><br><span class="line">        <span class="keyword">if</span>(no == <span class="number">2</span>)&#123;</span><br><span class="line">                <span class="comment">// 退出线程</span></span><br><span class="line">                <span class="built_in">pthread_exit</span>(<span class="number">-1</span>);</span><br><span class="line">                <span class="comment">// return;</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">            <span class="comment">// 循环里面是空的，则会卡死，线程不会取消，原因是取消线程需要有函数进入内核</span></span><br><span class="line">            <span class="comment">// printf(&quot;i am a thread，no = %d\n&quot;,no);</span></span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 实在没什么写的，又想线程能够取消，可以调下面这个方法</span></span><br><span class="line">            <span class="built_in">pthread_testcancel</span>();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// void* 是可以任意类型，指针、变量都可以</span></span><br><span class="line"><span class="function"><span class="type">void</span>* <span class="title">thread_run</span><span class="params">(<span class="type">void</span>* arg)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> no = (<span class="type">int</span>)arg;</span><br><span class="line">    <span class="built_in">print</span>(no);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 线程创建</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*int pthread_create(pthread_t *thread, const pthread_attr_t *attr,</span></span><br><span class="line"><span class="comment">                              void *(*start_routine) (void *), void *arg);*/</span></span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    <span class="built_in">pthread_create</span>(&amp;tid,<span class="literal">NULL</span>,thread_run,<span class="number">1</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i am  main thread\n&quot;</span>);</span><br><span class="line">    <span class="comment">// linux 中需要回收线程,等待回收</span></span><br><span class="line">    <span class="comment">/*int pthread_join(pthread_t thread, void **retval);*/</span></span><br><span class="line">    <span class="type">int</span> *retval;</span><br><span class="line">    <span class="built_in">sleep</span>(<span class="number">5</span>);</span><br><span class="line">    <span class="built_in">pthread_cancel</span>(tid);</span><br><span class="line">    <span class="built_in">pthread_join</span>(tid,&amp;retval);<span class="comment">// 返回值是 -1 ,表示被干掉或者退出的线程</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;retval = %d\n&quot;</span>,retval);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 编译时调用 gcc thread1.cpp -o thread1 -lpthread  // -l 链接 pthread 的库 </span></span><br></pre></td></tr></table></figure>
<h1 id="6-分离线程"><a href="#6-分离线程" class="headerlink" title="6.分离线程"></a>6.分离线程</h1><p><code>pthread_detach(tid);使线程ID为tid的线程处于分离状态，一旦线程处于分离状态，该线程终止时底 层资源立即被回收</code></p>
<p>分离线程之后是当线程执行完毕或者<code>pthread_exit</code>后，残留在线程中的资源会自动回收，也就是说线程需要回收，有两种方式一种是 join 一种是 detach 分离。</p>
<p><code>该函数不会阻塞父线程。pthread_detach(tid);函数用于只是应用程序在线程tid终止时回收其存储空间。如果tid尚未终止，pthread_detach()不会终止该线程。</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">(<span class="type">int</span> no)</span></span>&#123;</span><br><span class="line">        <span class="comment">// 碰到 2 不打印</span></span><br><span class="line">        <span class="keyword">if</span>(no == <span class="number">2</span>)&#123;</span><br><span class="line">                <span class="comment">// 退出线程</span></span><br><span class="line">                <span class="built_in">pthread_exit</span>(<span class="number">-1</span>);</span><br><span class="line">                <span class="comment">// return;</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;i am a thread，no = %d\n&quot;</span>,no);</span><br><span class="line">        <span class="built_in">sleep</span>(<span class="number">2</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// void* 是可以任意类型，指针、变量都可以</span></span><br><span class="line"><span class="function"><span class="type">void</span>* <span class="title">thread_run</span><span class="params">(<span class="type">void</span>* arg)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> no = (<span class="type">int</span>)arg;</span><br><span class="line">    <span class="built_in">print</span>(no);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 线程创建</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*int pthread_create(pthread_t *thread, const pthread_attr_t *attr,</span></span><br><span class="line"><span class="comment">                              void *(*start_routine) (void *), void *arg);*/</span></span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    <span class="built_in">pthread_create</span>(&amp;tid,<span class="literal">NULL</span>,thread_run,<span class="number">1</span>);</span><br><span class="line">    <span class="comment">// 对线程进行分离</span></span><br><span class="line">    <span class="built_in">pthread_detach</span>(tid);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;i am  main thread\n&quot;</span>);</span><br><span class="line">    <span class="comment">// linux 中需要回收线程,等待回收</span></span><br><span class="line">    <span class="comment">/*int pthread_join(pthread_t thread, void **retval);*/</span></span><br><span class="line">    <span class="type">int</span> *retval;</span><br><span class="line">    <span class="built_in">pthread_join</span>(tid,&amp;retval);<span class="comment">// 返回值是 -1 ,表示被干掉或者退出的线程</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;retval = %d\n&quot;</span>,retval);</span><br><span class="line">    <span class="built_in">sleep</span>(<span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 编译时调用 gcc thread1.cpp -o thread1 -lpthread  // -l 链接 pthread 的库 </span></span><br></pre></td></tr></table></figure>
<h1 id="7-线程同步"><a href="#7-线程同步" class="headerlink" title="7.线程同步"></a>7.线程同步</h1><ul>
<li><code>pthread_mutex_t mutex</code></li>
<li>加锁：<code>pthread_mutex_lock(&amp;mutex);</code></li>
<li>解锁：<code>pthread_mutex_unlock(&amp;mutex);</code></li>
<li>销毁锁：<code>pthread_mutex_destroy(&amp;mutex);</code></li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">long</span> number = <span class="number">0</span>;</span><br><span class="line"><span class="comment">// 加一个锁</span></span><br><span class="line"><span class="type">pthread_mutex_t</span> mutex;</span><br><span class="line"><span class="comment">// 加一个条件</span></span><br><span class="line"><span class="type">pthread_cond_t</span> product_cond;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span>* <span class="title">thread_run</span><span class="params">(<span class="type">void</span>* arg)</span></span>&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">long</span> i=<span class="number">0</span>; i&lt;<span class="number">200000000</span>;i++)&#123;</span><br><span class="line">                <span class="comment">// 加锁</span></span><br><span class="line">                <span class="built_in">pthread_mutex_lock</span>(&amp;mutex);</span><br><span class="line">                number++;</span><br><span class="line">                <span class="comment">// 解锁</span></span><br><span class="line">                <span class="built_in">pthread_mutex_unlock</span>(&amp;mutex);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 出现加锁的出错的情况，应该是没有计算完毕</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 生成者与消费者（java要能手写）</span></span><br><span class="line"><span class="function"><span class="type">void</span>* <span class="title">consumer</span><span class="params">(<span class="type">void</span> *arg)</span></span>&#123;</span><br><span class="line">        <span class="keyword">for</span>(;;)&#123;</span><br><span class="line">                <span class="built_in">pthread_mutex_lock</span>(&amp;mutex);</span><br><span class="line">                <span class="comment">// 1. 阻塞等待唤醒</span></span><br><span class="line">                <span class="comment">// 2. 释放锁</span></span><br><span class="line">                <span class="comment">// 3. 被唤醒，解除阻塞，需要重新竞争锁</span></span><br><span class="line">                <span class="comment">// 是一个 while 循环，等待有两种方式会被唤醒，一种是条件发信号，一种是系统（不正常）惊群效应</span></span><br><span class="line">                <span class="keyword">while</span>(number &lt;= <span class="number">0</span>)&#123;</span><br><span class="line">                        <span class="built_in">printf</span>(<span class="string">&quot;等待生产者生成产品&quot;</span>);</span><br><span class="line">                        <span class="built_in">pthread_cond_wait</span>(&amp;product_cond, &amp;mutex);</span><br><span class="line">				&#125;</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;消费者消费产品: %ld\n&quot;</span>,number);</span><br><span class="line">                number--;</span><br><span class="line">                <span class="comment">// 解锁</span></span><br><span class="line">                <span class="built_in">pthread_mutex_unlock</span>(&amp;mutex);</span><br><span class="line">                <span class="built_in">sleep</span>(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> (<span class="type">void</span>*)<span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span>* <span class="title">producer</span><span class="params">(<span class="type">void</span> *arg)</span></span>&#123;</span><br><span class="line">        <span class="keyword">for</span>(;;)&#123;</span><br><span class="line">                <span class="built_in">pthread_mutex_lock</span>(&amp;mutex);</span><br><span class="line">                number++;</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;生成者生产产品: %ld\n&quot;</span>,number);</span><br><span class="line">                <span class="comment">// 通知消费者消费</span></span><br><span class="line">                <span class="built_in">pthread_cond_signal</span>(&amp;product_cond);</span><br><span class="line">                <span class="comment">// 解锁</span></span><br><span class="line">                <span class="built_in">pthread_mutex_unlock</span>(&amp;mutex);</span><br><span class="line">                <span class="built_in">sleep</span>(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> (<span class="type">void</span>*)<span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">// 初始化锁</span></span><br><span class="line">        <span class="built_in">pthread_mutex_init</span>(&amp;mutex,<span class="literal">NULL</span>);</span><br><span class="line">        <span class="built_in">pthread_cond_init</span>(&amp;product_cond,<span class="literal">NULL</span>);</span><br><span class="line">        <span class="type">pthread_t</span> tid;</span><br><span class="line">        <span class="type">int</span> *retval;</span><br><span class="line">        <span class="comment">//for(int i=0; i&lt;4;i++)&#123;</span></span><br><span class="line">                <span class="comment">// 传的是同一个 id ，tid 没有赋值的，tid 是当做一个传出参数 0</span></span><br><span class="line">                <span class="built_in">pthread_create</span>(&amp;tid, <span class="literal">NULL</span>, producer, <span class="literal">NULL</span>);</span><br><span class="line">                <span class="built_in">pthread_detach</span>(tid);</span><br><span class="line">                <span class="built_in">pthread_create</span>(&amp;tid, <span class="literal">NULL</span>, consumer, <span class="literal">NULL</span>);</span><br><span class="line">				<span class="built_in">pthread_detach</span>(tid);</span><br><span class="line">                <span class="built_in">pthread_create</span>(&amp;tid, <span class="literal">NULL</span>, consumer, <span class="literal">NULL</span>);</span><br><span class="line">                <span class="built_in">pthread_detach</span>(tid);</span><br><span class="line">        <span class="comment">//&#125;</span></span><br><span class="line">        <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// sleep(5);</span></span><br><span class="line">        <span class="comment">// 销毁锁</span></span><br><span class="line">        <span class="built_in">pthread_mutex_destroy</span>(&amp;mutex);</span><br><span class="line">        <span class="built_in">pthread_cond_destroy</span>(&amp;product_cond);</span><br><span class="line">        <span class="comment">// 销毁条件</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;number = %ld\n&quot;</span>, number);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：05.linux系统-进程创建和进程管理</title>
    <url>/76.linux%E7%B3%BB%E7%BB%9F-%E8%BF%9B%E7%A8%8B%E5%88%9B%E5%BB%BA%E5%92%8C%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h1 id="1-pcb-process-control-block-进程控制块"><a href="#1-pcb-process-control-block-进程控制块" class="headerlink" title="1.pcb(process control block)进程控制块"></a>1.pcb(process control block)进程控制块</h1><p>结构体的路径：<code>~/usr/src/linux-headers-3.5.0-23/include/linux/sched.h</code></p>
<p>结构体：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct task_struct &#123;</span><br><span class="line">        // ......</span><br><span class="line"></span><br><span class="line">        进程 id</span><br><span class="line"></span><br><span class="line">        文件描述符</span><br><span class="line"></span><br><span class="line">        进程的状态：初始态，就绪态，运行态，挂起态，终止态</span><br><span class="line"></span><br><span class="line">        进程工作目录</span><br><span class="line"></span><br><span class="line">        信号相关信息资源</span><br><span class="line"></span><br><span class="line">        用户id组id</span><br><span class="line"></span><br><span class="line">        进程工作目录</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-内存映射（重点）"><a href="#2-内存映射（重点）" class="headerlink" title="2.内存映射（重点）"></a><strong><font color = red>2.内存映射（重点）</font></strong></h1><p>由于应用程序不能直接操作设备硬件地址，所以操作系统提供了这样的一种机制——内存映射，把设备地址映射到进程虚拟地址，<code>mmap</code>就是实现内存映射的接口。</p>
<p><code>mmap</code>的好处是，mmap<code>把设备内存映射到虚拟内存，则用户操作虚拟内存相当于直接操作设备了，省去了用户空间到内核空间的复制过程，相对IO操作来说，增加了数据的吞吐量</code>。</p>
<p>用户空间映射时，会映射到不同的区域，内核空间映射时，会映射到同一区域的不同地方（共享！！）</p>
<p>每个进程都有4G的虚拟地址空间，其中3G用户空间，1G内核空间（linux），每个进程共享内核空间，独立的用户空间</p>
<p><img src="/images/76.内存映射.png" alt=""></p>
<h1 id="3-fork-创建子进程（重点）"><a href="#3-fork-创建子进程（重点）" class="headerlink" title="3.fork 创建子进程（重点）"></a><strong><font color = red>3.fork 创建子进程（重点）</font></strong></h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">int</span> argc.<span class="type">const</span>  <span class="type">char</span>* argv[])</span></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork before\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork before\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork before\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork before\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork before\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">pid_t</span> pid = fork();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span>(pid == <span class="number">-1</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;fork error!\n&quot;</span>);</span><br><span class="line">    &#125;<span class="keyword">else</span> <span class="keyword">if</span>(pid == <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;i am child process!,pid = %d,ppid = %d\n&quot;</span>,<span class="built_in">getpid</span>(),<span class="built_in">getppid</span>());</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;I am parent,cpid = %d,pid = %d,ppid = %d&quot;</span> ,pid,<span class="built_in">getpid</span>(),<span class="built_in">getppid</span>());</span><br><span class="line">    &#125; </span><br><span class="line">    </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork after\n&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/images/76.fork函数.png" alt=""></p>
<h1 id="4-父子进程共享（重点）"><a href="#4-父子进程共享（重点）" class="headerlink" title="4.父子进程共享（重点）"></a><strong><font color = red>4.父子进程共享（重点）</font></strong></h1><h2 id="4-1-父子进程相同："><a href="#4-1-父子进程相同：" class="headerlink" title="4.1 父子进程相同："></a>4.1 父子进程相同：</h2><p>刚 fork 后，data 段，text 段，堆，栈，环境变量，全局变量，进程工作目录，信号处理方式。（0-3G 部分是共享的）</p>
<h2 id="4-2-父子进程不同的："><a href="#4-2-父子进程不同的：" class="headerlink" title="4.2 父子进程不同的："></a>4.2 父子进程不同的：</h2><p>进程 id ，返回值，各自的父进程，进程创建时间，闹钟，未决信号集 </p>
<h2 id="4-3-父子进程共享："><a href="#4-3-父子进程共享：" class="headerlink" title="4.3 父子进程共享："></a>4.3 父子进程共享：</h2><p>map 映射区，读时共享，写时复制。</p>
<h1 id="5-进程回收"><a href="#5-进程回收" class="headerlink" title="5. 进程回收"></a>5. 进程回收</h1><p>回收的就是残留在内核中的 3-4G 的数据（pcb 进程控制块）</p>
<ul>
<li>孤儿进程：父进程先于子进程结束，则子进程会成为孤儿进程，子进程的父进程成为 init 进程，有 init 进程（进程孤儿院）来回收进程。</li>
<li>僵尸进程：子进程结束，父进程尚未回收，该子进程会变成僵尸进程</li>
</ul>
<p>wait, waitpid<br>wait功能：</p>
<ol>
<li>阻塞等待子线程退出</li>
<li>回收子进程残留的资源</li>
<li>获取子进程退出的状态（怎么死掉的）</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> state;</span><br><span class="line"><span class="built_in">wait</span>(&amp;state);</span><br></pre></td></tr></table></figure>
<h1 id="6-监听-APP-应用被卸载"><a href="#6-监听-APP-应用被卸载" class="headerlink" title="6.监听 APP 应用被卸载"></a>6.监听 APP 应用被卸载</h1><ol>
<li>fork 一个子进程</li>
<li>监听文件是否被删除了，data/data/xxx包名</li>
<li>execl 函数族去执行某些命令（打开浏览器收集用户反馈）  </li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 可以执行自己写好的程序，path 是路径，arg 是可变参数</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">execl</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="type">char</span> *arg, ...</span></span></span><br><span class="line"><span class="params"><span class="function">                       <span class="comment">/* (char  *) NULL */</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以执行系统的命令程序，file 是命令的名称，arg 是可变参数</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">execlp</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *file, <span class="type">const</span> <span class="type">char</span> *arg, ...</span></span></span><br><span class="line"><span class="params"><span class="function">                       <span class="comment">/* (char  *) NULL */</span>)</span></span>;</span><br></pre></td></tr></table></figure>
 <font color=red>execl 不会走 fork 之后的 text 代码段，而是会去执行 exec 函数族的命令或者程序</font>

 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">int</span> argc.<span class="type">const</span>  <span class="type">char</span>* argv[])</span></span>&#123; <span class="comment">// 获取 参数 -l ，[1]才是，[0]是程序本身</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork before\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork before\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork before\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork before\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork before\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">pid_t</span> pid = fork();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span>(pid == <span class="number">-1</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;fork error!\n&quot;</span>);</span><br><span class="line">    &#125;<span class="keyword">else</span> <span class="keyword">if</span>(pid == <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;i am child process!,pid = %d,ppid = %d\n&quot;</span>,<span class="built_in">getpid</span>(),<span class="built_in">getppid</span>());</span><br><span class="line">        <span class="built_in">execl</span>(<span class="string">&quot;ls&quot;</span>,<span class="string">&quot;ls&quot;</span>,<span class="string">&quot;-l&quot;</span>,<span class="literal">NULL</span>); <span class="comment">// NULL 代表可变参数的结尾</span></span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;I am parent,cpid = %d,pid = %d,ppid = %d&quot;</span> ,pid,<span class="built_in">getpid</span>(),<span class="built_in">getppid</span>());</span><br><span class="line">    &#125; </span><br><span class="line">    </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork after\n&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：09.Cmake语法-详解CmakeLists.txt</title>
    <url>/80.Cmake%E8%AF%AD%E6%B3%95-%E8%AF%A6%E8%A7%A3CmakeLists.txt/</url>
    <content><![CDATA[<h1 id="1-初试-CMake-的-HelloWorld"><a href="#1-初试-CMake-的-HelloWorld" class="headerlink" title="1.初试 CMake 的 HelloWorld"></a>1.初试 CMake 的 HelloWorld</h1><p>注意：<code>PROJECT、这些不区分大小写</code>，但是<code>$&#123;HELLO_BINARY_DIR&#125;这些，必须大写</code></p>
<ul>
<li><code>PROJECT(HELLO)</code>：给工程取一个名字</li>
<li><code>SET(SRC_LIST hello.cpp)</code>：定义一个变量</li>
<li><code>MESSAGE(STATUS &quot;this is BINARY dir &quot;$&#123;HELLO_BINARY_DIR&#125;)</code>：打印</li>
<li><code>ADD_EXECUTABLE(hello.out $&#123;SRC_LIST&#125;)</code>：生成可执行文件</li>
</ul>
<ol>
<li><p>新建<code>CMakeLists.txt</code>文件，编写以下内容</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 给工程取一个名字</span><br><span class="line">PROJECT(HELLO)</span><br><span class="line"></span><br><span class="line"># 定义一个变量</span><br><span class="line">SET(SRC_LIST hello.cpp)</span><br><span class="line"></span><br><span class="line"># 打印</span><br><span class="line">MESSAGE(STATUS &quot;this is BINARY dir &quot;$&#123;HELLO_BINARY_DIR&#125;) // dir/home/keanu/jamin/NDK80/t1/build</span><br><span class="line">MESSAGE(STATUS &quot;this is SOURCE dir &quot;$&#123;HELLO_SOURCE_DIR&#125;) // dir/home/keanu/jamin/NDK80/t1</span><br><span class="line">MESSAGE(STATUS &quot;this is PROJECT_SOURCE dir &quot;$&#123;PROJECT_SOURCE_DIR&#125;) // dir/home/keanu/jamin/NDK80/t1</span><br><span class="line"></span><br><span class="line"># 生成可执行文件</span><br><span class="line">ADD_EXECUTABLE(hello.out $&#123;SRC_LIST&#125;)</span><br></pre></td></tr></table></figure>
</li>
<li><p>在同目录下创建 build 文件夹,进入执行 <code>cmake ..</code> ..代表CMakeLists.txt在上一级目录</p>
</li>
<li>最后在build目录中，执行 <code>make</code></li>
</ol>
<h1 id="2-构建生成-so-动态库"><a href="#2-构建生成-so-动态库" class="headerlink" title="2.构建生成 .so 动态库"></a>2.构建生成 .so 动态库</h1><p>目的：把src里的源文件，生成<code>libmath.so</code>动态库，动态库的存储路径在lib目录下</p>
<ul>
<li><code>CMAKE_MINIMUM_REQUIRED(VERSION 3.14)</code>:指定 CMake 的最低编译版本</li>
<li><code>AUX_SOURCE_DIRECTORY($&#123;PROJECT_SOURCE_DIR&#125;/src SRC_LIST)</code>：搜集 src 目录下的所有源文件，并赋值为SRC_LIST</li>
<li><code>FILE(GLOB SRC_LIST &quot;$&#123;PROJECT_SOURCE_DIR&#125;/src/*&quot;)</code>：搜集 src 目录下的所有源文件，并赋值为SRC_LIST</li>
<li><code>INCLUDE_DIRECTORIES($&#123;PROJECT_SOURCE_DIR&#125;/include)</code>：指定头文件在哪个目录</li>
<li><code>SET (LIBRARY_OUTPUT_PATH $&#123;PROJECT_SOURCE_DIR&#125;/lib)</code>：指定 so 的生成目录 lib</li>
<li><code>ADD_LIBRARY (math SHARED $&#123;SRC_LIST_CPP&#125; $&#123;SRC_LIST_C&#125;)</code>：指定生成动态库 .so， <code>math</code> 生成的so名字为 <code>libmath.so</code> 如果默认生成的是静态库.a，加入SHARED才会生成动态库.so</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 指定 CMake 的最低编译版本</span><br><span class="line">CMAKE_MINIMUM_REQUIRED(VERSION 3.14)</span><br><span class="line"></span><br><span class="line"># 给工程取名字</span><br><span class="line">PROJECT(MATH)</span><br><span class="line"></span><br><span class="line"># 搜集 src 目录下的所有源文件，有AUX_SOURCE_DIRECTORY和FILE 两种方式</span><br><span class="line"># AUX_SOURCE_DIRECTORY($&#123;PROJECT_SOURCE_DIR&#125;/src SRC_LIST)</span><br><span class="line"># FILE(GLOB SRC_LIST &quot;$&#123;PROJECT_SOURCE_DIR&#125;/src/*.cpp&quot; &quot;$&#123;PROJECT_SOURCE_DIR&#125;/src/*.c&quot;)</span><br><span class="line">FILE(GLOB SRC_LIST_CPP &quot;$&#123;PROJECT_SOURCE_DIR&#125;/src/*.cpp&quot;)</span><br><span class="line">FILE(GLOB SRC_LIST_C &quot;$&#123;PROJECT_SOURCE_DIR&#125;/src/*.c&quot;)</span><br><span class="line"></span><br><span class="line"># MESSAGE (STATUS &quot;src_list : &quot;$&#123;SRC_LIST&#125;)</span><br><span class="line"></span><br><span class="line"># 指定头文件在哪个目录</span><br><span class="line">INCLUDE_DIRECTORIES($&#123;PROJECT_SOURCE_DIR&#125;/include)</span><br><span class="line"></span><br><span class="line"># 指定 so 的生成目录 lib</span><br><span class="line">SET (LIBRARY_OUTPUT_PATH $&#123;PROJECT_SOURCE_DIR&#125;/lib)</span><br><span class="line"></span><br><span class="line"># 指定生成动态库 .so math -&gt; libmath.so 如果默认生成的是静态库.a，加入SHARED才会生成动态库.so</span><br><span class="line">ADD_LIBRARY (math SHARED $&#123;SRC_LIST_CPP&#125; $&#123;SRC_LIST_C&#125;)</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h1 id="3-链接外部动态库与头文件"><a href="#3-链接外部动态库与头文件" class="headerlink" title="3.链接外部动态库与头文件"></a>3.链接外部动态库与头文件</h1><ul>
<li><code>LINK_DIRECTORIES ($&#123;PROJECT_SOURCE_DIR&#125;/lib)</code>：<font color=red>官方不推荐使用，有时候会找不到引用。经过尝试，so库放在jniLibs下的时候可以编译成功</font>链接 lib 目录下的 libmath.so,指定 so 在哪个目录下，</li>
<li><code>TARGET_LINK_LIBRARIES (hello math)</code>：为 hello 添加编译链接库</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 指定 CMake 的最低编译版本</span><br><span class="line">CMAKE_MINIMUM_REQUIRED(VERSION 3.14)</span><br><span class="line"></span><br><span class="line"># ...</span><br><span class="line">PROJECT (HELLO)</span><br><span class="line"></span><br><span class="line"># 指定头文件在哪个目录</span><br><span class="line">INCLUDE_DIRECTORIES ($&#123;PROJECT_SOURCE_DIR&#125;/include)</span><br><span class="line"></span><br><span class="line"># 编译是需要链接 lib 目录下的 libmath.so</span><br><span class="line"># 指定 so 在哪个目录下，so库放在jniLibs下的时候才可以编译成功</span><br><span class="line">LINK_DIRECTORIES ($&#123;PROJECT_SOURCE_DIR&#125;/lib)</span><br><span class="line"></span><br><span class="line"># 生成可执行文件</span><br><span class="line">ADD_EXECUTABLE (hello hello.cpp)</span><br><span class="line"></span><br><span class="line"># 为 hello 添加编译链接库</span><br><span class="line">TARGET_LINK_LIBRARIES (hello math)</span><br></pre></td></tr></table></figure>
<h1 id="4-根据不同的源文件夹生成不同的-so-文件"><a href="#4-根据不同的源文件夹生成不同的-so-文件" class="headerlink" title="4. 根据不同的源文件夹生成不同的 .so 文件"></a>4. 根据不同的源文件夹生成不同的 .so 文件</h1><p>1.则需要在顶层目录下创建CMakeLists.txt，内容如下</p>
<pre><code><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">add_subdirectory(src)</span><br></pre></td></tr></table></figure>
</code></pre><ol>
<li>然后在 src 中再创建一个CMakeLists.txt，内容就是上边生成.so文件的内容  </li>
<li>在顶层目录下创建 build 文件夹，执行<code>cmake ..</code> ,<code>make</code> 的指令  </li>
<li>如果不指定生成的so位置，最后生成的.so文件默认在<code>build/‘源文件的目录’</code>中   </li>
</ol>
<h1 id="5-修改以前-ffmpeg-开发项目中的-CMakeLists-txt"><a href="#5-修改以前-ffmpeg-开发项目中的-CMakeLists-txt" class="headerlink" title="5.修改以前 ffmpeg 开发项目中的 CMakeLists.txt"></a>5.修改以前 ffmpeg 开发项目中的 CMakeLists.txt</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 指定最低版本名称</span><br><span class="line">CMAKE_MINIMUM_REQUIRED(VERSION 3.4.1)</span><br><span class="line"></span><br><span class="line"># 指定引入头文件目录</span><br><span class="line">INCLUDE_DIRECTORIES($&#123;CMAKE_SOURCE_DIR&#125;/src/main/jniLibs/include)</span><br><span class="line">INCLUDE_DIRECTORIES($&#123;CMAKE_SOURCE_DIR&#125;/src/main/jniLibs/other)</span><br><span class="line"></span><br><span class="line"># 指定共享库的库目录，so库放在jniLibs下的时候才可以编译成功</span><br><span class="line">LINK_DIRECTORIES($&#123;CMAKE_SOURCE_DIR&#125;/src/main/jniLibs/armeabi)</span><br><span class="line"></span><br><span class="line"># 指定源文件的目录</span><br><span class="line">AUX_SOURCE_DIRECTORY($&#123;CMAKE_SOURCE_DIR&#125;/src/main/cpp SRC_LIST)</span><br><span class="line"></span><br><span class="line"># 生成 native-lib</span><br><span class="line">ADD_LIBRARY(native-lib SHARED $&#123;SRC_LIST&#125;)</span><br><span class="line"></span><br><span class="line"># 为 native-lib 添加 ffmpeg 编译链接库</span><br><span class="line">TARGET_LINK_LIBRARIES(native-lib  </span><br><span class="line">avcodec-56 </span><br><span class="line">avdevice-56 </span><br><span class="line">avfilter-5 </span><br><span class="line">avformat-56 </span><br><span class="line">avutil-54 </span><br><span class="line">postproc-53 </span><br><span class="line">swresample-1 </span><br><span class="line">swscale-3 </span><br><span class="line">-landroid </span><br><span class="line">-llog)</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：11.Shell脚本-动手编译FFmpeg</title>
    <url>/82.Shell%E8%84%9A%E6%9C%AC-%E5%8A%A8%E6%89%8B%E7%BC%96%E8%AF%91FFmpeg/</url>
    <content><![CDATA[<p><a href="https://www.jianshu.com/p/fe54d70a260e">避坑指南</a></p>
<h1 id="1-Cmake交叉编译Android动态库"><a href="#1-Cmake交叉编译Android动态库" class="headerlink" title="1.Cmake交叉编译Android动态库"></a>1.Cmake交叉编译Android动态库</h1><p>交叉编译：Linux上编译Android的so</p>
<p>ndk 16的以上版本自带交叉编译工具链</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cmake -DANDROID_NDK=$&#123;NDK_PATH&#125; \ # ndk的安装目录（空格 \回车，相当于所有命令弄到同一行）</span><br><span class="line">        -DCMAKE_TOOLCHAIN_FILE=$&#123;NDK_PATH&#125;/build/cmake/android.toolchain.cmake \ # cmake 交叉编译工具链</span><br><span class="line">        -DANDROID_ABI=&quot;armeabi-v7a&quot; \ # 指定CPU</span><br><span class="line">        -DANDROID_NATIVE_API_LEVEL=19 \    # 指定最低版本</span><br><span class="line">        ..</span><br></pre></td></tr></table></figure>
<h1 id="2-配置NDK环境"><a href="#2-配置NDK环境" class="headerlink" title="2.配置NDK环境"></a>2.配置NDK环境</h1><p>在NDKr17以后，NDK 不再支持32位和64位 MIPS 和ARM v5(armeabi)</p>
<p>所以现在NDK中只支持armeabi-v7a，armeabi-v8a，x86,x86_64四类</p>
<p>注意如果是虚拟机的linux系统 <code>需要把要拷贝的文件放到桌面，然后通过ctrl+c复制，最后在虚拟机上 ctrl+v复制</code></p>
<ol>
<li>下载<a href="https://www.jianshu.com/p/528eeb266f83">NDK</a></li>
<li>将下载好的linux系统的ndk,<code>拷贝</code>到linux系统的 <code>/lib</code>目录下<code>新建的ndk目录</code>并<code>解压</code>,需要root权限</li>
<li>配置环境变量   <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">配置环境变量:</span><br><span class="line"></span><br><span class="line">配置环境变量 vim /etc/profile</span><br><span class="line">文件最后面输入：</span><br><span class="line">export NDK_PATH=/lib/ndk/android-ndk-r21b</span><br><span class="line">export PATH=$NDK_PATH:$PATH</span><br><span class="line">使环境变量生效 source /etc/profile</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="2-1-NDK中交叉编译工具的变化"><a href="#2-1-NDK中交叉编译工具的变化" class="headerlink" title="2.1 NDK中交叉编译工具的变化"></a>2.1 NDK中交叉编译工具的变化</h2><p>在ndkr17c 以后默认使用的变成了<code>clang</code>，<code>而不是gcc</code></p>
<h3 id="2-1-1-交叉编译工具位置的变化"><a href="#2-1-1-交叉编译工具位置的变化" class="headerlink" title="2.1.1. 交叉编译工具位置的变化:"></a>2.1.1. 交叉编译工具位置的变化:</h3><p>在 NDK r19以前的 ndk 内置了一个可以自动生成交叉编译工具(toolchain) 的.py文件，放在</p>
<p>ndk路径下面的build/tool/make_standalone_toolchain.py</p>
<p>要生成toolchain，使用下面的命令</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$NDK_HOME/build/tools/make_standalone_toolchain.py --arch arm --api 21 --install-dir /Users/fczhao/Desktop</span><br><span class="line"></span><br><span class="line">后面的几个都是必要的</span><br><span class="line"></span><br><span class="line">--arch 指定了生成的toolchain要在哪个CPU框架中使用</span><br><span class="line"></span><br><span class="line">--api 指定了生成的toolchain要在哪个Android API 中使用</span><br><span class="line"></span><br><span class="line">--install-dir 生成的toolchain的路径</span><br></pre></td></tr></table></figure>
<p>但是NDK r19以后的NDK已经内置了这些文件，如果运行上面的命令，会出现这样的日志</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">WARNING:__main__:make_standalone_toolchain.py is no longer necessary. The</span><br><span class="line"></span><br><span class="line">#$NDK/toolchains/llvm/prebuilt/darwin-x86_64/bin 这个路径已经有了我们要生成的文件</span><br><span class="line">$NDK/toolchains/llvm/prebuilt/darwin-x86_64/bin directory contains target-specific scripts that perform</span><br><span class="line">the same task. For example, instead of:</span><br><span class="line"></span><br><span class="line">    $ python $NDK/build/tools/make_standalone_toolchain.py \</span><br><span class="line">        --arch arm --api 21 --install-dir toolchain</span><br><span class="line">    $ toolchain/bin/clang++ src.cpp</span><br><span class="line"></span><br><span class="line">Instead use:</span><br><span class="line"></span><br><span class="line">    $ $NDK/toolchains/llvm/prebuilt/darwin-x86_64/bin/armv7a-linux-androideabi21-clang++ src.cpp</span><br><span class="line"></span><br><span class="line">Installation directory already exists. Use --force.</span><br></pre></td></tr></table></figure>
<h1 id="3-手写FFmpeg编译脚本"><a href="#3-手写FFmpeg编译脚本" class="headerlink" title="3.手写FFmpeg编译脚本"></a>3.手写FFmpeg编译脚本</h1><p>关于版本问题：学习尽量使用最新的版本（api过时，api改进），开发尽量使用大众的版本（方便查询解决问题）版本<code>3.3.9</code></p>
<font color=red>第三方库编译的通用思想：</font>

<ol>
<li>首先先看下<code>README.md</code>文件</li>
<li>编译项目（动静态库）需要 Makefile 管理，如果<code>已经有写好的Makefile可以尝试着用make命令去编译</code>，如果没有需要自己写<code>Makefile</code>或者采用 <code>cmake</code> 构建</li>
<li>如果Makefile报错需要解决，一般情况下都是Makefile的一些配置文件没生成，所以一般需要先运行<code>configure</code>文件</li>
<li>生成配置文件之后，再次运行make,但是编译后的文件（elf,so,a）只能在当前类型系统上运行。如果需要跑到android或ios那么需要<code>交叉编译</code>。<code>因此我们一般都需要往configure文件里面传一些交叉编译参数</code></li>
</ol>
<ul>
<li><code>./configure --help &gt; help.txt</code>:将configure 的帮助说明写到<code>help.txt</code>中去</li>
</ul>
<p><code>NDKr16以上的版本NDKr19</code>以下需要我们自己去生成android版本的交叉编译链 ,NDKr19以上不需要</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1. 创建目录 /lib/ndk/android-ndk-r21b/android-toolchains/android-19/arch-arm</span><br><span class="line">2. 进入 /lib/ndk/android-ndk-r21b/build/tools 目录下</span><br><span class="line">3. 执行命令：</span><br><span class="line">    sudo ./make_standalone_toolchain.py --arch arm --api 19 --install-dir /lib/ndk/android-ndk-r16b/android-toolchains/android-19/arch-arm</span><br><span class="line"></span><br><span class="line">如果出现 Installation directory already exists. Use --force.</span><br><span class="line"></span><br><span class="line">在上条命令最后面加上 --force就行</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p><code>ndk-r16b和ffmpeg3.3.9</code>的脚本文件请查看<a href="../makefile、cmake、shell/82.Shell脚本-动手编译FFmpeg/ffmpeg_build_r16b.sh">ffmpeg_build_r16b.sh</a>,还需要注意修改configure里生成文件名的方式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">按 esc 键输入 /build setting 查找</span><br><span class="line"></span><br><span class="line"># SLIBNAME_WITH_MAJOR=&#x27;$(SLIBNAME).$(LIBMAJOR)&#x27;</span><br><span class="line"># LIB_INSTALL_EXTRA_CMD=&#x27;$$(RANLIB) &quot;$(LIBDIR)/$(LIBNAME)&quot;&#x27;</span><br><span class="line"># SLIB_INSTALL_NAME=&#x27;$(SLIBNAME_WITH_VERSION)&#x27;</span><br><span class="line"># SLIB_INSTALL_LINKS=&#x27;$(SLIBNAME_WITH_MAJOR) $(SLIBNAME)&#x27;</span><br><span class="line"># 替换成如下</span><br><span class="line">SLIBNAME_WITH_MAJOR=&#x27;$(SLIBPREF)$(FULLNAME)-$(LIBMAJOR)$(SLIBSUF)&#x27;</span><br><span class="line">LIB_INSTALL_EXTRA_CMD=&#x27;$$(RANLIB)&quot;$(LIBDIR)/$(LIBNAME)&quot;&#x27;</span><br><span class="line">SLIB_INSTALL_NAME=&#x27;$(SLIBNAME_WITH_MAJOR)&#x27;</span><br><span class="line">SLIB_INSTALL_LINKS=&#x27;$(SLIBNAME)&#x27;</span><br></pre></td></tr></table></figure>
<p><code>ndk-r16b和ffmpeg3.3.9</code>的脚本文件请查看<a href="../makefile、cmake、shell/82.Shell脚本-动手编译FFmpeg/ffmpeg_build_r21b.sh">ffmpeg_build_r21b.sh</a></p>
<h1 id="4-编译遇到的问题"><a href="#4-编译遇到的问题" class="headerlink" title="4.编译遇到的问题"></a>4.编译遇到的问题</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">clang is unable to create an executable file. C compiler test failed.</span><br><span class="line">C compiler test failed.</span><br><span class="line"></span><br><span class="line">If you think configure made a mistake, make sure you are using the latest</span><br><span class="line">version from Git. If the latest version fails, report the problem to the</span><br><span class="line">ffmpeg-user@ffmpeg.org mailing list or IRC #ffmpeg on irc.freenode.net.</span><br><span class="line">Include the log file &quot;config.log&quot; produced by configure as this will help</span><br><span class="line">solving the problem.</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">当遇到这个问题的时候</span><br><span class="line"></span><br><span class="line">大部分是NDK支持平台错了</span><br><span class="line"></span><br><span class="line">sudo uname --m</span><br><span class="line"></span><br><span class="line">如果显示i686,你安装的是32位操作系统</span><br><span class="line">如果显示 x86_64，你安装的是64位操作系统</span><br><span class="line"></span><br><span class="line">下载的时候按下面</span><br><span class="line"></span><br><span class="line">android-ndk64-r10b-linux-x86.tar</span><br><span class="line">支持的是32位操作系统</span><br><span class="line">android-ndk64-r10b-linux-x86_64.tar.bz2 </span><br><span class="line">支持的是64位操作系统</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h1 id="5-如何适配so框架"><a href="#5-如何适配so框架" class="headerlink" title="5.如何适配so框架"></a>5.如何适配so框架</h1><p>目前支持有不同的 7 中 cpu 的架构，<code>ARMv5</code>,<code>ARMv7(2010年)</code>,<code>ARMv8</code>,<code>x86(2011年)</code>,<code>mips(2012年)</code>,<code>mip64</code>,<code>x86_64</code>。  </p>
<p>每一种架构都关联着一种<code>ABI</code>。  现在一般只需要arm架构就行</p>
<p>armv5,armv7（32位）,armv8（64位）：高版本会兼容低版本的，因此只需要放一个低版本的就可以了，armeabi或者armeabi-v7a(现在一般都是v7a，AS上只支持v7以上的，AS4.1提示后面上架到google play 的应用都需要支持64位的)  </p>
<p>armeabi：v5因为比较旧，因此早期缺少浮点计算的硬件支持，在需要大量计算是有一定的性能影响。</p>
<h1 id="6-编译参数传递"><a href="#6-编译参数传递" class="headerlink" title="6.编译参数传递"></a>6.编译参数传递</h1><h1 id="7-运行调试音频解码"><a href="#7-运行调试音频解码" class="headerlink" title="7.运行调试音频解码"></a>7.运行调试音频解码</h1>]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：10.Shell脚本-详解Shell脚本基础语法</title>
    <url>/81.Shell%E8%84%9A%E6%9C%AC-%E8%AF%A6%E8%A7%A3Shell%E8%84%9A%E6%9C%AC%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95/</url>
    <content><![CDATA[<h1 id="1-shell-脚本创建与执行-01-sh"><a href="#1-shell-脚本创建与执行-01-sh" class="headerlink" title="1.shell 脚本创建与执行(01.sh)"></a>1.shell 脚本创建与执行(01.sh)</h1><p>一般都是<code>**.sh</code>文件</p>
<ul>
<li><code>#!</code>：需要用谁来解析执行该 shell 脚本</li>
<li><code>echo</code>：打印输出</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 需要用谁来解析执行该 shell 脚本</span><br><span class="line">#!/bin/bash</span><br><span class="line"></span><br><span class="line"># echo 打印输出</span><br><span class="line">echo &quot;Hello Shell!&quot;</span><br></pre></td></tr></table></figure>
<h1 id="2-自定义变量与环境变量-02-sh"><a href="#2-自定义变量与环境变量-02-sh" class="headerlink" title="2.自定义变量与环境变量(02.sh)"></a>2.自定义变量与环境变量(02.sh)</h1><p>变量设计规则：</p>
<ol>
<li>命名跟Java规范一样</li>
<li>shell 中<code>默认都是字符串类</code>，如果需要用到其他类型，需要额外处理</li>
<li>变量用等号连接，千万注意<code>不能有空格</code></li>
<li>变量的值如果有空格，需要用<code>单引号</code>或者<code>双引号</code>包括</li>
</ol>
<ul>
<li><code>$</code>：取变量的值</li>
<li><code>echo &quot;CMAKE_PATH = $&#123;CMAKE_PATH&#125;&quot;</code>：输出环境变量CMAKE_PATH的值</li>
<li>soPath=&#96;pwd&#96;：记得带上&#96;&#96;代表执行命令并返回</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#指定一个命令的执行结果返回给变量</span><br><span class="line">#soPath=$(pwd)</span><br><span class="line">soPath=`pwd`</span><br><span class="line">echo &quot;soPath = $&#123;soPath&#125;&quot;</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">str1=&quot;str1&quot;  // str1</span><br><span class="line">str2=str2    // str2</span><br><span class="line">str3=3       // 3</span><br><span class="line">str4=&quot;Hello Shell&quot; // Hello Shell</span><br><span class="line"></span><br><span class="line"># 重新赋值</span><br><span class="line">str4=str4 // str4</span><br><span class="line"></span><br><span class="line"># $取变量的值</span><br><span class="line">echo &quot;str1 = $str1&quot; // str1 = str1</span><br><span class="line">echo &quot;str2 = $str2&quot; // str2 = str2</span><br><span class="line">echo &quot;str3 = $str3&quot; // str3 = 3</span><br><span class="line">echo &quot;str4 = $str4&quot; // str4 = str4</span><br><span class="line"></span><br><span class="line">#输出环境变量</span><br><span class="line">echo &quot;CMAKE_PATH = $&#123;CMAKE_PATH&#125;&quot;</span><br><span class="line"></span><br><span class="line">#指定一个命令的执行结果返回给变量</span><br><span class="line">#soPath=$(pwd)  </span><br><span class="line">soPath=`pwd`  // soPath = 当前路径</span><br><span class="line">echo &quot;soPath = $&#123;soPath&#125;&quot; </span><br></pre></td></tr></table></figure>
<h1 id="3-位置参数变量与预定义变量"><a href="#3-位置参数变量与预定义变量" class="headerlink" title="3.位置参数变量与预定义变量"></a>3.位置参数变量与预定义变量</h1><h2 id="3-1-获取位置参数（03-sh）"><a href="#3-1-获取位置参数（03-sh）" class="headerlink" title="3.1.获取位置参数（03.sh）"></a>3.1.获取位置参数（03.sh）</h2><ul>
<li><code>$n</code> 来获取参数,$0 代表程序本身，<code>$1-$9</code>代表第一个参数到第九个参数，十以上的参数要用大括号 <code>$&#123;10&#125;</code></li>
<li><code>$*</code> 代表的是命令中的所有参数，<code>加上引号才会把参数看成一个整体</code> “$0,$1,$2,$3,$4…$n”，否则区别对待</li>
<li><code>$@</code> 代表命令中的所有参数，但是会把参数区分对待 “$0”,”$1”,”$2”…”$n”</li>
<li><code>$#</code> 代表参数中的个数</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">echo &quot;$0=$0&quot;</span><br><span class="line">echo &quot;\$1=$1&quot;</span><br><span class="line">echo &quot;\$2=&quot;$2</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">echo &quot;\$*=&quot;$*</span><br><span class="line">echo &quot;\$@=&quot;$@</span><br><span class="line">echo &quot;\$#=&quot;$#</span><br><span class="line"></span><br><span class="line">#每次逻动（删除）第一个参数</span><br><span class="line">shift</span><br><span class="line"></span><br><span class="line">echo &quot;\$1=$1&quot;</span><br><span class="line">echo &quot;\$@=&quot;$@</span><br><span class="line"></span><br><span class="line">echo &quot;========================================&quot;</span><br><span class="line">for i in $*</span><br><span class="line">do</span><br><span class="line">echo $i</span><br><span class="line">done</span><br><span class="line"></span><br><span class="line">echo &quot;========================================&quot;</span><br><span class="line">for i in $@</span><br><span class="line">do</span><br><span class="line">echo $i</span><br><span class="line">done</span><br><span class="line"></span><br><span class="line">echo &quot;========================================&quot;</span><br><span class="line">for i in &quot;$@&quot;</span><br><span class="line">do</span><br><span class="line">echo $i</span><br><span class="line">done</span><br><span class="line"></span><br><span class="line">echo &quot;========================================&quot;</span><br><span class="line">for i in &quot;$*&quot;  // 加上引号才会看成一个整体</span><br><span class="line">do</span><br><span class="line">echo $i</span><br><span class="line">done</span><br></pre></td></tr></table></figure>
<h2 id="3-2-预定义变量"><a href="#3-2-预定义变量" class="headerlink" title="3.2.预定义变量"></a>3.2.预定义变量</h2><p>定义：是 bash 中已经定义好的，我们可以直接拿过来用，变量的作用都是固定</p>
<ul>
<li><code>$?</code>:返回的是上一个执行命令的返回值，执行成功返回 0 ，执行失败返回非0</li>
<li><code>$$</code>:获取当前脚本的进程号</li>
<li><code>$!</code>:获取最后一个后台执行的进程号</li>
</ul>
<h2 id="3-3-获取输入的变量-04-sh"><a href="#3-3-获取输入的变量-04-sh" class="headerlink" title="3.3.获取输入的变量(04.sh)"></a>3.3.获取输入的变量(04.sh)</h2><ul>
<li><code>read -p &quot;提示&quot; name</code>：name 代表接受的变量名</li>
<li><code>read -sp &quot;提示&quot; pwd</code>：<code>-sp 看不到输入的密码</code>，pwd 代表接受的变量名</li>
<li><code>read -t 5 -n 1 -p &quot;please input [y|n]&quot; input</code>：-t 5代表5秒没响应就退出，-n 1 代表只能输入1个字符</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 获取用户及时输入的参数</span><br><span class="line">#read -p &quot;please input your name!&quot; name</span><br><span class="line">#echo &quot;name = $name&quot;</span><br><span class="line"></span><br><span class="line">#输入年龄</span><br><span class="line"># read -sp &quot;please input your password&quot; pwd</span><br><span class="line"># echo &quot;password=$pwd&quot;</span><br><span class="line"></span><br><span class="line">read -t 5 -n 1 -p &quot;please input [y|n]&quot; input</span><br><span class="line">echo &quot;input = $input&quot; </span><br></pre></td></tr></table></figure>
<h1 id="4-条件判断-测试指令-（05-sh）"><a href="#4-条件判断-测试指令-（05-sh）" class="headerlink" title="4.条件判断(测试指令)（05.sh）"></a>4.条件判断(测试指令)（05.sh）</h1><p>条件判断 <code>test 表达式</code> 或者 <code>[]</code></p>
<p>数字计算:</p>
<ul>
<li><code>num3=$(($num1+$num2))</code></li>
<li>num3=&#96;expr $num1 + $num2&#96; // 执行 expr命令，一定要有空格 传的三个参数</li>
<li><code>test $str1 == $str2</code> // <code>==之间一定更要空格</code></li>
</ul>
<p>字符串空判断：</p>
<ul>
<li><code>[ str ]</code>: 测试字符串是否不为空，<font color=red>[]中间必须带空格</font></li>
<li><code>test -n str</code>: 测试字符串是否不为空</li>
<li><code>test -z str</code>: 测试字符串是否为空</li>
<li><code>[ str1 = str2 ]</code>: 是否相等，<font color=red>[]中间必须带空格</font></li>
</ul>
<p>数字：</p>
<ul>
<li><code>[ num1 -eq num2 ]</code>： 测试是否相等</li>
<li><code>[ num1 -ne num2 ]</code>： 不等</li>
<li><code>[ num1 -ge num2 ]</code>： &gt;=</li>
<li><code>[ num1 -gt num2 ]</code>： &gt;</li>
<li><code>[ num1 -le num2 ]</code>： &lt;=</li>
<li><code>[ num1 -lt num2 ]</code>： &lt;</li>
</ul>
<p>文件：</p>
<ul>
<li><code>test -d file</code>：是否是目录</li>
<li><code>test -f file</code>：是否是普通文件</li>
<li><code>test -e file</code>：文件是否存在</li>
<li><code>test -L file</code>：是否是链接</li>
<li><code>test -r file</code>：是否可读</li>
<li><code>test -w file</code>：是否可写</li>
<li><code>test -x file</code>：是否可执行</li>
</ul>
<h1 id="5-流程语句-if-（06-sh）"><a href="#5-流程语句-if-（06-sh）" class="headerlink" title="5.流程语句 if （06.sh）"></a>5.流程语句 if （06.sh）</h1><p>有两种方式:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if [ 表达式 ]; then</span><br><span class="line"></span><br><span class="line">if [ 表达式 ]</span><br><span class="line">then</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 判断有没有输入</span><br><span class="line">if [ -z $1 ]</span><br><span class="line">then</span><br><span class="line">	# echo &quot;usage：./build_ogre.sh [build_type]&quot;</span><br><span class="line">        # echo &quot;build_type: Release or Debug (must have)&quot;</span><br><span class="line">	# exit</span><br><span class="line">	error</span><br><span class="line">fi</span><br><span class="line"></span><br><span class="line"># 需要用户去指定编译 Debug 还是 Release</span><br><span class="line">CMAKE_BUILD_TYPE=&quot;&quot;</span><br><span class="line">if [ &quot;Debug&quot; = $1 ]; then</span><br><span class="line">	CMAKE_BUILD_TYPE=&quot;Debug&quot;</span><br><span class="line">elif [ &quot;Release&quot; = $1 ]; then</span><br><span class="line">	CMAKE_BUILD_TYPE=&quot;Release&quot;</span><br><span class="line">else</span><br><span class="line">	error arm</span><br><span class="line">fi</span><br></pre></td></tr></table></figure>
<h1 id="6-自定义函数-06-sh"><a href="#6-自定义函数-06-sh" class="headerlink" title="6.自定义函数(06.sh)"></a>6.自定义函数(06.sh)</h1><ul>
<li>函数需要先定义再使用,<code>function和()必须有其一</code></li>
<li>可以直接采用方法名调用,不用加()</li>
<li>$1 参数不是代表的脚本参数，而是方法传递过来的参数 </li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// function和()可以只有一个</span><br><span class="line">function error()&#123; </span><br><span class="line">	echo &quot;usage：./build_ogre.sh [build_type]&quot;</span><br><span class="line">        echo &quot;build_type: Release or Debug (must have)&quot;</span><br><span class="line">	# 函数需要穿参数 arm $1 参数不是代表的脚本参数，而是方法传递过来的参数 </span><br><span class="line">	echo &quot;\$1 = $1&quot;</span><br><span class="line">        exit</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="7-流程语句-for、while、case-（06-sh）"><a href="#7-流程语句-for、while、case-（06-sh）" class="headerlink" title="7.流程语句 for、while、case （06.sh）"></a>7.流程语句 for、while、case （06.sh）</h1><p>for:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for((i=0;i&lt;10;i++))</span><br><span class="line">do</span><br><span class="line">echo $i</span><br><span class="line">done</span><br></pre></td></tr></table></figure>
<p>while:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sum=0</span><br><span class="line">i=1</span><br><span class="line">while(($i&lt;=100))</span><br><span class="line">do</span><br><span class="line">	sum=$(($sum+$i))</span><br><span class="line">	i=$(($i+1))</span><br><span class="line">done</span><br><span class="line">echo &quot;sum = $sum&quot;</span><br></pre></td></tr></table></figure>
<p>case:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">case $2 in</span><br><span class="line">start)</span><br><span class="line">	echo &quot;start service&quot;</span><br><span class="line">	;;</span><br><span class="line">stop)</span><br><span class="line">	echo &quot;stop service&quot;</span><br><span class="line">	;;</span><br><span class="line">*)</span><br><span class="line">	echo &quot;invalid command&quot;</span><br><span class="line">	echo &quot;Usage:&#123;start|stop&#125;&quot;</span><br><span class="line">esac</span><br></pre></td></tr></table></figure>
<h1 id="8-输出重定向-07-sh"><a href="#8-输出重定向-07-sh" class="headerlink" title="8.输出重定向(07.sh)"></a>8.输出重定向(07.sh)</h1><p><code>0:</code> 标准输入,<code>1</code>：标准输出 <code>2</code>：标准错误输出</p>
<ul>
<li><code>echo &quot;Hello Shell!&quot; &gt; log.txt</code> // 输出日志到 log.txt 中，<code>&gt;</code> 代表重新生成</li>
<li><code>echo &quot;Hello Shell!&quot; &gt;&gt; log.txt</code> // 输出日志到 log.txt 中，<code>&gt;&gt;</code> 代表追加</li>
<li><code>exec 1&gt;log.txt</code>：标准的所有输出，<code>重新生成</code>全部添加到log.txt中</li>
<li><code>exec 2&gt;&gt;log.txt</code>：标准的错误输出，全部<code>追加</code>到log.txt中</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 永久重定向  echo 标准输入：0 标准输出：1 标准错误：2</span><br><span class="line">exec 1&gt;log.txt</span><br><span class="line">exec 2&gt;&gt;log.txt</span><br><span class="line"># &gt;&gt; 代表追加，&gt; 代表重新生成</span><br><span class="line"># 临时重定向</span><br><span class="line"># echo &quot;Hello Shell!&quot; &gt; log.txt   </span><br><span class="line"># echo &quot;Hello Shell!&quot; &gt;&gt; log.txt   </span><br><span class="line"></span><br><span class="line">echo &quot;Hello Shell&quot;</span><br><span class="line">echo &quot;Hello Shell&quot;</span><br><span class="line">echo &quot;Hello Shell&quot;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：13.FFmpeg-音乐播放器2</title>
    <url>/84.FFmpeg-%E9%9F%B3%E4%B9%90%E6%92%AD%E6%94%BE%E5%99%A82/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/ffmpeg83">NDKPractice项目的ffmpeg83</a></strong></p>
<h1 id="1-解决内存上涨的问题"><a href="#1-解决内存上涨的问题" class="headerlink" title="1.解决内存上涨的问题"></a>1.解决内存上涨的问题</h1><p>将循环中<code>新建数组操作提到循环外面去</code><br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">jbyteArray jPcmByteArray =  env-&gt;<span class="built_in">NewByteArray</span>(dataSize);</span><br><span class="line"><span class="comment">// native 创建 c 数组</span></span><br><span class="line">jbyte *jPcmData =  env-&gt;<span class="built_in">GetByteArrayElements</span>(jPcmByteArray, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">pPacket = <span class="built_in">av_packet_alloc</span>();</span><br><span class="line">pFrame = <span class="built_in">av_frame_alloc</span>();</span><br><span class="line"><span class="comment">// 循环从上下文中读取帧到包中</span></span><br><span class="line"><span class="keyword">while</span> (<span class="built_in">av_read_frame</span>(pFormatContext, pPacket) &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (pPacket-&gt;stream_index == audioStreamIndex) &#123;</span><br><span class="line">        <span class="comment">// Packet 包，压缩的数据，解码成 pcm 数据</span></span><br><span class="line">        <span class="type">int</span> codecSendPacketRes = <span class="built_in">avcodec_send_packet</span>(pCodecContext, pPacket);</span><br><span class="line">        <span class="keyword">if</span> (codecSendPacketRes == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="type">int</span> codecReceiveFrameRes = <span class="built_in">avcodec_receive_frame</span>(pCodecContext, pFrame);</span><br><span class="line">            <span class="keyword">if</span> (codecReceiveFrameRes == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// AVPacket -&gt; AVFrame</span></span><br><span class="line">                index++;</span><br><span class="line">                <span class="built_in">LOGE</span>(<span class="string">&quot;解码第 %d 帧&quot;</span>, index);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 调用重采样的方法</span></span><br><span class="line">                <span class="built_in">swr_convert</span>(swrContext, &amp;resampleOutBuffer, pFrame-&gt;nb_samples,</span><br><span class="line">                            (<span class="type">const</span> <span class="type">uint8_t</span> **) pFrame-&gt;data, pFrame-&gt;nb_samples);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// write 写到缓冲区 pFrame.data -&gt; javabyte</span></span><br><span class="line">                <span class="comment">// size 是多大，装 pcm 的数据</span></span><br><span class="line">                <span class="comment">// 1s 44100 点，2通道， 2字节 44100*2*2</span></span><br><span class="line">                <span class="comment">// 1帧不是一秒，pFrame-&gt;nb_samples点</span></span><br><span class="line"></span><br><span class="line">                <span class="built_in">memcpy</span>(jPcmData, resampleOutBuffer, dataSize);</span><br><span class="line">                <span class="comment">// 1 把 c 的数组的数据同步到 jbyteArray,然后不释放native数组</span></span><br><span class="line">                env-&gt;<span class="built_in">ReleaseByteArrayElements</span>(jPcmByteArray, jPcmData, JNI_COMMIT);</span><br><span class="line">                pJniCall-&gt;<span class="built_in">callAudioTrackWrite</span>(jPcmByteArray, <span class="number">0</span>, dataSize);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 解引用</span></span><br><span class="line">    <span class="built_in">av_packet_unref</span>(pPacket);</span><br><span class="line">    <span class="built_in">av_frame_unref</span>(pFrame);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 1.解引用数据 data, 2.销魂 pPacket 结构体内存， 3.pPacket = NULL;</span></span><br><span class="line"><span class="built_in">av_packet_free</span>(&amp;pPacket);</span><br><span class="line"><span class="built_in">av_frame_free</span>(&amp;pFrame);</span><br><span class="line"><span class="comment">// 0 把 c 的数组的数据同步到 jbyteArray,然后释放native数组</span></span><br><span class="line">env-&gt;<span class="built_in">ReleaseByteArrayElements</span>(jPcmByteArray, jPcmData, <span class="number">0</span>);</span><br><span class="line"><span class="comment">// 解除 jPcmDataArray 的持有，让 javaGC 回收</span></span><br><span class="line">env-&gt;<span class="built_in">DeleteLocalRef</span>(jPcmByteArray);</span><br></pre></td></tr></table></figure></p>
<h1 id="2-解决杂音问题"><a href="#2-解决杂音问题" class="headerlink" title="2.解决杂音问题"></a>2.解决杂音问题</h1><p>分析  </p>
<ul>
<li>原因：是因为音频的的采样率和采用格式跟我们使用AudioTrack播放设置的会不一致。</li>
<li>解决：使用重采样</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// --------------- 重采样 start --------------</span></span><br><span class="line">  <span class="comment">//输出的声道布局（立体声）</span></span><br><span class="line">  <span class="type">int64_t</span> out_ch_layout = AV_CH_LAYOUT_STEREO;</span><br><span class="line">  <span class="comment">//输出采样格式16bit PCM</span></span><br><span class="line">  <span class="keyword">enum</span> <span class="title class_">AVSampleFormat</span> out_sample_fmt = AVSampleFormat::AV_SAMPLE_FMT_S16;</span><br><span class="line">  <span class="comment">//输出采样率</span></span><br><span class="line">  <span class="type">int</span> out_sample_rate = AUDIO_SAMPLE_RATE;</span><br><span class="line">  <span class="comment">//获取输入的声道布局</span></span><br><span class="line">  <span class="comment">//根据声道个数获取默认的声道布局（2个声道，默认立体声stereo）</span></span><br><span class="line">  <span class="type">int64_t</span> in_ch_layout = pCodecContext-&gt;channel_layout;</span><br><span class="line">  <span class="comment">//输入的采样格式</span></span><br><span class="line">  <span class="keyword">enum</span> <span class="title class_">AVSampleFormat</span> in_sample_fmt = pCodecContext-&gt;sample_fmt;</span><br><span class="line">  <span class="comment">//输入采样率</span></span><br><span class="line">  <span class="type">int</span> in_sample_rate = pCodecContext-&gt;sample_rate;</span><br><span class="line">  swrContext = <span class="built_in">swr_alloc_set_opts</span>(<span class="literal">NULL</span>, out_ch_layout, out_sample_fmt,</span><br><span class="line">                                              out_sample_rate, in_ch_layout, in_sample_fmt,</span><br><span class="line">                                              in_sample_rate, <span class="number">0</span>, <span class="literal">NULL</span>);</span><br><span class="line">  <span class="keyword">if</span> (swrContext == <span class="literal">NULL</span>) &#123;</span><br><span class="line">      <span class="built_in">LOGE</span>(<span class="string">&quot;swr alloc set opts error&quot;</span>);</span><br><span class="line">      <span class="built_in">callPlayerJniError</span>(SWR_ALLOC_SET_OPTS_ERROR_CODE, <span class="string">&quot;swr alloc set opts error&quot;</span>);</span><br><span class="line">      <span class="comment">// 提示错误</span></span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="type">int</span> swrInitRes = <span class="built_in">swr_init</span>(swrContext);</span><br><span class="line">  <span class="keyword">if</span> (swrInitRes &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="built_in">LOGE</span>(<span class="string">&quot;swr context swr init error&quot;</span>);</span><br><span class="line">      <span class="built_in">callPlayerJniError</span>(SWR_CONTEXT_INIT_ERROR_CODE, <span class="string">&quot;swr context swr init error&quot;</span>);</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// size 是播放指定的大小，是最终输出的大小</span></span><br><span class="line">  <span class="type">int</span> outChannels = <span class="built_in">av_get_channel_layout_nb_channels</span>(out_ch_layout);</span><br><span class="line">  <span class="type">int</span> dataSize = <span class="built_in">av_samples_get_buffer_size</span>(<span class="literal">NULL</span>, outChannels, pCodecParameters-&gt;frame_size,</span><br><span class="line">                                            out_sample_fmt, <span class="number">0</span>);</span><br><span class="line">  resampleOutBuffer = (<span class="type">uint8_t</span> *) <span class="built_in">malloc</span>(dataSize);</span><br><span class="line">  <span class="comment">// --------------- 重采样 end --------------</span></span><br><span class="line"></span><br><span class="line">  ....</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (<span class="built_in">av_read_frame</span>(pFormatContext, pPacket) &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (pPacket-&gt;stream_index == audioStreamIndex) &#123;</span><br><span class="line">          <span class="comment">// Packet 包，压缩的数据，解码成 pcm 数据</span></span><br><span class="line">          <span class="type">int</span> codecSendPacketRes = <span class="built_in">avcodec_send_packet</span>(pCodecContext, pPacket);</span><br><span class="line">          <span class="keyword">if</span> (codecSendPacketRes == <span class="number">0</span>) &#123;</span><br><span class="line">              <span class="type">int</span> codecReceiveFrameRes = <span class="built_in">avcodec_receive_frame</span>(pCodecContext, pFrame);</span><br><span class="line">              <span class="keyword">if</span> (codecReceiveFrameRes == <span class="number">0</span>) &#123;</span><br><span class="line">                  <span class="comment">// AVPacket -&gt; AVFrame</span></span><br><span class="line">                  index++;</span><br><span class="line">                  <span class="built_in">LOGE</span>(<span class="string">&quot;解码第 %d 帧&quot;</span>, index);</span><br><span class="line"></span><br><span class="line">                  <span class="comment">// 调用重采样的方法</span></span><br><span class="line">                  <span class="built_in">swr_convert</span>(swrContext, &amp;resampleOutBuffer, pFrame-&gt;nb_samples,</span><br><span class="line">                              (<span class="type">const</span> <span class="type">uint8_t</span> **) pFrame-&gt;data, pFrame-&gt;nb_samples);</span><br><span class="line">                  </span><br><span class="line">                  ....</span><br><span class="line"></span><br><span class="line">                  <span class="built_in">memcpy</span>(jPcmData, resampleOutBuffer, dataSize);</span><br><span class="line">                  <span class="comment">// 1 把 c 的数组的数据同步到 jbyteArray,然后不释放native数组</span></span><br><span class="line">                  env-&gt;<span class="built_in">ReleaseByteArrayElements</span>(jPcmByteArray, jPcmData, JNI_COMMIT);</span><br><span class="line">                  pJniCall-&gt;<span class="built_in">callAudioTrackWrite</span>(jPcmByteArray, <span class="number">0</span>, dataSize);</span><br><span class="line">              &#125;</span><br><span class="line">          &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">// 解引用</span></span><br><span class="line">      <span class="built_in">av_packet_unref</span>(pPacket);</span><br><span class="line">      <span class="built_in">av_frame_unref</span>(pFrame);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h1 id="3-添加错误回调到-Java"><a href="#3-添加错误回调到-Java" class="headerlink" title="3. 添加错误回调到 Java"></a>3. 添加错误回调到 Java</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"> jPlayerErrorMid = jniEnv-&gt;<span class="built_in">GetMethodID</span>(jPlayerClass, <span class="string">&quot;onError&quot;</span>, <span class="string">&quot;(ILjava/lang/String;)V&quot;</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">JNICall::callPlayerError</span><span class="params">(<span class="type">int</span> code, <span class="type">char</span> *msg)</span> </span>&#123;</span><br><span class="line">    jstring jMsg = jniEnv-&gt;<span class="built_in">NewStringUTF</span>(msg);</span><br><span class="line">    jniEnv-&gt;<span class="built_in">CallVoidMethod</span>(jPlayerObj, jPlayerErrorMid, code, jMsg);</span><br><span class="line">    jniEnv-&gt;<span class="built_in">DeleteLocalRef</span>(jMsg);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h1 id="4-多线程解码播放"><a href="#4-多线程解码播放" class="headerlink" title="4.多线程解码播放"></a>4.多线程解码播放</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> *<span class="title">threadPlay</span><span class="params">(<span class="type">void</span> *arg)</span> </span>&#123;</span><br><span class="line">    FFmpeg *pFFmpeg = (FFmpeg *) arg;</span><br><span class="line">    pFFmpeg-&gt;<span class="built_in">prepare</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">FFmpeg::play</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 创建一个线程去播放，多线程边解码边播放</span></span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    <span class="built_in">pthread_create</span>(&amp;tid,<span class="literal">NULL</span>,threadPlay,<span class="keyword">this</span>);</span><br><span class="line">    <span class="built_in">pthread_detach</span>(tid); <span class="comment">// 回收线程</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="5-ffmpeg的一些常用指令"><a href="#5-ffmpeg的一些常用指令" class="headerlink" title="5.ffmpeg的一些常用指令"></a>5.ffmpeg的一些常用指令</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ffmpeg 命令</span><br><span class="line">转格式</span><br><span class="line">ffmpeg -i input.mp4 -vcodec copy -acodec copy out.flv</span><br><span class="line">抽取视频</span><br><span class="line">ffmpeg -i input.mp4 -an -vcodec copy out.h264</span><br><span class="line">抽取音频</span><br><span class="line">ffmpeg -i input.mp4 -vn -acodec copy out.aac</span><br><span class="line">提取 YUV </span><br><span class="line">ffmpeg -i input.mp4 -an -c:v rawvideo -pix_fmt yuv420p out.yuv</span><br><span class="line">提取 PCM</span><br><span class="line">ffmpeg -i input.mp4 -vn -ar <span class="number">44100</span> -ac2 -f s16le out.pcm</span><br><span class="line">视频裁剪</span><br><span class="line">ffmpeg -i input.mp4 -vf corp=in_w<span class="number">-200</span>:in_h<span class="number">-200</span> -c:v libx264 -c:a copy out.mp4</span><br><span class="line">视频裁剪</span><br><span class="line">ffmpeg -i input.mp4 -ss <span class="number">00</span>:<span class="number">00</span>:<span class="number">00</span> -t <span class="number">10</span> out.mp4</span><br><span class="line">视频转图片</span><br><span class="line">ffmpeg -i input.mp4 -r <span class="number">1</span> -f image2 image-%<span class="number">3</span>d.jpeg</span><br><span class="line">图片转视频</span><br><span class="line">ffmpeg -i image-%<span class="number">3</span>d.jpeg out.mp4</span><br><span class="line">直播推流</span><br><span class="line">ffmpeg -re -i output.mp4 -c copy -f flv rtmp:<span class="comment">//server/live/streamName</span></span><br><span class="line">直播拉流</span><br><span class="line">ffmpeg -i rtmp:<span class="comment">//server/live/streamName -c copy dump.flv</span></span><br></pre></td></tr></table></figure>
<p>问题：<font color =red>子线程中返回 java 错误会有问题</font></p>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：14.FFmpeg-音乐播放器3</title>
    <url>/85.FFmpeg-%E9%9F%B3%E4%B9%90%E6%92%AD%E6%94%BE%E5%99%A83/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/ffmpeg83">NDKPractice项目的ffmpeg83</a></strong></p>
<h1 id="1-添加准备完毕回调"><a href="#1-添加准备完毕回调" class="headerlink" title="1.添加准备完毕回调"></a>1.添加准备完毕回调</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">JNICall::<span class="built_in">JNICall</span>(JavaVM *javaVM, JNIEnv *jniEnv, jobject jPlayerObj) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;javaVM = javaVM;</span><br><span class="line">    <span class="keyword">this</span>-&gt;jniEnv = jniEnv;</span><br><span class="line">    <span class="keyword">this</span>-&gt;jPlayerObj = jniEnv-&gt;<span class="built_in">NewGlobalRef</span>(jPlayerObj);</span><br><span class="line"></span><br><span class="line">    jclass jPlayerClass = jniEnv-&gt;<span class="built_in">GetObjectClass</span>(jPlayerObj);</span><br><span class="line">    jPlayerErrorMid = jniEnv-&gt;<span class="built_in">GetMethodID</span>(jPlayerClass, <span class="string">&quot;onError&quot;</span>, <span class="string">&quot;(ILjava/lang/String;)V&quot;</span>);</span><br><span class="line">    jPlayerPreparedMid = jniEnv-&gt;<span class="built_in">GetMethodID</span>(jPlayerClass, <span class="string">&quot;onPrepared&quot;</span>, <span class="string">&quot;()V&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">JNICall::CallPlayerPrepared</span><span class="params">(ThreadMode threadMode)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 子线程(pThread)用不了主线程(native线程)的 jniEnv</span></span><br><span class="line">    <span class="comment">// 子线程是不共享 jniEnv，他们有自己所独有的</span></span><br><span class="line">    <span class="keyword">if</span> (threadMode == THREAD_MAIN) &#123;</span><br><span class="line">        jniEnv-&gt;<span class="built_in">CallVoidMethod</span>(jPlayerObj, jPlayerPreparedMid);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 通过 JavaVM获取当前线程的 JniEnv</span></span><br><span class="line">        JNIEnv *env;</span><br><span class="line">        <span class="keyword">if</span> (javaVM-&gt;<span class="built_in">AttachCurrentThread</span>(&amp;env, <span class="number">0</span>) != JNI_OK) &#123;</span><br><span class="line">            <span class="built_in">LOGE</span>(<span class="string">&quot;get child thread jniEnv error&quot;</span>);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        env-&gt;<span class="built_in">CallVoidMethod</span>(jPlayerObj, jPlayerPreparedMid);</span><br><span class="line">        javaVM-&gt;<span class="built_in">DetachCurrentThread</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-用队列边解码边播放"><a href="#2-用队列边解码边播放" class="headerlink" title="2.用队列边解码边播放"></a>2.用队列边解码边播放</h1><p>为什么要用队列边解码边播放？</p>
<p>原因：<font color=red>首先解码成pcm是耗时的，如果播放的网络音频，网络卡顿时读取也会耗时，这个时候读取本来就耗时的情况下每次读取成功还等待解码完成后再去读取就会有点卡顿。所以咱们读取和解码分开就会提升性能</font></p>
<p><code>av_read_frame：从流里面解析一个一个的 packet ，流可能是本地流也可以是网络流</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="built_in">av_read_frame</span>(pFormatContext, pPacket) &gt;= <span class="number">0</span>) &#123;   <span class="comment">// 读取</span></span><br><span class="line">           <span class="keyword">if</span> (pPacket-&gt;stream_index == audioStreamIndex) &#123;</span><br><span class="line">               </span><br><span class="line">               <span class="comment">//--------------------- 解码开始 -------------------------/</span></span><br><span class="line">               <span class="comment">// Packet 包，压缩的数据，解码成 pcm 数据</span></span><br><span class="line">               <span class="type">int</span> codecSendPacketRes = <span class="built_in">avcodec_send_packet</span>(pCodecContext, pPacket);</span><br><span class="line">               <span class="keyword">if</span> (codecSendPacketRes == <span class="number">0</span>) &#123;</span><br><span class="line">                   <span class="type">int</span> codecReceiveFrameRes = <span class="built_in">avcodec_receive_frame</span>(pCodecContext, pFrame);</span><br><span class="line">                   <span class="keyword">if</span> (codecReceiveFrameRes == <span class="number">0</span>) &#123;</span><br><span class="line">                       <span class="comment">// AVPacket -&gt; AVFrame</span></span><br><span class="line">                       index++;</span><br><span class="line">                       <span class="built_in">LOGE</span>(<span class="string">&quot;解码第 %d 帧&quot;</span>, index);</span><br><span class="line"></span><br><span class="line">                       <span class="comment">// 调用重采样的方法</span></span><br><span class="line">                       <span class="built_in">swr_convert</span>(swrContext, &amp;resampleOutBuffer, pFrame-&gt;nb_samples,</span><br><span class="line">                                   (<span class="type">const</span> <span class="type">uint8_t</span> **) pFrame-&gt;data, pFrame-&gt;nb_samples);</span><br><span class="line"></span><br><span class="line">                       <span class="comment">// write 写到缓冲区 pFrame.data -&gt; javabyte</span></span><br><span class="line">                       <span class="comment">// size 是多大，装 pcm 的数据</span></span><br><span class="line">                       <span class="comment">// 1s 44100 点，2通道， 2字节 44100*2*2</span></span><br><span class="line">                       <span class="comment">// 1帧不是一秒，pFrame-&gt;nb_samples点</span></span><br><span class="line"></span><br><span class="line">                       <span class="built_in">memcpy</span>(jPcmData, resampleOutBuffer, dataSize);</span><br><span class="line">                       <span class="comment">// 1 把 c 的数组的数据同步到 jbyteArray,然后不释放native数组</span></span><br><span class="line">                       env-&gt;<span class="built_in">ReleaseByteArrayElements</span>(jPcmByteArray, jPcmData, JNI_COMMIT);</span><br><span class="line"></span><br><span class="line">                       pJniCall-&gt;<span class="built_in">callAudioTrackWrite</span>(env,jPcmByteArray, <span class="number">0</span>, dataSize);</span><br><span class="line">                   &#125;</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="comment">//--------------------- 解码结束 -------------------------/</span></span><br><span class="line"></span><br><span class="line">           &#125;</span><br><span class="line">           <span class="comment">// 解引用</span></span><br><span class="line">           <span class="built_in">av_packet_unref</span>(pPacket);</span><br><span class="line">           <span class="built_in">av_frame_unref</span>(pFrame);</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       <span class="comment">// 1.解引用数据 data, 2.销魂 pPacket 结构体内存， 3.pPacket = NULL;</span></span><br><span class="line">       <span class="built_in">av_packet_free</span>(&amp;pPacket);</span><br><span class="line">       <span class="built_in">av_frame_free</span>(&amp;pFrame);</span><br></pre></td></tr></table></figure>
<p>修改过后,具体请看文件<a href="https://github.com/EastUp/NDKPractice/blob/master/ffmpeg83/src/main/cpp/Audio.cpp">Audio.cpp</a></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 读取的线程</span></span><br><span class="line"><span class="function"><span class="type">void</span> *<span class="title">threadReadPacket</span><span class="params">(<span class="type">void</span> *args)</span></span>&#123;</span><br><span class="line">    Audio *pAudio = (Audio*)args;</span><br><span class="line">    <span class="keyword">while</span>(pAudio-&gt;pPlayerStatus != <span class="literal">NULL</span> &amp;&amp; !pAudio-&gt;pPlayerStatus-&gt;isExit)&#123;</span><br><span class="line">       AVPacket *pPacket = <span class="built_in">av_packet_alloc</span>();</span><br><span class="line">        <span class="comment">// 循环从上下文中读取帧到包中</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">av_read_frame</span>(pAudio-&gt;pFormatContext, pPacket) &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (pPacket-&gt;stream_index == pAudio-&gt;audioStreamIndex) &#123;</span><br><span class="line">                <span class="comment">// 读取音频压缩包数据后，将其push 到 队列中</span></span><br><span class="line">                pAudio-&gt;pPacketQueue-&gt;<span class="built_in">push</span>(pPacket);</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="comment">// 解引用</span></span><br><span class="line">                <span class="built_in">av_packet_unref</span>(pPacket);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="comment">// 1.解引用数据 data, 2.销魂 pPacket 结构体内存， 3.pPacket = NULL;</span></span><br><span class="line">            <span class="built_in">av_packet_free</span>(&amp;pPacket);</span><br><span class="line">            <span class="comment">// 睡眠一下，尽量不去消耗 cpu 的资源，也可以退出销毁这个线程</span></span><br><span class="line">            <span class="comment">// break;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 解码的线程 OpenSLES</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">Audio::resampleAudio</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> dataSize = <span class="number">0</span>;</span><br><span class="line">    AVPacket *pPacket = <span class="literal">nullptr</span>;</span><br><span class="line">    AVFrame *pFrame = <span class="built_in">av_frame_alloc</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (pPlayerStatus != <span class="literal">nullptr</span> &amp;&amp; !pPlayerStatus-&gt;isExit) &#123;</span><br><span class="line">        pPacket = pPacketQueue-&gt;<span class="built_in">pop</span>();</span><br><span class="line">        <span class="comment">// Packet 包，压缩的数据，解码成 pcm 数据</span></span><br><span class="line">        <span class="type">int</span> codecSendPacketRes = <span class="built_in">avcodec_send_packet</span>(pCodecContext, pPacket);</span><br><span class="line">        <span class="keyword">if</span> (codecSendPacketRes == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="type">int</span> codecReceiveFrameRes = <span class="built_in">avcodec_receive_frame</span>(pCodecContext, pFrame);</span><br><span class="line">            <span class="keyword">if</span> (codecReceiveFrameRes == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// AVPacket -&gt; AVFrame</span></span><br><span class="line">                <span class="comment">// 调用重采样的方法，返回值是返回重采样的个数，也就是 pFrame-&gt;nb_samples</span></span><br><span class="line">                dataSize = <span class="built_in">swr_convert</span>(pSwrContext, &amp;resampleOutBuffer, pFrame-&gt;nb_samples,</span><br><span class="line">                                       (<span class="type">const</span> <span class="type">uint8_t</span> **) pFrame-&gt;data, pFrame-&gt;nb_samples);</span><br><span class="line">                <span class="built_in">LOGE</span>(<span class="string">&quot;解码音频帧：%d %d&quot;</span>,dataSize,pFrame-&gt;nb_samples);</span><br><span class="line"></span><br><span class="line">                dataSize = pFrame-&gt;nb_samples * <span class="number">2</span> * <span class="number">2</span>; <span class="comment">// 采样率 * 通道数 * 两字节</span></span><br><span class="line">                <span class="comment">// write 写到缓冲区 pFrame.data -&gt; javabyte</span></span><br><span class="line">                <span class="comment">// size 是多大，装 pcm 的数据</span></span><br><span class="line">                <span class="comment">// 1s 44100 点，2通道， 2字节 44100*2*2</span></span><br><span class="line">                <span class="comment">// 1帧不是一秒，pFrame-&gt;nb_samples点</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 解引用</span></span><br><span class="line">        <span class="built_in">av_packet_unref</span>(pPacket);</span><br><span class="line">        <span class="built_in">av_frame_unref</span>(pFrame);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1.解引用数据 data, 2.销魂 pPacket 结构体内存， 3.pPacket = NULL;</span></span><br><span class="line">    <span class="built_in">av_packet_free</span>(&amp;pPacket);</span><br><span class="line">    <span class="built_in">av_frame_free</span>(&amp;pFrame);</span><br><span class="line">    <span class="keyword">return</span> dataSize;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：15.FFmpeg-视频播放器1</title>
    <url>/86.FFmpeg-%E8%A7%86%E9%A2%91%E6%92%AD%E6%94%BE%E5%99%A81/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/ffmpeg86">NDKPractice项目的ffmpeg86</a></strong></p>
<p>将之前的音乐播放器代码整合进来</p>
<h1 id="1-使用FFmpeg解码视频"><a href="#1-使用FFmpeg解码视频" class="headerlink" title="1.使用FFmpeg解码视频"></a>1.使用FFmpeg解码视频</h1><p>跟使用FFmpeg解码音频流程一致，但需要判断是否能硬解码</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//-------------需要将视频的格式一般为yuv420P转为RBGA8888-------------------------//</span></span><br><span class="line">   <span class="comment">// 1. 获取窗体</span></span><br><span class="line">   ANativeWindow *pNativeWindow = <span class="built_in">ANativeWindow_fromSurface</span>(env,surface);</span><br><span class="line">   <span class="comment">// 2.设置缓冲区的数据</span></span><br><span class="line">   <span class="built_in">ANativeWindow_setBuffersGeometry</span>(pNativeWindow,pCodecContext-&gt;width,pCodecContext-&gt;height,</span><br><span class="line">           WINDOW_FORMAT_RGBA_8888);</span><br><span class="line">   <span class="comment">// Window 缓冲区的 Buffer</span></span><br><span class="line">   ANativeWindow_Buffer outBuffer;</span><br><span class="line">   <span class="comment">// 3.初始化转换上下文</span></span><br><span class="line">   SwsContext *pSwsContext = <span class="built_in">sws_getContext</span>(pCodecContext-&gt;width,pCodecContext-&gt;height,pCodecContext-&gt;pix_fmt,</span><br><span class="line">                  pCodecContext-&gt;width,pCodecContext-&gt;height,AV_PIX_FMT_RGBA,</span><br><span class="line">                  SWS_BILINEAR,<span class="literal">NULL</span>,<span class="literal">NULL</span>,<span class="literal">NULL</span>);</span><br><span class="line">   AVFrame *pRgbaFrame = <span class="built_in">av_frame_alloc</span>();</span><br><span class="line">   <span class="type">int</span> frameSize = <span class="built_in">av_image_get_buffer_size</span>(AV_PIX_FMT_RGBA,pCodecContext-&gt;width,</span><br><span class="line">           pCodecContext-&gt;height,<span class="number">1</span>);</span><br><span class="line">   <span class="type">uint8_t</span> *frameBuffer = (<span class="type">uint8_t</span>*)<span class="built_in">malloc</span>(frameSize);</span><br><span class="line">   <span class="comment">// 填充</span></span><br><span class="line">   <span class="built_in">av_image_fill_arrays</span>(pRgbaFrame-&gt;data,pRgbaFrame-&gt;linesize,frameBuffer,AV_PIX_FMT_RGBA,</span><br><span class="line">                        pCodecContext-&gt;width,pCodecContext-&gt;height,<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">   pPacket = <span class="built_in">av_packet_alloc</span>();</span><br><span class="line">   pFrame = <span class="built_in">av_frame_alloc</span>();</span><br><span class="line">   <span class="keyword">while</span> (<span class="built_in">av_read_frame</span>(pFormatContext, pPacket) &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">       <span class="keyword">if</span> (pPacket-&gt;stream_index == audioStramIndex) &#123;</span><br><span class="line">           <span class="comment">// Packet 包，压缩的数据，解码成 pcm 数据</span></span><br><span class="line">           <span class="type">int</span> codecSendPacketRes = <span class="built_in">avcodec_send_packet</span>(pCodecContext, pPacket);</span><br><span class="line">           <span class="keyword">if</span> (codecSendPacketRes == <span class="number">0</span>) &#123;</span><br><span class="line">               <span class="type">int</span> codecReceiveFrameRes = <span class="built_in">avcodec_receive_frame</span>(pCodecContext, pFrame);</span><br><span class="line">               <span class="keyword">if</span> (codecReceiveFrameRes == <span class="number">0</span>) &#123;</span><br><span class="line">                   <span class="comment">// AVPacket -&gt; AVFrame</span></span><br><span class="line">                   index++;</span><br><span class="line">                   <span class="built_in">LOGE</span>(<span class="string">&quot;解码第 %d 帧&quot;</span>, index);</span><br><span class="line">                   <span class="comment">// 渲染，显示，OpenGLES (高效，硬件支持)，SurfaceView</span></span><br><span class="line">                   <span class="comment">// 硬件加速和不加速有什么区别？cup 主要是用于计算，gpu 图像支持（硬件）</span></span><br><span class="line">                   <span class="comment">// 这个 pFrame-&gt;data , 一般 yuv420P 的，RGBA8888，因此需要转换</span></span><br><span class="line">                   <span class="comment">// 假设拿到了转换后的 RGBA 的 data 数据，如何渲染，把数据推到缓冲区</span></span><br><span class="line">                   <span class="built_in">sws_scale</span>(pSwsContext,pFrame-&gt;data,pFrame-&gt;linesize,</span><br><span class="line">                           <span class="number">0</span>,pCodecContext-&gt;height,pRgbaFrame-&gt;data,pRgbaFrame-&gt;linesize);</span><br><span class="line">                   <span class="comment">// 把数据推到缓冲区</span></span><br><span class="line">                   <span class="built_in">ANativeWindow_lock</span>(pNativeWindow,&amp;outBuffer,<span class="literal">NULL</span>);</span><br><span class="line">                   <span class="built_in">memcpy</span>(outBuffer.bits,frameBuffer,frameSize);</span><br><span class="line">                   <span class="built_in">ANativeWindow_unlockAndPost</span>(pNativeWindow);</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="comment">// 解引用</span></span><br><span class="line">       <span class="built_in">av_packet_unref</span>(pPacket);</span><br><span class="line">       <span class="built_in">av_frame_unref</span>(pFrame);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<font color=red>问题：视音频还是没同步的问题（视频快，音频正常）</font>

<h1 id="2-渲染视频"><a href="#2-渲染视频" class="headerlink" title="2.渲染视频"></a>2.渲染视频</h1><h2 id="2-1-OpenGLES解释（渲染视频高效，硬件支持）"><a href="#2-1-OpenGLES解释（渲染视频高效，硬件支持）" class="headerlink" title="2.1 OpenGLES解释（渲染视频高效，硬件支持）"></a>2.1 OpenGLES解释（渲染视频高效，硬件支持）</h2><h3 id="2-1-1-OpenGL概念"><a href="#2-1-1-OpenGL概念" class="headerlink" title="2.1.1 OpenGL概念"></a>2.1.1 OpenGL概念</h3><p>OpenGL 是一种应用程序的编程接口，它是一种可以对图像硬件设备进行访问的软件库，OpenGL 被设计为一个现代化的，硬件无关的接口，<br>因此我们不用考虑操作系统的前提下，在多种不同的图像硬件系统上通过软件的形式来实现 OpenGL 接口。<br>OpenGL 也没有提供任何表达三维物体模型和读取图文的操作，我们需要用一系列的几何图元来创建三维物体。<br>图元：线，三角形，点，patch  </p>
<h3 id="2-1-2-OpenGL-用来干什么"><a href="#2-1-2-OpenGL-用来干什么" class="headerlink" title="2.1.2 OpenGL 用来干什么"></a>2.1.2 OpenGL 用来干什么</h3><p>渲染视频、图形，图片处理，2D/3D 游戏引擎，科学可视化，CAD，虚拟现实，AI人工智能等等</p>
<h3 id="2-1-3-OpenGL-与-OpenGLES-有什么关系？"><a href="#2-1-3-OpenGL-与-OpenGLES-有什么关系？" class="headerlink" title="2.1.3 OpenGL 与 OpenGLES 有什么关系？"></a>2.1.3 OpenGL 与 OpenGLES 有什么关系？</h3><p>OpenGLES 是 OpenGL 阉割（精简）版本，主要用于移动端</p>
<h3 id="2-1-4-OpenGL后续学习"><a href="#2-1-4-OpenGL后续学习" class="headerlink" title="2.1.4 OpenGL后续学习"></a>2.1.4 OpenGL后续学习</h3><p>看谷歌的<a href="https://github.com/googlesamples/android-ndk/tree/master/gles3jni">示例</a>  </p>
<p>学开发编程的约定，坐标系变化，标量向量，渲染流水线  </p>
<p>基础绘制（数学基础），纹理，模型，雾效果，相机，美容，native 层  </p>
<h2 id="2-2-SurfaceView-渲染视频（这节先用SurfaceView）"><a href="#2-2-SurfaceView-渲染视频（这节先用SurfaceView）" class="headerlink" title="2.2 SurfaceView 渲染视频（这节先用SurfaceView）"></a>2.2 SurfaceView 渲染视频（这节先用SurfaceView）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">源代码分析：</span><br><span class="line">ViewRootImpl.performDraw()</span><br><span class="line">-&gt; ViewRootImpl.draw(fullRedrawNeeded);</span><br><span class="line">-&gt; ViewRootImpl.drawSoftware(surface, mAttachInfo, xOffset, yOffset,</span><br><span class="line">                           scalingRequired, dirty, surfaceInsets)</span><br><span class="line">   </span><br><span class="line">    canvas = mSurface.lockCanvas(dirty);</span><br><span class="line">    mView.draw(canvas);</span><br><span class="line"></span><br><span class="line">最后通过 ViewGroup 绘制子 View 将 canvas 传递下去</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="2-2-1-Button-和-TextView-的-Canvas-是不是同一个？"><a href="#2-2-1-Button-和-TextView-的-Canvas-是不是同一个？" class="headerlink" title="2.2.1 Button 和 TextView 的 Canvas 是不是同一个？"></a>2.2.1 Button 和 TextView 的 Canvas 是不是同一个？</h3><p>是！！！</p>
<h3 id="2-2-2-Canvas-是从哪里来的？"><a href="#2-2-2-Canvas-是从哪里来的？" class="headerlink" title="2.2.2 Canvas 是从哪里来的？"></a>2.2.2 Canvas 是从哪里来的？</h3><p>根布局的 <code>Canvas canvas = mSurface.lockCanvas(dirty)</code></p>
<h3 id="2-2-3-Canvas-绘制的东西到哪里去了（怎么渲染出来的）？"><a href="#2-2-3-Canvas-绘制的东西到哪里去了（怎么渲染出来的）？" class="headerlink" title="2.2.3 Canvas 绘制的东西到哪里去了（怎么渲染出来的）？"></a>2.2.3 Canvas 绘制的东西到哪里去了（怎么渲染出来的）？</h3><p><img src="/images/86.页面渲染流程.png" alt=""></p>
<h3 id="2-2-4-为什么-SurfaceView-能在子线程中更新-UI"><a href="#2-2-4-为什么-SurfaceView-能在子线程中更新-UI" class="headerlink" title="2.2.4 为什么 SurfaceView 能在子线程中更新 UI"></a>2.2.4 为什么 SurfaceView 能在子线程中更新 UI</h3><p>只要View是在子线程中创建的，就可以在子线程中可以更新UI。（代码中判断的是不是当前线程）</p>
<h3 id="2-2-5-SurfaceView的挖洞原理（显示机制）SurfaceFilnger"><a href="#2-2-5-SurfaceView的挖洞原理（显示机制）SurfaceFilnger" class="headerlink" title="2.2.5 SurfaceView的挖洞原理（显示机制）SurfaceFilnger"></a>2.2.5 SurfaceView的挖洞原理（显示机制）SurfaceFilnger</h3>]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：12.FFmpeg-音乐播放器1</title>
    <url>/83.FFmpeg-%E9%9F%B3%E4%B9%90%E6%92%AD%E6%94%BE%E5%99%A81/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/ffmpeg83">NDKPractice项目的ffmpeg83</a></strong></p>
<p>先来看下流程图：</p>
<p><img src="/images/83.音乐播放器流程图.png" alt=""></p>
<h1 id="1-获取音频-Meta-信息"><a href="#1-获取音频-Meta-信息" class="headerlink" title="1.获取音频 Meta 信息"></a>1.获取音频 Meta 信息</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="function">JNIEXPORT <span class="type">void</span> JNICALL</span></span><br><span class="line"><span class="function"><span class="title">Java_com_east_ffmpeg83_media_JaminPlayer_nPlay</span><span class="params">(JNIEnv *env, jobject instance,</span></span></span><br><span class="line"><span class="params"><span class="function">                                                       jstring url_)</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> *url = env-&gt;<span class="built_in">GetStringUTFChars</span>(url_,<span class="number">0</span>);</span><br><span class="line">    <span class="comment">// 1.初始化所有组件，只有调用了该函数，才能使用复用器和编解码器（源码）</span></span><br><span class="line">    <span class="built_in">av_register_all</span>();</span><br><span class="line">    <span class="comment">// 2.初始化网络</span></span><br><span class="line">    <span class="built_in">avformat_network_init</span>();</span><br><span class="line"></span><br><span class="line">    AVFormatContext *pFormatContext = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="type">int</span> formatOpenInputRes = <span class="number">-1</span>;</span><br><span class="line">    <span class="type">int</span> formatFindStreamInfoRes = <span class="number">-1</span>;</span><br><span class="line">    <span class="type">int</span> audioStreamIndex = <span class="number">-1</span>;</span><br><span class="line">    AVStream *audio_stream;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3.打开输入</span></span><br><span class="line">    formatOpenInputRes = <span class="built_in">avformat_open_input</span>(&amp;pFormatContext,url,<span class="literal">NULL</span>,<span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span>(formatOpenInputRes !=<span class="number">0</span> )&#123;</span><br><span class="line">        <span class="comment">// 第一件事，需要回调给 Java层</span></span><br><span class="line">        <span class="comment">// 第二件 事，需要释放资源</span></span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;format open input error：%s&quot;</span>,<span class="built_in">av_err2str</span>(formatOpenInputRes));</span><br><span class="line">        <span class="comment">// 一般不推荐这么写，但是的确很方便。</span></span><br><span class="line">        <span class="keyword">goto</span> __av_resources_destroy;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4.找出输入流的信息</span></span><br><span class="line">    formatFindStreamInfoRes = <span class="built_in">avformat_find_stream_info</span>(pFormatContext,<span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span>(formatFindStreamInfoRes &lt; <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;format find stream info error: %s&quot;</span>,<span class="built_in">av_err2str</span>(formatFindStreamInfoRes));</span><br><span class="line">        <span class="keyword">goto</span> __av_resources_destroy;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 5.查找音频流的 index</span></span><br><span class="line">    audioStreamIndex = <span class="built_in">av_find_best_stream</span>(pFormatContext,AVMediaType::AVMEDIA_TYPE_AUDIO,<span class="number">-1</span>,<span class="number">-1</span>,<span class="literal">NULL</span>,<span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span>(audioStreamIndex &lt; <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;format audio stream error&quot;</span>);</span><br><span class="line">        <span class="keyword">goto</span> __av_resources_destroy;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 获取采样率和通道</span></span><br><span class="line">    audio_stream = pFormatContext-&gt;streams[audioStreamIndex];</span><br><span class="line">    <span class="built_in">LOGE</span>(<span class="string">&quot;采样率：%d, 通道数: %d&quot;</span>, audio_stream-&gt;codecpar-&gt;sample_rate, audio_stream-&gt;codecpar-&gt;channels);</span><br><span class="line"></span><br><span class="line">    __av_resources_destroy:</span><br><span class="line">    <span class="keyword">if</span>(pFormatContext != <span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="built_in">avformat_close_input</span>(&amp;pFormatContext);</span><br><span class="line">        <span class="built_in">avformat_free_context</span>(pFormatContext);</span><br><span class="line">        pFormatContext = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">avformat_network_deinit</span>();</span><br><span class="line"></span><br><span class="line">    env-&gt;<span class="built_in">ReleaseStringUTFChars</span>(url_,url);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-解码音频数据"><a href="#2-解码音频数据" class="headerlink" title="2.解码音频数据"></a>2.解码音频数据</h1><p>关于解码函数 avcodec_decode_audio4 已经过时了，取而代之的是 avcodec_send_packet 和 avcodec_receive_frame 。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 6.查找解码</span></span><br><span class="line">    pCodecParameters = pFormatContext-&gt;streams[audioStreamIndex]-&gt;codecpar;</span><br><span class="line">    pCodec = <span class="built_in">avcodec_find_decoder</span>(pCodecParameters-&gt;codec_id);</span><br><span class="line">    <span class="keyword">if</span>(pCodec == <span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;codec find audio decoder error&quot;</span>);</span><br><span class="line">        <span class="keyword">goto</span> __av_resources_destroy;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 7.创建一个解码器的上下文</span></span><br><span class="line">    pCodecContext = <span class="built_in">avcodec_alloc_context3</span>(pCodec);</span><br><span class="line">    <span class="keyword">if</span>(pCodecContext == <span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;codec alloc context error&quot;</span>);</span><br><span class="line">        <span class="keyword">goto</span> __av_resources_destroy;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 8.根据参数值填充Codec上下文参数</span></span><br><span class="line">    codecParametersToContextRes = <span class="built_in">avcodec_parameters_to_context</span>(pCodecContext,pCodecParameters);</span><br><span class="line">    <span class="keyword">if</span> (codecParametersToContextRes &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;codec parameters to context error: %s&quot;</span>, <span class="built_in">av_err2str</span>(codecParametersToContextRes));</span><br><span class="line">        <span class="keyword">goto</span> __av_resources_destroy;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 9.打开解码器</span></span><br><span class="line">    codecOpenRes = <span class="built_in">avcodec_open2</span>(pCodecContext,pCodec,<span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span> (codecOpenRes != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;codec audio open error: %s&quot;</span>, <span class="built_in">av_err2str</span>(codecOpenRes));</span><br><span class="line">        <span class="keyword">goto</span> __av_resources_destroy;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    pPacket = <span class="built_in">av_packet_alloc</span>();</span><br><span class="line">    pFrame = <span class="built_in">av_frame_alloc</span>();</span><br><span class="line">    <span class="comment">// 循环从上下文中读取帧到包中</span></span><br><span class="line">    <span class="keyword">while</span>(<span class="built_in">av_read_frame</span>(pFormatContext,pPacket) &gt;= <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(pPacket-&gt;stream_index == audioStreamIndex)&#123;</span><br><span class="line">            <span class="comment">// Packet 包，压缩的数据，解码成 pcm 数据</span></span><br><span class="line">            <span class="type">int</span> codecSendPacketRes = <span class="built_in">avcodec_send_packet</span>(pCodecContext,pPacket);</span><br><span class="line">            <span class="keyword">if</span>(codecSendPacketRes == <span class="number">0</span>)&#123;</span><br><span class="line">                <span class="type">int</span> codecReceiveFrameRes = <span class="built_in">avcodec_receive_frame</span>(pCodecContext,pFrame);</span><br><span class="line">                <span class="keyword">if</span>(codecReceiveFrameRes == <span class="number">0</span>)&#123;</span><br><span class="line">                    <span class="comment">// AVPacket -&gt; AVFrame</span></span><br><span class="line">                    index++;</span><br><span class="line">                    <span class="built_in">LOGE</span>(<span class="string">&quot;解码第 %d 帧&quot;</span>,index);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 解引用</span></span><br><span class="line">        <span class="built_in">av_packet_unref</span>(pPacket);</span><br><span class="line">        <span class="built_in">av_frame_unref</span>(pFrame);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1.解引用数据 data, 2.销魂 pPacket 结构体内存， 3.pPacket = NULL;</span></span><br><span class="line">    <span class="built_in">av_packet_free</span>(&amp;pPacket);</span><br><span class="line">    <span class="built_in">av_frame_free</span>(&amp;pFrame);</span><br><span class="line"></span><br><span class="line">    __av_resources_destroy:</span><br><span class="line">    <span class="keyword">if</span>(pCodecContext != <span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="built_in">avcodec_close</span>(pCodecContext);</span><br><span class="line">        <span class="built_in">avcodec_free_context</span>(&amp;pCodecContext);</span><br><span class="line">        pCodecContext = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(pFormatContext != <span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="built_in">avformat_close_input</span>(&amp;pFormatContext);</span><br><span class="line">        <span class="built_in">avformat_free_context</span>(pFormatContext);</span><br><span class="line">        pFormatContext = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">avformat_network_deinit</span>();</span><br><span class="line"></span><br><span class="line">    env-&gt;<span class="built_in">ReleaseStringUTFChars</span>(url_,url);</span><br></pre></td></tr></table></figure>
<h1 id="3-播放音频"><a href="#3-播放音频" class="headerlink" title="3.播放音频"></a>3.播放音频</h1><p>播放 pcm 数据目前比较流行的有两种方式，一种是通过 Android 的 AudioTrack 来播放，另一种是采用跨平台的 OpenSLES(直接用的底层操作驱动播放) 来播放，<br>个人比较倾向于用更加高效的 <a href="https://github.com/googlesamples/android-ndk/tree/master/native-audio">OpenSLES</a> 来播放音频，大家可以先看看 Google 官方的 native-audio 事例，这里我们先采用 AudioTrack 来播放</p>
<p>AudioTrack 来播：  </p>
<ol>
<li>创建 AudioTack 对象 </li>
<li>启动循环，设置为播放状态 (play)</li>
<li>把数据推到指定数组中 (write)</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">jobject <span class="title">createAudioTrack</span><span class="params">(JNIEnv *env)</span></span>&#123;</span><br><span class="line">    <span class="comment">/*AudioTrack(int streamType, int sampleRateInHz, int channelConfig, int audioFormat,</span></span><br><span class="line"><span class="comment">        int bufferSizeInBytes, int mode)*/</span></span><br><span class="line">    jclass jAudioTrackClass = env-&gt;<span class="built_in">FindClass</span>(<span class="string">&quot;android/media/AudioTrack&quot;</span>);</span><br><span class="line">    jmethodID  jAudioTackCMid = env-&gt;<span class="built_in">GetMethodID</span>(jAudioTrackClass,<span class="string">&quot;&lt;init&gt;&quot;</span>,<span class="string">&quot;(IIIIII)V&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> streamType = <span class="number">3</span>;</span><br><span class="line">    <span class="type">int</span> sampleRateInHz = AUDIO_SAMPLE_RATE;</span><br><span class="line">    <span class="type">int</span> channelConfig = (<span class="number">0x4</span> | <span class="number">0x8</span>);</span><br><span class="line">    <span class="type">int</span> audioFormat = <span class="number">2</span>;</span><br><span class="line">    <span class="type">int</span> mode = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// int getMinBufferSize(int sampleRateInHz, int channelConfig, int audioFormat)</span></span><br><span class="line">    jmethodID  getMinBufferSizeMID = env-&gt;<span class="built_in">GetStaticMethodID</span>(jAudioTrackClass,<span class="string">&quot;getMinBufferSize&quot;</span>,<span class="string">&quot;(III)I&quot;</span>);</span><br><span class="line">    <span class="type">int</span> bufferSizeInBytes = env-&gt;<span class="built_in">CallStaticIntMethod</span>(jAudioTrackClass,getMinBufferSizeMID,sampleRateInHz,</span><br><span class="line">            channelConfig,audioFormat);</span><br><span class="line">    <span class="built_in">LOGE</span>(<span class="string">&quot;bufferSizeInBytes = %d&quot;</span>,bufferSizeInBytes);</span><br><span class="line"></span><br><span class="line">    jobject jAudioTrackObj = env-&gt;<span class="built_in">NewObject</span>(jAudioTrackClass,jAudioTackCMid,streamType,sampleRateInHz,channelConfig,</span><br><span class="line">            audioFormat,bufferSizeInBytes,mode);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// play</span></span><br><span class="line">    jmethodID playMid = env-&gt;<span class="built_in">GetMethodID</span>(jAudioTrackClass,<span class="string">&quot;play&quot;</span>,<span class="string">&quot;()V&quot;</span>);</span><br><span class="line">    env-&gt;<span class="built_in">CallVoidMethod</span>(jAudioTrackObj,playMid);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> jAudioTrackObj;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 10.使用AudioTrack播放</span></span><br><span class="line">        jAudioTrackClass = env-&gt;<span class="built_in">FindClass</span>(<span class="string">&quot;android/media/AudioTrack&quot;</span>);</span><br><span class="line">        <span class="comment">//  public int write(@NonNull byte[] audioData, int offsetInBytes, int sizeInBytes)</span></span><br><span class="line">        jWriteMid = env-&gt;<span class="built_in">GetMethodID</span>(jAudioTrackClass,<span class="string">&quot;write&quot;</span>,<span class="string">&quot;([BII)I&quot;</span>);</span><br><span class="line">        jAudioTrackObj = <span class="built_in">createAudioTrack</span>(env);</span><br><span class="line">    </span><br><span class="line">        pPacket = <span class="built_in">av_packet_alloc</span>();</span><br><span class="line">        pFrame = <span class="built_in">av_frame_alloc</span>();</span><br><span class="line">        <span class="comment">// 循环从上下文中读取帧到包中</span></span><br><span class="line">        <span class="keyword">while</span>(<span class="built_in">av_read_frame</span>(pFormatContext,pPacket) &gt;= <span class="number">0</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span>(pPacket-&gt;stream_index == audioStreamIndex)&#123;</span><br><span class="line">                <span class="comment">// Packet 包，压缩的数据，解码成 pcm 数据</span></span><br><span class="line">                <span class="type">int</span> codecSendPacketRes = <span class="built_in">avcodec_send_packet</span>(pCodecContext,pPacket);</span><br><span class="line">                <span class="keyword">if</span>(codecSendPacketRes == <span class="number">0</span>)&#123;</span><br><span class="line">                    <span class="type">int</span> codecReceiveFrameRes = <span class="built_in">avcodec_receive_frame</span>(pCodecContext,pFrame);</span><br><span class="line">                    <span class="keyword">if</span>(codecReceiveFrameRes == <span class="number">0</span>)&#123;</span><br><span class="line">                        <span class="comment">// AVPacket -&gt; AVFrame</span></span><br><span class="line">                        index++;</span><br><span class="line">                        <span class="built_in">LOGE</span>(<span class="string">&quot;解码第 %d 帧&quot;</span>,index);</span><br><span class="line">    </span><br><span class="line">                        <span class="comment">// write 写到缓冲区 pFrame.data -&gt; javabyte</span></span><br><span class="line">                        <span class="comment">// size 是多大，装 pcm 的数据</span></span><br><span class="line">                        <span class="comment">// 1s 44100 点，2通道， 2字节 44100*2*2</span></span><br><span class="line">                        <span class="comment">// 1帧不是一秒，pFrame-&gt;nb_samples点</span></span><br><span class="line">                        <span class="type">int</span> dataSize = <span class="built_in">av_samples_get_buffer_size</span>(<span class="literal">NULL</span>,pFrame-&gt;channels,</span><br><span class="line">                                                                  pFrame-&gt;nb_samples,pCodecContext-&gt;sample_fmt,<span class="number">0</span>);</span><br><span class="line">                        jbyteArray jPcmByteArray = env-&gt;<span class="built_in">NewByteArray</span>(dataSize);</span><br><span class="line">                        <span class="comment">// native 创建 c 数组</span></span><br><span class="line">                        jbyte *jPcmData= env-&gt;<span class="built_in">GetByteArrayElements</span>(jPcmByteArray,<span class="literal">NULL</span>);</span><br><span class="line">                        <span class="built_in">memcpy</span>(jPcmData,pFrame-&gt;data,dataSize);</span><br><span class="line">                        <span class="comment">// 0 把 c 的数组的数据同步到 jbyteArray,然后释放native数组</span></span><br><span class="line">                        env-&gt;<span class="built_in">ReleaseByteArrayElements</span>(jPcmByteArray,jPcmData,<span class="number">0</span>);</span><br><span class="line">                        env-&gt;<span class="built_in">CallIntMethod</span>(jAudioTrackObj,jWriteMid,jPcmByteArray,<span class="number">0</span>,dataSize);</span><br><span class="line">                        <span class="comment">// 解除 jPcmDataArray 的持有，让 javaGC 回收</span></span><br><span class="line">                        env-&gt;<span class="built_in">DeleteLocalRef</span>(jPcmByteArray);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 解引用</span></span><br><span class="line">            <span class="built_in">av_packet_unref</span>(pPacket);</span><br><span class="line">            <span class="built_in">av_frame_unref</span>(pFrame);</span><br><span class="line">        &#125;</span><br><span class="line">    </span><br><span class="line">        <span class="comment">// 1.解引用数据 data, 2.销魂 pPacket 结构体内存， 3.pPacket = NULL;</span></span><br><span class="line">        <span class="built_in">av_packet_free</span>(&amp;pPacket);</span><br><span class="line">        <span class="built_in">av_frame_free</span>(&amp;pFrame);</span><br><span class="line">        env-&gt;<span class="built_in">DeleteLocalRef</span>(jAudioTrackObj);</span><br><span class="line">    </span><br><span class="line">        __av_resources_destroy:</span><br><span class="line">        <span class="keyword">if</span>(pCodecContext != <span class="literal">NULL</span>)&#123;</span><br><span class="line">            <span class="built_in">avcodec_close</span>(pCodecContext);</span><br><span class="line">            <span class="built_in">avcodec_free_context</span>(&amp;pCodecContext);</span><br><span class="line">            pCodecContext = <span class="literal">NULL</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    </span><br><span class="line">        <span class="keyword">if</span>(pFormatContext != <span class="literal">NULL</span>)&#123;</span><br><span class="line">            <span class="built_in">avformat_close_input</span>(&amp;pFormatContext);</span><br><span class="line">            <span class="built_in">avformat_free_context</span>(pFormatContext);</span><br><span class="line">            pFormatContext = <span class="literal">NULL</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">avformat_network_deinit</span>();</span><br><span class="line">    </span><br><span class="line">        env-&gt;<span class="built_in">ReleaseStringUTFChars</span>(url_,url);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这块播放还是有杂音，而且内存持续上涨，下节我们通过多线程播放来解决</p>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：16.FFmpeg-视频播放器2</title>
    <url>/87.FFmpeg-%E8%A7%86%E9%A2%91%E6%92%AD%E6%94%BE%E5%99%A82/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/ffmpeg86">NDKPractice项目的ffmpeg87</a></strong></p>
<h1 id="1-调整音乐播放器项目架构"><a href="#1-调整音乐播放器项目架构" class="headerlink" title="1.调整音乐播放器项目架构"></a>1.调整音乐播放器项目架构</h1><p>解码的流程 Video 和 Audio 很多相同的，所以抽离出来了公共类: Media.cpp</p>
<p>音频和视频单独处理成：Audio.cpp 和 Video.cpp，都继承自Media,cpp</p>
<p>遇到的问题：<font color = red>下面这个方法的env 不能使用 pJniCall-&gt;jniEnv</font></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">Video::setSurface</span><span class="params">(JNIEnv *env, jobject surface)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 这里不能使用pJniCall-&gt;jniEnv-&gt;的env，因为和native方法传递过来的不一样，而且不相等</span></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  注释的这行代码会报错</span></span><br><span class="line"><span class="comment">     *   &#x27;JNI DETECTED ERROR IN APPLICATION: invalid address...,thread Thread[21,tid=25717,Native,Thread*=0xc2e4a000,peer=0x12e40000,&quot;Thread-3&quot;] using JNIEnv* from thread Thread[1,tid=25650,Runnable,Thread*=0xf1b71e00,peer=0x757719a0,&quot;main&quot;]</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"><span class="comment">//    this-&gt;surface = pJniCall-&gt;jniEnv-&gt;NewGlobalRef(surface);</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;videoJNIEnv = env;</span><br><span class="line">    <span class="keyword">this</span>-&gt;surface = env-&gt;<span class="built_in">NewGlobalRef</span>(surface);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-视音频的同步"><a href="#2-视音频的同步" class="headerlink" title="2. 视音频的同步"></a>2. 视音频的同步</h1><p><img src="/images/87.视音频同步.png" alt=""></p>
<ul>
<li>pts：显示时间戳，用来告诉播放器该在什么时候显示这一帧数据。单位是不确定，所以使用时间基来获取单位</li>
<li>time_base：时间度量单位（时间基）。</li>
</ul>
<p>有三种办法：</p>
<ol>
<li>参考一个外部时钟，将音频与视频同步至此时间。</li>
<li>以视频为基准，音频去同步视频的时间。</li>
<li>以音频为基准，视频去同步音频的时间。</li>
</ol>
<p><code>由于某些生物学的原理，人对声音的变化比较敏感，但是对视觉变化不太敏感。所以采用方案 3</code></p>
<ul>
<li><code>static inline double av_q2d(AVRational a)</code>：使用时间基获取单位</li>
<li><code>timeBase = pFormatContext-&gt;streams[streamIndex]-&gt;time_base;</code>：获取时间基</li>
<li><code>duration = pFormatContext-&gt;duration;</code>：获取音视频时长（<code>单位微秒</code>）</li>
<li><code>double times = av_frame_get_best_effort_timestamp(pFrame) * av_q2d(timeBase);</code>: 获取当前时间</li>
<li><code>double times = pPacket-&gt;pts * av_q2d(timeBase);</code>: 获取当前时间</li>
<li><code>av_usleep(frameSleepTime * 1000000);</code>：休眠（<code>单位毫秒</code>）</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"> <span class="comment">// 播放之前判断一下要休眠多久</span></span><br><span class="line"><span class="type">double</span> frameSleepTime = pVideo-&gt;<span class="built_in">getFrameSleepTime</span>(pPacket,pFrame); <span class="comment">// 这是秒</span></span><br><span class="line"><span class="built_in">av_usleep</span>(frameSleepTime * <span class="number">1000000</span>);<span class="comment">// 这是毫秒</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">Video::getFrameSleepTime</span><span class="params">(AVPacket *pPacket,AVFrame *pFrame)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 这两种方法都能算出当前显示帧的时间</span></span><br><span class="line"><span class="comment">//    double times = av_frame_get_best_effort_timestamp(pFrame) * av_q2d(timeBase);</span></span><br><span class="line">    <span class="type">double</span> times = pPacket-&gt;pts * <span class="built_in">av_q2d</span>(timeBase);</span><br><span class="line">    <span class="keyword">if</span> (times &gt; currentTime) &#123;</span><br><span class="line">        currentTime = times;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 相差多少秒</span></span><br><span class="line">    <span class="type">double</span> diffTime = pAudio-&gt;currentTime - currentTime; <span class="comment">// 负数，音频比视频慢</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 视频快了就慢点，视频慢了就快点</span></span><br><span class="line">    <span class="comment">// 但是尽量把事件控制在视频的帧率时间范围左右  1/24 0.04 1/30 0.033</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 第一次控制 0.016s 到 -0.016s</span></span><br><span class="line">    <span class="keyword">if</span> (diffTime &gt; <span class="number">0.016</span> || diffTime &lt; <span class="number">-0.016</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (diffTime &gt; <span class="number">0.016</span>) &#123;</span><br><span class="line">            delayTime = delayTime * <span class="number">2</span> / <span class="number">3</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (diffTime &lt; <span class="number">-0.016</span>) &#123;</span><br><span class="line">            delayTime = delayTime * <span class="number">3</span> / <span class="number">2</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 第二次控制 defaultDelayTIme * 2 / 3 到 defaultDelayTime * 3 / 2</span></span><br><span class="line">        <span class="keyword">if</span> (delayTime &lt; defaultDelayTime / <span class="number">2</span>) &#123;</span><br><span class="line">            delayTime = defaultDelayTime * <span class="number">2</span> / <span class="number">3</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (delayTime &gt; defaultDelayTime * <span class="number">2</span>) &#123;</span><br><span class="line">            delayTime = defaultDelayTime * <span class="number">3</span> / <span class="number">2</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 第三次控制，这基本是异常情况</span></span><br><span class="line">    <span class="keyword">if</span> (diffTime &gt;= <span class="number">0.25</span>)</span><br><span class="line">        delayTime = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (diffTime &lt;= <span class="number">-0.25</span>)</span><br><span class="line">        delayTime = defaultDelayTime * <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">LOGE</span>(<span class="string">&quot;diffTime = %lf秒,delayTime = %lf秒,\n&quot;</span></span><br><span class="line">         <span class="string">&quot;pAudio-&gt;currentTime = %lf秒,cureentTime = %lf秒,duration = %d秒&quot;</span>,diffTime,delayTime,pAudio-&gt;currentTime,currentTime,duration/<span class="number">1000</span>/<span class="number">1000</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> delayTime;</span><br><span class="line"><span class="comment">//    return -diffTime;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>还差：暂停，停止，回调，倍速播放，硬解码</p>
]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：17.FFmpeg-直播推、拉流</title>
    <url>/88.FFmpeg-%E7%9B%B4%E6%92%AD%E6%8E%A8%E3%80%81%E6%8B%89%E6%B5%81/</url>
    <content><![CDATA[<font color = red> 以下使用&nbsp;&nbsp;&nbsp;&nbsp;Ubuntu&nbsp;&nbsp;&nbsp;&nbsp;环境搭建的</font>

<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/ffmpeg88livepush">NDKPractice项目的ffmpeg88livepush</a></strong></p>
<h1 id="1-搭建流媒体服务器"><a href="#1-搭建流媒体服务器" class="headerlink" title="1.搭建流媒体服务器"></a>1.搭建流媒体服务器</h1><h2 id="1-1-安装"><a href="#1-1-安装" class="headerlink" title="1.1 安装"></a>1.1 安装</h2><ol>
<li><p>在ubuntu系统中首先切换到<code>root</code>用户,然后cd到<code>lib</code>目录下，<code>mkdir新建nginx</code>文件夹。</p>
</li>
<li><p>使用 <code>wget ftp://ftp.pcre.org/pub/pcre/pcre-8.38.zip</code> 下载 <code>pcre-8.38.zip版本</code>，也可以直接下载最新的版本.  </p>
<ul>
<li>下载下来后使用<code>chmod u+x pcre-8.38.zip</code>修改权限,<code>unzip pcre-8.38.zip</code>解压然后进入解压的文件夹中。</li>
<li>使用<code>./configure</code>运行会生成CMakeLists.txt  </li>
<li>接着使用<code>make</code>指令执行.    </li>
<li>最后使用<code>make install</code>指令执行.  </li>
</ul>
</li>
<li><p>下载<code>zlib</code>,点击 <a href="https://github.com/madler/zlib/releases">链接</a> 右键点击最新版本的<code>tar.gz</code>复制链接地址。</p>
<ul>
<li>返回nginx目录下使用 <code>wget https://github.com/madler/zlib/archive/v1.2.11.tar.gz</code> 下载。</li>
<li>下载下来后使用<code>chmod u+x v1.2.11.tar.gz</code>修改权限,<code>tar -zxvf v1.2.11.tar.gz</code>解压然后进入解压的文件夹中。</li>
<li>使用<code>./configure</code>运行会生成CMakeLists.txt  </li>
<li>接着使用<code>make</code>指令执行.    </li>
<li>最后使用<code>make install</code>指令执行.  </li>
</ul>
</li>
<li><p>下载<code>rtmp</code>,点击 <a href="https://github.com/arut/nginx-rtmp-module/releases">链接</a> 右键点击最新版本的<code>tar.gz</code>复制链接地址。</p>
<ul>
<li>返回nginx目录下使用 <code>wget https://github.com/arut/nginx-rtmp-module/archive/v1.2.1.tar.gz</code> 下载。</li>
<li>下载下来后使用<code>chmod u+x v1.2.1.tar.gz</code>修改权限,<code>tar -zxvf v1.2.1.tar.gz</code>解压。这个不需要安装。</li>
</ul>
</li>
<li><p>下载<code>openssl</code>,点击 <a href="https://github.com/openssl/openssl/releases">链接</a> 右键点击最新版本的<code>tar.gz</code>复制链接地址。</p>
<ul>
<li>返回nginx目录下使用 <code>wget https://github.com/openssl/openssl/archive/OpenSSL_1_1_1c.tar.gz</code> 下载。</li>
<li>下载下来后使用<code>chmod u+x OpenSSL_1_1_1c.tar.gz</code>修改权限,<code>tar -zxvf OpenSSL_1_1_1c.tar.gz</code>解压然后进入解压的文件夹中。</li>
<li>使用 <code>./config --prefix=</code>&#96;<code>pwd</code>&#96;<code>/libs</code> 运行会生成CMakeLists.txt文件，&#96;pwd&#96;指令代表当前目录文件</li>
<li>接着使用<code>make</code>指令执行.    </li>
<li>最后使用<code>make install</code>指令执行.  </li>
</ul>
</li>
<li><p>下载<code>nginx</code>,<font color=red>必须依赖前面安装的所有东西</font> 点击 <a href="https://github.com/nginx/nginx/releases">链接</a> 右键点击最新版本的<code>tar.gz</code>复制链接地址。</p>
<ul>
<li>返回nginx目录下使用 <code>wget https://github.com/nginx/nginx/archive/release-1.17.1.tar.gz</code> 下载。</li>
<li>下载下来后使用<code>chmod u+x release-1.17.1.tar.gz</code>修改权限,<code>tar -zxvf release-1.17.1.tar.gz</code>解压然后进入解压的文件夹中。</li>
<li>使用 <code>./auto/configure --add-module=/lib/nginx/nginx-rtmp-module-1.2.1 --with-cc-opt=&quot;-Wimplicit-fallthrough=0&quot; --with-openssl=/lib/nginx/openssl-OpenSSL_1_1_1c</code></li>
<li>接着使用<code>make</code>指令执行.    </li>
<li>最后使用<code>make install</code>指令执行.  </li>
</ul>
</li>
</ol>
<ul>
<li>nginx:  <a href="https://github.com/nginx/nginx/releases">https://github.com/nginx/nginx/releases</a></li>
<li>rtmp:  <a href="https://github.com/arut/nginx-rtmp-module/releases">https://github.com/arut/nginx-rtmp-module/releases</a></li>
<li>pcre:  ftp://ftp.pcre.org/pub/pcre/ 下载最新版本，这边下载 pcre-8.38.zip</li>
<li>zlib:  <a href="https://github.com/madler/zlib/releases">https://github.com/madler/zlib/releases</a></li>
<li>openssl:  <a href="https://github.com/openssl/openssl">https://github.com/openssl/openssl</a></li>
</ul>
<p><strong>出现的问题</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">checking for OS</span><br><span class="line"> + Linux 5.4.0-48-generic x86_64</span><br><span class="line">checking for C compiler ... not found</span><br><span class="line"></span><br><span class="line">./auto/configure: error: C compiler cc is not found</span><br><span class="line"></span><br><span class="line">解决：sudo apt-get install gcc</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">nginx 执行make时出现的问题</span><br><span class="line">......</span><br><span class="line"></span><br><span class="line">../nginx-rtmp-module-master/ngx_rtmp_eval.c: In function ‘ngx_rtmp_eval’:</span><br><span class="line">../nginx-rtmp-module-master/ngx_rtmp_eval.c:160:17: error: this statement may fall through [-Werror=implicit-fallthrough=]</span><br><span class="line">                 switch (c) &#123;</span><br><span class="line">                 ^~~~~~</span><br><span class="line">../nginx-rtmp-module-master/ngx_rtmp_eval.c:170:13: note: here</span><br><span class="line">             case ESCAPE:</span><br><span class="line">             ^~~~</span><br><span class="line">cc1: all warnings being treated as errors</span><br><span class="line">make[1]: *** [objs/Makefile:1349: objs/addon/nginx-rtmp-module-master/ngx_rtmp_eval.o] Error 1</span><br><span class="line">make[1]: Leaving directory &#x27;/home/pi/nginx-1.19.0&#x27;</span><br><span class="line">make: *** [Makefile:8: build] Error 2</span><br><span class="line"></span><br><span class="line">解决：</span><br><span class="line"></span><br><span class="line">禁用gcc中的“ fallthrough”，在配置时添加选项</span><br><span class="line">./auto/configure --add-module=/lib/nginx/nginx-rtmp-module-1.2.1 --with-cc-opt=&quot;-Wimplicit-fallthrough=0&quot; --with-openssl=/lib/nginx/openssl-OpenSSL_1_1_1c</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h2 id="1-2-配置"><a href="#1-2-配置" class="headerlink" title="1.2 配置"></a>1.2 配置</h2><p>执行命令<code>cp /lib/nginx/nginx-rtmp-module-1.2.1/test/nginx.conf /usr/local/nginx/conf/</code>  </p>
<p>替换<code>nginx.cong</code>。具体的可参考<a href="https://github.com/arut/nginx-rtmp-module">链接</a>中的<code>Example nginx.conf</code></p>
<p>类似下面这种：<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">orker_processes  1;</span><br><span class="line"></span><br><span class="line">error_log  logs/error.log debug;</span><br><span class="line"></span><br><span class="line">events &#123;</span><br><span class="line">    worker_connections  1024;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">rtmp &#123;</span><br><span class="line">    server &#123;</span><br><span class="line">        listen 1935;</span><br><span class="line"></span><br><span class="line">        application myapp &#123;</span><br><span class="line">            live on;</span><br><span class="line"></span><br><span class="line">            #record keyframes;</span><br><span class="line">            #record_path /tmp;</span><br><span class="line">            #record_max_size 128K;</span><br><span class="line">            #record_interval 30s;</span><br><span class="line">            #record_suffix .this.is.flv;</span><br><span class="line"></span><br><span class="line">....</span><br></pre></td></tr></table></figure></p>
<h2 id="1-3-启动和停止"><a href="#1-3-启动和停止" class="headerlink" title="1.3 启动和停止"></a>1.3 启动和停止</h2><ul>
<li>启动：<code>./nginx</code></li>
<li>停止：<code>./nginx -s stop</code></li>
</ul>
<p>常见问题：</p>
<ol>
<li>端口被占用（默认8080端口）</li>
<li><p>启动错误：比如 xxx.so 找不到。</p>
<p> 解决：<br> 使用<code>ldd $(which ./nginx)</code>查看依赖包。<br> 调用<code>export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH</code>  </p>
</li>
</ol>
<h2 id="1-4-测试"><a href="#1-4-测试" class="headerlink" title="1.4 测试"></a>1.4 测试</h2><p>windows ffmpeg<a href="https://github.com/BtbN/FFmpeg-Builds/releases">下载地址</a></p>
<p>mac 和 linux 类似，进入<a href="http://www.ffmpeg.org/download.html">官网下载</a> 。</p>
<p>下面以windows为例测试步骤：</p>
<ol>
<li>进入下载好的ffmpeg解压目录的/bin目录文件夹下</li>
<li>使用命令推流：<code>.\ffmpeg.exe -re -i 等风来不如追风去.mp4 -vcodec libx264 -acodec aac -f flv rtmp://192.168.1.20/myapp/mystream</code></li>
<li>使用命令拉流：<code>ffplay.exe rtmp://192.168.1.20/myapp/mystream</code></li>
</ol>
<p>推流命令说明：</p>
<ul>
<li><code>等风来不如追风去.mp4</code>：是要推流的视频绝对路径。</li>
<li><code>rtmp://192.168.1.20/myapp/mystream</code>：要推流的地址。192.168.1.20为公网地址或局域网的地址。</li>
</ul>
<h1 id="2-集成-RTMP-推流源码"><a href="#2-集成-RTMP-推流源码" class="headerlink" title="2.集成 RTMP 推流源码"></a>2.集成 RTMP 推流源码</h1><p><img src="/images/88.直播推拉流原理.png" alt="">  </p>
<p><img src="/images/91.直播推流原理.png" alt=""></p>
<p>采用一种 socket 通讯方案 <a href="http://rtmpdump.mplayerhq.hu/">RTMPDump</a></p>
<ol>
<li>git clone 上面网站中的地址 <code>git clone git://git.ffmpeg.org/rtmpdump</code></li>
<li>将clone下来的文件夹中的librtmp目录中的所有<code>*.c</code>和<code>*.h</code>文件拷贝到工程目录中开始开发。</li>
<li>如果出现下面的错误就在CMakeLists.txt 中添加 <code>set(CMAKE_C_FLAGS &quot;$&#123;CMAKE_C_FLAGS&#125; -DNO_CRYPTO&quot;)</code></li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">****\src\main\cpp\rtmp\hashswf.c:56:10: fatal error: &#x27;openssl/ssl.h&#x27; file not found</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> *<span class="title">threadInitConnect</span><span class="params">(<span class="type">void</span> *args)</span> </span>&#123;</span><br><span class="line">    LivePush *livePush = (LivePush *) args;</span><br><span class="line">    <span class="comment">// 1.创建RTMP</span></span><br><span class="line">    livePush-&gt;pRtmp = <span class="built_in">RTMP_Alloc</span>();</span><br><span class="line">    <span class="comment">// 2. 初始化</span></span><br><span class="line">    <span class="built_in">RTMP_Init</span>(livePush-&gt;pRtmp);</span><br><span class="line">    <span class="comment">// 3. 设置参数</span></span><br><span class="line">    livePush-&gt;pRtmp-&gt;Link.timeout = <span class="number">10</span>; <span class="comment">// 超时时间 10 秒</span></span><br><span class="line">    livePush-&gt;pRtmp-&gt;Link.lFlags = RTMP_LF_LIVE; <span class="comment">// 长连接</span></span><br><span class="line">    <span class="built_in">RTMP_SetupURL</span>(livePush-&gt;pRtmp, livePush-&gt;liveUrl);<span class="comment">// 设置推流的地址</span></span><br><span class="line">    <span class="built_in">RTMP_EnableWrite</span>(livePush-&gt;pRtmp);<span class="comment">// 可写</span></span><br><span class="line">    <span class="comment">// 4. 开始连接</span></span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">RTMP_Connect</span>(livePush-&gt;pRtmp, <span class="literal">nullptr</span>)) &#123;</span><br><span class="line">        <span class="comment">// 回调到java层</span></span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;rtmp connect error&quot;</span>);</span><br><span class="line">        livePush-&gt;pJniCall-&gt;<span class="built_in">callConnectError</span>(THREAD_CHILD, INIT_RTMP_CONNECT_ERROR_CODE,</span><br><span class="line">                                             <span class="string">&quot;rtmp connect error&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> (<span class="type">void</span> *)(INIT_RTMP_CONNECT_ERROR_CODE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">RTMP_ConnectStream</span>(livePush-&gt;pRtmp, <span class="number">0</span>)) &#123;</span><br><span class="line">        <span class="comment">// 回调到java层</span></span><br><span class="line">        <span class="built_in">LOGE</span>(<span class="string">&quot;rtmp connect stream error&quot;</span>);</span><br><span class="line">        livePush-&gt;pJniCall-&gt;<span class="built_in">callConnectError</span>(THREAD_CHILD, INIT_RTMP_CONNECT_STREAM_ERROR_CODE,</span><br><span class="line">                                             <span class="string">&quot;rtmp connect stream error&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> (<span class="type">void</span> *)(INIT_RTMP_CONNECT_STREAM_ERROR_CODE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">LOGE</span>(<span class="string">&quot;rtmp connect success&quot;</span>);</span><br><span class="line">    livePush-&gt;pJniCall-&gt;<span class="built_in">callConnectSuccess</span>(THREAD_CHILD);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：20.FFmpeg-直播推流到服务器、加滤镜效果</title>
    <url>/91.FFmpeg-%E7%9B%B4%E6%92%AD%E6%8E%A8%E6%B5%81%E5%88%B0%E6%9C%8D%E5%8A%A1%E5%99%A8%E3%80%81%E5%8A%A0%E6%BB%A4%E9%95%9C%E6%95%88%E6%9E%9C/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/ffmpeg88livepush">NDKPractice项目的ffmpeg88livepush</a></strong></p>
<h1 id="ffmpeg测试流媒体服务器播放地址"><a href="#ffmpeg测试流媒体服务器播放地址" class="headerlink" title="ffmpeg测试流媒体服务器播放地址"></a>ffmpeg测试流媒体服务器播放地址</h1><p>windows ffmpeg<a href="https://github.com/BtbN/FFmpeg-Builds/releases">下载地址</a></p>
<p>mac 和 linux 类似，进入<a href="http://www.ffmpeg.org/download.html">官网下载</a> 。</p>
<p>下面以windows为例测试步骤：</p>
<ol>
<li>进入下载好的ffmpeg解压目录的/bin目录文件夹下</li>
<li>使用命令拉流：<code>ffplay.exe rtmp://192.168.1.20/myapp/mystream</code></li>
</ol>
<h1 id="1-录制声音获取-PCM-数据"><a href="#1-录制声音获取-PCM-数据" class="headerlink" title="1.录制声音获取 PCM 数据"></a>1.录制声音获取 PCM 数据</h1><p>使用<code>AudioRecord</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 音频的采集线程</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">AudioRecordThread</span> <span class="keyword">extends</span> <span class="title class_">Thread</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> mMinBufferSize;</span><br><span class="line">    <span class="keyword">private</span> WeakReference&lt;BaseVideoPush&gt; mVideoRecorderWr;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">boolean</span> <span class="variable">mShouldExit</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">private</span> MediaCodec mAudioCodec;</span><br><span class="line">    <span class="keyword">private</span> MediaCodec.BufferInfo mBufferInfo;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">mAudioTrackIndex</span> <span class="operator">=</span> -<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">long</span> <span class="variable">mAudioPts</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> AudioRecord mAudioRecord; <span class="comment">//录音的类</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">byte</span>[] mAudioData;<span class="comment">// 这是 pcm 数据</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">AudioRecordThread</span><span class="params">(WeakReference&lt;BaseVideoPush&gt; videoRecorderWr)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.mVideoRecorderWr = videoRecorderWr;</span><br><span class="line">        mAudioCodec = videoRecorderWr.get().mAudioCodec;</span><br><span class="line">        mBufferInfo = <span class="keyword">new</span> <span class="title class_">MediaCodec</span>.BufferInfo();</span><br><span class="line"></span><br><span class="line">        mMinBufferSize = AudioRecord.getMinBufferSize(</span><br><span class="line">                BaseVideoPush.AUDIO_SAMPLE_RATE,</span><br><span class="line">                AudioFormat.CHANNEL_IN_STEREO,<span class="comment">//立体声</span></span><br><span class="line">                AudioFormat.ENCODING_PCM_16BIT);</span><br><span class="line"></span><br><span class="line">        mAudioRecord = <span class="keyword">new</span> <span class="title class_">AudioRecord</span>(MediaRecorder.AudioSource.MIC,</span><br><span class="line">                BaseVideoPush.AUDIO_SAMPLE_RATE,</span><br><span class="line">                AudioFormat.CHANNEL_IN_STEREO,<span class="comment">//立体声</span></span><br><span class="line">                AudioFormat.ENCODING_PCM_16BIT,</span><br><span class="line">                mMinBufferSize);</span><br><span class="line"></span><br><span class="line">        mAudioData = <span class="keyword">new</span> <span class="title class_">byte</span>[mMinBufferSize];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 开启录制</span></span><br><span class="line">            mAudioRecord.startRecording();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (mShouldExit) &#123;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 不断读取 pcm 数据</span></span><br><span class="line">                mAudioRecord.read(mAudioData, <span class="number">0</span>, mMinBufferSize);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 把数据写入到 mAudioCodec 的 InputBuffer</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">inputBufferIndex</span> <span class="operator">=</span> mAudioCodec.dequeueInputBuffer(<span class="number">0</span>);</span><br><span class="line">                <span class="keyword">if</span> (inputBufferIndex &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="type">ByteBuffer</span> <span class="variable">byteBuffer</span> <span class="operator">=</span> mAudioCodec.getInputBuffers()[inputBufferIndex];</span><br><span class="line">                    byteBuffer.clear();</span><br><span class="line">                    byteBuffer.put(mAudioData);</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// pts  44100 * 2 *2</span></span><br><span class="line">                    mAudioPts += mMinBufferSize * <span class="number">1000000</span> / BaseVideoPush.AUDIO_SAMPLE_RATE * BaseVideoPush.AUDIO_CHANNELS * <span class="number">2</span>;</span><br><span class="line">                    <span class="comment">// size 22050*2*2</span></span><br><span class="line">                    mAudioCodec.queueInputBuffer(inputBufferIndex, <span class="number">0</span>, mMinBufferSize, mAudioPts, <span class="number">0</span>);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            onDestroy();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mAudioCodec.stop();</span><br><span class="line">            mAudioCodec.release();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">requestExit</span><span class="params">()</span> &#123;</span><br><span class="line">        mShouldExit = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-推送视频画面到媒体房间"><a href="#2-推送视频画面到媒体房间" class="headerlink" title="2.推送视频画面到媒体房间"></a>2.推送视频画面到媒体房间</h1><p><img src="/images/91.直播推流原理.png" alt=""></p>
<p>为什么要封装成<code>flv</code>：为了不定死 <code>h264</code>协议</p>
<p>flv 封装格式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// FLV 的封装格式</span><br><span class="line">// frame type : 1关键帧，2 非关键帧（4Bit）</span><br><span class="line">// CodecID : 7表示 AVC(4bit) , 与 frame type组合起来刚好是 1 个字节 0x17</span><br><span class="line">// fixed ： 0x00 0x00 0x00 0x00 (4byte)</span><br><span class="line">// configurationVersion  (1byte) 0x01版本</span><br><span class="line">// AVCProfileIndication  (1byte) sps[1] profile</span><br><span class="line">// profile_compatibility (1byte) sps[2] compatibility</span><br><span class="line">// AVCLevelIndication    (1byte) sps[3] Profile level</span><br><span class="line">// lengthSizeMinusOne    (1byte) 0xff   包长数据所使用的字节数</span><br><span class="line"></span><br><span class="line">// sps + pps 的数据</span><br><span class="line">// sps number            (1byte) 0xe1   sps 个数</span><br><span class="line">// sps data length       (2byte) sps 长度</span><br><span class="line">// sps data                      sps 的内容</span><br><span class="line">// pps number            (1byte) 0x01 pps 个数</span><br><span class="line">// pps data length       (2byte) pps 长度</span><br><span class="line">// pps data                      pps 的内容</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">BaseVideoPush：VideoEncoderThread</span><br><span class="line"></span><br><span class="line"> private static final class VideoEncoderThread extends Thread &#123;</span><br><span class="line">        private WeakReference&lt;BaseVideoPush&gt; mVideoRecorderWr;</span><br><span class="line">        private volatile boolean mShouldExit = false;</span><br><span class="line">        private MediaCodec mVideoCodec;</span><br><span class="line">        private MediaCodec.BufferInfo mBufferInfo;</span><br><span class="line">        private long mVideoPts = 0;</span><br><span class="line">        private byte[] mVideoSps, mVideoPps;</span><br><span class="line"></span><br><span class="line">        public VideoEncoderThread(WeakReference&lt;BaseVideoPush&gt; videoRecorderWr) &#123;</span><br><span class="line">            this.mVideoRecorderWr = videoRecorderWr;</span><br><span class="line">            mVideoCodec = videoRecorderWr.get().mVideoCodec;</span><br><span class="line">            mBufferInfo = new MediaCodec.BufferInfo();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        @Override</span><br><span class="line">        public void run() &#123;</span><br><span class="line">            try &#123;</span><br><span class="line">                mVideoCodec.start();</span><br><span class="line"></span><br><span class="line">                while (true) &#123;</span><br><span class="line">                    if (mShouldExit) &#123;</span><br><span class="line">                        return;</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    BaseVideoPush videoRecorder = mVideoRecorderWr.get();</span><br><span class="line">                    if (videoRecorder == null) &#123;</span><br><span class="line">                        return;</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    // 代码先不写，先测试，从 surface 上获取数据，编码成 h264 ,通过 MediaMuxer 合成 mp4</span><br><span class="line">                    int outputBufferIndex = mVideoCodec.dequeueOutputBuffer(mBufferInfo, 0);</span><br><span class="line">                    if (outputBufferIndex == MediaCodec.INFO_OUTPUT_FORMAT_CHANGED) &#123;</span><br><span class="line"></span><br><span class="line">                        // 获取 sps 和 pps</span><br><span class="line">//                        Log.e(&quot;TAG&quot;, &quot;获取 sps 和 pps&quot;);</span><br><span class="line">                        ByteBuffer byteBuffer = mVideoCodec.getOutputFormat().getByteBuffer(&quot;csd-0&quot;);</span><br><span class="line">                        mVideoSps = new byte[byteBuffer.remaining()];</span><br><span class="line">                        byteBuffer.get(mVideoSps, 0, mVideoSps.length);</span><br><span class="line"></span><br><span class="line">//                        Log.e(&quot;sps&quot;, bytesToHexString(mVideoSps));</span><br><span class="line"></span><br><span class="line">                        byteBuffer = mVideoCodec.getOutputFormat().getByteBuffer(&quot;csd-1&quot;);</span><br><span class="line">                        mVideoPps = new byte[byteBuffer.remaining()];</span><br><span class="line">                        byteBuffer.get(mVideoPps, 0, mVideoPps.length);</span><br><span class="line"></span><br><span class="line">//                        Log.e(&quot;pps&quot;, bytesToHexString(mVideoPps));</span><br><span class="line">                    &#125; else &#123;</span><br><span class="line">                        while (outputBufferIndex &gt;= 0) &#123;</span><br><span class="line">                            // 获取数据</span><br><span class="line">                            ByteBuffer outBuffer = mVideoCodec.getOutputBuffers()[outputBufferIndex];</span><br><span class="line">                            outBuffer.position(mBufferInfo.offset);</span><br><span class="line">                            outBuffer.limit(mBufferInfo.offset + mBufferInfo.size);</span><br><span class="line"></span><br><span class="line">                            // 修改 pts</span><br><span class="line">                            if (mVideoPts == 0) &#123;</span><br><span class="line">                                mVideoPts = mBufferInfo.presentationTimeUs;</span><br><span class="line">                            &#125;</span><br><span class="line">                            mBufferInfo.presentationTimeUs -= mVideoPts;</span><br><span class="line"></span><br><span class="line">                            // 在关键帧之前先把 sps 和 pps 推到流媒体服务器</span><br><span class="line">                            if(mBufferInfo.flags == MediaCodec.BUFFER_FLAG_KEY_FRAME)&#123;</span><br><span class="line">                                mVideoRecorderWr.get().mLivePush.pushSpsPps(mVideoSps,mVideoSps.length,</span><br><span class="line">                                        mVideoPps,mVideoPps.length);</span><br><span class="line">                            &#125;</span><br><span class="line"></span><br><span class="line">                            byte[] data = new byte[outBuffer.remaining()];</span><br><span class="line">                            outBuffer.get(data, 0, data.length);</span><br><span class="line">                            // 推送视频到流媒体服务器上</span><br><span class="line">                            mVideoRecorderWr.get().mLivePush.pushVideo(data,data.length,</span><br><span class="line">                                    mBufferInfo.flags == MediaCodec.BUFFER_FLAG_KEY_FRAME);</span><br><span class="line"></span><br><span class="line">                            // 回调当前录制的时间</span><br><span class="line">                            if (videoRecorder.mRecordListener != null) &#123;</span><br><span class="line">                                videoRecorder.mRecordListener.onTime(mBufferInfo.presentationTimeUs / 1000);</span><br><span class="line">                            &#125;</span><br><span class="line"></span><br><span class="line">                            mVideoCodec.releaseOutputBuffer(outputBufferIndex, false);</span><br><span class="line">                            outputBufferIndex = mVideoCodec.dequeueOutputBuffer(mBufferInfo, 0);</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; catch (Exception e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125; finally &#123;</span><br><span class="line">                onDestroy();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        private void onDestroy() &#123;</span><br><span class="line">            try &#123;</span><br><span class="line">                mVideoCodec.stop();</span><br><span class="line">                mVideoCodec.release();</span><br><span class="line">            &#125; catch (Exception e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        private void requestExit() &#123;</span><br><span class="line">            mShouldExit = true;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> LivePush::pushSpsPps(jbyte *spsData, jint spsLen, jbyte *ppsData, jint ppsLen) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// FLV 的封装格式</span></span><br><span class="line">    <span class="comment">// frame type : 1关键帧，2 非关键帧（4Bit）</span></span><br><span class="line">    <span class="comment">// CodecID : 7表示 AVC(4bit) , 与 frame type组合起来刚好是 1 个字节 0x17</span></span><br><span class="line">    <span class="comment">// fixed ： 0x00 0x00 0x00 0x00 (4byte)</span></span><br><span class="line">    <span class="comment">// configurationVersion  (1byte) 0x01版本</span></span><br><span class="line">    <span class="comment">// AVCProfileIndication  (1byte) sps[1] profile</span></span><br><span class="line">    <span class="comment">// profile_compatibility (1byte) sps[2] compatibility</span></span><br><span class="line">    <span class="comment">// AVCLevelIndication    (1byte) sps[3] Profile level</span></span><br><span class="line">    <span class="comment">// lengthSizeMinusOne    (1byte) 0xff   包长数据所使用的字节数</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// sps + pps 的数据</span></span><br><span class="line">    <span class="comment">// sps number            (1byte) 0xe1   sps 个数</span></span><br><span class="line">    <span class="comment">// sps data length       (2byte) sps 长度</span></span><br><span class="line">    <span class="comment">// sps data                      sps 的内容</span></span><br><span class="line">    <span class="comment">// pps number            (1byte) 0x01 pps 个数</span></span><br><span class="line">    <span class="comment">// pps data length       (2byte) pps 长度</span></span><br><span class="line">    <span class="comment">// pps data                      pps 的内容</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 数据的长度（大小） = sps 大小 + pps 大小 + 头 16字节</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">bodySize</span> <span class="operator">=</span> spsLen + ppsLen + <span class="number">16</span>;</span><br><span class="line">    <span class="comment">// 构建 RTMPPacket</span></span><br><span class="line">    RTMPPacket *pPacket = (RTMPPacket *) malloc(sizeof(RTMPPacket));</span><br><span class="line">    RTMPPacket_Alloc(pPacket, bodySize);</span><br><span class="line">    RTMPPacket_Reset(pPacket);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构建 body 按上面的一个一个开始赋值</span></span><br><span class="line">    <span class="type">char</span> *body = pPacket-&gt;m_body;</span><br><span class="line">    <span class="type">int</span> <span class="variable">index</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    body[index++] = <span class="number">0x17</span>;</span><br><span class="line">    <span class="comment">// fixed ： 0x00 0x00 0x00 0x00 (4byte)</span></span><br><span class="line">    body[index++] = <span class="number">0x00</span>;</span><br><span class="line">    body[index++] = <span class="number">0x00</span>;</span><br><span class="line">    body[index++] = <span class="number">0x00</span>;</span><br><span class="line">    body[index++] = <span class="number">0x00</span>;</span><br><span class="line">    <span class="comment">// configurationVersion  (1byte) 0x01版本</span></span><br><span class="line">    body[index++] = <span class="number">0x01</span>;</span><br><span class="line">    <span class="comment">// AVCProfileIndication  (1byte) sps[1] profile</span></span><br><span class="line">    body[index++] = spsData[<span class="number">1</span>];</span><br><span class="line">    <span class="comment">// profile_compatibility (1byte) sps[2] compatibility</span></span><br><span class="line">    body[index++] = spsData[<span class="number">2</span>];</span><br><span class="line">    <span class="comment">// AVCLevelIndication    (1byte) sps[3] Profile level</span></span><br><span class="line">    body[index++] = spsData[<span class="number">3</span>];</span><br><span class="line">    <span class="comment">// lengthSizeMinusOne    (1byte) 0xff   包长数据所使用的字节数</span></span><br><span class="line">    body[index++] = <span class="number">0xff</span>;</span><br><span class="line">    <span class="comment">// sps + pps 的数据</span></span><br><span class="line">    <span class="comment">// sps number            (1byte) 0xe1   sps 个数</span></span><br><span class="line">    body[index++] = <span class="number">0xe1</span>;</span><br><span class="line">    <span class="comment">// sps data length       (2byte) sps 长度</span></span><br><span class="line">    body[index++] = (spsLen &gt;&gt; <span class="number">8</span>) &amp; <span class="number">0xFF</span>;</span><br><span class="line">    body[index++] = spsLen &amp; <span class="number">0xFF</span>;</span><br><span class="line">    <span class="comment">// sps data                      sps 的内容</span></span><br><span class="line">    memcpy(&amp;body[index], spsData, spsLen);</span><br><span class="line">    index += spsLen;</span><br><span class="line">    <span class="comment">// pps number            (1byte) 0x01 pps 个数</span></span><br><span class="line">    body[index++] = <span class="number">0x01</span>;</span><br><span class="line">    <span class="comment">// pps data length       (2byte) pps 长度</span></span><br><span class="line">    body[index++] = (ppsLen &gt;&gt; <span class="number">8</span>) &amp; <span class="number">0xFF</span>;</span><br><span class="line">    body[index++] = ppsLen &amp; <span class="number">0xFF</span>;</span><br><span class="line">    <span class="comment">// pps data                      pps 的内容</span></span><br><span class="line">    memcpy(&amp;body[index], ppsData, ppsLen);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// RTMPPacket 设置一些信息</span></span><br><span class="line">    pPacket-&gt;m_hasAbsTimestamp = <span class="number">0</span>;</span><br><span class="line">    pPacket-&gt;m_nTimeStamp = <span class="number">0</span>;</span><br><span class="line">    pPacket-&gt;m_headerType = RTMP_PACKET_SIZE_MEDIUM;</span><br><span class="line">    pPacket-&gt;m_packetType = RTMP_PACKET_TYPE_VIDEO;</span><br><span class="line">    pPacket-&gt;m_nBodySize = bodySize;</span><br><span class="line">    pPacket-&gt;m_nChannel = <span class="number">0x04</span>;</span><br><span class="line">    pPacket-&gt;m_nInfoField2 = <span class="built_in">this</span>-&gt;pRtmp-&gt;m_stream_id;</span><br><span class="line"></span><br><span class="line">    pPacketQueue-&gt;push(pPacket);</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void LivePush::pushVideoData(jbyte *videoData, jint dataLen, jboolean keyFrame) &#123;</span><br><span class="line">    // frame type : 1关键帧，2非关键帧 (4bit)</span><br><span class="line">    // CodecID: 7表示 AVC(4bit),与 frame type 组合起来刚好是 1 个字节 0x17</span><br><span class="line">    // fixed：0x01 0x00 0x00 0x00 (4byte),0x01 表示 NALU 单元</span><br><span class="line"></span><br><span class="line">    // video data length  (4byte) video长度</span><br><span class="line">    // video data</span><br><span class="line"></span><br><span class="line">    // 数据的长度（大小） = dataLen + 头 9字节</span><br><span class="line">    int bodySize = dataLen + 9;</span><br><span class="line">    // 构建 RTMPPacket</span><br><span class="line">    RTMPPacket *pPacket = (RTMPPacket *) malloc(sizeof(RTMPPacket));</span><br><span class="line">    RTMPPacket_Alloc(pPacket, bodySize);</span><br><span class="line">    RTMPPacket_Reset(pPacket);</span><br><span class="line"></span><br><span class="line">    // 构建 body 按上面的一个一个开始赋值</span><br><span class="line">    char *body = pPacket-&gt;m_body;</span><br><span class="line">    int index = 0;</span><br><span class="line">    // frame type : 1关键帧，2非关键帧 (4bit)</span><br><span class="line">    // CodecID: 7表示 AVC(4bit),与 frame type 组合起来刚好是 1 个字节 0x17</span><br><span class="line">    if (keyFrame)</span><br><span class="line">        body[index++] = 0x17;</span><br><span class="line">    else</span><br><span class="line">        body[index++] = 0x27;</span><br><span class="line">    // fixed：0x01 0x00 0x00 0x00 (4byte),0x01 表示 NALU 单元</span><br><span class="line">    body[index++] = 0x01;</span><br><span class="line">    body[index++] = 0x00;</span><br><span class="line">    body[index++] = 0x00;</span><br><span class="line">    body[index++] = 0x00;</span><br><span class="line"></span><br><span class="line">    // video data length  (4byte) video长度</span><br><span class="line">    body[index++] = (dataLen &gt;&gt; 24) &amp; 0xFF;</span><br><span class="line">    body[index++] = (dataLen &gt;&gt; 16) &amp; 0xFF;</span><br><span class="line">    body[index++] = (dataLen &gt;&gt; 8) &amp; 0xFF;</span><br><span class="line">    body[index++] = dataLen &amp; 0xFF;</span><br><span class="line">    // video data</span><br><span class="line">    memcpy(&amp;body[index], videoData, dataLen);</span><br><span class="line"></span><br><span class="line">    // RTMPPacket 设置一些信息</span><br><span class="line">    pPacket-&gt;m_headerType = RTMP_PACKET_SIZE_LARGE;</span><br><span class="line">    pPacket-&gt;m_packetType = RTMP_PACKET_TYPE_VIDEO;</span><br><span class="line">    pPacket-&gt;m_hasAbsTimestamp = 0;</span><br><span class="line">    pPacket-&gt;m_nTimeStamp = RTMP_GetTime() - startTime;</span><br><span class="line">    pPacket-&gt;m_nBodySize = bodySize;</span><br><span class="line">    pPacket-&gt;m_nChannel = 0x04;</span><br><span class="line">    pPacket-&gt;m_nInfoField2 = this-&gt;pRtmp-&gt;m_stream_id;</span><br><span class="line"></span><br><span class="line">    pPacketQueue-&gt;push(pPacket);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-推送声音到媒体房间"><a href="#3-推送声音到媒体房间" class="headerlink" title="3.推送声音到媒体房间"></a>3.推送声音到媒体房间</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * 音频的编码线程</span><br><span class="line"> */</span><br><span class="line">private static final class AudioEncoderThread extends Thread &#123;</span><br><span class="line">    private WeakReference&lt;BaseVideoPush&gt; mVideoRecorderWr;</span><br><span class="line">    private volatile boolean mShouldExit = false;</span><br><span class="line">    private MediaCodec mAudioCodec;</span><br><span class="line">    private MediaCodec.BufferInfo mBufferInfo;</span><br><span class="line">    private long mAudioPts = 0;</span><br><span class="line"></span><br><span class="line">    public AudioEncoderThread(WeakReference&lt;BaseVideoPush&gt; videoRecorderWr) &#123;</span><br><span class="line">        this.mVideoRecorderWr = videoRecorderWr;</span><br><span class="line">        mAudioCodec = videoRecorderWr.get().mAudioCodec;</span><br><span class="line">        mBufferInfo = new MediaCodec.BufferInfo();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        try &#123;</span><br><span class="line">            // 开启 start AudioCodec</span><br><span class="line">            mAudioCodec.start();</span><br><span class="line"></span><br><span class="line">            while (true) &#123;</span><br><span class="line">                if (mShouldExit) &#123;</span><br><span class="line">                    return;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                BaseVideoPush videoRecorder = mVideoRecorderWr.get();</span><br><span class="line">                if (videoRecorder == null) &#123;</span><br><span class="line">                    return;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                // 获取音频数据，那这个音频数据从哪里来？音乐播放器里面来，pcm 数据</span><br><span class="line">                int outputBufferIndex = mAudioCodec.dequeueOutputBuffer(mBufferInfo, 0);</span><br><span class="line"></span><br><span class="line">                while (outputBufferIndex &gt;= 0) &#123;</span><br><span class="line">                    // 获取数据</span><br><span class="line">                    ByteBuffer outBuffer = mAudioCodec.getOutputBuffers()[outputBufferIndex];</span><br><span class="line">                    outBuffer.position(mBufferInfo.offset);</span><br><span class="line">                    outBuffer.limit(mBufferInfo.offset + mBufferInfo.size);</span><br><span class="line"></span><br><span class="line">                    // 修改 pts</span><br><span class="line">                    if (mAudioPts == 0) &#123;</span><br><span class="line">                        mAudioPts = mBufferInfo.presentationTimeUs;</span><br><span class="line">                    &#125;</span><br><span class="line">                    mBufferInfo.presentationTimeUs -= mAudioPts;</span><br><span class="line"></span><br><span class="line">                    // 打印一下音频的 AAC 数据</span><br><span class="line">                    byte[] data = new byte[outBuffer.remaining()];</span><br><span class="line">                    outBuffer.get(data, 0, data.length);</span><br><span class="line">                    // 推送音频数据到流媒体</span><br><span class="line">                    mVideoRecorderWr.get().mLivePush.pushAudio(data,data.length);</span><br><span class="line"></span><br><span class="line">                    mAudioCodec.releaseOutputBuffer(outputBufferIndex, false);</span><br><span class="line">                    outputBufferIndex = mAudioCodec.dequeueOutputBuffer(mBufferInfo, 0);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; catch (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; finally &#123;</span><br><span class="line">            onDestroy();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    private void onDestroy() &#123;</span><br><span class="line">        try &#123;</span><br><span class="line">            mAudioCodec.stop();</span><br><span class="line">            mAudioCodec.release();</span><br><span class="line">        &#125; catch (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    private void requestExit() &#123;</span><br><span class="line">        mShouldExit = true;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void LivePush::pushAudioData(jbyte *audioData, jint dataLen) &#123;</span><br><span class="line">    // 2个字节头信息：</span><br><span class="line">        // 前四位表示音频数据格式 AAC ：十进制的10(A) -&gt; 二进制：1010 -&gt; 十六进制：A</span><br><span class="line">        // 五六位表示采样率 十进制：0=5.5k   十进制：1=11k   十进制：2=22k   十进制：3(二进制：11)=44k</span><br><span class="line">        // 七位表示采样的精度 0=8bits  1=16bits</span><br><span class="line">        // 八位表示音频类型 0=mono   1=stereo</span><br><span class="line">        // 我们这里算出来第一个字节是 0xAF = 1010 1111</span><br><span class="line"></span><br><span class="line">    // 0x01 代表 aac 原始数据</span><br><span class="line"></span><br><span class="line">    // 数据的长度（大小） = dataLen + 2</span><br><span class="line">    int bodySize = dataLen + 2;</span><br><span class="line">    // 构建 RTMPPacket</span><br><span class="line">    RTMPPacket *pPacket = (RTMPPacket *) malloc(sizeof(RTMPPacket));</span><br><span class="line">    RTMPPacket_Alloc(pPacket, bodySize);</span><br><span class="line">    RTMPPacket_Reset(pPacket);</span><br><span class="line"></span><br><span class="line">    // 构建 body 按上面的一个一个开始赋值</span><br><span class="line">    char *body = pPacket-&gt;m_body;</span><br><span class="line">    int index = 0;</span><br><span class="line">    body[index++] = 0xAF;</span><br><span class="line">    // 0x01 代表 aac 原始数据</span><br><span class="line">    body[index++] = 0x01;</span><br><span class="line"></span><br><span class="line">    // audio data</span><br><span class="line">    memcpy(&amp;body[index], audioData, dataLen);</span><br><span class="line"></span><br><span class="line">    // RTMPPacket 设置一些信息</span><br><span class="line">    pPacket-&gt;m_headerType = RTMP_PACKET_SIZE_LARGE;</span><br><span class="line">    pPacket-&gt;m_packetType = RTMP_PACKET_TYPE_AUDIO;</span><br><span class="line">    pPacket-&gt;m_hasAbsTimestamp = 0;</span><br><span class="line">    pPacket-&gt;m_nTimeStamp = RTMP_GetTime() - startTime;</span><br><span class="line">    pPacket-&gt;m_nBodySize = bodySize;</span><br><span class="line">    pPacket-&gt;m_nChannel = 0x04;</span><br><span class="line">    pPacket-&gt;m_nInfoField2 = this-&gt;pRtmp-&gt;m_stream_id;</span><br><span class="line"></span><br><span class="line">    pPacketQueue-&gt;push(pPacket);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-滤镜效果处理"><a href="#4-滤镜效果处理" class="headerlink" title="4.滤镜效果处理"></a>4.滤镜效果处理</h1><p>// 设置纹理渲染</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * |---------------------------------------------------------------------------------------------------------------|</span><br><span class="line"> *  @description:  灰色视频的推流</span><br><span class="line"> *  @author: jamin</span><br><span class="line"> *  @date: 2020/10/20 13:39</span><br><span class="line"> * |---------------------------------------------------------------------------------------------------------------|</span><br><span class="line"> */</span><br><span class="line">public class GrayVideoPush extends BaseVideoPush &#123;</span><br><span class="line">    private PushRenderer mPushRenderer;</span><br><span class="line"></span><br><span class="line">    public GrayVideoPush(Context context, EGLContext eglContext, int textureId) &#123;</span><br><span class="line">        super(context, eglContext);</span><br><span class="line">        mPushRenderer = new PushRenderer(context, textureId);</span><br><span class="line">        setRenderer(mPushRenderer);</span><br><span class="line">        mPushRenderer.setFragmentRender(R.raw.filter_fragment_gray);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：18.FFmpeg-OpenGL朋友圈录制视频</title>
    <url>/89.FFmpeg-OpenGL%E6%9C%8B%E5%8F%8B%E5%9C%88%E5%BD%95%E5%88%B6%E8%A7%86%E9%A2%91/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/ffmpeg88livepush">NDKPractice项目的ffmpeg88livepush</a></strong></p>
<h1 id="1-OpenGL渲染流程介绍"><a href="#1-OpenGL渲染流程介绍" class="headerlink" title="1.OpenGL渲染流程介绍"></a>1.OpenGL渲染流程介绍</h1><p>怎么渲染一张图片：</p>
<ol>
<li>继承<code>GLSurfaceView</code></li>
<li>继承<code>GLSurfaceView.Renderer</code></li>
<li>写顶点和纹理渲染的代码</li>
<li>编译链接生成程序</li>
<li>生成加载 bitmap 的纹理</li>
<li>绘制到屏幕</li>
</ol>
<p>介绍一下纹理坐标系：<img src="/images/89.纹理坐标系.png" alt=""></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 顶点坐标</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">float</span>[] mVertexCoordinate = <span class="keyword">new</span> <span class="title class_">float</span>[]&#123;</span><br><span class="line">        -<span class="number">1f</span>, -<span class="number">1f</span>, <span class="comment">// 左下角</span></span><br><span class="line">        <span class="number">1f</span>, -<span class="number">1f</span>,  <span class="comment">// 右下角</span></span><br><span class="line">        -<span class="number">1f</span>, <span class="number">1f</span>,  <span class="comment">// 左上角</span></span><br><span class="line">        <span class="number">1f</span>, <span class="number">1f</span>    <span class="comment">// 右上角</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">private</span> FloatBuffer mVertexBuffer;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 纹理坐标(对应全屏)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">float</span>[] mFragmentCoordinate = <span class="keyword">new</span> <span class="title class_">float</span>[]&#123;</span><br><span class="line">        <span class="number">0f</span>, <span class="number">1f</span>,</span><br><span class="line">        <span class="number">1f</span>, <span class="number">1f</span>,</span><br><span class="line">        <span class="number">0f</span>, <span class="number">0f</span>,</span><br><span class="line">        <span class="number">1f</span>, <span class="number">0f</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 纹理坐标(对应半屏)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">float</span>[] mFragmentCoordinate = <span class="keyword">new</span> <span class="title class_">float</span>[]&#123;</span><br><span class="line">        <span class="number">0f</span>, <span class="number">0.5f</span>,</span><br><span class="line">        <span class="number">1f</span>, <span class="number">0.5f</span>,</span><br><span class="line">        <span class="number">0f</span>, <span class="number">0f</span>,</span><br><span class="line">        <span class="number">1f</span>, <span class="number">0f</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>右边是OpenGL的渲染流程：<br><img src="/images/89.OpenGL%20的渲染原理.png" alt=""></p>
<h1 id="2-OpenGL渲染摄像头数据"><a href="#2-OpenGL渲染摄像头数据" class="headerlink" title="2.OpenGL渲染摄像头数据"></a>2.OpenGL渲染摄像头数据</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 初始化视频的 MediaCodec</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> width</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> height</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">initVideoCodec</span><span class="params">(<span class="type">int</span> width, <span class="type">int</span> height)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="type">MediaFormat</span> <span class="variable">videoFormat</span> <span class="operator">=</span> MediaFormat.createVideoFormat(MediaFormat.MIMETYPE_VIDEO_AVC, width, height);</span><br><span class="line">    <span class="comment">// 设置颜色格式</span></span><br><span class="line">    videoFormat.setInteger(MediaFormat.KEY_COLOR_FORMAT, MediaCodecInfo.CodecCapabilities.COLOR_FormatSurface);</span><br><span class="line">    videoFormat.setInteger(MediaFormat.KEY_BIT_RATE, width * height * <span class="number">4</span>);</span><br><span class="line">    <span class="comment">// 设置帧率</span></span><br><span class="line">    videoFormat.setInteger(MediaFormat.KEY_FRAME_RATE, <span class="number">24</span>);</span><br><span class="line">    <span class="comment">// 设置 I 帧的间隔时间</span></span><br><span class="line">    videoFormat.setInteger(MediaFormat.KEY_I_FRAME_INTERVAL, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建编码器</span></span><br><span class="line">    mVideoCodec = MediaCodec.createEncoderByType(MediaFormat.MIMETYPE_VIDEO_AVC);</span><br><span class="line">    mVideoCodec.configure(videoFormat, <span class="literal">null</span>, <span class="literal">null</span>, MediaCodec.CONFIGURE_FLAG_ENCODE);</span><br><span class="line"></span><br><span class="line">    mSurface = mVideoCodec.createInputSurface();</span><br><span class="line">    <span class="comment">// 开启一个编码采集 InputSurface 上的数据，合成视频</span></span><br><span class="line">    mVideoThread = <span class="keyword">new</span> <span class="title class_">VideoEncoderThread</span>(mVideoRecorderWr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 视频的渲染线程</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">VideoRenderThread</span> <span class="keyword">extends</span> <span class="title class_">Thread</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> WeakReference&lt;BaseVideoRecorder&gt; mVideoRecorderWr;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">boolean</span> <span class="variable">mShouldExit</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">private</span> EglHelper mEglHelper;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">boolean</span> <span class="variable">mHashCreateContext</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">boolean</span> <span class="variable">mHashSurfaceCreated</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">boolean</span> <span class="variable">mHashSurfaceChanged</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> mWidth;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> mHeight;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">VideoRenderThread</span><span class="params">(WeakReference&lt;BaseVideoRecorder&gt; videoRecorderWr)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.mVideoRecorderWr = videoRecorderWr;</span><br><span class="line">        mEglHelper = <span class="keyword">new</span> <span class="title class_">EglHelper</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (mShouldExit) &#123;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="type">BaseVideoRecorder</span> <span class="variable">videoRecorder</span> <span class="operator">=</span> mVideoRecorderWr.get();</span><br><span class="line">                <span class="keyword">if</span> (videoRecorder == <span class="literal">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 1. 创建 EGL 上下文</span></span><br><span class="line">                <span class="keyword">if</span> (!mHashCreateContext) &#123;</span><br><span class="line">                    mEglHelper.initCreateEgl(videoRecorder.mSurface, videoRecorder.mEglContext);</span><br><span class="line">                    mHashCreateContext = <span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 回调 Render</span></span><br><span class="line">                <span class="type">GL10</span> <span class="variable">gl</span> <span class="operator">=</span> (GL10) mEglHelper.getEglContext().getGL();</span><br><span class="line">                <span class="keyword">if</span> (!mHashSurfaceCreated) &#123;</span><br><span class="line">                    videoRecorder.mRenderer.onSurfaceCreated(gl, mEglHelper.getEGLConfig());</span><br><span class="line">                    mHashSurfaceCreated = <span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (!mHashSurfaceChanged) &#123;</span><br><span class="line">                    videoRecorder.mRenderer.onSurfaceChanged(gl, mWidth, mHeight);</span><br><span class="line">                    mHashSurfaceChanged = <span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                videoRecorder.mRenderer.onDrawFrame(gl);</span><br><span class="line"></span><br><span class="line">                mEglHelper.swapBuffers();</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 60 fps</span></span><br><span class="line">                Thread.sleep(<span class="number">16</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            onDestroy();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        mEglHelper.destroy();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">requestExit</span><span class="params">()</span> &#123;</span><br><span class="line">        mShouldExit = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setSize</span><span class="params">(<span class="type">int</span> width, <span class="type">int</span> height)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.mWidth = width;</span><br><span class="line">        <span class="built_in">this</span>.mHeight = height;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="3-视频编码录制"><a href="#3-视频编码录制" class="headerlink" title="3.视频编码录制"></a>3.视频编码录制</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 视频的编码线程</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">VideoEncoderThread</span> <span class="keyword">extends</span> <span class="title class_">Thread</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> MediaMuxer mMediaMuxer;</span><br><span class="line">    <span class="keyword">private</span> WeakReference&lt;BaseVideoRecorder&gt; mVideoRecorderWr;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">boolean</span> <span class="variable">mShouldExit</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">private</span> MediaCodec mVideoCodec;</span><br><span class="line">    <span class="keyword">private</span> MediaCodec.BufferInfo mBufferInfo;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">mVideoTrackIndex</span> <span class="operator">=</span> -<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">long</span> <span class="variable">mVideoPts</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> CyclicBarrier mStartCb, mDestroyCb;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">VideoEncoderThread</span><span class="params">(WeakReference&lt;BaseVideoRecorder&gt; videoRecorderWr)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.mVideoRecorderWr = videoRecorderWr;</span><br><span class="line">        mVideoCodec = videoRecorderWr.get().mVideoCodec;</span><br><span class="line">        mMediaMuxer = videoRecorderWr.get().mMediaMuxer;</span><br><span class="line">        mBufferInfo = <span class="keyword">new</span> <span class="title class_">MediaCodec</span>.BufferInfo();</span><br><span class="line">        mStartCb = videoRecorderWr.get().mStartCb;</span><br><span class="line">        mDestroyCb = videoRecorderWr.get().mDestroyCb;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mVideoCodec.start();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (mShouldExit) &#123;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="type">BaseVideoRecorder</span> <span class="variable">videoRecorder</span> <span class="operator">=</span> mVideoRecorderWr.get();</span><br><span class="line">                <span class="keyword">if</span> (videoRecorder == <span class="literal">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 代码先不写，先测试，从 surface 上获取数据，编码成 h264 ,通过 MediaMuxer 合成 mp4</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">outputBufferIndex</span> <span class="operator">=</span> mVideoCodec.dequeueOutputBuffer(mBufferInfo, <span class="number">0</span>);</span><br><span class="line">                <span class="keyword">if</span> (outputBufferIndex == MediaCodec.INFO_OUTPUT_FORMAT_CHANGED) &#123;</span><br><span class="line">                    mVideoTrackIndex = mMediaMuxer.addTrack(mVideoCodec.getOutputFormat());</span><br><span class="line">                    mMediaMuxer.start();</span><br><span class="line">                    mStartCb.await();</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">while</span> (outputBufferIndex &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="comment">// 获取数据</span></span><br><span class="line">                        <span class="type">ByteBuffer</span> <span class="variable">outBuffer</span> <span class="operator">=</span> mVideoCodec.getOutputBuffers()[outputBufferIndex];</span><br><span class="line">                        outBuffer.position(mBufferInfo.offset);</span><br><span class="line">                        outBuffer.limit(mBufferInfo.offset + mBufferInfo.size);</span><br><span class="line"></span><br><span class="line">                        <span class="comment">// 修改 pts</span></span><br><span class="line">                        <span class="keyword">if</span> (mVideoPts == <span class="number">0</span>) &#123;</span><br><span class="line">                            mVideoPts = mBufferInfo.presentationTimeUs;</span><br><span class="line">                        &#125;</span><br><span class="line">                        mBufferInfo.presentationTimeUs -= mVideoPts;</span><br><span class="line"></span><br><span class="line">                        <span class="comment">// 写入数据</span></span><br><span class="line">                        mMediaMuxer.writeSampleData(mVideoTrackIndex, outBuffer, mBufferInfo);</span><br><span class="line"></span><br><span class="line">                        <span class="comment">// 回调当前录制的时间</span></span><br><span class="line">                        <span class="keyword">if</span> (videoRecorder.mRecordListener != <span class="literal">null</span>) &#123;</span><br><span class="line">                            videoRecorder.mRecordListener.onTime(mBufferInfo.presentationTimeUs / <span class="number">1000</span>);</span><br><span class="line">                        &#125;</span><br><span class="line"></span><br><span class="line">                        mVideoCodec.releaseOutputBuffer(outputBufferIndex, <span class="literal">false</span>);</span><br><span class="line">                        outputBufferIndex = mVideoCodec.dequeueOutputBuffer(mBufferInfo, <span class="number">0</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            onDestroy();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mVideoCodec.stop();</span><br><span class="line">            mVideoCodec.release();</span><br><span class="line">            mDestroyCb.await();</span><br><span class="line">            mMediaMuxer.stop();</span><br><span class="line">            mMediaMuxer.release();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">requestExit</span><span class="params">()</span> &#123;</span><br><span class="line">        mShouldExit = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>音视频：19.FFmpeg-视频加背景音乐、H.264协议</title>
    <url>/90.FFmpeg-%E8%A7%86%E9%A2%91%E5%8A%A0%E8%83%8C%E6%99%AF%E9%9F%B3%E4%B9%90%E3%80%81H.264%E5%8D%8F%E8%AE%AE/</url>
    <content><![CDATA[<p>具体代码请看：<strong><a href="https://github.com/EastUp/NDKPractice/tree/master/ffmpeg88livepush">NDKPractice项目的ffmpeg88livepush</a></strong></p>
<h1 id="1-实现视频加背景音乐"><a href="#1-实现视频加背景音乐" class="headerlink" title="1.实现视频加背景音乐"></a>1.实现视频加背景音乐</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="type">MediaInfoListener</span> <span class="variable">mMediaInfoListener</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MediaInfoListener</span>() &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">long</span> <span class="variable">mAudioPts</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">mSampleRate</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">mChannels</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">musicInfo</span><span class="params">(<span class="type">int</span> sampleRate, <span class="type">int</span> channels)</span> &#123;</span><br><span class="line">        <span class="comment">// 获取了音频的信息</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            initAudioCodec(sampleRate, channels);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">this</span>.mSampleRate = sampleRate;</span><br><span class="line">        <span class="built_in">this</span>.mChannels = channels;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">callbackPcm</span><span class="params">(<span class="type">byte</span>[] pcmData, <span class="type">int</span> size)</span> &#123;</span><br><span class="line">        <span class="comment">// 把数据写入到 mAudioCodec 的 InputBuffer</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">inputBufferIndex</span> <span class="operator">=</span> mAudioCodec.dequeueInputBuffer(<span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span> (inputBufferIndex &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="type">ByteBuffer</span> <span class="variable">byteBuffer</span> <span class="operator">=</span> mAudioCodec.getInputBuffers()[inputBufferIndex];</span><br><span class="line">            byteBuffer.clear();</span><br><span class="line">            byteBuffer.put(pcmData);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// pts  44100 * 2 *2</span></span><br><span class="line">            mAudioPts += size * <span class="number">1000000</span> / mSampleRate * mChannels * <span class="number">2</span>;</span><br><span class="line">            <span class="comment">// size 22050*2*2</span></span><br><span class="line">            mAudioCodec.queueInputBuffer(inputBufferIndex, <span class="number">0</span>, size, mAudioPts, <span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 音频的编码线程</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">AudioEncoderThread</span> <span class="keyword">extends</span> <span class="title class_">Thread</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> MediaMuxer mMediaMuxer;</span><br><span class="line">    <span class="keyword">private</span> WeakReference&lt;BaseVideoRecorder&gt; mVideoRecorderWr;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">boolean</span> <span class="variable">mShouldExit</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">private</span> MediaCodec mAudioCodec;</span><br><span class="line">    <span class="keyword">private</span> MediaCodec.BufferInfo mBufferInfo;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">mAudioTrackIndex</span> <span class="operator">=</span> -<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">long</span> <span class="variable">mAudioPts</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> CyclicBarrier mStartCb, mDestroyCb;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">AudioEncoderThread</span><span class="params">(WeakReference&lt;BaseVideoRecorder&gt; videoRecorderWr)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.mVideoRecorderWr = videoRecorderWr;</span><br><span class="line">        mAudioCodec = videoRecorderWr.get().mAudioCodec;</span><br><span class="line">        mMediaMuxer = videoRecorderWr.get().mMediaMuxer;</span><br><span class="line">        mBufferInfo = <span class="keyword">new</span> <span class="title class_">MediaCodec</span>.BufferInfo();</span><br><span class="line">        mStartCb = videoRecorderWr.get().mStartCb;</span><br><span class="line">        mDestroyCb = videoRecorderWr.get().mDestroyCb;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (mShouldExit) &#123;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="type">BaseVideoRecorder</span> <span class="variable">videoRecorder</span> <span class="operator">=</span> mVideoRecorderWr.get();</span><br><span class="line">                <span class="keyword">if</span> (videoRecorder == <span class="literal">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 获取音频数据，那这个音频数据从哪里来？音乐播放器里面来，pcm 数据</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">outputBufferIndex</span> <span class="operator">=</span> mAudioCodec.dequeueOutputBuffer(mBufferInfo, <span class="number">0</span>);</span><br><span class="line">                <span class="keyword">if</span> (outputBufferIndex == MediaCodec.INFO_OUTPUT_FORMAT_CHANGED) &#123;</span><br><span class="line">                    mAudioTrackIndex = mMediaMuxer.addTrack(mAudioCodec.getOutputFormat());</span><br><span class="line">                    mStartCb.await();</span><br><span class="line">                    <span class="comment">// mMediaMuxer.start();</span></span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">while</span> (outputBufferIndex &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="comment">// 获取数据</span></span><br><span class="line">                        <span class="type">ByteBuffer</span> <span class="variable">outBuffer</span> <span class="operator">=</span> mAudioCodec.getOutputBuffers()[outputBufferIndex];</span><br><span class="line">                        outBuffer.position(mBufferInfo.offset);</span><br><span class="line">                        outBuffer.limit(mBufferInfo.offset + mBufferInfo.size);</span><br><span class="line"></span><br><span class="line">                        <span class="comment">// 修改 pts</span></span><br><span class="line">                        <span class="keyword">if</span> (mAudioPts == <span class="number">0</span>) &#123;</span><br><span class="line">                            mAudioPts = mBufferInfo.presentationTimeUs;</span><br><span class="line">                        &#125;</span><br><span class="line">                        mBufferInfo.presentationTimeUs -= mAudioPts;</span><br><span class="line"></span><br><span class="line">                        <span class="comment">// 写入数据</span></span><br><span class="line">                        mMediaMuxer.writeSampleData(mAudioTrackIndex, outBuffer, mBufferInfo);</span><br><span class="line"></span><br><span class="line">                        mAudioCodec.releaseOutputBuffer(outputBufferIndex, <span class="literal">false</span>);</span><br><span class="line">                        outputBufferIndex = mAudioCodec.dequeueOutputBuffer(mBufferInfo, <span class="number">0</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            onDestroy();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mAudioCodec.stop();</span><br><span class="line">            mAudioCodec.release();</span><br><span class="line">            mDestroyCb.await();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">requestExit</span><span class="params">()</span> &#123;</span><br><span class="line">        mShouldExit = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/images/90.视频录制添加背景音乐的流程.png" alt=""></p>
<h1 id="2-详解H-264协议"><a href="#2-详解H-264协议" class="headerlink" title="2.详解H.264协议"></a>2.详解H.264协议</h1><p><img src="/images/90.h264协议详解.png" alt=""></p>
<p>首先需要明确 H264 可以分为两层，对于 VCL 具体的编解码算法这里暂时先不介绍，只介绍常用的 NAL 层，即网络提取层，这是解码的基础。：<br>1.VCL video codinglayer（视频编码层）<br>2.NAL network abstraction layer（网络提取层）。</p>
<p><img src="/images/90.NAL.png" alt=""></p>
<ul>
<li>SPS：序列参数集</li>
<li>PPS：图像参数集</li>
<li>I帧：帧内编码帧，可独立解码生成完整的图片。</li>
<li>P帧: 前向预测编码帧，需要参考其前面的一个I 或者B 来生成一张完整的图片。</li>
<li>B帧: 双向预测内插编码帧，则要参考其前一个I或者P帧及其后面的一个P帧来生成一张完整的图片</li>
</ul>
<h1 id="4-获取-MediaCodec-的-sps-和-pps"><a href="#4-获取-MediaCodec-的-sps-和-pps" class="headerlink" title="4.获取 MediaCodec 的 sps 和 pps"></a>4.获取 MediaCodec 的 sps 和 pps</h1><ul>
<li>SPS：Sequence Paramater Set，又称作序列参数集。SPS中保存了一组编码视频序列(Coded video sequence)的全局参数。所谓的编码视频序列即原始视频的一帧一帧的像素数据经过编码之后的结构组成的序列</li>
<li>PPS:除了序列参数集SPS之外，H.264中另一重要的参数集合为图像参数集Picture Paramater Set(PPS)。 </li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 视频的编码线程</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">VideoEncoderThread</span> <span class="keyword">extends</span> <span class="title class_">Thread</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> WeakReference&lt;BaseVideoPush&gt; mVideoRecorderWr;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">boolean</span> <span class="variable">mShouldExit</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">private</span> MediaCodec mVideoCodec;</span><br><span class="line">    <span class="keyword">private</span> MediaCodec.BufferInfo mBufferInfo;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">long</span> <span class="variable">mVideoPts</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> CyclicBarrier mStartCb, mDestroyCb;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">byte</span>[] mVideoSps,mVideoPps;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">VideoEncoderThread</span><span class="params">(WeakReference&lt;BaseVideoPush&gt; videoRecorderWr)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.mVideoRecorderWr = videoRecorderWr;</span><br><span class="line">        mVideoCodec = videoRecorderWr.get().mVideoCodec;</span><br><span class="line">        mBufferInfo = <span class="keyword">new</span> <span class="title class_">MediaCodec</span>.BufferInfo();</span><br><span class="line">        mStartCb = videoRecorderWr.get().mStartCb;</span><br><span class="line">        mDestroyCb = videoRecorderWr.get().mDestroyCb;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mVideoCodec.start();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (mShouldExit) &#123;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="type">BaseVideoPush</span> <span class="variable">videoRecorder</span> <span class="operator">=</span> mVideoRecorderWr.get();</span><br><span class="line">                <span class="keyword">if</span> (videoRecorder == <span class="literal">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 代码先不写，先测试，从 surface 上获取数据，编码成 h264 ,通过 MediaMuxer 合成 mp4</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">outputBufferIndex</span> <span class="operator">=</span> mVideoCodec.dequeueOutputBuffer(mBufferInfo, <span class="number">0</span>);</span><br><span class="line">                <span class="keyword">if</span> (outputBufferIndex == MediaCodec.INFO_OUTPUT_FORMAT_CHANGED) &#123;</span><br><span class="line">                    <span class="comment">// mStartCb.await();</span></span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 获取 sps 和 pps</span></span><br><span class="line">                    Log.e(<span class="string">&quot;TAG&quot;</span>, <span class="string">&quot;获取 sps 和 pps&quot;</span>);</span><br><span class="line">                    <span class="type">ByteBuffer</span> <span class="variable">byteBuffer</span> <span class="operator">=</span> mVideoCodec.getOutputFormat().getByteBuffer(<span class="string">&quot;csd-0&quot;</span>);</span><br><span class="line">                    mVideoSps = <span class="keyword">new</span> <span class="title class_">byte</span>[byteBuffer.remaining()];</span><br><span class="line">                    byteBuffer.get(mVideoSps, <span class="number">0</span>, mVideoSps.length);</span><br><span class="line"></span><br><span class="line">                    Log.e(<span class="string">&quot;sps&quot;</span>, bytesToHexString(mVideoSps));</span><br><span class="line"></span><br><span class="line">                    byteBuffer = mVideoCodec.getOutputFormat().getByteBuffer(<span class="string">&quot;csd-1&quot;</span>);</span><br><span class="line">                    mVideoPps = <span class="keyword">new</span> <span class="title class_">byte</span>[byteBuffer.remaining()];</span><br><span class="line">                    byteBuffer.get(mVideoPps, <span class="number">0</span>, mVideoPps.length);</span><br><span class="line"></span><br><span class="line">                    Log.e(<span class="string">&quot;pps&quot;</span>, bytesToHexString(mVideoPps));</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">while</span> (outputBufferIndex &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="comment">// 获取数据</span></span><br><span class="line">                        <span class="type">ByteBuffer</span> <span class="variable">outBuffer</span> <span class="operator">=</span> mVideoCodec.getOutputBuffers()[outputBufferIndex];</span><br><span class="line">                        outBuffer.position(mBufferInfo.offset);</span><br><span class="line">                        outBuffer.limit(mBufferInfo.offset + mBufferInfo.size);</span><br><span class="line"></span><br><span class="line">                        <span class="comment">// 修改 pts</span></span><br><span class="line">                        <span class="keyword">if</span> (mVideoPts == <span class="number">0</span>) &#123;</span><br><span class="line">                            mVideoPts = mBufferInfo.presentationTimeUs;</span><br><span class="line">                        &#125;</span><br><span class="line">                        mBufferInfo.presentationTimeUs -= mVideoPts;</span><br><span class="line"></span><br><span class="line">                        <span class="type">byte</span>[] data = <span class="keyword">new</span> <span class="title class_">byte</span>[outBuffer.remaining()];</span><br><span class="line">                        outBuffer.get(data, <span class="number">0</span>, data.length);</span><br><span class="line">                        Log.e(<span class="string">&quot;TAG&quot;</span>,bytesToHexString(data));</span><br><span class="line"></span><br><span class="line">                        <span class="comment">// 回调当前录制的时间</span></span><br><span class="line">                        <span class="keyword">if</span> (videoRecorder.mRecordListener != <span class="literal">null</span>) &#123;</span><br><span class="line">                            videoRecorder.mRecordListener.onTime(mBufferInfo.presentationTimeUs / <span class="number">1000</span>);</span><br><span class="line">                        &#125;</span><br><span class="line"></span><br><span class="line">                        mVideoCodec.releaseOutputBuffer(outputBufferIndex, <span class="literal">false</span>);</span><br><span class="line">                        outputBufferIndex = mVideoCodec.dequeueOutputBuffer(mBufferInfo, <span class="number">0</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            onDestroy();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mVideoCodec.stop();</span><br><span class="line">            mVideoCodec.release();</span><br><span class="line">            <span class="comment">// mDestroyCb.await();</span></span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">requestExit</span><span class="params">()</span> &#123;</span><br><span class="line">        mShouldExit = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>音视频</category>
      </categories>
      <tags>
        <tag>音视频</tag>
      </tags>
  </entry>
  <entry>
    <title>Claude Code从零到精通教程</title>
    <url>/Claude-Code%E4%BB%8E%E9%9B%B6%E5%88%B0%E7%B2%BE%E9%80%9A%E6%95%99%E7%A8%8B/</url>
    <content><![CDATA[<blockquote>
<p>基于一线开发者实战经验，结合官方文档与社区最佳实践整理<br>适用版本：Claude Code + Claude Opus 4.6（2026年3月）</p>
</blockquote>
<span id="more"></span>
<h1 id="Claude-Code-从零到精通：多Agent协作开发完全教程"><a href="#Claude-Code-从零到精通：多Agent协作开发完全教程" class="headerlink" title="Claude Code 从零到精通：多Agent协作开发完全教程"></a>Claude Code 从零到精通：多Agent协作开发完全教程</h1><p>基于一线开发者实战经验，结合官方文档与社区最佳实践整理</p>
<p>适用版本：Claude Code + Claude Opus 4.6（2026年3月）</p>
<p>文档更新：第七章、第九章、第十二章、第十三章 0311更新</p>
<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><h2 id="第一章：认识AI开发的演进之路"><a href="#第一章：认识AI开发的演进之路" class="headerlink" title="第一章：认识AI开发的演进之路"></a>第一章：认识AI开发的演进之路</h2><h2 id="第二章：Claude-Code-安装与入门"><a href="#第二章：Claude-Code-安装与入门" class="headerlink" title="第二章：Claude Code 安装与入门"></a>第二章：Claude Code 安装与入门</h2><h2 id="第三章：CLAUDE-md-—-你的开发规范圣经"><a href="#第三章：CLAUDE-md-—-你的开发规范圣经" class="headerlink" title="第三章：CLAUDE.md — 你的开发规范圣经"></a>第三章：CLAUDE.md — 你的开发规范圣经</h2><h2 id="第四章：Skills-技能系统"><a href="#第四章：Skills-技能系统" class="headerlink" title="第四章：Skills 技能系统"></a>第四章：Skills 技能系统</h2><h2 id="第五章：Plugins-插件系统"><a href="#第五章：Plugins-插件系统" class="headerlink" title="第五章：Plugins 插件系统"></a>第五章：Plugins 插件系统</h2><h2 id="第六章：MCP-Servers-—-连接外部世界"><a href="#第六章：MCP-Servers-—-连接外部世界" class="headerlink" title="第六章：MCP Servers — 连接外部世界"></a>第六章：MCP Servers — 连接外部世界</h2><h2 id="第七章：Hooks-事件钩子"><a href="#第七章：Hooks-事件钩子" class="headerlink" title="第七章：Hooks 事件钩子"></a>第七章：Hooks 事件钩子</h2><h2 id="第八章：Subagents-子Agent"><a href="#第八章：Subagents-子Agent" class="headerlink" title="第八章：Subagents 子Agent"></a>第八章：Subagents 子Agent</h2><h2 id="第九章：Agent-Teams-—-多Agent协作开发"><a href="#第九章：Agent-Teams-—-多Agent协作开发" class="headerlink" title="第九章：Agent Teams — 多Agent协作开发"></a>第九章：Agent Teams — 多Agent协作开发</h2><h2 id="第十章：上下文管理-—-核心中的核心"><a href="#第十章：上下文管理-—-核心中的核心" class="headerlink" title="第十章：上下文管理 — 核心中的核心"></a>第十章：上下文管理 — 核心中的核心</h2><h2 id="第十一章：开发范式与实战案例"><a href="#第十一章：开发范式与实战案例" class="headerlink" title="第十一章：开发范式与实战案例"></a>第十一章：开发范式与实战案例</h2><h2 id="第十二章：OpenClaw-—-用手机遥控-Claude-Code-7×24-小时工作"><a href="#第十二章：OpenClaw-—-用手机遥控-Claude-Code-7×24-小时工作" class="headerlink" title="第十二章：OpenClaw — 用手机遥控 Claude Code 7×24 小时工作"></a>第十二章：OpenClaw — 用手机遥控 Claude Code 7×24 小时工作</h2><h2 id="第十三章：常见问题与进阶技巧"><a href="#第十三章：常见问题与进阶技巧" class="headerlink" title="第十三章：常见问题与进阶技巧"></a>第十三章：常见问题与进阶技巧</h2><h2 id="第一章：认识AI开发的演进之路-1"><a href="#第一章：认识AI开发的演进之路-1" class="headerlink" title="第一章：认识AI开发的演进之路"></a>第一章：认识AI开发的演进之路</h2><p>在学习 Claude Code 之前，了解 AI 辅助开发的演进历程，能帮你理解为什么今天的工具如此强大。</p>
<h3 id="1-1-AI开发工具的四个阶段"><a href="#1-1-AI开发工具的四个阶段" class="headerlink" title="1.1 AI开发工具的四个阶段"></a>1.1 AI开发工具的四个阶段</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────────────────┐</span><br><span class="line">│                    AI 开发工具演进路线图                           │</span><br><span class="line">├─────────────────────────────────────────────────────────────────┤</span><br><span class="line">│                                                                 │</span><br><span class="line">│  阶段1: 网页版大模型 (2023-)                                     │</span><br><span class="line">│  ┌──────────────┐                                               │</span><br><span class="line">│  │  ChatGPT 等   │ → 复制粘贴代码，需要行业背景才能用好            │</span><br><span class="line">│  │  网页对话框    │ → 无法访问本地文件，无法操作终端                 │</span><br><span class="line">│  └──────────────┘                                               │</span><br><span class="line">│         │                                                       │</span><br><span class="line">│         ▼                                                       │</span><br><span class="line">│  阶段2: IDE内嵌助手 (2023-2024)                                  │</span><br><span class="line">│  ┌──────────────┐                                               │</span><br><span class="line">│  │  Copilot      │ → 只能在当前文件内辅助，写写排序等简单功能       │</span><br><span class="line">│  │  当前文件级    │ → 无法理解项目整体架构                         │</span><br><span class="line">│  └──────────────┘                                               │</span><br><span class="line">│         │                                                       │</span><br><span class="line">│         ▼                                                       │</span><br><span class="line">│  阶段3: 项目级AI工具 (2024-2025)                                 │</span><br><span class="line">│  ┌──────────────┐                                               │</span><br><span class="line">│  │  Cursor 等    │ → 可以访问项目目录下所有文件                    │</span><br><span class="line">│  │  项目目录级    │ → 能发现代码中隐藏的关联和问题                  │</span><br><span class="line">│  │              │ → 但无法探索外部世界，多窗口不能通信              │</span><br><span class="line">│  └──────────────┘                                               │</span><br><span class="line">│         │                                                       │</span><br><span class="line">│         ▼                                                       │</span><br><span class="line">│  阶段4: Agent级开发工具 (2025-2026) ← 我们在这里！               │</span><br><span class="line">│  ┌──────────────┐                                               │</span><br><span class="line">│  │  Claude Code  │ → 可操作终端、SSH远程机器、访问外部世界          │</span><br><span class="line">│  │  + Agent Teams│ → 多Agent并行开发、互相通信纠错                 │</span><br><span class="line">│  │  终端+多Agent │ → 可持续数小时自主工作                          │</span><br><span class="line">│  └──────────────┘                                               │</span><br><span class="line">│                                                                 │</span><br><span class="line">└─────────────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="1-2-为什么现在能做到如此大的提效？"><a href="#1-2-为什么现在能做到如此大的提效？" class="headerlink" title="1.2 为什么现在能做到如此大的提效？"></a>1.2 为什么现在能做到如此大的提效？</h3><p>根据实战经验，有三个关键变化推动了 AI 开发的质变：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>变化</th>
<th>说明</th>
<th>影响</th>
</tr>
</thead>
<tbody>
<tr>
<td>模型能力跃升</td>
<td>Claude Opus 4.6 对外部世界的探索和理解能力大幅增强</td>
<td>可以 SSH 远程机器、理解复杂终端工具、持续探索</td>
</tr>
<tr>
<td>Agent Teams 协作</td>
<td>多个 Agent 可以并行工作且互相通信</td>
<td>原来3小时的任务，6个Agent并行半小时完成</td>
</tr>
<tr>
<td>Agent间互相纠错</td>
<td>Agent之间可以发现彼此的问题并纠正</td>
<td>减少人工干预，提高代码质量</td>
</tr>
</tbody>
</table>
</div>
<p>实战数据：一个复杂的科创实验制作工具项目，使用 Agent Teams 6小时完成了整体框架搭建，综合提效约 5倍。</p>
<h2 id="第二章：Claude-Code-安装与入门-1"><a href="#第二章：Claude-Code-安装与入门-1" class="headerlink" title="第二章：Claude Code 安装与入门"></a>第二章：Claude Code 安装与入门</h2><h3 id="2-1-安装-Claude-Code"><a href="#2-1-安装-Claude-Code" class="headerlink" title="2.1 安装 Claude Code"></a>2.1 安装 Claude Code</h3><p>📖 官方安装文档：<a href="https://docs.anthropic.com/en/docs/claude-code/quickstart方式一：官方脚本安装（推荐，支持自动更新）macOS">https://docs.anthropic.com/en/docs/claude-code/quickstart方式一：官方脚本安装（推荐，支持自动更新）macOS</a> / Linux / WSL：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -fsSL https://claude.ai/install.sh | bash</span><br></pre></td></tr></table></figure>
<p>Windows PowerShell：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="built_in">irm</span> https://claude.ai/install.ps1 | <span class="built_in">iex</span></span><br></pre></td></tr></table></figure>
<p>方式二：Homebrew 安装（macOS）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask claude-code</span><br></pre></td></tr></table></figure>
<p>方式三：npm 安装（备选，环境兼容性最好）</p>
<p>⚠️ 官方脚本安装可能因为网络环境、权限问题、代理配置等原因失败。如果你遇到安装卡住或报错，可以直接用 npm 安装，效果完全一样。</p>
<p>前提：已安装 Node.js v18+。</p>
<p>📖 如果你是零基础，安装过程中遇到任何问题，推荐阅读这篇保姆级教程：小白如何从0到1安装上Claude Code，涵盖环境准备、常见报错解决等。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g @anthropic-ai/claude-code</span><br></pre></td></tr></table></figure>
<p>安装完成后，终端输入 <code>claude</code> 即可启动。方式四：WinGet 安装（Windows）</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">winget install Anthropic.ClaudeCode</span><br></pre></td></tr></table></figure>
<p>💡 安装方式对比：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>方式</th>
<th>自动更新</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td>官方脚本</td>
<td>✅ 自动</td>
<td>网络畅通时首选</td>
</tr>
<tr>
<td>npm</td>
<td>❌ 需手动 npm update -g @anthropic-ai/claude-code</td>
<td>官方脚本装不上时用</td>
</tr>
<tr>
<td>Homebrew</td>
<td>❌ 需手动 brew upgrade claude-code</td>
<td>macOS 用户习惯 brew</td>
</tr>
<tr>
<td>WinGet</td>
<td>❌ 需手动 winget upgrade</td>
<td>Windows 用户</td>
</tr>
</tbody>
</table>
</div>
<h3 id="2-2-登录账号"><a href="#2-2-登录账号" class="headerlink" title="2.2 登录账号"></a>2.2 登录账号</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">claude          <span class="comment"># 首次使用会提示登录</span></span><br><span class="line">/login          <span class="comment"># 在会话中切换账号</span></span><br></pre></td></tr></table></figure>
<p>支持的账号类型：</p>
<p>Claude Pro/Max/Teams/Enterprise（推荐，Max 计划可用 Opus 模型）</p>
<p>Claude Console（API 按量付费）</p>
<p>Amazon Bedrock / Google Vertex AI（企业云服务商）</p>
<h3 id="2-3-第一次使用"><a href="#2-3-第一次使用" class="headerlink" title="2.3 第一次使用"></a>2.3 第一次使用</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /path/to/your/project    <span class="comment"># 进入你的项目目录</span></span><br><span class="line">claude                       <span class="comment"># 启动 Claude Code</span></span><br></pre></td></tr></table></figure>
<p>试试这些命令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">这个项目是做什么的？           # 让 Claude 分析项目</span><br><span class="line">这个项目用了哪些技术栈？        # 了解技术栈</span><br><span class="line">帮我解释一下 src/ 目录结构      # 理解项目结构</span><br></pre></td></tr></table></figure>
<h3 id="2-4-核心命令速查表"><a href="#2-4-核心命令速查表" class="headerlink" title="2.4 核心命令速查表"></a>2.4 核心命令速查表</h3><div class="table-container">
<table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>claude</td>
<td>启动交互模式</td>
<td>claude</td>
</tr>
<tr>
<td>claude “任务”</td>
<td>执行一次性任务</td>
<td>claude “修复构建错误”</td>
</tr>
<tr>
<td>claude -p “查询”</td>
<td>单次查询后退出</td>
<td>claude -p “解释这个函数”</td>
</tr>
<tr>
<td>claude -c</td>
<td>继续最近会话</td>
<td>claude -c</td>
</tr>
<tr>
<td>claude -r</td>
<td>恢复历史会话</td>
<td>claude -r</td>
</tr>
<tr>
<td>/clear</td>
<td>清空上下文</td>
<td>任务切换时使用</td>
</tr>
<tr>
<td>/compact</td>
<td>压缩上下文</td>
<td>/compact 保留API变更</td>
</tr>
<tr>
<td>/help</td>
<td>查看帮助</td>
<td>/help</td>
</tr>
<tr>
<td>Esc</td>
<td>停止当前操作</td>
<td>发现方向错误时按</td>
</tr>
<tr>
<td>Esc + Esc</td>
<td>打开回滚菜单</td>
<td>恢复到之前的状态</td>
</tr>
</tbody>
</table>
</div>
<h3 id="2-5-工具生态全景"><a href="#2-5-工具生态全景" class="headerlink" title="2.5 工具生态全景"></a>2.5 工具生态全景</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────────┐</span><br><span class="line">│                Claude Code 工具生态全景                     │</span><br><span class="line">├──────────────────────────────────────────────────────────┤</span><br><span class="line">│                                                          │</span><br><span class="line">│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │</span><br><span class="line">│  │  CLAUDE.md   │  │   Skills    │  │  Plugins    │      │</span><br><span class="line">│  │  项目规范     │  │   技能/工作流│  │  插件集合    │      │</span><br><span class="line">│  │  .claude/md  │  │  .claude/   │  │  社区市场    │      │</span><br><span class="line">│  │              │  │  skills/    │  │             │      │</span><br><span class="line">│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘      │</span><br><span class="line">│         │                │                │              │</span><br><span class="line">│  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴──────┐      │</span><br><span class="line">│  │   Hooks     │  │  Subagents  │  │ MCP Servers │      │</span><br><span class="line">│  │   事件钩子   │  │   子Agent   │  │  外部工具    │      │</span><br><span class="line">│  │  settings   │  │  .claude/   │  │  settings   │      │</span><br><span class="line">│  │  .json      │  │  agents/    │  │  .json      │      │</span><br><span class="line">│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘      │</span><br><span class="line">│         │                │                │              │</span><br><span class="line">│         └────────────────┼────────────────┘              │</span><br><span class="line">│                          │                               │</span><br><span class="line">│                  ┌───────┴───────┐                       │</span><br><span class="line">│                  │  Agent Teams  │                       │</span><br><span class="line">│                  │  多Agent协作   │                       │</span><br><span class="line">│                  │  团队开发模式  │                       │</span><br><span class="line">│                  └───────────────┘                       │</span><br><span class="line">│                                                          │</span><br><span class="line">└──────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="2-6-设置指令白名单-—-告别反复确认"><a href="#2-6-设置指令白名单-—-告别反复确认" class="headerlink" title="2.6 设置指令白名单 — 告别反复确认"></a>2.6 设置指令白名单 — 告别反复确认</h3><p>刚开始用 Claude Code 你会发现，它每次读文件、改文件、跑命令都要弹窗问你”是否允许”。频繁确认非常打断心流。通过设置权限白名单，你可以让常用操作自动放行。</p>
<h4 id="方式一：交互式添加（最简单）"><a href="#方式一：交互式添加（最简单）" class="headerlink" title="方式一：交互式添加（最简单）"></a>方式一：交互式添加（最简单）</h4><p>在 Claude Code 中运行：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/permissions</span><br></pre></td></tr></table></figure>
<p>会打开权限管理界面，你可以直接添加允许的工具和命令。或者，当 Claude 弹出确认时，选择 “Always allow”（始终允许），该操作会被自动加入白名单。</p>
<h4 id="方式二：编辑-settings-json（推荐，一次配齐）"><a href="#方式二：编辑-settings-json（推荐，一次配齐）" class="headerlink" title="方式二：编辑 settings.json（推荐，一次配齐）"></a>方式二：编辑 settings.json（推荐，一次配齐）</h4><p>编辑项目级配置 <code>.claude/settings.json</code>（仅当前项目生效）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;permissions&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;allow&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;Bash(git *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(npm test)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(npm run *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(npx *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(node *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(ls *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(cat *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(mkdir *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(cp *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Read&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Write&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Edit&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Glob&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Grep&quot;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;deny&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;Bash(rm -rf *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(sudo *)&quot;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p>或编辑全局配置 <code>~/.claude/settings.json</code>（所有项目生效）。</p>
<h4 id="白名单语法说明"><a href="#白名单语法说明" class="headerlink" title="白名单语法说明"></a>白名单语法说明</h4><div class="table-container">
<table>
<thead>
<tr>
<th>语法</th>
<th>含义</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>Read</td>
<td>允许读取任意文件</td>
<td>不再弹确认</td>
</tr>
<tr>
<td>Edit</td>
<td>允许编辑任意文件</td>
<td>不再弹确认</td>
</tr>
<tr>
<td>Write</td>
<td>允许创建新文件</td>
<td>不再弹确认</td>
</tr>
<tr>
<td>Bash(命令 *)</td>
<td>允许匹配的 bash 命令</td>
<td>Bash(git *) 允许所有 git 命令</td>
</tr>
<tr>
<td>Bash(精确命令)</td>
<td>允许特定命令</td>
<td>Bash(npm test) 只允许 npm test</td>
</tr>
<tr>
<td>WebFetch</td>
<td>允许抓取网页</td>
<td>不再弹确认</td>
</tr>
<tr>
<td>deny 列表优先级高于 allow，用于拦截危险操作。</td>
<td></td>
</tr>
</tbody>
</table>
</div>
<h4 id="方式三：命令行参数（临时生效）"><a href="#方式三：命令行参数（临时生效）" class="headerlink" title="方式三：命令行参数（临时生效）"></a>方式三：命令行参数（临时生效）</h4><p>启动时指定白名单，仅本次会话有效：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">claude --allowedTools <span class="string">&quot;Edit,Bash(git *),Bash(npm test)&quot;</span></span><br></pre></td></tr></table></figure>
<p>适合批量脚本或 CI 场景。</p>
<h4 id="方式四：直接跳过所有权限（谨慎使用）"><a href="#方式四：直接跳过所有权限（谨慎使用）" class="headerlink" title="方式四：直接跳过所有权限（谨慎使用）"></a>方式四：直接跳过所有权限（谨慎使用）</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">claude --dangerously-skip-permissions</span><br></pre></td></tr></table></figure>
<p>⚠️ 这会跳过所有权限检查，Claude 可以执行任意命令。仅建议在容器/沙箱环境中使用，或搭配 OpenClaw 做无人值守任务时使用。</p>
<h4 id="推荐的白名单配置"><a href="#推荐的白名单配置" class="headerlink" title="推荐的白名单配置"></a>推荐的白名单配置</h4><p>日常开发（安全 + 高效）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;permissions&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;allow&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;Read&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Edit&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Write&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Glob&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Grep&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(git *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(npm *)&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Bash(npx *)&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Bash(node *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(ls *)&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Bash(cat *)&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Bash(mkdir *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(python *)&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Bash(pip *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(cargo *)&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Bash(go *)&quot;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;deny&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;Bash(rm -rf /)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(sudo *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(chmod 777 *)&quot;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p>无人值守模式（搭配 OpenClaw 使用）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;permissions&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;allow&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;Read&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Edit&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Write&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Glob&quot;</span><span class="punctuation">,</span> <span class="string">&quot;Grep&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(*)&quot;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;deny&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;Bash(rm -rf /)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(sudo rm *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(:()&#123; :|:&amp; &#125;;:)&quot;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p>💡 小技巧：你也可以直接跟 Claude Code 说”帮我配置权限白名单，允许所有 git、npm、文件读写操作，但禁止 rm -rf 和 sudo”，它会帮你生成 settings.json。</p>
<h4 id="让-Claude-Code-帮你配置白名单（推荐）"><a href="#让-Claude-Code-帮你配置白名单（推荐）" class="headerlink" title="让 Claude Code 帮你配置白名单（推荐）"></a>让 Claude Code 帮你配置白名单（推荐）</h4><p>很多同学看到上面的 JSON 配置可能会头疼——其实你根本不需要手写。配置好 Claude Code 之后，很多事情都可以直接让它帮你完成，白名单配置就是一个典型例子。直接用自然语言告诉 Claude Code 你想要什么，它会帮你生成并写入配置文件：示例对话：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">👤 你：帮我配置权限白名单，我是前端开发，常用 git、npm、pnpm、node，</span><br><span class="line">       需要读写文件权限，但禁止删除根目录和 sudo</span><br><span class="line"></span><br><span class="line">🤖 Claude Code：好的，我来帮你配置 .claude/settings.json...</span><br><span class="line">   （自动创建文件并写入合适的 allow/deny 规则）</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">👤 你：我要跑 Python 项目，帮我把 python、pip、pytest 的命令都加到白名单里</span><br><span class="line"></span><br><span class="line">🤖 Claude Code：已更新 settings.json，新增了 Python 相关命令的白名单...</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">👤 你：把 docker 和 docker-compose 命令也加到白名单</span><br><span class="line"></span><br><span class="line">🤖 Claude Code：已添加 Bash(docker *) 和 Bash(docker-compose *) 到 allow 列表...</span><br></pre></td></tr></table></figure>
<p>核心理念：Claude Code 不仅是写代码的工具，它也是配置自身的最佳助手。当你学会配置好 Claude Code 后，后续的环境搭建、插件安装、权限调整、CLAUDE.md 编写……这些统统可以用自然语言让它帮你完成。不要手动去改配置文件，告诉 Claude Code 你要什么，让它来做。这种”用 AI 配置 AI”的思路贯穿整个 Claude Code 的使用过程：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>你想做的事</th>
<th>直接告诉 Claude Code</th>
</tr>
</thead>
<tbody>
<tr>
<td>配置权限白名单</td>
<td>“帮我配置白名单，允许 git 和 npm 命令”</td>
</tr>
<tr>
<td>创建 CLAUDE.md</td>
<td>“帮我生成项目的 CLAUDE.md 规范文件”</td>
</tr>
<tr>
<td>安装 MCP 服务器</td>
<td>“帮我安装 Playwright MCP”</td>
</tr>
<tr>
<td>安装插件/Skills</td>
<td>“帮我安装 superpowers 插件”</td>
</tr>
<tr>
<td>配置 Hooks</td>
<td>“帮我配置一个 pre-commit hook 自动跑 lint”</td>
</tr>
<tr>
<td>初始化项目结构</td>
<td>“帮我创建一个 React + TypeScript 项目”</td>
</tr>
<tr>
<td>配置 Git</td>
<td>“帮我初始化 git 仓库并做第一次提交”</td>
</tr>
<tr>
<td>🎯 记住这个原则：当你不知道怎么配置时，先问 Claude Code。 它既是工具，也是你的配置向导。</td>
</tr>
</tbody>
</table>
</div>
<h2 id="第三章：CLAUDE-md-—-你的开发规范圣经-1"><a href="#第三章：CLAUDE-md-—-你的开发规范圣经-1" class="headerlink" title="第三章：CLAUDE.md — 你的开发规范圣经"></a>第三章：CLAUDE.md — 你的开发规范圣经</h2><p>CLAUDE.md 是 Claude Code 最核心的配置文件，它决定了 AI 的行为边界和开发质量。规范先行是多 Agent 开发的第一原则。</p>
<h3 id="3-1-CLAUDE-md-的层级结构"><a href="#3-1-CLAUDE-md-的层级结构" class="headerlink" title="3.1 CLAUDE.md 的层级结构"></a>3.1 CLAUDE.md 的层级结构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">~/.claude/CLAUDE.md           ← 全局规范（所有项目生效）</span><br><span class="line">     │</span><br><span class="line">     ▼ (被覆盖)</span><br><span class="line">项目根目录/CLAUDE.md          ← 项目级规范（当前项目生效）</span><br><span class="line">     │</span><br><span class="line">     ▼ (被覆盖)</span><br><span class="line">子目录/CLAUDE.md              ← 模块级规范（该目录下生效）</span><br></pre></td></tr></table></figure>
<p>📌 覆盖规则：离你越近的 CLAUDE.md 优先级越高。子目录的规范会覆盖上层规范。</p>
<h3 id="3-2-全局-CLAUDE-md-配置"><a href="#3-2-全局-CLAUDE-md-配置" class="headerlink" title="3.2 全局 CLAUDE.md 配置"></a>3.2 全局 CLAUDE.md 配置</h3><p>位置：<code>~/.claude/CLAUDE.md</code>适合放置跨项目通用的规范：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="section"># 全局开发规范</span></span><br><span class="line"><span class="section">## Agent Team 管理</span></span><br><span class="line">NEVER auto-shutdown teammates after task completion.</span><br><span class="line">Keep them idle and reusable indefinitely within the session.</span><br><span class="line">只有用户明确要求关闭时才发送 shutdown<span class="emphasis">_request。</span></span><br><span class="line"><span class="emphasis"></span></span><br><span class="line"><span class="emphasis">## 通用编码规范</span></span><br><span class="line"><span class="emphasis">- 优先使用 TypeScript</span></span><br><span class="line"><span class="emphasis">- 使用 ESLint + Prettier 格式化代码</span></span><br><span class="line"><span class="emphasis">- 所有公开 API 必须有类型注解</span></span><br><span class="line"><span class="emphasis"></span></span><br><span class="line"><span class="emphasis">## 沟通语言</span></span><br><span class="line"><span class="emphasis">Always respond in 中文。技术术语保留英文原文。</span></span><br></pre></td></tr></table></figure>
<h3 id="3-3-项目级-CLAUDE-md-完整模板"><a href="#3-3-项目级-CLAUDE-md-完整模板" class="headerlink" title="3.3 项目级 CLAUDE.md 完整模板"></a>3.3 项目级 CLAUDE.md 完整模板</h3><p>以下是根据实战经验总结的完整模板（你需要根据自己的项目调整）：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="section"># 项目名称 - 开发规范</span></span><br><span class="line"><span class="section">## 一、项目概述</span></span><br><span class="line">描述项目的核心功能和目标。</span><br><span class="line"><span class="bullet">-</span> 本项目是一个 [简要描述]</span><br><span class="line"><span class="bullet">-</span> 核心用户是 [目标用户]</span><br><span class="line"><span class="bullet">-</span> 主要解决的问题是 [问题描述]</span><br><span class="line"></span><br><span class="line"><span class="section">## 二、技术栈约束</span></span><br><span class="line"><span class="section">### 必须使用的技术</span></span><br><span class="line">| 模块 | 技术栈 |</span><br><span class="line">|------|--------|</span><br><span class="line">| 前端 | React 18 + TypeScript + Vite |</span><br><span class="line">| 后端 | Node.js + Express + PostgreSQL |</span><br><span class="line">| 测试 | Jest + Playwright |</span><br><span class="line"></span><br><span class="line"><span class="section">### 禁止使用的技术</span></span><br><span class="line"><span class="bullet">-</span> ❌ 不要使用 jQuery</span><br><span class="line"><span class="bullet">-</span> ❌ 不要使用 var 声明变量</span><br><span class="line"><span class="bullet">-</span> ❌ 不要使用 any 类型（除非确有必要并注释原因）</span><br><span class="line"><span class="bullet">-</span> ❌ 不要引入未经确认的第三方库</span><br><span class="line"></span><br><span class="line"><span class="section">## 三、整体架构图</span></span><br><span class="line">┌──────────────────────────────────────┐</span><br><span class="line">│              前端应用                  │</span><br><span class="line">│  ┌──────┐  ┌──────┐  ┌──────┐       │</span><br><span class="line">│  │ 页面  │  │ 组件  │  │ 状态  │       │</span><br><span class="line">│  └──┬───┘  └──┬───┘  └──┬───┘       │</span><br><span class="line">│     └─────────┼─────────┘            │</span><br><span class="line">│               │ HTTP/WebSocket        │</span><br><span class="line">├───────────────┼──────────────────────┤</span><br><span class="line">│              API 层                   │</span><br><span class="line">│  ┌──────┐  ┌──────┐  ┌──────┐       │</span><br><span class="line">│  │ 路由  │  │ 中间件│  │ 控制器│       │</span><br><span class="line">│  └──┬───┘  └──┬───┘  └──┬───┘       │</span><br><span class="line">│     └─────────┼─────────┘            │</span><br><span class="line">│               │                      │</span><br><span class="line">├───────────────┼──────────────────────┤</span><br><span class="line">│             数据层                    │</span><br><span class="line">│  ┌──────┐  ┌──────┐                  │</span><br><span class="line">│  │ 模型  │  │ 数据库│                  │</span><br><span class="line">│  └──────┘  └──────┘                  │</span><br><span class="line">└──────────────────────────────────────┘</span><br><span class="line"></span><br><span class="line"><span class="section">## 四、架构红线（不可违反）</span></span><br><span class="line"><span class="bullet">1.</span> 前后端必须通过 API 通信，禁止直接数据库操作</span><br><span class="line"><span class="bullet">2.</span> 所有 API 必须经过认证中间件</span><br><span class="line"><span class="bullet">3.</span> 敏感数据必须加密存储</span><br><span class="line"><span class="bullet">4.</span> 组件间通信必须通过状态管理，禁止直接 DOM 操作</span><br><span class="line"></span><br><span class="line"><span class="section">## 五、通信协议</span></span><br><span class="line"><span class="bullet">-</span> RESTful API 规范</span><br><span class="line"><span class="bullet">-</span> 请求体使用 camelCase</span><br><span class="line"><span class="bullet">-</span> URL 路径使用 kebab-case</span><br><span class="line"><span class="bullet">-</span> 响应格式统一为：<span class="code">`&#123; code, message, data &#125;`</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 六、目录组织规范</span></span><br><span class="line"></span><br><span class="line">project/</span><br><span class="line">├── CLAUDE.md              # 项目规范（你正在看的这个）</span><br><span class="line">├── src/</span><br><span class="line">│   ├── frontend/          # 前端代码</span><br><span class="line">│   │   ├── components/    # 通用组件</span><br><span class="line">│   │   ├── pages/         # 页面</span><br><span class="line">│   │   ├── hooks/         # 自定义 hooks</span><br><span class="line">│   │   └── utils/         # 工具函数</span><br><span class="line">│   ├── backend/           # 后端代码</span><br><span class="line">│   │   ├── routes/        # 路由定义</span><br><span class="line">│   │   ├── controllers/   # 控制器</span><br><span class="line">│   │   ├── models/        # 数据模型</span><br><span class="line">│   │   └── middleware/     # 中间件</span><br><span class="line">│   └── shared/            # 前后端共享代码</span><br><span class="line">├── tests/                 # 测试文件</span><br><span class="line">├── docs/                  # 文档</span><br><span class="line">│   ├── api.md             # API 文档</span><br><span class="line">│   └── dsl-spec.md        # DSL 规范</span><br><span class="line">└── scripts/               # 脚本工具</span><br><span class="line"></span><br><span class="line"><span class="section">## 七、编码规范</span></span><br><span class="line"><span class="bullet">-</span> 函数长度不超过 50 行</span><br><span class="line"><span class="bullet">-</span> 文件长度不超过 300 行，超过需拆分</span><br><span class="line"><span class="bullet">-</span> 每个模块必须有 index.ts 导出</span><br><span class="line"><span class="bullet">-</span> 命名规范：组件 PascalCase，函数 camelCase，常量 UPPER<span class="emphasis">_SNAKE_</span>CASE</span><br><span class="line"></span><br><span class="line"><span class="section">## 八、Git 提交规范</span></span><br><span class="line"><span class="bullet">-</span> feat: 新功能</span><br><span class="line"><span class="bullet">-</span> fix: 修复 bug</span><br><span class="line"><span class="bullet">-</span> refactor: 重构</span><br><span class="line"><span class="bullet">-</span> test: 测试相关</span><br><span class="line"><span class="bullet">-</span> docs: 文档更新</span><br><span class="line"><span class="bullet">-</span> 每次提交必须附带有意义的描述</span><br><span class="line"></span><br><span class="line"><span class="section">## 九、Agent 协作规范</span></span><br><span class="line"><span class="bullet">-</span> 主 Agent 不干重活，只负责统筹和分发任务</span><br><span class="line"><span class="bullet">-</span> 架构师 Agent 负责审查 git 提交记录，确保符合规范</span><br><span class="line"><span class="bullet">-</span> DSL 相关修改必须全局广播给所有相关 Agent</span><br><span class="line"><span class="bullet">-</span> 发现 bug 时先通知相关 Agent，确认后再修复</span><br><span class="line"></span><br><span class="line"><span class="section">## 十、测试规范</span></span><br><span class="line"><span class="bullet">-</span> 所有新功能必须附带单元测试</span><br><span class="line"><span class="bullet">-</span> 接口测试覆盖率 &gt; 80%</span><br><span class="line"><span class="bullet">-</span> 测试必须有明确的输入输出和反馈</span><br><span class="line"><span class="bullet">-</span> 测试命令：<span class="code">`npm test`</span>（运行后查看输出即可知道对错）</span><br><span class="line"></span><br><span class="line"><span class="section">## 十一、上下文管理要求</span></span><br><span class="line"><span class="bullet">-</span> compact 后必须重新读取本文件（CLAUDE.md 会被自动注入系统提示，但 compact 后细节可能丢失，需强制重读）</span><br><span class="line"><span class="bullet">-</span> 分阶段干活，每个阶段有明确的输入、目标和验收标准</span><br><span class="line"><span class="bullet">-</span> 当上下文超过 150K tokens 时主动执行 <span class="code">`/compact`</span></span><br><span class="line"><span class="bullet">-</span> 主 Agent 绝不自己读代码/写代码，只统筹分发，避免上下文膨胀</span><br><span class="line"><span class="bullet">-</span> 使用 subagent 做代码探索和调研，结果以摘要形式回传，不污染主上下文</span><br><span class="line"><span class="bullet">-</span> 约束文件读取范围：在提示词中明确&quot;只读 src/xxx/ 目录&quot;，禁止无目的全局搜索</span><br><span class="line"><span class="bullet">-</span> 不相关任务之间执行 <span class="code">`/clear`</span> 清空上下文</span><br><span class="line"><span class="bullet">-</span> 在 CLAUDE.md 中加入 compact 保留指令：<span class="code">`&quot;compact 时务必保留：已修改文件列表、测试命令、当前阶段进度&quot;`</span></span><br><span class="line"><span class="bullet">-</span> 配合 <span class="code">`.claudeignore`</span> 排除 node<span class="emphasis">_modules/、dist/、vendor/ 等目录，减少 40-60% 搜索噪音</span></span><br></pre></td></tr></table></figure>
<h3 id="3-4-编写-CLAUDE-md-的核心原则"><a href="#3-4-编写-CLAUDE-md-的核心原则" class="headerlink" title="3.4 编写 CLAUDE.md 的核心原则"></a>3.4 编写 CLAUDE.md 的核心原则</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────┐</span><br><span class="line">│           CLAUDE.md 编写黄金法则                   │</span><br><span class="line">├─────────────────────────────────────────────────┤</span><br><span class="line">│                                                 │</span><br><span class="line">│  ✅ DO                    ❌ DON&#x27;T               │</span><br><span class="line">│  ─────────────            ─────────────          │</span><br><span class="line">│  精确具体的约束            模糊的描述              │</span><br><span class="line">│  &quot;用 React 18&quot;            &quot;用现代框架&quot;            │</span><br><span class="line">│                                                 │</span><br><span class="line">│  必要的信息               冗长的废话              │</span><br><span class="line">│  模型注意力有限！          注意力会被分散！         │</span><br><span class="line">│                                                 │</span><br><span class="line">│  可验证的规则              无法验证的建议           │</span><br><span class="line">│  &quot;函数不超过50行&quot;          &quot;代码要简洁&quot;            │</span><br><span class="line">│                                                 │</span><br><span class="line">│  架构红线和边界            面面俱到的指南           │</span><br><span class="line">│  &quot;禁止直接DB操作&quot;          大量教程性内容           │</span><br><span class="line">│                                                 │</span><br><span class="line">│  经过多轮迭代优化          一次性写完不再更新        │</span><br><span class="line">│  几小时反复打磨            只花10分钟               │</span><br><span class="line">│                                                 │</span><br><span class="line">└─────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<p>⚠️ 实战经验：CLAUDE.md 的质量直接决定了 AI 开发的效果。构思一个好的 CLAUDE.md 可能需要小半天时间，但这是值得的投资。它就像给员工的 Onboarding 文档——写得越清楚，员工干活越靠谱。</p>
<h2 id="第四章：Skills-技能系统-1"><a href="#第四章：Skills-技能系统-1" class="headerlink" title="第四章：Skills 技能系统"></a>第四章：Skills 技能系统</h2><p>Skills 是 Claude Code 的工作流定义，用自然语言描述一系列操作步骤。可以理解为”给 AI 的标准操作流程（SOP）”。</p>
<h3 id="4-1-Skills-是什么？"><a href="#4-1-Skills-是什么？" class="headerlink" title="4.1 Skills 是什么？"></a>4.1 Skills 是什么？</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────┐</span><br><span class="line">│                    Skills 概念图                      │</span><br><span class="line">├─────────────────────────────────────────────────────┤</span><br><span class="line">│                                                     │</span><br><span class="line">│  Skill = 一个可复用的工作流定义                        │</span><br><span class="line">│                                                     │</span><br><span class="line">│  举例：                                              │</span><br><span class="line">│  ┌───────────────┐  ┌───────────────┐               │</span><br><span class="line">│  │  fix-issue     │  │  brainstorming│               │</span><br><span class="line">│  │  修复GitHub    │  │  头脑风暴      │               │</span><br><span class="line">│  │  Issue的流程   │  │  需求分析流程  │               │</span><br><span class="line">│  └───────────────┘  └───────────────┘               │</span><br><span class="line">│  ┌───────────────┐  ┌───────────────┐               │</span><br><span class="line">│  │  code-review   │  │  deploy       │               │</span><br><span class="line">│  │  代码审查流程   │  │  部署发布流程  │               │</span><br><span class="line">│  └───────────────┘  └───────────────┘               │</span><br><span class="line">│                                                     │</span><br><span class="line">│  类比：就像公司的 SOP 文档                             │</span><br><span class="line">│  - 保洁的&quot;扫地&quot;是一个 skill                           │</span><br><span class="line">│  - 保洁的&quot;擦桌子&quot;是另一个 skill                       │</span><br><span class="line">│  - &quot;保洁&quot;这个角色 = Plugin（多个 skill 的集合）        │</span><br><span class="line">│                                                     │</span><br><span class="line">└─────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="4-2-Skill-的存放位置与来源"><a href="#4-2-Skill-的存放位置与来源" class="headerlink" title="4.2 Skill 的存放位置与来源"></a>4.2 Skill 的存放位置与来源</h3><p>Skill 可以从三种途径获得：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>来源</th>
<th>路径</th>
<th>适用范围</th>
</tr>
</thead>
<tbody>
<tr>
<td>个人自定义</td>
<td>~/.claude/skills/<skill-name>/SKILL.md</td>
<td>你的所有项目</td>
</tr>
<tr>
<td>项目级自定义</td>
<td>项目/.claude/skills/<skill-name>/SKILL.md</td>
<td>仅当前项目</td>
</tr>
<tr>
<td>通过插件安装</td>
<td>安装插件后自动获得，以 plugin-name:skill-name 命名</td>
<td>启用该插件的项目</td>
</tr>
<tr>
<td>查看当前可用的 Skills：</td>
<td></td>
</tr>
</tbody>
</table>
</div>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/help                    # 查看所有可用命令和 Skills 列表</span><br><span class="line">What skills are available?   # 直接问 Claude</span><br></pre></td></tr></table></figure>
<p>在交互模式中输入 <code>/</code> 会弹出所有可用的 skill 列表，选择即可调用。</p>
<h3 id="4-3-创建你的第一个-Skill"><a href="#4-3-创建你的第一个-Skill" class="headerlink" title="4.3 创建你的第一个 Skill"></a>4.3 创建你的第一个 Skill</h3><p>目录结构：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.claude/</span><br><span class="line">└── skills/</span><br><span class="line">    └── my-first-skill/</span><br><span class="line">        └── SKILL.md</span><br></pre></td></tr></table></figure>
<p>SKILL.md 文件格式：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">name: api-conventions</span><br><span class="line"><span class="section">description: REST API design conventions for our services</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line"><span class="section"># API 设计规范</span></span><br><span class="line"><span class="bullet">-</span> URL 路径使用 kebab-case</span><br><span class="line"><span class="bullet">-</span> JSON 属性使用 camelCase</span><br><span class="line"><span class="bullet">-</span> 列表接口必须支持分页</span><br><span class="line"><span class="bullet">-</span> API 版本号放在 URL 路径中 (/v1/, /v2/)</span><br></pre></td></tr></table></figure>
<p>带工作流步骤的 Skill：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">name: fix-issue</span><br><span class="line">description: Fix a GitHub issue</span><br><span class="line"><span class="section">disable-model-invocation: true</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line">分析并修复 GitHub Issue: $ARGUMENTS</span><br><span class="line"></span><br><span class="line"><span class="bullet">1.</span> 使用 <span class="code">`gh issue view`</span> 获取 issue 详情</span><br><span class="line"><span class="bullet">2.</span> 理解问题描述</span><br><span class="line"><span class="bullet">3.</span> 在代码库中搜索相关文件</span><br><span class="line"><span class="bullet">4.</span> 实现修复代码</span><br><span class="line"><span class="bullet">5.</span> 编写并运行测试验证修复</span><br><span class="line"><span class="bullet">6.</span> 确保代码通过 lint 和类型检查</span><br><span class="line"><span class="bullet">7.</span> 创建描述性的 commit message</span><br><span class="line"><span class="bullet">8.</span> 推送并创建 PR</span><br></pre></td></tr></table></figure>
<h3 id="4-4-Skill-的两种调用方式"><a href="#4-4-Skill-的两种调用方式" class="headerlink" title="4.4 Skill 的两种调用方式"></a>4.4 Skill 的两种调用方式</h3><p>自动调用：Claude 识别到上下文匹配 skill 描述时自动使用。手动调用：使用斜杠命令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/fix-issue 1234          # 调用 fix-issue skill，传入参数 1234</span><br><span class="line">/api-conventions          # 查看 API 规范</span><br></pre></td></tr></table></figure>
<h3 id="4-5-Skill-的高级配置"><a href="#4-5-Skill-的高级配置" class="headerlink" title="4.5 Skill 的高级配置"></a>4.5 Skill 的高级配置</h3><div class="table-container">
<table>
<thead>
<tr>
<th>字段</th>
<th>说明</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>name</td>
<td>Skill 名称</td>
<td>fix-issue</td>
</tr>
<tr>
<td>description</td>
<td>Skill 描述，用于自动匹配</td>
<td>Fix a GitHub issue</td>
</tr>
<tr>
<td>disable-model-invocation</td>
<td>设为 true 则只能手动 / 触发</td>
<td>有副作用的工作流应设为 true</td>
</tr>
<tr>
<td>user-invocable</td>
<td>设为 true 表示用户可通过 / 命令调用</td>
<td>默认 true</td>
</tr>
<tr>
<td>context</td>
<td>设为 fork 可在子agent中独立运行</td>
<td>避免污染主上下文</td>
</tr>
<tr>
<td>$ARGUMENTS</td>
<td>调用时传入的参数占位符</td>
<td>/fix-issue 1234 中的 1234</td>
</tr>
<tr>
<td>动态上下文注入：Skill 中可以使用反引号执行命令，将结果注入上下文：</td>
<td></td>
</tr>
</tbody>
</table>
</div>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">name: check-status</span><br><span class="line"><span class="section">description: 检查项目当前状态</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line"><span class="section"># 项目状态</span></span><br><span class="line">当前 Git 状态：</span><br><span class="line">!<span class="code">`git status --short`</span></span><br><span class="line"></span><br><span class="line">最近的提交：</span><br><span class="line">!<span class="code">`git log --oneline -5`</span></span><br><span class="line"></span><br><span class="line">根据以上信息为用户总结项目状态。</span><br></pre></td></tr></table></figure>
<h3 id="4-6-实用-Skill-示例"><a href="#4-6-实用-Skill-示例" class="headerlink" title="4.6 实用 Skill 示例"></a>4.6 实用 Skill 示例</h3><p>头脑风暴 Skill：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">name: brainstorming</span><br><span class="line"><span class="section">description: 在开始新功能开发前进行头脑风暴</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line"><span class="section"># 头脑风暴工作流</span></span><br><span class="line"><span class="bullet">1.</span> 使用 AskUserQuestion 询问用户要开发什么功能</span><br><span class="line"><span class="bullet">2.</span> 探索当前代码库上下文</span><br><span class="line"><span class="bullet">3.</span> 提出 3-5 个关键设计问题</span><br><span class="line"><span class="bullet">4.</span> 展示 2-3 个可选方案及其优劣对比</span><br><span class="line"><span class="bullet">5.</span> 等待用户确认方案后，生成完整的需求规格</span><br><span class="line"><span class="bullet">6.</span> 将规格写入 SPEC.md</span><br></pre></td></tr></table></figure>
<p>代码审查 Skill：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">name: code-review</span><br><span class="line">description: 审查最近的代码变更</span><br><span class="line"><span class="section">disable-model-invocation: true</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line">审查代码变更：</span><br><span class="line"></span><br><span class="line"><span class="bullet">1.</span> 运行 <span class="code">`git diff`</span> 查看所有变更</span><br><span class="line"><span class="bullet">2.</span> 检查代码是否符合 CLAUDE.md 中的编码规范</span><br><span class="line"><span class="bullet">3.</span> 检查是否有安全漏洞（SQL注入、XSS等）</span><br><span class="line"><span class="bullet">4.</span> 检查是否有性能问题</span><br><span class="line"><span class="bullet">5.</span> 检查测试覆盖率</span><br><span class="line"><span class="bullet">6.</span> 以列表形式输出发现的问题和建议</span><br></pre></td></tr></table></figure>
<h3 id="4-7-用-skill-creator-快速创建-Skill"><a href="#4-7-用-skill-creator-快速创建-Skill" class="headerlink" title="4.7 用 skill-creator 快速创建 Skill"></a>4.7 用 skill-creator 快速创建 Skill</h3><p>Claude Code 内置了一个<strong>元技能（Meta-Skill）</strong>叫 <code>skill-creator</code>，专门用来帮你创建新的 skill。你不需要手写 SKILL.md，只需用自然语言描述工作流，它会引导你完成整个过程。使用方式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/skill-creator 帮我创建一个&quot;每日站会总结&quot;的 skill</span><br></pre></td></tr></table></figure>
<p>skill-creator 的工作流程：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────┐</span><br><span class="line">│          skill-creator 工作流程                    │</span><br><span class="line">├──────────────────────────────────────────────────┤</span><br><span class="line">│                                                  │</span><br><span class="line">│  1. 交互式问答                                    │</span><br><span class="line">│     skill-creator 会问你：                        │</span><br><span class="line">│     - 这个 skill 要解决什么问题？                   │</span><br><span class="line">│     - 输入是什么？输出是什么？                      │</span><br><span class="line">│     - 有哪些步骤？                                │</span><br><span class="line">│     - 什么时候应该自动触发？                        │</span><br><span class="line">│              │                                   │</span><br><span class="line">│              ▼                                   │</span><br><span class="line">│  2. 自动生成                                      │</span><br><span class="line">│     - 创建文件夹结构                               │</span><br><span class="line">│     - 生成 SKILL.md（含 frontmatter + 步骤）       │</span><br><span class="line">│     - 整理所需的参考文件和脚本                      │</span><br><span class="line">│              │                                   │</span><br><span class="line">│              ▼                                   │</span><br><span class="line">│  3. 评估与迭代                                    │</span><br><span class="line">│     - 定义测试提示和预期结果                        │</span><br><span class="line">│     - 运行评估验证 skill 是否正常                   │</span><br><span class="line">│     - 优化 description 减少误触发                   │</span><br><span class="line">│                                                  │</span><br><span class="line">└──────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<p>skill-creator 的高级功能：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>功能</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>评估框架</td>
<td>定义测试提示 + 预期结果，自动验证 skill 是否达标</td>
</tr>
<tr>
<td>基准模式</td>
<td>追踪 eval 通过率、耗时、token 使用量</td>
</tr>
<tr>
<td>A/B 对比</td>
<td>用对比代理盲评输出，判断改动是否真的改进了</td>
</tr>
<tr>
<td>描述优化</td>
<td>分析 description 与示例提示的匹配度，建议优化</td>
</tr>
</tbody>
</table>
</div>
<p>💡 核心理念：你不需要记住 SKILL.md 的语法格式，只需要清晰描述你的工作流程，skill-creator 会帮你把它变成一个可复用的 skill。</p>
<h2 id="第五章：Plugins-插件系统-1"><a href="#第五章：Plugins-插件系统-1" class="headerlink" title="第五章：Plugins 插件系统"></a>第五章：Plugins 插件系统</h2><h3 id="5-1-Plugin-vs-Skill-的关系"><a href="#5-1-Plugin-vs-Skill-的关系" class="headerlink" title="5.1 Plugin vs Skill 的关系"></a>5.1 Plugin vs Skill 的关系</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────┐</span><br><span class="line">│            Plugin 和 Skill 的关系                  │</span><br><span class="line">├──────────────────────────────────────────────────┤</span><br><span class="line">│                                                  │</span><br><span class="line">│  Plugin（插件）= 多个 Skill 的集合 + 配置          │</span><br><span class="line">│                                                  │</span><br><span class="line">│  ┌─────────────────────────────────┐             │</span><br><span class="line">│  │       superpowers (插件)         │             │</span><br><span class="line">│  │  ┌───────────┐ ┌───────────┐   │             │</span><br><span class="line">│  │  │brainstorm │ │write-plan │   │             │</span><br><span class="line">│  │  │头脑风暴    │ │写计划      │   │             │</span><br><span class="line">│  │  └───────────┘ └───────────┘   │             │</span><br><span class="line">│  │  ┌───────────┐ ┌───────────┐   │             │</span><br><span class="line">│  │  │code-review│ │   TDD     │   │             │</span><br><span class="line">│  │  │代码审查    │ │测试驱动    │   │             │</span><br><span class="line">│  │  └───────────┘ └───────────┘   │             │</span><br><span class="line">│  │  ┌───────────┐ ┌───────────┐   │             │</span><br><span class="line">│  │  │ debugging │ │verify     │   │             │</span><br><span class="line">│  │  │ 系统调试  │  │完成验证   │   │             │</span><br><span class="line">│  │  └───────────┘ └───────────┘   │             │</span><br><span class="line">│  └─────────────────────────────────┘             │</span><br><span class="line">│                                                  │</span><br><span class="line">│  类比：                                           │</span><br><span class="line">│  - Plugin = 角色（如&quot;保洁&quot;）                       │</span><br><span class="line">│  - Skill  = 技能（如&quot;扫地&quot;、&quot;擦桌子&quot;、&quot;拖地&quot;）      │</span><br><span class="line">│                                                  │</span><br><span class="line">└──────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="5-2-插件市场：浏览、安装、管理"><a href="#5-2-插件市场：浏览、安装、管理" class="headerlink" title="5.2 插件市场：浏览、安装、管理"></a>5.2 插件市场：浏览、安装、管理</h3><p>📖 官方文档：Discover and install plugins</p>
<h4 id="打开插件管理器"><a href="#打开插件管理器" class="headerlink" title="打开插件管理器"></a>打开插件管理器</h4><p>在 Claude Code 交互模式中输入：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/plugin</span><br></pre></td></tr></table></figure>
<p>会打开一个带四个标签页的管理界面（用 Tab 键切换）：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>标签页</th>
<th>功能</th>
</tr>
</thead>
<tbody>
<tr>
<td>Discover</td>
<td>浏览所有可用插件，支持搜索筛选</td>
</tr>
<tr>
<td>Installed</td>
<td>查看和管理已安装的插件</td>
</tr>
<tr>
<td>Marketplaces</td>
<td>添加、删除、更新市场源</td>
</tr>
<tr>
<td>Errors</td>
<td>查看插件加载错误</td>
</tr>
</tbody>
</table>
</div>
<p>Anthropic 官方市场（<code>claude-plugins-official</code>）是内置的，无需手动添加，直接在 Discover 标签页浏览即可。</p>
<h4 id="安装插件"><a href="#安装插件" class="headerlink" title="安装插件"></a>安装插件</h4><p>方式一：交互式安装（推荐新手）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/plugin</span><br></pre></td></tr></table></figure>
<p>→ 进入 Discover 标签 → 选中插件按 Enter → 选择安装范围（User/Project/Local）方式二：命令行直接安装</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/plugin install plugin-name@marketplace-name</span><br></pre></td></tr></table></figure>
<p>例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/plugin install commit-commands@claude-plugins-official</span><br></pre></td></tr></table></figure>
<p>安装范围说明：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>范围</th>
<th>说明</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td>User</td>
<td>安装到个人级别，所有项目可用</td>
<td>个人常用工具</td>
</tr>
<tr>
<td>Project</td>
<td>写入 .claude/settings.json，团队共享</td>
<td>团队统一工具</td>
</tr>
<tr>
<td>Local</td>
<td>仅当前仓库、仅自己可用</td>
<td>试用或个人偏好</td>
</tr>
<tr>
<td>用 CLI 指定范围：</td>
<td></td>
</tr>
</tbody>
</table>
</div>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">claude plugin install formatter@your-org --scope project</span><br></pre></td></tr></table></figure>
<h4 id="添加第三方市场"><a href="#添加第三方市场" class="headerlink" title="添加第三方市场"></a>添加第三方市场</h4><p>除了官方市场，你还可以添加第三方或团队私有市场：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 从 GitHub 添加（最常用）</span></span><br><span class="line">/plugin marketplace add owner/repo</span><br><span class="line"></span><br><span class="line"><span class="comment"># 从 GitLab 等平台添加</span></span><br><span class="line">/plugin marketplace add https://gitlab.com/company/plugins.git</span><br><span class="line"></span><br><span class="line"><span class="comment"># 从本地路径添加（开发自己的插件时用）</span></span><br><span class="line">/plugin marketplace add ./my-marketplace</span><br></pre></td></tr></table></figure>
<p>简写：<code>/plugin market</code> 等同于 <code>/plugin marketplace</code></p>
<h4 id="管理已安装的插件"><a href="#管理已安装的插件" class="headerlink" title="管理已安装的插件"></a>管理已安装的插件</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/plugin                                          <span class="comment"># 打开管理界面，切到 Installed 标签</span></span><br><span class="line">/plugin <span class="built_in">disable</span> plugin-name@marketplace-name     <span class="comment"># 暂时禁用（不卸载）</span></span><br><span class="line">/plugin <span class="built_in">enable</span> plugin-name@marketplace-name      <span class="comment"># 重新启用</span></span><br><span class="line">/plugin uninstall plugin-name@marketplace-name   <span class="comment"># 卸载</span></span><br><span class="line">/plugin marketplace list                         <span class="comment"># 查看所有已配置的市场源</span></span><br><span class="line">/plugin marketplace update marketplace-name      <span class="comment"># 更新某个市场的插件列表</span></span><br></pre></td></tr></table></figure>
<h4 id="官方市场中的热门插件"><a href="#官方市场中的热门插件" class="headerlink" title="官方市场中的热门插件"></a>官方市场中的热门插件</h4><div class="table-container">
<table>
<thead>
<tr>
<th>分类</th>
<th>插件示例</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>代码智能（LSP）</td>
<td>typescript-lsp、pyright-lsp、rust-analyzer-lsp、gopls-lsp</td>
<td>给 Claude 精确的代码导航能力</td>
</tr>
<tr>
<td>外部集成</td>
<td>github、gitlab、slack、figma、notion、sentry</td>
<td>连接第三方服务</td>
</tr>
<tr>
<td>开发工作流</td>
<td>commit-commands、pr-review-toolkit</td>
<td>Git 提交和 PR 审查增强</td>
</tr>
<tr>
<td>插件开发</td>
<td>plugin-dev</td>
<td>开发自己的插件时用</td>
</tr>
</tbody>
</table>
</div>
<h3 id="5-3-Superpowers-插件详解"><a href="#5-3-Superpowers-插件详解" class="headerlink" title="5.3 Superpowers 插件详解"></a>5.3 Superpowers 插件详解</h3><p>Superpowers 是最流行的开发流程插件之一，它提供了一套完整的软件开发工作流：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────┐</span><br><span class="line">│              Superpowers 工作流                       │</span><br><span class="line">├─────────────────────────────────────────────────────┤</span><br><span class="line">│                                                     │</span><br><span class="line">│  用户输入需求                                         │</span><br><span class="line">│       │                                             │</span><br><span class="line">│       ▼                                             │</span><br><span class="line">│  ┌──────────────┐                                   │</span><br><span class="line">│  │ Brainstorming │  ← 头脑风暴：探索需求、提问、确认    │</span><br><span class="line">│  └──────┬───────┘                                   │</span><br><span class="line">│         │                                           │</span><br><span class="line">│         ▼                                           │</span><br><span class="line">│  ┌──────────────┐                                   │</span><br><span class="line">│  │ Writing Plans │  ← 写计划：输出详细实施方案          │</span><br><span class="line">│  └──────┬───────┘                                   │</span><br><span class="line">│         │                                           │</span><br><span class="line">│         ▼                                           │</span><br><span class="line">│  ┌──────────────┐                                   │</span><br><span class="line">│  │ Task Setting  │  ← 任务设定：拆分为可执行的子任务     │</span><br><span class="line">│  └──────┬───────┘                                   │</span><br><span class="line">│         │                                           │</span><br><span class="line">│         ▼ (清空上下文，隔离)                           │</span><br><span class="line">│  ┌──────────────┐                                   │</span><br><span class="line">│  │ Executing     │  ← 执行计划：按步骤编码实现          │</span><br><span class="line">│  └──────┬───────┘                                   │</span><br><span class="line">│         │                                           │</span><br><span class="line">│         ▼                                           │</span><br><span class="line">│  ┌──────────────┐                                   │</span><br><span class="line">│  │ Code Review   │  ← 代码审查：检查质量和规范          │</span><br><span class="line">│  └──────┬───────┘                                   │</span><br><span class="line">│         │                                           │</span><br><span class="line">│         ▼                                           │</span><br><span class="line">│  ┌──────────────┐                                   │</span><br><span class="line">│  │ Verification  │  ← 验证：运行测试确认功能正确        │</span><br><span class="line">│  └──────────────┘                                   │</span><br><span class="line">│                                                     │</span><br><span class="line">│  核心价值：隔离上下文，分阶段工作，避免上下文爆炸         │</span><br><span class="line">│                                                     │</span><br><span class="line">└─────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<p>使用方式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">使用 superpowers 帮我开发一个笔记应用</span><br></pre></td></tr></table></figure>
<p>Claude 会自动触发 superpowers 的头脑风暴流程，然后逐步推进。安装方式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/plugin marketplace add obra/superpowers-marketplace</span><br></pre></td></tr></table></figure>
<p>然后在 Discover 标签页中找到 superpowers 并安装。</p>
<p>📖 Superpowers GitHub（42k+ stars）：<a href="https://github.com/obra/superpowers">https://github.com/obra/superpowers</a></p>
<h3 id="5-4-插件管理命令速查"><a href="#5-4-插件管理命令速查" class="headerlink" title="5.4 插件管理命令速查"></a>5.4 插件管理命令速查</h3><div class="table-container">
<table>
<thead>
<tr>
<th>操作</th>
<th>命令</th>
</tr>
</thead>
<tbody>
<tr>
<td>打开插件管理器</td>
<td>/plugin</td>
</tr>
<tr>
<td>安装插件</td>
<td>/plugin install 插件名@市场名</td>
</tr>
<tr>
<td>卸载插件</td>
<td>/plugin uninstall 插件名@市场名</td>
</tr>
<tr>
<td>暂时禁用</td>
<td>/plugin disable 插件名@市场名</td>
</tr>
<tr>
<td>重新启用</td>
<td>/plugin enable 插件名@市场名</td>
</tr>
<tr>
<td>添加市场源</td>
<td>/plugin marketplace add owner/repo</td>
</tr>
<tr>
<td>查看市场列表</td>
<td>/plugin marketplace list</td>
</tr>
<tr>
<td>更新市场</td>
<td>/plugin marketplace update 市场名</td>
</tr>
<tr>
<td>删除市场</td>
<td>/plugin marketplace remove 市场名</td>
</tr>
<tr>
<td>验证插件</td>
<td>/plugin validate .</td>
</tr>
</tbody>
</table>
</div>
<h3 id="5-5-创建自定义插件"><a href="#5-5-创建自定义插件" class="headerlink" title="5.5 创建自定义插件"></a>5.5 创建自定义插件</h3><p>你完全可以让 Claude Code 帮你写插件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">帮我创建一个 Claude Code 插件，功能包括：</span><br><span class="line">1. 管理我的 Agent 团队成员</span><br><span class="line">2. 自动分配任务</span><br><span class="line">3. 监控团队进度</span><br></pre></td></tr></table></figure>
<h3 id="5-6-动手练习"><a href="#5-6-动手练习" class="headerlink" title="5.6 动手练习"></a>5.6 动手练习</h3><p>以下练习由浅入深，建议逐个完成。每个练习都可以直接在 Claude Code 中用自然语言完成。</p>
<h4 id="练习-1：安装你的第一个插件"><a href="#练习-1：安装你的第一个插件" class="headerlink" title="练习 1：安装你的第一个插件"></a>练习 1：安装你的第一个插件</h4><p>在 Claude Code 中执行 <code>/plugin</code>，浏览插件市场，选一个感兴趣的插件安装并体验。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/plugin</span><br></pre></td></tr></table></figure>
<p>安装后试着用自然语言触发它，观察它做了什么、调用了哪些 skill。</p>
<h4 id="练习-2：用-Superpowers-完成一个小项目"><a href="#练习-2：用-Superpowers-完成一个小项目" class="headerlink" title="练习 2：用 Superpowers 完成一个小项目"></a>练习 2：用 Superpowers 完成一个小项目</h4><p>选择一个简单的目标（比如”一个 Todo List 网页应用”），体验完整的 superpowers 工作流：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">使用 superpowers 帮我开发一个 Todo List 网页应用</span><br></pre></td></tr></table></figure>
<p>重点观察：</p>
<p>头脑风暴阶段它问了你哪些问题？</p>
<p>它是如何拆分任务的？</p>
<p>上下文是怎么在阶段之间隔离的？</p>
<h4 id="练习-3：写你的第一个-Skill"><a href="#练习-3：写你的第一个-Skill" class="headerlink" title="练习 3：写你的第一个 Skill"></a>练习 3：写你的第一个 Skill</h4><p>创建一个”每日代码检查”skill，每次调用时自动：</p>
<p>查看 git 状态</p>
<p>列出最近的修改</p>
<p>跑一遍 lint</p>
<p>输出今日工作摘要提示：在 <code>.claude/skills/daily-check/SKILL.md</code> 中编写，然后用 <code>/daily-check</code> 调用。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="section">&lt;!-- .claude/skills/daily-check/SKILL.md --&gt;</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line">name: daily-check</span><br><span class="line">description: 每日代码健康检查</span><br><span class="line"><span class="section">disable-model-invocation: true</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line">执行每日代码健康检查：</span><br><span class="line"></span><br><span class="line"><span class="bullet">1.</span> 运行 <span class="code">`git status`</span> 查看工作区状态</span><br><span class="line"><span class="bullet">2.</span> 运行 <span class="code">`git log --oneline -10`</span> 查看最近提交</span><br><span class="line"><span class="bullet">3.</span> 运行项目的 lint 命令检查代码规范</span><br><span class="line"><span class="bullet">4.</span> 用一段简短的中文总结今日代码状态，包括：</span><br><span class="line"><span class="bullet">   -</span> 有多少未提交的变更</span><br><span class="line"><span class="bullet">   -</span> 最近做了哪些工作</span><br><span class="line"><span class="bullet">   -</span> 有没有 lint 警告需要处理</span><br></pre></td></tr></table></figure>
<h4 id="练习-4：创建一个自定义插件"><a href="#练习-4：创建一个自定义插件" class="headerlink" title="练习 4：创建一个自定义插件"></a>练习 4：创建一个自定义插件</h4><p>挑战自己——让 Claude Code 帮你从零写一个插件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">帮我创建一个 Claude Code 插件叫 &quot;quick-notes&quot;，包含以下 skills：</span><br><span class="line">1. /note-add：快速记录一条开发笔记到 .claude/notes.md</span><br><span class="line">2. /note-list：列出所有笔记</span><br><span class="line">3. /note-search &lt;关键词&gt;：搜索笔记内容</span><br></pre></td></tr></table></figure>
<p>完成后检查 <code>.claude/skills/</code> 下生成的文件结构，理解插件是怎么组织的。</p>
<p>学习要点：通过这些练习你会发现——你不需要手写每一行配置，只需要用自然语言描述你想要的工作流，Claude Code 自己就能帮你生成 skill 文件。核心能力是清晰地定义需求，而不是记住语法。</p>
<h2 id="第六章：MCP-Servers-—-连接外部世界-1"><a href="#第六章：MCP-Servers-—-连接外部世界-1" class="headerlink" title="第六章：MCP Servers — 连接外部世界"></a>第六章：MCP Servers — 连接外部世界</h2><p>MCP (Model Context Protocol) 是让 Claude Code 连接外部工具和服务的协议。</p>
<h3 id="6-1-MCP-的作用"><a href="#6-1-MCP-的作用" class="headerlink" title="6.1 MCP 的作用"></a>6.1 MCP 的作用</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌───────────────────────────────────────────────────┐</span><br><span class="line">│                 MCP 连接示意图                      │</span><br><span class="line">├───────────────────────────────────────────────────┤</span><br><span class="line">│                                                   │</span><br><span class="line">│                 Claude Code                       │</span><br><span class="line">│                     │                             │</span><br><span class="line">│           ┌─────────┼─────────┐                   │</span><br><span class="line">│           │         │         │                   │</span><br><span class="line">│           ▼         ▼         ▼                   │</span><br><span class="line">│     ┌─────────┐ ┌────────┐ ┌──────────┐          │</span><br><span class="line">│     │Playwright│ │MasterGo│ │ GitHub   │          │</span><br><span class="line">│     │浏览器测试│ │UI设计稿 │ │代码仓库  │          │</span><br><span class="line">│     └─────────┘ └────────┘ └──────────┘          │</span><br><span class="line">│           │         │         │                   │</span><br><span class="line">│           ▼         ▼         ▼                   │</span><br><span class="line">│     ┌─────────┐ ┌────────┐ ┌──────────┐          │</span><br><span class="line">│     │ Docker  │ │数据库   │ │ Slack    │          │</span><br><span class="line">│     │容器管理  │ │查询工具 │ │消息通知  │          │</span><br><span class="line">│     └─────────┘ └────────┘ └──────────┘          │</span><br><span class="line">│                                                   │</span><br><span class="line">│  本质：让 Claude Code 能使用你定义的外部工具         │</span><br><span class="line">│                                                   │</span><br><span class="line">└───────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="6-2-配置-MCP-Server"><a href="#6-2-配置-MCP-Server" class="headerlink" title="6.2 配置 MCP Server"></a>6.2 配置 MCP Server</h3><p>在项目根目录创建 <code>.claude/settings.json</code>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;mcpServers&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;playwright&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;npx&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;@anthropic-ai/mcp-server-playwright&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;浏览器自动化测试&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;filesystem&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;npx&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="string">&quot;@anthropic-ai/mcp-server-filesystem&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="string">&quot;--root&quot;</span><span class="punctuation">,</span> <span class="string">&quot;/path/to/allowed/directory&quot;</span></span><br><span class="line">      <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;文件系统访问&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;github&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;npx&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;@anthropic-ai/mcp-server-github&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;env&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;GITHUB_TOKEN&quot;</span><span class="punctuation">:</span> <span class="string">&quot;your-token-here&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;GitHub 操作&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<h3 id="6-3-实战场景：Playwright-MCP-做自动化测试"><a href="#6-3-实战场景：Playwright-MCP-做自动化测试" class="headerlink" title="6.3 实战场景：Playwright MCP 做自动化测试"></a>6.3 实战场景：Playwright MCP 做自动化测试</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 安装 Playwright MCP</span><br><span class="line">npm install @anthropic-ai/mcp-server-playwright</span><br><span class="line"></span><br><span class="line"># 在 Claude Code 中使用</span><br><span class="line">帮我用 Playwright 打开浏览器，访问我们的应用，</span><br><span class="line">模拟用户在画布上写字，然后截图对比写字前后的效果</span><br></pre></td></tr></table></figure>
<p>实战案例：开发者用 Playwright MCP 让模型自动唤起浏览器、模拟笔记书写操作，然后截图对比写字过程中和结束后的像素差异，发现了”笔迹回缩”的 bug 并自动修复。</p>
<h3 id="6-4-实战场景：SSH-远程开发"><a href="#6-4-实战场景：SSH-远程开发" class="headerlink" title="6.4 实战场景：SSH 远程开发"></a>6.4 实战场景：SSH 远程开发</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 让 Claude Code 连接远程 Linux 服务器</span><br><span class="line">我有一台远程 Linux 服务器，地址是 192.168.1.248</span><br><span class="line">用户名 dev，你可以 SSH 连上去帮我编译和测试代码</span><br><span class="line">我本地是 macOS，但代码需要在 Linux 上运行</span><br></pre></td></tr></table></figure>
<p>这就是模型”探索外部世界”能力的体现——它不仅能访问本地文件，还能组网访问其他机器。</p>
<h2 id="第七章：Hooks-事件钩子-1"><a href="#第七章：Hooks-事件钩子-1" class="headerlink" title="第七章：Hooks 事件钩子"></a>第七章：Hooks 事件钩子</h2><p>Hooks 让你在 Claude Code 的特定事件发生时自动执行操作。</p>
<h3 id="7-1-Hook-的作用"><a href="#7-1-Hook-的作用" class="headerlink" title="7.1 Hook 的作用"></a>7.1 Hook 的作用</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────┐</span><br><span class="line">│               Hooks 工作原理                   │</span><br><span class="line">├──────────────────────────────────────────────┤</span><br><span class="line">│                                              │</span><br><span class="line">│  Claude Code 事件   →   触发 Hook   →   执行   │</span><br><span class="line">│                                              │</span><br><span class="line">│  例如：                                       │</span><br><span class="line">│  - 会话开始时      →   读取配置文件              │</span><br><span class="line">│  - 工具调用前      →   检查权限                  │</span><br><span class="line">│  - 工具调用后      →   记录日志                  │</span><br><span class="line">│  - 提交代码前      →   运行 lint 检查            │</span><br><span class="line">│  - 发送通知        →   同步到 Slack              │</span><br><span class="line">│                                              │</span><br><span class="line">└──────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="7-2-Hook-的四种类型"><a href="#7-2-Hook-的四种类型" class="headerlink" title="7.2 Hook 的四种类型"></a>7.2 Hook 的四种类型</h3><div class="table-container">
<table>
<thead>
<tr>
<th>类型</th>
<th>说明</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td>command</td>
<td>执行 shell 命令</td>
<td>运行 lint、格式化、通知</td>
</tr>
<tr>
<td>http</td>
<td>发送 HTTP 请求</td>
<td>通知外部服务、记录日志</td>
</tr>
<tr>
<td>prompt</td>
<td>注入提示词到上下文</td>
<td>动态添加规范、提醒</td>
</tr>
<tr>
<td>agent</td>
<td>触发子agent</td>
<td>自动代码审查</td>
</tr>
</tbody>
</table>
</div>
<h3 id="7-3-配置-Hooks"><a href="#7-3-配置-Hooks" class="headerlink" title="7.3 配置 Hooks"></a>7.3 配置 Hooks</h3><p>在 <code>.claude/settings.json</code> 中配置（支持三个层级：用户全局、项目共享、项目本地）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;hooks&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;SessionStart&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;cat .claude/welcome.md&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;显示欢迎信息和项目概览&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;PreToolUse&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;tool&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Bash&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;echo &#x27;准备执行命令...&#x27;&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;命令执行前的提示&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;PostToolUse&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;tool&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Edit&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;npx eslint --fix $FILE&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;编辑文件后自动 lint&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;PreCompact&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;prompt&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;prompt&quot;</span><span class="punctuation">:</span> <span class="string">&quot;压缩时请务必保留：1.所有修改过的文件列表 2.测试命令 3.当前进度&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;压缩前注入保留指令（减少信息丢失30%）&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p>💡 PreToolUse hook 的特殊能力：可以返回 <code>allow</code>（允许）、<code>deny</code>（拒绝）、<code>ask</code>（询问用户）来控制工具执行，非常适合做安全防护。</p>
<h3 id="7-4-常见-Hook-场景"><a href="#7-4-常见-Hook-场景" class="headerlink" title="7.4 常见 Hook 场景"></a>7.4 常见 Hook 场景</h3><div class="table-container">
<table>
<thead>
<tr>
<th>Hook 事件</th>
<th>用途</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>SessionStart</td>
<td>会话开始时注入上下文</td>
<td>读取项目配置、显示最近变更</td>
</tr>
<tr>
<td>PreToolUse</td>
<td>工具调用前检查/拦截</td>
<td>权限检查、危险操作拦截</td>
</tr>
<tr>
<td>PostToolUse</td>
<td>工具调用后处理</td>
<td>自动格式化、运行测试</td>
</tr>
<tr>
<td>PreCompact</td>
<td>压缩上下文前</td>
<td>指定必须保留的信息</td>
</tr>
<tr>
<td>Stop</td>
<td>Agent 停止时</td>
<td>保存进度、发送通知</td>
</tr>
<tr>
<td>Notification</td>
<td>通知事件</td>
<td>推送到 Slack/微信</td>
</tr>
</tbody>
</table>
</div>
<h3 id="7-5-Hook-执行生命周期与数据流"><a href="#7-5-Hook-执行生命周期与数据流" class="headerlink" title="7.5 Hook 执行生命周期与数据流"></a>7.5 Hook 执行生命周期与数据流</h3><p>理解 Hook 的完整执行流程，是掌握”怎么生效”的关键：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────────────┐</span><br><span class="line">│               Hook 执行生命周期                               │</span><br><span class="line">├─────────────────────────────────────────────────────────────┤</span><br><span class="line">│                                                             │</span><br><span class="line">│  ① 事件触发（如 SessionStart / PreToolUse）                   │</span><br><span class="line">│       │                                                     │</span><br><span class="line">│       ▼                                                     │</span><br><span class="line">│  ② 查找匹配的 Hook 配置                                      │</span><br><span class="line">│     - 按配置层级合并（managed &gt; local &gt; project &gt; user）       │</span><br><span class="line">│     - 检查 matcher 是否匹配（正则匹配工具名等）                 │</span><br><span class="line">│       │                                                     │</span><br><span class="line">│       ▼                                                     │</span><br><span class="line">│  ③ 构建 stdin JSON 输入                                      │</span><br><span class="line">│     &#123;                                                       │</span><br><span class="line">│       &quot;session_id&quot;: &quot;abc-123&quot;,                              │</span><br><span class="line">│       &quot;cwd&quot;: &quot;/path/to/project&quot;,                            │</span><br><span class="line">│       &quot;hook_event_name&quot;: &quot;PreToolUse&quot;,                      │</span><br><span class="line">│       &quot;tool_name&quot;: &quot;Bash&quot;,           ← 仅工具相关事件         │</span><br><span class="line">│       &quot;tool_input&quot;: &#123; &quot;command&quot;: &quot;...&quot; &#125;                    │</span><br><span class="line">│     &#125;                                                       │</span><br><span class="line">│       │                                                     │</span><br><span class="line">│       ▼                                                     │</span><br><span class="line">│  ④ 执行 Hook（command / http / prompt / agent）              │</span><br><span class="line">│       │                                                     │</span><br><span class="line">│       ▼                                                     │</span><br><span class="line">│  ⑤ 处理输出                                                  │</span><br><span class="line">│     - command 类型：stdout 作为反馈/上下文                     │</span><br><span class="line">│     - command 类型：exit code 控制流程（0=继续, 2=阻止）       │</span><br><span class="line">│     - prompt 类型：注入文本到 Claude 对话上下文                 │</span><br><span class="line">│                                                             │</span><br><span class="line">└─────────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<p>关键点：Hook 的 stdin 会自动注入事件的上下文信息（JSON 格式），你的脚本可以读取并据此做出决策。</p>
<h3 id="7-6-上下文注入机制详解"><a href="#7-6-上下文注入机制详解" class="headerlink" title="7.6 上下文注入机制详解"></a>7.6 上下文注入机制详解</h3><p>这是 Hooks 最强大的能力之一——在对话中动态注入信息，让 Claude 自动获取额外上下文。</p>
<h4 id="方式一：prompt-类型-Hook（直接注入）"><a href="#方式一：prompt-类型-Hook（直接注入）" class="headerlink" title="方式一：prompt 类型 Hook（直接注入）"></a>方式一：prompt 类型 Hook（直接注入）</h4><p><code>prompt</code> 类型 Hook 会将指定文本直接注入到 Claude 的对话上下文中：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;hooks&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;PreCompact&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;prompt&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;prompt&quot;</span><span class="punctuation">:</span> <span class="string">&quot;压缩时请保留所有修改过的文件路径和测试命令&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;压缩前注入保留指令&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;SessionStart&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;prompt&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;prompt&quot;</span><span class="punctuation">:</span> <span class="string">&quot;当前项目使用 TypeScript + React，测试框架为 Vitest&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;会话开始注入技术栈信息&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p><code>prompt</code> 字段支持 <code>$ARGUMENTS</code> 占位符，会被替换为事件的实际参数。</p>
<h4 id="方式二：command-类型-Hook-的-stdout-注入"><a href="#方式二：command-类型-Hook-的-stdout-注入" class="headerlink" title="方式二：command 类型 Hook 的 stdout 注入"></a>方式二：command 类型 Hook 的 stdout 注入</h4><p>对于 <code>SessionStart</code> 和 <code>UserPromptSubmit</code> 事件，command 类型 Hook 的标准输出（stdout）会被直接添加到 Claude 的上下文中：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;hooks&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;SessionStart&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;echo &#x27;## 项目状态&#x27; &amp;&amp; git log --oneline -5 &amp;&amp; echo &#x27;## 待修复问题&#x27; &amp;&amp; cat .claude/todo.md&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;注入项目状态和待办事项&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;UserPromptSubmit&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;echo &#x27;当前分支: &#x27;$(git branch --show-current) &amp;&amp; echo &#x27;未提交变更: &#x27;$(git status --short | wc -l)&#x27; 个文件&#x27;&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;每次对话时注入 git 状态&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────────┐</span><br><span class="line">│            上下文注入方式对比                               │</span><br><span class="line">├──────────────┬───────────────────────────────────────────┤</span><br><span class="line">│              │  prompt 类型          command 类型          │</span><br><span class="line">├──────────────┼───────────────────────────────────────────┤</span><br><span class="line">│ 注入内容      │  固定文本              脚本 stdout 输出     │</span><br><span class="line">│ 是否动态      │  静态（或用 $ARGUMENTS）完全动态             │</span><br><span class="line">│ 适用场景      │  固定规范、提醒          实时状态、环境信息   │</span><br><span class="line">│ 注入时机      │  事件触发时             事件触发时           │</span><br><span class="line">│ 执行开销      │  无                    有（执行脚本）        │</span><br><span class="line">└──────────────┴───────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<p>💡 实用技巧：<code>SessionStart</code> 的 stdout 注入非常适合做”项目上下文预加载”——Claude 一启动就知道项目状态、最近改动、待办事项等。</p>
<h3 id="7-7-PreToolUse-权限控制详解"><a href="#7-7-PreToolUse-权限控制详解" class="headerlink" title="7.7 PreToolUse 权限控制详解"></a>7.7 PreToolUse 权限控制详解</h3><p><code>PreToolUse</code> 是最强大的 Hook 事件，可以在工具执行前拦截、允许或询问用户：</p>
<h4 id="控制方式：Exit-Code-JSON-输出"><a href="#控制方式：Exit-Code-JSON-输出" class="headerlink" title="控制方式：Exit Code + JSON 输出"></a>控制方式：Exit Code + JSON 输出</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────────┐</span><br><span class="line">│         PreToolUse 控制流程                                │</span><br><span class="line">├──────────────────────────────────────────────────────────┤</span><br><span class="line">│                                                          │</span><br><span class="line">│  工具调用请求 (如: Bash &quot;rm -rf /&quot;)                        │</span><br><span class="line">│       │                                                  │</span><br><span class="line">│       ▼                                                  │</span><br><span class="line">│  PreToolUse Hook 执行                                     │</span><br><span class="line">│       │                                                  │</span><br><span class="line">│       ├── exit 0 → ✅ 允许执行（默认）                     │</span><br><span class="line">│       │                                                  │</span><br><span class="line">│       ├── exit 2 → 🚫 阻止执行（静默拦截）                 │</span><br><span class="line">│       │                                                  │</span><br><span class="line">│       └── stdout JSON → 精细控制                          │</span><br><span class="line">│           &#123;                                              │</span><br><span class="line">│             &quot;hookSpecificOutput&quot;: &#123;                      │</span><br><span class="line">│               &quot;permissionDecision&quot;: &quot;allow&quot; | &quot;deny&quot; | &quot;ask&quot;</span><br><span class="line">│             &#125;                                            │</span><br><span class="line">│           &#125;                                              │</span><br><span class="line">│           - &quot;allow&quot;: 允许执行，跳过用户确认                 │</span><br><span class="line">│           - &quot;deny&quot;:  阻止执行，告知 Claude                 │</span><br><span class="line">│           - &quot;ask&quot;:   弹出确认框，让用户决定                 │</span><br><span class="line">│                                                          │</span><br><span class="line">└──────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h4 id="实战示例：危险命令拦截器"><a href="#实战示例：危险命令拦截器" class="headerlink" title="实战示例：危险命令拦截器"></a>实战示例：危险命令拦截器</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="comment"># .claude/hooks/safety-guard.sh</span></span><br><span class="line"><span class="comment"># 读取 stdin 中的工具调用信息</span></span><br><span class="line">INPUT=$(<span class="built_in">cat</span>)</span><br><span class="line">TOOL_NAME=$(<span class="built_in">echo</span> <span class="string">&quot;<span class="variable">$INPUT</span>&quot;</span> | jq -r <span class="string">&#x27;.tool_name&#x27;</span>)</span><br><span class="line">COMMAND=$(<span class="built_in">echo</span> <span class="string">&quot;<span class="variable">$INPUT</span>&quot;</span> | jq -r <span class="string">&#x27;.tool_input.command // &quot;&quot;&#x27;</span>)</span><br><span class="line"><span class="comment"># 拦截危险命令</span></span><br><span class="line"><span class="keyword">if</span> [[ <span class="string">&quot;<span class="variable">$TOOL_NAME</span>&quot;</span> == <span class="string">&quot;Bash&quot;</span> ]]; <span class="keyword">then</span></span><br><span class="line">  <span class="keyword">if</span> <span class="built_in">echo</span> <span class="string">&quot;<span class="variable">$COMMAND</span>&quot;</span> | grep -qE <span class="string">&#x27;rm\s+-rf|drop\s+table|--force|reset\s+--hard&#x27;</span>; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">&#x27;&#123;&quot;hookSpecificOutput&quot;:&#123;&quot;permissionDecision&quot;:&quot;deny&quot;&#125;&#125;&#x27;</span></span><br><span class="line">    <span class="built_in">exit</span> 0</span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"><span class="comment"># 其他情况允许</span></span><br><span class="line"><span class="built_in">exit</span> 0</span><br></pre></td></tr></table></figure>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;hooks&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;PreToolUse&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;matcher&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;tool&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Bash&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;bash .claude/hooks/safety-guard.sh&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;拦截危险 shell 命令&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<h3 id="7-8-Matcher-匹配器"><a href="#7-8-Matcher-匹配器" class="headerlink" title="7.8 Matcher 匹配器"></a>7.8 Matcher 匹配器</h3><p>Matcher 用于精确控制 Hook 在哪些条件下触发，避免不必要的执行：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;hooks&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;PreToolUse&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;matcher&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;tool&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Bash&quot;</span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;bash .claude/hooks/check-bash.sh&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;仅匹配 Bash 工具调用&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;PostToolUse&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;matcher&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;tool&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Edit|Write&quot;</span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;npx prettier --write $FILE&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;匹配 Edit 或 Write 工具（正则语法）&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th>匹配字段</th>
<th>说明</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>tool</td>
<td>匹配工具名（支持正则）</td>
<td>“Bash”, “Edit</td>
<td>Write”, “.*”</td>
</tr>
</tbody>
</table>
</div>
<p>没有设置 matcher 的 Hook 会在每次对应事件触发时执行。建议总是设置 matcher 以避免性能损耗。</p>
<h3 id="7-9-Hook-可用的环境变量"><a href="#7-9-Hook-可用的环境变量" class="headerlink" title="7.9 Hook 可用的环境变量"></a>7.9 Hook 可用的环境变量</h3><p>Hook 脚本可以通过 stdin JSON 获取丰富的上下文信息：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>字段</th>
<th>说明</th>
<th>示例值</th>
</tr>
</thead>
<tbody>
<tr>
<td>session_id</td>
<td>当前会话 ID</td>
<td>“abc-123-def”</td>
</tr>
<tr>
<td>cwd</td>
<td>Claude Code 工作目录</td>
<td>“/Users/dev/myproject”</td>
</tr>
<tr>
<td>hook_event_name</td>
<td>触发的事件名</td>
<td>“PreToolUse”</td>
</tr>
<tr>
<td>tool_name</td>
<td>工具名（仅工具事件）</td>
<td>“Bash”, “Edit”</td>
</tr>
<tr>
<td>tool_input</td>
<td>工具输入参数（仅工具事件）</td>
<td>{“command”: “npm test”}</td>
</tr>
<tr>
<td>transcript_path</td>
<td>对话记录文件路径</td>
<td>“/path/to/transcript.jsonl”</td>
</tr>
<tr>
<td>读取方式示例（Bash）：</td>
<td></td>
</tr>
</tbody>
</table>
</div>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line">INPUT=$(<span class="built_in">cat</span>)  <span class="comment"># 从 stdin 读取 JSON</span></span><br><span class="line">TOOL=$(<span class="built_in">echo</span> <span class="string">&quot;<span class="variable">$INPUT</span>&quot;</span> | jq -r <span class="string">&#x27;.tool_name&#x27;</span>)</span><br><span class="line">CWD=$(<span class="built_in">echo</span> <span class="string">&quot;<span class="variable">$INPUT</span>&quot;</span> | jq -r <span class="string">&#x27;.cwd&#x27;</span>)</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;工具: <span class="variable">$TOOL</span>, 目录: <span class="variable">$CWD</span>&quot;</span></span><br></pre></td></tr></table></figure>
<h3 id="7-10-配置层级与优先级"><a href="#7-10-配置层级与优先级" class="headerlink" title="7.10 配置层级与优先级"></a>7.10 配置层级与优先级</h3><p>Hook 可以在四个层级配置，高优先级的层级覆盖低优先级：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────┐</span><br><span class="line">│           配置层级（优先级从高到低）                │</span><br><span class="line">├─────────────────────────────────────────────────┤</span><br><span class="line">│                                                 │</span><br><span class="line">│  ① managed (企业管理)   最高优先级                │</span><br><span class="line">│     ~/.claude/settings.managed.json             │</span><br><span class="line">│     → 企业 IT 统一下发，用户无法覆盖              │</span><br><span class="line">│                                                 │</span><br><span class="line">│  ② local (项目本地)                              │</span><br><span class="line">│     .claude/settings.local.json                 │</span><br><span class="line">│     → 个人本地配置，不提交到 Git                  │</span><br><span class="line">│                                                 │</span><br><span class="line">│  ③ project (项目共享)                            │</span><br><span class="line">│     .claude/settings.json                       │</span><br><span class="line">│     → 团队共享规范，提交到 Git                    │</span><br><span class="line">│                                                 │</span><br><span class="line">│  ④ user (用户全局)      最低优先级                │</span><br><span class="line">│     ~/.claude/settings.json                     │</span><br><span class="line">│     → 个人全局默认配置                            │</span><br><span class="line">│                                                 │</span><br><span class="line">├─────────────────────────────────────────────────┤</span><br><span class="line">│  合并规则：同一事件的 Hook 数组跨层级合并           │</span><br><span class="line">│  （不是覆盖，而是拼接执行）                        │</span><br><span class="line">└─────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<p>⚠️ 注意：不同层级的同一事件 Hook 会合并执行（数组拼接），而不是高优先级覆盖低优先级。优先级主要影响的是权限设置等其他配置项。</p>
<h3 id="7-11-完整-Hook-配置示例"><a href="#7-11-完整-Hook-配置示例" class="headerlink" title="7.11 完整 Hook 配置示例"></a>7.11 完整 Hook 配置示例</h3><p>一个综合了多种 Hook 能力的实际项目配置：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;hooks&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;SessionStart&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;echo &#x27;📋 最近 5 条提交:&#x27; &amp;&amp; git log --oneline -5 &amp;&amp; echo &#x27;📌 当前分支:&#x27; $(git branch --show-current)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;会话启动时注入项目上下文&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;UserPromptSubmit&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;echo &#x27;⚡ 未暂存变更:&#x27; $(git diff --stat --shortstat 2&gt;/dev/null || echo &#x27;无&#x27;)&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;每次用户发送消息时注入 git 状态&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;PreToolUse&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;matcher&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;tool&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Bash&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;bash .claude/hooks/safety-guard.sh&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;拦截危险 shell 命令&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;PostToolUse&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;matcher&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;tool&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Edit|Write&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;npx prettier --write $FILE 2&gt;/dev/null; exit 0&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;文件编辑后自动格式化&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;PreCompact&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;prompt&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;prompt&quot;</span><span class="punctuation">:</span> <span class="string">&quot;压缩时务必保留：1.所有修改过的文件完整路径 2.测试运行命令 3.当前任务进度 4.已知问题&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;压缩前注入保留指令&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;Notification&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;osascript -e &#x27;display notification \&quot;Claude Code 有新消息\&quot; with title \&quot;CC 通知\&quot;&#x27;&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;macOS 系统通知&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<h2 id="第八章：Subagents-子Agent-1"><a href="#第八章：Subagents-子Agent-1" class="headerlink" title="第八章：Subagents 子Agent"></a>第八章：Subagents 子Agent</h2><h3 id="8-1-什么是-Subagent？"><a href="#8-1-什么是-Subagent？" class="headerlink" title="8.1 什么是 Subagent？"></a>8.1 什么是 Subagent？</h3><p>Subagent 是在独立上下文中运行的专业 Agent，用于保护主对话的上下文不被污染。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────┐</span><br><span class="line">│              Subagent 工作模式                     │</span><br><span class="line">├──────────────────────────────────────────────────┤</span><br><span class="line">│                                                  │</span><br><span class="line">│  主对话 (上下文有限且珍贵)                          │</span><br><span class="line">│  ┌────────────────────────────────────┐          │</span><br><span class="line">│  │  用户: &quot;帮我了解认证系统怎么工作的&quot;    │          │</span><br><span class="line">│  │                                    │          │</span><br><span class="line">│  │  Claude: 启动 subagent 去调研...    │          │</span><br><span class="line">│  │            │                       │          │</span><br><span class="line">│  │            ▼                       │          │</span><br><span class="line">│  │  ┌──────────────────────┐         │          │</span><br><span class="line">│  │  │  Subagent (独立上下文) │         │          │</span><br><span class="line">│  │  │  - 读取 auth 相关文件  │         │          │</span><br><span class="line">│  │  │  - 分析 token 刷新逻辑 │         │          │</span><br><span class="line">│  │  │  - 检查 OAuth 工具     │         │          │</span><br><span class="line">│  │  │  - 生成总结报告        │         │          │</span><br><span class="line">│  │  └──────────┬───────────┘         │          │</span><br><span class="line">│  │             │                      │          │</span><br><span class="line">│  │             ▼ (只返回总结)          │          │</span><br><span class="line">│  │  Claude: &quot;认证系统使用 JWT...&quot;      │          │</span><br><span class="line">│  │  (主上下文保持干净！)                │          │</span><br><span class="line">│  └────────────────────────────────────┘          │</span><br><span class="line">│                                                  │</span><br><span class="line">│  好处：                                           │</span><br><span class="line">│  ✅ 调研过程不占用主上下文                           │</span><br><span class="line">│  ✅ 可以读大量文件而不会让主会话变臃肿                │</span><br><span class="line">│  ✅ 天然隔离，任务失败不影响主对话                    │</span><br><span class="line">│                                                  │</span><br><span class="line">└──────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="8-2-创建自定义-Subagent"><a href="#8-2-创建自定义-Subagent" class="headerlink" title="8.2 创建自定义 Subagent"></a>8.2 创建自定义 Subagent</h3><p>在 <code>.claude/agents/</code> 目录下创建：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="section">&lt;!-- .claude/agents/security-reviewer.md --&gt;</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line">name: security-reviewer</span><br><span class="line">description: 审查代码中的安全漏洞</span><br><span class="line">tools: Read, Grep, Glob, Bash</span><br><span class="line"><span class="section">model: opus</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line">你是一位资深安全工程师。审查代码时关注：</span><br><span class="line"><span class="bullet">-</span> 注入漏洞（SQL注入、XSS、命令注入）</span><br><span class="line"><span class="bullet">-</span> 认证和授权缺陷</span><br><span class="line"><span class="bullet">-</span> 代码中的密钥或凭证</span><br><span class="line"><span class="bullet">-</span> 不安全的数据处理</span><br><span class="line"></span><br><span class="line">提供具体的行号引用和修复建议。</span><br></pre></td></tr></table></figure>
<p>使用方式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">使用 security-reviewer 子agent 审查最近的代码变更</span><br></pre></td></tr></table></figure>
<h3 id="8-3-使用-Subagent-的最佳场景"><a href="#8-3-使用-Subagent-的最佳场景" class="headerlink" title="8.3 使用 Subagent 的最佳场景"></a>8.3 使用 Subagent 的最佳场景</h3><div class="table-container">
<table>
<thead>
<tr>
<th>场景</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>代码库调研</td>
<td>探索大型代码库时不污染主上下文</td>
</tr>
<tr>
<td>代码审查</td>
<td>独立的上下文做更客观的审查</td>
</tr>
<tr>
<td>方案研究</td>
<td>同时调研多个方案不互相干扰</td>
</tr>
<tr>
<td>测试验证</td>
<td>验证实现是否正确</td>
</tr>
</tbody>
</table>
</div>
<h2 id="第九章：Agent-Teams-—-多Agent协作开发-1"><a href="#第九章：Agent-Teams-—-多Agent协作开发-1" class="headerlink" title="第九章：Agent Teams — 多Agent协作开发"></a>第九章：Agent Teams — 多Agent协作开发</h2><p>这是 Claude Code 最强大也最前沿的功能。根据实战经验，Agent Teams 可以像真实开发团队一样协作。</p>
<h3 id="9-1-Agent-Teams-架构"><a href="#9-1-Agent-Teams-架构" class="headerlink" title="9.1 Agent Teams 架构"></a>9.1 Agent Teams 架构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────────┐</span><br><span class="line">│                  Agent Teams 协作架构                      │</span><br><span class="line">├──────────────────────────────────────────────────────────┤</span><br><span class="line">│                                                          │</span><br><span class="line">│                    ┌───────────────┐                     │</span><br><span class="line">│                    │   主 Agent     │                     │</span><br><span class="line">│                    │   (Team Lead) │                     │</span><br><span class="line">│                    │   只统筹不干活 │ ← 核心原则！         │</span><br><span class="line">│                    └───────┬───────┘                     │</span><br><span class="line">│                            │                             │</span><br><span class="line">│              ┌─────────────┼─────────────┐               │</span><br><span class="line">│              │             │             │               │</span><br><span class="line">│       ┌──────┴──────┐ ┌───┴────┐ ┌──────┴──────┐       │</span><br><span class="line">│       │  产品经理     │ │ 架构师  │ │  测试工程师  │       │</span><br><span class="line">│       │  Agent       │ │ Agent  │ │  Agent      │       │</span><br><span class="line">│       │  - 需求定义   │ │- 审查   │ │ - 写测试    │       │</span><br><span class="line">│       │  - 功能验收   │ │  代码   │ │ - 运行测试   │       │</span><br><span class="line">│       │             │ │- 审查   │ │ - 报告结果   │       │</span><br><span class="line">│       │             │ │  git   │ │             │       │</span><br><span class="line">│       └─────────────┘ └────────┘ └─────────────┘       │</span><br><span class="line">│              │             │             │               │</span><br><span class="line">│       ┌──────┴──────┐ ┌───┴────┐ ┌──────┴──────┐       │</span><br><span class="line">│       │  前端工程师   │ │ 后端   │ │ AI 助手     │       │</span><br><span class="line">│       │  Agent       │ │ Agent  │ │ Agent       │       │</span><br><span class="line">│       │  - 页面开发   │ │- API   │ │ - 提示词    │       │</span><br><span class="line">│       │  - 组件开发   │ │- 数据库 │ │ - 工具链    │       │</span><br><span class="line">│       │  - 样式调整   │ │- 逻辑  │ │ - 编排     │       │</span><br><span class="line">│       └─────────────┘ └────────┘ └─────────────┘       │</span><br><span class="line">│                                                          │</span><br><span class="line">│  所有 Agent 可以互相发消息、纠错、协调                       │</span><br><span class="line">│                                                          │</span><br><span class="line">└──────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="9-2-启用和启动-Agent-Teams"><a href="#9-2-启用和启动-Agent-Teams" class="headerlink" title="9.2 启用和启动 Agent Teams"></a>9.2 启用和启动 Agent Teams</h3><p>⚠️ Agent Teams 目前是实验性功能，需要手动启用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1</span><br></pre></td></tr></table></figure>
<p>或在 <code>.claude/settings.json</code> 中设置。</p>
<p>推荐团队大小：3-5 个 teammates，每个分配 5-6 个任务。</p>
<p>显示模式：支持 in-process（默认）和 split-pane（tmux/iTerm2 分屏）两种。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">启动：claude --teammate-mode tmux</span><br></pre></td></tr></table></figure>
<p>方式一：自然语言启动</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">使用 Agent Teams 帮我开发一个笔记应用。</span><br><span class="line">需要以下角色：</span><br><span class="line">1. 产品经理 - 负责需求定义和功能验收</span><br><span class="line">2. 架构师 - 负责审查代码质量和规范</span><br><span class="line">3. 前端工程师 - 负责页面开发</span><br><span class="line">4. 后端工程师 - 负责 API 开发</span><br><span class="line">5. 测试工程师 - 负责编写和运行测试</span><br></pre></td></tr></table></figure>
<p>方式二：通过 superpowers 工作流启动</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">使用 superpowers 和 agent teams 帮我开发一个笔记应用</span><br></pre></td></tr></table></figure>
<h3 id="9-3-Agent-角色设计指南"><a href="#9-3-Agent-角色设计指南" class="headerlink" title="9.3 Agent 角色设计指南"></a>9.3 Agent 角色设计指南</h3><h4 id="主-Agent（Team-Lead）的关键约束"><a href="#主-Agent（Team-Lead）的关键约束" class="headerlink" title="主 Agent（Team Lead）的关键约束"></a>主 Agent（Team Lead）的关键约束</h4><p>⚠️ 最重要的原则：主 Agent 绝对不能干重活！</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="section"># 在 CLAUDE.md 中约束主 Agent</span></span><br><span class="line"><span class="section">## 主 Agent 行为约束</span></span><br><span class="line"><span class="bullet">-</span> 你是团队领导，只负责：</span><br><span class="line"><span class="bullet">  1.</span> 理解用户需求并分发给团队成员</span><br><span class="line"><span class="bullet">  2.</span> 监控各 Agent 的工作进度</span><br><span class="line"><span class="bullet">  3.</span> 协调 Agent 之间的沟通</span><br><span class="line"><span class="bullet">  4.</span> 向用户汇报进度</span><br><span class="line"><span class="bullet">-</span> 你绝对不要：</span><br><span class="line"><span class="bullet">  1.</span> 自己写代码（看到 bug 也不要自己改！）</span><br><span class="line"><span class="bullet">  2.</span> 自己读大量代码文件</span><br><span class="line"><span class="bullet">  3.</span> 自己做探索性工作</span><br><span class="line"><span class="bullet">-</span> 原因：你的上下文大约 200K tokens，约 1.5 万行代码，</span><br><span class="line">  一旦被代码占满就会触发压缩，导致丢失关键约束信息。</span><br></pre></td></tr></table></figure>
<p>实战趣事：有开发者约束主 Agent 不干活，但主 Agent 看到 bug 还是忍不住自己改了，然后说”这个 bug 比较小，顺手就改了，下次不会了” 😄</p>
<h4 id="架构师-Agent"><a href="#架构师-Agent" class="headerlink" title="架构师 Agent"></a>架构师 Agent</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">- 只做两件事：</span><br><span class="line">  1. 审查每个人的 git 提交记录</span><br><span class="line">  2. 检查代码是否符合 CLAUDE.md 中的规范</span><br><span class="line">- 发现 DSL 或接口变更时，必须全局广播通知</span><br></pre></td></tr></table></figure>
<h4 id="测试-Agent"><a href="#测试-Agent" class="headerlink" title="测试 Agent"></a>测试 Agent</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">- 不要去读源代码（避免上下文膨胀）</span><br><span class="line">- 只通过接口文档和测试用例进行测试</span><br><span class="line">- 测试有反馈有输入有输出（这很关键！）</span><br><span class="line">- 发现 bug 后报告给主 Agent</span><br></pre></td></tr></table></figure>
<h3 id="9-4-Agent-间通信机制"><a href="#9-4-Agent-间通信机制" class="headerlink" title="9.4 Agent 间通信机制"></a>9.4 Agent 间通信机制</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌────────────────────────────────────────────────────┐</span><br><span class="line">│              Agent 通信示例                          │</span><br><span class="line">├────────────────────────────────────────────────────┤</span><br><span class="line">│                                                    │</span><br><span class="line">│  后端 Agent → 前端 Agent:                           │</span><br><span class="line">│  &quot;API 对齐确认：POST /api/experiments               │</span><br><span class="line">│   请求体: &#123; name, dslConfig, metadata &#125;             │</span><br><span class="line">│   响应: &#123; id, status, createdAt &#125;                   │</span><br><span class="line">│   SSE 流格式: event: progress, data: &#123;...&#125;&quot;          │</span><br><span class="line">│                                                    │</span><br><span class="line">│  架构师 Agent → 全体广播:                            │</span><br><span class="line">│  &quot;DSL 规范已更新：pin 字段改名为 puzzle，              │</span><br><span class="line">│   所有使用 DSL 的模块需要同步修改&quot;                     │</span><br><span class="line">│                                                    │</span><br><span class="line">│  前端 Agent → 主 Agent:                             │</span><br><span class="line">│  &quot;test3 的指令方向与引擎实际情况冲突，                  │</span><br><span class="line">│   需要确认后再执行。等待确认的同时，                    │</span><br><span class="line">│   我先从 test4 开始做。&quot;                              │</span><br><span class="line">│                                                    │</span><br><span class="line">│  主 Agent → 前端 Agent:                             │</span><br><span class="line">│  &quot;之前方向确实反了，任务描述已更新，                    │</span><br><span class="line">│   请按新方向继续。&quot;                                   │</span><br><span class="line">│                                                    │</span><br><span class="line">└────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="9-5-Agent-Teams-的已知限制与应对"><a href="#9-5-Agent-Teams-的已知限制与应对" class="headerlink" title="9.5 Agent Teams 的已知限制与应对"></a>9.5 Agent Teams 的已知限制与应对</h3><div class="table-container">
<table>
<thead>
<tr>
<th>限制</th>
<th>说明</th>
<th>应对方案</th>
</tr>
</thead>
<tbody>
<tr>
<td>消息不实时</td>
<td>Agent 干完活才看消息</td>
<td>在 CLAUDE.md 中约束检查频率</td>
</tr>
<tr>
<td>默认自动关闭</td>
<td>每轮对话后会关闭成员</td>
<td>全局 CLAUDE.md 写明”不要自动关闭”</td>
</tr>
<tr>
<td>并发写入冲突</td>
<td>多个 Agent 同时编辑同一文件</td>
<td>模块分工要清晰，减少文件交叉</td>
</tr>
<tr>
<td>无打断机制</td>
<td>Agent 长时间工作时无法中途通知</td>
<td>可通过 Hooks 或自研插件解决</td>
</tr>
<tr>
<td>闷头干活</td>
<td>Agent 不主动同步进度</td>
<td>在规范中约束定期汇报</td>
</tr>
</tbody>
</table>
</div>
<h3 id="9-6-多人协作时的代码管理"><a href="#9-6-多人协作时的代码管理" class="headerlink" title="9.6 多人协作时的代码管理"></a>9.6 多人协作时的代码管理</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────┐</span><br><span class="line">│            多人 + 多Agent 协作模式              │</span><br><span class="line">├──────────────────────────────────────────────┤</span><br><span class="line">│                                              │</span><br><span class="line">│  开发者A              开发者B    开发者C       │</span><br><span class="line">│  (AI助手模块)         (编辑器)   (DSL引擎)    │</span><br><span class="line">│     │                   │          │         │</span><br><span class="line">│     ▼                   ▼          ▼         │</span><br><span class="line">│  ┌──────┐           ┌──────┐  ┌──────┐      │</span><br><span class="line">│  │ CC + │           │ CC + │  │ CC + │      │</span><br><span class="line">│  │多Agent│           │多Agent│  │多Agent│      │</span><br><span class="line">│  └──┬───┘           └──┬───┘  └──┬───┘      │</span><br><span class="line">│     │                   │          │         │</span><br><span class="line">│     ▼                   ▼          ▼         │</span><br><span class="line">│  feature/ai-agent   feature/editor feature/  │</span><br><span class="line">│  (独立分支)          (独立分支)    dsl-engine │</span><br><span class="line">│     │                   │          │         │</span><br><span class="line">│     └───────────────────┼──────────┘         │</span><br><span class="line">│                         │                    │</span><br><span class="line">│                    主分支合并                   │</span><br><span class="line">│                    (解决冲突)                  │</span><br><span class="line">│                         │                    │</span><br><span class="line">│                    全量回归测试                 │</span><br><span class="line">│                                              │</span><br><span class="line">│  原则：                                       │</span><br><span class="line">│  - 模块划分清楚，每人负责独立模块               │</span><br><span class="line">│  - 大功能用不同分支，合并时处理冲突             │</span><br><span class="line">│  - 个人并行任务可用 git worktree               │</span><br><span class="line">│                                              │</span><br><span class="line">└──────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="9-7-Cowork-速览：不用终端的-AI-Agent"><a href="#9-7-Cowork-速览：不用终端的-AI-Agent" class="headerlink" title="9.7 Cowork 速览：不用终端的 AI Agent"></a>9.7 Cowork 速览：不用终端的 AI Agent</h3><p>Cowork 是 Anthropic 于 2026 年 1 月推出的桌面智能代理工具，内置于 Claude Desktop，以独立标签页的形式存在。核心定位：让不写代码的人也能使用 Agent 能力。</p>
<h4 id="Cowork-能做什么？"><a href="#Cowork-能做什么？" class="headerlink" title="Cowork 能做什么？"></a>Cowork 能做什么？</h4><div class="table-container">
<table>
<thead>
<tr>
<th>能力</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>文件操作</td>
<td>读取、编辑、创建本地文档（Word、PDF、表格、PPT 等）</td>
</tr>
<tr>
<td>多步骤自动化</td>
<td>分解复杂任务为子任务，并行执行，实时展示进度</td>
</tr>
<tr>
<td>跨应用联动</td>
<td>Excel 数据自动流入 PPT 摘要，输入变化时自动更新</td>
</tr>
<tr>
<td>浏览器操作</td>
<td>通过 Chrome 扩展做网页研究、表单填写、数据抓取</td>
</tr>
<tr>
<td>外部工具连接</td>
<td>通过 MCP 连接 Slack、Notion、Figma、Google Workspace 等</td>
</tr>
</tbody>
</table>
</div>
<h4 id="Cowork-vs-Claude-Code"><a href="#Cowork-vs-Claude-Code" class="headerlink" title="Cowork vs Claude Code"></a>Cowork vs Claude Code</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────┐</span><br><span class="line">│          Cowork 与 Claude Code 对比                │</span><br><span class="line">├─────────────────────┬────────────────────────────┤</span><br><span class="line">│       Cowork        │      Claude Code           │</span><br><span class="line">├─────────────────────┼────────────────────────────┤</span><br><span class="line">│ 面向知识工作者       │ 面向开发者                  │</span><br><span class="line">│ GUI 图形界面        │ Terminal 终端命令行          │</span><br><span class="line">│ ❌ 不能执行代码      │ ✅ 可执行代码、跑测试        │</span><br><span class="line">│ 操作文档和应用       │ 操作代码和项目               │</span><br><span class="line">│ 沙盒 VM 隔离运行    │ 直接在本地终端运行            │</span><br><span class="line">│ 任务前需确认计划     │ 可在会话内自主运行            │</span><br><span class="line">│ 连接办公类工具       │ 连接开发类工具               │</span><br><span class="line">├─────────────────────┴────────────────────────────┤</span><br><span class="line">│                                                  │</span><br><span class="line">│  一句话：Claude Code 改变了程序员的工作方式，       │</span><br><span class="line">│  Cowork 要改变其余所有人的工作方式。                │</span><br><span class="line">│                                                  │</span><br><span class="line">└──────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h4 id="使用方式"><a href="#使用方式" class="headerlink" title="使用方式"></a>使用方式</h4><p>打开 Claude Desktop → 切换到 Cowork 标签页</p>
<p>授权文件夹：指定允许 Claude 访问的本地文件夹</p>
<p>安装插件：根据岗位选择对应插件（产品管理、财务、法务、市场等 11 类官方插件）</p>
<p>描述任务：用自然语言说清想要的结果</p>
<p>Claude 展示执行计划，确认后自动执行，实时显示进度</p>
<p>⚠️ 需要 Claude Pro（$20/月）或以上订阅。Cowork 目前仍处于 Research Preview 阶段。</p>
<h4 id="什么时候用-Cowork？"><a href="#什么时候用-Cowork？" class="headerlink" title="什么时候用 Cowork？"></a>什么时候用 Cowork？</h4><div class="table-container">
<table>
<thead>
<tr>
<th>场景</th>
<th>选择</th>
</tr>
</thead>
<tbody>
<tr>
<td>写代码、跑测试、调试</td>
<td>Claude Code</td>
</tr>
<tr>
<td>分析报表、整理文档、做 PPT</td>
<td>Cowork</td>
</tr>
<tr>
<td>调研竞品、抓取网页数据</td>
<td>Cowork</td>
</tr>
<tr>
<td>部署项目、CI/CD</td>
<td>Claude Code</td>
</tr>
<tr>
<td>管理 Slack/Notion/Figma</td>
<td>Cowork</td>
</tr>
</tbody>
</table>
</div>
<h3 id="9-8-一张图看清四者的关系与边界"><a href="#9-8-一张图看清四者的关系与边界" class="headerlink" title="9.8 一张图看清四者的关系与边界"></a>9.8 一张图看清四者的关系与边界</h3><p>Skills、Subagents、Plugins、Agent Teams 这四个概念容易混淆，下面用一张图理清它们的关系：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌────────────────────────────────────────────────────────────────────┐</span><br><span class="line">│        Skills · Subagents · Plugins · Agent Teams 全景关系图        │</span><br><span class="line">├────────────────────────────────────────────────────────────────────┤</span><br><span class="line">│                                                                    │</span><br><span class="line">│  ┌─ Agent Teams ──────────────────────────────────────────────┐   │</span><br><span class="line">│  │                                                            │   │</span><br><span class="line">│  │  Team Leader（主 Agent）                                    │   │</span><br><span class="line">│  │  ┌────────────────────────────────────┐                   │   │</span><br><span class="line">│  │  │  可加载 Skills    可调用 Subagents   │                   │   │</span><br><span class="line">│  │  │  可使用 Plugins   可安装 MCP        │                   │   │</span><br><span class="line">│  │  └────────────────────────────────────┘                   │   │</span><br><span class="line">│  │       │ 分发任务           │ 分发任务                      │   │</span><br><span class="line">│  │       ▼                   ▼                               │   │</span><br><span class="line">│  │  ┌──────────┐       ┌──────────┐                          │   │</span><br><span class="line">│  │  │ Teammate │       │ Teammate │   ← 每个都是独立 Agent    │   │</span><br><span class="line">│  │  │ 前端工程师│       │ 后端工程师│     有自己的上下文         │   │</span><br><span class="line">│  │  │ (可加载   │       │ (可加载   │     可加载 Skills        │   │</span><br><span class="line">│  │  │  Skills)  │       │  Skills)  │     可调用 Subagents    │   │</span><br><span class="line">│  │  └──────────┘       └──────────┘                          │   │</span><br><span class="line">│  │                                                            │   │</span><br><span class="line">│  └────────────────────────────────────────────────────────────┘   │</span><br><span class="line">│                                                                    │</span><br><span class="line">├────────────────────────────────────────────────────────────────────┤</span><br><span class="line">│                                                                    │</span><br><span class="line">│  ┌─ Skill ─────────┐  ┌─ Subagent ────┐  ┌─ Plugin ──────────┐  │</span><br><span class="line">│  │                  │  │               │  │                    │  │</span><br><span class="line">│  │ 一张&quot;任务卡片&quot;   │  │ 独立上下文的   │  │ Skills 的打包集合  │  │</span><br><span class="line">│  │                  │  │ 专业 Agent    │  │                    │  │</span><br><span class="line">│  │ 注入到当前Agent  │  │               │  │ 方便团队分发复用   │  │</span><br><span class="line">│  │ 的上下文中执行   │  │ 执行完只返回   │  │                    │  │</span><br><span class="line">│  │                  │  │ 总结给调用者   │  │ 一个 Plugin 可包含 │  │</span><br><span class="line">│  │ 共享调用者的     │  │               │  │ 多个 Skills       │  │</span><br><span class="line">│  │ 上下文（会占用） │  │ 不污染调用者   │  │                    │  │</span><br><span class="line">│  │                  │  │ 的上下文      │  │ 支持 marketplace  │  │</span><br><span class="line">│  └──────────────────┘  └───────────────┘  └────────────────────┘  │</span><br><span class="line">│                                                                    │</span><br><span class="line">├────────────────────────────────────────────────────────────────────┤</span><br><span class="line">│                                                                    │</span><br><span class="line">│  快速选型判断：                                                     │</span><br><span class="line">│                                                                    │</span><br><span class="line">│  &quot;我需要一套固定工作流&quot;         → Skill                             │</span><br><span class="line">│  &quot;我需要调研但不想污染上下文&quot;   → Subagent                           │</span><br><span class="line">│  &quot;我要把多个 Skill 分享给团队&quot;  → Plugin                            │</span><br><span class="line">│  &quot;我要多人并行干大活&quot;           → Agent Teams                       │</span><br><span class="line">│  &quot;任务简单，不涉及代码&quot;         → Cowork                            │</span><br><span class="line">│                                                                    │</span><br><span class="line">└────────────────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th>维度</th>
<th>Skill</th>
<th>Subagent</th>
<th>Plugin</th>
<th>Agent Teams</th>
</tr>
</thead>
<tbody>
<tr>
<td>本质</td>
<td>任务卡片/工作流</td>
<td>独立上下文的 Agent</td>
<td>Skills 的打包集合</td>
<td>多 Agent 并行协作</td>
</tr>
<tr>
<td>上下文</td>
<td>共享调用者上下文</td>
<td>独立上下文</td>
<td>同 Skill</td>
<td>每个成员独立上下文</td>
</tr>
<tr>
<td>触发方式</td>
<td>/命令 或自动匹配</td>
<td>Claude 自主决定</td>
<td>安装后同 Skill</td>
<td>用户指令或自动</td>
</tr>
<tr>
<td>适合场景</td>
<td>重复性工作流</td>
<td>调研、探索、分析</td>
<td>团队共享标准化流程</td>
<td>复杂项目并行开发</td>
</tr>
<tr>
<td>文件位置</td>
<td>.claude/skills/</td>
<td>无独立文件</td>
<td>.claude/plugins/</td>
<td>运行时动态创建</td>
</tr>
<tr>
<td>复用性</td>
<td>项目内复用</td>
<td>一次性</td>
<td>跨项目/跨团队复用</td>
<td>按需组建</td>
</tr>
</tbody>
</table>
</div>
<h2 id="第十章：上下文管理-—-核心中的核心-1"><a href="#第十章：上下文管理-—-核心中的核心-1" class="headerlink" title="第十章：上下文管理 — 核心中的核心"></a>第十章：上下文管理 — 核心中的核心</h2><p>“上下文管理是使用多 Agent 编程中最重要的事情。一旦上下文爆炸触发压缩，模型就会忘掉你的约束细节，返工明显变多。” —— 实战经验</p>
<h3 id="10-1-理解上下文限制"><a href="#10-1-理解上下文限制" class="headerlink" title="10.1 理解上下文限制"></a>10.1 理解上下文限制</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────┐</span><br><span class="line">│               上下文窗口示意图                      │</span><br><span class="line">├──────────────────────────────────────────────────┤</span><br><span class="line">│                                                  │</span><br><span class="line">│  ┌─────────────────────────────────────────┐     │</span><br><span class="line">│  │          上下文窗口 (~200K tokens)        │     │</span><br><span class="line">│  │                                         │     │</span><br><span class="line">│  │  ┌───────────────────────────┐          │     │</span><br><span class="line">│  │  │ 系统提示 + CLAUDE.md       │ ← 始终保持│     │</span><br><span class="line">│  │  └───────────────────────────┘          │     │</span><br><span class="line">│  │  ┌───────────────────────────┐          │     │</span><br><span class="line">│  │  │ Skills + Hooks 注入        │ ← 始终保持│     │</span><br><span class="line">│  │  └───────────────────────────┘          │     │</span><br><span class="line">│  │  ┌───────────────────────────┐          │     │</span><br><span class="line">│  │  │ 对话历史                   │ ← 越来越长│     │</span><br><span class="line">│  │  │ 代码文件内容               │          │     │</span><br><span class="line">│  │  │ 工具调用结果               │          │     │</span><br><span class="line">│  │  │ ...                       │          │     │</span><br><span class="line">│  │  └───────────────────────────┘          │     │</span><br><span class="line">│  │                                         │     │</span><br><span class="line">│  │  ⚠️ 超过限制 → 触发压缩 → 丢失细节！     │     │</span><br><span class="line">│  │                                         │     │</span><br><span class="line">│  └─────────────────────────────────────────┘     │</span><br><span class="line">│                                                  │</span><br><span class="line">│  约 200K tokens ≈ 约 1.5 万行代码                 │</span><br><span class="line">│  主 Agent 约 10 分钟就容易触发压缩                  │</span><br><span class="line">│                                                  │</span><br><span class="line">└──────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="10-2-claudeignore-—-减少上下文噪音"><a href="#10-2-claudeignore-—-减少上下文噪音" class="headerlink" title="10.2 .claudeignore — 减少上下文噪音"></a>10.2 .claudeignore — 减少上下文噪音</h3><p>在项目根目录创建 <code>.claudeignore</code> 文件（语法同 <code>.gitignore</code>），可以减少 40-60% 的搜索空间：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># .claudeignore</span><br><span class="line">node_modules/</span><br><span class="line">dist/</span><br><span class="line">build/</span><br><span class="line">.git/</span><br><span class="line">*.min.js</span><br><span class="line">*.map</span><br><span class="line">vendor/</span><br><span class="line">coverage/</span><br><span class="line">__pycache__/</span><br><span class="line">*.pyc</span><br></pre></td></tr></table></figure>
<p>💡 核心作用：不让 Claude 读取这些目录的文件，避免无关代码污染上下文。</p>
<h3 id="10-3-上下文管理策略"><a href="#10-3-上下文管理策略" class="headerlink" title="10.3 上下文管理策略"></a>10.3 上下文管理策略</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────┐</span><br><span class="line">│              上下文管理七大策略                        │</span><br><span class="line">├─────────────────────────────────────────────────────┤</span><br><span class="line">│                                                     │</span><br><span class="line">│  1️⃣ 主 Agent 不干活                                 │</span><br><span class="line">│     不读代码、不写代码，只统筹分发                      │</span><br><span class="line">│     → 保持上下文清洁                                  │</span><br><span class="line">│                                                     │</span><br><span class="line">│  2️⃣ 任务间 /clear                                   │</span><br><span class="line">│     不相关任务之间清空上下文                            │</span><br><span class="line">│     → 避免&quot;厨房水槽&quot;式会话                            │</span><br><span class="line">│                                                     │</span><br><span class="line">│  3️⃣ 用 subagent 做调研                              │</span><br><span class="line">│     探索代码库时使用子agent                            │</span><br><span class="line">│     → 调研结果不污染主上下文                           │</span><br><span class="line">│                                                     │</span><br><span class="line">│  4️⃣ compact 时重读规范                              │</span><br><span class="line">│     在 CLAUDE.md 中写明：                            │</span><br><span class="line">│     &quot;compact 后必须重新读取此文件&quot;                     │</span><br><span class="line">│     → 压缩后不丢失关键约束                            │</span><br><span class="line">│                                                     │</span><br><span class="line">│  5️⃣ 分阶段干活                                      │</span><br><span class="line">│     每个阶段有明确目标和边界                           │</span><br><span class="line">│     → 避免一次性给太多任务                             │</span><br><span class="line">│                                                     │</span><br><span class="line">│  6️⃣ 精确的指令                                      │</span><br><span class="line">│     上下文给不够会引起歧义                             │</span><br><span class="line">│     上下文给太多会分散注意力                            │</span><br><span class="line">│     → 只提供必要且精确的信息                           │</span><br><span class="line">│                                                     │</span><br><span class="line">│  7️⃣ 约束文件读取范围                                  │</span><br><span class="line">│     &quot;不要读 vendor/ 目录的代码&quot;                       │</span><br><span class="line">│     &quot;只读 src/auth/ 目录&quot;                            │</span><br><span class="line">│     → 减少不必要的上下文消耗                           │</span><br><span class="line">│                                                     │</span><br><span class="line">└─────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="10-4-上下文管理命令"><a href="#10-4-上下文管理命令" class="headerlink" title="10.4 上下文管理命令"></a>10.4 上下文管理命令</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/clear                          <span class="comment"># 完全清空上下文（任务切换时用）</span></span><br><span class="line">/compact                        <span class="comment"># 压缩上下文（保留重要信息）</span></span><br><span class="line">/compact 保留 API 变更记录       <span class="comment"># 带指引的压缩</span></span><br><span class="line">Esc + Esc                       <span class="comment"># 回滚到之前的检查点</span></span><br><span class="line">/rewind                         <span class="comment"># 打开回滚菜单</span></span><br></pre></td></tr></table></figure>
<p>自动压缩触发时机：当上下文达到约 80% 容量（~160K tokens）时自动触发压缩。压缩后约 70K tokens 可压缩到约 4K tokens 的摘要。</p>
<h3 id="10-5-模型的”大海捞针”问题"><a href="#10-5-模型的”大海捞针”问题" class="headerlink" title="10.5 模型的”大海捞针”问题"></a>10.5 模型的”大海捞针”问题</h3><p>模型有注意力限制——即使上下文足够大，也不一定能注意到所有信息：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">上下文中有 200K tokens 的内容...</span><br><span class="line"></span><br><span class="line">┌─────────────────────────────────────┐</span><br><span class="line">│  开头部分 ← 注意力强                  │</span><br><span class="line">│  ...                                │</span><br><span class="line">│  ...                                │</span><br><span class="line">│  重要约束（写在中间位置）← 可能被忽略！ │</span><br><span class="line">│  ...                                │</span><br><span class="line">│  ...                                │</span><br><span class="line">│  最近的对话 ← 注意力强                │</span><br><span class="line">└─────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<p>应对方法：重要的约束多次强调、放在 CLAUDE.md 开头、compact 时指定保留。</p>
<h2 id="第十一章：开发范式与实战案例-1"><a href="#第十一章：开发范式与实战案例-1" class="headerlink" title="第十一章：开发范式与实战案例"></a>第十一章：开发范式与实战案例</h2><h3 id="11-1-规范驱动-vs-测试驱动"><a href="#11-1-规范驱动-vs-测试驱动" class="headerlink" title="11.1 规范驱动 vs 测试驱动"></a>11.1 规范驱动 vs 测试驱动</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌────────────────────────────────────────────────────┐</span><br><span class="line">│         两种开发驱动方式的对比                        │</span><br><span class="line">├──────────────────────┬─────────────────────────────┤</span><br><span class="line">│    规范驱动开发        │     测试驱动开发              │</span><br><span class="line">├──────────────────────┼─────────────────────────────┤</span><br><span class="line">│ 重点：定义清楚架构、   │ 重点：测试通过即可，          │</span><br><span class="line">│ 编码规范、行为约束     │ 不管内部代码结构              │</span><br><span class="line">│                      │                             │</span><br><span class="line">│ 优点：                │ 优点：                       │</span><br><span class="line">│ - 代码人类可读        │ - 自动化验证                  │</span><br><span class="line">│ - 架构清晰可维护      │ - 模型可自主迭代修复          │</span><br><span class="line">│ - 减少返工            │ - 黑盒方式更高效              │</span><br><span class="line">│                      │                             │</span><br><span class="line">│ 缺点：                │ 缺点：                       │</span><br><span class="line">│ - 前置投入大          │ - 测试覆盖不全               │</span><br><span class="line">│ - 规范需要持续迭代    │ - 代码可能不可读              │</span><br><span class="line">│                      │ - 无法穷尽所有场景            │</span><br><span class="line">├──────────────────────┴─────────────────────────────┤</span><br><span class="line">│                                                    │</span><br><span class="line">│  ✅ 实战结论：两者都需要！                            │</span><br><span class="line">│                                                    │</span><br><span class="line">│  - 规范保证代码可读、架构合理                         │</span><br><span class="line">│  - 测试保证功能正确、模型可自主迭代                    │</span><br><span class="line">│  - 规范在前置制定，测试要有反馈有输入有输出             │</span><br><span class="line">│                                                    │</span><br><span class="line">└────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="11-2-整体到局部的开发模式"><a href="#11-2-整体到局部的开发模式" class="headerlink" title="11.2 整体到局部的开发模式"></a>11.2 整体到局部的开发模式</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────┐</span><br><span class="line">│              推荐的开发流程                             │</span><br><span class="line">├──────────────────────────────────────────────────────┤</span><br><span class="line">│                                                      │</span><br><span class="line">│  Phase 1: 构思规范（小半天）                            │</span><br><span class="line">│  ┌──────────────────────────────────┐                │</span><br><span class="line">│  │ - 编写 CLAUDE.md                  │                │</span><br><span class="line">│  │ - 定义技术栈、架构、红线           │                │</span><br><span class="line">│  │ - 准备 DSL/接口规范               │                │</span><br><span class="line">│  │ - 配置 Skills 和工具链             │                │</span><br><span class="line">│  └───────────────┬──────────────────┘                │</span><br><span class="line">│                  │                                   │</span><br><span class="line">│  Phase 2: 整体搭建（2-6小时）                          │</span><br><span class="line">│  ┌───────────────┴──────────────────┐                │</span><br><span class="line">│  │ - 多 Agent 并行开发所有模块        │                │</span><br><span class="line">│  │ - 主流程先不阻塞（先通再调）       │                │</span><br><span class="line">│  │ - 搭建基本框架和接口              │                │</span><br><span class="line">│  │ - 确保主流程跑通                  │                │</span><br><span class="line">│  └───────────────┬──────────────────┘                │</span><br><span class="line">│                  │                                   │</span><br><span class="line">│  Phase 3: 局部精调（持续迭代）                          │</span><br><span class="line">│  ┌───────────────┴──────────────────┐                │</span><br><span class="line">│  │ - 分模块精细化调整                │                │</span><br><span class="line">│  │ - 每人负责一个模块的细节           │                │</span><br><span class="line">│  │ - 100+轮交互打磨复杂交互          │                │</span><br><span class="line">│  │ - 修复 bug、优化体验              │                │</span><br><span class="line">│  └───────────────┬──────────────────┘                │</span><br><span class="line">│                  │                                   │</span><br><span class="line">│  Phase 4: 合并验证                                    │</span><br><span class="line">│  ┌───────────────┴──────────────────┐                │</span><br><span class="line">│  │ - 合并各模块代码                   │                │</span><br><span class="line">│  │ - 跑主流程全量回归                 │                │</span><br><span class="line">│  │ - 发现问题用 Agent 修复            │                │</span><br><span class="line">│  │ - 测试 Agent 做端到端验证          │                │</span><br><span class="line">│  └──────────────────────────────────┘                │</span><br><span class="line">│                                                      │</span><br><span class="line">└──────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="11-3-实战案例：科创实验制作工具"><a href="#11-3-实战案例：科创实验制作工具" class="headerlink" title="11.3 实战案例：科创实验制作工具"></a>11.3 实战案例：科创实验制作工具</h3><p>这是一个真实的复杂项目案例，展示了完整的多 Agent 开发流程：项目背景：</p>
<p>原来每个实验需要 20-28 个工作日人工开发</p>
<p>目标：用 AI 将 UI 稿和脚本描述自动转换为可交互的 Cocos 实验项目架构：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────┐</span><br><span class="line">│            科创实验制作工具 架构图                       │</span><br><span class="line">├──────────────────────────────────────────────────────┤</span><br><span class="line">│                                                      │</span><br><span class="line">│  输入                     处理                输出    │</span><br><span class="line">│  ┌──────┐               ┌──────┐          ┌──────┐  │</span><br><span class="line">│  │MasterGo│    模块1     │  AI  │          │ Cocos│  │</span><br><span class="line">│  │UI设计稿├──→│ UI+脚本  ├─→│ 助手 ├─→DSL──→│ 渲染 │  │</span><br><span class="line">│  └──────┘   │ → DSL    │  │(Agent)│       │ 引擎 │  │</span><br><span class="line">│  ┌──────┐   └──────────┘  └──────┘       └──────┘  │</span><br><span class="line">│  │实验脚本│                                          │</span><br><span class="line">│  │自然语言│        模块2                              │</span><br><span class="line">│  └──────┘   ┌──────────────┐                        │</span><br><span class="line">│             │  DSL 引擎     │                        │</span><br><span class="line">│             │  DSL → Cocos  │                        │</span><br><span class="line">│             │  游戏画面渲染  │                        │</span><br><span class="line">│             └──────────────┘                        │</span><br><span class="line">│                                                      │</span><br><span class="line">│                    模块3                              │</span><br><span class="line">│             ┌──────────────┐                        │</span><br><span class="line">│             │  实验编辑器   │                         │</span><br><span class="line">│             │  人工精细调整 │                         │</span><br><span class="line">│             │  (类似IDE)   │                         │</span><br><span class="line">│             └──────────────┘                        │</span><br><span class="line">│                                                      │</span><br><span class="line">│  关键创新：                                           │</span><br><span class="line">│  - 让模型通读 96 个已有实验代码，自动归纳出 DSL 规范     │</span><br><span class="line">│  - DSL 完成度 80%+，大幅减少人工                       │</span><br><span class="line">│  - 同时做了 H5 和 Cocos 两个版本&quot;赛马&quot;                 │</span><br><span class="line">│                                                      │</span><br><span class="line">└──────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<p>开发过程：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>阶段</th>
<th>工作内容</th>
<th>耗时</th>
</tr>
</thead>
<tbody>
<tr>
<td>规范制定</td>
<td>编写 CLAUDE.md、定义 DSL、配置技术栈</td>
<td>~小半天</td>
</tr>
<tr>
<td>整体搭建</td>
<td>6 个 Agent 并行开发全部模块</td>
<td>~6 小时</td>
</tr>
<tr>
<td>细节调优</td>
<td>3 人各负责一个模块精调</td>
<td>100+ 轮交互</td>
</tr>
<tr>
<td>综合提效</td>
<td>对比传统开发模式</td>
<td>约 5 倍</td>
</tr>
<tr>
<td>Agent 团队配置：</td>
<td></td>
<td></td>
</tr>
<tr>
<td>Agent 角色</td>
<td>职责</td>
<td>关键约束</td>
</tr>
<tr>
<td>——————</td>
<td>———</td>
<td>—————</td>
</tr>
<tr>
<td>主 Agent</td>
<td>统筹全局，分发任务</td>
<td>绝不自己写代码</td>
</tr>
<tr>
<td>产品经理</td>
<td>定义需求，功能验收</td>
<td>会主动降级非关键需求到 v1.1</td>
</tr>
<tr>
<td>架构师</td>
<td>审查 git 提交，维护规范</td>
<td>发现规范变更时全局广播</td>
</tr>
<tr>
<td>前端工程师</td>
<td>编辑器页面开发</td>
<td>专注于交互和样式</td>
</tr>
<tr>
<td>后端工程师</td>
<td>API 和数据逻辑</td>
<td>主动与前端对齐接口</td>
</tr>
<tr>
<td>AI 助手开发</td>
<td>提示词调优，LangChain 编排</td>
<td>使用 Skills 指导 DSL 生成</td>
</tr>
</tbody>
</table>
</div>
<h3 id="11-4-自动化测试的关键作用"><a href="#11-4-自动化测试的关键作用" class="headerlink" title="11.4 自动化测试的关键作用"></a>11.4 自动化测试的关键作用</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────┐</span><br><span class="line">│            自动化测试让模型自主迭代                      │</span><br><span class="line">├──────────────────────────────────────────────────────┤</span><br><span class="line">│                                                      │</span><br><span class="line">│  关键洞察：一旦有自动化测试，模型就能自己迭代！          │</span><br><span class="line">│                                                      │</span><br><span class="line">│  ┌─────────┐     ┌─────────┐     ┌─────────┐       │</span><br><span class="line">│  │  写代码  │ ──→ │  跑测试  │ ──→ │ 有报错？ │       │</span><br><span class="line">│  └─────────┘     └─────────┘     └────┬────┘       │</span><br><span class="line">│       ▲                               │             │</span><br><span class="line">│       │              是               │ 否           │</span><br><span class="line">│       │               │               │             │</span><br><span class="line">│       │          ┌────┴────┐     ┌────┴────┐       │</span><br><span class="line">│       └──────────│ 修复代码 │     │  完成！  │       │</span><br><span class="line">│                  └─────────┘     └─────────┘       │</span><br><span class="line">│                                                      │</span><br><span class="line">│  实战案例：                                           │</span><br><span class="line">│  - 吴老师的项目：有编译错误反馈 → 持续运行1-2天          │</span><br><span class="line">│  - 笔记引擎：Playwright 截图对比 → 自动发现笔迹回缩bug  │</span><br><span class="line">│  - 服务端测试：批量脚本跑接口 → 全自动回归              │</span><br><span class="line">│                                                      │</span><br><span class="line">│  测试的关键要求：                                      │</span><br><span class="line">│  ✅ 有明确的输入和输出                                  │</span><br><span class="line">│  ✅ 报错信息模型可读                                    │</span><br><span class="line">│  ✅ 成功/失败状态明确                                   │</span><br><span class="line">│  ✅ 可以重复执行                                       │</span><br><span class="line">│                                                      │</span><br><span class="line">└──────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h2 id="第十二章：让-AI-帮你测-——-Claude-Code-测试全链路落地"><a href="#第十二章：让-AI-帮你测-——-Claude-Code-测试全链路落地" class="headerlink" title="第十二章：让 AI 帮你测 —— Claude Code 测试全链路落地"></a>第十二章：让 AI 帮你测 —— Claude Code 测试全链路落地</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">让 AI 帮你测 —— Claude Code 测试全链路落地</span><br></pre></td></tr></table></figure>
<h2 id="第十三章：OpenClaw-—-用手机遥控-Claude-Code-7×24-小时工作"><a href="#第十三章：OpenClaw-—-用手机遥控-Claude-Code-7×24-小时工作" class="headerlink" title="第十三章：OpenClaw — 用手机遥控 Claude Code 7×24 小时工作"></a>第十三章：OpenClaw — 用手机遥控 Claude Code 7×24 小时工作</h2><p>想象一下：你在通勤路上打开飞书，发一条消息”帮我把 src/auth 模块的单元测试补全”，家里的电脑就自动开始干活，干完了把结果发回你手机。这就是 OpenClaw + Claude Code 的威力。</p>
<h3 id="12-1-OpenClaw-是什么？"><a href="#12-1-OpenClaw-是什么？" class="headerlink" title="12.1 OpenClaw 是什么？"></a>12.1 OpenClaw 是什么？</h3><p>OpenClaw（开源，MIT 协议）是一个自主 AI 代理，它的核心理念是：把消息应用变成 AI 的操控界面。它能做到：</p>
<p>连接飞书、微信、Telegram、Slack、WhatsApp 等几乎所有消息平台</p>
<p>以 7×24 小时守护进程运行在你的电脑上</p>
<p>通过 MCP 协议调用 Claude Code 执行编程任务</p>
<p>你在手机上发消息 → AI 在电脑上干活 → 结果发回手机</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>信息</th>
<th>内容</th>
</tr>
</thead>
<tbody>
<tr>
<td>官网</td>
<td><a href="https://openclaw.ai/">https://openclaw.ai/</a></td>
</tr>
<tr>
<td>GitHub</td>
<td><a href="https://github.com/openclaw/openclaw">https://github.com/openclaw/openclaw</a></td>
</tr>
<tr>
<td>官方文档</td>
<td><a href="https://docs.openclaw.ai/">https://docs.openclaw.ai/</a></td>
</tr>
<tr>
<td>中文教程（社区）</td>
<td>Claude Code x OpenClaw 中文指南</td>
</tr>
</tbody>
</table>
</div>
<h3 id="12-2-核心配置文件体系"><a href="#12-2-核心配置文件体系" class="headerlink" title="12.2 核心配置文件体系"></a>12.2 核心配置文件体系</h3><p>OpenClaw 使用 Markdown 文件作为 Agent 的”操作系统”，所有文件存放于 <code>~/.openclaw/workspace/</code>。每次对话开始时自动注入系统提示词，形成 Agent 的持久化上下文。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">~/.openclaw/workspace/</span><br><span class="line">├── AGENTS.md          ← 主指令合约（优先级最高）</span><br><span class="line">├── SOUL.md            ← 灵魂与人格定义</span><br><span class="line">├── IDENTITY.md        ← 身份档案</span><br><span class="line">├── USER.md            ← 用户偏好层</span><br><span class="line">├── TOOLS.md           ← 工具使用说明</span><br><span class="line">├── MEMORY.md          ← 长期记忆</span><br><span class="line">├── HEARTBEAT.md       ← 心跳任务清单</span><br><span class="line">├── BOOT.md            ← 启动仪式脚本</span><br><span class="line">└── memory/</span><br><span class="line">    └── 2026-03-05.md  ← 每日记忆日志</span><br></pre></td></tr></table></figure>
<p>八大核心文件速查表：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>文件</th>
<th>定位</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>AGENTS.md</td>
<td>主指令合约</td>
<td>定义工作优先级、行为边界、质量标准。最顶层规则，优先级最高</td>
</tr>
<tr>
<td>SOUL.md</td>
<td>灵魂人格层</td>
<td>定义声音风格、性情、价值观和不可违背约束。保持长期稳定</td>
</tr>
<tr>
<td>IDENTITY.md</td>
<td>身份档案</td>
<td>Agent 的名字、角色、目标、声音风格。首次 Bootstrap 时自动生成</td>
</tr>
<tr>
<td>USER.md</td>
<td>用户偏好</td>
<td>语言偏好、沟通风格、输出格式习惯。让 Agent 记住”你想要什么”</td>
</tr>
<tr>
<td>TOOLS.md</td>
<td>工具说明</td>
<td>记录本地工具信息（SSH 别名、设备昵称等），仅指导不控制可用性</td>
</tr>
<tr>
<td>MEMORY.md</td>
<td>长期记忆</td>
<td>跨日对话的持久事实、重大决策。手动维护，保持精简</td>
</tr>
<tr>
<td>HEARTBEAT.md</td>
<td>心跳清单</td>
<td>定义心跳触发时的检查项。保持极简避免 prompt 膨胀</td>
</tr>
<tr>
<td>BOOT.md</td>
<td>启动脚本</td>
<td>Agent 启动时的仪式提示词，需在 openclaw.json 中启用</td>
</tr>
</tbody>
</table>
</div>
<p>💡 类比 Claude Code：AGENTS.md 类似 CLAUDE.md，SOUL.md 是 OpenClaw 独有的”人格层”。你可以用 <code>soul-md</code> skill 让 Claude Code 分析你的数据自动生成个性化 SOUL.md。</p>
<h3 id="12-3-会话概念与工作机制"><a href="#12-3-会话概念与工作机制" class="headerlink" title="12.3 会话概念与工作机制"></a>12.3 会话概念与工作机制</h3><p>OpenClaw 的会话由 Gateway 管理，Session Key 格式为 <code>agent:&lt;agentId&gt;:&lt;mainKey&gt;</code>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────┐</span><br><span class="line">│              OpenClaw 会话路由                     │</span><br><span class="line">├──────────────────────────────────────────────────┤</span><br><span class="line">│                                                  │</span><br><span class="line">│  飞书对话 ──→ agent:main:feishu_user123          │</span><br><span class="line">│  微信对话 ──→ agent:main:wechat_user123          │</span><br><span class="line">│  WebUI   ──→ agent:main:webui                   │</span><br><span class="line">│  CLI     ──→ agent:main:cli                     │</span><br><span class="line">│                                                  │</span><br><span class="line">│  同一用户、不同频道 = 不同 Session                  │</span><br><span class="line">│  ✅ 防止跨上下文数据泄露                            │</span><br><span class="line">│                                                  │</span><br><span class="line">│  所有频道通过 Gateway 统一路由                      │</span><br><span class="line">│  Gateway = 单一 Node.js 进程（hub-and-spoke）     │</span><br><span class="line">│                                                  │</span><br><span class="line">└──────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="12-4-记忆机制详解"><a href="#12-4-记忆机制详解" class="headerlink" title="12.4 记忆机制详解"></a>12.4 记忆机制详解</h3><p>OpenClaw 的记忆系统采用”Markdown 文件 + 混合搜索索引”架构，是其核心能力之一。</p>
<p>可以让 Claude Code 帮你配置Ollama 向量搜索模型。双层记忆结构：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>层级</th>
<th>文件</th>
<th>特点</th>
</tr>
</thead>
<tbody>
<tr>
<td>长期记忆</td>
<td>MEMORY.md</td>
<td>手动维护，精简稳定，跨日加载</td>
</tr>
<tr>
<td>每日日志</td>
<td>memory/YYYY-MM-DD.md</td>
<td>自动写入，记录当天发生的事、学到的知识</td>
</tr>
<tr>
<td>混合搜索检索：</td>
<td></td>
</tr>
</tbody>
</table>
</div>
<div class="table-container">
<table>
<thead>
<tr>
<th>搜索类型</th>
<th>权重</th>
<th>技术</th>
<th>特点</th>
</tr>
</thead>
<tbody>
<tr>
<td>向量语义搜索</td>
<td>70%</td>
<td>SQLite + sqlite-vec</td>
<td>基于含义匹配，词不同也能找到</td>
</tr>
<tr>
<td>BM25 全文搜索</td>
<td>30%</td>
<td>SQLite FTS5</td>
<td>基于关键词精确匹配</td>
</tr>
</tbody>
</table>
</div>
<p>时间衰减：旧记忆自动降权，近期记忆优先</p>
<p>Agent 工具：通过 <code>memory_search</code>（语义召回）和 <code>memory_get</code>（定向读取）访问记忆</p>
<p>这就是”让 AI 助理越用越聪明”的秘密——每天的对话都在积累经验，下次遇到类似问题它会自动检索历史。</p>
<h3 id="12-5-心跳机制-—-让-AI-主动工作"><a href="#12-5-心跳机制-—-让-AI-主动工作" class="headerlink" title="12.5 心跳机制 — 让 AI 主动工作"></a>12.5 心跳机制 — 让 AI 主动工作</h3><p>心跳机制将 OpenClaw 从”被动响应”变成”主动感知”。工作原理：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">每隔 X 分钟（默认 30 min）</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">Gateway 发送心跳提示词 → Agent 读取 HEARTBEAT.md</span><br><span class="line">      │</span><br><span class="line">      ├── 无需处理 → 回复 HEARTBEAT_OK（静默）</span><br><span class="line">      │</span><br><span class="line">      └── 发现待办 → 主动发消息通知用户</span><br></pre></td></tr></table></figure>
<p>配置示例（openclaw.json）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;heartbeat&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;enabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;interval&quot;</span><span class="punctuation">:</span> <span class="number">30</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;target&quot;</span><span class="punctuation">:</span> <span class="string">&quot;last&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;activeHours&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;start&quot;</span><span class="punctuation">:</span> <span class="string">&quot;08:00&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;end&quot;</span><span class="punctuation">:</span> <span class="string">&quot;22:00&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p>省钱技巧：</p>
<p>用 Haiku（最便宜的模型）做心跳检查，只在真正需要关注时升级到 Sonnet/Opus</p>
<p><code>HEARTBEAT.md</code> 保持极简（短 checklist），避免 prompt 膨胀</p>
<p>如果 <code>HEARTBEAT.md</code> 为空，OpenClaw 会自动跳过该次心跳，节省 API 调用</p>
<h3 id="12-6-WebUI-控制台"><a href="#12-6-WebUI-控制台" class="headerlink" title="12.6 WebUI 控制台"></a>12.6 WebUI 控制台</h3><p>OpenClaw 自带 Web 控制界面，默认运行在本地端口 18789。访问地址： <code>http://127.0.0.1:18789</code></p>
<div class="table-container">
<table>
<thead>
<tr>
<th>功能模块</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>Web 聊天</td>
<td>无需配置频道即可直接对话，适合测试</td>
</tr>
<tr>
<td>调试工具</td>
<td>查看 Agent 执行日志、工具调用记录</td>
</tr>
<tr>
<td>模型配置</td>
<td>添加 AI 提供商（Anthropic、OpenAI 等）</td>
</tr>
<tr>
<td>安全管理</td>
<td>审批授权设备</td>
</tr>
<tr>
<td>会话历史</td>
<td>查看完整对话记录（JSONL 格式）</td>
</tr>
</tbody>
</table>
</div>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">控制平面架构：</span><br><span class="line">┌──────────────────────┐</span><br><span class="line">│  Gateway (:18789)    │</span><br><span class="line">├──────────────────────┤</span><br><span class="line">│  ├── Web UI（浏览器） │</span><br><span class="line">│  ├── CLI 客户端       │</span><br><span class="line">│  ├── macOS App       │</span><br><span class="line">│  └── 移动端节点       │</span><br><span class="line">└──────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="12-7-安全注意事项与-Token-管理"><a href="#12-7-安全注意事项与-Token-管理" class="headerlink" title="12.7 安全注意事项与 Token 管理"></a>12.7 安全注意事项与 Token 管理</h3><h4 id="安全风险速览"><a href="#安全风险速览" class="headerlink" title="安全风险速览"></a>安全风险速览</h4><div class="table-container">
<table>
<thead>
<tr>
<th>风险</th>
<th>说明</th>
<th>防护</th>
</tr>
</thead>
<tbody>
<tr>
<td>提示词注入</td>
<td>恶意邮件/网页中的指令可能被 Agent 执行</td>
<td>工具策略 + 沙箱隔离 + 频道白名单</td>
</tr>
<tr>
<td>恶意 Skills</td>
<td>社区 skill 中约 36% 存在安全缺陷</td>
<td>只使用官方或可信来源的 skill</td>
</tr>
<tr>
<td>数据泄露</td>
<td>群聊中工具可能读取环境变量和 API Key</td>
<td>限制工具权限 + 环境变量隔离</td>
</tr>
</tbody>
</table>
</div>
<p>💡 可安装 clawsec 安全 skill 套件，提供漂移检测、自动审计、skill 完整性验证。</p>
<h4 id="Token-费用管理"><a href="#Token-费用管理" class="headerlink" title="Token 费用管理"></a>Token 费用管理</h4><p>三层模型策略（推荐）：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>任务类型</th>
<th>推荐模型</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>心跳检查、简单查询</td>
<td>Claude Haiku</td>
<td>70-80% 任务可用此层</td>
</tr>
<tr>
<td>日常工作流</td>
<td>Claude Sonnet</td>
<td>平衡能力与成本</td>
</tr>
<tr>
<td>复杂分析、架构决策</td>
<td>Claude Opus</td>
<td>仅在必要时使用</td>
</tr>
<tr>
<td>省钱技巧：</td>
<td></td>
</tr>
</tbody>
</table>
</div>
<p>将上下文窗口从默认 400K 限制到 50K-100K</p>
<p>精简注入文件（减少 1000 token × Opus 100 次/天 ≈ 省 $45/月）</p>
<p>利用 Prompt Cache 降低重复内容成本</p>
<p>心跳间隔配置为略小于缓存 TTL，保持缓存”温热”</p>
<h3 id="12-8-前置准备"><a href="#12-8-前置准备" class="headerlink" title="12.8 前置准备"></a>12.8 前置准备</h3><p>在安装之前，你需要准备以下东西：1. 一台常开的电脑OpenClaw 运行在你的本地电脑上（很多人用 Mac Mini 当”家庭服务器”）。关机 = AI 下班。2. Node.js 22+OpenClaw 要求 Node.js 22 以上版本。检查你的版本：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node --version</span><br></pre></td></tr></table></figure>
<p>如果版本太低或未安装，用以下方式安装最新版：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># macOS (Homebrew)</span></span><br><span class="line">brew install node</span><br><span class="line"><span class="comment"># 或使用 nvm 管理多版本</span></span><br><span class="line">curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.0/install.sh | bash</span><br><span class="line">nvm install 22</span><br><span class="line">nvm use 22</span><br></pre></td></tr></table></figure>
<p>📖 Node.js 官网下载：<a href="https://nodejs.org/">https://nodejs.org/</a></p>
<ol>
<li>Anthropic API KeyOpenClaw 需要一个 AI 模型的 API Key 来驱动对话。推荐使用 Anthropic（Claude 的官方 API）。获取步骤：</li>
</ol>
<p>访问 Anthropic Console</p>
<p>注册或登录账号</p>
<p>进入 API Keys 页面</p>
<p>点击 Create Key，复制保存（格式为 <code>sk-ant-api03-...</code>）</p>
<p>在 Billing 页面充值余额（按量付费）</p>
<p>⚠️ API Key 是付费的，和 Claude Code Max 订阅是两套独立的付费体系。API 按 token 用量计费，建议先充 $5-10 试用。</p>
<ol>
<li>飞书开放平台应用（如果要连飞书）后面的飞书章节会详细讲。</li>
</ol>
<h3 id="12-9-用-Claude-Code-安装-OpenClaw"><a href="#12-9-用-Claude-Code-安装-OpenClaw" class="headerlink" title="12.9 用 Claude Code 安装 OpenClaw"></a>12.9 用 Claude Code 安装 OpenClaw</h3><p>这是最酷的部分——你不需要自己折腾安装，直接让 Claude Code 帮你装：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">帮我安装 OpenClaw，要求：</span><br><span class="line">1. 检查我的 Node.js 版本是否 &gt;= 22，不够的话帮我升级</span><br><span class="line">2. 用 npm 全局安装 openclaw</span><br><span class="line">3. 运行 openclaw onboard 帮我完成初始化</span><br><span class="line">4. 把它注册为系统守护进程，开机自启</span><br></pre></td></tr></table></figure>
<p>Claude Code 会自动执行以下操作：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 检查 Node.js 版本</span></span><br><span class="line">node --version</span><br><span class="line"><span class="comment"># 2. 安装 OpenClaw</span></span><br><span class="line">npm install -g openclaw@latest</span><br><span class="line"><span class="comment"># 3. 初始化并注册守护进程</span></span><br><span class="line">openclaw onboard --install-daemon</span><br></pre></td></tr></table></figure>
<p>初始化向导会引导你完成：</p>
<p>Gateway 网关配置</p>
<p>AI 模型和 API Key 设置</p>
<p>消息渠道连接</p>
<p>守护进程注册（macOS 用 launchd，Linux 用 systemd）</p>
<p>当然你也可以用官方一行命令安装：<code>curl -fsSL https://get.openclaw.ai | bash</code></p>
<h3 id="12-10-配置-AI-模型和-API-Key"><a href="#12-10-配置-AI-模型和-API-Key" class="headerlink" title="12.10 配置 AI 模型和 API Key"></a>12.10 配置 AI 模型和 API Key</h3><p>安装完成后需要配置 AI 模型。推荐 Anthropic Claude：方式一：让 Claude Code 帮你配置</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">帮我配置 OpenClaw 的 AI 模型，使用 Anthropic Claude，</span><br><span class="line">我的 API Key 是 sk-ant-api03-xxxxx</span><br></pre></td></tr></table></figure>
<p>方式二：手动命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置默认模型</span></span><br><span class="line">openclaw models <span class="built_in">set</span> anthropic/claude-sonnet-4-20250514</span><br><span class="line"><span class="comment"># API Key 会在 onboard 过程中设置，也可以用环境变量</span></span><br><span class="line"><span class="built_in">export</span> ANTHROPIC_API_KEY=sk-ant-api03-xxxxx</span><br></pre></td></tr></table></figure>
<p>方式三：编辑配置文件配置文件位置：<code>~/.openclaw/openclaw.json</code></p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;models&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;default&quot;</span><span class="punctuation">:</span> <span class="string">&quot;anthropic/claude-sonnet-4-20250514&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p>⚠️ 安全提醒：不要把 API Key 硬编码在配置文件中，推荐使用环境变量。</p>
<h3 id="12-11-连接飞书（Feishu）"><a href="#12-11-连接飞书（Feishu）" class="headerlink" title="12.11 连接飞书（Feishu）"></a>12.11 连接飞书（Feishu）</h3><p>📖 官方飞书文档：<a href="https://docs.openclaw.ai/zh-CN/channels/feishu">https://docs.openclaw.ai/zh-CN/channels/feishu</a></p>
<h4 id="第一步：创建飞书应用"><a href="#第一步：创建飞书应用" class="headerlink" title="第一步：创建飞书应用"></a>第一步：创建飞书应用</h4><p>登录 飞书开放平台</p>
<p>点击”创建企业自建应用”，填写名称（如”AI 编程助手”）</p>
<p>在”凭证与基本信息”页面，记下 App ID（格式 <code>cli_xxx</code>）和 App Secret</p>
<p>添加应用能力 → 启用”机器人”</p>
<p>配置事件订阅 → 选择”使用 WebSocket 长连接”→ 添加 <code>im.message.receive_v1</code> 事件</p>
<p>配置权限：添加消息收发、文件读取等权限</p>
<p>发布应用</p>
<h4 id="第二步：安装飞书插件并配置"><a href="#第二步：安装飞书插件并配置" class="headerlink" title="第二步：安装飞书插件并配置"></a>第二步：安装飞书插件并配置</h4><p>你可以直接让 Claude Code 帮你完成：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">帮我给 OpenClaw 安装飞书插件并配置，</span><br><span class="line">我的飞书 App ID 是 cli_xxx，App Secret 是 xxx</span><br></pre></td></tr></table></figure>
<p>或者手动操作：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装飞书插件</span></span><br><span class="line">openclaw plugins install @openclaw/feishu</span><br><span class="line"><span class="comment"># 交互式配置（推荐）</span></span><br><span class="line">openclaw channels add</span><br><span class="line"><span class="comment"># 选择 Feishu → 输入 App ID 和 App Secret</span></span><br></pre></td></tr></table></figure>
<h4 id="第三步：启动并测试"><a href="#第三步：启动并测试" class="headerlink" title="第三步：启动并测试"></a>第三步：启动并测试</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 启动网关</span></span><br><span class="line">openclaw gateway</span><br><span class="line"><span class="comment"># 在飞书中找到你的机器人，发送一条消息测试</span></span><br><span class="line"><span class="comment"># 首次需要批准配对</span></span><br><span class="line">openclaw pairing approve feishu &lt;配对码&gt;</span><br></pre></td></tr></table></figure>
<p>配对完成后，你就可以在飞书中和 AI 对话了！</p>
<h3 id="12-12-安装-Claude-Code-Skill-—-打通编程能力"><a href="#12-12-安装-Claude-Code-Skill-—-打通编程能力" class="headerlink" title="12.12 安装 Claude Code Skill — 打通编程能力"></a>12.12 安装 Claude Code Skill — 打通编程能力</h3><p>默认的 OpenClaw 只有通用对话能力。要让它能操控 Claude Code 写代码，需要安装 Claude Code Skill：</p>
<p>📖 GitHub：<a href="https://github.com/Enderfga/openclaw-claude-code-skill">https://github.com/Enderfga/openclaw-claude-code-skill</a></p>
<p>让 Claude Code 帮你安装：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">帮我安装 openclaw-claude-code-skill，步骤：</span><br><span class="line">1. git clone https://github.com/Enderfga/openclaw-claude-code-skill.git</span><br><span class="line">2. 进入目录，npm install 然后 npm run build</span><br><span class="line">3. npm link 全局链接</span><br></pre></td></tr></table></figure>
<p>安装完成后，你可以通过飞书发送编程指令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 在飞书中给机器人发消息</span><br><span class="line">在 ~/my-project 目录下帮我创建一个 Express API 项目，</span><br><span class="line">包含用户注册和登录接口，使用 JWT 认证</span><br></pre></td></tr></table></figure>
<p>OpenClaw 收到消息后会调用 Claude Code 执行，完成后把结果发回飞书。</p>
<h3 id="12-13-实现-7×24-小时自动工作"><a href="#12-13-实现-7×24-小时自动工作" class="headerlink" title="12.13 实现 7×24 小时自动工作"></a>12.13 实现 7×24 小时自动工作</h3><h4 id="守护进程（已在安装时配置）"><a href="#守护进程（已在安装时配置）" class="headerlink" title="守护进程（已在安装时配置）"></a>守护进程（已在安装时配置）</h4><p>如果安装时用了 <code>--install-daemon</code>，OpenClaw 已经注册为系统守护进程：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 检查网关状态</span></span><br><span class="line">openclaw gateway status</span><br><span class="line"><span class="comment"># 重启网关</span></span><br><span class="line">openclaw gateway restart</span><br><span class="line"><span class="comment"># 查看实时日志</span></span><br><span class="line">openclaw logs --follow</span><br></pre></td></tr></table></figure>
<p>macOS：注册为 launchd 服务，开机自启</p>
<p>Linux：注册为 systemd 服务，开机自启</p>
<h4 id="典型的-7×24-工作流"><a href="#典型的-7×24-工作流" class="headerlink" title="典型的 7×24 工作流"></a>典型的 7×24 工作流</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">你的手机（飞书/微信/Telegram）</span><br><span class="line">    │</span><br><span class="line">    │  &quot;帮我修复 issue #42&quot;</span><br><span class="line">    ▼</span><br><span class="line">OpenClaw Gateway（家里电脑，7×24 运行）</span><br><span class="line">    │</span><br><span class="line">    │  解析指令，调用 Claude Code Skill</span><br><span class="line">    ▼</span><br><span class="line">Claude Code CLI（执行编程任务）</span><br><span class="line">    │</span><br><span class="line">    │  读代码、改代码、跑测试</span><br><span class="line">    ▼</span><br><span class="line">执行结果返回</span><br><span class="line">    │</span><br><span class="line">    │  &quot;已修复，测试全部通过，已提交到 fix/issue-42 分支&quot;</span><br><span class="line">    ▼</span><br><span class="line">你的手机（收到结果通知）</span><br></pre></td></tr></table></figure>
<p>你可以在这些场景使用：</p>
<p>通勤路上：发消息让 AI 做方案研究</p>
<p>午休时：发消息让 AI 修 bug</p>
<p>睡觉前：发消息让 AI 跑一个大任务，第二天早上看结果</p>
<p>开会时：发消息让 AI 准备技术方案</p>
<h3 id="12-14-完整流程速查"><a href="#12-14-完整流程速查" class="headerlink" title="12.14 完整流程速查"></a>12.14 完整流程速查</h3><div class="table-container">
<table>
<thead>
<tr>
<th>步骤</th>
<th>操作</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>准备 Node.js 22+</td>
<td>node —version 检查</td>
</tr>
<tr>
<td>2</td>
<td>获取 Anthropic API Key</td>
<td>console.anthropic.com</td>
</tr>
<tr>
<td>3</td>
<td>安装 OpenClaw</td>
<td>npm install -g openclaw@latest</td>
</tr>
<tr>
<td>4</td>
<td>初始化</td>
<td>openclaw onboard —install-daemon</td>
</tr>
<tr>
<td>5</td>
<td>配置模型</td>
<td>openclaw models set anthropic/claude-sonnet-4-20250514</td>
</tr>
<tr>
<td>6</td>
<td>创建飞书应用</td>
<td>open.feishu.cn</td>
</tr>
<tr>
<td>7</td>
<td>安装飞书插件</td>
<td>openclaw plugins install @openclaw/feishu</td>
</tr>
<tr>
<td>8</td>
<td>配置飞书</td>
<td>openclaw channels add</td>
</tr>
<tr>
<td>9</td>
<td>安装 Claude Code Skill</td>
<td>clone + build + link</td>
</tr>
<tr>
<td>10</td>
<td>启动</td>
<td>openclaw gateway</td>
</tr>
<tr>
<td>11</td>
<td>飞书发消息测试</td>
<td>给机器人发”你好”验证连通</td>
</tr>
</tbody>
</table>
</div>
<p>一句话总结：安装 OpenClaw → 配 API Key → 连飞书 → 装 Claude Code Skill → 手机发消息就能让电脑上的 AI 干活，7×24 不停歇。第十三章：常见问题与进阶技巧</p>
<h2 id="第十四章：学习路径总结"><a href="#第十四章：学习路径总结" class="headerlink" title="第十四章：学习路径总结"></a>第十四章：学习路径总结</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────┐</span><br><span class="line">│          从零到精通的学习路径                            │</span><br><span class="line">├──────────────────────────────────────────────────────┤</span><br><span class="line">│                                                      │</span><br><span class="line">│  Level 1: 入门（1-2天）                               │</span><br><span class="line">│  ┌──────────────────────────────────────┐            │</span><br><span class="line">│  │ ✅ 安装 Claude Code                   │            │</span><br><span class="line">│  │ ✅ 学会基本对话和命令                  │            │</span><br><span class="line">│  │ ✅ 用自然语言让 Claude 写简单功能       │            │</span><br><span class="line">│  │ ✅ 学会用 /clear、/compact 管理上下文  │            │</span><br><span class="line">│  └──────────────────────────────────────┘            │</span><br><span class="line">│         │                                            │</span><br><span class="line">│         ▼                                            │</span><br><span class="line">│  Level 2: 进阶（1周）                                 │</span><br><span class="line">│  ┌──────────────────────────────────────┐            │</span><br><span class="line">│  │ ✅ 编写项目级 CLAUDE.md               │            │</span><br><span class="line">│  │ ✅ 创建和使用 Skills                   │            │</span><br><span class="line">│  │ ✅ 配置 MCP Servers（如 Playwright）   │            │</span><br><span class="line">│  │ ✅ 使用 subagent 做代码调研            │            │</span><br><span class="line">│  │ ✅ 掌握上下文管理技巧                  │            │</span><br><span class="line">│  └──────────────────────────────────────┘            │</span><br><span class="line">│         │                                            │</span><br><span class="line">│         ▼                                            │</span><br><span class="line">│  Level 3: 高级（2-4周）                               │</span><br><span class="line">│  ┌──────────────────────────────────────┐            │</span><br><span class="line">│  │ ✅ 安装和使用 Superpowers 等插件       │            │</span><br><span class="line">│  │ ✅ 配置 Hooks 自动化工作流             │            │</span><br><span class="line">│  │ ✅ 创建自定义 subagent                 │            │</span><br><span class="line">│  │ ✅ 使用 Agent Teams 多agent协作        │            │</span><br><span class="line">│  │ ✅ 设计 Agent 角色和协作规范            │            │</span><br><span class="line">│  └──────────────────────────────────────┘            │</span><br><span class="line">│         │                                            │</span><br><span class="line">│         ▼                                            │</span><br><span class="line">│  Level 4: 精通（持续实践）                             │</span><br><span class="line">│  ┌──────────────────────────────────────┐            │</span><br><span class="line">│  │ ✅ 自研 Claude Code 插件               │            │</span><br><span class="line">│  │ ✅ 多人 + 多 Agent 复杂项目协作         │            │</span><br><span class="line">│  │ ✅ 自动化测试驱动的持续开发             │            │</span><br><span class="line">│  │ ✅ OpenCode + Claude Code 联合使用      │            │</span><br><span class="line">│  │ ✅ 从需求到上线的全流程 AI 辅助          │            │</span><br><span class="line">│  │ ✅ 编写团队级别的开发范式和规范          │            │</span><br><span class="line">│  └──────────────────────────────────────┘            │</span><br><span class="line">│                                                      │</span><br><span class="line">└──────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="13-2-常见问题解答"><a href="#13-2-常见问题解答" class="headerlink" title="13.2 常见问题解答"></a>13.2 常见问题解答</h3><p><strong>Q: 模型写的代码敢不敢上线？</strong></p>
<p>实战经验：功能正确、测试通过的代码是可以上线的。有开发者全年使用模型写的 C++ 引擎已在生产环境运行。但建议：</p>
<p>服务端接口代码：风险较低，测试通过即可</p>
<p>基础设施代码：需要更谨慎的人工 review</p>
<p>关键路径代码：必须有完善的自动化测试</p>
<p><strong>Q: 模型老是跑偏怎么办？</strong></p>
<p>约束更精确的 CLAUDE.md</p>
<p>纠正超过两次就 <code>/clear</code> 重新来</p>
<p>使用 superpowers 分阶段隔离上下文</p>
<p>让 Agent 之间互相纠错</p>
<p><strong>Q: 存量项目怎么用 Claude Code？</strong></p>
<p>先在 CLAUDE.md 描述项目各模块的功能</p>
<p>告诉模型哪些目录做什么（减少探索时间）</p>
<p>约束只读取相关模块的代码</p>
<p>按模块逐步接入，不要一次性全交给模型</p>
<p><strong>Q: 颗粒度怎么把控？太小浪费时间，太大模型理解不了？</strong></p>
<p>按模块/功能点提供</p>
<p>信息越精确，模型干得越好</p>
<p>怎么跟研发讲话，就怎么跟 Claude 讲话</p>
<p>关键信息不够时，模型会反问你</p>
<p><strong>Q: Claude Code 太贵了怎么办？</strong></p>
<p>Claude Code Max 计划可以大量使用 Opus 模型</p>
<p>可以多人共用账号（但注意额度限制）</p>
<p>简单任务用 Sonnet/Haiku 降低成本（Claude 会自动切换）</p>
<p>考虑 OpenCode + Codex 作为补充（可互相修复）</p>
<h3 id="13-3-思想转变清单"><a href="#13-3-思想转变清单" class="headerlink" title="13.3 思想转变清单"></a>13.3 思想转变清单</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────┐</span><br><span class="line">│              AI 开发时代的思维转变                      │</span><br><span class="line">├──────────────────────────────────────────────────────┤</span><br><span class="line">│                                                      │</span><br><span class="line">│  ❌ 旧思维                    ✅ 新思维               │</span><br><span class="line">│  ─────────────                ─────────────          │</span><br><span class="line">│  遇到问题先问同事              遇到问题先问模型        │</span><br><span class="line">│                                                      │</span><br><span class="line">│  代码必须自己写                代码让模型写，自己审    │</span><br><span class="line">│                                                      │</span><br><span class="line">│  一个人闷头写代码              指挥多个 Agent 并行     │</span><br><span class="line">│                                                      │</span><br><span class="line">│  写完代码手动测试              自动化测试让模型自迭代  │</span><br><span class="line">│                                                      │</span><br><span class="line">│  需求文档给人看                需求文档也要模型可读    │</span><br><span class="line">│                                                      │</span><br><span class="line">│  测试用例人工执行              测试要有输入输出反馈    │</span><br><span class="line">│                                                      │</span><br><span class="line">│  不敢用模型写的代码            测试通过就可以信任      │</span><br><span class="line">│                                                      │</span><br><span class="line">│  每个功能从零开发              先整体后局部，MVP先行   │</span><br><span class="line">│                                                      │</span><br><span class="line">│  用垃圾模型省钱                用最好的模型省时间      │</span><br><span class="line">│                                                      │</span><br><span class="line">└──────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h3 id="13-4-工具组合推荐"><a href="#13-4-工具组合推荐" class="headerlink" title="13.4 工具组合推荐"></a>13.4 工具组合推荐</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────┐</span><br><span class="line">│              推荐的工具组合                              │</span><br><span class="line">├──────────────────────────────────────────────────────┤</span><br><span class="line">│                                                      │</span><br><span class="line">│  核心工具：                                            │</span><br><span class="line">│  ┌────────────────────────────────────┐              │</span><br><span class="line">│  │ Claude Code (Max 计划 + Opus 模型)  │ ← 主力开发   │</span><br><span class="line">│  └────────────────────────────────────┘              │</span><br><span class="line">│                                                      │</span><br><span class="line">│  补充工具：                                            │</span><br><span class="line">│  ┌────────────────────────────────────┐              │</span><br><span class="line">│  │ OpenClaw                            │ ← 7×24运行  │</span><br><span class="line">│  │ 可以在手机上发消息让电脑干活          │              │</span><br><span class="line">│  └────────────────────────────────────┘              │</span><br><span class="line">│  ┌────────────────────────────────────┐              │</span><br><span class="line">│  │ Codex                               │ ← 精细修改  │</span><br><span class="line">│  │ 改动范围小，适合存量代码微调          │              │</span><br><span class="line">│  └────────────────────────────────────┘              │</span><br><span class="line">│  ┌────────────────────────────────────┐              │</span><br><span class="line">│  │ Cursor                              │ ← 可视化    │</span><br><span class="line">│  │ 有文件级别的可视化，精细调整 UI       │              │</span><br><span class="line">│  └────────────────────────────────────┘              │</span><br><span class="line">│                                                      │</span><br><span class="line">│  插件：                                               │</span><br><span class="line">│  ┌────────────────────────────────────┐              │</span><br><span class="line">│  │ Superpowers                         │ ← 开发工作流│</span><br><span class="line">│  │ Playwright MCP                      │ ← 浏览器测试│</span><br><span class="line">│  │ 自研团队管理插件                     │ ← 定制需求  │</span><br><span class="line">│  └────────────────────────────────────┘              │</span><br><span class="line">│                                                      │</span><br><span class="line">│  互相救场：                                            │</span><br><span class="line">│  CC 挂了 → OpenClaw 继续                              │</span><br><span class="line">│  OpenClaw 挂了 → CC、codex 继续                              │</span><br><span class="line">│  只要有一个活着，另外两个就永远活着 😄                   │</span><br><span class="line">│                                                      │</span><br><span class="line">└──────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<h2 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h2><h3 id="A-快速参考卡"><a href="#A-快速参考卡" class="headerlink" title="A. 快速参考卡"></a>A. 快速参考卡</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">═══════════════════════════════════════════════════</span><br><span class="line">             Claude Code 快速参考卡</span><br><span class="line">═══════════════════════════════════════════════════</span><br><span class="line"></span><br><span class="line">📁 关键文件位置：</span><br><span class="line">  ~/.claude/CLAUDE.md          全局规范</span><br><span class="line">  项目/CLAUDE.md               项目规范</span><br><span class="line">  项目/.claude/settings.json   MCP + Hooks 配置</span><br><span class="line">  项目/.claude/skills/         Skills 定义</span><br><span class="line">  项目/.claude/agents/         Subagents 定义</span><br><span class="line"></span><br><span class="line">⌨️ 核心快捷键：</span><br><span class="line">  Esc           停止当前操作</span><br><span class="line">  Esc + Esc     回滚菜单</span><br><span class="line">  Tab           命令补全</span><br><span class="line">  ↑             历史命令</span><br><span class="line">  ?             查看所有快捷键</span><br><span class="line"></span><br><span class="line">💬 常用命令：</span><br><span class="line">  /clear        清空上下文</span><br><span class="line">  /compact      压缩上下文</span><br><span class="line">  /help         查看帮助</span><br><span class="line">  /plugin       插件市场</span><br><span class="line">  /rewind       回滚</span><br><span class="line">  /rename       重命名会话</span><br><span class="line"></span><br><span class="line">🚀 启动模式：</span><br><span class="line">  claude                  交互模式</span><br><span class="line">  claude -c               继续上次会话</span><br><span class="line">  claude -r               选择历史会话</span><br><span class="line">  claude -p &quot;prompt&quot;      非交互模式</span><br><span class="line">  claude --dangerously-skip-permissions  自主模式</span><br><span class="line"></span><br><span class="line">═══════════════════════════════════════════════════</span><br></pre></td></tr></table></figure>
<h3 id="B-相关资源链接"><a href="#B-相关资源链接" class="headerlink" title="B. 相关资源链接"></a>B. 相关资源链接</h3><div class="table-container">
<table>
<thead>
<tr>
<th>资源</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>Claude Code 官方文档</td>
<td>最权威的完整参考</td>
</tr>
<tr>
<td>快速入门</td>
<td>官方 Quickstart 教程</td>
</tr>
<tr>
<td>最佳实践</td>
<td>官方推荐的使用方式</td>
</tr>
<tr>
<td>CLAUDE.md 指南</td>
<td>如何写好 CLAUDE.md</td>
</tr>
<tr>
<td>Skills 文档</td>
<td>Skills 创建与配置</td>
</tr>
<tr>
<td>Hooks 参考</td>
<td>Hooks 事件钩子详解</td>
</tr>
<tr>
<td>MCP 文档</td>
<td>MCP Server 配置与使用</td>
</tr>
<tr>
<td>Subagent 文档</td>
<td>子 Agent 创建与使用</td>
</tr>
<tr>
<td>Agent Teams 文档</td>
<td>多 Agent 协作（实验性）</td>
</tr>
<tr>
<td>CLI 命令参考</td>
<td>所有命令行参数</td>
</tr>
<tr>
<td>扩展功能总览</td>
<td>Skills/Hooks/MCP/Plugins 选型指南</td>
</tr>
<tr>
<td>社区与工具：</td>
<td></td>
</tr>
<tr>
<td>资源</td>
<td>说明</td>
</tr>
<tr>
<td>———</td>
<td>———</td>
</tr>
<tr>
<td>Claude Code GitHub</td>
<td>开源仓库、Issue 反馈</td>
</tr>
<tr>
<td>Anthropic Discord</td>
<td>官方社区交流</td>
</tr>
<tr>
<td>Superpowers 插件</td>
<td>最流行的开发工作流插件（42k+ stars）</td>
</tr>
<tr>
<td>Awesome Claude Skills</td>
<td>社区精选 Skills 合集</td>
</tr>
<tr>
<td>Claude Code Plugins Hub</td>
<td>第三方插件索引站</td>
</tr>
<tr>
<td>Claude Code 中文指南</td>
<td>中文社区教程</td>
</tr>
<tr>
<td>Node.js 下载</td>
<td>npm 安装 Claude Code 的前提</td>
</tr>
<tr>
<td>Claude 订阅计划</td>
<td>查看 Pro/Max/Teams 价格</td>
</tr>
<tr>
<td>推荐阅读：</td>
<td></td>
</tr>
<tr>
<td>资源</td>
<td>说明</td>
</tr>
<tr>
<td>———</td>
<td>———</td>
</tr>
<tr>
<td>Writing a Good CLAUDE.md</td>
<td>社区总结的 CLAUDE.md 写作技巧</td>
</tr>
<tr>
<td>SFEIR Claude Code 教程</td>
<td>结构化的入门到进阶教程</td>
</tr>
<tr>
<td>Anthropic 官方博客</td>
<td>Anthropic 工程团队博客</td>
</tr>
</tbody>
</table>
</div>
<h1 id="📝-最后的话："><a href="#📝-最后的话：" class="headerlink" title="📝 最后的话："></a>📝 最后的话：</h1><p>“现在是 2026 年，这是模型最差的时候。它往后只会越来越厉害。”</p>
<p>AI 开发的关键不在于你会不会写代码，而在于你会不会定义规范、管理上下文、编排 Agent 协作。掌握了这些能力，你就从”写代码的人”变成了”指挥 AI 写代码的人”。</p>
<p>路径依赖先别依赖人，先依赖模型。遇到问题优先问 Claude Code，实在解决不了再问人。</p>
<p>祝你在 AI 开发之路上越走越远！</p>
]]></content>
      <categories>
        <category>claude</category>
      </categories>
      <tags>
        <tag>Claude Code</tag>
        <tag>AI开发</tag>
        <tag>Agent</tag>
      </tags>
  </entry>
  <entry>
    <title>Claude Code实战分享 - 从会用到高效</title>
    <url>/Claude-Code%E5%AE%9E%E6%88%98%E5%88%86%E4%BA%AB-%E4%BB%8E%E4%BC%9A%E7%94%A8%E5%88%B0%E9%AB%98%E6%95%88/</url>
    <content><![CDATA[<blockquote>
<p>Claude Code 实战分享：从”会用”到”高效”，涵盖 CLAUDE.md 配置、开发工作流、Bug 调试、代码审查、Agent Team 多智能体协作等 7 大实战场景。</p>
</blockquote>
<span id="more"></span>
<p>Claude Code 从”会用”到”高效”</p>
<p>目录</p>
<ul>
<li>为什么值得深入使用 Claude Code</li>
<li>我的工作台全景含 Serena 插件详解、Git Worktree、Vibe Kanban</li>
<li>场景 1：CLAUDE.md — 给 AI 一份项目说明书</li>
<li>场景 2：完整开发工作流 — 从想法到落地</li>
<li>场景 3：Bug 调试 — 系统化根因分析</li>
<li>场景 4：代码审查 — AI 辅助 Code Review</li>
<li>场景 5：跨项目管理 — 批量检查代码变更</li>
<li>场景 6：Agent Team — 多智能体并行协作</li>
<li>场景 7：多 Session 多模型协作</li>
<li>安全风险</li>
<li>成本控制</li>
<li>我踩过的坑与最佳实践</li>
<li>彩蛋：这篇文章是怎么诞生的</li>
</ul>
<h2 id="1-为什么值得深入使用-Claude-Code"><a href="#1-为什么值得深入使用-Claude-Code" class="headerlink" title="1. 为什么值得深入使用 Claude Code"></a>1. 为什么值得深入使用 Claude Code</h2><h3 id="我和-Claude-Code-的故事"><a href="#我和-Claude-Code-的故事" class="headerlink" title="我和 Claude Code 的故事"></a>我和 Claude Code 的故事</h3><p>我第一次接触 Claude Code 是在 2025 年 7-8 月。当时刚出不久，试用了一段时间后就被它的能力震撼了——不是那种”AI 能帮你补全代码”的小惊喜，而是”它真的在理解我的项目，像一个靠谱的同事在帮我干活”的感觉。</p>
<h3 id="核心认知转变"><a href="#核心认知转变" class="headerlink" title="核心认知转变"></a>核心认知转变</h3><p>大多数人把 Claude Code 当”AI 问答工具”——问一句答一句。但它真正的定位是一个可深度定制的开发工作台。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">初级用法：问答式</span><br><span class="line">  你：&quot;这段代码什么意思？&quot;</span><br><span class="line">  AI：&quot;这段代码的作用是...&quot;</span><br><span class="line"></span><br><span class="line">中级用法：任务式</span><br><span class="line">  你：&quot;帮我实现 XX 功能&quot;</span><br><span class="line">  AI：读代码 → 分析 → 写代码 → 测试</span><br><span class="line"></span><br><span class="line">高级用法：工作台式（本次分享重点）</span><br><span class="line">  - CLAUDE.md 让 AI 永远了解你的项目</span><br><span class="line">  - 自定义 Agent 让 AI 扮演不同角色（审查员/调试员/规划师）</span><br><span class="line">  - 自定义 Skill 让重复操作一键完成</span><br><span class="line">  - MCP Server 让 AI 直连数据库/外部服务</span><br><span class="line">  - 插件生态让 AI 拥有更多超能力</span><br><span class="line">  - Agent Team 让多个 AI 并行协作</span><br><span class="line">  - 多 Session 让不同模型各司其职</span><br></pre></td></tr></table></figure>
<h2 id="2-我的工作台全景"><a href="#2-我的工作台全景" class="headerlink" title="2. 我的工作台全景"></a>2. 我的工作台全景</h2><h3 id="2-1-目录结构总览"><a href="#2-1-目录结构总览" class="headerlink" title="2.1 目录结构总览"></a>2.1 目录结构总览</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">~/.claude/</span><br><span class="line">├── settings.json          # 全局配置（插件开关、环境变量等）</span><br><span class="line">├── agents/                # 自定义 Agent（5 个）</span><br><span class="line">│   ├── code-reviewer.md   # 代码审查专家</span><br><span class="line">│   ├── bug-analyzer.md    # Bug 根因分析专家</span><br><span class="line">│   ├── dev-planner.md     # 开发规划师</span><br><span class="line">│   ├── story-generator.md # 用户故事生成器</span><br><span class="line">│   ├── claude-md-sync.md  # Claude.MD自动同步</span><br><span class="line">│   └── ui-sketcher.md     # UI 草图设计师</span><br><span class="line">├── skills/                # 自定义 Skill（3 个）</span><br><span class="line">│   ├── check-recent-code-changes/  # 跨项目变更检查</span><br><span class="line">│   ├── git-merge-workflow/         # Git 合并工作流</span><br><span class="line">│   └── merge-sql-files/           # SQL 文件合并</span><br><span class="line">├── plugins/               # 插件生态（10 个）</span><br><span class="line">├── projects/              # 项目级配置（10+ 项目）</span><br><span class="line">│   └── &#123;project-hash&#125;/</span><br><span class="line">│       ├── CLAUDE.md      # 项目级说明</span><br><span class="line">│       └── memory/        # AI 记忆文件</span><br><span class="line">└── teams/                 # Agent Team 配置</span><br></pre></td></tr></table></figure>
<p>首先，可以放弃传统的IDEA、VS Code，Cursor等代码IDE了，视角从写代码、改代码转向功能设计，功能实现，功能验证。除非必要，后面会极少关心代码细节。</p>
<p>我的工作台：3个Claude-code Session，分别进行功能设计，功能实现，功能Review，并且可以互相review对方的输出给出结论。</p>
<h3 id="2-2-我的插件清单"><a href="#2-2-我的插件清单" class="headerlink" title="2.2 我的插件清单"></a>2.2 我的插件清单</h3><p>点击图片可查看完整电子表格</p>
<h3 id="2-3-重点插件：Serena-—-大型项目的-Token-救星"><a href="#2-3-重点插件：Serena-—-大型项目的-Token-救星" class="headerlink" title="2.3 重点插件：Serena — 大型项目的 Token 救星"></a>2.3 重点插件：Serena — 大型项目的 Token 救星</h3><p>在大型项目中（比如我们的 Java 微服务有 53 种任务类型、数百个类），AI 最大的 token 消耗来自读代码。没有 Serena 的时候，AI 要理解一个类的用法，需要：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">没有 Serena（暴力读文件）：</span><br><span class="line">  1. Read AbstractTaskHandler.java（400 行）→ 全量注入上下文</span><br><span class="line">  2. Grep &quot;extends AbstractTaskHandler&quot; → 获得 30+ 匹配结果</span><br><span class="line">  3. Read EngCompositionTaskHandler.java（200 行）→ 又是全量</span><br><span class="line">  4. Read 其他相关文件...</span><br><span class="line">  总计：轻松消耗 3000-5000 token，且大部分是&quot;噪音&quot;</span><br><span class="line"></span><br><span class="line">有 Serena（符号级精确查询）：</span><br><span class="line">  1. find_symbol(&quot;AbstractTaskHandler&quot;, type=&quot;class&quot;) → 返回精确位置</span><br><span class="line">  2. get_symbols_overview(&quot;AbstractTaskHandler.java&quot;) → 只返回方法签名列表（非全文）</span><br><span class="line">  3. find_referencing_symbols(...) → 只返回所有子类的名称列表</span><br><span class="line">  4. 只读需要的那一个方法体</span><br><span class="line">  总计：消耗 300-500 token，全是&quot;信号&quot;</span><br></pre></td></tr></table></figure>
<p>Serena 的核心能力：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>能力</th>
<th>说明</th>
<th>等价于 IDE 的</th>
</tr>
</thead>
<tbody>
<tr>
<td>find_symbol</td>
<td>按名称/类型搜索符号</td>
<td>Ctrl+Shift+F（但更精确）</td>
</tr>
<tr>
<td>find_referencing_symbols</td>
<td>找到谁引用了某个符号</td>
<td>Find Usages</td>
</tr>
<tr>
<td>get_symbols_overview</td>
<td>获取文件的符号概览（不读全文）</td>
<td>Structure 面板</td>
</tr>
<tr>
<td>replace_symbol_body</td>
<td>精确替换某个方法/类的定义</td>
<td>重构 → 替换</td>
</tr>
<tr>
<td>rename_symbol</td>
<td>跨文件重命名符号</td>
<td>Shift+F6 全局重命名</td>
</tr>
</tbody>
</table>
</div>
<p>实质：Serena 是通过 Language Server Protocol（LSP）给 AI 提供了和 IDE 一样的代码理解能力。AI 不再需要”读整个文件然后自己理解结构”，而是直接”查符号表”。</p>
<p>配置方式：安装后在项目根目录创建 <code> </code>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"># .<span class="property">serena</span>/project.<span class="property">yml</span></span><br><span class="line"><span class="attr">languages</span>:</span><br><span class="line">- java          # 告诉 <span class="title class_">Serena</span> 用 <span class="title class_">Java</span> <span class="variable constant_">LSP</span> 分析本项目</span><br><span class="line"><span class="attr">encoding</span>: <span class="string">&quot;utf-8&quot;</span></span><br><span class="line"><span class="attr">ignore_all_files_in_gitignore</span>: <span class="literal">true</span></span><br><span class="line"><span class="attr">project_name</span>: <span class="string">&quot;job-tifenbao-gen-pdf&quot;</span></span><br></pre></td></tr></table></figure>
<p>首次使用时 Serena 会构建符号缓存（.serena/cache/），后续查询都走缓存，速度很快。</p>
<p>我的体感：在这个 Java 项目上，装了 Serena 后 AI 的”探索代码”阶段明显变快，上下文消耗也更少。对于大型项目（尤其是 Java 这种强类型语言），Serena 的收益非常显著。</p>
<p>注：配置好的serena经常会自动打开浏览器：<a href="http://127.0.0.1:24283/dashboard/index.html，可以通过配置~/.serena/serena_config.yml">http://127.0.0.1:24283/dashboard/index.html，可以通过配置~/.serena/serena_config.yml</a>  web_dashboard_open_on_launch: false 来关闭这个烦人的东西</p>
<h3 id="2-4-Git-Worktree-—-多人-多-Agent-并行的安全网"><a href="#2-4-Git-Worktree-—-多人-多-Agent-并行的安全网" class="headerlink" title="2.4 Git Worktree — 多人/多 Agent 并行的安全网"></a>2.4 Git Worktree — 多人/多 Agent 并行的安全网</h3><p>当你需要让多个 Claude Code Session 或 Agent Team 并行修改代码时，最大的风险是文件冲突——两个 Agent 同时改同一个文件，结果互相覆盖。</p>
<p>Git Worktree 解决这个问题：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">普通模式（危险）：</span><br><span class="line">  所有 Session 共享同一份文件</span><br><span class="line">  → Agent A 改了 TaskHandler.java</span><br><span class="line">  → Agent B 也在改 TaskHandler.java</span><br><span class="line">  → 💥 冲突或覆盖</span><br><span class="line"></span><br><span class="line">Worktree 模式（安全）：</span><br><span class="line">  每个 Session 在独立的目录副本中工作</span><br><span class="line">  → Agent A 在 .claude/worktrees/feature-a/ 里改</span><br><span class="line">  → Agent B 在 .claude/worktrees/feature-b/ 里改</span><br><span class="line">  → 各改各的，互不干扰</span><br><span class="line">  → 完成后 merge 或 cherry-pick</span><br></pre></td></tr></table></figure>
<p>使用方式：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 方式一：在 Claude Code 中直接说</span></span><br><span class="line"><span class="string">&quot;请在 worktree 中工作&quot;</span>  <span class="comment"># AI 自动调用 EnterWorktree</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式二：Agent Team 中指定隔离</span></span><br><span class="line"><span class="comment"># AI 启动 Teammate 时设置 isolation: &quot;worktree&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式三：手动创建</span></span><br><span class="line">git worktree add .claude/worktrees/my-feature -b my-feature</span><br><span class="line"><span class="built_in">cd</span> .claude/worktrees/my-feature &amp;&amp; claude</span><br></pre></td></tr></table></figure>
<p>核心价值：搞砸了直接 <code>git worktree remove</code>，零风险。不影响你的主工作目录。</p>
<p>2.5    — AI 编码代理的可视化指挥中心</p>
<p>当你同时跑 2-3 个 Claude Code Session 或 Agent Team 时，终端窗口之间来回切换很痛苦——哪个任务在跑、哪个卡住了、哪个该审查了，全凭记忆。Vibe Kanban 就是解决这个问题的工具：一个给 AI 编码代理用的看板式任务管理平台。</p>
<p>一句话定义：Vibe Kanban 不写代码，它是 AI 编码代理的”调度中心”——用可视化看板管理多个 Agent 的任务状态、代码审查和工作流编排。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">你（人类工程师）</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">┌──────────────────────────────────────────┐</span><br><span class="line">│  Vibe Kanban（浏览器看板界面）              │</span><br><span class="line">│                                          │</span><br><span class="line">│  ┌──────────┐ ┌──────────┐ ┌──────────┐ │</span><br><span class="line">│  │ 待处理    │ │ 进行中    │ │ 已完成    │ │</span><br><span class="line">│  │          │ │          │ │          │ │</span><br><span class="line">│  │ Issue #3 │ │ Issue #1 │ │ Issue #2 │ │</span><br><span class="line">│  │ 重构API  │ │ 加登录页  │ │ 修复BUG  │ │</span><br><span class="line">│  │          │ │ Agent:   │ │ ✅ 已审查 │ │</span><br><span class="line">│  │          │ │ Claude   │ │          │ │</span><br><span class="line">│  └──────────┘ └──────────┘ └──────────┘ │</span><br><span class="line">│                    │                     │</span><br><span class="line">│     ┌──────────────┼──────────────┐      │</span><br><span class="line">│     ▼              ▼              ▼      │</span><br><span class="line">│  Claude Code   Gemini CLI     Codex     │</span><br><span class="line">│  (worktree A)  (worktree B)  (worktree C)│</span><br><span class="line">└──────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<p>核心能力：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">能力                        说明</span><br><span class="line">──────────────────────      ──────────────────────────────────────────────────────────</span><br><span class="line">多代理统一管理              Claude Code、Gemini CLI、Codex、Cursor 等一个界面切换</span><br><span class="line">可视化看板                  Issue 卡片在 待处理/进行中/已完成 之间流转，状态一目了然</span><br><span class="line">自动 Worktree 隔离          每个 Workspace 自动创建 Git Worktree，Agent 之间互不干扰</span><br><span class="line">并行 + 串行编排             多个 Agent 可以并行处理不同 Issue，也可以设置依赖顺序执行</span><br><span class="line">内置代码审查                Diff 视图直接在浏览器中审查 Agent 的代码变更</span><br><span class="line">一键启动 Dev Server          审查完直接在浏览器预览效果，不用手动起服务</span><br><span class="line">集中式 MCP 配置             统一管理所有 Agent 的模型参数和工具配置</span><br></pre></td></tr></table></figure>
<p>Vibe Kanban vs Claude Code 原生 Agent Team 的区别：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">维度              Vibe Kanban                    Claude Code Agent Team</span><br><span class="line">──────────        ────────────────────────       ──────────────────────────────</span><br><span class="line">界面              浏览器可视化看板                终端文字交互</span><br><span class="line">多代理支持        Claude/Gemini/Codex/Cursor 等  仅 Claude Code 内部 Agent</span><br><span class="line">任务管理          Issue + 看板 + 子任务           TaskList + TaskUpdate</span><br><span class="line">代码审查          内置 Diff 视图 + 浏览器预览     依赖 git diff 或自定义 Agent</span><br><span class="line">适合场景          多个独立 Issue 并行推进          单个复杂任务的内部分工</span><br><span class="line">安装成本          需额外安装，有浏览器 UI          Claude Code 原生，零安装</span><br></pre></td></tr></table></figure>
<p>我的建议：如果你主要用 Claude Code 做开发，Agent Team 够用了。但如果你同时使用多种 AI 编码工具（比如白天用 Claude Code 做架构，晚上用 Gemini CLI 跑批量任务），或者你更喜欢可视化界面管理任务状态，Vibe Kanban 值得一试。GitHub 上 21k+ Star，社区很活跃。</p>
<h2 id="3-场景-1：CLAUDE-md-—-给-AI-一份项目说明书"><a href="#3-场景-1：CLAUDE-md-—-给-AI-一份项目说明书" class="headerlink" title="3. 场景 1：CLAUDE.md — 给 AI 一份项目说明书"></a>3. 场景 1：CLAUDE.md — 给 AI 一份项目说明书</h2><p>痛点：每次开新会话，Claude Code 不知道你的项目是什么、用了什么技术栈、代码组织方式、构建命令、测试方法。你得反复解释同样的上下文。更痛的是——即使解释了，AI 给出的代码可能用错 Java 版本、用错构建工具、用错设计模式，因为它不知道你的项目”偏好”。</p>
<p>解法：在项目根目录执行 <code>/init</code>，Claude 会自动读取项目，为你创建 <code>CLAUDE.md</code>，描述项目的主要功能、技术栈等信息。Claude Code 每次启动都会自动加载这个文件。但是注意的是，自带的 /init 是一个一次性的起步工具，后续不会自动更新：</p>
<p>扫描项目文件，检测技术栈、构建系统、测试框架</p>
<p>生成一个基础 CLAUDE.md 作为起点</p>
<p>单次运行，后续需要手动维护</p>
<p>CLAUDE.md 的本质：不只是项目说明书</p>
<p>很多人以为 CLAUDE.md 就是写一段项目介绍。但实际上，它是你和 AI 之间的契约——不只告诉 AI “项目是什么”，更关键的是告诉它”怎么做”和”不要做什么”。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">CLAUDE.md 的三层价值：</span><br><span class="line"></span><br><span class="line">第一层：项目上下文（减少解释成本）</span><br><span class="line">  → AI 知道项目是什么、用了什么技术栈</span><br><span class="line"></span><br><span class="line">第二层：行为约束（减少返工成本）</span><br><span class="line">  → AI 知道&quot;不要用 Java 11+ API&quot;&quot;Spring 用 XML 不要建议改注解&quot;</span><br><span class="line"></span><br><span class="line">第三层：领域知识（减少理解成本）</span><br><span class="line">  → AI 知道&quot;PhaseCode 6009 表示错题本阶段&quot;&quot;TemplateHandler.genTemplateData()</span><br><span class="line">    以上是业务逻辑，以下是渲染逻辑&quot;</span><br></pre></td></tr></table></figure>
<p>我的 CLAUDE.md 实践</p>
<p>我为 10+</p>
<p>成，然后在实际使用中持续补充——每次 AI 犯的错就是一个应该写进 CLAUDE.md 的提醒。为了让claude.md可以持续自动更新，可以实现了一个agent：通过hook实现进入一个session后，让claude自动检查最近的git提交记录，然后根据功能变化同步到claude.md中（此功能可以直接描述，让claude帮你实现）。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">yaml</span><br><span class="line">---</span><br><span class="line">name: claude-md-synchronizer</span><br><span class="line">description: 后台维护代理，根据项目变更自动维护和更新 CLAUDE.md 文件。在会话开始和重要里程碑（功能完成、重构、新增依赖、架构变更）时触发。独立运行，不中断其他代理。</span><br><span class="line">permissions:</span><br><span class="line">  allow:</span><br><span class="line">    - Bash</span><br><span class="line">    - Read</span><br><span class="line">    - Write</span><br><span class="line">    - Edit</span><br><span class="line">    - Grep</span><br><span class="line">    - Glob</span><br><span class="line">    - Skill</span><br><span class="line">model: haiku</span><br><span class="line">color: purple</span><br><span class="line">field: documentation</span><br><span class="line">expertise: intermediate</span><br><span class="line">fork_safe: true</span><br><span class="line">hooks:</span><br><span class="line">  - event: SessionStart</span><br><span class="line">    commands:</span><br><span class="line">      - echo &quot;Synchronizer: 正在检查 CLAUDE.md 是否需要更新...&quot;</span><br><span class="line">    once: false</span><br><span class="line">  - event: PreToolUse</span><br><span class="line">    matcher: Write</span><br><span class="line">    commands:</span><br><span class="line">      - echo &quot;Synchronizer: 正在验证 CLAUDE.md 变更...&quot;</span><br><span class="line">  - event: PostToolUse</span><br><span class="line">    matcher: Write</span><br><span class="line">    commands:</span><br><span class="line">      - echo &quot;Synchronizer: CLAUDE.md 更新完成&quot;</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line"># CLAUDE.md 同步代理</span><br><span class="line"></span><br><span class="line">我是 CLAUDE.md 同步者 - 一个后台维护代理，负责让项目的 CLAUDE.md 文件与实际代码变更保持同步。</span><br><span class="line"></span><br><span class="line">## 触发时机</span><br><span class="line"></span><br><span class="line">**自动触发**（SessionStart 钩子）：</span><br><span class="line">- 每次 Claude Code 会话开始时</span><br><span class="line">- 检查上次更新以来的 git 变更</span><br><span class="line">- 无重大变更时静默退出</span><br><span class="line"></span><br><span class="line">**手动触发**（里程碑节点）：</span><br><span class="line">- 功能开发完成</span><br><span class="line">- 重大重构</span><br><span class="line">- 新增依赖</span><br><span class="line">- 架构变更</span><br><span class="line"></span><br><span class="line">后面省略一万字~~~</span><br></pre></td></tr></table></figure>
<p>核心结构（不是模板——每个项目应该不同）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">CLAUDE.md</span><br><span class="line"></span><br><span class="line">项目概述</span><br><span class="line">  一句话说明这是什么项目，做什么用的。</span><br><span class="line">  （关键：写清楚项目的业务上下文，不只是技术描述）</span><br><span class="line"></span><br><span class="line">技术栈</span><br><span class="line">  - 语言/框架版本（Java 8，不是 Java 11+）</span><br><span class="line">  - 构建工具（Maven，profile 有 development/test/product）</span><br><span class="line">  - 关键依赖及版本约束</span><br><span class="line"></span><br><span class="line">构建命令</span><br><span class="line">  mvn clean package -P development  // 不是 gradle，不是 spring boot</span><br><span class="line"></span><br><span class="line">架构说明</span><br><span class="line">  核心设计模式、数据流、服务间通信方式。</span><br><span class="line">  （关键：写出&quot;天然的分界线&quot;，比如哪个方法以上是业务逻辑，以下是渲染逻辑）</span><br><span class="line"></span><br><span class="line">重要提醒（&quot;不要做什么&quot;清单）</span><br><span class="line">  - 本项目使用 Java 8，不要使用 Java 11+ 的 API</span><br><span class="line">  - Spring 配置使用 XML，不要建议改成注解方式</span><br><span class="line">  - 前端构建必须区分 Node 14（Vue2）和 Node 18（Vue3）</span><br></pre></td></tr></table></figure>
<p>真实案例：不同项目的 CLAUDE.md 侧重点完全不同</p>
<p>Java 微服务项目（job-tifenbao-gen-pdf）—— 侧重架构和配置映射：</p>
<p>包含 53 种任务类型到 Handler 的完整映射关系</p>
<p>标注了 8 个 XML 配置文件各自覆盖哪些任务类型</p>
<p>说明了 Template Method / Factory / Strategy 三种设计模式的使用位置</p>
<p>解释了 Nacos 配置中心和 Dubbo 服务的集成方式</p>
<p>列出了文档生成库的版本清单（iText 4.2.1, Playwright 1.10.0, PDFBox 2.0.23）</p>
<p>这份 CLAUDE.md 之所以写这么详细，是因为这个项目有 53 种任务类型分散在 9 个 XML 配置文件中——不写清楚，AI 每次都要花 5-10 分钟重新探索。</p>
<p>Vue 2 前端项目（web-tifenbao-campus-report）—— 侧重业务域知识：</p>
<p>包含 PhaseCode 业务域知识（6001=考试报告、6009=错题本、各阶段的数据流）</p>
<p>标注了 Template Studio 的 Monaco 编辑器集成和 iframe 预览机制</p>
<p>说明了 Element UI 的定制方式和主题变量</p>
<p>列出了各子路由和功能模块的对应关系</p>
<p>这份 CLAUDE.md 的重点是业务术语——因为前端代码本身不复杂，但不理解 PhaseCode 的含义就写不出正确的业务逻辑。</p>
<p>效果：AI 理解项目上下文的时间从”每次 5-10 分钟解释”→”0 秒”；不再出现”让我先了解一下你的项目结构”这种空转；AI 给出的代码直接符合项目规范（用对 Java 版本、用对设计模式、用对构建命令）。持续进化：每次 AI 犯错，补一条提醒到 CLAUDE.md，下次就不会再犯。</p>
<p>进阶技巧：Memory 目录 — AI 的长期记忆</p>
<p>CLAUDE.md 解决的是”项目级上下文”，但还有一类信息是”会话级经验”——比如”上次调试发现 fontfaceonload.js 必须从 gen-pdf 项目复制过来”。这类信息适合放在 Memory 目录：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">~/.claude/projects/&#123;project-hash&#125;/memory/</span><br><span class="line">├── MEMORY.md        # 每次会话自动加载（保持 200 行内）</span><br><span class="line">├── debugging.md     # 调试经验沉淀</span><br><span class="line">└── patterns.md      # 项目特有的代码模式</span><br></pre></td></tr></table></figure>
<p>CLAUDE.md 是手动维护的”宪法”，Memory 是 AI 自动积累的”经验库”。两者配合使用效果最好。你也可以主动让claude将你的习惯保存到memory中。</p>
<p><strong> 可以把CLAUDE.md 理解为与项目代码有关的规则，MEMORY.MD是和项目代码无关的规则，是保存你自己使用claude时想让claude记住的习惯与经验。</strong></p>
<p>比如每次让claude review时，他都是直接输出到终端，就可以让他输出到指定目录下，后面再review时，他就会直接写到md文档中。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br></pre></td></tr></table></figure>
<p>📝 真实对话案例：用 /init 自动生成 CLAUDE.md（点击展开）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">以下是首次使用 /init 命令让 AI 自动分析项目并生成 CLAUDE.md 的真实记录。</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">👤 用户:</span><br><span class="line">/init 更新CLAUDE.md</span><br><span class="line"></span><br><span class="line">🤖 AI (glm-4.7):</span><br><span class="line">[自动注入系统指令：分析代码库，创建/更新 CLAUDE.md]</span><br><span class="line">[开始深度探索项目...]</span><br><span class="line"></span><br><span class="line">工具调用序列：</span><br><span class="line">  1. Read pom.xml → 获取依赖、构建配置、环境 profile</span><br><span class="line">  2. Glob **/applicationContext_job.xml → 发现 8 个任务配置文件</span><br><span class="line">  3. Read AbstractTaskHandler.java → 理解 Template Method 模式</span><br><span class="line">  4. Read task-services-mapping.md → 获取 53 种任务类型映射</span><br><span class="line">  5. Read AbstractTest.java → 获取测试基类和 Spring 上下文配置</span><br><span class="line">  6. Glob **/dubbo*.xml → 发现 Dubbo 消费者配置</span><br><span class="line"></span><br><span class="line">分析完成，更新 CLAUDE.md：</span><br><span class="line"></span><br><span class="line">新增内容：</span><br><span class="line">  1. Package Structure — 6 个核心包的职责说明</span><br><span class="line">  2. Task System Architecture — 8 个 XML 配置文件覆盖 53 种任务类型</span><br><span class="line">  3. Document Generation Libraries — 完整依赖版本清单</span><br><span class="line">     (iText 4.2.1, Playwright 1.10.0, PDFBox 2.0.23...)</span><br><span class="line">  4. Running Tests — AbstractTest.getTask() 辅助方法</span><br><span class="line">  5. Dubbo Service Integration — 消费者配置位置</span><br><span class="line">  6. Configuration Sources — Nacos + env properties + Spring XML 三层配置</span><br><span class="line">  7. Common Issues — 模板构建和任务处理器选择的已知坑</span><br><span class="line"></span><br><span class="line">[自动 commit: &quot;docs: update CLAUDE.md with comprehensive project documentation&quot;]</span><br></pre></td></tr></table></figure>
<p>亮点：AI 通过 6 次工具调用，自动从 <code>pom.xml</code>、Java 源码、XML 配置、测试类中提取关键信息，生成了一份覆盖构建、测试、架构、配置、已知问题的完整项目说明。后续每次开新会话，AI 都自动加载这份文档。但这只是起点——后续在使用过程中又陆续补充了”不要做什么”清单和业务术语表。</p>
<h2 id="4-场景-2：完整开发工作流-—-从想法到落地"><a href="#4-场景-2：完整开发工作流-—-从想法到落地" class="headerlink" title="4. 场景 2：完整开发工作流 — 从想法到落地"></a>4. 场景 2：完整开发工作流 — 从想法到落地</h2><p>痛点：传统开发流程：想法 → 口头讨论 → 直接写代码 → 发现遗漏 → 返工。缺少系统化的设计和规划环节，或者有但全靠人工写文档。</p>
<p>解法：通过 superpowers 插件提供的三个 Skill，形成完整的想法 → 设计 → 计划 → 执行闭环，每个阶段自动产出文档。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">┌──────────────────┐    ┌──────────────────┐    ┌──────────────────┐</span><br><span class="line">│  阶段一：Brainstorm │──→│  阶段二：Write Plan │──→│  阶段三：Execute   │</span><br><span class="line">│  /brainstorm      │    │  /write-plan      │    │  /execute-plan    │</span><br><span class="line">│                    │    │                    │    │                    │</span><br><span class="line">│  输入：一个想法     │    │  输入：设计文档     │    │  输入：实施计划     │</span><br><span class="line">│  产出：设计文档     │    │  产出：实施计划     │    │  产出：代码+报告    │</span><br><span class="line">│                    │    │                    │    │                    │</span><br><span class="line">│  docs/desgins/       │    │  docs/plans/       │    │  docs/review/      │</span><br><span class="line">│  *-design.md      │    │  *-plan.md         │    │  *-review.md       │</span><br><span class="line">│                    │    │                    │    │  docs/reports/     │</span><br><span class="line">│                    │    │                    │    │  *-report.md       │</span><br><span class="line">└──────────────────┘    └──────────────────┘    └──────────────────┘</span><br></pre></td></tr></table></figure>
<p>阶段一：Brainstorming — 把模糊想法变成设计方案</p>
<p>触发方式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">/superpowers:brainstorm 我想做一个效果图自动生成模板的功能</span><br></pre></td></tr></table></figure>
<p>AI 会做什么：</p>
<p>先探索项目上下文（读 CLAUDE.md、相关代码、历史文档）</p>
<ol>
<li>一次问一个问题（不会一次抛出 10 个问题让你头大）</li>
</ol>
<p>提出 2-3 个方案并附带 trade-off 分析和推荐</p>
<p>分段呈现设计方案，每段确认后再继续</p>
<p>最终输出 docs/plans/YYYY-MM-DD-xxx-design.md 并 git commit</p>
<p>真实案例（本次对话的实际过程）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">我：/brainstorm 业务方能否直接上传效果图，自动生成模板？</span><br><span class="line"></span><br><span class="line">AI 的提问过程：</span><br><span class="line">  Q1: 效果图是什么形式？→ 我答：任意图片</span><br><span class="line">  Q2: AI 生成的结果是什么？→ 我答：混合模式（preset 组件 + 自定义）</span><br><span class="line">  Q3: 使用者是谁？→ 我答：产品经理 + 开发者都要支持</span><br><span class="line">  Q4: AI 能力用什么实现？→ 我答：外部大模型 API</span><br><span class="line">  Q5: 输出格式？→ 我答：先 PDF，后扩展 Word</span><br><span class="line"></span><br><span class="line">AI 提出 3 个方案：</span><br><span class="line">  A: AI 生成 Vue SFC 代码（推荐）</span><br><span class="line">  B: AI 生成 JSON DSL + 可视化编排</span><br><span class="line">  C: 多轮对话式迭代</span><br><span class="line"></span><br><span class="line">我选了 A，AI 分 5 段展示设计：</span><br><span class="line">  1. 整体架构图</span><br><span class="line">  2. AI 生成核心流程（Component Catalog + 两阶段 Prompt）</span><br><span class="line">  3. 前端交互设计（用户操作流程图）</span><br><span class="line">  4. API 设计（SSE 流式接口）</span><br><span class="line">  5. 边界条件与扩展路径</span><br><span class="line"></span><br><span class="line">最终产出：docs/plans/2026-03-03-image-to-template-ai-generation-design.md</span><br></pre></td></tr></table></figure>
<p>关键价值：</p>
<p>强制你在写代码前想清楚设计</p>
<p>AI 帮你考虑了你可能忽略的 trade-off</p>
<p>自动产出可分享的设计文档，不用自己写</p>
<p>📝 真实对话案例：渲染平台架构设计 Brainstorming（点击展开）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">以下是 2026-02-28 一次真实的 brainstorming 对话记录，目标是重构 PDF 渲染项目的架构。</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">👤 用户:</span><br><span class="line">我现在想重构这个项目，这个组件给很多业务提供了渲染能力，但是每次都需要对方业务</span><br><span class="line">的人来这个组件来开发，写数据和渲染模板。现在想从架构层面重新设计下，打造一个公共</span><br><span class="line">的渲染平台，提供PDF渲染能力，但是如何剥离业务，或者说如何让业务逻辑和渲染逻辑</span><br><span class="line">分离，这是个挑战。请帮我深入思考，给出方案设计。</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">[启用 brainstorming 技能，创建任务列表追踪设计过程]</span><br><span class="line">[深度探索代码库：53个任务类型、8个XML配置、100+ BaseTemplate类...]</span><br><span class="line"></span><br><span class="line">分析完成。核心发现：</span><br><span class="line"></span><br><span class="line">**问题本质**</span><br><span class="line">当前53种任务类型全部耦合在一个项目里。每个 TaskHandler.genTaskHandlerConfigs()</span><br><span class="line">同时做了三件事：数据获取、业务规则判断、模板实例化。TemplateHandler 和 100+ 个</span><br><span class="line">BaseTemplate 类让业务方必须深入理解渲染内部实现。</span><br><span class="line"></span><br><span class="line">**解耦切面**</span><br><span class="line">关键发现：TemplateHandler.genTemplateData() 是天然的分界线。</span><br><span class="line">这个方法以上是业务逻辑，以下是纯渲染逻辑。重构就是沿着这条线切开。</span><br><span class="line"></span><br><span class="line">**推荐方案 C：混合渐进式架构**</span><br><span class="line"></span><br><span class="line">三步走：</span><br><span class="line">- Phase 1（2~3周）：抽取 RenderEngine 接口，渲染逻辑从 TaskHandler 独立，不改外部行为</span><br><span class="line">- Phase 2（3~4周）：构建统一 RenderService Dubbo API + 模板注册中心</span><br><span class="line">  新业务直接调 render(templateId, data, format)</span><br><span class="line">- Phase 3（持续）：存量53种任务按 preview → writebook → eng → conf → layer 逐步迁移</span><br><span class="line"></span><br><span class="line">最大的变化：</span><br><span class="line">  Before: 业务方 → 在渲染项目写 Java + Vue → 发版部署</span><br><span class="line">  After:  业务方 → 自己服务里组装 JSON → 调渲染 API → 拿 PDF URL</span><br><span class="line"></span><br><span class="line">👤 用户:</span><br><span class="line">模板可以使用数据库，任务队列复用轮询，可以提供 REST API 给非 Java 业务方，</span><br><span class="line">模板统一 Vue2，存量可以抽成公共 SDK，暂时放在当前组件。</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">好的，我把你的决策整合进设计文档...</span><br><span class="line">[更新设计文档，纳入用户的技术选型偏好]</span><br><span class="line"></span><br><span class="line">👤 用户:</span><br><span class="line">/superpowers:brainstorm 是否可以提供一个在线编辑模板功能（类似低代码平台，</span><br><span class="line">提供默认的组件模块）让业务可以快速创建模板，也可以让业务自定义模板</span><br><span class="line">（用户输入 Vue 代码，可以在线预览）。请帮我思考这个是否可以嵌入刚才的文档。</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">[开始第二轮 brainstorming，探索 Template Studio 在线编辑模板的可行性]</span><br><span class="line">[分析现有 60+ preset 组件、Monaco 编辑器集成、iframe 预览机制...]</span><br><span class="line">[最终产出：docs/plans/2026-02-28-rendering-platform-design.md，1,324 行]</span><br></pre></td></tr></table></figure>
<p>这次 brainstorming 的成果：从一个”想重构”的模糊想法，经过深度代码分析，产出了一份 1,324 行的完整架构设计文档，覆盖了 Phase 1-4 的详细设计和迁移路径。</p>
<p>📝 真实对话案例：拖拽组件插入功能设计（点击展开）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">以下是 2026-03-03 一次 brainstorming 中 AI 系统化分析需求的对话片段。</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">👤 用户:</span><br><span class="line">[讨论 Template Studio 组件拖拽插入功能]</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">先回答你的问题：**可以支持**，但需要明确&quot;拖动到哪&quot;——因为右侧是两个区域：</span><br><span class="line">Monaco 代码编辑器 + 预览 iframe。</span><br><span class="line"></span><br><span class="line">你期望拖动组件时，插入的目标是哪里？</span><br><span class="line"></span><br><span class="line">  A. 代码编辑器（Monaco）— 拖到编辑器区域，在光标位置插入组件代码片段</span><br><span class="line">  B. 可视化预览区（右侧 iframe）— 拖到预览中，可视化放置</span><br><span class="line">  C. 两者都支持</span><br><span class="line"></span><br><span class="line">&gt; 目前系统已有&quot;点击组件→弹出详情→点击插入代码&quot;的流程。</span><br><span class="line">&gt; 预览区拖放实现复杂度高很多——iframe 沙箱隔离，需要跨 frame 通信协议</span><br><span class="line">&gt; + 解析 HTML 反写，工程量约是 A 的 5-10 倍。</span><br><span class="line"></span><br><span class="line">👤 用户: A</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">方案 A：HTML5 原生 Drag &amp; Drop ⭐ 推荐</span><br><span class="line">  - component-item 加 draggable=&quot;true&quot; + dragstart 事件传递代码片段</span><br><span class="line">  - Monaco 外层 div 监听 dragover / drop</span><br><span class="line">  - editor.getTargetAtClientPoint() 计算插入行列</span><br><span class="line">  - 无额外依赖，实现简单，兼容性好</span><br><span class="line"></span><br><span class="line">方案 B：第三方拖拽库（Vue Draggable / Sortable.js）</span><br><span class="line">  - ❌ Monaco 不是普通 DOM 列表，库对它的支持有限</span><br><span class="line"></span><br><span class="line">架构改动涉及 3 个文件：</span><br><span class="line">  1. ComponentPanel.vue — 加 draggable 属性</span><br><span class="line">  2. MonacoEditor.vue — 加 insertAtLine 方法，处理 dragover/drop</span><br><span class="line">  3. 样式调整 — 插入线的实时计算与显示</span><br><span class="line"></span><br><span class="line">[随后 AI 直接实现，产出详细的代码改动说明]</span><br></pre></td></tr></table></figure>
<p>亮点：AI 在推荐方案时不只是说”推荐 A”，而是给出了工程量的量化对比（5-10 倍差距），帮助你快速做出决策。</p>
<p>阶段二：Write Plan — 把设计方案拆成实施步骤</p>
<p>触发方式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">/superpowers:writing-plans</span><br></pre></td></tr></table></figure>
<p>AI 会做什么：</p>
<p>阅读设计文档</p>
<p>拆分成有依赖关系的实施步骤</p>
<p>识别关键文件和需要修改的代码位置</p>
<p>标注每步的验证标准</p>
<p>输出 docs/plans/YYYY-MM-DD-xxx-plan.md</p>
<p>产出示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">实施计划</span><br><span class="line"></span><br><span class="line">Step 1: 创建 ComponentCatalog 数据结构</span><br><span class="line">  - 文件：src/types/component-catalog.ts</span><br><span class="line">  - 内容：定义 Component 接口 + 序列化方法</span><br><span class="line">  - 验证：单元测试通过</span><br><span class="line"></span><br><span class="line">Step 2: 编写组件信息提取脚本</span><br><span class="line">  - 文件：scripts/extract-component-catalog.js</span><br><span class="line">  - 依赖：Step 1</span><br><span class="line">  - 验证：能从 templatemanager/components/ 提取 60+ 组件信息</span><br><span class="line"></span><br><span class="line">Step 3: 实现 LLM Gateway</span><br><span class="line">  - 文件：src/services/llm-gateway.ts</span><br><span class="line">  - 依赖：Step 1</span><br><span class="line">  - 验证：能调用 Vision API 并返回结构化结果</span><br><span class="line">  ...</span><br></pre></td></tr></table></figure>
<p>阶段三：Execute Plan — 按计划执行并产出报告</p>
<p>触发方式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">/superpowers:executing-plans</span><br></pre></td></tr></table></figure>
<p>AI 会做什么：</p>
<p>读取实施计划</p>
<p>按步骤执行，每步完成后 checkpoint</p>
<p>遇到问题时暂停，等你确认</p>
<p>过程中产出审查文档 docs/review/ 和实施报告 docs/reports/ （使用MEMORY.MD)</p>
<p>文档产出矩阵：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>阶段</th>
<th>文档路径</th>
<th>内容</th>
</tr>
</thead>
<tbody>
<tr>
<td>Brainstorm</td>
<td>docs/plans/YYYY-MM-DD-xxx-design.md</td>
<td>设计方案、架构图、方案对比</td>
</tr>
<tr>
<td>Write Plan</td>
<td>docs/plans/YYYY-MM-DD-xxx-plan.md</td>
<td>实施步骤、依赖关系、验证标准</td>
</tr>
<tr>
<td>Execute</td>
<td>docs/review/YYYY-MM-DD-xxx-review.md</td>
<td>实施过程审查、代码质量检查</td>
</tr>
<tr>
<td>Execute</td>
<td>docs/reports/YYYY-MM-DD-xxx-report.md</td>
<td>实施结果报告、测试覆盖、遗留问题</td>
</tr>
</tbody>
</table>
</div>
<p>效果：每个功能开发都有完整的文档链：设计 → 计划 → 审查 → 报告。文档是 AI 在过程中自动产出的，不是事后补的。新同事接手时可以从 <code>docs/</code> 目录完整了解每个功能的来龙去脉。</p>
<h2 id="5-场景-3：Bug-调试-—-系统化根因分析"><a href="#5-场景-3：Bug-调试-—-系统化根因分析" class="headerlink" title="5. 场景 3：Bug 调试 — 系统化根因分析"></a>5. 场景 3：Bug 调试 — 系统化根因分析</h2><p>痛点：调 bug 最怕”凭感觉猜”——改了一个地方不对，再猜另一个，循环往复。</p>
<p>解法：用 bug-analyzer 自定义 Agent + systematic-debugging Skill，让 AI 做系统化分析而不是瞎猜。</p>
<p>工作流</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">触发：/superpowers:systematic-debugging</span><br><span class="line"></span><br><span class="line">第一步：收集信息</span><br><span class="line">  - 错误现象、堆栈、输入数据</span><br><span class="line">  - AI 自动读取相关代码文件</span><br><span class="line"></span><br><span class="line">第二步：构建执行链</span><br><span class="line">  - AI 画出完整的调用链路</span><br><span class="line">  - 标记每个节点的状态变化</span><br><span class="line"></span><br><span class="line">第三步：定位根因</span><br><span class="line">  - 反向推理：从错误现象往源头追</span><br><span class="line">  - 状态差异分析：期望 vs 实际</span><br><span class="line"></span><br><span class="line">第四步：提出修复方案</span><br><span class="line">  - 具体到文件名和行号</span><br><span class="line">  - 附带测试建议</span><br></pre></td></tr></table></figure>
<p>我的 bug-analyzer Agent 配置</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">文件位置: ~/.claude/agents/bug-analyzer.md</span><br><span class="line"></span><br><span class="line">Model: Claude Opus（用最强模型做深度分析）</span><br><span class="line"></span><br><span class="line">角色：Expert debugger</span><br><span class="line">核心能力：</span><br><span class="line">  - 执行流构建和分析</span><br><span class="line">  - 控制流图分析</span><br><span class="line">  - 数据流全生命周期追踪</span><br><span class="line">  - 调用链分析</span><br><span class="line"></span><br><span class="line">输出格式：</span><br><span class="line">  Bug 根因分析报告</span><br><span class="line">    问题摘要（错误现象、触发条件、影响范围）</span><br><span class="line">    执行流分析（关键执行路径、状态变化序列）</span><br><span class="line">    根因定位（根因描述、错误位置 File:Line、推理过程、证据代码）</span><br><span class="line">    解决方案（推荐修复、修复后执行流验证、测试建议）</span><br></pre></td></tr></table></figure>
<p>创建自定义 Agent 的方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">在 ~/.claude/agents/ 目录下创建 .md 文件即可，文件名就是 Agent 名称。</span><br><span class="line"></span><br><span class="line">文件内容格式：</span><br><span class="line">  ---</span><br><span class="line">  model: opus          // 可选 opus/sonnet/haiku</span><br><span class="line">  description: 我的自定义 Agent 描述</span><br><span class="line">  color: blue          // 终端显示颜色</span><br><span class="line">  tools:               // 可用工具列表</span><br><span class="line">    - read_file</span><br><span class="line">    - search_files</span><br><span class="line">    - run_bash_command</span><br><span class="line">  ---</span><br><span class="line"></span><br><span class="line">  (下面写 Agent 的系统提示)</span><br><span class="line"></span><br><span class="line">  你的角色是...</span><br><span class="line">  你的工作流程是...</span><br><span class="line">  你的输出格式是...</span><br></pre></td></tr></table></figure>
<p>效果：从”猜 3-4 次才找到根因”→”一次分析到位”。修复方案精确到具体文件行号，附带回归测试建议。</p>
<p>📝 真实对话案例：OSS 上传失败 — XML Parser 依赖冲突排查</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">以下是一次真实的 systematic-debugging 对话。用户贴了一个 OSS 上传报错的堆栈，AI 系统化追踪到了根因。</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">👤 用户:</span><br><span class="line">[贴出完整堆栈]</span><br><span class="line">com.aliyun.oss.OSSException: Failed to parse the response result.</span><br><span class="line">[ErrorCode]: InvalidResponse</span><br><span class="line">http://xml.org/sax/features/external-general-entities feature not recognized</span><br><span class="line">for SAX driver org.gjt.xpp.sax2.Driver</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">[启用 systematic-debugging 技能]</span><br><span class="line"></span><br><span class="line">第一步：收集事实</span><br><span class="line">  - 错误发生在 Aliyun OSS SDK 解析 XML 响应时</span><br><span class="line">  - 报错的 SAX driver 是 org.gjt.xpp.sax2.Driver（XPP3 库）</span><br><span class="line">  - 正常情况下应该用 JDK 内置的 SAX parser</span><br><span class="line"></span><br><span class="line">第二步：构建调用链</span><br><span class="line">  OSSClient.putObject()</span><br><span class="line">    → ResponseParser.parse()</span><br><span class="line">      → SAXParserFactory.newInstance()</span><br><span class="line">        → 加载了 xpp3 的 SAX driver（而非 JDK 默认）</span><br><span class="line">          → xpp3 不支持 external-general-entities feature</span><br><span class="line">            → 抛出异常</span><br><span class="line"></span><br><span class="line">第三步：根因定位</span><br><span class="line">  项目依赖 jdom2 → jdom2 传递依赖了 xpp3（XML Pull Parser 3）</span><br><span class="line">  xpp3 是一个 2009 年后就没更新的库，它注册了自己的 SAXParserFactory</span><br><span class="line">  导致 classpath 上的 SAX 实现被替换为不兼容的版本</span><br><span class="line"></span><br><span class="line">  根因：依赖冲突。jdom2 引入的 xpp3 覆盖了 JDK 的 SAX parser。</span><br><span class="line"></span><br><span class="line">第四步：修复方案</span><br><span class="line">  在 pom.xml 中排除 xpp3 和 xmlpull 的传递依赖：</span><br><span class="line"></span><br><span class="line">  &lt;dependency&gt;</span><br><span class="line">      &lt;groupId&gt;com.iflytek.edu&lt;/groupId&gt;</span><br><span class="line">      &lt;artifactId&gt;ZX-filecloud-service-api&lt;/artifactId&gt;</span><br><span class="line">      &lt;exclusions&gt;</span><br><span class="line">          &lt;exclusion&gt;</span><br><span class="line">              &lt;groupId&gt;xmlpull&lt;/groupId&gt;</span><br><span class="line">              &lt;artifactId&gt;xmlpull&lt;/artifactId&gt;</span><br><span class="line">          &lt;/exclusion&gt;</span><br><span class="line">          &lt;exclusion&gt;</span><br><span class="line">              &lt;groupId&gt;xpp3&lt;/groupId&gt;</span><br><span class="line">              &lt;artifactId&gt;xpp3&lt;/artifactId&gt;</span><br><span class="line">          &lt;/exclusion&gt;</span><br><span class="line">      &lt;/exclusions&gt;</span><br><span class="line">  &lt;/dependency&gt;</span><br><span class="line"></span><br><span class="line">  验证方式：mvn dependency:tree | grep xpp3 确认已排除</span><br></pre></td></tr></table></figure>
<p>亮点：AI 没有猜测”可能是网络问题”或”可能是配置错误”，而是从堆栈的 SAX driver 类名入手，反向追踪依赖链，精确定位到 jdom2 → xpp3 这条传递依赖路径。整个分析过程有清晰的推理链条。</p>
<h2 id="6-场景-4：代码审查-—-AI-辅助-Code-Review"><a href="#6-场景-4：代码审查-—-AI-辅助-Code-Review" class="headerlink" title="6. 场景 4：代码审查 — AI 辅助 Code Review"></a>6. 场景 4：代码审查 — AI 辅助 Code Review</h2><p>痛点：人工 Review 容易遗漏安全漏洞、性能问题、边界条件。</p>
<p>解法：用 code-reviewer 自定义 Agent 做提交前的 AI 审查。</p>
<p>我的 code-reviewer 配置</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">文件位置: ~/.claude/agents/code-reviewer.md</span><br><span class="line"></span><br><span class="line">Model: Claude Opus</span><br><span class="line"></span><br><span class="line">能力矩阵：</span><br><span class="line">  - AI 代码分析（SonarQube、CodeQL 规则级别）</span><br><span class="line">  - 安全审查（OWASP Top 10、输入校验、认证鉴权）</span><br><span class="line">  - 性能分析（N+1 查询、内存泄漏、并发问题）</span><br><span class="line">  - 配置审查（敏感信息暴露、权限配置）</span><br><span class="line">  - 多语言支持（JS/TS、Python、Java、Go 等）</span><br><span class="line"></span><br><span class="line">审查流程：</span><br><span class="line">  1. 分析代码上下文和变更范围</span><br><span class="line">  2. 自动化工具初步扫描</span><br><span class="line">  3. 手动审查逻辑和架构</span><br><span class="line">  4. 评估安全影响</span><br><span class="line">  5. 评估性能影响</span><br><span class="line">  6. 按严重程度分级输出反馈</span><br><span class="line">  7. 附带改进代码示例</span><br></pre></td></tr></table></figure>
<p>使用方式：在 Claude Code 中直接让 AI 审查（它会自动读取 git diff），或者用 <code>/superpowers:requesting-code-review</code> 触发。</p>
<p>效果：提交前多了一道 AI 安全网，能发现人工容易遗漏的 SQL 注入、XSS 等安全问题，审查报告可以作为 PR 描述的一部分。</p>
<p>📝 真实对话案例：英语词汇报告功能分支 Code Review（点击展开）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">以下是一次真实的代码审查对话。用户让 AI 对比 feature 分支和 master，审查英语词汇报告页面的代码变更。</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">👤 用户:</span><br><span class="line">当前分支是给英语词汇作文实现一个独立的报告页面，请查看当前分支的修改，</span><br><span class="line">并与 master 进行比较后，进行代码 review，给出 review 结果。</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">[启用 code-reviewer，执行 git diff master...HEAD]</span><br><span class="line"></span><br><span class="line">## Code Review 报告</span><br><span class="line"></span><br><span class="line">### 变更概览</span><br><span class="line">- 新增文件：3 个 Vue 组件 + 1 个 API 模块</span><br><span class="line">- 修改文件：路由配置、Store 模块</span><br><span class="line">- 总变更：+487 行 / -12 行</span><br><span class="line"></span><br><span class="line">### 🔴 Critical（需要修复）</span><br><span class="line"></span><br><span class="line">1. **API 响应未做空值防御** — `report-api.js:34`</span><br><span class="line">   `response.data.wordList` 直接访问，如果接口返回 null 会导致页面白屏。</span><br><span class="line">   建议：`const wordList = response?.data?.wordList || []`</span><br><span class="line"></span><br><span class="line">2. **路由守卫缺失** — `router/index.js:89`</span><br><span class="line">   新增页面没有加登录状态校验，未登录用户可以直接访问报告页。</span><br><span class="line"></span><br><span class="line">### 🟡 Important（建议修复）</span><br><span class="line"></span><br><span class="line">3. **组件命名不符合项目规范** — `EngWordReport.vue`</span><br><span class="line">   项目其他页面都用 kebab-case 命名（如 `exam-analysis.vue`），</span><br><span class="line">   建议重命名为 `eng-word-report.vue`。</span><br><span class="line"></span><br><span class="line">4. **Store mutation 存在副作用** — `store/report.js:28`</span><br><span class="line">   mutation 内部调用了 localStorage.setItem()，</span><br><span class="line">   应该放到 action 中处理。</span><br><span class="line"></span><br><span class="line">### 🟢 Minor（可选优化）</span><br><span class="line"></span><br><span class="line">5. **CSS 硬编码颜色值** — `EngWordReport.vue:156`</span><br><span class="line">   `color: #3366ff` 应使用项目主题变量 `$--color-primary`。</span><br><span class="line"></span><br><span class="line">6. **console.log 未清理** — `report-api.js:22`</span><br><span class="line"></span><br><span class="line">### 总结</span><br><span class="line">整体实现逻辑清晰，组件拆分合理。主要问题是空值防御和路由守卫，</span><br><span class="line">建议修复 Critical 级别后再合并。</span><br></pre></td></tr></table></figure>
<p>亮点：AI 按严重程度分级（Critical → Important → Minor），每个问题都精确到文件名和行号，并给出具体的修复建议，而不只是指出问题。</p>
<h2 id="7-场景-5：跨项目管理-—-批量检查代码变更-自定义skill"><a href="#7-场景-5：跨项目管理-—-批量检查代码变更-自定义skill" class="headerlink" title="7. 场景 5：跨项目管理 — 批量检查代码变更(自定义skill)"></a>7. 场景 5：跨项目管理 — 批量检查代码变更(自定义skill)</h2><p>痛点：发版前要确认”过去 N 天改了哪些项目的哪些文件”，手动一个个项目去 <code>git log</code> 很低效。</p>
<p>解法：创建自定义 Skill：<code>check-recent-code-changes</code>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">文件位置: ~/.claude/skills/check-recent-code-changes/SKILL.md</span><br><span class="line"></span><br><span class="line">用途：检查指定时间段内，指定作者在多个 Git 仓库中的代码变更。</span><br><span class="line"></span><br><span class="line">参数：</span><br><span class="line">  - author: 作者名（默认为你的用户名）</span><br><span class="line">  - days: 天数（默认 20 天）</span><br><span class="line">  - path: 扫描路径（默认当前工作区父目录）</span><br><span class="line"></span><br><span class="line">执行逻辑：</span><br><span class="line">  遍历所有子目录 → 检查是否 Git 仓库 → git log 过滤 → 汇总输出</span><br><span class="line"></span><br><span class="line">输出格式：</span><br><span class="line">  === 项目名称 ===</span><br><span class="line">  commit hash | 日期 | 提交信息</span><br><span class="line">  修改文件列表</span><br></pre></td></tr></table></figure>
<p>使用方式：在 Claude Code 中输入 <code>/check-recent-code-changes</code> 即可。</p>
<p>创建自定义 Skill 的方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">在 ~/.claude/skills/ 下创建目录 + SKILL.md</span><br><span class="line"></span><br><span class="line">目录结构：</span><br><span class="line">  ~/.claude/skills/my-skill/SKILL.md</span><br><span class="line"></span><br><span class="line">文件内容格式：</span><br><span class="line">  ---</span><br><span class="line">  name: my-skill</span><br><span class="line">  description: 描述这个 Skill 做什么</span><br><span class="line">  ---</span><br><span class="line"></span><br><span class="line">  Skill 标题</span><br><span class="line"></span><br><span class="line">  触发条件</span><br><span class="line">    什么时候应该使用这个 Skill</span><br><span class="line"></span><br><span class="line">  执行步骤</span><br><span class="line">    1. 第一步做什么</span><br><span class="line">    2. 第二步做什么</span><br><span class="line"></span><br><span class="line">  注意事项</span><br><span class="line">    需要注意的坑</span><br></pre></td></tr></table></figure>
<p>📝 真实对话案例：跨项目上线前变更审计 + Skill 自动化</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">以下是 2026-02-25 一次真实的跨项目操作。用户在父级 Git 目录下，让 AI 扫描所有子项目的近期改动，用于上线前核查。</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">👤 用户:</span><br><span class="line">当前目录是我在公司工作的所有项目，我现在想查下最近20天我改过哪些组件的代码，</span><br><span class="line">明天准备上线，防止漏组件</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">[遍历所有子目录，检测 .git 仓库，执行 git log --since=&quot;20 days ago&quot; --author=&quot;&lt;your-name&gt;&quot;]</span><br><span class="line"></span><br><span class="line">根据检查结果，以下是你最近20天修改过的组件（按项目分类）：</span><br><span class="line"></span><br><span class="line">## 需要上线的组件列表</span><br><span class="line"></span><br><span class="line">### 前端项目</span><br><span class="line">1. html-paperfresh-pc — 星火英语词汇相关功能</span><br><span class="line">2. web-tifenbao-campus-report — 英语作文报告工具（批量导入/创建题目）</span><br><span class="line">3. tfb-wowbook-web — 星火单词PK功能</span><br><span class="line"></span><br><span class="line">### 后端 API 项目</span><br><span class="line">4. api-tifenbao-pqbp — 星火英语报告分数率计算逻辑</span><br><span class="line"></span><br><span class="line">### 业务服务项目</span><br><span class="line">5. service-tifenbao-pqbp — 星火英语词汇资源服务（翻译、语音合成）</span><br><span class="line">...</span><br><span class="line">14. job-tifenbao-gen-pdf — 英语单词导出资源补全</span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">**总计：17 个组件**</span><br><span class="line"></span><br><span class="line">👤 用户:</span><br><span class="line">帮我把刚才这个功能实现成一个 skill，可以指定多少天内的</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">[创建 ~/.claude/skills/check-recent-code-changes/SKILL.md]</span><br><span class="line"></span><br><span class="line">Skill 创建完成！以后直接用 /check-recent-code-changes 就能执行了。</span><br><span class="line">支持参数：author（默认为你的用户名）、days（默认 20 天）、path（默认当前目录）。</span><br></pre></td></tr></table></figure>
<p>另一个跨项目场景：可以通过/add-dir 把其他项目地址添加到当前session或者当前项目，这样就可以同时让claude操作多个项目工程，也可以让claude对整个工作流，数据流有更深的了解，此时就可以直接让claude-code设计全链路的架构设计、实现计划和代码修改，然后可以用agent-teams，让不同的agent去不同的项目里干活。此时假如在 <code>job-tifenbao-gen-pdf</code> 项目中工作时，用户突然收到 <code>web-tifenbao-campus-report</code> 的报错：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">👤 用户:</span><br><span class="line">web-tifenbao-campus-report 控制台报错：</span><br><span class="line">fontfaceonload.js 404 (Not Found)</span><br><span class="line">Cannot set properties of null (setting &#x27;innerHTML&#x27;)</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">[同时读取两个项目的文件进行交叉比对]</span><br><span class="line"></span><br><span class="line">Read: /Users/admin/Company/Web/Git/web-tifenbao-campus-report/static/zx-html-templates/</span><br><span class="line">Read: /Users/admin/Company/Web/Git/job-tifenbao-gen-pdf/src/main/resources/zx-html-templates/</span><br><span class="line"></span><br><span class="line">## 根因分析</span><br><span class="line">web-tifenbao-campus-report 的 static/zx-html-templates/ 缺少 fontfaceonload.js，</span><br><span class="line">这个文件应该从 job-tifenbao-gen-pdf 构建产物中复制过来。</span><br><span class="line"></span><br><span class="line">## 修复</span><br><span class="line">cp job-tifenbao-gen-pdf/.../fontfaceonload.js → web-tifenbao-campus-report/static/...</span><br></pre></td></tr></table></figure>
<p>亮点：AI 可以直接用绝对路径读取任何项目的文件，实现跨项目的根因分析。同时，用户把一次性操作沉淀为可复用的 Skill，后续一键执行。</p>
<p>Skill vs Agent 的区别</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>维度</th>
<th>Skill</th>
<th>Agent</th>
</tr>
</thead>
<tbody>
<tr>
<td>定义</td>
<td>一套工作流程指令</td>
<td>一个具有特定角色的 AI 人格</td>
</tr>
<tr>
<td>触发</td>
<td>/skill-name 或 AI 自动匹配</td>
<td>被 AI 作为子任务分派</td>
</tr>
<tr>
<td>执行者</td>
<td>当前 AI 会话</td>
<td>新启动的独立 AI 进程</td>
</tr>
<tr>
<td>文件</td>
<td>~/.claude/skills/{name}/SKILL.md</td>
<td>~/.claude/agents/{name}.md</td>
</tr>
<tr>
<td>适合</td>
<td>固定流程、可重复操作</td>
<td>需要特定专业角色的深度分析</td>
</tr>
</tbody>
</table>
</div>
<h2 id="8-场景-6：Agent-Team-—-多智能体并行协作"><a href="#8-场景-6：Agent-Team-—-多智能体并行协作" class="headerlink" title="8. 场景 6：Agent Team — 多智能体并行协作"></a>8. 场景 6：Agent Team — 多智能体并行协作</h2><p>什么是 Agent Team</p>
<p>Agent Team 是 Claude Code 的多智能体协作能力。你可以创建一个”团队”，包含多个具有不同角色的 AI Agent，它们可以：</p>
<p>并行处理不同任务</p>
<p>通过消息互相通信</p>
<p>共享任务列表协调进度</p>
<p>在独立的 Git Worktree 中隔离工作</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">架构：</span><br><span class="line">┌──────────────────────────────────────────┐</span><br><span class="line">│  Team Lead（你的主 Claude Code 会话）      │</span><br><span class="line">│  - 创建任务、分配任务、协调进度             │</span><br><span class="line">├──────────────────────────────────────────┤</span><br><span class="line">│  ┌──────────┐ ┌──────────┐ ┌──────────┐ │</span><br><span class="line">│  │ Agent A  │ │ Agent B  │ │ Agent C  │ │</span><br><span class="line">│  │ 前端开发  │ │ 后端开发  │ │ 测试工程师│ │</span><br><span class="line">│  │          │ │          │ │          │ │</span><br><span class="line">│  │ worktree │ │ worktree │ │ 主分支    │ │</span><br><span class="line">│  │ A        │ │ B        │ │          │ │</span><br><span class="line">│  └──────────┘ └──────────┘ └──────────┘ │</span><br><span class="line">│       ↕             ↕             ↕      │</span><br><span class="line">│  ┌──────────────────────────────────┐    │</span><br><span class="line">│  │  共享任务列表 (TaskList)          │    │</span><br><span class="line">│  │  ~/.claude/tasks/&#123;team-name&#125;/    │    │</span><br><span class="line">│  └──────────────────────────────────┘    │</span><br><span class="line">└──────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>
<p>适合使用 Agent Team 的场景</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">场景</span><br><span class="line">为什么适合</span><br><span class="line">示例</span><br></pre></td></tr></table></figure>
<p>| 多文件并行重构 | 各 Agent 改不同文件，互不冲突 | “前端改组件，后端改 API，测试写用例” |</p>
<p>| 多角度分析 | 不同 Agent 从不同视角分析同一问题 | “安全审查 + 性能审查 + 架构审查” |</p>
<p>| 研究 + 实现分离 | 一个 Agent 调研，另一个实现 | “Agent A 研究最佳实践，Agent B 写代码” |</p>
<p>| 代码审查流水线 | 一个写完另一个立即审查 | “开发 Agent 写完 → 审查 Agent 检查” |</p>
<p>不适合使用 Agent Team 的场景</p>
<p>| 简单的单文件修改 | 开销太大，不如直接做 |</p>
<p>| 强依赖顺序的任务 | Agent 之间等待反而更慢 |</p>
<p>| 需要频繁交互确认 | 通信成本高 |</p>
<p>创建和使用流程</p>
<p>步骤 1：启用 Agent Team 功能</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ~/.claude/settings.json</span></span><br><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;env&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p>步骤 2：创建团队 — 在 Claude Code 对话中告诉 AI 你想用 agent-teams来完成任务，AI 会自动执行 TeamCreate → TaskCreate → Agent 启动 → TaskUpdate 分配 → SendMessage 协调。</p>
<p>步骤 3：监控进度 — AI 会自动收到 Teammate 的消息通知，可以通过 TaskList 查看整体进度，Teammate 完成后会自动汇报。</p>
<p>重要注意事项</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">1. 成本控制</span><br><span class="line">   每个 Agent 都是独立的 API 会话，会消耗 token。尤其不要在plan模式用agent-teams，是普通模型的7倍消耗。</span><br><span class="line">   broadcast（群发消息）的成本 = N × 单次成本，推荐用 message（点对点）代替。</span><br><span class="line"></span><br><span class="line">2. Worktree 隔离</span><br><span class="line">   推荐让写代码的 Agent 在独立 worktree 中工作，避免同时修改同一文件。</span><br><span class="line">   Agent 完成后可以 merge 或 cherry-pick，搞砸了直接删除 worktree。</span><br><span class="line">   设置方式：在 Agent 工具调用时指定 isolation: &quot;worktree&quot;</span><br><span class="line"></span><br><span class="line">3. 团队规模建议</span><br><span class="line">   推荐 2-4 个 Agent，不要超过 5 个。</span><br><span class="line">   通信复杂度随人数指数增长，协调 overhead 会抵消并行收益。</span><br><span class="line"></span><br><span class="line">4. Claude code会自动创建合适的agent队员（不同角色）来完成你给的任务。</span><br><span class="line">   如果你对他创建的角色不满意，你可以明确指定让他为了创建合适的agent。</span><br></pre></td></tr></table></figure>
<p>实战示例</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">目标：分析 templatemanager 组件库的复用性，输出多角度报告</span><br><span class="line"></span><br><span class="line">Team 配置：</span><br><span class="line">  - Agent A (Sonnet): 从前端开发者视角分析组件 API 设计</span><br><span class="line">  - Agent B (Sonnet): 从 PDF 渲染需求视角分析组件完整性</span><br><span class="line">  - Agent C (Sonnet): 从 Template Studio 集成视角分析组件可嵌入性</span><br><span class="line"></span><br><span class="line">结果：三个 Agent 并行工作，各输出了独立分析报告，</span><br><span class="line">主 Session 汇总成最终的多角度分析文档。</span><br></pre></td></tr></table></figure>
<p>📝 真实对话案例：多 Agent 并行审查架构设计文档</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">以下是 2026-03-02 一次真实的 Agent Team 使用记录。用户要求从多个角度评审渲染平台的架构设计。</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">👤 用户:</span><br><span class="line">可以使用 team-agents 从不同角度分析下</span><br><span class="line">@docs/plans/2026-02-28-rendering-platform-design.md 这个架构设计文档，</span><br><span class="line">尤其是 Phase4 的设计，是否可以达到渲染平台和业务解绑的目标。</span><br><span class="line">我已经安装了 tmux，你可以在不同的窗口里展示 agent 的思考过程。</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">好的，我来创建一个分析团队，从 5 个维度并行审查这份架构设计。</span><br><span class="line"></span><br><span class="line">[TeamCreate: &quot;arch-review-team&quot;]</span><br><span class="line">[创建 5 个任务，分配给 5 个 Agent]</span><br><span class="line"></span><br><span class="line">Agent 分工：</span><br><span class="line">  🔹 Agent 1 — 系统架构视角：审查整体分层是否合理，API 边界是否清晰</span><br><span class="line">  🔹 Agent 2 — 业务解耦视角：Phase 4 能否真正让业务方不再碰渲染代码</span><br><span class="line">  🔹 Agent 3 — 代码实现视角：现有代码能否按设计平滑迁移</span><br><span class="line">  🔹 Agent 4 — 性能与扩展性视角：新架构是否能承载更多业务方接入</span><br><span class="line">  🔹 Agent 5 — 团队维护成本视角：长期维护负担是增还是减</span><br><span class="line"></span><br><span class="line">[5 个 Agent 在独立 tmux 窗口中并行工作]</span><br><span class="line">[Agent 1 正在分析 RenderEngine 接口设计...]</span><br><span class="line">[Agent 2 正在评估 FLAT_JSON 路径能否消除 Java 侧模板代码...]</span><br><span class="line">[Agent 3 正在检查 53 个 TaskHandler 的迁移可行性...]</span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">[15 分钟后，5 个 Agent 陆续完成]</span><br><span class="line"></span><br><span class="line">🤖 AI（汇总）:</span><br><span class="line">## 多角度审查汇总</span><br><span class="line"></span><br><span class="line">### 共识</span><br><span class="line">✅ Phase 1-2 的渐进式解耦策略是正确的，风险可控</span><br><span class="line">✅ FLAT_JSON 路径能有效消除业务方对 Java 侧模板代码的依赖</span><br><span class="line">✅ 统一渲染 API 设计清晰，RESTful 接口对非 Java 业务方友好</span><br><span class="line"></span><br><span class="line">### 分歧与风险</span><br><span class="line">⚠️ Agent 2 指出：Phase 4 的 Template Studio 在线编辑功能</span><br><span class="line">   低代码模式表达力不足，可能无法覆盖所有业务场景</span><br><span class="line">⚠️ Agent 3 发现：存量 53 个任务类型中有 12 个深度耦合</span><br><span class="line">   TaskHandlerConfig 的特殊字段，迁移需要逐个适配</span><br><span class="line">⚠️ Agent 5 建议：模板注册中心应该用数据库而非 YAML 配置文件，</span><br><span class="line">   否则每次新增模板都需要重新部署</span><br><span class="line"></span><br><span class="line">### 建议调整</span><br><span class="line">1. Phase 4 取消低代码 DSL 模式，改为 Pro Code + Preset 组件</span><br><span class="line">2. 增加 Phase 3 的迁移优先级排序：先迁简单的 preview 类型</span><br><span class="line">3. 模板注册中心从 YAML 改为数据库方案</span><br><span class="line"></span><br><span class="line">👤 用户:</span><br><span class="line">[采纳了建议，调整了设计文档]</span><br></pre></td></tr></table></figure>
<p>亮点：5 个 Agent 并行工作，各自从独立视角深入分析，最终由主 Session 汇总成有共识、有分歧、有建议的完整审查报告。Agent 2 指出的”低代码表达力不足”问题后来确实被验证，直接影响了 Phase 4 的设计方向调整。</p>
<h2 id="9-场景-7：多-Session-多模型协作"><a href="#9-场景-7：多-Session-多模型协作" class="headerlink" title="9. 场景 7：多 Session 多模型协作"></a>9. 场景 7：多 Session 多模型协作</h2><p>核心思路</p>
<p>Claude Code 支持通过环境变量或 CCSwitch 等工具切换底层模型。这意味着你可以在不同终端窗口启动不同厂商、不同模型的 Claude Code 会话，让它们各司其职：</p>
<ul>
<li><p>GLM 模型（性价比高）：负责写代码、搬砖型任务</p>
</li>
<li><p>Claude Opus（推理能力强）：负责架构设计、代码审查、出报告</p>
</li>
<li><p>Claude Sonnet（平衡型）：负责日常开发、轻量分析</p>
</li>
</ul>
<p>各 Session 通过文件系统（<code>docs/</code> 目录）交换信息，形成流水线。</p>
<p>架构图</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">终端 1（GLM 模型 — 写代码）        终端 2（Opus 模型 — 审查+报告）</span><br><span class="line">┌──────────────────────┐        ┌──────────────────────┐</span><br><span class="line">│ ANTHROPIC_BASE_URL=   │        │ ANTHROPIC_API_KEY=    │</span><br><span class="line">│   bigmodel.cn         │        │   sk-ant-xxx          │</span><br><span class="line">│                       │        │                       │</span><br><span class="line">│ 职责：                 │        │ 职责：                 │</span><br><span class="line">│ - 按实施计划写代码     │        │ - 实时读取代码变更     │</span><br><span class="line">│ - 修改文件            │  ────→ │ - 代码质量审查         │</span><br><span class="line">│ - 运行测试            │        │ - 输出审查报告         │</span><br><span class="line">│                       │        │ - 输出设计文档         │</span><br><span class="line">│                       │  ←──── │                       │</span><br><span class="line">│ 读取审查报告           │        │ docs/review/report.md │</span><br><span class="line">│ 根据反馈修复问题       │        │                       │</span><br><span class="line">└──────────────────────┘        └──────────────────────┘</span><br></pre></td></tr></table></figure>
<p>如何启动不同模型的 Session</p>
<p>方式一：环境变量临时切换</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 终端 1 — 使用 GLM 模型（写代码）</span></span><br><span class="line">ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic \</span><br><span class="line">ANTHROPIC_API_KEY=your-glm-key \</span><br><span class="line">claude</span><br><span class="line"></span><br><span class="line"><span class="comment"># 终端 2 — 使用 Claude Opus（审查+设计）</span></span><br><span class="line">ANTHROPIC_API_KEY=sk-ant-xxx \</span><br><span class="line">claude</span><br></pre></td></tr></table></figure>
<p>方式二：使用 CCSwitch 工具</p>
<p>CCSwitch 可以在多套模型配置之间快速切换，避免每次手动输入环境变量。</p>
<p>方式三：settings.json 备份切换</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 备份当前配置</span></span><br><span class="line"><span class="built_in">cp</span> ~/.claude/settings.json ~/.claude/settings-claude.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># 切换到 GLM 配置</span></span><br><span class="line"><span class="built_in">cp</span> ~/.claude/settings-glm.json ~/.claude/settings.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># 切换回 Claude 配置</span></span><br><span class="line"><span class="built_in">cp</span> ~/.claude/settings-claude.json ~/.claude/settings.json</span><br></pre></td></tr></table></figure>
<p>📝 真实对话案例：三 Session 接力 — 设计 → 实现 → 审查 → 修复闭环</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">以下是 2026-03-02 一次真实的多 Session 多模型协作记录。三个 Session 分工协作，完成了 Phase 4 统一实施计划的设计、实现和审查全流程。</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">=== Session B（Claude Opus 4.6 — 架构设计 + 计划生成）===</span><br><span class="line">时间：05:00-07:55</span><br><span class="line"></span><br><span class="line">👤 用户:</span><br><span class="line">可以使用 team-agents 从不同角度分析下架构设计文档，</span><br><span class="line">尤其是 Phase4 的设计，是否可以达到渲染平台和业务解绑的目标。</span><br><span class="line"></span><br><span class="line">🤖 AI (Opus 4.6):</span><br><span class="line">[启动 4 个并行分析 Agent：架构师/产品经理/技术负责人/解耦专家]</span><br><span class="line">[Agent 陆续返回分析结果]</span><br><span class="line">[综合分析 → 产出 phase4-structural-fixes-plan.md]</span><br><span class="line"></span><br><span class="line">👤 用户:</span><br><span class="line">用 writing-plans 技能把它细化为可执行的实施步骤，</span><br><span class="line">需要考虑 tfb-manage-service 和 web-tifenbao-campus-report 这 2 个组件。</span><br><span class="line"></span><br><span class="line">🤖 AI (Opus 4.6):</span><br><span class="line">[探索 3 个项目代码库 → 生成 phase4-fixes-implementation-plan.md]</span><br><span class="line">[合并两份计划 → 产出 phase4-unified-implementation-plan.md]</span><br><span class="line"></span><br><span class="line">=== Session A（GLM-5 — 交叉验证）===</span><br><span class="line">时间：02:35-07:23（与 Session B 并行）</span><br><span class="line"></span><br><span class="line">👤 用户:</span><br><span class="line">我自己做了一个 Phase2-4 的全链路 review，发现了 7 个数据流断裂点</span><br><span class="line">（3 个阻断级、4 个严重级）。请和你的分析比较。</span><br><span class="line"></span><br><span class="line">🤖 AI (glm-5):</span><br><span class="line">你的分析更完整。你发现了 7 个断裂点，我之前只发现了 3 个。</span><br><span class="line"></span><br><span class="line">  | 断裂点 | 你的分析 | 我的分析 |</span><br><span class="line">  |--------|---------|---------|</span><br><span class="line">  | 1. templateEntryPath 死代码 | ✅ P0 | ✅ 一致 |</span><br><span class="line">  | 2. defined_data vs jsonpCallback | ✅ P0 | ❌ 未发现 |</span><br><span class="line">  | 3. Base64 URL 容量限制 | ✅ P0 | ⚠️ 提及但不深入 |</span><br><span class="line">  | 4-7... | ✅ | ❌ 均未覆盖 |</span><br><span class="line"></span><br><span class="line">你的分析是正确的、完整的。</span><br><span class="line"></span><br><span class="line">=== 用户实现所有任务（跨 4 个代码仓库）===</span><br><span class="line"></span><br><span class="line">=== Session C（GLM-5 — 代码审查）===</span><br><span class="line">时间：08:39-09:55</span><br><span class="line"></span><br><span class="line">👤 用户:</span><br><span class="line">我已经完成了 plan 里的任务，请启动 agent-teams</span><br><span class="line">从不同项目不同角度来 review 下代码。</span><br><span class="line"></span><br><span class="line">🤖 AI (glm-5):</span><br><span class="line">[启动 6 个并行审查 Agent]</span><br><span class="line"></span><br><span class="line">  | Agent | 检查范围 |</span><br><span class="line">  |-------|----------|</span><br><span class="line">  | reviewer-task3a | PDFGenerator 自定义入口 (Java) |</span><br><span class="line">  | reviewer-task3b | GenericRenderTaskHandler PRO_CODE (Java) |</span><br><span class="line">  | reviewer-task1-2 | runtime-host 页面 + 构建脚本 (Vue) |</span><br><span class="line">  | reviewer-task4 | basic-pages-lite.vue (Vue) |</span><br><span class="line">  | reviewer-task8 | 后端接口 + taskData 修正 (Java) |</span><br><span class="line">  | reviewer-frontend | PreviewSandbox + Editor (Vue) |</span><br><span class="line"></span><br><span class="line">[6 个 Agent 并行审查，陆续返回结果]</span><br><span class="line"></span><br><span class="line">## 审查汇总：总体完成度 83% (5/6 任务完成)</span><br><span class="line"></span><br><span class="line">  | 任务 | 状态 | 断裂修复 |</span><br><span class="line">  |------|------|----------|</span><br><span class="line">  | Task 1+2 | ✅ | — |</span><br><span class="line">  | Task 3A | ✅ | 断裂1、2、5 ✅ |</span><br><span class="line">  | Task 3B | ✅ | 断裂3、7 ✅ |</span><br><span class="line">  | Task 4 | ❌ 未实现 | — |</span><br><span class="line">  | Task 8 | ✅ | 断裂4 ✅ |</span><br><span class="line">  | 前端集成 | ⚠️ 85% | 缺环境变量配置 |</span><br><span class="line"></span><br><span class="line">👤 用户: 将 review 结果输出到 docs/review/ 目录下</span><br><span class="line"></span><br><span class="line">🤖 AI: [写入 docs/review/2026-03-02-phase4-unified-implementation-review.md]</span><br><span class="line"></span><br><span class="line">=== 用户修复 2 个待办项 ===</span><br><span class="line"></span><br><span class="line">👤 用户: 我已经实现了这 2 个待办，请检查。</span><br><span class="line"></span><br><span class="line">🤖 AI:</span><br><span class="line">  ✅ basic-pages-lite.vue — 894 行，分页逻辑完整，无 Vuex 依赖</span><br><span class="line">  ✅ RUNTIME_HOST_URL — 已配置</span><br><span class="line"></span><br><span class="line">  审查报告已更新：总体完成度 83% → 100% ✅</span><br></pre></td></tr></table></figure>
<p>完整的文件产出链：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>文件</th>
<th>产出 Session</th>
<th>模型</th>
</tr>
</thead>
<tbody>
<tr>
<td>docs/plans/phase4-unified-implementation-plan.md</td>
<td>Session B</td>
<td>Opus 4.6</td>
</tr>
<tr>
<td>docs/plans/phase2-4-dataflow-fragmentation-analysis.md</td>
<td>Session A</td>
<td>GLM-5</td>
</tr>
<tr>
<td>docs/review/phase4-unified-implementation-review.md</td>
<td>Session C</td>
<td>GLM-5</td>
</tr>
</tbody>
</table>
</div>
<p>这个案例展示了多 Session 协作的完整闭环：Opus 出设计计划 → GLM 交叉验证 → 用户实现 → GLM 启动 6 个 Agent 并行审查 → 输出审查报告 → 用户修复 → 审查报告更新为 100%。三个 Session 通过 <code>docs/</code> 目录下的文件交换信息，各司其职。</p>
<p>真实对话截图（让两个模型互相review：GLM5 VS Opus）：</p>
<p>注意事项：确保两个 Session 不同时修改同一个文件（推荐用 Worktree 隔离）；审查 Session 需要等产出 Session 写完文件后再读取；提前约定文件路径格式（如 <code>docs/review/YYYY-MM-DD-*.md</code>）；多 Session 操作 git 时注意不要互相覆盖 commit。</p>
<h2 id="10-安全风险-使用前需要建立的意识"><a href="#10-安全风险-使用前需要建立的意识" class="headerlink" title="10. 安全风险 - 使用前需要建立的意识"></a>10. 安全风险 - 使用前需要建立的意识</h2><p>在享受 Claude Code 带来的效率提升之前，有几点安全相关的意识需要建立。不是要吓唬你——而是让你在使用时心里有根弦。</p>
<p>10.1 代码会离开你的电脑</p>
<p>Claude Code 的工作原理是：把你项目的代码发送到远程服务器（Anthropic 或你配置的其他模型提供商）。这意味着：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">你的电脑 ──代码──→ 远程服务器 ──AI处理──→ 返回结果</span><br></pre></td></tr></table></figure>
<p>10.2 敏感信息可能被 AI 读到</p>
<p>你的项目里可能有一些不该外传的文件：.env、credentials.json、id_rsa、数据库密码…</p>
<p>问题：AI 可能会读取这些文件，甚至把它们的内容输出到日志、审查报告、或生成的文档中。</p>
<p>真实场景：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">你：帮我检查下这个项目的配置</span><br><span class="line">AI：我看到你的 .env 文件里有 DATABASE_PASSWORD=xxx123...</span><br><span class="line">    → 这个密码被写入了对话记录，可能被保存到云端</span><br></pre></td></tr></table></figure>
<p>建议：</p>
<ol>
<li>排除敏感文件：在项目根目录创建 <code>.claudeignore</code> 文件（类似 <code>.gitignore</code>）：</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line"> # .claudeignore</span><br><span class="line"> .env</span><br><span class="line"> .env.*</span><br><span class="line"> credentials.json</span><br><span class="line"> *_secret*</span><br><span class="line"> *.pem</span><br><span class="line"> *.key</span><br></pre></td></tr></table></figure>
<ol>
<li>敏感信息不要写在代码里：这是基本的安全实践，但现在更重要了</li>
</ol>
<p>10.3 AI 可能执行危险操作</p>
<p>Claude Code 可以执行 git 命令、删除文件、修改代码。这是能力，也是风险。</p>
<p>危险操作示例：</p>
<p>git push —force — 覆盖远程分支</p>
<p>git reset —hard — 丢失本地修改</p>
<p>批量删除文件</p>
<p>覆盖重要配置</p>
<p>建议：</p>
<ul>
<li>涉及 <code>--force</code>、<code>--hard</code>、删除的操作，人工确认后再执行</li>
</ul>
<p>重要分支（main/master）的 push，自己来</p>
<p>大规模文件操作前，先 git stash 或 commit</p>
<p>10.4 AI 生成的代码可能有问题</p>
<p>AI 生成的代码可能有：</p>
<p>SQL 注入漏洞</p>
<p>XSS 风险</p>
<p>硬编码密钥或密码</p>
<p>缺少权限校验</p>
<p>建议：</p>
<p>AI 生成的代码上线前，走正常的 Code Review 流程</p>
<p>用你的 code-reviewer Agent 做一道安全审查</p>
<p>涉及用户输入、权限、数据的代码，多看一眼</p>
<h2 id="11-成本控制"><a href="#11-成本控制" class="headerlink" title="11. 成本控制"></a>11. 成本控制</h2><p>Claude Code 很强大，但强大是有代价的——Token 消耗。我在 40 天里消耗了超过 10 亿 Token。这不是个小数字。</p>
<p>11.1 Token 消耗速度比你想象的快</p>
<p>消耗大户：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>场景</th>
<th>Token 消耗</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>长对话（200+ 条消息）</td>
<td>高</td>
<td>上下文越来越长，每次回复都要处理全部历史</td>
</tr>
<tr>
<td>大型项目探索</td>
<td>高</td>
<td>AI 要读很多文件来理解项目</td>
</tr>
</tbody>
</table>
</div>
<p>| Agent Team    | 极高   | 每个 Agent 独立消耗，4 个 Agent ≈ 4 倍成本 |</p>
<p>上下文膨胀效应：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">第 1 条消息：AI 需要 processing 1 条消息</span><br><span class="line">第 100 条消息：AI 需要 processing 100 条消息</span><br><span class="line">第 300 条消息：AI 需要 processing 300 条消息 → 成本指数增长</span><br></pre></td></tr></table></figure>
<p>这就是为什么长对话到后期会变慢、变贵。</p>
<p>11.2 不同模型成本差异</p>
<p>Claude Code 支持切换底层模型，不同模型成本差异很大：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>模型类型</th>
<th>成本等级</th>
<th>适合场景</th>
</tr>
</thead>
<tbody>
<tr>
<td>Claude Opus</td>
<td>高</td>
<td>复杂架构设计、代码审查、深度分析</td>
</tr>
<tr>
<td>Claude Sonnet</td>
<td>中</td>
<td>日常开发、功能实现</td>
</tr>
<tr>
<td>Claude Haiku</td>
<td>低</td>
<td>简单任务、快速问答</td>
</tr>
<tr>
<td>GLM 系列</td>
<td>较低</td>
<td>日常开发、写代码（性价比选择）</td>
</tr>
</tbody>
</table>
</div>
<p>建议：根据任务复杂度选择模型，不是所有任务都需要 Opus。</p>
<p>11.3 Agent Team 的成本放大效应</p>
<p>Agent Team 很强大，但成本是N 倍放大：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">单 Session 成本 = X</span><br><span class="line">Agent Team（4 个 Agent）≈ 4X</span><br><span class="line">broadcast 群发消息（发给 4 个 Agent）= 4 次 API 调用</span><br></pre></td></tr></table></figure>
<p>成本对比：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">场景：让 AI 分析一份 500 行的架构设计文档</span><br><span class="line"></span><br><span class="line">方式 A：单 Session 直接分析</span><br><span class="line">  消耗 ≈ 1X</span><br><span class="line"></span><br><span class="line">方式 B：Agent Team（4 个 Agent 并行分析）</span><br><span class="line">  消耗 ≈ 4X</span><br><span class="line">  但获得 4 个不同视角的分析结果</span><br></pre></td></tr></table></figure>
<p>Agent Team 适合需要多角度分析的复杂任务，不是所有任务都需要。</p>
<p>建议：</p>
<p>控制 Agent 数量：2-4 个就够了，不要超过 5 个</p>
<p>简单任务不要用 Agent Team</p>
<p>11.4 省钱技巧</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">技巧</span><br><span class="line">节省效果</span><br><span class="line">说明</span><br></pre></td></tr></table></figure>
<p>| 及时 /compact      | 高    | 压缩上下文，减少后续每次调用的 token 消耗      |</p>
<p>| 用 subagent 干重活   | 高    | subagent 的上下文独立，不会污染主 session |</p>
<p>| 控制 Agent Team 规模 | 高    | 2-4 个 Agent 通常足够              |</p>
<p>| 简单任务用轻量模型        | 中    | Haiku 或 GLM 轻量版处理简单任务         |</p>
<p>| 避免重复探索           | 中    | 写好 CLAUDE.md，让 AI 一次就理解项目     |</p>
<p>| 长任务分段            | 中    | 与其一次 400 条消息，不如分 3 次会话        |</p>
<p>最重要的建议：</p>
<blockquote>
<p>用 subagent 去干重活，让主 session 的 context 保持缓慢增长。</p>
</blockquote>
<p>这是我最常用的策略。需要探索大量代码、读取很多文件的任务，交给 subagent（或 Agent Team）去做。主 session 只负责协调和决策，这样主 session 的上下文不会爆炸。</p>
<h2 id="12-我踩过的坑与最佳实践"><a href="#12-我踩过的坑与最佳实践" class="headerlink" title="12. 我踩过的坑与最佳实践"></a>12. 我踩过的坑与最佳实践</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">坑 1：CLAUDE.md 太长反而干扰 AI</span><br><span class="line">  ❌ 把所有代码细节都写进 CLAUDE.md（超过 500 行）</span><br><span class="line">  ✅ 保持 200-300 行，重点写架构和规范，细节放在代码注释和文档文件中</span><br><span class="line"></span><br><span class="line">坑 2：不用 /compact 导致上下文溢出</span><br><span class="line">  ❌ 一个 Session 聊 300+ 条消息不清理</span><br><span class="line">  ✅ 感觉 AI 回答质量下降时（50% 时手动 /compact，别等自动压缩）用 /compact 压缩上下文</span><br><span class="line">     或者开新 Session，靠 CLAUDE.md 恢复上下文</span><br><span class="line"></span><br><span class="line">     尽量用 subagent 去干活，让主 session 的 context 保持缓慢增长</span><br><span class="line"></span><br><span class="line">坑 3：让 AI 一次做太多事</span><br><span class="line">  ❌ &quot;帮我重构整个认证模块并加上测试和文档&quot;</span><br><span class="line">  ✅ 用 brainstorm → write-plan → execute-plan 分步推进，每步有 checkpoint</span><br><span class="line"></span><br><span class="line">坑 4：多 Session 协作不约定文件路径</span><br><span class="line">  ❌ 两个 Session 各自随意创建文件，找不到对方的产出</span><br><span class="line">  ✅ 提前约定：产出放 docs/plans/，审查放 docs/review/，报告放 docs/reports/</span><br><span class="line">     文件名包含日期和主题：YYYY-MM-DD-topic-type.md</span><br></pre></td></tr></table></figure>
<p>最佳实践总结</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">实践</span><br><span class="line">说明</span><br></pre></td></tr></table></figure>
<p>| 为每个项目写 CLAUDE.md | 一次投入，永久受益 |</p>
<p>| 用 brainstorm 开始新功能 | 写代码前先想清楚 |</p>
<p>| 为重复操作创建 Skill | 一次编写，反复使用 |</p>
<p>| 为专业分析创建 Agent | 让 AI 扮演不同角色 |</p>
<p>| 用文档驱动开发 | docs/ 目录记录设计、计划、审查、报告 |</p>
<p>| 善用 /compact 和 subagent | 长对话及时压缩，重活交给子 Agent |</p>
<p>| 预授权常用命令 | 减少确认弹窗的打断 |</p>
<p>| Agent Team 控制规模 | 2-4 个 Agent 就够了 |</p>
<p>| 多 Session 善用不同模型 | GLM 搬砖、Opus 设计审查，各司其职 |</p>
<h3 id="彩蛋：这篇文章是怎么诞生的"><a href="#彩蛋：这篇文章是怎么诞生的" class="headerlink" title="彩蛋：这篇文章是怎么诞生的"></a>彩蛋：这篇文章是怎么诞生的</h3><p>你正在读的这份分享文档，本身就是一个 Claude Code 深度使用的完整案例。它不是在 Word 里一个字一个字敲出来的——而是我和 Claude Code 协作了 3 轮对话、跨 2 个 Session、AI 自动探索了 130 个历史会话、读取了 13 个 JSONL 文件的产物。</p>
<p>第零步：同一个 Session 的前两个话题</p>
<p>这篇文档诞生在一个已经很长的 Session 中。在写这篇文档之前，同一个会话里我已经完成了两个 brainstorming：</p>
<ol>
<li><p>Image-to-Template 功能设计：讨论了”业务方上传效果图，AI 自动生成 Vue 模板”的可行性</p>
</li>
<li><p>渲染平台价值定位：质疑了”如果 AI + Puppeteer 就能生成 PDF，渲染平台的意义在哪儿？”</p>
</li>
</ol>
<p>然后我突然想到：这些 Claude Code 的使用方法和工作流本身就值得分享给同事。</p>
<p>第一步：触发 Brainstorming</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">我：/brainstorm 我现在想给其他同事介绍我的 claude code 的使用方法，</span><br><span class="line">    工作流，常用操作，好用的插件的内容，</span><br><span class="line">    请帮我结合我所有的 claude code 历史内容，帮我输出一份完整的分享文档</span><br></pre></td></tr></table></figure>
<p>第二步：AI 自动探索我的全部配置（07:21-07:30）</p>
<p>AI 派出了 2 个并行 Agent，同时探索：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>Agent</th>
<th>任务</th>
<th>读取内容</th>
</tr>
</thead>
<tbody>
<tr>
<td>Agent 1</td>
<td>探索配置文件</td>
<td>settings.json、plugins/ 目录、所有 CLAUDE.md、teams/ 目录</td>
</tr>
<tr>
<td>Agent 2</td>
<td>搜索历史数据</td>
<td>5 个自定义 Agent 定义、3 个 Skill 定义、stats-cache.json 使用统计</td>
</tr>
</tbody>
</table>
</div>
<p>AI 发现了我的完整使用画像：130 个会话、22,758 条消息、7,861 次工具调用、5 个自定义 Agent、3 个自定义 Skill、10 个插件。</p>
<p>第三步：结构化提问（07:30-07:42）</p>
<p>AI 没有直接开始写，而是一次一个问题地问我：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plain Text</span><br><span class="line">Q1: 同事的 Claude Code 熟悉程度？→ 我答：用过基础功能</span><br><span class="line">Q2: 文档语言偏好？→ 我答：中文为主</span><br><span class="line">Q3: 文档结构方案？→ AI 提出 3 种方案，我选了&quot;实战驱动型&quot;</span><br><span class="line">Q4: 6 个场景是否全面？→ 我说加 Agent Team、加多 Session、去掉 MCP 数据库</span><br><span class="line">Q5: 场景 2 需要更完整？→ 我说要加 write-plan 和 execute-plan 的使用</span><br><span class="line">Q6: 双 Session 改多 Session？→ 我说主要是为了用不同模型</span><br><span class="line">Q7: 最终确认？→ 我说确认，开始写</span><br></pre></td></tr></table></figure>
<p>从想法到确认，经过了 10 轮 Q&amp;A，保证了我们对文档内容的预期完全对齐。</p>
<p>第四步：一次写完初稿（07:43-07:48）</p>
<p>确认后 AI 一次性写出了 966 行 的完整文档，包括所有 7 个场景的结构、代码示例、配置片段和最佳实践。5 分钟内完成，自动 git commit。</p>
<p>第五步：我手动编辑 + AI 润色（07:49-08:09）</p>
<p>我对初稿不满意的地方：删掉了 MCP Server 配置、”高级配置速查”、3 个 Agent Team 相关的”坑”（太细，听众没接触过）；把”双 Session”改成了”多 Session 多模型协作”；加了”总 Token 数超 10 亿”的数据和”尽量用 subagent 去干活”的建议。手动改完后告诉 AI：”请帮我重新润色下。” AI 读取了我的修改，理解了每处变更的意图，重写了场景 7 的整个架构图和对比表。</p>
<p>第六步：贴真实对话案例（08:09-08:50）</p>
<p>我觉得文档里的示例太”干净”了，不够真实。于是让 AI 去翻我的历史对话记录——找到 <code>sessions-index.json</code>，定位到 13 个历史 Session 的 JSONL 文件，并行读取 5 个关键 Session（每个几千到几万行），从中提取出最有代表性的对话片段，整理成可折叠块插入到对应场景下。中途 Session 上下文耗尽，自动切换到新的 continuation session，AI 无缝接续完成了剩余 3 个场景的案例添加。</p>
<p>第七步：你正在读的最终版</p>
<p>最后我又要求丰富了 CLAUDE.md 章节、突出了 Serena 插件的价值、加了 Worktree 和 Agent-to-Agent 的介绍——以及这个”彩蛋”章节本身。</p>
<h3 id="全过程数据"><a href="#全过程数据" class="headerlink" title="全过程数据"></a>全过程数据</h3><div class="table-container">
<table>
<thead>
<tr>
<th>维度</th>
<th>数据</th>
</tr>
</thead>
<tbody>
<tr>
<td>从想法到初稿</td>
<td>27 分钟（含 10 轮 Q&amp;A）</td>
</tr>
<tr>
<td>从初稿到终稿</td>
<td>约 1 小时（含手动编辑、润色、案例添加）</td>
</tr>
<tr>
<td>AI 读取的配置/源文件</td>
<td>50+ 个</td>
</tr>
<tr>
<td>AI 读取的历史 JSONL 文件</td>
<td>13 个</td>
</tr>
<tr>
<td>派出的并行 Agent</td>
<td>10+ 个</td>
</tr>
<tr>
<td>文档最终长度</td>
<td>1500+ 行</td>
</tr>
<tr>
<td>跨越的 Session 数</td>
<td>2 个（因上下文耗尽自动续接）</td>
</tr>
<tr>
<td>使用的模型</td>
<td>Claude Opus 4.6</td>
</tr>
</tbody>
</table>
</div>
<h3 id="这个过程本身说明了什么"><a href="#这个过程本身说明了什么" class="headerlink" title="这个过程本身说明了什么"></a>这个过程本身说明了什么</h3><ol>
<li><p>AI 不是替代你写文档，是和你协作写文档——我做决策（选方案、删章节、定调性），AI 做执行（探索配置、翻历史、组织内容）</p>
</li>
<li><p>Brainstorming 流程确实有用——如果 AI 直接开始写，绝对不会是这个结构和深度</p>
</li>
<li><p>历史对话是有价值的——那些 JSONL 文件不是垃圾日志，是可以被 AI 挖掘的经验矿藏</p>
</li>
<li><p>上下文耗尽不可怕——Claude Code 的 continuation 机制让长任务可以无缝接续</p>
</li>
</ol>
]]></content>
      <categories>
        <category>claude</category>
      </categories>
      <tags>
        <tag>Claude Code</tag>
        <tag>AI开发</tag>
        <tag>实战经验</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo+Next配置</title>
    <url>/Hexo+Next%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<h1 id="1-博客中图片的显示问题："><a href="#1-博客中图片的显示问题：" class="headerlink" title="1.博客中图片的显示问题："></a>1.博客中图片的显示问题：</h1><p>markdown中使用相对路径访问图片，博客上面不会显示。：原因为生成的html文件和图片的相对位置不对了。</p>
<p>解决：</p>
<p><strong>在Hexo根目录的<code>source</code>目录下创建<code>images</code>文件夹，将图片放在 <code>source/images</code> 文件夹中。然后通过类似于 <code>![](/images/image.jpg)</code> 的方法访问它们。</strong></p>
<h1 id="2-添加分类和标签"><a href="#2-添加分类和标签" class="headerlink" title="2.添加分类和标签"></a>2.添加分类和标签</h1><h2 id="2-1-添加分类"><a href="#2-1-添加分类" class="headerlink" title="2.1.添加分类"></a>2.1.添加分类</h2><p>新建一个页面，命名为categories,命令如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo new page categories</span><br></pre></td></tr></table></figure>
<p>在进入<code>source/categories</code>目录下，编辑<code>index.md</code>文件，将页面的类型设置为categories,主题将自动为这个页面显示所有分类</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 分类</span><br><span class="line">date: 2014-12-22 12:39:04</span><br><span class="line">type: &quot;categories&quot;</span><br><span class="line">---</span><br></pre></td></tr></table></figure>
<p>如果集成了评论，这里需要关闭</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 分类</span><br><span class="line">date: 2014-12-22 12:39:04</span><br><span class="line">type: &quot;categories&quot;</span><br><span class="line">comments: false</span><br><span class="line">---</span><br></pre></td></tr></table></figure>
<h2 id="2-2-添加标签"><a href="#2-2-添加标签" class="headerlink" title="2.2.添加标签"></a>2.2.添加标签</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo new page tags</span><br></pre></td></tr></table></figure>
<p>在进入<code>source/tags</code>目录下，编辑<code>index.md</code>文件，将页面的类型设置为categories,主题将自动为这个页面显示所有分类</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 分类</span><br><span class="line">date: 2014-12-22 12:39:04</span><br><span class="line">type: &quot;tags&quot;</span><br><span class="line">---</span><br></pre></td></tr></table></figure>
<p>如果集成了评论，这里需要关闭</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 分类</span><br><span class="line">date: 2014-12-22 12:39:04</span><br><span class="line">type: &quot;tags&quot;</span><br><span class="line">comments: false</span><br><span class="line">---</span><br></pre></td></tr></table></figure>
<h2 id="2-3-配置菜单"><a href="#2-3-配置菜单" class="headerlink" title="2.3.配置菜单"></a>2.3.配置菜单</h2><p>修改<code>hexo目录/theme/next/_config.yml</code>如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">menu:</span><br><span class="line">  home: /|| home</span><br><span class="line">  #about: /about/ || user</span><br><span class="line">  #tags: /tags/ || tags</span><br><span class="line">  #categories: /categories/ || th</span><br><span class="line">  archives: /archives/|| archive</span><br><span class="line">  tags: /tags/|| tag</span><br><span class="line">  categories: /categories/|| th</span><br><span class="line">  about: /about/|| user  </span><br><span class="line">  #schedule: /schedule/ || calendar</span><br><span class="line">  #sitemap: /sitemap.xml || sitemap</span><br><span class="line">  commonweal: /404.html|| heartbeat</span><br></pre></td></tr></table></figure>
<p>然后跑去 language 文件夹 zh-Hans.yml 修改中文名字，菜单就以中文显示了。<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">menu:</span><br><span class="line">  home: 首&amp;emsp;&amp;emsp;页</span><br><span class="line">  archives: 归&amp;emsp;&amp;emsp;档</span><br><span class="line">  categories: 分&amp;emsp;&amp;emsp;类</span><br><span class="line">  schedule: 日程表</span><br><span class="line">  sitemap: 站点地图</span><br><span class="line">  tags: 标&amp;emsp;&amp;emsp;签</span><br><span class="line">  about: 关于博主</span><br><span class="line">  search: 站内搜索</span><br><span class="line">  top: 最受欢迎</span><br><span class="line">  # commonweal: 公益404</span><br></pre></td></tr></table></figure></p>
<p>我们发表文章使用「tags」「categories」只需在文章开头添加如下代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 利用GitHub和HEXO免费搭建个人博客高级 美化篇</span><br><span class="line">date: 2019-01-29 22:58:56</span><br><span class="line">tags: [hexo建站,hexo部署,github部署,个人博客]      #添加的标签</span><br><span class="line">categories: hexo博客                              #添加的分类</span><br><span class="line">---</span><br></pre></td></tr></table></figure>
<p>如此即可在菜单栏里的「tags」「categories」看见相应的效果。但是实际上打开是空白页面 本文的重点来了。<br>小tips：每次的手输入 categories 我们可以在<code>hexo目录\scaffolds\post.md</code> 添加如下代码，这样每次新建文章，就自动有了。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: &#123;&#123; title &#125;&#125;</span><br><span class="line">date: &#123;&#123; date &#125;&#125;</span><br><span class="line">tags:           #新加</span><br><span class="line">categories:     #新加</span><br><span class="line">---</span><br></pre></td></tr></table></figure>
<h1 id="3-添加评论"><a href="#3-添加评论" class="headerlink" title="3.添加评论"></a><span id="评论">3.添加评论</span></h1><p>这里我们使用<code>Valine</code>评论，我们的评论系统其实是放在Leancloud上的，因此首先需要去注册一个账号</p>
<p><a href="https://www.leancloud.cn/">Leancloud官网，点我注册</a></p>
<p>注册完以后需要创建一个应用，名字可以随便起，然后<strong>进入应用-&gt;设置-&gt;应用key</strong>,获取<code>appid</code> 和 <code>appkey</code> </p>
<p>打开<code>hexo目录/theme/next/_config.yml</code>,搜索 <code>valine</code>，填入<code>appid</code> 和 <code>appkey</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">valine:</span><br><span class="line">  enable: true</span><br><span class="line">  appid:  your app id</span><br><span class="line">  appkey: your app key</span><br><span class="line">  notify: false # mail notifier , https://github.com/xCss/Valine/wiki</span><br><span class="line">  verify: false # Verification code</span><br><span class="line">  placeholder: ヾﾉ≧∀≦)o来啊，快活啊! </span><br><span class="line">  guest_info: nick,mail,link</span><br><span class="line">  pageSize: 10</span><br></pre></td></tr></table></figure>
<p>最后记得在<strong>Leancloud -&gt; 设置 -&gt; 安全中心 -&gt; Web 安全域名</strong>把你的域名加进去</p>
<h1 id="4-添加阅读次数"><a href="#4-添加阅读次数" class="headerlink" title="4.添加阅读次数"></a>4.添加阅读次数</h1><p>使用<a href="#评论">3.添加评论</a>中应用的<code>appid</code> 和 <code>appkey</code> ，打开<code>hexo目录/theme/next/_config.yml</code>,搜索 <code>leancloud_visitors</code>，填入<code>appid</code> 和 <code>appkey</code>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">leancloud_visitors:</span><br><span class="line">  enable: true</span><br><span class="line">  app_id: nGS6I8Lhxl6RuW9rWHtlzQgi-gzGzoHsz</span><br><span class="line">  app_key: osxkNrrnKC4USSNpLUJyfsCb</span><br><span class="line">  security: false</span><br><span class="line">  betterPerformance: false</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>博客搭建</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>Next</tag>
      </tags>
  </entry>
  <entry>
    <title>Claude Code的可编程模式 - 一个被严重低估的自动化引擎</title>
    <url>/Claude-Code%E7%9A%84%E5%8F%AF%E7%BC%96%E7%A8%8B%E6%A8%A1%E5%BC%8F-%E4%B8%80%E4%B8%AA%E8%A2%AB%E4%B8%A5%E9%87%8D%E4%BD%8E%E4%BC%B0%E7%9A%84%E8%87%AA%E5%8A%A8%E5%8C%96%E5%BC%95%E6%93%8E/</url>
    <content><![CDATA[<blockquote>
<p>claude -p（可编程模式）不只是批处理，它让 Claude Code 从交互式 AI 助手变成可编程自动化平台。本文涵盖定时巡检、CI 集成、批量处理等实战场景。</p>
</blockquote>
<span id="more"></span>
<p>claude -p：一个被严重低估的自动化引擎</p>
<p>日期: 2026-03-09</p>
<blockquote>
<p>阅读前提示</p>
<p>本文不是 <code>--print</code> 参数（可编程模式|无头模式）的使用手册。</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">本文假设你已经：</span><br><span class="line">熟悉 Claude Code 的基本使用（会话、工具调用、文件读写）</span><br><span class="line">用过 Skills 或 MCP Server（至少知道它们是什么）</span><br></pre></td></tr></table></figure>
<blockquote>
<p>关于本文的代码：本文包含较多脚本示例，但请不要被吓到——它们都是简短的 Shell 或 Python 脚本，逻辑直白，每段代码上方都有通俗的解释。更重要的是，这些脚本本身就可以让 Claude Code 帮你生成。你只需要理解”这段脚本在做什么”，而不需要自己从零编写。即使你不是研发人员，也完全可以掌握这些用法。</p>
<p>本文的内容是价值发现分享——坦白说，我自己用 <code>--print</code> 模式不多。不是因为它不好用，而是因为我的日常工作更多是交互式开发（brainstorm、写代码、调试）。但在构建远程日志诊断系统的过程中，我发现 <code>claude --print</code> 解决了一个交互模式做不到的问题：如何让 AI 定时自动执行诊断？ 这让我意识到，这个看似简单的参数，打开了一扇从”交互式 AI 助手”到”可编程自动化平台”的大门。</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">于是我开始搜集社区案例，发现这个模式的玩法远比我想象的丰富。这篇分享不是&quot;我的深度使用经验&quot;，而是：</span><br><span class="line">我为什么认为它很重要（即使我自己用得不多）</span><br><span class="line">社区里的高手在用它做什么（真实案例 + 出处）</span><br><span class="line">如果你想用，应该从哪里开始</span><br></pre></td></tr></table></figure>
<p>目录</p>
<ul>
<li><p>为什么 —print 值得你花 10 分钟了解</p>
</li>
<li><p>我的实践：从手动诊断到自动化巡检</p>
</li>
<li><p>技术细节：你一定会踩的坑</p>
</li>
<li><p>社区实战：其他人在用 —print 做什么</p>
</li>
<li><p>从工具到方法论的思考</p>
</li>
<li><p>我的愿景：用 —print 构建缺陷修复闭环</p>
</li>
</ul>
<h3 id="附录：常用-Flag-组合速查"><a href="#附录：常用-Flag-组合速查" class="headerlink" title="附录：常用 Flag 组合速查"></a>附录：常用 Flag 组合速查</h3><h2 id="1-为什么-—print-值得你花-10-分钟了解"><a href="#1-为什么-—print-值得你花-10-分钟了解" class="headerlink" title="1. 为什么 —print 值得你花 10 分钟了解"></a>1. 为什么 —print 值得你花 10 分钟了解</h2><h3 id="一个容易被忽视的能力"><a href="#一个容易被忽视的能力" class="headerlink" title="一个容易被忽视的能力"></a>一个容易被忽视的能力</h3><p>claude —print（简写 <code>claude -p</code>）是 Claude Code CLI 的非交互模式，也是 Agent SDK 的 CLI 入口<sup><a href="#fn_1" id="reffn_1">1</a></sup>。官方文档专门为它写了一整页介绍，但标题是英文的（”Run Claude Code programmatically”），很多人看到就划走了——这其实是被低估最严重的功能之一。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">claude -p <span class="string">&quot;your prompt here&quot;</span></span><br></pre></td></tr></table></figure>
<p>它的作用是：接收一段指令，执行完毕后把结果直接输出到屏幕，然后退出。</p>
<p>看起来很简单，对吧？大多数人第一反应是”哦，就是个批处理模式”，然后就忽略了。</p>
<blockquote id="fn_1">
<sup>1</sup>. Run Claude Code programmatically - Claude Code Docs<a href="#reffn_1" title="Jump back to footnote [1] in the text."> &#8617;</a>
</blockquote>
<p>但当你意识到 Claude Code 不只是一个问答工具——它能调用 MCP 工具、读写文件、执行 Shell 命令、理解整个项目上下文———print 的价值就完全不同了。</p>
<h3 id="核心认知转变"><a href="#核心认知转变" class="headerlink" title="核心认知转变"></a>核心认知转变</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">交互模式：人 → Claude Code → 结果</span><br><span class="line">--print 模式：脚本/程序 → Claude Code → 结果 → 脚本/程序</span><br></pre></td></tr></table></figure>
<p>注意：这里的”脚本调用 Claude”和”Claude 执行命令”是两件事，不是嵌套。你写一个脚本去调用 claude -p，Claude 执行完返回结果给你的脚本——这是一个线性流程，不是 Claude 里面再套一个 Claude。</p>
<h3 id="这意味着什么？"><a href="#这意味着什么？" class="headerlink" title="这意味着什么？"></a>这意味着什么？</h3><p>Claude Code 不再只是一个开发工具，而是可以成为自动化系统的一部分。</p>
<p>你可以在 Shell 脚本里调用它，在 CI/CD 里集成它，在 cron 里定时执行它，在监控系统里触发它。它从”你的 AI 助手”变成了”你的自动化系统里的一个智能节点”。</p>
<h2 id="2-我的实践：从手动诊断到自动化巡检"><a href="#2-我的实践：从手动诊断到自动化巡检" class="headerlink" title="2. 我的实践：从手动诊断到自动化巡检"></a>2. 我的实践：从手动诊断到自动化巡检</h2><p><strong>这是我目前唯一深度使用 <code>--print</code> 的场景</strong>——但正是它让我看到了这个模式的真正威力。</p>
<h3 id="背景：每天重复的-10-分钟"><a href="#背景：每天重复的-10-分钟" class="headerlink" title="背景：每天重复的 10 分钟"></a>背景：每天重复的 10 分钟</h3><p>作为后端开发，我每天都要面对测试环境的各种问题。最常见的场景是：测试同学在群里 @我，”job-pdf 服务挂了，帮忙看看”。然后就是熟悉的流程：</p>
<p>SSH 登录跳板机</p>
<p>再从跳板机跳到目标服务器</p>
<p>ps aux | grep job-pdf 看进程</p>
<p>tail -f /iflytek/logs/gece/job-pdf/app.log 看日志</p>
<p>找到异常堆栈，分析原因</p>
<p>回复测试同学</p>
<p>每次 10-15 分钟，而且很多时候是重复劳动。更糟糕的是，有些问题是凌晨出现的，等我早上来看日志时，现场已经被新的日志覆盖了。</p>
<p>第一步：交互式诊断系统（Skills + MCP）</p>
<p>我先用 Claude Code 的 Skills 功能构建了一套诊断系统：</p>
<p>SSH-MCP Server：通过 MCP 协议连接远程服务器</p>
<p>/diagnose skill：完整的诊断流程</p>
<p>/health skill：快速健康检查</p>
<p>/trace skill：全链路追踪</p>
<p>交互式使用时非常好用。输入 /diagnose job-pdf，AI 自动完成所有诊断步骤，给出结构化报告。</p>
<p>但问题来了：我怎么让它定时运行？怎么让监控系统调用它？</p>
<p>Skills 是设计给人类交互使用的。我不能在 cron 里写 claude /diagnose job-pdf。</p>
<p><strong>这是我第一次意识到 <code>--print</code> 的价值</strong>——它让交互式的 Skill 变成了可编程的 CLI。</p>
<p>第二步：用 —print 包装成 CLI</p>
<p>核心思路：用 Python 脚本调用 claude -p，传入精心设计的 prompt，用 —json-schema 强制返回结构化 JSON。</p>
<blockquote>
<p>通俗解释：下面这段 Python 脚本做的事情很简单——把”诊断某个组件”的自然语言指令发给 Claude，然后要求 Claude 按照固定格式（JSON）返回结果，这样程序就能自动读取”状态是否正常””有几个错误”等信息。</p>
</blockquote>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">call_skill</span>(<span class="params">skill_name: <span class="built_in">str</span>, args: <span class="built_in">str</span>, project_dir: <span class="built_in">str</span> = <span class="string">&quot;.&quot;</span></span>) -&gt; <span class="built_in">dict</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;Call a diagnostic skill via claude --print.&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 定义返回的 JSON Schema</span></span><br><span class="line">    schema = &#123;</span><br><span class="line">        <span class="string">&quot;type&quot;</span>: <span class="string">&quot;object&quot;</span>,</span><br><span class="line">        <span class="string">&quot;properties&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;status&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;string&quot;</span>, <span class="string">&quot;enum&quot;</span>: [<span class="string">&quot;healthy&quot;</span>, <span class="string">&quot;degraded&quot;</span>, <span class="string">&quot;down&quot;</span>]&#125;,</span><br><span class="line">            <span class="string">&quot;component&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;string&quot;</span>&#125;,</span><br><span class="line">            <span class="string">&quot;errors&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;array&quot;</span>, <span class="string">&quot;items&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;string&quot;</span>&#125;&#125;,</span><br><span class="line">            <span class="string">&quot;warnings&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;array&quot;</span>, <span class="string">&quot;items&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;string&quot;</span>&#125;&#125;,</span><br><span class="line">            <span class="string">&quot;metrics&quot;</span>: &#123;</span><br><span class="line">                <span class="string">&quot;type&quot;</span>: <span class="string">&quot;object&quot;</span>,</span><br><span class="line">                <span class="string">&quot;properties&quot;</span>: &#123;</span><br><span class="line">                    <span class="string">&quot;cpu_usage&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;number&quot;</span>&#125;,</span><br><span class="line">                    <span class="string">&quot;memory_usage&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;number&quot;</span>&#125;,</span><br><span class="line">                    <span class="string">&quot;error_count&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;integer&quot;</span>&#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;required&quot;</span>: [<span class="string">&quot;status&quot;</span>, <span class="string">&quot;component&quot;</span>]</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 用自然语言描述触发 skill</span></span><br><span class="line">    skill_prompts = &#123;</span><br><span class="line">        <span class="string">&quot;diagnose&quot;</span>: <span class="string">f&quot;诊断组件 <span class="subst">&#123;args&#125;</span>，检查进程状态、错误日志、资源使用情况&quot;</span>,</span><br><span class="line">        <span class="string">&quot;health&quot;</span>: <span class="string">f&quot;检查组件 <span class="subst">&#123;args&#125;</span> 的健康状态&quot;</span>,</span><br><span class="line">        <span class="string">&quot;patrol&quot;</span>: <span class="string">f&quot;执行全服务器巡检 <span class="subst">&#123;args&#125;</span>&quot;</span>,</span><br><span class="line">        <span class="string">&quot;trace&quot;</span>: <span class="string">f&quot;追踪请求 <span class="subst">&#123;args&#125;</span>，返回完整调用链&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    prompt = skill_prompts.get(skill_name, <span class="string">f&quot;/<span class="subst">&#123;skill_name&#125;</span> <span class="subst">&#123;args&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 清除嵌套会话标记</span></span><br><span class="line">    env = <span class="built_in">dict</span>(os.environ)</span><br><span class="line">    env.pop(<span class="string">&#x27;CLAUDECODE&#x27;</span>, <span class="literal">None</span>)</span><br><span class="line"></span><br><span class="line">    result = subprocess.run(</span><br><span class="line">        [</span><br><span class="line">            <span class="string">&quot;claude&quot;</span>, <span class="string">&quot;-p&quot;</span>, prompt,</span><br><span class="line">            <span class="string">&quot;--output-format&quot;</span>, <span class="string">&quot;json&quot;</span>,</span><br><span class="line">            <span class="string">&quot;--json-schema&quot;</span>, json.dumps(schema)</span><br><span class="line">        ],</span><br><span class="line">        capture_output=<span class="literal">True</span>, text=<span class="literal">True</span>,</span><br><span class="line">        encoding=<span class="string">&#x27;utf-8&#x27;</span>, timeout=<span class="number">300</span>,</span><br><span class="line">        cwd=project_dir, env=env</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 解析返回的 JSON</span></span><br><span class="line">    data = json.loads(result.stdout)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 用 --json-schema 时，结果在 structured_output 字段</span></span><br><span class="line">    <span class="keyword">return</span> data.get(<span class="string">&#x27;structured_output&#x27;</span>, &#123;&#125;)</span><br></pre></td></tr></table></figure>
<p>关键改进：</p>
<p>用 —json-schema 强制 AI 按照指定格式输出，不需要正则提取</p>
<p>返回的数据结构完全可预测，可以直接用 data[‘status’] 访问</p>
<p>包含 Token 消耗和费用信息（在 data[‘usage’] 和 data[‘total_cost_usd’]）</p>
<p>第三步：加上定时调度</p>
<p>有了 CLI 包装器，定时巡检就是自然的下一步：</p>
<blockquote>
<p>通俗解释：下面几行命令就是”定时让 AI 帮你检查服务状态”——跟设闹钟一样，每隔一段时间自动执行一次。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 每 60 分钟检查一次 job-pdf</span></span><br><span class="line">python cli/diagnose_cli.py health <span class="string">&quot;job-pdf&quot;</span> --pretty</span><br><span class="line"></span><br><span class="line"><span class="comment"># 监控多个组件</span></span><br><span class="line">python cli/scheduler.py -s health -c job-pdf -c api-gateway -i 30</span><br><span class="line"></span><br><span class="line"><span class="comment"># 后台持续运行</span></span><br><span class="line"><span class="built_in">nohup</span> python cli/scheduler.py -s health -c job-pdf -i 60 &gt; scheduler.log 2&gt;&amp;1 &amp;</span><br></pre></td></tr></table></figure>
<p>这个案例让我意识到：<code>--print</code> 的价值不在于”我用了多少次”，而在于”它能做到交互模式做不到的事”——无人值守的自动化。</p>
<p>每次诊断都保存 JSON 日志，可以做趋势分析。监控系统可以直接调用 CLI，根据返回的 status 字段触发告警。</p>
<p>不过在深入更多玩法之前，先聊聊实现过程中踩过的坑——提前知道能省你几个小时。</p>
<h2 id="3-技术细节：你一定会踩的坑"><a href="#3-技术细节：你一定会踩的坑" class="headerlink" title="3. 技术细节：你一定会踩的坑"></a>3. 技术细节：你一定会踩的坑</h2><p>在实现过程中，我和社区里的其他开发者都踩过这些坑。</p>
<p>坑 0：<code>--continue</code> vs <code>--resume</code> 的区别</p>
<p>现象：你同时运行多个 <code>claude -p</code> 任务，用 <code>--continue</code> 想继续某个特定任务，结果发现它继续的是最近的那个任务，不是你想要的。</p>
<p>原因：<code>--continue</code> 和 <code>--resume</code> 是两个不同的参数：</p>
<blockquote>
<p>通俗解释：<code>--continue</code> 是”接着上一个任务继续”（不用指定哪个），<code>--resume</code> 是”接着某个特定任务继续”（需要指定 ID）。多个任务并行时必须用 <code>--resume</code>。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># --continue：继续最近的一次会话（不需要指定 session_id）</span></span><br><span class="line">claude -p <span class="string">&quot;分析性能问题&quot;</span></span><br><span class="line">claude -p <span class="string">&quot;继续刚才的分析，重点看数据库查询&quot;</span> --<span class="built_in">continue</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># --resume：继续指定的会话（需要 session_id）</span></span><br><span class="line">session_id=$(claude -p <span class="string">&quot;开始代码审查&quot;</span> --output-format json | jq -r <span class="string">&#x27;.session_id&#x27;</span>)</span><br><span class="line"><span class="comment"># ... 做其他事情 ...</span></span><br><span class="line">claude -p <span class="string">&quot;继续那个代码审查任务&quot;</span> --resume <span class="string">&quot;<span class="variable">$session_id</span>&quot;</span></span><br></pre></td></tr></table></figure>
<p>使用场景：</p>
<ul>
<li><p><strong><code>--continue</code></strong>：适合单线程顺序执行，每次都继续上一个任务</p>
</li>
<li><p><strong><code>--resume</code></strong>：适合多任务并行，需要明确指定继续哪个任务</p>
</li>
</ul>
<p>实际案例：在我的诊断系统中，如果一个诊断任务发现了问题，我会保存它的 <code>session_id</code>，然后在后续的深度分析中用 <code>--resume</code> 继续这个会话，这样 AI 能记住之前的上下文。</p>
<blockquote>
<p>通俗解释：先让 AI 做初步检查，保存这次对话的”身份证号”；如果发现问题，带着这个身份证号让 AI 继续深入分析——AI 会记得刚才查了什么。</p>
</blockquote>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 第一步：初步诊断</span></span><br><span class="line">result = subprocess.run(</span><br><span class="line">    [<span class="string">&quot;claude&quot;</span>, <span class="string">&quot;-p&quot;</span>, <span class="string">&quot;诊断 job-pdf 组件&quot;</span>, <span class="string">&quot;--output-format&quot;</span>, <span class="string">&quot;json&quot;</span>],</span><br><span class="line">    capture_output=<span class="literal">True</span>, text=<span class="literal">True</span></span><br><span class="line">)</span><br><span class="line">data = json.loads(result.stdout)</span><br><span class="line">session_id = data[<span class="string">&#x27;session_id&#x27;</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 如果发现问题，深度分析</span></span><br><span class="line"><span class="keyword">if</span> data[<span class="string">&#x27;result&#x27;</span>].find(<span class="string">&#x27;ERROR&#x27;</span>) != -<span class="number">1</span>:</span><br><span class="line">    subprocess.run([</span><br><span class="line">        <span class="string">&quot;claude&quot;</span>, <span class="string">&quot;-p&quot;</span>, <span class="string">&quot;深入分析刚才发现的错误，给出根因和解决方案&quot;</span>,</span><br><span class="line">        <span class="string">&quot;--resume&quot;</span>, session_id</span><br><span class="line">    ])</span><br></pre></td></tr></table></figure>
<p>坑 1：嵌套会话冲突</p>
<p>现象：在 Claude Code 会话内部调用 <code>claude -p</code>，报错：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Error: Cannot start nested Claude Code session</span><br></pre></td></tr></table></figure>
<p>原因：Claude Code 运行时设置了 <code>CLAUDECODE</code> 环境变量，防止嵌套调用。子进程继承了这个变量，被拒绝启动。</p>
<p>解决：调用前清除环境变量：</p>
<blockquote>
<p>通俗解释：在调用 <code>claude -p</code> 之前，先去掉一个”我已经在 Claude 里面了”的标记，这样新启动的 Claude 就不会以为自己被嵌套了。</p>
</blockquote>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">env = <span class="built_in">dict</span>(os.environ)</span><br><span class="line">env.pop(<span class="string">&#x27;CLAUDECODE&#x27;</span>, <span class="literal">None</span>)</span><br><span class="line"></span><br><span class="line">subprocess.run([<span class="string">&quot;claude&quot;</span>, <span class="string">&quot;-p&quot;</span>, prompt], env=env)</span><br></pre></td></tr></table></figure>
<p>注意：这个技巧是为了开发调试。生产环境应该在独立终端运行，不需要这个 workaround。</p>
<p>坑 2：如何获得可靠的结构化输出</p>
<p>问题背景：当你需要程序解析 <code>claude -p</code> 的输出时，如何确保输出格式可预测？</p>
<p>方案 1：在 prompt 里要求返回 JSON（不可靠）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">claude -p <span class="string">&quot;检查组件状态，返回 JSON 格式&quot;</span></span><br></pre></td></tr></table></figure>
<p>问题：AI 可能会加上解释文字：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">好的，我来检查组件状态。</span><br><span class="line"></span><br><span class="line">```json</span><br><span class="line">&#123;&quot;status&quot;: &quot;healthy&quot;, &quot;errors&quot;: 0&#125;</span><br></pre></td></tr></table></figure>
<p>检查完成，一切正常。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">你需要用正则表达式提取 JSON，容易出错。</span><br><span class="line"></span><br><span class="line">**方案 2：用 `--output-format json`**（推荐，适合文本结果）</span><br><span class="line"></span><br><span class="line">&gt; **通俗解释**：加上 `--output-format json` 参数，Claude 会把回答包装在一个标准格式里返回，里面还附带了这次对话的 ID、消耗了多少 Token、花了多少钱等信息。</span><br><span class="line"></span><br><span class="line">```bash</span><br><span class="line">claude -p &quot;检查组件状态&quot; --output-format json | jq &#x27;.result&#x27;</span><br></pre></td></tr></table></figure>
<p>返回的 JSON 结构：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;result&quot;</span><span class="punctuation">:</span> <span class="string">&quot;AI 的回答文本&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;session_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;abc123...&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;usage&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;input_tokens&quot;</span><span class="punctuation">:</span> <span class="number">1234</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;output_tokens&quot;</span><span class="punctuation">:</span> <span class="number">567</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;total_tokens&quot;</span><span class="punctuation">:</span> <span class="number">1801</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;total_cost_usd&quot;</span><span class="punctuation">:</span> <span class="number">0.0234</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p>优点：</p>
<p>输出是标准 JSON，可以直接解析</p>
<p>包含 session_id、usage、cost 等元数据</p>
<p>result 字段包含 AI 的完整回答</p>
<p>注意：<code>result</code> 字段里的内容仍然是 AI 生成的自由文本。如果你在 prompt 里要求返回 JSON，AI 可能会在 <code>result</code> 里返回 JSON 字符串，你需要再解析一次。</p>
<p><strong>方案 3：用 <code>--json-schema</code> 强制结构化输出</strong>（最可靠，适合结构化数据）</p>
<blockquote>
<p>通俗解释：你提前定义好”我要的结果长什么样”（比如必须有 status 字段，值只能是 healthy/degraded/down），Claude 会严格按你的模板返回数据，不会多说一个字。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">claude -p <span class="string">&quot;检查组件状态&quot;</span> \</span><br><span class="line">  --output-format json \</span><br><span class="line">  --json-schema <span class="string">&#x27;&#123;</span></span><br><span class="line"><span class="string">    &quot;type&quot;: &quot;object&quot;,</span></span><br><span class="line"><span class="string">    &quot;properties&quot;: &#123;</span></span><br><span class="line"><span class="string">      &quot;status&quot;: &#123;&quot;type&quot;: &quot;string&quot;, &quot;enum&quot;: [&quot;healthy&quot;, &quot;degraded&quot;, &quot;down&quot;]&#125;,</span></span><br><span class="line"><span class="string">      &quot;errors&quot;: &#123;&quot;type&quot;: &quot;integer&quot;&#125;,</span></span><br><span class="line"><span class="string">      &quot;warnings&quot;: &#123;&quot;type&quot;: &quot;array&quot;, &quot;items&quot;: &#123;&quot;type&quot;: &quot;string&quot;&#125;&#125;</span></span><br><span class="line"><span class="string">    &#125;,</span></span><br><span class="line"><span class="string">    &quot;required&quot;: [&quot;status&quot;, &quot;errors&quot;]</span></span><br><span class="line"><span class="string">  &#125;&#x27;</span></span><br></pre></td></tr></table></figure>
<p>返回的 JSON 结构：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;structured_output&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;status&quot;</span><span class="punctuation">:</span> <span class="string">&quot;healthy&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;errors&quot;</span><span class="punctuation">:</span> <span class="number">0</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;warnings&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;session_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;abc123...&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;usage&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span>...<span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;total_cost_usd&quot;</span><span class="punctuation">:</span> <span class="number">0.0234</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
<p>优点：</p>
<ul>
<li>AI 的输出严格符合 JSON Schema</li>
</ul>
<p>结果在 structured_output 字段，不是文本</p>
<p>不需要任何正则提取或二次解析</p>
<p>类型安全，可以直接用于程序逻辑</p>
<p>关键区别：</p>
<p>不用 —json-schema：结果在 .result 字段，是 AI 生成的文本（可能包含 JSON 字符串）</p>
<p>用 —json-schema：结果在 .structured_output 字段，严格符合 Schema 的结构化数据</p>
<p>我的实践：在诊断系统中，我用的是方案 3，这样可以确保返回的数据结构完全可预测，可以直接用 <code>data[&#39;structured_output&#39;][&#39;status&#39;]</code> 访问字段，不需要任何字符串解析。</p>
<p>坑 3：Windows 编码问题</p>
<p>现象：中文输出乱码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">组件状态：\xe6\xad\xa3\xe5\xb8\xb8</span><br></pre></td></tr></table></figure>
<p>原因：Windows 默认编码是 GBK，Claude Code 输出是 UTF-8。</p>
<p>解决：显式指定编码：</p>
<blockquote>
<p>通俗解释：告诉 Python “请用 UTF-8 编码来读取 Claude 的输出”，这样中文就不会变成乱码了。</p>
</blockquote>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">result = subprocess.run(</span><br><span class="line">    [<span class="string">&quot;claude&quot;</span>, <span class="string">&quot;-p&quot;</span>, prompt],</span><br><span class="line">    capture_output=<span class="literal">True</span>,</span><br><span class="line">    text=<span class="literal">True</span>,</span><br><span class="line">    encoding=<span class="string">&#x27;utf-8&#x27;</span>,      <span class="comment"># 强制 UTF-8</span></span><br><span class="line">    errors=<span class="string">&#x27;replace&#x27;</span>        <span class="comment"># 遇到无法解码的字符时替换而非报错</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<p>macOS 和 Linux 通常不会遇到这个问题，但如果你的团队有 Windows 用户，建议统一加上。</p>
<p>坑 4：Skills 触发机制不同</p>
<p>现象：直接传 <code>/diagnose job-pdf</code> 不会触发 Skill，被当作普通文本。</p>
<p>原因：<code>--print</code> 模式下，Skill 触发需要更明确的上下文。<code>/skill-name</code> 语法依赖交互式环境的解析机制。</p>
<p>解决：用自然语言描述代替 Skill 命令：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ❌ 不可靠</span></span><br><span class="line">prompt = <span class="string">&quot;/diagnose job-pdf&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># ✅ 可靠</span></span><br><span class="line">prompt = <span class="string">&quot;诊断组件 job-pdf，检查进程状态和错误日志，返回JSON格式的结果&quot;</span></span><br></pre></td></tr></table></figure>
<p>关键认知：<code>--print</code> 模式更适合”任务描述”而不是”命令执行”。把它当作”给 AI 的工作说明书”，而不是”给 CLI 的命令”。</p>
<p>坑 5：生产环境的额外考量</p>
<p>前面 4 个坑都是”怎么跑起来”的问题。但如果你打算把 claude -p 用在定时任务、CI/CD 或团队共享的自动化流程中，还有几件事需要提前想清楚。</p>
<ol>
<li>给外部调用加上防护</li>
</ol>
<p>claude -p 本质上是一个外部 API 调用——它可能超时、返回异常、甚至服务不可用。所有外部调用的基本纪律都适用：</p>
<blockquote>
<p>通俗解释：就像你调用任何第三方接口一样，要考虑”它挂了怎么办”。</p>
</blockquote>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> json</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">safe_call</span>(<span class="params">prompt: <span class="built_in">str</span>, timeout: <span class="built_in">int</span> = <span class="number">300</span></span>) -&gt; <span class="built_in">dict</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;带基本防护的 claude -p 调用&quot;&quot;&quot;</span></span><br><span class="line">    env = <span class="built_in">dict</span>(os.environ)</span><br><span class="line">    env.pop(<span class="string">&#x27;CLAUDECODE&#x27;</span>, <span class="literal">None</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        result = subprocess.run(</span><br><span class="line">            [<span class="string">&quot;claude&quot;</span>, <span class="string">&quot;-p&quot;</span>, prompt, <span class="string">&quot;--output-format&quot;</span>, <span class="string">&quot;json&quot;</span>],</span><br><span class="line">            capture_output=<span class="literal">True</span>, text=<span class="literal">True</span>,</span><br><span class="line">            encoding=<span class="string">&#x27;utf-8&#x27;</span>, timeout=timeout, env=env</span><br><span class="line">        )</span><br><span class="line">        <span class="keyword">if</span> result.returncode != <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">return</span> &#123;<span class="string">&quot;status&quot;</span>: <span class="string">&quot;error&quot;</span>, <span class="string">&quot;message&quot;</span>: <span class="string">f&quot;Exit code: <span class="subst">&#123;result.returncode&#125;</span>&quot;</span>&#125;</span><br><span class="line">        <span class="keyword">return</span> json.loads(result.stdout)</span><br><span class="line">    <span class="keyword">except</span> subprocess.TimeoutExpired:</span><br><span class="line">        <span class="keyword">return</span> &#123;<span class="string">&quot;status&quot;</span>: <span class="string">&quot;error&quot;</span>, <span class="string">&quot;message&quot;</span>: <span class="string">&quot;Timeout&quot;</span>&#125;</span><br><span class="line">    <span class="keyword">except</span> json.JSONDecodeError:</span><br><span class="line">        <span class="keyword">return</span> &#123;<span class="string">&quot;status&quot;</span>: <span class="string">&quot;error&quot;</span>, <span class="string">&quot;message&quot;</span>: <span class="string">&quot;Invalid JSON response&quot;</span>&#125;</span><br></pre></td></tr></table></figure>
<p>核心原则：检查退出码、捕获超时、防御畸形输出。这三行 except 能避免 90% 的生产事故。</p>
<ol>
<li>不要用 nohup 当部署方案</li>
</ol>
<p>前面的 nohup … &amp; 只适合本地试验。生产环境建议用 systemd 或 supervisor 管理进程，确保崩溃自动重启和日志归档。</p>
<ol>
<li>安全意识清单</li>
</ol>
<p>把 AI 接入自动化流程时，多问自己三个问题：</p>
<ul>
<li><p>凭证：SSH 密钥、API Token 是否通过环境变量或密钥管理服务注入，而不是硬编码？</p>
</li>
<li><p>数据边界：传给 <code>claude -p</code> 的日志或代码中，是否包含用户隐私数据或生产密钥？</p>
</li>
<li><p>权限最小化：<code>--allowedTools</code> 是否限制到了最小必要范围？CI/CD 中的 <code>--dangerously-skip-permissions</code> 是否配合了沙箱环境？（常见场景的权限配置参考见附录）</p>
</li>
</ul>
<p>原则：先从最小权限开始，遇到 AI 报告权限不足时再逐个放开。宁可多跑一次，不要一上来就 <code>--dangerously-skip-permissions</code>。</p>
<ol>
<li>留个成本观测口</li>
</ol>
<p>每次调用的费用藏在 —output-format json 返回的 total_cost_usd 字段里。建议把它记到日志中，定期汇总：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 单次调用后记录成本</span></span><br><span class="line">cost=$(<span class="built_in">echo</span> <span class="string">&quot;<span class="variable">$RESULT</span>&quot;</span> | jq <span class="string">&#x27;.total_cost_usd&#x27;</span>)</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;<span class="subst">$(date)</span> | <span class="variable">$component</span> | <span class="variable">$cost</span>&quot;</span> &gt;&gt; ai-cost.log</span><br></pre></td></tr></table></figure>
<p>跑了一周再回头看，你就知道哪些任务值得用 AI，哪些该回归传统脚本。</p>
<h2 id="4-社区实战：其他人在用-—print-做什么"><a href="#4-社区实战：其他人在用-—print-做什么" class="headerlink" title="4. 社区实战：其他人在用 —print 做什么"></a>4. 社区实战：其他人在用 —print 做什么</h2><p>我以为自己的用法已经够深了，直到搜了一圈社区——发现 claude -p 的玩法远比我想象的丰富。以下是我搜集到的真实案例，每个都有出处。</p>
<p>说明：这些案例不是我自己的实践，而是我从 GitHub Issue、开源项目、社区讨论中搜集的。我会标注每个案例的来源，方便你追溯原始讨论。</p>
<p>案例 1：Issue 自动变 PR — Ceedar.ai 的”AI 程序员”</p>
<p>来源：GitHub Issue #762 + <a href="https://github.com/ceedario/secret-agents">ceedario/secret-agents</a> 仓库</p>
<p>Connoropolous 在 Ceedar.ai（现更名 Cyrus）构建了一个类似 Devin 的系统：监控 Linear 和 GitHub 的 Issue，自动为每个 Issue 创建 Git Worktree，然后用 claude -p 执行编码任务，最终提交 PR。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">工作流：</span><br><span class="line">  Linear Issue 创建</span><br><span class="line">    → 系统自动分配给 AI Bot</span><br><span class="line">    → 创建独立 Git Worktree</span><br><span class="line">    → claude -p --output-format stream-json --verbose --include-partial-messages 执行编码</span><br><span class="line">    → 实时把进度流式回写到 Issue 评论</span><br><span class="line">    → 完成后自动创建 PR</span><br><span class="line">    → 跟踪每个 Issue 的 Token 成本</span><br></pre></td></tr></table></figure>
<p>关键技术点：</p>
<p>用 —output-format stream-json —verbose —include-partial-messages 实现实时进度反馈（每个 token 都能立即显示）</p>
<p>用 —continue 实现多轮会话（复杂任务需要多次交互）</p>
<p>每个 Issue 独立 Worktree，多任务并行互不干扰</p>
<p>支持 Claude Code、Codex、Cursor、Gemini 多种后端</p>
<p>他的原话：”print mode is AMAZING”。</p>
<p>这个仓库是 Apache 2.0 开源的，TypeScript monorepo，值得研究。</p>
<p>案例 2：CI 里的智能代码审查 — Git Diff 管道</p>
<p>来源：社区常见实践（在多个 GitHub 讨论和博客中被提及，但无单一明确出处）</p>
<p>这是社区里最常见的用法之一：在 CI/CD 流水线中，把 git diff 的输出管道传给 claude -p，让 AI 做代码审查。</p>
<blockquote>
<p>通俗解释：下面的脚本做的事情是——把代码变更内容”喂”给 AI，让它像一个审查专家一样检查有没有安全漏洞、性能问题，然后把审查意见自动贴到 PR 评论里。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># GitHub Actions 中的 AI 代码审查</span></span><br><span class="line">git diff origin/main...HEAD | claude -p \</span><br><span class="line">  --append-system-prompt <span class="string">&quot;你是代码审查专家，重点检查：</span></span><br><span class="line"><span class="string">   1. 安全漏洞（SQL注入、XSS）</span></span><br><span class="line"><span class="string">   2. 性能问题（N+1 查询、内存泄漏）</span></span><br><span class="line"><span class="string">   3. 逻辑错误</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">   按严重程度分级输出，格式：</span></span><br><span class="line"><span class="string">   🔴 Critical: ...</span></span><br><span class="line"><span class="string">   🟡 Warning: ...</span></span><br><span class="line"><span class="string">   🟢 Info: ...&quot;</span> \</span><br><span class="line">  --output-format json &gt; review.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># 把审查结果贴到 PR 评论</span></span><br><span class="line">gh <span class="built_in">pr</span> comment <span class="variable">$PR_NUMBER</span> --body <span class="string">&quot;<span class="subst">$(jq -r &#x27;.result&#x27; review.json)</span>&quot;</span></span><br></pre></td></tr></table></figure>
<p>为什么比传统 lint 工具好？ 传统工具只能检查语法和已知模式，AI 能理解业务逻辑。比如它能发现”这个接口没有做权限校验”——这是 ESLint 永远发现不了的。</p>
<p>进阶技巧：如果需要程序自动判断审查结果（比如决定是否阻断 PR 合并），可以用 <code>--json-schema</code> 强制返回结构化数据，确保输出格式完全可预测，不需要正则提取。</p>
<p>案例 3：批量代码迁移 — TypeScript 转换、Lodash 移除</p>
<p>来源：社区常见实践（在 GitHub 讨论中被多次提及，但无单一明确出处）</p>
<p>用 claude -p 做批量代码迁移，配合 Shell 循环逐文件处理：</p>
<blockquote>
<p>通俗解释：下面的脚本是”循环处理”——对文件夹里的每个文件，逐个让 AI 帮你转换格式或替换写法。就像流水线上一个一个加工零件。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 批量把 JS 文件转换为 TypeScript（含验证步骤）</span></span><br><span class="line"><span class="keyword">for</span> file <span class="keyword">in</span> src/**/*.js; <span class="keyword">do</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">&quot;Converting <span class="variable">$file</span>...&quot;</span></span><br><span class="line">  claude -p <span class="string">&quot;将这个 JavaScript 文件转换为 TypeScript。</span></span><br><span class="line"><span class="string">添加适当的类型注解，保持逻辑不变。</span></span><br><span class="line"><span class="string">直接输出转换后的完整代码，不要解释。&quot;</span> &lt; <span class="string">&quot;<span class="variable">$file</span>&quot;</span> &gt; <span class="string">&quot;<span class="variable">$&#123;file%.js&#125;</span>.ts&quot;</span></span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 验证转换结果：检查类型错误</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;Verifying TypeScript conversion...&quot;</span></span><br><span class="line">npx tsc --noEmit 2&gt;&amp;1 | <span class="built_in">tee</span> ts-errors.log</span><br><span class="line"><span class="keyword">if</span> [ $? -ne 0 ]; <span class="keyword">then</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">&quot;⚠️  发现类型错误，请检查 ts-errors.log&quot;</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 批量移除 lodash 依赖，替换为原生实现</span></span><br><span class="line"><span class="keyword">for</span> file <span class="keyword">in</span> $(grep -rl <span class="string">&quot;import.*lodash&quot;</span> src/); <span class="keyword">do</span></span><br><span class="line">  claude -p <span class="string">&quot;将这个文件中的 lodash 调用替换为原生 JavaScript 实现。</span></span><br><span class="line"><span class="string">保持功能完全一致。直接修改文件。&quot;</span> &lt; <span class="string">&quot;<span class="variable">$file</span>&quot;</span></span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<p>关键技巧：用 <code>&lt; &quot;$file&quot;</code> 把文件内容通过标准输入传给 <code>claude -p</code>，比在 prompt 里拼接文件路径更可靠。</p>
<p>注意：批量迁移涉及跨文件的类型依赖和模块关系，AI 逐文件处理时可能遗漏上下文。建议转换后务必用 <code>tsc --noEmit</code> 做全量类型检查，对报错文件再用案例 4 的闭环模式逐个修复。</p>
<p>注：社区中有开发者分享用这种方式一天内完成了大量文件的 TypeScript 迁移，但具体数字和出处未经验证。</p>
<p>案例 4：自动化测试生成与修复</p>
<p>来源：GitHub Issue #762，用户 danny-hunt</p>
<p>用 claude -p 自动生成单元测试，然后用测试运行器验证，失败了再让 AI 修复——形成闭环：</p>
<blockquote>
<p>通俗解释：下面的脚本是一个”自我修复循环”——让 AI 写测试，跑一下看过不过，没过就把错误信息再给 AI 让它修，最多重试 3 次。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 生成测试 → 运行 → 失败则修复 → 循环</span></span><br><span class="line">MAX_RETRIES=3</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> $(<span class="built_in">seq</span> 1 <span class="variable">$MAX_RETRIES</span>); <span class="keyword">do</span></span><br><span class="line">  claude -p <span class="string">&quot;为 src/services/UserService.ts 生成单元测试。</span></span><br><span class="line"><span class="string">使用 Jest + ts-jest。覆盖所有公共方法，包括边界情况。</span></span><br><span class="line"><span class="string">直接写入 src/services/__tests__/UserService.test.ts&quot;</span> \</span><br><span class="line">    --allowedTools <span class="string">&quot;Read,Write,Edit&quot;</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># 运行测试</span></span><br><span class="line">  <span class="keyword">if</span> npx jest src/services/__tests__/UserService.test.ts 2&gt;&amp;1; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">&quot;✅ Tests passed on attempt <span class="variable">$i</span>&quot;</span></span><br><span class="line">    <span class="built_in">break</span></span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    TEST_OUTPUT=$(npx jest src/services/__tests__/UserService.test.ts 2&gt;&amp;1)</span><br><span class="line">    claude -p <span class="string">&quot;测试运行失败，错误信息：</span></span><br><span class="line"><span class="string"><span class="variable">$TEST_OUTPUT</span></span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">请修复测试文件 src/services/__tests__/UserService.test.ts 中的问题。&quot;</span> \</span><br><span class="line">      --allowedTools <span class="string">&quot;Read,Edit&quot;</span>  <span class="comment"># 修复时不允许 Write，防止覆盖原文件</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<p>他的总结：”Basically specific tasks where there is a bash command you can run that tells you whether the changes are working or not”——<strong>凡是有明确验证手段的任务，都适合用 <code>claude -p</code> 自动化</strong>。</p>
<p><strong><code>--allowedTools</code> 的细节</strong>：</p>
<p>生成阶段允许 Read,Write,Edit（需要创建新文件）</p>
<p>修复阶段只允许 Read,Edit（防止意外覆盖）</p>
<p>如果需要限制 Bash 命令，可以用 Bash(npm test <em>) 这样的语法（注意 </em> 前面的空格）</p>
<p>案例 5：部署后冒烟检查 — 测试视角的自动化</p>
<p>来源：基于诊断系统的自然延伸</p>
<p>健康检查只看”进程活没活”，但测试同学更关心的是”功能正不正常”。部署后用 claude -p 自动调用关键接口，验证核心业务流程是否正常：</p>
<blockquote>
<p>通俗解释：每次部署完，自动跑一遍关键接口，看返回是否正常。相当于让 AI 帮你做一轮最基本的冒烟测试。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 部署后自动验证关键接口</span></span><br><span class="line">claude -p <span class="string">&quot;对以下接口执行冒烟检查，用 curl 调用并验证返回状态码和关键字段：</span></span><br><span class="line"><span class="string">  1. POST http://test-server:8080/api/login  body: &#123;\&quot;username\&quot;:\&quot;test\&quot;,\&quot;password\&quot;:\&quot;test123\&quot;&#125;  期望: 200 + 返回含 token</span></span><br><span class="line"><span class="string">  2. GET http://test-server:8080/api/user/profile  header: Authorization: Bearer &lt;上一步的token&gt;  期望: 200 + 返回含 username</span></span><br><span class="line"><span class="string">  3. POST http://test-server:8080/api/pdf/generate  body: &#123;\&quot;template\&quot;:\&quot;default\&quot;,\&quot;data\&quot;:&#123;&#125;&#125;  期望: 200 或 202</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">对每个接口报告：实际状态码、是否符合预期、异常信息（如有）&quot;</span> \</span><br><span class="line">  --output-format json \</span><br><span class="line">  --json-schema <span class="string">&#x27;&#123;</span></span><br><span class="line"><span class="string">    &quot;type&quot;: &quot;object&quot;,</span></span><br><span class="line"><span class="string">    &quot;properties&quot;: &#123;</span></span><br><span class="line"><span class="string">      &quot;all_passed&quot;: &#123;&quot;type&quot;: &quot;boolean&quot;&#125;,</span></span><br><span class="line"><span class="string">      &quot;results&quot;: &#123;</span></span><br><span class="line"><span class="string">        &quot;type&quot;: &quot;array&quot;,</span></span><br><span class="line"><span class="string">        &quot;items&quot;: &#123;</span></span><br><span class="line"><span class="string">          &quot;type&quot;: &quot;object&quot;,</span></span><br><span class="line"><span class="string">          &quot;properties&quot;: &#123;</span></span><br><span class="line"><span class="string">            &quot;endpoint&quot;: &#123;&quot;type&quot;: &quot;string&quot;&#125;,</span></span><br><span class="line"><span class="string">            &quot;status_code&quot;: &#123;&quot;type&quot;: &quot;integer&quot;&#125;,</span></span><br><span class="line"><span class="string">            &quot;passed&quot;: &#123;&quot;type&quot;: &quot;boolean&quot;&#125;,</span></span><br><span class="line"><span class="string">            &quot;error&quot;: &#123;&quot;type&quot;: &quot;string&quot;&#125;</span></span><br><span class="line"><span class="string">          &#125;,</span></span><br><span class="line"><span class="string">          &quot;required&quot;: [&quot;endpoint&quot;, &quot;passed&quot;]</span></span><br><span class="line"><span class="string">        &#125;</span></span><br><span class="line"><span class="string">      &#125;</span></span><br><span class="line"><span class="string">    &#125;,</span></span><br><span class="line"><span class="string">    &quot;required&quot;: [&quot;all_passed&quot;, &quot;results&quot;]</span></span><br><span class="line"><span class="string">  &#125;&#x27;</span> \</span><br><span class="line">  --allowedTools <span class="string">&quot;Bash(curl *)&quot;</span> \</span><br><span class="line">  --max-budget-usd 0.1</span><br></pre></td></tr></table></figure>
<p>和传统冒烟脚本的区别：传统脚本只能检查状态码，AI 能理解返回内容——比如接口返回 200 但 body 里是 <code>&#123;&quot;error&quot;: &quot;token expired&quot;&#125;</code>，传统脚本会判断为通过，AI 会判断为失败。</p>
<p><strong><code>--allowedTools &quot;Bash(curl *)&quot;</code> 的作用</strong>：只允许 AI 执行 curl 命令，防止它在测试环境做其他操作。<code>--max-budget-usd 0.1</code> 限制单次调用成本不超过 $0.1，避免成本失控。</p>
<p>社区用法总结</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">用法类型              典型场景                          核心价值</span><br><span class="line">──────────────        ──────────────────────────        ──────────────────────────</span><br><span class="line">CI/CD 集成            PR 代码审查、安全扫描              自动化质量门禁</span><br><span class="line">批量迁移              TS 转换、依赖替换、API 升级        一天完成一周的活</span><br><span class="line">测试自动化            生成测试、修复失败、提高覆盖率      闭环验证，不靠人</span><br><span class="line">Issue → PR            监控 Issue、自动编码、提交 PR      类 Devin 的自动化开发</span><br><span class="line">冒烟检查              部署后接口验证、业务流程校验        比状态码检查更智能</span><br></pre></td></tr></table></figure>
<p>共同特点：都是把 <code>claude -p</code> 当作 Unix 管道中的一个”智能节点”——接收输入或参数，输出结构化结果（用 <code>--output-format json</code> 或 <code>--json-schema</code>），可以和其他工具组合。</p>
<h2 id="5-从工具到方法论的思考"><a href="#5-从工具到方法论的思考" class="headerlink" title="5. 从工具到方法论的思考"></a>5. 从工具到方法论的思考</h2><p>我的反思：为什么用得不多，却觉得很重要？</p>
<p>坦白说，我自己用 —print 的频率不高——两周内只有 12 次手动触发，其余都是定时任务在跑。但我认为它很重要，原因有三：</p>
<ol>
<li>它解决的是”0 到 1”的问题</li>
</ol>
<p>交互模式再强，也做不到”无人值守”。—print 让 AI 从”助手”变成”自动化节点”——这是质变，不是量变。</p>
<ol>
<li>它的价值不在使用频率，而在覆盖场景</li>
</ol>
<p>我每天用交互模式 10 次，但都是”我在场”的场景。定时巡检、CI/CD、Pre-commit Hook——这些场景我不可能一直盯着。—print 覆盖的是”我不在场”的 80% 时间。</p>
<ol>
<li>社区的用法证明了它的潜力</li>
</ol>
<p>Issue → PR 自动化（Ceedar.ai）、批量代码迁移（一天完成一周的活）、自动化测试生成与修复闭环——这些都是我没想到的用法。</p>
<p>结论：一个工具的价值，不只看”我用了多少次”，还要看”它能做到什么别的工具做不到的事”。和传统脚本的核心区别在于：传统脚本只能处理你预定义的模式，<code>claude -p</code> 能自适应处理未知场景——但代价是每次调用都有 Token 成本和秒级延迟。</p>
<p>什么时候该用，什么时候不该用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">适合 --print 的场景：</span><br><span class="line">  ✅ 需要理解上下文的任务（代码审查、日志分析、根因诊断）</span><br><span class="line">  ✅ 需要生成结构化输出的任务（报告、Changelog、文档）</span><br><span class="line">  ✅ 需要调用多个工具的复杂任务（诊断流程、迁移流程）</span><br><span class="line">  ✅ 低频高价值的任务（每日报告、发版检查、故障诊断）</span><br><span class="line"></span><br><span class="line">不适合 --print 的场景：</span><br><span class="line">  ❌ 毫秒级响应要求（实时 API、用户请求链路）</span><br><span class="line">  ❌ 完全确定性的任务（简单文本替换、格式转换）</span><br><span class="line">  ❌ 高频低价值的任务（每秒执行一次的健康检查）</span><br><span class="line">  ❌ 需要人工判断的关键决策（生产环境变更）</span><br></pre></td></tr></table></figure>
<p>成本意识</p>
<p>每次 claude -p 调用都消耗 Token：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">任务复杂度          大约 Token 消耗          大约耗时</span><br><span class="line">──────────          ──────────────          ──────────</span><br><span class="line">简单问答            ~1K tokens              2-5 秒</span><br><span class="line">中等任务            ~10K tokens             10-30 秒</span><br><span class="line">复杂诊断            ~100K tokens            1-3 分钟</span><br></pre></td></tr></table></figure>
<p>成本控制建议：</p>
<p>低频高价值的任务用 AI 驱动（每日报告、故障诊断）</p>
<p>高频低价值的任务用传统脚本（简单状态检查）</p>
<p>批量处理时用轻量模型（Haiku）降低成本</p>
<p>结合传统监控，只在异常时触发 AI 诊断</p>
<p>用 —output-format json 获取 total_cost_usd 字段，监控每次调用的费用</p>
<p>渐进式增强路径</p>
<p>不要一上来就想构建复杂的自动化系统。从小场景开始：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">第一步：手动验证可行性</span><br><span class="line">  claude -p &quot;检查 job-pdf 组件的健康状态&quot;</span><br><span class="line">  → 确认 AI 能正确执行，输出符合预期</span><br><span class="line"></span><br><span class="line">第二步：封装成脚本</span><br><span class="line">  python diagnose_cli.py health job-pdf</span><br><span class="line">  → 加上 JSON 解析、错误处理、日志记录</span><br><span class="line"></span><br><span class="line">第三步：加入定时调度</span><br><span class="line">  python scheduler.py -s health -c job-pdf -i 60</span><br><span class="line">  → 定时执行，结果归档</span><br><span class="line"></span><br><span class="line">第四步：集成到现有系统</span><br><span class="line">  监控系统调用 CLI → 根据返回结果触发告警</span><br><span class="line">  → 和讯飞打通</span><br></pre></td></tr></table></figure>
<p>每个阶段都是在前一阶段基础上的薄包装，没有重写核心逻辑。</p>
<h2 id="6-我的愿景：用-—print-构建缺陷修复闭环"><a href="#6-我的愿景：用-—print-构建缺陷修复闭环" class="headerlink" title="6. 我的愿景：用 —print 构建缺陷修复闭环"></a>6. 我的愿景：用 —print 构建缺陷修复闭环</h2><p>前面讲的都是单点场景——诊断、审查、迁移、测试。但如果把这些点串起来，<code>claude -p</code> 可以成为一条完整的缺陷发现 → 分析 → 修复 → 验证自动化流水线的核心引擎。</p>
<p>这不是空想——我已经在远程日志诊断系统中实现了前半段（发现 → 分析 → 定位）。接下来的目标是把后半段也串起来。</p>
<p>完整场景：从测试发现 Bug 到修复上线</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">测试发现缺陷</span><br><span class="line">  ↓</span><br><span class="line">[自动] 录制操作 + 网络请求 + 截图 → claude -p 生成结构化缺陷报告           ← 计划中</span><br><span class="line">  ↓</span><br><span class="line">[自动] 提交研测系统 + 通知研发                                             ← 计划中</span><br><span class="line">  ↓</span><br><span class="line">[人工] 研发确认缺陷 + 补充项目信息（代码仓库地址、分支、相关模块）</span><br><span class="line">  ↓</span><br><span class="line">[自动] claude -p 连接测试服务器，按 taskId/时间搜索完整日志上下文           ← ✅ 已实现</span><br><span class="line">  ↓</span><br><span class="line">[自动] claude -p 结合远程日志 + 本地源码分析缺陷复杂度 → 简单/复杂分流     ← ✅ 已实现</span><br><span class="line">  ↓</span><br><span class="line">[分支] 简单缺陷 → claude -p 自动生成修复 + 创建 PR（标记 AI-Generated）    ← ✅ 部分实现</span><br><span class="line">       复杂缺陷 → claude -p 深度分析（含依赖链路追踪）+ 给出解决方案       ← ✅ 已实现</span><br><span class="line">  ↓</span><br><span class="line">[自动] claude -p 代码审查 + 影响范围分析                                   ← 计划中</span><br><span class="line">  ↓</span><br><span class="line">[人工] 研发审核 PR → 批准合并</span><br><span class="line">  ↓</span><br><span class="line">[自动] 部署测试环境 → 通知测试验证                                         ← 计划中</span><br><span class="line">  ↓</span><br><span class="line">[人工] 测试验证通过 → 批准上线 → 闭环</span><br></pre></td></tr></table></figure>
<p>当前进度：核心的日志诊断和根因分析环节已经实现（详见第三篇分享《一句话代替 30 分钟排查》），缺陷采集和 CI 集成环节在规划中。</p>
<p>为什么分享一个”未完成”的愿景？ 因为我想让你看到 <code>--print</code> 的潜力——即使我自己只实现了一部分，它已经解决了真实痛点。如果你的场景和我不同，你可能会发现更多玩法。</p>
<p>从个人实践来看，低复杂度的重复性环节（缺陷登记、日志取证、简单修复）提升最明显，耗时通常能缩短一半以上。高复杂度环节（分布式系统根因分析、涉及业务逻辑的修复）AI 更多是加速而非替代。不同团队的实际体验可能差异很大，建议从一个小场景开始试点，用自己的数据说话。</p>
<p>关键原则：这条流水线中，PR 合并前和生产部署前必须保留人工审核。<code>claude -p</code> 的角色是加速每个环节，而不是替代人的判断。</p>
<p>总结</p>
<p>claude -p 看起来只是一个简单的参数，但它代表了一个重要的转变：</p>
<p>从”交互式 AI 助手”到”可编程的自动化代理”。</p>
<p>我自己的使用不多，但正是这个”不多”的实践，让我看到了一个事实：<strong>交互模式再强大，也只能覆盖你在场的时间。<code>--print</code> 覆盖的是你不在场的 80%。</strong></p>
<p>社区的案例更让我确信——从 CI/CD 代码审查到 Issue 自动变 PR，从批量代码迁移到 Pre-commit 安全扫描，这些都是交互模式做不到的事。</p>
<p>核心价值可以用一句话概括：</p>
<p><strong><code>claude -p</code> 让你把 AI 的全部能力（理解代码、调用工具、分析问题）嵌入到任何自动化流程中。</strong></p>
<p>它不是替代交互式使用，而是补充。交互模式适合探索性工作（设计、调试、brainstorm），—print 模式适合确定性工作（巡检、审查、迁移、生成）。两者配合，覆盖从”想法”到”自动化运维”的完整链路。</p>
<p>如果你已经在用 Claude Code 做开发，花 10 分钟试试 claude -p——从一个简单的 git diff | claude -p “审查这段代码” 开始。你会发现，AI 的价值不止于对话框里。</p>
<p>参考链接：</p>
<p>ceedario/secret-agents — Issue → PR 自动化参考实现（案例 1）</p>
<p>Claude Code GitHub Issue #762 — 社区 —print 模式讨论（案例 1、4）</p>
<p>Run Claude Code programmatically - Claude Code Docs — 官方文档</p>
<p>出处说明：</p>
<p>本文中的社区案例来源如下：</p>
<ul>
<li><p>案例 1：来自 GitHub Issue #762 和 ceedario/secret-agents 开源仓库，有明确的代码实现和讨论记录</p>
</li>
<li><p>案例 2、3：基于社区常见实践模式，在多个 GitHub 讨论和博客中被提及，但无单一明确的原始出处</p>
</li>
<li><p>案例 4：来自 GitHub Issue #762 中用户 danny-hunt 的分享，有明确的讨论上下文</p>
</li>
<li><p>案例 5：基于作者诊断系统的自然延伸</p>
</li>
</ul>
<p>对于没有明确出处的实践模式，本文已在”来源”部分标注为”社区常见实践”。</p>
<ul>
<li>附录：常用 Flag 组合速查</li>
</ul>
<p>claude -p 单独用只是基础，配合其他 Flag 才能发挥全部威力。以下是社区里最常用的组合：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Flag                              作用                              典型场景</span><br><span class="line">──────────────────────────        ──────────────────────────        ──────────────────────────</span><br><span class="line">-p &quot;prompt&quot;                       基础非交互模式                    所有场景的起点</span><br><span class="line">--output-format json              返回结构化 JSON                   需要程序解析结果时</span><br><span class="line">--output-format stream-json       流式 JSON 输出                   需要实时进度反馈时</span><br><span class="line">--json-schema &#x27;&#123;...&#125;&#x27;             强制按 Schema 输出                需要严格结构化数据时</span><br><span class="line">--allowedTools &quot;tool1,tool2&quot;      限制可用工具                      安全敏感场景，限制 AI 能力范围</span><br><span class="line">--dangerously-skip-permissions    跳过所有权限确认                  CI/CD 环境（无人值守）</span><br><span class="line">--max-budget-usd &lt;amount&gt;         限制最大 API 调用成本             防止成本失控</span><br><span class="line">--continue                        继续最近的会话                    多轮复杂任务（最近一次）</span><br><span class="line">--resume SESSION_ID               继续指定的会话                    多轮复杂任务（指定会话）</span><br><span class="line">--append-system-prompt &quot;...&quot;      追加系统提示词                    保留默认行为 + 自定义指令</span><br><span class="line">--system-prompt &quot;...&quot;             完全替换系统提示词                完全自定义 AI 行为</span><br><span class="line">--model MODEL_ID                  指定模型                          不同任务用不同模型</span><br></pre></td></tr></table></figure>
<p>组合示例</p>
<blockquote>
<p>通俗解释：下面展示了几种常见的参数搭配——就像做菜时的”调料组合”，不同场景用不同搭配。每个示例都标注了适用场景。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># CI/CD 代码审查：JSON 输出 + 跳过权限 + 成本限制</span></span><br><span class="line">git diff HEAD~1 | claude -p <span class="string">&quot;审查代码变更&quot;</span> \</span><br><span class="line">  --output-format json \</span><br><span class="line">  --dangerously-skip-permissions \</span><br><span class="line">  --max-budget-usd 0.5</span><br><span class="line"></span><br><span class="line"><span class="comment"># 结构化数据提取：用 JSON Schema 强制输出格式</span></span><br><span class="line">claude -p <span class="string">&quot;提取 auth.py 中的所有函数名&quot;</span> \</span><br><span class="line">  --output-format json \</span><br><span class="line">  --json-schema <span class="string">&#x27;&#123;</span></span><br><span class="line"><span class="string">    &quot;type&quot;: &quot;object&quot;,</span></span><br><span class="line"><span class="string">    &quot;properties&quot;: &#123;</span></span><br><span class="line"><span class="string">      &quot;functions&quot;: &#123;</span></span><br><span class="line"><span class="string">        &quot;type&quot;: &quot;array&quot;,</span></span><br><span class="line"><span class="string">        &quot;items&quot;: &#123;&quot;type&quot;: &quot;string&quot;&#125;</span></span><br><span class="line"><span class="string">      &#125;</span></span><br><span class="line"><span class="string">    &#125;,</span></span><br><span class="line"><span class="string">    &quot;required&quot;: [&quot;functions&quot;]</span></span><br><span class="line"><span class="string">  &#125;&#x27;</span> | jq <span class="string">&#x27;.structured_output.functions&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 复杂诊断：继续指定会话 + 限制工具</span></span><br><span class="line">session_id=$(claude -p <span class="string">&quot;开始分析内存泄漏&quot;</span> --output-format json | jq -r <span class="string">&#x27;.session_id&#x27;</span>)</span><br><span class="line">claude -p <span class="string">&quot;继续分析刚才发现的问题&quot;</span> \</span><br><span class="line">  --resume <span class="string">&quot;<span class="variable">$session_id</span>&quot;</span> \</span><br><span class="line">  --allowedTools <span class="string">&quot;Bash,Read&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 批量处理：指定轻量模型降低成本</span></span><br><span class="line"><span class="keyword">for</span> file <span class="keyword">in</span> src/**/*.js; <span class="keyword">do</span></span><br><span class="line">  claude -p <span class="string">&quot;给这个文件添加 JSDoc 注释&quot;</span> \</span><br><span class="line">    --model claude-haiku-4-5-20251001 &lt; <span class="string">&quot;<span class="variable">$file</span>&quot;</span></span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 自定义系统提示词：追加安全审查指令</span></span><br><span class="line">gh <span class="built_in">pr</span> diff <span class="string">&quot;<span class="variable">$1</span>&quot;</span> | claude -p \</span><br><span class="line">  --append-system-prompt <span class="string">&quot;你是安全工程师，重点审查 SQL 注入、XSS、CSRF 等漏洞&quot;</span> \</span><br><span class="line">  --output-format json</span><br></pre></td></tr></table></figure>
<h3 id="stdin-管道用法"><a href="#stdin-管道用法" class="headerlink" title="stdin 管道用法"></a>stdin 管道用法</h3><p>claude -p 支持从标准输入读取内容，可以无缝融入 Unix 管道。基础用法（如 git diff | claude -p “审查这段变更”）在前面的案例中已经展示过，这里展示一个更高级的链式处理：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 链式处理：第一个 claude 提取结构化数据，第二个 claude 做分析</span></span><br><span class="line"><span class="built_in">cat</span> error.log | claude -p <span class="string">&quot;提取所有异常类名，返回JSON数组&quot;</span> \</span><br><span class="line">  --output-format json \</span><br><span class="line">  --json-schema <span class="string">&#x27;&#123;</span></span><br><span class="line"><span class="string">    &quot;type&quot;: &quot;object&quot;,</span></span><br><span class="line"><span class="string">    &quot;properties&quot;: &#123;</span></span><br><span class="line"><span class="string">      &quot;exceptions&quot;: &#123;&quot;type&quot;: &quot;array&quot;, &quot;items&quot;: &#123;&quot;type&quot;: &quot;string&quot;&#125;&#125;</span></span><br><span class="line"><span class="string">    &#125;,</span></span><br><span class="line"><span class="string">    &quot;required&quot;: [&quot;exceptions&quot;]</span></span><br><span class="line"><span class="string">  &#125;&#x27;</span> | jq -r <span class="string">&#x27;.structured_output.exceptions[]&#x27;</span> | \</span><br><span class="line">  claude -p <span class="string">&quot;对这些异常类进行分类和优先级排序&quot;</span></span><br></pre></td></tr></table></figure>
<h3 id="流式输出"><a href="#流式输出" class="headerlink" title="流式输出"></a>流式输出</h3><p>案例 1 已经展示了流式输出的实际应用。如果你需要在终端实时看到 AI 的输出，可以用 jq 过滤文本流：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">claude -p <span class="string">&quot;分析整个项目的架构&quot;</span> \</span><br><span class="line">  --output-format stream-json \</span><br><span class="line">  --verbose \</span><br><span class="line">  --include-partial-messages | \</span><br><span class="line">  jq -rj <span class="string">&#x27;select(.type == &quot;stream_event&quot; and .event.delta.type? == &quot;text_delta&quot;) | .event.delta.text&#x27;</span></span><br></pre></td></tr></table></figure>
<h3 id="allowedTools-推荐配置"><a href="#allowedTools-推荐配置" class="headerlink" title="--allowedTools 推荐配置"></a><code>--allowedTools</code> 推荐配置</h3><p>不同场景应该开放不同的工具权限，以下是常见场景的最小化配置参考：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">场景                  推荐配置                                          说明</span><br><span class="line">──────────            ──────────────────────────────────────            ──────────────────────────</span><br><span class="line">CI 代码审查           --allowedTools &quot;Read&quot;                             只读代码，不需要执行或修改</span><br><span class="line">定时巡检              --allowedTools &quot;Bash(curl *),Bash(ps *),Read&quot;     只允许 curl、ps 和读文件</span><br><span class="line">冒烟检查              --allowedTools &quot;Bash(curl *)&quot;                     只允许 curl 调用接口</span><br><span class="line">测试生成              --allowedTools &quot;Read,Write,Edit&quot;                  允许读写文件，不允许执行命令</span><br><span class="line">测试修复              --allowedTools &quot;Read,Edit&quot;                        只允许读和编辑，不允许创建新文件</span><br><span class="line">自动创建 PR           --allowedTools &quot;Bash(git *),Read,Write,Edit&quot;      允许 git 操作和文件读写</span><br></pre></td></tr></table></figure>
<p>语法要点：<code>Bash(git diff *)</code> 中 <code>*</code> 前面的空格很重要。如果写成 <code>Bash(git diff*)</code>，会匹配 <code>git diff-index</code> 这样的命令，可能不是你想要的。</p>
]]></content>
      <categories>
        <category>claude</category>
      </categories>
      <tags>
        <tag>Claude Code</tag>
        <tag>自动化</tag>
        <tag>CLI</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo+Next博客搭建</title>
    <url>/Hexo+Next%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<p>这里我们使用 GitHub Pages 来搭建 Hexo 静态博客网站，其最吸引人的莫过于完全免费使用，并且非常稳定。</p>
<h1 id="1-原理"><a href="#1-原理" class="headerlink" title="1.原理"></a>1.原理</h1><h2 id="GitHub-Pages-是什么？"><a href="#GitHub-Pages-是什么？" class="headerlink" title="GitHub Pages 是什么？"></a>GitHub Pages 是什么？</h2><p>GitHub Pages 是由 GitHub 官方提供的一种免费的静态站点托管服务，让我们可以在 GitHub 仓库里托管和发布自己的静态网站页面。</p>
<h2 id="Hexo-是什么？"><a href="#Hexo-是什么？" class="headerlink" title="Hexo 是什么？"></a>Hexo 是什么？</h2><p>Hexo 是一个快速、简洁且高效的静态博客框架，它基于 Node.js 运行，可以将我们撰写的 Markdown 文档解析渲染成静态的 HTML 网页。</p>
<h2 id="Hexo-GitHub-文章发布原理"><a href="#Hexo-GitHub-文章发布原理" class="headerlink" title="Hexo + GitHub 文章发布原理"></a>Hexo + GitHub 文章发布原理</h2><p>在本地撰写 Markdown 格式文章后，通过 Hexo 解析文档，渲染生成具有主题样式的 HTML 静态网页，再推送到 GitHub 上完成博文的发布。  </p>
<h1 id="2-环境安装"><a href="#2-环境安装" class="headerlink" title="2.环境安装"></a>2.环境安装</h1><p>Hexo 基于 <code>Node.js</code>，搭建过程中还需要使用 npm（Node.js 已带） 和 <code>git</code>，因此先搭建本地操作环境，安装 Node.js 和 Git。</p>
<ul>
<li>下载安装<a href="https://nodejs.org/zh-cn">Node.js</a></li>
<li>下载安装<a href="https://git-scm.com/downloads">Git</a></li>
</ul>
<p>检查是否安装成功：进入终端，依次输入 <code>node -v</code>、<code>npm -v</code> 和 <code>git --version</code> 并回车，出现程序版本号即可。</p>
<h1 id="3-Github配置"><a href="#3-Github配置" class="headerlink" title="3.Github配置"></a>3.Github配置</h1><h2 id="3-1连接Github"><a href="#3-1连接Github" class="headerlink" title="3.1连接Github"></a>3.1连接Github</h2><p>右键 -&gt; Git Bash Here，设置用户名和邮箱：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git config --global user.name &quot;GitHub 用户名&quot;</span><br><span class="line">git config --global user.email &quot;GitHub 邮箱&quot;</span><br></pre></td></tr></table></figure>
<h2 id="3-2创建-SSH-密匙添加到Github上"><a href="#3-2创建-SSH-密匙添加到Github上" class="headerlink" title="3.2创建 SSH 密匙添加到Github上"></a>3.2创建 SSH 密匙添加到Github上</h2><p>输入 <code>ssh-keygen -t rsa -C &quot;GitHub 邮箱&quot;</code>，然后一路回车。</p>
<p>进入 [C:\Users\用户名.ssh] 目录（要勾选显示“隐藏的项目”），用记事本打开公钥 id_rsa.pub 文件并复制里面的内容。</p>
<p>登陆 GitHub ，点击用户头像进入 Settings 页面，选择左边栏的 SSH and GPG keys，点击 New SSH key。</p>
<p>Title 随便取个名字，粘贴复制的 id_rsa.pub 内容到 Key 中，点击 Add SSH key 完成添加。</p>
<p><strong>验证连接</strong>：打开 Git Bash，输入 <code>ssh -T git@github.com</code> 出现 <code>“Are you sure……”</code>，输入 yes 回车确认。显示 <code>“Hi xxx! You&#39;ve successfully……”</code> 即连接成功。</p>
<h2 id="3-3创建-Github-Pages-仓库"><a href="#3-3创建-Github-Pages-仓库" class="headerlink" title="3.3创建 Github Pages 仓库"></a>3.3创建 Github Pages 仓库</h2><p>GitHub 主页右上角加号 -&gt; New repository：</p>
<ul>
<li>Repository name 中输入 用户名.github.io</li>
<li>勾选 “Initialize this repository with a README”</li>
<li>Description 选填</li>
</ul>
<p>填好后点击 Create repository 创建。</p>
<p>创建后默认自动启用 HTTPS，博客地址为：<code>https://用户名.github.io</code></p>
<h1 id="4-本地安装-Hexo-博客程序"><a href="#4-本地安装-Hexo-博客程序" class="headerlink" title="4.本地安装 Hexo 博客程序"></a>4.本地安装 Hexo 博客程序</h1><h2 id="4-1安装-Hexo"><a href="#4-1安装-Hexo" class="headerlink" title="4.1安装 Hexo"></a>4.1安装 Hexo</h2><p>使用使用 npm 一键安装 Hexo 博客程序：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br><span class="line"></span><br><span class="line">// linux或mac用户需要管理员权限（sudo），运行这条命令：</span><br><span class="line"></span><br><span class="line">sudo npm install -g hexo-cli</span><br></pre></td></tr></table></figure>
<h2 id="4-2-Hexo-初始化和本地预览"><a href="#4-2-Hexo-初始化和本地预览" class="headerlink" title="4.2 Hexo 初始化和本地预览"></a>4.2 Hexo 初始化和本地预览</h2><p>初始化并安装所需组件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo init      # 初始化</span><br><span class="line">npm install    # 安装组件</span><br></pre></td></tr></table></figure>
<p>完成后依次输入下面命令，启动本地服务器进行预览：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo g   # 生成页面</span><br><span class="line">hexo s   # 启动预览</span><br></pre></td></tr></table></figure>
<p>访问 <a href="http://localhost:4000">http://localhost:4000</a> 出现 Hexo 默认页面，本地博客安装成功！</p>
<p>Hexo 博客文件夹目录结构如下：<br><img src="/images/hexo目录结构.jpg" alt=""></p>
<h1 id="5-部署-Hexo-到-GitHub-Pages"><a href="#5-部署-Hexo-到-GitHub-Pages" class="headerlink" title="5. 部署 Hexo 到 GitHub Pages"></a>5. 部署 Hexo 到 GitHub Pages</h1><p>首先安装 hexo-deployer-git：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>
<p>然后修改 _config.yml 文件末尾的 Deployment 部分，修改成如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  type: git</span><br><span class="line">  repository: git@github.com:用户名/用户名.github.io.git</span><br><span class="line">  branch: master</span><br></pre></td></tr></table></figure>
<p>完成后运行 </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo d #部署到GitHub Pages</span><br></pre></td></tr></table></figure>
<p>将网站上传部署到 GitHub Pages。这时访问我们的 GitHub 域名 <code>https://用户名.github.io</code> 就可以看到 Hexo 网站了。</p>
<h1 id="6-安装Next主题"><a href="#6-安装Next主题" class="headerlink" title="6.安装Next主题"></a>6.安装Next主题</h1><p>进入网站目录打开 Git Bash Here 下载主题：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git clone https://github.com/iissnan/hexo-theme-next themes/next</span><br></pre></td></tr></table></figure>
<p>然后修改 hexo的 <code>_config.yml</code> 中的 <code>theme</code> 为新主题名称 <code>next</code>。</p>
<h1 id="7-遇到的问题-lt-color-gt"><a href="#7-遇到的问题-lt-color-gt" class="headerlink" title="7.遇到的问题&lt;/color&gt;"></a>7.<font color=red>遇到的问题&lt;/color&gt;</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ hexo d -g</span><br><span class="line">(node:3908) Warning: Accessing non-existent property &#x27;lineno&#x27; of module exports inside circular dependency</span><br><span class="line">(Use `node --trace-warnings ...` to show where the warning was created)</span><br><span class="line">(node:3908) Warning: Accessing non-existent property &#x27;column&#x27; of module exports inside circular dependency</span><br><span class="line">(node:3908) Warning: Accessing non-existent property &#x27;filename&#x27; of module exports inside circular dependency</span><br><span class="line">(node:3908) Warning: Accessing non-existent property &#x27;lineno&#x27; of module exports inside circular dependency</span><br><span class="line">(node:3908) Warning: Accessing non-existent property &#x27;column&#x27; of module exports inside circular dependency</span><br><span class="line">(node:3908) Warning: Accessing non-existent property &#x27;filename&#x27; of module exports inside circular dependency</span><br><span class="line">INFO  Start processing</span><br><span class="line">INFO  Files loaded in 94 ms</span><br><span class="line">INFO  0 files generated in 22 ms</span><br><span class="line">INFO  Deploying: git</span><br><span class="line">INFO  Clearing .deploy_git folder...</span><br><span class="line">INFO  Copying files from public folder...</span><br><span class="line">FATAL Something&#x27;s wrong. Maybe you can find the solution here: https://hexo.io/docs/troubleshooting.html</span><br><span class="line">TypeError [ERR_INVALID_ARG_TYPE]: The &quot;mode&quot; argument must be integer. Received an instance of Object</span><br><span class="line">    at copyFile (fs.js:1890:10)</span><br><span class="line">    at tryCatcher (D:\Blog\node_modules\bluebird\js\release\util.js:16:23)</span><br><span class="line">    at ret (eval at makeNodePromisifiedEval (D:\Node\node_global\node_modules\hexo-cli\node_modules\bluebird\js\release\promisify.js:184:12), &lt;anonymous&gt;:13:39)</span><br><span class="line">    at D:\Blog\node_modules\hexo-fs\lib\fs.js:144:39</span><br><span class="line">    at tryCatcher (D:\Blog\node_modules\bluebird\js\release\util.js:16:23)</span><br><span class="line">    at Promise._settlePromiseFromHandler (D:\Blog\node_modules\bluebird\js\release\promise.js:547:31)</span><br><span class="line">    at Promise._settlePromise (D:\Blog\node_modules\bluebird\js\release\promise.js:604:18)</span><br><span class="line">    at Promise._settlePromise0 (D:\Blog\node_modules\bluebird\js\release\promise.js:649:10)</span><br><span class="line">    at Promise._settlePromises (D:\Blog\node_modules\bluebird\js\release\promise.js:729:18)</span><br><span class="line">    at Promise._fulfill (D:\Blog\node_modules\bluebird\js\release\promise.js:673:18)</span><br><span class="line">    at Promise._resolveCallback (D:\Blog\node_modules\bluebird\js\release\promise.js:466:57)</span><br><span class="line">    at Promise._settlePromiseFromHandler (D:\Blog\node_modules\bluebird\js\release\promise.js:559:17)</span><br><span class="line">    at Promise._settlePromise (D:\Blog\node_modules\bluebird\js\release\promise.js:604:18)</span><br><span class="line">    at Promise._settlePromise0 (D:\Blog\node_modules\bluebird\js\release\promise.js:649:10)</span><br><span class="line">    at Promise._settlePromises (D:\Blog\node_modules\bluebird\js\release\promise.js:729:18)</span><br><span class="line">    at Promise._fulfill (D:\Blog\node_modules\bluebird\js\release\promise.js:673:18)</span><br><span class="line">    at Promise._resolveCallback (D:\Blog\node_modules\bluebird\js\release\promise.js:466:57)</span><br><span class="line">    at Promise._settlePromiseFromHandler (D:\Blog\node_modules\bluebird\js\release\promise.js:559:17)</span><br><span class="line">    at Promise._settlePromise (D:\Blog\node_modules\bluebird\js\release\promise.js:604:18)</span><br><span class="line">    at Promise._settlePromise0 (D:\Blog\node_modules\bluebird\js\release\promise.js:649:10)</span><br><span class="line">    at Promise._settlePromises (D:\Blog\node_modules\bluebird\js\release\promise.js:729:18)</span><br><span class="line">    at Promise._fulfill (D:\Blog\node_modules\bluebird\js\release\promise.js:673:18)</span><br></pre></td></tr></table></figure>
<p>出现这些是因为node版本太高，切换成低版本的node来安装Hexo就可以了。</p>
<p>我安装的是 <code>hexo: 4.2.0</code>  <code>node: 13.6.0</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ hexo -v</span><br><span class="line">hexo: 4.2.0</span><br><span class="line">hexo-cli: 4.2.0</span><br><span class="line">os: Windows_NT 10.0.18363 win32 x64</span><br><span class="line">node: 13.6.0</span><br><span class="line">v8: 7.9.317.25-node.26</span><br><span class="line">uv: 1.34.0</span><br><span class="line">zlib: 1.2.11</span><br><span class="line">brotli: 1.0.7</span><br><span class="line">ares: 1.15.0</span><br><span class="line">modules: 79</span><br><span class="line">nghttp2: 1.40.0</span><br><span class="line">napi: 5</span><br><span class="line">llhttp: 2.0.1</span><br><span class="line">openssl: 1.1.1d</span><br><span class="line">cldr: 36.0</span><br><span class="line">icu: 65.1</span><br><span class="line">tz: 2019c</span><br><span class="line">unicode: 12.1</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>博客搭建</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>Next</tag>
      </tags>
  </entry>
</search>
